console.error(
  "Local publication is disabled. Follow docs/release-process.md: reviewed release plan, successful main CI, manual SHA-confirmed dispatch and environment approval.",
);
process.exitCode = 1;
