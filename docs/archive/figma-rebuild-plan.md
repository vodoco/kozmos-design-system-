# Kozmos Figma Rebuild Plan

Last checked: 2026-06-21

## Current Checkpoint

The original rebuild plan below is now mostly historical. The active state is:

- `Kozmos DS - Core Library` remains the canonical Figma target.
- `STATUS.md` reports Core at 68/68 across Web, Web tests, linked Web Code
  Connect, iOS, linked iOS Code Connect, Android, and linked Android Code
  Connect.
- Current `.figma.*` mappings contain no `node-id=TBD` placeholders.
- `pnpm figma:parse:linked`, `pnpm figma:parse:native:linked`,
  `pnpm components:contract:check`, and `pnpm tokens:contrast:check` are the
  local release gates before publishing linked Code Connect lanes.
- Use this file for rebuild history and process shape. Use `STATUS.md`,
  `docs/figma-library-manifest.json`, and the Figma plugin `Audit Library`
  output for current go/no-go status.

## Current Figma Access

- Local `.env` contains `FIGMA_FILE_KEY` and `FIGMA_ACCESS_TOKEN`.
- `FIGMA_FILE_KEY` points at the new `Kozmos DS - Core Library` file.
- API access to `Kozmos DS - Core Library` is valid.
- Current role on the file is `owner`.
- Figma Variables REST access is blocked because the current token still does not include `file_variables:read`.
- Retest after token rotation on 2026-05-17: identity, file read, components, and styles work; `/v1/files/:key/variables/local` returns 403 and explicitly requires `file_variables:read`.
- Local `pnpm --filter @kozmos/react figma:publish:dry` loads `.env` through `scripts/figma-connect-publish.mjs` and blocks publishing while placeholder `node-id=TBD` mappings remain.
- New target library file: `Kozmos DS - Core Library` in the `Published Libraries` project.
- New target file key: `Yj4O8p6Y9h2Sa9zJVoAiVY`.
- Foundation import succeeded through the local Figma plugin on 2026-05-17.
- Current verified file structure: `Cover`, `Getting Started`, `Foundations`, `Components`, `Utilities`, `Archive / Legacy Reference`.
- Current verified component/style counts through REST: 0 local components, 0 local styles.

## Existing Figma File Audit

`Kozmos Design System 2.0` is useful as visual reference, but should not be treated as the canonical design system source.

- Last modified: 2026-03-26
- Top-level pages: 4
- Local components: 317
- Local component sets: 3
- Local styles: 7
- Component sets found:
  - `button`
  - `counter`
  - `betaTag`

The file is heavily centered on button-era assets. Its Button variant model uses axes such as `Type`, `Emotion`, `State`, and `Disabled`, while the current React Button API uses code variants such as `variant`, `size`, `disabled`, and `isLoading`. That mismatch makes direct connection risky.

## Source Of Truth

Use code as the source of truth for the rebuild.

- Tokens: `packages/tokens/src/tokens-light.json` and `packages/tokens/src/tokens-dark.json`
- React components: `packages/react/src/components`
- Existing React Code Connect templates: `packages/react/src/components/**/*.figma.tsx`
- Native components: `packages/ios/Sources/Components` and `packages/android/src/main/java/com/kozmos/components`
- Canonical build manifest: `docs/figma-library-manifest.json`
- Canonical Figma foundations payload: `docs/figma-foundations-payload.json`

## Current Coverage

Generated by `scripts/skills/check-completion.ts`.

- Web components: 79/79
- Web stories: 69/79
- Web tests: 64/79
- Web Code Connect files: 25/79
- Web Code Connect scaffolds: 24/79
- Web Code Connect linked: 1/79
- iOS components: 69/79
- iOS Code Connect files: 63/79
- iOS Code Connect scaffolds: 63/79
- iOS Code Connect linked: 0/79
- Android components: 79/79
- Android Code Connect files: 63/79
- Android Code Connect scaffolds: 63/79
- Android Code Connect linked: 0/79

`Code Connect files` are scaffolds until they use real Figma node IDs and pass `figma:publish:dry`. `Code Connect linked` means the mapping uses a real node ID and has no placeholder markers such as `node-id=TBD`.

## Recommended Build Order

### Phase 1: Foundations

Create or update Figma variables before components.

- Primitive color variables
- Semantic color variables with light/dark modes
- Spacing variables
- Radius variables
- Typography variables/styles
- Shadow/effect styles
- Code syntax bindings for Web, iOS, and Android

If using Figma REST variable sync, the token needs `file_variables:read` and likely `file_variables:write`. The current token does not include `file_variables:read`.

The GitHub Figma variable sync workflow is therefore gated behind the repository variable `FIGMA_VARIABLES_API_ENABLED=true`. Keep it disabled unless the account/token can access the Figma Variables REST API. Until then, use the repository token JSON files as the source of truth and create/update Figma variables through the Figma plugin/MCP flow.

`pnpm figma:foundations` generates the import payload for the plugin/MCP flow:

- Total tokens: 601
- Figma variable candidates: 563
- Style/documentation-only tokens: 38
- Collections: `Kozmos Primitives`, `Kozmos Semantics`, `Kozmos Components`, `Kozmos Effects`
- Modes: `Light`, `Dark`

Use the local development plugin at `figma/foundations-importer/manifest.json` to import the payload into the open Figma file. This bypasses the missing Variables REST scope by using Figma's in-file Plugin API.

The same plugin now includes controlled Core component builders/updaters. Current canonical component set names are unsuffixed, such as `Button`, and Update should be preferred because it preserves the stable node ID needed for Code Connect.

### Phase 2: File Structure

Create a clean library structure:

- `Cover`
- `Getting Started`
- `Foundations`
- `Components`
- `Utilities`

Do not create new Archive / Legacy Reference content during rebuilds. If old assets are useful for comparison, keep them outside the canonical library surface or as a temporary manual reference, not as generated rebuild output.

### Phase 3: Current Core Component Batch

The current Core Figma library uses 37 canonical component-set mappings after SegmentedControl was linked. Text, Heading, and Label are maintained as typography styles/tokens rather than component sets.

- Accordion
- Alert
- Avatar
- Badge
- Box
- Breadcrumb
- Button
- Card
- Checkbox
- Chip
- Container
- Counter
- Dialog
- Drawer
- IconButton
- Input
- Link
- List
- Menu
- Pagination
- Popover
- Progress
- Radio
- Search
- Select
- SegmentedControl
- Separator
- Skeleton
- Slider
- Spinner
- Stack
- Switch
- Table
- Tabs
- Textarea
- Toast
- Tooltip

For each component:

1. Build the Figma component/component set from the code API.
2. Bind fills, strokes, text, radius, spacing, and shadows to variables.
3. Add Figma component properties that match code props.
4. Review the matching `.figma.tsx` scaffold against the actual component props.
5. Capture the real Figma node ID.
6. Replace `node-id=TBD` in the matching `.figma.tsx`.
7. Add SwiftUI and Compose `.figma` mappings when the native component API exists.
8. Run `pnpm figma:publish:linked:dry` and `pnpm figma:publish:native:linked:dry`.

Some mappings target subcomponents or variant-specific examples rather than the directory's default component, such as `DialogContent`, `MenuContent`, `SelectTrigger`, and `RadioGroupItem`. Multiple Code Connect chips for one Figma component are expected when those chips represent distinct framework/variant snippets.

Button first-pass notes:

- React `Button` exposes `variant`, `size`, `disabled`, and `isLoading`.
- Storybook now includes the `glass` variant.
- Code Connect maps Figma `State` to `disabled` and `isLoading`, avoiding separate boolean properties that would not drive visual state.
- All Core React, SwiftUI, and Compose linked Code Connect mappings now point at the new `Kozmos DS - Core Library` file key.
- `Button` is audited in Figma as component set node `77:1055` (`node-id=77-1055`) with 84 variants and is linked in React, SwiftUI, and Compose Code Connect.
- Designer change governance lives in `docs/figma-change-workflow.md`.
- Icon strategy lives in `docs/archive/figma-icon-strategy.md`; use the existing Simple Icons library as the artwork source and expose icon slots from Kozmos components.

### Phase 4: Remaining Web Coverage

Root `pnpm figma:publish:dry` is still blocked by non-core scaffold mappings such as Grid, SplitButton, ToggleButton, and FloatingActionButton. Treat those as future library expansion work rather than Core release blockers.

### Phase 5: Native Code Connect

The 37 Core iOS and Android `.figma.*` mappings are now linked. Future native work should focus on API parity where Figma/Web expose props that native components do not yet expose, such as Slider label/status behavior.

## CI Visual Regression Strategy

Required CI starts Storybook once and runs React interaction tests plus Axe governance checks. Chromatic remains the required visual regression gate.

`pnpm test:visual` remains available for manual screenshot-baseline repair, but it is no longer part of required CI until the stale and missing baselines are refreshed.

## Local Validation Commands

```bash
pnpm figma:manifest
pnpm --filter @kozmos/tokens build
pnpm tsx scripts/skills/check-completion.ts --check
pnpm figma:plugin:check
pnpm components:contract:check
pnpm figma:publish:linked:dry
pnpm figma:publish:native:linked:dry
pnpm build
pnpm test
```

Root `figma:publish:dry` is expected to fail fast until non-Core placeholders receive real Figma node IDs or are excluded from the broad publish lane.
