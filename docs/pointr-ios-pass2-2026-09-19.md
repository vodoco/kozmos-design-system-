# Pointr iOS — Pass 2: a place's live data on the card, 19 September 2026

Claude Code, continuing [Astra's handoff](claude-code-handoff-2026-09-19.md) after
[Pass 1](pointr-ios-pass1-2026-09-19.md). Handoff item E and §10's Pass 2. Branch
`claude/pointr-browse-repairs`, still unpushed, in `/private/tmp/kozmos-browser-compat.uqPMBD`.
No Cloud content was changed.

| Commit    | What                                                                             |
| --------- | -------------------------------------------------------------------------------- |
| `43d6793` | The pinned taxonomy 10.12.0 projection generated for iOS from the web examples'  |
| `ac09652` | The web taxonomy adapter ported natively, rule for rule, with its tests          |
| `a8de911` | The adapter: taxonomy attributes, rating and price, contacts, hours, description |

## What Design-QA's places actually carry

Measured with a temporary probe over every place PointrKit returns for the site — 1,196 across
12 buildings — before any mapping was designed. The probe was deleted; the numbers are the
evidence.

| Field                                    | On Design-QA                                                                                |
| ---------------------------------------- | ------------------------------------------------------------------------------------------- |
| `rating`, `ratingMax`, `numberOfRatings` | `-999999` on every place: PointrKit's sentinel for absent                                   |
| `priceSign`, `priceRange`, `priceMax`    | `SAR / -999999 / 4` on every place: absent, on a scale of four                              |
| `openingHours`                           | Seven `PTRPoiDaySchedule` on every place, **zero time slots** on all of them                |
| `buttons`                                | 269 places: `Website` (href, 250; one with no scheme) and `Call` (tel, 203)                 |
| `longDescription`                        | 601 places, 4 to 1,453 characters, median 109; some only the name again ("Alamo ")          |
| `imageUrls`, `logoUrl`                   | 362 places with images; logo through the SDK on none (the CMS holds them under custom keys) |
| `keywords`                               | 50 places, pipe-joined search terms ("Bathroom\|Washroom\|Toilet")                          |
| `tags`                                   | 1 place                                                                                     |
| `extraData`                              | Empty on every place                                                                        |

Taxonomy properties travel in `PTRFeature.attributes`, beside the venue's own CMS keys:

| Taxonomy property        | Places | Values seen                                                              |
| ------------------------ | ------ | ------------------------------------------------------------------------ |
| `serviceTypes` (array)   | 129    | 11 distinct, e.g. Shuttle and Transportation Assistance, Banking…        |
| `productTypes` (array)   | 97     | 9 distinct, e.g. Art Supplies, Books and Media, Accessories              |
| `accessRestrictions`     | 14     | `["membership-required"]` — an array, as the dictionary says             |
| `genderDesignation`      | 7      | `female` 4, `male` 3                                                     |
| `hasAssistance`          | 1      | `true` — an `NSNumber` boolean, indistinguishable from `1` by Swift type |
| `isWheelchairAccessible` | 1      | `true`                                                                   |

And 40-odd CMS keys of the venue's own: `Hours of Operation / Schedule` (224, escaped HTML) and
`hours of operation text` (162, plain), `Phone Number` (211) and `Website URL` (259) — the same
addresses the buttons carry — `Payment Options` (48, escaped HTML prose, not the taxonomy's
`paymentMethods`), `unitNumber` (281; the taxonomy's key is `unitId`), `Apple Primary Category`
(274) and `Google Places Type` (149), `mapPersonas` (all), `Temporary`, `Do Not Route`, storefront
photo and logo addresses, and more.

## What the card shows now

`SDKPOIAdapter.details` maps a place into `KozmosPOIDetailsPresentation` on plain values, so every
rule has a test that needs no SDK object.

- **Taxonomy attributes** go through `TaxonomyPresenter`, a rule-for-rule port of the web's
  `POITaxonomy.fixtures.ts` over the same pinned 10.12.0 projection (`TaxonomyProjection.swift`,
  generated by `scripts/sync-ios-taxonomy.mjs`; CI checks it is fresh). Labels and value labels
  from the dictionary; value icons for enum and array values, the property icon for booleans and
  integers; `order` for chips, `highlight` for the summary row; `false` only when the dictionary
  names it; zero as a value; the crowd level carrying the wait; the product's highlight-only set;
  dictionary colours onto the card's tones and nothing invented; three highlights after sorting.
  The SDK's typed fields replace their raw twins in the attributes. The web's twelve edge cases
  pass natively.
- **Rating and price** come from the SDK's numbers, where the taxonomy only says "object": absent
  on the sentinel, off the scale, or on a scale other than the card's four (reported).
- **Contacts.** A button becomes an action only with an address the host can open — a web address
  with a scheme and a host, a phone number with digits, a mailbox with a host — and only the host
  opens it. A simulator cannot call, and the card is told ("Calling isn't available on this
  device.") rather than nothing happening. Unopenable or custom buttons are diagnostics.
- **Opening hours** are the venue's text, plain over escaped HTML, never an open-or-closed claim;
  the disclosure's note says so. A structured schedule with slots would be reported, not rendered:
  PointrKit 10.3.0's reference says only "Keys are days of the week" and never which day comes
  first, and Design-QA has no slots to measure it from.
- **Description** as plain text, dropped when it is only the name again. **Tags** as before.
- **Diagnostics** go to the unified log, subsystem `com.kozmos.pointrqa`, category `poi`, one line
  per issue, place content only.

Availability stays `nil` — no status is inferred — and route estimates wait for Pass 3.

## Verified

Live, iPhone 17 Pro, Design-QA:

- **Dunkin'** (Terminal B): Website and Call actions, "Open 24 hours" as a disclosure with the
  note, the description, the gallery. Call on the simulator shows the unavailable message.
- **Boston AMERICA!** (Terminal B): a **Product Types → Accessories** chip from the dictionary,
  Website and Call, "5a - 30 mins prior to last departure", the description.
- **British Airways Lounge** (Terminal E): "6a - 1p & 2:30p - 11p", a long description with Read
  more, its diagnostics line naming only CMS keys.
- Panning the map from Terminal B switched the building to Terminal E, 194 places — item G's
  mechanism, observed.

Tests: 31 in the QA app (12 presenter, 19 integration), all passing; `tokens:raw:check`, the
projection freshness check, prettier and eslint on the script, the workflow YAML.

**Not reached live.** The summary row: no place in the buildings the map visited carries a
highlight property. Terminal A's four "Smoking Area" places carry `accessRestrictions`
(`membership-required` → a brand-toned "Access Restrictions" highlight and a "Membership
Required" chip), and one place somewhere carries `isWheelchairAccessible`; the mapping is
unit-tested against exactly those shapes. `genderDesignation` chips likewise. A real `tel:` or
`https:` open on a device. A rating or a price: none exist on Design-QA.

## Upstream findings, for §9's list

1. **Hours live in CMS text, not the taxonomy object.** Every structured schedule is empty; the
   venue types hours into `hours of operation text` and an HTML twin. The adapter reads those two
   keys — venue-specific, named in one constant.
2. **PointrKit flattens the day names away.** `openingHours` is `[PTRPoiDaySchedule]` with no day
   field; the reference documents no order. Until Pointr documents it, structured hours cannot be
   rendered honestly.
3. **`Payment Options` is prose, not `paymentMethods`.** "Cash or credit card (Mastercard, Visa…)
   are accepted" as escaped HTML. It is not mapped; the taxonomy value list would give icons.
4. **`unitNumber` is not `unitId`**, and the category strings (`Apple Primary Category`, `Google
Places Type`) are not the taxonomy's `mainType/subType`. The card shows no category label yet:
   a display name for a type needs a projection of the taxonomy's types, which the web examples do
   not carry either.
5. **Keywords are search terms**, not display tags, and the dictionary agrees (segment "Search",
   no order). They belong to item F.
6. **A description that repeats the name** ("Alamo ", "Avis") is content, dropped on the card.

## Not done

Category labels (item 4 above). Keywords in search (F). Routing and route estimates (Pass 3).
The web product's own button mapping — the web examples take supplementary actions from fixtures;
a web host on live PointrKit data needs this adapter's rules too. Android: none of this.

## Change it yourself

| Behaviour                                         | File                                                                    |
| ------------------------------------------------- | ----------------------------------------------------------------------- |
| Which attributes reach the presenter; hours keys  | `apps/PointrPlayground/Sources/App/SDKPOIAdapter.swift`                 |
| Rating/price rules, contact validation, scrubbing | same file: `ratingHighlight`, `contact(for:)`, `plainText`              |
| The web's presentation rules                      | `apps/PointrPlayground/Sources/App/Taxonomy/TaxonomyPresenter.swift`    |
| The dictionary's shape                            | `apps/PointrPlayground/Sources/App/Taxonomy/TaxonomyDictionary.swift`   |
| The pinned projection (generated, do not edit)    | `apps/PointrPlayground/Sources/App/Model/TaxonomyProjection.swift`      |
| Opening a contact; the unavailable messages       | `SDKSession.swift`: `perform(action:poiId:)`                            |
| Tests                                             | `Tests/TaxonomyPresenterTests.swift`, `Tests/SDKIntegrationTests.swift` |

```sh
node scripts/sync-ios-taxonomy.mjs --write     # after changing the web projection JSON
node scripts/sync-ios-taxonomy.mjs             # the freshness check CI runs
# A selected place's diagnostics, from the simulator's unified log:
xcrun simctl spawn 51937B59-CEAE-4BC7-BC34-FEB17E28FAE7 log show \
  --predicate 'subsystem == "com.kozmos.pointrqa"' --last 5m --style compact
```

To render structured hours once Pointr documents the day order: `openingHours(structuredSlots:…)`
is where the slots are counted; build rows there, with weekday names from `Calendar`, and delete
the "not rendered" issue.
