import SwiftUI

public struct KozmosNavigationAnnouncer: View {
    public init() {}
    
    public var body: some View {
        Text("NavigationAnnouncer")
            .padding()
            .background(Color.gray.opacity(0.1))
            .cornerRadius(8)
    }
}
