import SwiftUI

public struct KozmosSaveLocationCard: View {
    public init() {}
    
    public var body: some View {
        Text("SaveLocationCard")
            .padding()
            .background(Color.gray.opacity(0.1))
            .cornerRadius(8)
    }
}
