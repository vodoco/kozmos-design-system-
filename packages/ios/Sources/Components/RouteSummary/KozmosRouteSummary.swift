import SwiftUI

public struct KozmosRouteSummary: View {
    public init() {}
    
    public var body: some View {
        Text("RouteSummary")
            .padding()
            .background(Color.gray.opacity(0.1))
            .cornerRadius(8)
    }
}
