import SwiftUI

public struct KozmosFieldWrapper: View {
    public init() {}
    
    public var body: some View {
        Text("FieldWrapper")
            .padding()
            .background(Color.gray.opacity(0.1))
            .cornerRadius(8)
    }
}
