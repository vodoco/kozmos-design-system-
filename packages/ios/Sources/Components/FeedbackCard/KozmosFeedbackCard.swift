import SwiftUI

public struct KozmosFeedbackCard: View {
    public init() {}
    
    public var body: some View {
        Text("FeedbackCard")
            .padding()
            .background(Color.gray.opacity(0.1))
            .cornerRadius(8)
    }
}
