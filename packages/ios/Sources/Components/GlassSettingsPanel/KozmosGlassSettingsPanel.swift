import SwiftUI

public struct KozmosGlassSettingsPanel: View {
    public init() {}
    
    public var body: some View {
        Text("GlassSettingsPanel")
            .padding()
            .background(Color.gray.opacity(0.1))
            .cornerRadius(8)
    }
}
