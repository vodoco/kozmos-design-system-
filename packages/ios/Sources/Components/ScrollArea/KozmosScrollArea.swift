import SwiftUI

public struct KozmosScrollArea: View {
    public init() {}
    
    public var body: some View {
        Text("ScrollArea")
            .padding()
            .background(Color.gray.opacity(0.1))
            .cornerRadius(8)
    }
}
