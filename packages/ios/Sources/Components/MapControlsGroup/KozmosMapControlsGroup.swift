import SwiftUI

public struct KozmosMapControlsGroup: View {
    public init() {}
    
    public var body: some View {
        Text("MapControlsGroup")
            .padding()
            .background(Color.gray.opacity(0.1))
            .cornerRadius(8)
    }
}
