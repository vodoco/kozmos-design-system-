import { Canvas, Meta, Story } from "@storybook/blocks";
import { Controls } from "../DocumentationControls";
import * as AISearchButtonStories from "./AISearchButton.stories";
import { PlatformSnippets } from "../PlatformSnippets";

<Meta of={AISearchButtonStories} />

# AISearchButton

The AI search, beside the search field: a 48 disc inside a 66 ring whose gradient runs through the theme's own ramp — from the 300 step to the 600 and back — with a 16 icon. The first gradient the system draws is made of tokens, so it follows the theme like everything else. The button shows the icon alone and is named by its label to assistive technology.

<Canvas>
  <Story of={AISearchButtonStories.BesideTheSearchField} />
</Canvas>

<Controls />

## Implementation

<div className="subheading">Code</div>

<PlatformSnippets
    react={`
import { AISearchButton } from "@kozmos/react";

export function Example() {
  return <AISearchButton label="AI search" onClick={() => {}} />;
}
    `}
    swift={`
import SwiftUI

KozmosAISearchButton(label: "AI search", action: {})
`}
    kotlin={`
KozmosAISearchButton(label = "AI search", onClick = {})
    `}
/>
