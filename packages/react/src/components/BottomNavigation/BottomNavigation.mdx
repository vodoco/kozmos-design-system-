import { Canvas, Meta, Story } from "@storybook/blocks";
import { Controls } from "../DocumentationControls";
import * as BottomNavigationStories from "./BottomNavigation.stories";
import { PlatformSnippets } from "../PlatformSnippets";

<Meta of={BottomNavigationStories} />

# BottomNavigation

Displays the BottomNavigation interface topology natively.

<Canvas>
  <Story of={BottomNavigationStories.Default} />
</Canvas>

<Controls />

## Implementation

<div className="subheading">Code</div>

<PlatformSnippets
    react={`
import { BottomNavigation, Icon } from "@kozmos/react";
export function Example() {
  return (
    <BottomNavigation
      items={[
        {
          label: "Explore",
          href: "/explore",
          icon: <Icon name="search" />,
          active: true,
        },
        { label: "Saved", href: "/saved", icon: <Icon name="bookmark" /> },
      ]}
    />
  );
}
    `}
    vue={`
<script setup>
import { KozmosBottomNavigation } from "@kozmos/vue"
</script>

<template>
  <KozmosBottomNavigation />
</template>
    `}
    swift={`
import SwiftUI

KozmosBottomNavigation(
selection: .constant(0),
items: [
(icon: "house", title: "Home"),
(icon: "map", title: "Maps"),
(icon: "bell", title: "Alerts")
]
)
`}
    kotlin={`
package com.kozmos.components.bottomnavigation

import androidx.compose.material3.Icon
import androidx.compose.material3.NavigationBar
import androidx.compose.material3.NavigationBarItem
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.graphics.vector.ImageVector
import com.kozmos.tokens.KozmosColors

data class BottomNavigationItem(
val title: String,
val icon: ImageVector,
val route: String
)

@Composable
fun KozmosBottomNavigation(
items: List<BottomNavigationItem>,
currentRoute: String,
onNavigate: (String) -> Unit,
modifier: Modifier = Modifier
) {
NavigationBar(
modifier = modifier,
containerColor = KozmosColors.primitivesColorsBackground0
) {
items.forEach { item ->
NavigationBarItem(
icon = { Icon(item.icon, contentDescription = item.title) },
label = { Text(item.title) },
selected = currentRoute == item.route,
onClick = { onNavigate(item.route) }
)
}
}
}

    `}

/>
