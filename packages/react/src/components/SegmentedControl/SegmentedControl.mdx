import { Canvas, Controls, Meta, Story } from "@storybook/blocks";
import * as SegmentedControlStories from "./SegmentedControl.stories";
import { PlatformSnippets } from "../PlatformSnippets";

<Meta of={SegmentedControlStories} />

# SegmentedControl

Use SegmentedControl when users choose one option from a short, visible set.
Prefer Radio for longer lists and Tabs when the selected item changes visible
content regions.

<Canvas>
  <Story of={SegmentedControlStories.Default} />
</Canvas>

<Controls />

## Implementation

<div className="subheading">Code</div>

<PlatformSnippets
  react={`
import { SegmentedControl } from "@kozmos/react"

<SegmentedControl
  defaultValue="overview"
  items={[
    { value: "overview", label: "Overview" },
    { value: "details", label: "Details" },
    { value: "activity", label: "Activity" },
  ]}
/>
  `}
  vue={`
<script setup>
import { KozmosSegmentedControl } from "@kozmos/vue"
</script>

<template>
  <KozmosSegmentedControl />
</template>
  `}
  swift={`
@State private var selection = 0

KozmosSegmentedControl(
selection: $selection,
items: ["Overview", "Details", "Activity"]
)
`}
  kotlin={`
var selectedIndex by remember { mutableIntStateOf(0) }

KozmosSegmentedControl(
options = listOf("Overview", "Details", "Activity"),
selectedIndex = selectedIndex,
onOptionSelected = { selectedIndex = it }
)
`}
/>
