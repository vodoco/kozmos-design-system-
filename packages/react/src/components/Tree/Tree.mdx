import { Canvas, Meta, Story } from "@storybook/blocks";
import { Controls } from "../DocumentationControls";
import * as TreeStories from "./Tree.stories";
import { PlatformSnippets } from "../PlatformSnippets";

<Meta of={TreeStories} />

# Tree

Tree presents hierarchical data with disclosure, selection, keyboard navigation,
optional counts, metadata, and safe nested row actions. Use `TreeParentItemRow`
and `TreeChildItemRow` when product screens need to compose custom trees from
reusable parent and terminal row primitives.

`Tree` supports both data-driven rendering and a composed shell mode. Use `data`
when the tree owns expansion, roving focus, and selection. Use children when a
screen needs to hand-compose parent and child row primitives inside a semantic
tree container.

<Canvas>
  <Story of={TreeStories.Default} />
</Canvas>

<Canvas>
  <Story of={TreeStories.WithRowActions} />
</Canvas>

<Canvas>
  <Story of={TreeStories.RowPrimitive} />
</Canvas>

<Controls />

## Implementation

<div className="subheading">Code</div>

<PlatformSnippets
    react={`
import { Tree } from "@kozmos/react"

<Tree
  ariaLabel="Map content"
  data={[
    {
      id: "content",
      name: "Map content",
      count: 23,
      actions: <button type="button">Lock</button>,
      children: [{ id: "item", name: "Place item" }],
    },
  ]}
  activationMode="select"
/>
    `}
    vue={`
<script setup>
import { KozmosTree } from "@kozmos/vue"
</script>

<template>
  <KozmosTree />
</template>
    `}
    swift={`
import SwiftUI

KozmosTree(
nodes: [
Node(
value: "Map content",
children: [
Node(value: "Places"),
Node(value: "Amenities"),
]
),
],
density: .default
)
`}
    kotlin={`
package com.kozmos.components.tree

import androidx.compose.runtime.Composable

@Composable
fun TreeExample() {
KozmosTree(
data = listOf(
TreeNode(
id = "map-content",
name = "Map content",
children = listOf(
TreeNode(id = "places", name = "Places"),
TreeNode(id = "amenities", name = "Amenities")
)
)
),
density = KozmosTreeDensity.Default
)
}
`}
/>
