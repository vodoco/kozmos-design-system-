import { Canvas, Meta, Source, Story } from "@storybook/blocks";
import { Controls } from "../DocumentationControls";
import * as DrawerStories from "./Drawer.stories";
import { PlatformSnippets } from "../PlatformSnippets";

<Meta of={DrawerStories} />

# Drawer

Drawer presents contextual content from an edge of the viewport without
committing to a dashboard, map, or CMS-specific layout.

<Canvas>
  <Story of={DrawerStories.Default} />
</Canvas>

<Controls />

## Implementation

<Source of={DrawerStories.Default} />

<PlatformSnippets
  react={`
import { useState } from "react";
import {
  Button,
  Drawer,
  DrawerContent,
  DrawerDescription,
  DrawerFooter,
  DrawerHeader,
  DrawerTitle,
  DrawerTrigger,
  Input,
} from "@kozmos/react";
export function Example({ onSave }: { onSave: (name: string) => void }) {
  const [open, setOpen] = useState(false);
  const [name, setName] = useState("Ground floor");
  return (
    <Drawer open={open} onOpenChange={setOpen}>
      <DrawerTrigger asChild>
        <Button>Manage layer</Button>
      </DrawerTrigger>
      <DrawerContent side="right">
        <DrawerHeader>
          <DrawerTitle>Manage layer</DrawerTitle>
          <DrawerDescription>Update the layer name.</DrawerDescription>
        </DrawerHeader>
        <Input
          label="Layer name"
          value={name}
          onChange={(event) => setName(event.target.value)}
        />
        <DrawerFooter>
          <Button variant="outline" onClick={() => setOpen(false)}>
            Cancel
          </Button>
          <Button
            onClick={() => {
              onSave(name);
              setOpen(false);
            }}
          >
            Save changes
          </Button>
        </DrawerFooter>
      </DrawerContent>
    </Drawer>
  );
}
  `}
  vue={`
<script setup>
import {
  Drawer,
  DrawerContent,
  DrawerDescription,
  DrawerFooter,
  DrawerHeader,
  DrawerTitle,
  DrawerTrigger,
} from "@kozmos/vue"
</script>

<template>
  <Drawer>
    <DrawerTrigger>Open drawer</DrawerTrigger>
    <DrawerContent side="right">
      <DrawerHeader>
        <DrawerTitle>Manage layers</DrawerTitle>
        <DrawerDescription>Update map display settings.</DrawerDescription>
      </DrawerHeader>
      <p>Drawer body content</p>
      <DrawerFooter>
        <KozmosButton variant="outline">Cancel</KozmosButton>
        <KozmosButton>Save changes</KozmosButton>
      </DrawerFooter>
    </DrawerContent>
  </Drawer>
</template>
  `}
  swift={`
KozmosDrawer(
    isPresented: $isDrawerPresented,
    side: .right,
    title: "Manage layers",
    description: "Update map display settings.",
    bodyText: "Drawer body content"
)
  `}
  kotlin={`
KozmosDrawer(
    open = isDrawerOpen,
    onDismissRequest = { isDrawerOpen = false },
    side = KozmosDrawerSide.Right,
    title = "Manage layers",
    description = "Update map display settings.",
    bodyText = "Drawer body content"
) {
    ScreenContent()
}
  `}
/>
