import { Canvas, Meta, Story } from "@storybook/blocks";
import { Controls } from "../DocumentationControls";
import * as FloatingActionButtonStories from "./FloatingActionButton.stories";
import { PlatformSnippets } from "../PlatformSnippets";

<Meta of={FloatingActionButtonStories} />

# FloatingActionButton

Displays the FloatingActionButton interface topology natively.

<Canvas>
  <Story of={FloatingActionButtonStories.Default} />
</Canvas>

<Controls />

## Implementation

<div className="subheading">Code</div>

<PlatformSnippets
    react={`
import { FloatingActionButton } from "@kozmos/react";
export function Example() {
  return (
    <>
      <FloatingActionButton aria-label="Add place" />
    </>
  );
}
    `}
    vue={`
<script setup>
import { KozmosFloatingActionButton } from "@kozmos/vue"
</script>

<template>
  <KozmosFloatingActionButton />
</template>
    `}
    swift={`
import SwiftUI

KozmosFloatingActionButton(
iconName: "plus",
action: {}
)
`}
    kotlin={`
package com.kozmos.components.floatingactionbutton

import androidx.compose.material.icons.Icons
import androidx.compose.material.icons.filled.Add
import androidx.compose.material3.FloatingActionButton
import androidx.compose.material3.Icon
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.graphics.vector.ImageVector
import com.kozmos.tokens.KozmosColors

@Composable
fun KozmosFloatingActionButton(
onClick: () -> Unit,
modifier: Modifier = Modifier,
icon: ImageVector = Icons.Default.Add
) {
FloatingActionButton(
onClick = onClick,
modifier = modifier,
containerColor = KozmosColors.primitivesColorsTheme500,
contentColor = KozmosColors.primitivesColorsBackground0
) {
Icon(icon, contentDescription = "Action")
}
}

    `}

/>
