import { Canvas, Meta, Story, Controls } from "@storybook/blocks";
import * as ProgressStories from "./Progress.stories";
import { PlatformSnippets } from "../PlatformSnippets";

<Meta of={ProgressStories} />

# Progress

Communicates progress with an accessible name. React's `value` is measured against
`max` (100 by default), and the visual fill and announced value use the same scale.
Use `null` or omit `value` when progress is indeterminate.

<Canvas>
  <Story of={ProgressStories.Default} />
</Canvas>

<Controls />

## Implementation

<div className="subheading">Code</div>

<PlatformSnippets
    react={`
import { Progress } from "@kozmos/react"

<Progress value={25} max={50} aria-label="Upload progress" />
    `}
    vue={`
<script setup>
import { KozmosProgress } from "@kozmos/vue"
</script>

<template>
  <KozmosProgress />
</template>
    `}
    swift={`
import SwiftUI

KozmosProgress(value: 0.6)
`}
    kotlin={`
package com.kozmos.components.progress

import androidx.compose.material3.LinearProgressIndicator
import androidx.compose.material3.MaterialTheme
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import com.kozmos.tokens.KozmosColors

@Composable
fun KozmosProgress(
progress: Float,
modifier: Modifier = Modifier
) {
LinearProgressIndicator(
progress = progress,
modifier = modifier,
color = KozmosColors.primitivesColorsTheme500,
trackColor = KozmosColors.primitivesColorsBackground300,
)
}

    `}

/>
