import { Canvas, Meta, Story } from "@storybook/blocks";
import { Controls } from "../DocumentationControls";
import * as MenuStories from "./Menu.stories";
import { PlatformSnippets } from "../PlatformSnippets";

<Meta of={MenuStories} />

# Menu

Displays the Menu interface topology natively.

<Canvas>
  <Story of={MenuStories.Default} />
</Canvas>

<Controls />

## Implementation

<div className="subheading">Code</div>

<PlatformSnippets
    react={`
import { useState } from "react";
import {
  Menu,
  MenuTrigger,
  MenuContent,
  MenuItem,
  Button,
} from "@kozmos-ds/react";
export function Example() {
  const [sort, setSort] = useState("Nearby");
  return (
    <>
      <Menu>
        <MenuTrigger asChild>
          <Button>Sort places</Button>
        </MenuTrigger>
        <MenuContent>
          <MenuItem onSelect={() => setSort("Nearby")}>Nearby</MenuItem>
          <MenuItem onSelect={() => setSort("Name")}>Name</MenuItem>
        </MenuContent>
      </Menu>
      <output>Sort: {sort}</output>
    </>
  );
}
    `}
    vue={`
<script setup>
import { KozmosMenu } from "@kozmos-ds/vue"
</script>

<template>
  <KozmosMenu />
</template>
    `}
    swift={`
import SwiftUI

KozmosMenu(
title: "Actions",
items: ["Edit", "Duplicate", "Delete"],
onSelect: { \_ in }
)
`}
    kotlin={`
package com.kozmos.components.menu

import androidx.compose.material3.DropdownMenu
import androidx.compose.material3.DropdownMenuItem
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

@Composable
fun KozmosMenu(
expanded: Boolean,
onDismissRequest: () -> Unit,
items: List<String>,
onItemClick: (String) -> Unit,
modifier: Modifier = Modifier
) {
DropdownMenu(
expanded = expanded,
onDismissRequest = onDismissRequest,
modifier = modifier
) {
items.forEach { item ->
DropdownMenuItem(
text = { Text(item) },
onClick = {
onItemClick(item)
onDismissRequest()
}
)
}
}
}

    `}

/>
