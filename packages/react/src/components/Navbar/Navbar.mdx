import { Canvas, Meta, Story } from "@storybook/blocks";
import { Controls } from "../DocumentationControls";
import * as NavbarStories from "./Navbar.stories";
import { PlatformSnippets } from "../PlatformSnippets";

<Meta of={NavbarStories} />

# Navbar

Horizontal app navigation shell. Compose route items with `NavigationItem` inside the `navigation` slot, and pass product context through named slots such as `logo`, `context`, `primaryAction`, `actions`, `utilities`, and `account`.

The shell wraps according to its available width, including inside a narrow pane
on a large screen. Its minimum height is 64px, not a fixed height: context, actions,
and navigation remain available rather than disappearing at a breakpoint. Allow
the host layout to grow with it. Consumer-provided navigation groups should wrap
and should not impose fixed widths larger than their container. `navigationLabel`
names the navigation landmark (default: “Main navigation”).

<Canvas>
  <Story of={NavbarStories.Default} />
</Canvas>

<Controls />

## Implementation

<div className="subheading">Code</div>

<PlatformSnippets
    react={`
import { Navbar } from "@kozmos/react";
import { NavigationItem } from "@kozmos/react";
export function Example() {
  return (
    <>
      <Navbar
        logo={<strong>Kozmos</strong>}
        navigation={
          <div className="flex items-center gap-2">
            <NavigationItem href="/overview" placement="top" selected>
              Overview
            </NavigationItem>
            <NavigationItem href="/explore" placement="top">
              Explore
            </NavigationItem>
          </div>
        }
        account={<button type="button">Account</button>}
      />
    </>
  );
}
    `}
    vue={`
<script setup>
import { KozmosNavbar } from "@kozmos/vue"
</script>

<template>
  <KozmosNavbar />
</template>
    `}
    swift={`
import SwiftUI

KozmosNavbar(
logo: {
Text("Kozmos")
},
context: {
Text("Active site")
},
navigation: {
HStack(spacing: 8) {
KozmosNavigationItem(label: "Overview", placement: .top, selected: true)
KozmosNavigationItem(label: "Explore", placement: .top)
KozmosNavigationItem(label: "Settings", placement: .top)
}
},
primaryAction: {
KozmosButton("Publish", action: {})
},
actions: {
Text("Actions")
},
utilities: {
Text("Notifications")
},
account: {
Text("Account")
}
)
`}
    kotlin={`
package com.kozmos.components.navbar

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import com.kozmos.components.button.KozmosButton
import com.kozmos.components.navigationitem.KozmosNavigationItem
import com.kozmos.components.navigationitem.KozmosNavigationItemPlacement

@Composable
fun NavbarExample() {
KozmosNavbar(
logo = {
Text("Kozmos")
},
context = {
Text("Active site")
},
navigation = {
KozmosNavigationItem(
label = "Overview",
placement = KozmosNavigationItemPlacement.Top,
selected = true
)
KozmosNavigationItem(label = "Explore", placement = KozmosNavigationItemPlacement.Top)
KozmosNavigationItem(label = "Settings", placement = KozmosNavigationItemPlacement.Top)
},
primaryAction = {
KozmosButton(onClick = {}) {
Text("Publish")
}
},
actions = {
Text("Actions")
},
utilities = {
Text("Notifications")
},
account = {
Text("Account")
}
)
}

    `}

/>
