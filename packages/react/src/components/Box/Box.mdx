import { Canvas, Meta, Story } from "@storybook/blocks";
import { Controls } from "../DocumentationControls";
import * as BoxStories from "./Box.stories";
import { PlatformSnippets } from "../PlatformSnippets";

<Meta of={BoxStories} />

# Box

Displays the Box interface topology natively.

<Canvas>
  <Story of={BoxStories.Default} />
</Canvas>

<Controls />

## Implementation

<div className="subheading">Code</div>

<PlatformSnippets
    react={`
import { Box } from "@kozmos-ds/react";
export function Example() {
  return (
    <>
      <Box>
        <p>Venue overview</p>
      </Box>
    </>
  );
}
    `}
    vue={`
<script setup>
import { KozmosBox } from "@kozmos-ds/vue"
</script>

<template>
  <KozmosBox />
</template>
    `}
    swift={`
import SwiftUI

Box {
Text("Content")
}
`}
    kotlin={`
package com.kozmos.components.box

import androidx.compose.foundation.layout.Box
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

@Composable
fun KozmosBox(
modifier: Modifier = Modifier,
content: @Composable () -> Unit
) {
Box(modifier = modifier) {
content()
}
}

    `}

/>
