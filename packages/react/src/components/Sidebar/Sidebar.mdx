import { Canvas, Meta, Story } from "@storybook/blocks";
import { Controls } from "../DocumentationControls";
import * as SidebarStories from "./Sidebar.stories";
import { PlatformSnippets } from "../PlatformSnippets";

<Meta of={SidebarStories} />

# Sidebar

Vertical navigation shell. Compose persistent destinations with `NavigationItem` inside the `navigation` slot, and place workspace, tools, or account surfaces through the named `header`, `tools`, and `footer` slots.

<Canvas>
  <Story of={SidebarStories.Default} />
</Canvas>

<Controls />

## Implementation

<div className="subheading">Code</div>

<PlatformSnippets
    react={`
import { Sidebar } from "@kozmos/react";
import { NavigationItem } from "@kozmos/react";
export function Example() {
  return (
    <>
      <Sidebar
        header={<strong>Workspace</strong>}
        navigation={
          <div className="flex flex-col gap-1">
            <NavigationItem selected>Overview</NavigationItem>
            <NavigationItem>Explore</NavigationItem>
          </div>
        }
        footer={<NavigationItem>Account</NavigationItem>}
      />
    </>
  );
}
    `}
    vue={`
<script setup>
import { KozmosSidebar } from "@kozmos/vue"
</script>

<template>
  <KozmosSidebar />
</template>
    `}
    swift={`
import SwiftUI

KozmosSidebar(
variant: .expanded,
header: {
Text("Workspace")
},
navigation: {
VStack(alignment: .leading, spacing: 8) {
KozmosNavigationItem(label: "Overview", selected: true)
KozmosNavigationItem(label: "Explore")
KozmosNavigationItem(label: "Settings")
}
},
tools: {
KozmosNavigationItem(label: "Settings")
},
footer: {
KozmosNavigationItem(label: "Account")
}
)
`}
    kotlin={`
package com.kozmos.components.sidebar

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import com.kozmos.components.navigationitem.KozmosNavigationItem

@Composable
fun SidebarExample() {
KozmosSidebar(
variant = KozmosSidebarVariant.Expanded,
header = {
Text("Workspace")
},
navigation = {
KozmosNavigationItem(label = "Overview", selected = true)
KozmosNavigationItem(label = "Explore")
KozmosNavigationItem(label = "Settings")
},
tools = {
KozmosNavigationItem(label = "Settings")
},
footer = {
KozmosNavigationItem(label = "Account")
}
)
}

    `}

/>
