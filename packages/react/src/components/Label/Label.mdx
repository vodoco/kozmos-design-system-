import { Canvas, Meta, Story, Controls } from "@storybook/blocks";
import * as LabelStories from "./Label.stories";
import { PlatformSnippets } from "../PlatformSnippets";

<Meta of={LabelStories} />

# Label

Displays the Label interface topology natively.

<Canvas>
  <Story of={LabelStories.Default} />
</Canvas>

<Controls />

## Implementation

<div className="subheading">Code</div>

<PlatformSnippets
    react={`
import { Label } from "@kozmos/react"

<Label />
    `}
    vue={`
<script setup>
import { KozmosLabel } from "@kozmos/vue"
</script>

<template>
  <KozmosLabel />
</template>
    `}
    swift={`
import SwiftUI

struct KozmosLabel: View {
    var body: some View {
        Text("Label Content placeholder")
    }
}
    `}
    kotlin={`
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

@Composable
fun KozmosLabel() {
    Text("Label Content placeholder")
}
    `}
/>
