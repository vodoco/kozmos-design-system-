import { Canvas, Meta, Story } from "@storybook/blocks";
import { Controls } from "../DocumentationControls";
import * as LabelStories from "./Label.stories";
import { PlatformSnippets } from "../PlatformSnippets";

<Meta of={LabelStories} />

# Label

Names a form control. Set `htmlFor` to the control's `id`, and provide meaningful
text. Label has no Outline variant; the old story was a mislabeled duplicate.
Use FieldWrapper when you need required marks, helper text or error messages.

<Canvas>
  <Story of={LabelStories.Default} />
</Canvas>

<Controls />

## Implementation

<div className="subheading">Code</div>

<PlatformSnippets
    react={`
import { Label, Input } from "@kozmos/react";
export function Example() {
  return (
    <>
      <>
        <Label htmlFor="email">Email</Label>
        <Input id="email" type="email" />
      </>
    </>
  );
}
    `}
    vue={`
<script setup>
import { KozmosLabel } from "@kozmos/vue"
</script>

<template>
  <KozmosLabel />
</template>
    `}
    swift={`
import SwiftUI

KozmosLabel("Email", required: true)
`}
    kotlin={`
package com.kozmos.components.label

import androidx.compose.runtime.Composable

@Composable
fun KozmosLabelExample() {
KozmosLabel(text = "Email", required = true)
}
`}
/>
