import { Meta, Story, Canvas, Source } from '@storybook/blocks';
import * as PasswordInputStories from './PasswordInput.stories';

<Meta of={PasswordInputStories} />

# PasswordInput

A text input for secrets, with a toggle that reveals what has been typed.

## Usage

Use it wherever a password is entered:

- Sign-in and registration forms
- Change-password and re-authentication flows
- Any field holding a secret the user may need to check before submitting

The reveal toggle is on by default. Turn it off with `showToggle={false}` only
where a shoulder-surfing risk outweighs the usability gain — a shared kiosk, for
instance.

Keep `showPasswordLabel` and `hidePasswordLabel` localized. They are the
toggle's accessible name, so a screen reader announces the action rather than an
unlabelled button.

## Implementation

import { PlatformSnippets } from '../PlatformSnippets';

<div className="subheading">Platform Code</div>

<PlatformSnippets
  react={`
import { useState } from "react";
import { PasswordInput } from "@kozmos/react";
export function Example() {
  const [visible, setVisible] = useState(false);
  return (
    <>
      <PasswordInput label="Password" placeholder="Enter your password" />
      <PasswordInput
        label="Password with controlled visibility"
        visible={visible}
        onVisibleChange={setVisible}
      />
      <PasswordInput
        label="New password"
        error="Must be at least 12 characters"
      />
    </>
  );
}
  `}
  vue={`
<script setup>
import { KozmosPasswordInput } from "@kozmos/vue"
import { ref } from "vue"

const password = ref("")
</script>

<template>
  <KozmosPasswordInput
    label="Password"
    placeholder="Enter your password"
    :value="password"
    @change="(event) => (password = event.target.value)"
  />
</template>
  `}
  swift={`
import SwiftUI
import Kozmos

struct SignInView: View {
    @State private var password = ""

    var body: some View {
        KozmosPasswordInput(
            text: $password,
            label: "Password",
            placeholder: "Enter your password",
            showToggle: true,
            showPasswordLabel: "Show password",
            hidePasswordLabel: "Hide password"
        )
    }
}
  `}
  kotlin={`
package com.kozmos.example

import androidx.compose.runtime.*
import com.kozmos.components.passwordinput.KozmosPasswordInput

@Composable
fun SignInScreen() {
    var password by remember { mutableStateOf("") }

    KozmosPasswordInput(
        value = password,
        onValueChange = { password = it },
        label = "Password",
        placeholder = "Enter your password",
        showToggle = true
    )
}
  `}
/>

## Accessibility

- The toggle is a button with its own accessible name, not an icon alone.
- Toggling reveals the value in place; focus and caret position are preserved so
  a keyboard user does not lose their spot.
- `error` is associated with the field, so the message is announced rather than
  conveyed by colour alone.

## Stories

### Default
<Canvas of={PasswordInputStories.Default} />

### Visible
<Canvas of={PasswordInputStories.Visible} />

### Error
<Canvas of={PasswordInputStories.Error} />

### Without Toggle
<Canvas of={PasswordInputStories.WithoutToggle} />
