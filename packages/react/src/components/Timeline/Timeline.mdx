import { Canvas, Meta, Story } from "@storybook/blocks";
import { Controls } from "../DocumentationControls";
import * as TimelineStories from "./Timeline.stories";
import { PlatformSnippets } from "../PlatformSnippets";

<Meta of={TimelineStories} />

# Timeline

Displays ordered events with composed time, title, and description primitives.

<Canvas>
  <Story of={TimelineStories.Default} />
</Canvas>

<Controls />

## Implementation

<div className="subheading">Code</div>

<PlatformSnippets
    react={`
import {
  Timeline,
  TimelineItem,
  TimelineTime,
  TimelineTitle,
  TimelineDescription,
} from "@kozmos/react"

<Timeline density="default">
  <TimelineItem>
    <TimelineTime>February 2022</TimelineTime>
    <TimelineTitle>Application UI code in Tailwind CSS</TimelineTitle>
    <TimelineDescription>
      Dashboard layouts, charts, boards, and calendar screens shipped.
    </TimelineDescription>
  </TimelineItem>
</Timeline>
    `}
    vue={`
<script setup>
import { KozmosTimeline } from "@kozmos/vue"
</script>

<template>
  <KozmosTimeline />
</template>
    `}
    swift={`
import SwiftUI

KozmosTimeline(density: .default) {
KozmosTimelineItem {
KozmosTimelineTime("February 2022")
KozmosTimelineTitle("Application UI code in Tailwind CSS")
KozmosTimelineDescription("Dashboard layouts, charts, boards, and calendar screens shipped.")
}
}
`}
    kotlin={`
package com.kozmos.components.timeline

import androidx.compose.runtime.Composable

@Composable
fun TimelineExample() {
KozmosTimeline(density = KozmosTimelineDensity.Default) {
KozmosTimelineItem {
KozmosTimelineTime("February 2022")
KozmosTimelineTitle("Application UI code in Tailwind CSS")
KozmosTimelineDescription(
"Dashboard layouts, charts, boards, and calendar screens shipped."
)
}
}
}

    `}

/>
