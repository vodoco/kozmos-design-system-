import { Canvas, Meta, Story, Controls } from "@storybook/blocks";
import * as IconStories from "./Icon.stories";
import { PlatformSnippets } from "../PlatformSnippets";

<Meta of={IconStories} />

# Icon

Displays the Icon interface topology natively.

<Canvas>
  <Story of={IconStories.Default} />
</Canvas>

<Controls />

## Implementation

<div className="subheading">Code</div>

<PlatformSnippets
    react={`
import { Icon } from "@kozmos/react"

<Icon />
    `}
    vue={`
<script setup>
import { KozmosIcon } from "@kozmos/vue"
</script>

<template>
  <KozmosIcon />
</template>
    `}
    swift={`
import SwiftUI

struct KozmosIcon: View {
    var body: some View {
        Text("Icon Content placeholder")
    }
}
    `}
    kotlin={`
package com.kozmos.components.icon

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

@Composable
fun KozmosIcon(
    name: String,
    modifier: Modifier = Modifier
) {
    // Placeholder
}

    `}
/>
