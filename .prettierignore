# Generated by `pnpm taxonomy:gen` from the published Pointr taxonomy. Formatting it here makes the
# generator's output differ from what is checked in, so every regeneration shows a whole-file diff
# and the "does the checked-in data still match the pinned release?" check stops working.
apps/mapscale-review/src/mock/taxonomyData.ts
