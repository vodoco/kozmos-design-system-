/* global figma, __html__ */
figma.showUI(__html__, {
  width: 560,
  height: 720,
  themeColors: true,
});

const RUN_NAMESPACE = "kozmos_ds_importer";
const EXAMPLE_CHILD_SIZING_DATA_KEY = "exampleChildSizing";
const DEFAULT_FONT_CONFIG = Object.freeze({
  family: "Readex Pro",
  regularStyle: "Regular",
  mediumStyle: "Medium",
  boldStyle: "Bold",
});
let activeFontConfig = {
  family: DEFAULT_FONT_CONFIG.family,
  regularStyle: DEFAULT_FONT_CONFIG.regularStyle,
  mediumStyle: DEFAULT_FONT_CONFIG.mediumStyle,
  boldStyle: DEFAULT_FONT_CONFIG.boldStyle,
};
let activeFontConfigKey = fontConfigKey(activeFontConfig);
const VALID_VARIABLE_TYPES = new Set(["COLOR", "FLOAT", "STRING", "BOOLEAN"]);
const TEXT_SIZES = [
  "XS",
  "Small",
  "Base",
  "Large",
  "XLarge",
  "2XLarge",
  "3XLarge",
  "4XLarge",
];
const TEXT_WEIGHTS = ["Normal", "Medium", "Semibold", "Bold"];
const TEXT_TONES = ["Default", "Muted", "Primary", "Destructive"];
const HEADING_LEVELS = ["H1", "H2", "H3", "H4", "H5", "H6"];
const LINK_VARIANTS = ["Default", "Subtle"];
const LINK_STATES = ["Default", "Focus"];
const LABEL_STATES = ["Default", "Disabled"];
const SEPARATOR_ORIENTATIONS = ["Horizontal", "Vertical"];
const SKELETON_SHAPES = ["Line", "Block", "Circle"];
const BOX_SURFACES = ["Transparent", "Surface", "Outlined"];
const STACK_DIRECTIONS = ["Column", "Row"];
const STACK_GAPS = ["2", "4", "6"];
const GRID_COLUMNS = ["1", "2", "3", "4"];
const GRID_GAPS = ["2", "4", "6"];
const GRID_PREVIEW_ROWS = 2;
const CONTAINER_CENTERED = ["True", "False"];
const SCROLL_AREA_ORIENTATIONS = ["Vertical", "Horizontal", "Both"];
const SCROLL_AREA_SCROLLBARS = ["Hidden", "Visible"];
const TREE_CONTENT = ["Basic", "Count", "Actions"];
const TREE_EXPANDED = ["False", "True"];
const TREE_DENSITIES = ["Default", "Compact"];
const TREE_STATES = ["Idle", "Hover", "Selected", "Focus"];
const TREE_ITEM_TYPES = ["Parent", "Child"];
const TREE_ITEM_DEPTHS = ["0", "1", "2"];
const TIMELINE_CONTENT = ["Basic", "Detailed"];
const TIMELINE_DENSITIES = ["Default", "Compact"];
const BREADCRUMB_CONTENT = ["Basic", "Ellipsis"];
const ACCORDION_STATES = ["Closed", "Open"];
const TAG_VARIANTS = ["Default", "Secondary", "Destructive", "Outline"];
const TAG_REMOVABLE = ["False", "True"];
const BUTTON_VARIANTS = [
  "Default",
  "Destructive",
  "Outline",
  "Secondary",
  "Ghost",
  "Link",
  "Glass",
];
const BUTTON_SIZES = ["Default", "Small", "Large", "Icon"];
const BUTTON_STATES = ["Default", "Disabled", "Loading"];
const BUTTON_ICON_PROPERTY = "Icon";
const BUTTON_ICON_ON_FILL_PROPERTY = "Icon on Fill";
const BUTTON_ICON_ON_ACCENT_PROPERTY = "Icon on Accent";
const BUTTON_ICON_ON_NEUTRAL_PROPERTY = "Icon on Neutral";
const ICON_BUTTON_SIZES = ["Default", "Small", "Large"];
const TOGGLE_BUTTON_VARIANTS = ["Default", "Outline"];
const TOGGLE_BUTTON_SIZES = ["Small", "Default", "Large"];
const TOGGLE_BUTTON_STATES = ["Default", "Pressed", "Disabled", "Focus"];
const SPLIT_BUTTON_VARIANTS = [
  "Default",
  "Secondary",
  "Destructive",
  "Outline",
];
const SPLIT_BUTTON_SIZES = ["Small", "Default", "Large"];
const SPLIT_BUTTON_STATES = ["Default", "Disabled", "Focus"];
const FLOATING_ACTION_BUTTON_VARIANTS = ["Default", "Secondary", "Destructive"];
const FLOATING_ACTION_BUTTON_SIZES = ["Small", "Default", "Large"];
const FLOATING_ACTION_BUTTON_STATES = ["Default", "Disabled", "Focus"];
const BADGE_VARIANTS = [
  "Default",
  "Destructive",
  "Outline",
  "Secondary",
  "Ghost",
  "Link",
];
const BADGE_SIZES = ["Default", "Small", "Large", "Icon"];
const CHIP_VARIANTS = ["Neutral", "Brand", "Destructive"];
const CHIP_SIZES = ["Small", "Default", "Large"];
const CHIP_STATES = ["Default", "Selected", "Disabled"];
const CHIP_REMOVABLE = ["False", "True"];
const SEGMENTED_CONTROL_SIZES = ["Small", "Default", "Large"];
const SEGMENTED_CONTROL_ACTIVE = ["One", "Two", "Three"];
const SEGMENTED_CONTROL_STATES = ["Default", "Focus", "Disabled", "Error"];
const COUNTER_TONES = ["Neutral", "Brand", "Destructive", "Inverse"];
const COUNTER_SIZES = ["Small", "Default"];
const CHECKBOX_CHECKED = ["Unchecked", "Checked"];
const CHECKBOX_STATES = ["Default", "Disabled", "Error"];
const RADIO_CHECKED = ["Unchecked", "Checked"];
const RADIO_STATES = ["Default", "Disabled", "Error"];
const SWITCH_CHECKED = ["Unchecked", "Checked"];
const SWITCH_STATES = ["Default", "Disabled", "Error"];
const INPUT_STATES = ["Default", "Focus", "Disabled", "Readonly"];
const INPUT_STATUSES = ["Default", "Error", "Warning", "Success"];
const PASSWORD_INPUT_VISIBILITY = ["Hidden", "Visible"];
const NUMBER_INPUT_STEPPERS = ["True", "False"];
const OTP_INPUT_LENGTHS = ["4", "6"];
const COMBOBOX_STATES = ["Default", "Focus", "Disabled", "Readonly"];
const COMBOBOX_STATUSES = ["Default", "Error", "Warning", "Success"];
const COMBOBOX_CONTENT = ["Closed", "Open"];
const MULTI_SELECT_STATES = ["Default", "Focus", "Disabled", "Readonly"];
const MULTI_SELECT_STATUSES = ["Default", "Error", "Warning", "Success"];
const MULTI_SELECT_CONTENT = ["Empty", "Selected", "Open"];
const LISTBOX_SELECTION = ["Single", "Multiple"];
const LISTBOX_STATES = ["Default", "Focus", "Disabled"];
const FORM_FIELD_CONTENT = ["Basic", "Description", "Helper", "Full"];
const FORM_FIELD_STATUSES = ["Default", "Error", "Warning", "Success"];
const FORM_FIELD_REQUIRED = ["False", "True"];
const DATE_PICKER_STATES = ["Default", "Focus", "Disabled", "Readonly"];
const DATE_PICKER_STATUSES = ["Default", "Error", "Warning", "Success"];
const DATE_PICKER_CONTENT = ["Closed", "Open"];
const DATE_PICKER_EXAMPLE = {
  year: 2026,
  monthIndex: 4,
  selectedDay: 25,
  todayDay: 22,
};
const DATE_PICKER_GRID_ROWS = 6;
const DATE_PICKER_GRID_ROW_HEIGHT = 36;
const DATE_PICKER_GRID_GAP = 4;
const DATE_PICKER_GRID_HEIGHT =
  DATE_PICKER_GRID_ROWS * DATE_PICKER_GRID_ROW_HEIGHT +
  (DATE_PICKER_GRID_ROWS - 1) * DATE_PICKER_GRID_GAP;
const DATE_PICKER_CALENDAR_HEIGHT = 328;
const DATE_PICKER_OPEN_VARIANT_HEIGHT = 430;
const DATE_PICKER_STATE_ROW_STEP = 500;
const DATE_RANGE_PICKER_STATES = ["Default", "Focus", "Disabled", "Readonly"];
const DATE_RANGE_PICKER_STATUSES = ["Default", "Error", "Warning", "Success"];
const DATE_RANGE_PICKER_CONTENT = ["Closed", "Open"];
const DATE_RANGE_PICKER_EXAMPLE = {
  startYear: 2026,
  startMonthIndex: 4,
  startDay: 21,
  endYear: 2026,
  endMonthIndex: 4,
  endDay: 25,
  nextMonthIndex: 5,
};
const DATE_RANGE_PICKER_GRID_ROWS = 6;
const DATE_RANGE_PICKER_GRID_ROW_HEIGHT = 40;
const DATE_RANGE_PICKER_GRID_GAP = 4;
const DATE_RANGE_PICKER_GRID_HEIGHT =
  DATE_RANGE_PICKER_GRID_ROWS * DATE_RANGE_PICKER_GRID_ROW_HEIGHT +
  (DATE_RANGE_PICKER_GRID_ROWS - 1) * DATE_RANGE_PICKER_GRID_GAP;
const DATE_RANGE_PICKER_MONTH_HEIGHT = 328;
const DATE_RANGE_PICKER_CALENDAR_HEIGHT = 352;
const DATE_RANGE_PICKER_OPEN_VARIANT_HEIGHT = 490;
const DATE_RANGE_PICKER_STATE_ROW_STEP = 580;
const TIME_PICKER_STATES = ["Default", "Focus", "Disabled", "Readonly"];
const TIME_PICKER_STATUSES = ["Default", "Error", "Warning", "Success"];
const TIME_PICKER_CONTENT = ["Closed", "Open"];
const FILE_UPLOAD_STATES = ["Default", "Focus", "Dragging", "Disabled"];
const FILE_UPLOAD_STATUSES = ["Default", "Error", "Warning", "Success"];
const FILE_UPLOAD_CONTENT = ["Dropzone", "Files"];
const COLOR_PICKER_STATES = ["Default", "Focus", "Disabled", "Readonly"];
const COLOR_PICKER_STATUSES = ["Default", "Error", "Warning", "Success"];
const COLOR_PICKER_CONTENT = ["Closed", "Open"];
const COLOR_PICKER_FORMATS = ["HSL", "RGB", "HEX"];
const TEXTAREA_STATES = ["Default", "Focus", "Disabled", "Readonly"];
const TEXTAREA_STATUSES = ["Default", "Error"];
const SEARCH_STATES = ["Default", "Focus", "Disabled", "Readonly"];
const SEARCH_STATUSES = ["Default", "Error"];
const SELECT_STATES = ["Default", "Focus", "Disabled"];
const SELECT_STATUSES = ["Default", "Error"];
const SLIDER_STATES = ["Default", "Focus", "Disabled"];
const SLIDER_STATUSES = ["Default", "Error"];
const SLIDER_TYPES = ["Single", "Range"];
const SLIDER_THUMB_SIZE = 20;
const RATING_VALUES = ["0", "1", "2", "3", "4", "5"];
const RATING_STATES = ["Default", "Readonly"];
const STEPPER_COUNTS = ["2", "3", "4"];
const STEPPER_CURRENT = ["1", "2", "3", "4"];
const PROGRESS_VALUES = ["0", "25", "50", "75", "100"];
const SPINNER_SIZES = ["Small", "Medium", "Large", "XLarge"];
const AVATAR_CONTENT = ["Fallback", "Image"];
const ALERT_VARIANTS = ["Default", "Destructive", "Success", "Warning", "Info"];
const EMPTY_STATE_CONTENT = ["Basic", "Icon", "Action"];
const CARD_CONTENT = ["Basic", "Header", "Full"];
const LIST_DENSITIES = ["Default", "Compact"];
const TABLE_DENSITIES = ["Default", "Compact"];
// Product / SDK lane. These compose Core primitives and stay domain-specific;
// see docs/figma-core-gap-audit.md for why they are not promoted into Core.
const DIRECTION_STEP_TYPES = ["Straight", "Left", "Right", "Destination"];
const FLOOR_SELECTOR_VARIANTS = [
  "VerticalList",
  "HorizontalList",
  "CompactStepper",
];
const LOCATION_PIN_STATES = [
  "Default",
  "Selected",
  "Featured",
  "OffFloor",
  "Disabled",
];
// Marker footprint. Mirrors the React LocationPin size prop and the native
// KozmosLocationPinSize enum, so all four platforms agree on sm/md/lg.
const LOCATION_PIN_SIZES = ["Sm", "Md", "Lg"];
const LOCATION_PIN_SIZE_DIAMETERS = { Sm: 24, Md: 32, Lg: 40 };
const POI_CARD_CONTENT = ["Basic", "Media", "Full"];
const WAYFINDING_CARD_CONTENT = ["Basic", "Titled"];
const ADAPTIVE_MAP_SHELL_PANEL_PLACEMENTS = ["Start", "End"];
const MAP_CONTROL_BUTTON_PRESENTATIONS = ["IconOnly", "Labelled"];
const MAP_CONTROLS_GROUP_PRESENTATIONS = ["IconOnly", "Labelled"];
const MAP_OVERLAY_WIDTHS = ["Auto", "Small", "Medium", "Large", "Full"];
const MAP_OVERLAY_WIDTH_SIZES = {
  Auto: 240,
  Small: 280,
  Medium: 360,
  Large: 480,
  Full: 640,
};
const POI_DETAIL_PANEL_PRESENTATIONS = ["Inline", "Sheet", "Panel"];
const BROWSE_CATEGORIES_PANEL_CONTENT = ["Basic", "Search", "Empty"];
const CATEGORY_TILE_STATES = ["Default", "Selected", "Disabled"];
const POI_MEDIA_GALLERY_CONTENT = ["Single", "Multiple", "Empty"];
const POI_RESULT_CARD_STATES = [
  "Default",
  "Selected",
  "Featured",
  "Unavailable",
];
const POI_RESULT_LIST_CONTENT = ["Basic", "Selected", "Empty"];
const ROUTE_OPTION_CARD_STATES = [
  "Default",
  "Selected",
  "Warning",
  "Unavailable",
];
const ROUTE_PREVIEW_PANEL_STATUSES = [
  "Idle",
  "Calculating",
  "Ready",
  "NoRoute",
  "Error",
];
const ROUTE_PREVIEW_PANEL_STATUS_COPY = {
  Idle: "Choose a destination to preview routes.",
  Calculating: "Calculating routes...",
  Ready: "3 routes available",
  NoRoute: "No route to this destination.",
  Error: "Routes could not be loaded.",
};
const ROUTE_SUMMARY_STATES = ["Preview", "Active"];
const ROUTING_INPUT_GROUP_CONTENT = ["TwoPoints", "ThreePoints"];
const SAVE_LOCATION_CARD_STATES = ["Default", "Saved"];
const USER_LOCATION_MARKER_HEADINGS = ["Hidden", "Visible"];
// Platform / form-factor lane. These validate on a specific device surface
// before any Core promotion is considered.
const DYNAMIC_ISLAND_STATES = ["Compact", "Expanded", "Minimal"];
const DYNAMIC_ISLAND_GEOMETRY = {
  Compact: { width: 240, height: 48, radius: 24 },
  Expanded: { width: 360, height: 180, radius: 32 },
  Minimal: { width: 64, height: 48, radius: 24 },
};
const FEEDBACK_CARD_STATES = ["Default", "Submitting", "Success"];
const MAP_VIEW_CONTENT = ["Empty", "Overlay"];
const PAGINATION_CONTENT = ["Basic", "Ellipsis", "Compact"];
const PAGINATION_SIZES = ["Small", "Default"];
const DIALOG_CONTENT = ["Basic", "Form", "Footer"];
const DRAWER_SIDES = ["Right", "Left", "Top", "Bottom"];
const POPOVER_SIDES = ["Top", "Right", "Bottom", "Left"];
const MENU_CONTENT = ["Basic", "Checkbox", "Radio", "Submenu"];
const TOAST_CONTENT = ["Basic", "Action"];
const TABS_COUNTS = ["Two", "Three", "Four"];
const TABS_ACTIVE = ["One", "Two", "Three", "Four"];
const TABS_STATES = ["Default", "Focus", "Disabled"];
const TOOLTIP_SIDES = ["Top", "Right", "Bottom", "Left"];
const BACKDROP_VISIBILITY = ["Visible", "Hidden"];
const BOTTOM_SHEET_CONTENT = ["Basic", "Form", "Footer"];
const BOTTOM_SHEET_CONTENT_SLOT_HEIGHT = 44;
const BOTTOM_SHEET_FORM_INPUTS = [
  {
    name: "Primary Field Input",
    label: "Field label",
    placeholder: "Enter value",
  },
  {
    name: "Supporting Field Input",
    label: "Supporting field",
    placeholder: "Add optional context",
  },
];
const SEARCH_BAR_VARIANTS = ["Inline", "Floating"];
const SEARCH_BAR_STATES = ["Default", "Focus", "Filled", "Disabled"];
const BOTTOM_NAVIGATION_COUNTS = ["Three", "Four", "Five"];
const BOTTOM_NAVIGATION_ACTIVE = ["One", "Two", "Three", "Four", "Five"];
const NAVIGATION_ITEM_PLACEMENTS = ["Top", "Side", "Rail"];
const NAVIGATION_ITEM_CONTENT = [
  "Label",
  "Icon Label",
  "Icon Only",
  "Badge",
  "Trailing",
];
const NAVIGATION_ITEM_DENSITIES = ["Default", "Compact"];
const NAVIGATION_ITEM_STATES = [
  "Default",
  "Hover",
  "Selected",
  "Focus",
  "Disabled",
];
const NAVIGATION_ITEM_VARIANT_LAYOUT = {
  stateColumnStep: 340,
  densityRowGap: 40,
  contentGroupGap: 80,
  placementGroupGap: 200,
  outerPadding: 80,
  rowHeights: {
    Top: 44,
    Side: 44,
    Rail: 72,
  },
};
const NAVBAR_CONTENT = ["Basic", "Actions", "Contextual"];
const NAVBAR_SLOT_NAMES = [
  "Logo Slot",
  "Context Slot",
  "Navigation Slot",
  "Primary Action Slot",
  "Actions Slot",
  "Utility Slot",
  "Account Slot",
];
const NAVBAR_DEFAULT_WIDTH = 880;
const NAVBAR_DEFAULT_HEIGHT = 64;
const NAVBAR_CONTEXT_SLOT_WIDTH = 184;
const NAVBAR_CONTEXT_SLOT_HEIGHT = 44;
const NAVBAR_CONTEXT_TEXT_WIDTH = 162;
const SIDEBAR_CONTENT = ["Basic", "Sections", "Tools", "Rail"];
const SIDEBAR_SLOT_NAMES = [
  "Header Slot",
  "Navigation Slot",
  "Tools Slot",
  "Footer Slot",
];
const SIDEBAR_DEFAULT_WIDTH = 280;
const SIDEBAR_RAIL_WIDTH = 80;
const SIDEBAR_DEFAULT_HEIGHT = 360;
const SIDEBAR_SLOT_WIDTH = 248;
const SIDEBAR_RAIL_SLOT_WIDTH = 72;
const SIDEBAR_HEADER_FOOTER_HEIGHT = 44;
const SIDEBAR_NAVIGATION_HEIGHT = 220;
const SIDEBAR_TOOLS_NAVIGATION_HEIGHT = 204;
const SIDEBAR_RAIL_NAVIGATION_HEIGHT = 264;
const SIDEBAR_TOOLS_SLOT_HEIGHT = 84;
const TABS_COUNT_TO_NUMBER = {
  Two: 2,
  Three: 3,
  Four: 4,
};
const TABS_ACTIVE_TO_INDEX = {
  One: 0,
  Two: 1,
  Three: 2,
  Four: 3,
};
const COMPONENT_SET_LEGACY_SUFFIX = " / v1";
const COMPONENTS_PAGE_NAME = "Components";
const COMPONENT_PAGE_LAYOUT_X = 80;
const COMPONENT_PAGE_LAYOUT_Y = 80;
const COMPONENT_PAGE_LAYOUT_ROW_GAP = 420;
const COMPONENT_PAGE_LAYOUT_COLUMN_GAP = 320;
const COMPONENT_PAGE_LAYOUT_SECTION_GAP = 220;
// Column packing. A single column of sections, each itself a single column of
// component sets, made the page 94,012px tall — a 1:16 ribbon that no one can
// review, and one that got worse with every set added. Both levels now flow
// into columns instead.
const COMPONENT_PAGE_LAYOUT_MAX_COLUMNS = 6;
const COMPONENT_PAGE_LAYOUT_SECTION_COLUMN_GAP = 480;
const COMPONENT_PAGE_LAYOUT_MAX_SECTION_COLUMNS = 5;
const COMPONENT_PAGE_LAYOUT_SECTION_PADDING_X = 40;
const COMPONENT_PAGE_LAYOUT_SECTION_PADDING_TOP = 56;
const COMPONENT_PAGE_LAYOUT_SECTION_PADDING_BOTTOM = 40;
const COMPONENT_PAGE_LAYOUT_SECTION_HEADING_GAP = 36;
const COMPONENT_PAGE_LAYOUT_MIN_FOOTPRINT_HEIGHT = 260;
const COMPONENT_PAGE_LAYOUT_MIN_HEIGHTS = {
  Link: 260,
  Separator: 260,
  Button: 900,
  IconButton: 820,
  ToggleButton: 620,
  SplitButton: 720,
  FloatingActionButton: 620,
  Badge: 260,
  Counter: 260,
  Tag: 360,
  Chip: 640,
  SegmentedControl: 820,
  Box: 360,
  Card: 420,
  Container: 360,
  Grid: 760,
  ScrollArea: 420,
  Stack: 520,
  List: 360,
  Table: 520,
  AdaptiveMapShell: 520,
  BrowseCategoriesPanel: 460,
  CategoryTile: 260,
  DirectionStep: 260,
  DynamicIsland: 320,
  FeedbackCard: 360,
  FloorSelector: 420,
  MapControlButton: 260,
  MapControlsGroup: 320,
  MapOverlay: 320,
  POIDetailPanel: 620,
  POIMediaGallery: 320,
  POIResultCard: 260,
  POIResultList: 460,
  RouteOptionCard: 260,
  RoutePreviewPanel: 480,
  RouteSummary: 260,
  RoutingInputGroup: 320,
  SaveLocationCard: 300,
  UserLocationMarker: 260,
  LocationPin: 320,
  MapView: 520,
  POICard: 620,
  WayfindingCard: 420,
  TreeParentItem: 2500,
  TreeChildItem: 1900,
  TreeItem: 2600,
  Tree: 520,
  Timeline: 520,
  Breadcrumb: 260,
  BottomNavigation: 620,
  NavigationItem: 4200,
  Navbar: 420,
  Pagination: 360,
  Sidebar: 520,
  Tabs: 620,
  Accordion: 320,
  Backdrop: 360,
  BottomSheet: 520,
  Dialog: 760,
  Drawer: 420,
  Menu: 420,
  Popover: 360,
  Tooltip: 360,
  Checkbox: 260,
  Radio: 260,
  Switch: 260,
  Input: 720,
  PasswordInput: 760,
  FormField: 820,
  NumberInput: 720,
  OTPInput: 760,
  Combobox: 1040,
  MultiSelect: 1200,
  Listbox: 520,
  DatePicker: 2100,
  DateRangePicker: 2400,
  TimePicker: 1380,
  FileUpload: 1420,
  ColorPicker: 10500,
  SearchBar: 620,
  Search: 620,
  Select: 420,
  Slider: 500,
  Rating: 360,
  Textarea: 760,
  Alert: 260,
  Stepper: 420,
  Progress: 260,
  Skeleton: 260,
  Spinner: 260,
  Toast: 260,
  EmptyState: 360,
  Avatar: 260,
};
const COMPONENT_PAGE_LAYOUT_SECTIONS = [
  { title: "Typography", components: ["Link", "Separator"] },
  {
    title: "Actions",
    components: [
      "Button",
      "IconButton",
      "ToggleButton",
      "SplitButton",
      "FloatingActionButton",
    ],
  },
  { title: "Status", components: ["Badge", "Counter", "Tag"] },
  { title: "Selection", components: ["Chip", "SegmentedControl"] },
  {
    title: "Layout",
    components: ["Box", "Card", "Container", "Grid", "ScrollArea", "Stack"],
  },
  {
    title: "Data display",
    components: [
      "List",
      "Table",
      "TreeParentItem",
      "TreeChildItem",
      "TreeItem",
      "Tree",
      "Timeline",
    ],
  },
  {
    title: "Navigation",
    components: [
      "BottomNavigation",
      "Breadcrumb",
      "NavigationItem",
      "Navbar",
      "Pagination",
      "Sidebar",
      "Tabs",
      "Stepper",
    ],
  },
  { title: "Disclosure", components: ["Accordion"] },
  {
    // Product / SDK compositions. Kept in their own section so Core stays
    // domain-neutral and designers can see the boundary on the page.
    title: "Product / SDK",
    components: [
      "AdaptiveMapShell",
      "BrowseCategoriesPanel",
      "CategoryTile",
      "DirectionStep",
      "FloorSelector",
      "LocationPin",
      "MapControlButton",
      "MapControlsGroup",
      "MapOverlay",
      "MapView",
      "POICard",
      "POIDetailPanel",
      "POIMediaGallery",
      "POIResultCard",
      "POIResultList",
      "RouteOptionCard",
      "RoutePreviewPanel",
      "RouteSummary",
      "RoutingInputGroup",
      "SaveLocationCard",
      "UserLocationMarker",
      "WayfindingCard",
    ],
  },
  {
    // Device-specific surfaces. Kept out of Product / SDK because they need
    // form-factor validation of their own before any promotion.
    title: "Platform / Form-Factor",
    components: ["DynamicIsland", "FeedbackCard"],
  },
  {
    title: "Overlay",
    components: [
      "Backdrop",
      "BottomSheet",
      "Dialog",
      "Drawer",
      "Menu",
      "Popover",
      "Tooltip",
    ],
  },
  { title: "Controls", components: ["Checkbox", "Radio", "Switch"] },
  {
    title: "Inputs",
    components: [
      "Input",
      "PasswordInput",
      "FormField",
      "NumberInput",
      "OTPInput",
      "Combobox",
      "MultiSelect",
      "Listbox",
      "DatePicker",
      "DateRangePicker",
      "TimePicker",
      "FileUpload",
      "ColorPicker",
      "SearchBar",
      "Search",
      "Select",
      "Slider",
      "Rating",
      "Textarea",
    ],
  },
  {
    title: "Feedback",
    components: [
      "Alert",
      "EmptyState",
      "Progress",
      "Skeleton",
      "Spinner",
      "Toast",
    ],
  },
  { title: "Media", components: ["Avatar"] },
];
const COMPONENT_PAGE_LAYOUT_ORDER = componentPageLayoutOrder(
  COMPONENT_PAGE_LAYOUT_SECTIONS,
);
const COMPONENT_DOCS_PAGE_NAME = "Docs";
const COMPONENT_DOC_SPLIT_PAGE_PREFIX = "Docs / ";
const EXAMPLES_PAGE_NAME = "Examples";
const EXAMPLE_TEMPLATE_COLLISION_GAP = 96;
const EXAMPLE_TEMPLATE_CONFIGS = {
  dashboard: {
    name: "Example / Dashboard Table Page",
    x: 80,
    y: 80,
    width: 840,
    height: 680,
  },
  settings: {
    name: "Example / Settings Form Page",
    x: 980,
    y: 80,
    width: 560,
    height: 680,
  },
  mobileDrawer: {
    name: "Example / Mobile Drawer Flow",
    x: 80,
    y: 860,
    width: 390,
    height: 844,
  },
  operationsConsole: {
    name: "Example / Operations Console",
    x: 520,
    y: 860,
    width: 1120,
    height: 760,
  },
  sdkRouteFlow: {
    name: "Example / SDK Route Flow",
    x: 1680,
    y: 80,
    width: 430,
    height: 920,
  },
  kioskHandoff: {
    name: "Example / Kiosk Handoff Flow",
    x: 1680,
    y: 1040,
    width: 920,
    height: 720,
  },
};
const DOCS_CANVAS_X = 80;
const DOCS_CANVAS_Y = 80;
const DOCS_COLUMNS = 3;
const DOCS_SECTION_WIDTH = 760;
const DOCS_SECTION_PADDING = 32;
const DOCS_SECTION_PADDING_BOTTOM = 44;
const DOCS_SECTION_GAP = 48;
const DOCS_ROW_GAP = 56;
const DOCS_INNER_WIDTH = DOCS_SECTION_WIDTH - DOCS_SECTION_PADDING * 2;
const DOCS_CATALOG_WIDTH =
  DOCS_SECTION_WIDTH * DOCS_COLUMNS + DOCS_SECTION_GAP * (DOCS_COLUMNS - 1);
const SURFACE_QA_PAGE_NAME = "QA / Transparent Surfaces";
const SURFACE_QA_CANVAS_X = 80;
const SURFACE_QA_CANVAS_Y = 80;
const SURFACE_QA_PANEL_WIDTH = 760;
const SURFACE_QA_PANEL_GAP = 56;
const SURFACE_QA_GROUP_GAP = 18;
const SURFACE_QA_COMPONENT_GROUPS = [
  {
    title: "Actions",
    rows: [
      [
        {
          componentSetName: "Button",
          variantName: "Variant=Ghost, Size=Default, State=Default",
          text: { "Label Text": "Add to library" },
        },
        {
          componentSetName: "Button",
          variantName: "Variant=Link, Size=Default, State=Default",
          text: { "Label Text": "View details" },
        },
        {
          componentSetName: "Button",
          variantName: "Variant=Glass, Size=Default, State=Default",
          text: { "Label Text": "Navigate" },
        },
        {
          componentSetName: "IconButton",
          variantName: "Variant=Ghost, Size=Default, State=Default",
        },
        {
          componentSetName: "IconButton",
          variantName: "Variant=Glass, Size=Default, State=Default",
        },
        {
          componentSetName: "Badge",
          variantName: "Variant=Ghost, Size=Default",
          text: { "Label Text": "Indoor" },
        },
      ],
    ],
  },
  {
    title: "Fields",
    rows: [
      [
        {
          componentSetName: "Input",
          variantName: "State=Default, Status=Default",
          text: { "Label Text": "Level", "Placeholder Text": "Choose level" },
        },
        {
          componentSetName: "Search",
          variantName: "State=Default, Status=Default",
          text: { "Label Text": "Search", "Placeholder Text": "Search venue" },
        },
      ],
      [
        {
          componentSetName: "Select",
          variantName: "State=Default, Status=Default",
          text: { "Placeholder Text": "Destination" },
        },
        {
          componentSetName: "Slider",
          variantName: "State=Default, Status=Default",
          text: { "Label Text": "Zoom" },
        },
      ],
      [
        {
          componentSetName: "Textarea",
          variantName: "State=Default, Status=Default",
          text: {
            "Label Text": "Notes",
            "Placeholder Text": "Optional context",
          },
        },
      ],
    ],
  },
  {
    title: "Selection and feedback",
    rows: [
      [
        {
          componentSetName: "Checkbox",
          variantName: "Checked=Unchecked, State=Default",
          text: { "Label Text": "Accessible route" },
        },
        {
          componentSetName: "Radio",
          variantName: "Checked=Checked, State=Default",
          text: { "Label Text": "Fastest" },
        },
        {
          componentSetName: "Switch",
          variantName: "Checked=Checked, State=Default",
          text: { "Label Text": "Live updates" },
        },
      ],
      [
        {
          componentSetName: "Progress",
          variantName: "Value=50",
        },
        {
          componentSetName: "Spinner",
          variantName: "Size=Medium",
        },
      ],
    ],
  },
];
const COMPONENT_DOCS = [
  {
    componentName: "AdaptiveMapShell",
    componentSetName: "AdaptiveMapShell",
    category: "Product / SDK",
    summary:
      "AdaptiveMapShell is the map screen frame: a canvas, its floating controls, and a docked panel.",
    usage: [
      "Use Start when the panel belongs at the inline start of the reading direction.",
      "Use End for the common desktop layout with the panel trailing the map.",
      "Compose the panel slot with POIDetailPanel, RoutePreviewPanel, or BrowseCategoriesPanel.",
    ],
    api: [
      "PanelPlacement maps to AdaptiveMapShell.panelPlacement.",
      "Panel Slot maps to AdaptiveMapShell.panel, Top Bar Slot to .topBar.",
      "Controls Slot maps to AdaptiveMapShell.controls.",
      "mapStatus, mapLabel, and collisionInsets are behaviour with no visual counterpart.",
    ],
    properties: ["PanelPlacement: Start, End", "Panel Label Text"],
    accessibility: [
      "Placement is expressed in flow order, so the shell mirrors correctly in RTL.",
      "The host app must name the map surface via mapLabel and the panel via panelLabel.",
      "Collision insets must be passed to the renderer so controls never cover attribution.",
    ],
  },
  {
    componentName: "MapControlButton",
    componentSetName: "MapControlButton",
    category: "Product / SDK",
    summary:
      "MapControlButton is a single map affordance such as zoom, compass reset, or recentre.",
    usage: [
      "Use IconOnly inside a stacked control group over the canvas.",
      "Use Labelled where the action needs to be readable without a tooltip.",
      "Do not use for general actions; Core Button and IconButton cover those.",
    ],
    api: [
      "Presentation maps to MapControlButton.presentation.",
      "Label Text maps to MapControlButton.label.",
      "pressed maps to the toggled surface treatment.",
      "stateLabel extends the accessible name only.",
    ],
    properties: ["Presentation: IconOnly, Labelled", "Label Text"],
    accessibility: [
      "Both presentations keep a 44px target, including the icon-only variant.",
      "label is the accessible name in both presentations, not just the visible one.",
      "Glyphs never auto-mirror, so a compass keeps its physical orientation in RTL.",
    ],
  },
  {
    componentName: "MapControlsGroup",
    componentSetName: "MapControlsGroup",
    category: "Product / SDK",
    summary:
      "MapControlsGroup stacks the standard zoom, compass, and locate affordances.",
    usage: [
      "Use IconOnly over a dense canvas where space is tight.",
      "Use Labelled when the locate control needs an explicit name.",
      "Place the group inside the AdaptiveMapShell controls slot.",
    ],
    api: [
      "LocationPresentation maps to MapControlsGroup.locationPresentation.",
      "Location Label Text maps to MapControlsGroup.locationLabel.",
      "onZoomIn, onZoomOut, onCompassReset, and onMyLocation are behaviour.",
      "compassBearing rotates the renderer glyph and has no variant.",
    ],
    properties: [
      "LocationPresentation: IconOnly, Labelled",
      "Location Label Text",
    ],
    accessibility: [
      "Every control keeps a 44px target so the group satisfies the touch-target contract.",
      "locationState must be announced by product code; it is not colour-only here.",
      "The group needs an accessible name via label so it reads as one control cluster.",
    ],
  },
  {
    componentName: "MapOverlay",
    componentSetName: "MapOverlay",
    category: "Product / SDK",
    summary:
      "MapOverlay is the floating surface that holds product chrome above the map canvas.",
    usage: [
      "Use Auto when the content should size itself, Full when it should span the canvas.",
      "Use Small through Large for search, filters, and summary chrome.",
      "Do not draw map content inside it; the canvas is renderer output.",
    ],
    api: [
      "Width maps to MapOverlay.width (auto, sm, md, lg, full).",
      "Overlay Content Slot maps to MapOverlay children.",
      "position is renderer placement and stays outside the variant matrix.",
      "collisionInsets come from the map adapter.",
    ],
    properties: [
      "Width: Auto, Small, Medium, Large, Full",
      "Overlay Title Text",
    ],
    accessibility: [
      "Overlays must not cover the renderer attribution; pass collision insets instead.",
      "Content keeps its own reading order; the overlay adds no landmark of its own.",
      "Contrast is carried by the surface token, not by the basemap underneath.",
    ],
  },
  {
    componentName: "POIDetailPanel",
    componentSetName: "POIDetailPanel",
    category: "Product / SDK",
    summary:
      "POIDetailPanel presents one venue in full: media, description, services, and actions.",
    usage: [
      "Use Panel when docked beside the map, Sheet when anchored to the bottom edge.",
      "Use Inline when the panel sits in document flow with no surface chrome.",
      "Compose the media slot with POIMediaGallery rather than extending this set.",
    ],
    api: [
      "Presentation maps to POIDetailPanel.presentation.",
      "Title Text maps to poi.name, Subtitle Text to category, floor, and availability labels.",
      "Description Text maps to poi.description.",
      "Actions Slot maps to poi.actions resolved through actionLabels.",
    ],
    properties: [
      "Presentation: Inline, Sheet, Panel",
      "Title Text, Subtitle Text, Description Text",
    ],
    accessibility: [
      "Sheet shows a grabber so the drag affordance is not gesture-only.",
      "Inline drops the close affordance because there is nothing to dismiss.",
      "actionStates carry loading and error messages that product code must announce.",
    ],
  },
  {
    componentName: "BrowseCategoriesPanel",
    componentSetName: "BrowseCategoriesPanel",
    category: "Product / SDK",
    summary:
      "BrowseCategoriesPanel is the category browser that opens before a search has been made.",
    usage: [
      "Use Basic when categories are the whole panel.",
      "Use Search when a search field sits above the grid.",
      "Use Empty when no category matches the current filter.",
    ],
    api: [
      "Content covers the panel with and without search, plus its empty state.",
      "Panel Label Text maps to BrowseCategoriesPanel.label.",
      "Category Tile Slots map to categories rendered through CategoryTile.",
      "onSelect and renderIcon are behaviour with no visual slot.",
    ],
    properties: ["Content: Basic, Search, Empty", "Panel Label Text"],
    accessibility: [
      "Empty replaces the grid, so assistive technology never reads an empty list.",
      "The panel needs an accessible name via label so it reads as one region.",
      "Category selection state belongs to CategoryTile, not to the panel.",
    ],
  },
  {
    componentName: "CategoryTile",
    componentSetName: "CategoryTile",
    category: "Product / SDK",
    summary:
      "CategoryTile is one selectable venue category inside the browse grid.",
    usage: [
      "Use Selected for the category currently filtering results.",
      "Use Disabled when the category has no results on the active floor.",
      "Do not use as a general-purpose button; Core Button covers that.",
    ],
    api: [
      "State maps to category.selected and category.disabled.",
      "Label Text maps to category.label.",
      "Icon Slot maps to renderIcon(category).",
      "resultCountLabel is announced by product code.",
    ],
    properties: ["State: Default, Selected, Disabled", "Label Text"],
    accessibility: [
      "Selection thickens the border as well as recolouring it, so it is not colour-only.",
      "The tile is a button; product code owns its pressed state.",
      "Disabled dims the tile but must also be exposed as disabled to assistive technology.",
    ],
  },
  {
    componentName: "POIMediaGallery",
    componentSetName: "POIMediaGallery",
    category: "Product / SDK",
    summary:
      "POIMediaGallery pages through the licensed images attached to a venue.",
    usage: [
      "Use Single when only one image exists.",
      "Use Multiple when the venue has a pageable set.",
      "Use Empty when no licensed media is available.",
    ],
    api: [
      "Content covers one image, a pageable set, and the no-media case.",
      "Position Text maps to positionLabel(current, total).",
      "Media Slot maps to media[activeIndex].",
      "activeIndex and onActiveIndexChange are behaviour.",
    ],
    properties: ["Content: Single, Multiple, Empty", "Position Text"],
    accessibility: [
      "Single drops the control row rather than showing disabled arrows.",
      "positionLabel is the announced position; it is not inferred from the visual.",
      "Each image carries its own alt text from product data.",
    ],
  },
  {
    componentName: "POIResultCard",
    componentSetName: "POIResultCard",
    category: "Product / SDK",
    summary:
      "POIResultCard is one venue row in a result list, mirroring its pin on the map.",
    usage: [
      "Use Selected for the result whose pin is currently selected.",
      "Use Featured for sponsored or promoted results.",
      "Use Unavailable when the venue is closed or unreachable.",
    ],
    api: [
      "State maps to result.selected, result.featured, and result.available.",
      "Title Text maps to poi.name.",
      "Meta Text maps to floorLabel and travelEstimate.",
      "onSelect receives the canonical POI ID.",
    ],
    properties: [
      "State: Default, Selected, Featured, Unavailable",
      "Title Text, Meta Text",
    ],
    accessibility: [
      "Featured uses a labelled badge, so a sponsored result is never identified by colour alone.",
      "Selection thickens the border as well as recolouring it.",
      "Product code must keep the row and its LocationPin announced together.",
    ],
  },
  {
    componentName: "POIResultList",
    componentSetName: "POIResultList",
    category: "Product / SDK",
    summary:
      "POIResultList stacks venue results under a count and an optional empty state.",
    usage: [
      "Use Basic for a plain result list.",
      "Use Selected to show the list with an active row.",
      "Use Empty when the query returned nothing.",
    ],
    api: [
      "Content covers the plain list, a selected row, and the empty state.",
      "Result Count Text maps to POIResultList.resultCountLabel.",
      "Result Slots map to items rendered through POIResultCard.",
      "selectedPoiId is a single canonical POI ID.",
    ],
    properties: ["Content: Basic, Selected, Empty", "Result Count Text"],
    accessibility: [
      "Exactly one row is selected, matching selectedPoiId being a single ID.",
      "resultCountLabel is the announced count and must stay in sync with the rows.",
      "The list needs an accessible name via label.",
    ],
  },
  {
    componentName: "RouteOptionCard",
    componentSetName: "RouteOptionCard",
    category: "Product / SDK",
    summary:
      "RouteOptionCard presents one route choice with its duration, distance, and caveats.",
    usage: [
      "Use Selected for the route the user has picked.",
      "Use Warning when the route has a caveat such as a lift outage.",
      "Use Unavailable when the route cannot currently be taken.",
    ],
    api: [
      "State maps to option.selected, option.warning, and option.available.",
      "Option Label Text maps to option.label.",
      "Option Meta Text maps to durationLabel and distanceLabel.",
      "Warning Text maps to option.warning.",
    ],
    properties: [
      "State: Default, Selected, Warning, Unavailable",
      "Option Label Text, Option Meta Text, Warning Text",
    ],
    accessibility: [
      "The warning is its own text node, so assistive technology reads it as separate content.",
      "Selection thickens the border as well as recolouring it.",
      "preference selects the mode icon; it is product data, not a variant.",
    ],
  },
  {
    componentName: "RoutePreviewPanel",
    componentSetName: "RoutePreviewPanel",
    category: "Product / SDK",
    summary:
      "RoutePreviewPanel lists the routes to a destination and tracks route readiness.",
    usage: [
      "Use Ready when routes exist and can be chosen.",
      "Use Calculating while routing is in flight, Idle before a destination is set.",
      "Use NoRoute and Error for the two failure cases, which read differently to users.",
    ],
    api: [
      "Status maps to RoutePreviewPanel.status (idle, calculating, ready, no-route, error).",
      "Destination Text maps to destinationName.",
      "Status Text maps to the readiness copy; Status Slot maps to statusContent.",
      "Option Slots map to options rendered through RouteOptionCard.",
    ],
    properties: [
      "Status: Idle, Calculating, Ready, NoRoute, Error",
      "Destination Text, Status Text",
    ],
    accessibility: [
      "Only Ready lists options, so the panel never renders an empty option list.",
      "selectedRouteAnnouncement must be announced when the choice changes.",
      "Error uses the danger token plus its own copy, so it is not colour-only.",
    ],
  },
  {
    componentName: "RouteSummary",
    componentSetName: "RouteSummary",
    category: "Product / SDK",
    summary:
      "RouteSummary is the persistent ETA bar shown while previewing or following a route.",
    usage: [
      "Use Preview before navigation starts, while the user can still change their mind.",
      "Use Active once turn-by-turn guidance is running.",
      "Compose it above the map rather than inside the route panel.",
    ],
    api: [
      "State maps to RouteSummary.state (preview, active).",
      "ETA Text maps to etaText; Distance Text maps to distanceText.",
      "Start Navigation Button maps to onStartNavigation, End Route Button to onEndRoute.",
      "transportModeIcon fills the mode slot.",
    ],
    properties: ["State: Preview, Active", "ETA Text, Distance Text"],
    accessibility: [
      "Preview and Active never show both actions, so there is no ambiguous control.",
      "Both strings arrive already localized from product code.",
      "The bar must not cover the renderer attribution.",
    ],
  },
  {
    componentName: "RoutingInputGroup",
    componentSetName: "RoutingInputGroup",
    category: "Product / SDK",
    summary:
      "RoutingInputGroup collects the origin, destination, and any intermediate stops.",
    usage: [
      "Use TwoPoints for the common origin-to-destination case.",
      "Use ThreePoints when the user has added a stop.",
      "Compose the fields with Core Input rather than extending this set.",
    ],
    api: [
      "Content covers two points and an added intermediate stop.",
      "Point Field Slots map to points; Point Label Text maps to point.placeholder.",
      "Swap Button maps to onSwap.",
      "onAddPoint and onRemovePoint are behaviour with no visual slot.",
    ],
    properties: ["Content: TwoPoints, ThreePoints", "Point Label Text"],
    accessibility: [
      "Labels follow route order, so a third point reads as a stop between start and destination.",
      "Swapping must move focus predictably and announce the new order.",
      "Every field keeps a 44px target.",
    ],
  },
  {
    componentName: "SaveLocationCard",
    componentSetName: "SaveLocationCard",
    category: "Product / SDK",
    summary:
      "SaveLocationCard offers to keep a place in the user's saved locations.",
    usage: [
      "Use Default when the place has not been saved.",
      "Use Saved once it is in the saved list.",
      "Do not use for generic confirmations; Core Card and Alert cover those.",
    ],
    api: [
      "State maps to SaveLocationCard.isSaved.",
      "Title Text maps to title; Description Text maps to description.",
      "Save Toggle Button maps to onSaveToggle.",
      "Route and Edit Note map to onRouteToLocation and onEditNote.",
    ],
    properties: ["State: Default, Saved", "Title Text, Description Text"],
    accessibility: [
      "Saved changes the glyph and the label, so the state is not carried by colour alone.",
      "The toggle must expose its pressed state to assistive technology.",
      "All three actions keep a 44px target.",
    ],
  },
  {
    componentName: "UserLocationMarker",
    componentSetName: "UserLocationMarker",
    category: "Product / SDK",
    summary:
      "UserLocationMarker shows the device position on the map, with or without a bearing.",
    usage: [
      "Use Hidden when no reliable heading is available.",
      "Use Visible when the device reports a bearing worth showing.",
      "Do not use for POIs; LocationPin covers those.",
    ],
    api: [
      "Heading maps to UserLocationMarker.showHeading.",
      "heading is a bearing in degrees that rotates the cone.",
      "The marker has no text slots.",
      "Position on the canvas belongs to the map renderer.",
    ],
    properties: ["Heading: Hidden, Visible"],
    accessibility: [
      "The heading cone is a distinct shape, so a known bearing is not signalled by colour alone.",
      "The renderer owns the marker's accessible name and position.",
      "The accuracy halo is decorative and must not be announced separately.",
    ],
  },
  {
    componentName: "DynamicIsland",
    componentSetName: "DynamicIsland",
    category: "Platform / Form-Factor",
    summary:
      "DynamicIsland models the iOS bezel pill in its compact, expanded, and minimal states.",
    usage: [
      "Use Compact for the two-slot leading and trailing presentation.",
      "Use Expanded for the full activity view, Minimal for the collapsed dot.",
      "Treat it as a device surface, not a Core component.",
    ],
    api: [
      "State maps to DynamicIsland.islandState (compact, expanded, minimal).",
      "Compact Leading and Trailing Slots map to compactLeading and compactTrailing.",
      "Expanded Content Slot maps to expandedContent.",
      "Minimal Content Slot maps to minimalContent.",
    ],
    properties: ["State: Compact, Expanded, Minimal"],
    accessibility: [
      "The pill sits on the bezel, so it uses the inverse foreground rather than a Surface token.",
      "Host content owns every accessible name; the island itself has none.",
      "State transitions must be announced by the live activity, not inferred from motion.",
    ],
  },
  {
    componentName: "FeedbackCard",
    componentSetName: "FeedbackCard",
    category: "Platform / Form-Factor",
    summary:
      "FeedbackCard collects a rating and a comment about the wayfinding experience.",
    usage: [
      "Use Default for the editable form.",
      "Use Submitting while the submission is in flight.",
      "Use Success for the confirmation that replaces the form.",
    ],
    api: [
      "State covers the editable form, the in-flight submit, and the confirmation.",
      "Title Text maps to title; Description Text maps to description.",
      "Success Text maps to successMessage.",
      "Rating Slot and Comment Slot compose Core Rating and Textarea.",
    ],
    properties: [
      "State: Default, Submitting, Success",
      "Title Text, Description Text, Success Text",
    ],
    accessibility: [
      "Success replaces the form, so nothing reads as still editable after submitting.",
      "Submitting must be announced; the dimmed button alone is not sufficient.",
      "The rating control owns its own accessible value.",
    ],
  },
  {
    componentName: "DirectionStep",
    componentSetName: "DirectionStep",
    category: "Product / SDK",
    summary:
      "DirectionStep presents one turn-by-turn instruction with its distance and duration.",
    usage: [
      "Use inside a route directions list, one step per manoeuvre.",
      "Supply already-localized instruction, distance, and duration strings.",
      "Do not use for generic list rows; use List or Card in Core instead.",
    ],
    api: [
      "Type maps to DirectionStep.type.",
      "Instruction Text maps to DirectionStep.instruction.",
      "Distance Text maps to DirectionStep.distance.",
      "Duration Text maps to DirectionStep.duration.",
    ],
    properties: [
      "Type: Straight, Left, Right, Destination",
      "Instruction Text, Distance Text, Duration Text",
    ],
    accessibility: [
      "Turn glyphs never auto-mirror, so a left turn stays physically left in RTL.",
      "Direction is conveyed by glyph and instruction text, not by color alone.",
      "The step is non-interactive; the surrounding list owns focus order.",
    ],
  },
  {
    componentName: "FloorSelector",
    componentSetName: "FloorSelector",
    category: "Product / SDK",
    summary:
      "FloorSelector switches the active level of a venue using stable floor IDs.",
    usage: [
      "Use VerticalList beside a map on tall surfaces.",
      "Use HorizontalList when vertical space is constrained.",
      "Use CompactStepper when only the current level and stepping matter.",
    ],
    api: [
      "Variant maps to FloorSelector.variant.",
      "Selected Floor Text maps to the option matching FloorSelector.selectedFloor.",
      "Floor n Text maps to FloorSelector.floors[n].shortLabel.",
      "Floor identity and ordering remain product data.",
    ],
    properties: [
      "Variant: VerticalList, HorizontalList, CompactStepper",
      "Selected Floor Text, Floor 2 Text, Floor 3 Text",
    ],
    accessibility: [
      "Every floor target is 44px so it satisfies the shared touch-target contract.",
      "The selected floor uses a filled treatment plus its own text node.",
      "Product code must expose the selected level to assistive technology.",
    ],
  },
  {
    componentName: "LocationPin",
    componentSetName: "LocationPin",
    category: "Product / SDK",
    summary:
      "LocationPin marks a POI on the map and mirrors the selection state of its result row.",
    usage: [
      "Derive Selected from the same canonical POI ID used by POIResultList.",
      "Use Featured for sponsored or promoted results.",
      "Use OffFloor when the POI sits on a level other than the active one.",
    ],
    api: [
      "State maps to the selected, featured, offFloor, and disabled props.",
      "Label Text maps to LocationPin.label or externalLabel.",
      "Number Text maps to LocationPin.number.",
      "Placement and collision handling stay with the map renderer.",
    ],
    properties: [
      "State: Default, Selected, Featured, OffFloor, Disabled",
      "Label Text, Number Text",
    ],
    accessibility: [
      "Off-floor pins render as a hollow ring so the state is not color-only.",
      "Selected pins grow as well as recolor.",
      "Product code must keep pin and result-row selection announced together.",
    ],
  },
  {
    componentName: "MapView",
    componentSetName: "MapView",
    category: "Product / SDK",
    summary:
      "MapView models the map surface, its attribution, and an overlay slot.",
    usage: [
      "Use Empty when the renderer draws the whole surface.",
      "Use Overlay to show where product overlays sit above the canvas.",
      "Do not draw map content in Figma; the canvas is renderer output.",
    ],
    api: [
      "Content maps to MapView children.",
      "Attribution Text maps to renderer attribution supplied by the host app.",
      "mapLabel is an accessibility prop with no visual counterpart.",
    ],
    properties: ["Content: Empty, Overlay", "Attribution Text"],
    accessibility: [
      "The host app must give the map surface an accessible name via mapLabel.",
      "Attribution must remain legible against the rendered basemap.",
      "Collision insets must be supplied to the renderer, not faked with layout.",
    ],
  },
  {
    componentName: "POICard",
    componentSetName: "POICard",
    category: "Product / SDK",
    summary:
      "POICard presents a venue with optional media, metadata, and actions.",
    usage: [
      "Use Basic for text-only venue summaries.",
      "Use Media when licensed venue imagery exists.",
      "Use Full when the card offers actions such as navigate or save.",
    ],
    api: [
      "Content maps to composed POICard anatomy.",
      "Title Text maps to POICard.title.",
      "Subtitle Text maps to POICard.subtitle.",
      "Description Text maps to POICard.description.",
      "Full cards compose a live Button instance for POICard.actions.",
    ],
    properties: [
      "Content: Basic, Media, Full",
      "Title Text, Subtitle Text, Description Text",
    ],
    accessibility: [
      "Actions sit outside the primary selection target so they cannot trigger selection.",
      "Omit Media when no licensed venue imagery is available.",
      "Product code owns the accessible name for the selection target.",
    ],
  },
  {
    componentName: "WayfindingCard",
    componentSetName: "WayfindingCard",
    category: "Product / SDK",
    summary:
      "WayfindingCard is the dismissible shell for route and guidance content.",
    usage: [
      "Use Basic when the card has no heading or close affordance.",
      "Use Titled when the card is dismissible.",
      "Compose route content inside the slot rather than extending this set.",
    ],
    api: [
      "Content maps to the presence of WayfindingCard.title and onClose.",
      "Title Text maps to WayfindingCard.title.",
      "Body Text maps to WayfindingCard.children.",
      "Titled cards compose a live IconButton instance for onClose.",
    ],
    properties: ["Content: Basic, Titled", "Title Text, Body Text"],
    accessibility: [
      "The close control is a live IconButton, so it keeps its 44px target.",
      "Product code must give the close control a localized accessible name.",
      "Dismissal must return focus to the control that opened the card.",
    ],
  },
  {
    componentName: "Link",
    componentSetName: "Link",
    category: "Typography",
    summary:
      "Link presents navigational or inline text actions with a visible focus state.",
    usage: [
      "Use Default when the link should read as an action.",
      "Use Subtle for secondary inline navigation inside dense content.",
      "Use Button when the action commits a product task rather than navigating.",
    ],
    api: [
      "Variant maps to Link.variant.",
      "State maps to focus-visible examples in Code Connect.",
      "Link Text maps to children in Code Connect.",
      "Focus Visible controls the generated focus ring.",
    ],
    properties: [
      "Variant: Default, Subtle",
      "State: Default, Focus",
      "Link Text, Focus Visible",
    ],
    accessibility: [
      "Link text contrast passes in Light and Dark modes.",
      "The Focus state shows the generated keyboard focus ring.",
      "Product code should preserve native link semantics or accessible button semantics.",
    ],
  },
  {
    componentName: "Separator",
    componentSetName: "Separator",
    category: "Layout",
    summary:
      "Separator visually divides related content with horizontal or vertical orientation.",
    usage: [
      "Use Horizontal between stacked sections or menu groups.",
      "Use Vertical inside toolbars and dense inline control groups.",
      "Avoid using Separator as decoration when spacing alone can clarify hierarchy.",
    ],
    api: [
      "Orientation maps to Separator.orientation.",
      "Decorative behavior remains a runtime accessibility concern.",
      "Sizing uses component float variables for consistent thickness.",
    ],
    properties: ["Orientation: Horizontal, Vertical"],
    accessibility: [
      "Separator uses a foreground boundary token with non-text contrast headroom.",
      "Product code should mark purely visual separators as decorative.",
      "Separator is non-interactive.",
    ],
  },
  {
    componentName: "Skeleton",
    componentSetName: "Skeleton",
    category: "Feedback",
    summary:
      "Skeleton reserves space while content is loading, using non-interactive placeholder shapes.",
    usage: [
      "Use Line for text rows and metadata placeholders.",
      "Use Block for cards, media, and larger content regions.",
      "Use Circle for avatar or icon placeholders.",
    ],
    api: [
      "Shape maps to common Skeleton composition examples.",
      "Animation remains a runtime concern in React.",
      "Sizing uses component float variables for stable placeholders.",
    ],
    properties: ["Shape: Line, Block, Circle"],
    accessibility: [
      "Skeleton is non-interactive and should not receive focus.",
      "Product code should expose loading status when the wait is meaningful.",
      "Placeholder contrast is visual only and does not communicate state by itself.",
    ],
  },
  {
    componentName: "Box",
    componentSetName: "Box",
    category: "Layout",
    summary:
      "Box provides a simple composition surface for arbitrary child content.",
    usage: [
      "Use Transparent when Box only owns spacing or semantic structure.",
      "Use Surface or Outlined when the child content needs a visible boundary.",
      "Prefer Card when the content has a title, description, and actions.",
    ],
    api: [
      "Surface maps to common Box composition examples.",
      "Content Slot maps to Box children in Code Connect.",
      "Runtime Box remains a lightweight polymorphic wrapper.",
    ],
    properties: ["Surface: Transparent, Surface, Outlined", "Content Slot"],
    accessibility: [
      "Box is non-interactive unless composed around an interactive child.",
      "Placeholder text contrast passes in Light and Dark modes.",
      "Visible surfaces use shared radius, padding, and boundary variables.",
    ],
  },
  {
    componentName: "Stack",
    componentSetName: "Stack",
    category: "Layout",
    summary:
      "Stack arranges child content in a row or column with consistent spacing.",
    usage: [
      "Use Column for vertical field groups, panels, and content blocks.",
      "Use Row for inline control groups and compact metadata.",
      "Use these Figma variants as canonical layout examples, not every possible flexbox combination.",
    ],
    api: [
      "Direction maps to Stack.direction.",
      "Gap maps to Stack.gap.",
      "Content Slot maps to Stack children in Code Connect.",
      "Alignment, justification, and wrapping remain product-code composition choices.",
    ],
    properties: ["Direction: Column, Row", "Gap: 2, 4, 6", "Content Slot"],
    accessibility: [
      "Stack is non-interactive unless composed around interactive children.",
      "Spacing does not communicate state by itself.",
      "Child controls must preserve their own labels, focus states, and touch targets.",
    ],
  },
  {
    componentName: "Grid",
    componentSetName: "Grid",
    category: "Layout",
    summary:
      "Grid arranges child content into repeatable columns with consistent gaps.",
    usage: [
      "Use for card groups, metric groups, compact media grids, and repeated content blocks.",
      "Use Stack when content is a single row or column.",
      "Keep Grid as a layout primitive; product rows, data cards, and map content remain composed children.",
    ],
    api: [
      "Columns maps to Grid.cols.",
      "Gap maps to Grid.gap or platform spacing.",
      "Cell slots map to Grid children in row-major order in Code Connect.",
      "Alignment, item spans, responsive breakpoints, and dense layout behavior remain product-code composition choices.",
    ],
    properties: [
      "Columns: 1, 2, 3, 4",
      "Gap: 2, 4, 6",
      "Cell 1 Slot through visible cell slots",
    ],
    accessibility: [
      "Grid is non-interactive unless composed around interactive children.",
      "Column count must not be the only way hierarchy or meaning is communicated.",
      "Child controls must preserve their own labels, focus states, and touch targets.",
    ],
  },
  {
    componentName: "ScrollArea",
    componentSetName: "ScrollArea",
    category: "Layout",
    summary:
      "ScrollArea defines a bounded viewport for overflow content with optional scrollbar affordances.",
    usage: [
      "Use when content needs a constrained viewport inside a panel, menu, or picker.",
      "Use Vertical for most content lists and Both only for canvas-like or table-like regions.",
      "Avoid hiding scrollbars when discoverability matters more than visual quietness.",
    ],
    api: [
      "Orientation maps to ScrollArea.orientation.",
      "Scrollbar maps to visible or hidden scrollbar examples.",
      "Content Slot maps to ScrollArea children in Code Connect.",
      "Native scroll physics, momentum, and keyboard scrolling remain runtime concerns.",
    ],
    properties: [
      "Orientation: Vertical, Horizontal, Both",
      "Scrollbar: Hidden, Visible",
      "Content Slot",
    ],
    accessibility: [
      "Scrollable regions should preserve keyboard and screen reader access to all content.",
      "Visible scrollbar examples pass non-text contrast in both modes.",
      "Child controls keep their own labels, focus states, and touch targets.",
    ],
  },
  {
    componentName: "Container",
    componentSetName: "Container",
    category: "Layout",
    summary:
      "Container provides responsive page gutters and optional centered max-width composition.",
    usage: [
      "Use Centered True for regular page sections and bounded reading layouts.",
      "Use Centered False when the layout should fill the available width.",
      "Keep Container focused on outer page structure; use Box or Card for local surfaces.",
    ],
    api: [
      "Centered maps to Container.centered.",
      "Content Slot maps to Container children in Code Connect.",
      "Responsive breakpoints and max-width behavior remain runtime CSS concerns.",
    ],
    properties: ["Centered: True, False", "Content Slot"],
    accessibility: [
      "Container is non-interactive unless composed around interactive children.",
      "Gutters and max-width should support readable line length without hiding content.",
      "Child controls must preserve their own semantic labels and focus states.",
    ],
  },
  {
    componentName: "BottomNavigation",
    componentSetName: "BottomNavigation",
    category: "Navigation",
    summary:
      "BottomNavigation provides mobile-first access to the primary destinations in an app.",
    usage: [
      "Use for three to five top-level destinations that users need often.",
      "Use Tabs for local content switching and Sidebar or Navbar for larger app structures.",
      "Keep destination labels short and stable so muscle memory can form.",
    ],
    api: [
      "Count maps to the number of visible navigation items.",
      "Active maps to the selected destination example.",
      "Item text properties map to each destination label.",
      "Runtime code owns safe-area padding, routing, and platform navigation semantics.",
    ],
    properties: [
      "Count: Three, Four, Five",
      "Active: One, Two, Three, Four, Five",
      "Item 1 Text through Item 5 Text",
    ],
    accessibility: [
      "Product code exposes navigation semantics and marks the current destination.",
      "Each item keeps a 44px-or-larger interaction area.",
      "Icon and label contrast passes in active and inactive states.",
    ],
  },
  {
    componentName: "NavigationItem",
    componentSetName: "NavigationItem",
    category: "Navigation",
    summary:
      "NavigationItem is the reusable item primitive for top navigation, side navigation, and compact rails.",
    usage: [
      "Use inside Navbar, Sidebar, Drawer, or other navigation shells instead of hand-built nav rows.",
      "Use Placement to switch between top-bar, side-list, and rail anatomy.",
      "Use Content variants for label-only, icon-led, badge, and trailing-action rows.",
      "Keep product route data, hrefs, and selection callbacks in application code.",
    ],
    api: [
      "Placement maps to NavigationItem.placement.",
      "Content maps to NavigationItem.content and optional icon, badge, and trailing slots.",
      "Leading Icon maps to the default icon instance-swap inside Leading Icon Slot.",
      "Density maps to default and compact row rhythm.",
      "State maps to selected, disabled, focus-visible, and hover preview states.",
      "Label Text maps to NavigationItem children.",
    ],
    properties: [
      "Placement: Top, Side, Rail",
      "Content: Label, Icon Label, Icon Only, Badge, Trailing",
      "Density: Default, Compact",
      "State: Default, Hover, Selected, Focus, Disabled",
      "Label Text",
      "Leading Icon",
      "Leading Icon Slot",
      "Badge Slot",
      "Trailing Slot",
      "Focus Visible",
    ],
    accessibility: [
      "Use aria-current page for the selected route in product code.",
      "Disabled items must suppress activation and expose disabled state.",
      "Icon-only items require an accessible label.",
      "Focus Visible controls the generated focus ring for keyboard navigation.",
    ],
  },
  {
    componentName: "Navbar",
    componentSetName: "Navbar",
    category: "Navigation",
    summary:
      "Navbar provides a horizontal app-level navigation shell with brand, navigation, and optional actions.",
    usage: [
      "Use for desktop and large-screen app navigation where users need persistent top-level destinations.",
      "Use Actions when account, notification, create, or utility controls belong in the top bar.",
      "Use Contextual when a workspace selector, primary action, route items, utilities, and account controls share one bar.",
      "Compose NavigationItem instances inside Navigation Slot for route items.",
      "Prefer BottomNavigation for compact mobile primary navigation and Sidebar for dense app sections.",
    ],
    api: [
      "Content maps to Basic, Actions, or Contextual shell examples.",
      "Logo Slot maps to the leading brand region.",
      "Context Slot maps to workspace, tenant, project, or scope switcher content.",
      "Navigation Slot maps to Navbar children in Code Connect.",
      "Primary Action Slot maps to the main command region.",
      "Actions Slot maps to Navbar.actions in Actions examples.",
      "Utility Slot maps to secondary icon actions.",
      "Account Slot maps to account and profile affordances.",
      "Runtime code owns routing, sticky behavior, responsive collapse, and landmark semantics.",
    ],
    properties: [
      "Content: Basic, Actions, Contextual",
      "Logo Slot",
      "Context Slot",
      "Navigation Slot",
      "Primary Action Slot",
      "Actions Slot",
      "Utility Slot",
      "Account Slot",
    ],
    accessibility: [
      "Product code exposes a navigation landmark when Navbar represents site or app navigation.",
      "Interactive children preserve their own focus states and accessible names.",
      "Navbar itself is a composition shell and is non-interactive by default.",
    ],
  },
  {
    componentName: "Breadcrumb",
    componentSetName: "Breadcrumb",
    category: "Navigation",
    summary:
      "Breadcrumb shows the user's current location inside a navigable hierarchy.",
    usage: [
      "Use Basic for short trails where every parent can remain visible.",
      "Use Ellipsis when middle ancestors need to collapse in constrained layouts.",
      "Keep the current page as plain text, not a link to itself.",
    ],
    api: [
      "Content maps to composed BreadcrumbList examples.",
      "Item text properties map to BreadcrumbLink or BreadcrumbPage children.",
      "Separators and ellipsis remain composition primitives in product code.",
    ],
    properties: [
      "Content: Basic, Ellipsis",
      "Item 1 Text",
      "Item 2 Text",
      "Current Page Text",
    ],
    accessibility: [
      "Breadcrumb uses a nav landmark with aria-label in product code.",
      "Current Page maps to BreadcrumbPage with aria-current page.",
      "Separators and ellipsis are presentational and hidden from assistive tech.",
    ],
  },
  {
    componentName: "Pagination",
    componentSetName: "Pagination",
    category: "Navigation",
    summary:
      "Pagination lets users move through known pages of table, list, and search results.",
    usage: [
      "Use Basic for short ranges where nearby page numbers fit comfortably.",
      "Use Ellipsis when the range is long and middle pages collapse.",
      "Use Compact in constrained panels where total context matters more than direct page jumps.",
    ],
    api: [
      "Content maps to composed PaginationContent examples.",
      "Size maps to PaginationLink.size for page links and previous/next controls.",
      "Page text properties map to PaginationLink children.",
      "Runtime owns hrefs, disabled edge states, and page-change behavior.",
    ],
    properties: [
      "Content: Basic, Ellipsis, Compact",
      "Size: Small, Default",
      "Previous Text, Next Text",
      "Page 1 Text, Page 2 Text, Page 3 Text, Last Page Text, Compact Text",
    ],
    accessibility: [
      "Product code uses a navigation landmark with an accessible pagination label.",
      "The active page maps to aria-current page.",
      "All controls keep a 44px-or-larger visual interaction target.",
    ],
  },
  {
    componentName: "Sidebar",
    componentSetName: "Sidebar",
    category: "Navigation",
    summary:
      "Sidebar provides a vertical app navigation or utility region for dense product surfaces.",
    usage: [
      "Use for persistent sections, filters, tools, or nested navigation in larger app layouts.",
      "Use Sections when navigation items need grouped headings or secondary structure.",
      "Use Tools when secondary actions should pin below the primary navigation.",
      "Use Rail when navigation is icon-led and space is constrained.",
      "Compose NavigationItem instances inside Navigation Slot for persistent destinations.",
      "Pair with Drawer or BottomNavigation on small screens when a persistent sidebar would crowd content.",
    ],
    api: [
      "Content maps to Basic, Sections, Tools, or Rail examples.",
      "Header Slot maps to a leading brand or account region.",
      "Navigation Slot maps to primary Sidebar children in Code Connect.",
      "Tools Slot maps to secondary action rows.",
      "Footer Slot maps to pinned account, status, or support content.",
      "Runtime code owns responsive collapse, route state, and scroll behavior.",
    ],
    properties: [
      "Content: Basic, Sections, Tools, Rail",
      "Header Slot",
      "Navigation Slot",
      "Tools Slot",
      "Footer Slot",
    ],
    accessibility: [
      "Product code exposes appropriate complementary or navigation semantics.",
      "Nested navigation rows preserve their own keyboard, focus, and selected-state behavior.",
      "Sidebar itself is a layout shell and is non-interactive by default.",
    ],
  },
  {
    componentName: "Accordion",
    componentSetName: "Accordion",
    category: "Disclosure",
    summary:
      "Accordion reveals or hides related content inside a compact disclosure group.",
    usage: [
      "Use Closed for the default collapsed row.",
      "Use Open when the item content is visible and belongs under the trigger.",
      "Keep accordions for related, scannable content rather than primary navigation.",
    ],
    api: [
      "State maps to the visual data-state examples for AccordionTrigger and AccordionContent.",
      "Trigger Text maps to AccordionTrigger children.",
      "Content Text maps to AccordionContent children.",
      "Root type, collapsible behavior, and animation remain product-code concerns.",
    ],
    properties: ["State: Closed, Open", "Trigger Text", "Content Text"],
    accessibility: [
      "Product code uses Radix Accordion for keyboard and ARIA behavior.",
      "Trigger rows keep a 44px-or-larger visual target.",
      "Open content remains adjacent to the trigger so context is preserved.",
    ],
  },
  {
    componentName: "Button",
    componentSetName: "Button",
    category: "Actions",
    summary:
      "Buttons trigger intentional product actions. Use them for committed actions in forms, dialogs, toolbars, and focused task flows.",
    usage: [
      "Use Default for the main action in a local context.",
      "Use Destructive only when the action can remove or damage user data.",
      "Use Ghost, Link, or Glass when the surrounding surface already carries visual weight.",
    ],
    api: [
      "Variant maps to Button.variant.",
      "Size maps to Button.size.",
      "State maps to disabled and isLoading in Code Connect.",
      "Label Text maps to children and Icon is an instance-swap slot.",
    ],
    properties: [
      "Variant: Default, Destructive, Outline, Secondary, Ghost, Link, Glass",
      "Size: Default, Small, Large, Icon",
      "State: Default, Disabled, Loading",
      "Label Text, Icon, Focus Visible",
    ],
    accessibility: [
      "All visual sizes meet the 44px minimum interactive target.",
      "Focus Visible controls the generated focus ring used by keyboard navigation.",
      "Text and icon contrast pass in Light and Dark modes.",
    ],
  },
  {
    componentName: "IconButton",
    componentSetName: "IconButton",
    category: "Actions",
    summary:
      "IconButton presents a compact action when the icon is sufficient or an accessible label is supplied in code.",
    usage: [
      "Use for dense toolbars, map controls, and repeated actions.",
      "Prefer a text Button when the action is uncommon or ambiguous.",
      "Swap the Icon property with a curated icon source.",
    ],
    api: [
      "Variant maps to IconButton.variant.",
      "Size maps to IconButton.size: Default to icon, Small to sm, Large to lg.",
      "State maps to disabled and isLoading in Code Connect.",
      "Icon is an instance-swap slot with curated preferred values.",
    ],
    properties: [
      "Variant: Default, Destructive, Outline, Secondary, Ghost, Link, Glass",
      "Size: Default, Small, Large",
      "State: Default, Disabled, Loading",
      "Icon, Focus Visible",
    ],
    accessibility: [
      "All sizes use a 44px interaction frame.",
      "Consumers must provide an accessible name when no visible text exists.",
      "Focus Visible controls the generated focus ring.",
    ],
  },
  {
    componentName: "ToggleButton",
    componentSetName: "ToggleButton",
    category: "Actions",
    summary:
      "ToggleButton switches a single pressed state while keeping the control visually button-like.",
    usage: [
      "Use for toolbar options, view modes, and compact settings that can be on or off.",
      "Use Checkbox or Switch when the label reads as a form setting instead of an action.",
      "Keep copy short; toggle state should remain obvious beside sibling controls.",
    ],
    api: [
      "Variant maps to ToggleButton.variant.",
      "Size maps to ToggleButton.size.",
      "State maps to pressed and disabled in Code Connect.",
      "Label Text maps to children in Code Connect.",
      "Focus Visible controls the generated focus ring.",
    ],
    properties: [
      "Variant: Default, Outline",
      "Size: Small, Default, Large",
      "State: Default, Pressed, Disabled, Focus",
      "Label Text, Focus Visible",
    ],
    accessibility: [
      "Pressed state must be exposed in product code through aria-pressed.",
      "All visual sizes keep a 44px interaction target in the library.",
      "Focus Visible controls the generated keyboard focus ring.",
    ],
  },
  {
    componentName: "SplitButton",
    componentSetName: "SplitButton",
    category: "Actions",
    summary:
      "SplitButton pairs a primary action with a secondary menu of related actions.",
    usage: [
      "Use when one action is clearly primary and related alternatives belong behind a menu.",
      "Prefer a normal Button when there is only one meaningful action.",
      "Keep menu items short and aligned to the primary action context.",
    ],
    api: [
      "Variant maps to SplitButton.variant.",
      "Size maps to SplitButton.size.",
      "State maps to disabled and focus examples in Code Connect.",
      "Label Text maps to children; the chevron trigger remains part of the component anatomy.",
    ],
    properties: [
      "Variant: Default, Secondary, Destructive, Outline",
      "Size: Small, Default, Large",
      "State: Default, Disabled, Focus",
      "Label Text, Focus Visible",
    ],
    accessibility: [
      "The main action and menu trigger must each keep a keyboard reachable control in product code.",
      "Menu trigger code should provide an accessible name such as More options.",
      "Focus Visible controls the generated focus ring around the split control.",
    ],
  },
  {
    componentName: "FloatingActionButton",
    componentSetName: "FloatingActionButton",
    category: "Actions",
    summary:
      "FloatingActionButton presents a high-prominence icon action for mobile, map, and canvas surfaces.",
    usage: [
      "Use for one dominant contextual action, not as a generic toolbar button.",
      "Let the consuming layout own placement; the component itself does not encode fixed screen position.",
      "Prefer Button or IconButton when the action belongs inside regular page flow.",
    ],
    api: [
      "Variant maps to FloatingActionButton.variant.",
      "Size maps to FloatingActionButton.size.",
      "State maps to disabled and focus examples in Code Connect.",
      "Icon is an instance-swap slot with curated preferred values.",
    ],
    properties: [
      "Variant: Default, Secondary, Destructive",
      "Size: Small, Default, Large",
      "State: Default, Disabled, Focus",
      "Icon, Focus Visible",
    ],
    accessibility: [
      "Consumers must provide an accessible name when the action is icon-only.",
      "All sizes are at least 48px and pass the 44px interaction target guidance.",
      "Focus Visible controls the generated keyboard focus ring.",
    ],
  },
  {
    componentName: "Counter",
    componentSetName: "Counter",
    category: "Status",
    summary:
      "Counter presents a compact numeric value, such as counts, unread items, or applied filters.",
    usage: [
      "Use for short numeric values that need a stronger shape than plain text.",
      "Use inside Badge when the count belongs to a label.",
      "Keep Counter non-interactive; wrap it in another control only when the whole control is actionable.",
    ],
    api: [
      "Tone maps to Counter.tone.",
      "Size maps to Counter.size.",
      "Counter Text maps to Counter children in Code Connect.",
      "Badge composes Counter as a hidden nested instance for opt-in count display.",
    ],
    properties: [
      "Tone: Neutral, Brand, Destructive, Inverse",
      "Size: Small, Default",
      "Counter Text",
    ],
    accessibility: [
      "Counter is static status content unless wrapped by an interactive component.",
      "Text contrast passes in Light and Dark modes.",
      "Short numeric text uses tabular sizing to keep repeated counts stable.",
    ],
  },
  {
    componentName: "Badge",
    componentSetName: "Badge",
    category: "Status",
    summary:
      "Badge labels compact metadata, status, or categorization. It is non-interactive by default.",
    usage: [
      "Use for short labels such as state, type, role, or count context.",
      "Keep copy concise so badges remain scannable.",
      "Do not use Badge as a replacement for primary actions.",
    ],
    api: [
      "Variant maps to Badge.variant.",
      "Size maps to Badge.size.",
      "Label Text maps to Badge children in Code Connect.",
      "Icon maps to Badge.icon for icon-sized badges.",
      "Show Counter maps to Badge.showCounter.",
      "Badge count content comes from the exposed nested Counter instance.",
      "Badge can inherit focus-visible classes when composed inside interactive elements.",
    ],
    properties: [
      "Variant: Default, Destructive, Outline, Secondary, Ghost, Link",
      "Size: Default, Small, Large, Icon",
      "Label Text, Icon, Show Counter, nested Counter",
    ],
    accessibility: [
      "Badge is treated as static content unless wrapped by an interactive component.",
      "Text contrast passes in Light and Dark modes.",
      "Icon-sized badges retain a 44px visual frame in this library.",
    ],
  },
  {
    componentName: "Tag",
    componentSetName: "Tag",
    category: "Status",
    summary:
      "Tag labels compact metadata with optional removal while staying visually lighter than selectable chips.",
    usage: [
      "Use for static labels such as category, source, status, or lightweight attributes.",
      "Use Removable only when the tag represents user-managed metadata that can be cleared.",
      "Use Chip when the value is selectable, pressed, or part of a filter interaction.",
    ],
    api: [
      "Variant maps to Tag.variant.",
      "Removable maps to whether a remove affordance is shown.",
      "Label Text maps to Tag children in Code Connect.",
      "Runtime code owns remove callbacks and accessible labels for removable tags.",
    ],
    properties: [
      "Variant: Default, Secondary, Destructive, Outline",
      "Removable: False, True",
      "Label Text",
    ],
    accessibility: [
      "Tag is static metadata unless product code wraps it in a control.",
      "Removable tags should expose a labelled remove action in code.",
      "Text and remove icon contrast pass in Light and Dark modes.",
    ],
  },
  {
    componentName: "Chip",
    componentSetName: "Chip",
    category: "Selection",
    summary:
      "Chip presents compact selectable, filterable, and removable values without turning passive metadata into an action.",
    usage: [
      "Use for applied filters, category choices, and short selected values.",
      "Use Badge for passive metadata that is not selectable or removable.",
      "Keep labels short so wrapped chip groups remain easy to scan.",
    ],
    api: [
      "Variant maps to Chip.variant: neutral, brand, and destructive.",
      "Size maps to Chip.size: sm, default, and lg.",
      "State maps to selected and disabled props in Code Connect.",
      "Removable maps to the presence of Chip.onRemove.",
      "Label Text maps to Chip children.",
    ],
    properties: [
      "Variant: Neutral, Brand, Destructive",
      "Size: Small, Default, Large",
      "State: Default, Selected, Disabled",
      "Removable: False, True",
      "Label Text, Focus Visible",
    ],
    accessibility: [
      "Selectable chips expose pressed state in product code.",
      "Chips use a compact visual pill; product code should preserve a 44px hit area in touch contexts.",
      "Focus Visible controls the generated focus ring for keyboard navigation.",
    ],
  },
  {
    componentName: "SegmentedControl",
    componentSetName: "SegmentedControl",
    category: "Selection",
    summary:
      "SegmentedControl lets users choose one option from a short, visible set.",
    usage: [
      "Use for two or three peer options where the current choice should remain visible.",
      "Use Tabs when the selection changes a content panel.",
      "Use Radio when the list is longer or labels need more descriptive text.",
    ],
    api: [
      "Size maps to SegmentedControl.size.",
      "Active maps to defaultValue or selectedIndex in Code Connect.",
      "State maps to disabled, focus, and error examples.",
      "Item text properties map to segment labels.",
    ],
    properties: [
      "Size: Small, Default, Large",
      "Active: One, Two, Three",
      "State: Default, Focus, Disabled, Error",
      "Item 1 Text, Item 2 Text, Item 3 Text, Focus Visible",
    ],
    accessibility: [
      "Product code exposes the group as a single-choice segmented control.",
      "Each segment keeps a 44px-or-larger interaction frame.",
      "Focus Visible controls the generated focus ring for keyboard navigation.",
    ],
  },
  {
    componentName: "Card",
    componentSetName: "Card",
    category: "Layout",
    summary:
      "Card groups related content and actions on a contained surface with consistent padding, border, radius, and elevation.",
    usage: [
      "Use for contained content blocks, settings panels, summaries, and forms.",
      "Use the Header content option when title and description help scanning.",
      "Use Full only when the card owns its local actions.",
    ],
    api: [
      "Content maps to composed Card anatomy in Code Connect.",
      "Title Text maps to CardTitle children.",
      "Description Text maps to CardDescription children.",
      "Body Text maps to CardContent children.",
      "Full cards compose live Button instances for footer actions.",
    ],
    properties: [
      "Content: Basic, Header, Full",
      "Title Text, Description Text, Body Text",
    ],
    accessibility: [
      "Card is non-interactive unless composed inside or around a control.",
      "Text contrast passes in Light and Dark modes.",
      "Actions inside a Card must retain their own keyboard and touch targets.",
    ],
  },
  {
    componentName: "List",
    componentSetName: "List",
    category: "Data Display",
    summary:
      "List presents a short stack of related items without encoding product-specific row semantics.",
    usage: [
      "Use for simple grouped options, settings, and scannable item lists.",
      "Keep POI rows, taxonomy rows, and dashboard records as product compositions.",
      "Use Compact only where surrounding density is already high.",
    ],
    api: [
      "Density maps to the row spacing example in Code Connect.",
      "Item text properties map to ListItem children.",
      "Product-specific leading icons, metadata, and actions remain composition concerns.",
    ],
    properties: [
      "Density: Default, Compact",
      "Item 1 Text, Item 2 Text, Item 3 Text",
    ],
    accessibility: [
      "List is non-interactive unless composed with interactive children.",
      "Rows preserve readable text contrast in Light and Dark modes.",
      "Product code should use native list semantics when rows are navigable.",
    ],
  },
  {
    componentName: "Table",
    componentSetName: "Table",
    category: "Data Display",
    summary:
      "Table presents structured rows and columns for dashboard, CMS, and data-heavy product surfaces.",
    usage: [
      "Use for comparable values that need column alignment.",
      "Keep sortable headers, row menus, selection, and domain content as composed product patterns.",
      "Use Compact only for dense operational views where scanning speed matters.",
    ],
    api: [
      "Density maps to the row spacing example in Code Connect.",
      "Header and cell text properties map to TableHead and TableCell children.",
      "Selection, sorting, pagination, and empty states remain separate Core or product concerns.",
    ],
    properties: [
      "Density: Default, Compact",
      "Header 1 Text, Header 2 Text, Header 3 Text",
      "Row 1-3 Cell 1-3 Text",
    ],
    accessibility: [
      "Product code keeps native table semantics on web.",
      "Header and body text contrast passes in Light and Dark modes.",
      "Dense tables should preserve readable line height and target sizes for any interactive children.",
    ],
  },
  {
    componentName: "Tree",
    componentSetName: "Tree",
    category: "Data Display",
    summary:
      "Tree provides a semantic composition shell for hierarchical row primitives.",
    usage: [
      "Use Tree as the container for nested navigation, file hierarchies, outlines, and structured object trees.",
      "Compose TreeParentItem and TreeChildItem rows inside the Content Slot instead of relying on fixed example data.",
      "Use List when the content is flat and does not need branch disclosure.",
      "Keep domain-specific item actions, badges, and metadata as composed product content.",
    ],
    api: [
      "Content Slot maps to Tree children in Code Connect and product code.",
      "Density maps to default and compact row rhythm.",
      "TreeParentItem and TreeChildItem own row states, labels, counts, icons, and secondary actions.",
      "Runtime code owns data, keyboard navigation, expansion state, selection callbacks, and nested action handlers.",
    ],
    properties: ["Density: Default, Compact", "Content Slot"],
    accessibility: [
      "Product code should preserve tree, treeitem, and group semantics.",
      "Expandable items expose aria-expanded and selected items expose aria-selected.",
      "Keyboard interaction should support Enter, Space, ArrowRight, and ArrowLeft.",
    ],
  },
  {
    componentName: "TreeParentItem",
    componentSetName: "TreeParentItem",
    category: "Data Display",
    summary:
      "TreeParentItem is the reusable collapsible row primitive for hierarchical trees.",
    usage: [
      "Use TreeParentItem for rows that can expand or collapse child content.",
      "Use Actions when the row exposes secondary tools such as hide or lock.",
      "Use Hover and Selected states to preview the secondary actions and row highlight.",
      "Compose TreeParentItem with TreeChildItem inside Tree for product-specific hierarchies.",
    ],
    api: [
      "Expanded maps to disclosure open or closed state.",
      "Depth maps to indentation examples for nested hierarchy.",
      "State maps to idle, hover, selected, and focus-visible treatment.",
      "Product code owns data, keyboard behavior, selection callbacks, and nested action handlers.",
    ],
    properties: [
      "Content: Basic, Count, Actions",
      "Expanded: False, True",
      "Depth: 0, 1, 2",
      "Density: Default, Compact",
      "State: Idle, Hover, Selected, Focus",
      "Label Text, Count Text",
    ],
    accessibility: [
      "TreeParentItem rows should expose aria-expanded in product code.",
      "Secondary actions must preserve labels, focus states, and event handling.",
      "Hover-only actions need a selected or always-visible equivalent on touch devices.",
    ],
  },
  {
    componentName: "TreeChildItem",
    componentSetName: "TreeChildItem",
    category: "Data Display",
    summary:
      "TreeChildItem is the reusable terminal row primitive for hierarchical trees.",
    usage: [
      "Use TreeChildItem for terminal rows that do not expand or collapse.",
      "Use Actions when the row exposes secondary tools such as edit.",
      "Use Hover and Selected states to preview the secondary actions and row highlight.",
      "Compose TreeChildItem with TreeParentItem inside Tree for product-specific hierarchies.",
    ],
    api: [
      "Depth maps to indentation examples for nested hierarchy.",
      "State maps to idle, hover, selected, and focus-visible treatment.",
      "Product code owns data, keyboard behavior, selection callbacks, and nested action handlers.",
    ],
    properties: [
      "Content: Basic, Count, Actions",
      "Depth: 0, 1, 2",
      "Density: Default, Compact",
      "State: Idle, Hover, Selected, Focus",
      "Label Text, Count Text",
    ],
    accessibility: [
      "TreeChildItem rows should live inside a semantic Tree or equivalent platform hierarchy.",
      "Secondary actions must preserve labels, focus states, and event handling.",
      "Hover-only actions need a selected or always-visible equivalent on touch devices.",
    ],
  },
  {
    componentName: "TreeItem",
    componentSetName: "TreeItem",
    category: "Data Display",
    summary:
      "TreeItem is the compatibility row primitive for hierarchical navigation and management trees.",
    usage: [
      "Prefer TreeParentItem and TreeChildItem for new designs.",
      "Use TreeItem when a single switchable parent/child row primitive is useful.",
      "Use Parent for expandable rows and Child for terminal rows.",
      "Use Actions when a row exposes secondary tools such as hide, lock, or edit.",
      "Use Expanded on Parent rows to show open and closed disclosure states.",
    ],
    api: [
      "Type maps to parent versus child row anatomy.",
      "Depth maps to indentation examples for nested hierarchy.",
      "State maps to idle, hover, selected, and focus-visible row treatment.",
      "Tree composes this row anatomy as a generated example; product code owns actual data and keyboard behavior.",
    ],
    properties: [
      "Type: Parent, Child",
      "Content: Basic, Count, Actions",
      "Expanded: False, True",
      "Depth: 0, 1, 2",
      "Density: Default, Compact",
      "State: Idle, Hover, Selected, Focus",
      "Label Text, Count Text",
    ],
    accessibility: [
      "TreeItem rows should live inside a semantic Tree or equivalent platform hierarchy.",
      "Parent rows need an expanded/collapsed state in product code.",
      "Nested actions must preserve labels, focus states, and event handling.",
    ],
  },
  {
    componentName: "Timeline",
    componentSetName: "Timeline",
    category: "Data Display",
    summary:
      "Timeline presents ordered events, history, progress narratives, or changelog-style records.",
    usage: [
      "Use for chronological status, audit history, activity feeds, and setup progress.",
      "Use Detailed when timestamps or dates help scanning; use Basic when the order itself is enough.",
      "Keep domain-specific cards, route steps, and SDK states as composed product content.",
    ],
    api: [
      "Content maps to Basic and Detailed event examples.",
      "Density maps to vertical row rhythm.",
      "Item time, title, and description text map to composed Timeline children.",
      "Timeline is non-interactive by default; product code owns links, actions, and live updates.",
    ],
    properties: [
      "Content: Basic, Detailed",
      "Density: Default, Compact",
      "Item 1-3 Time, Title, and Description text",
    ],
    accessibility: [
      "Timeline order must remain meaningful in source order.",
      "Marker color is supplemental; text communicates the event state.",
      "Interactive children must preserve their own focus states and target sizes.",
    ],
  },
  {
    componentName: "Tabs",
    componentSetName: "Tabs",
    category: "Navigation",
    summary:
      "Tabs switch between related panels while keeping the current context visible.",
    usage: [
      "Use when two to four peer views belong in the same local context.",
      "Keep tab labels short and specific.",
      "Prefer another navigation pattern when the destination list is long or hierarchical.",
    ],
    api: [
      "Count maps to the number of TabsTrigger examples.",
      "Active maps to Tabs.defaultValue in Code Connect.",
      "State maps to disabled and focus examples for the generated triggers.",
      "Tab 1 Text through Tab 4 Text map to TabsTrigger children.",
    ],
    properties: [
      "Count: Two, Three, Four",
      "Active: One, Two, Three, Four",
      "State: Default, Focus, Disabled",
      "Tab 1 Text, Tab 2 Text, Tab 3 Text, Tab 4 Text, Focus Visible",
    ],
    accessibility: [
      "The tab list keeps a 44px outer interaction frame.",
      "Active, inactive, disabled, and focus-visible text contrast passes in Light and Dark modes.",
      "Product code should pair each trigger with a matching TabsContent panel.",
    ],
  },
  {
    componentName: "Stepper",
    componentSetName: "Stepper",
    category: "Navigation",
    summary:
      "Stepper shows progress through a short ordered flow with completed, current, and upcoming steps.",
    usage: [
      "Use for multi-step forms, checkout flows, and setup tasks with a known sequence.",
      "Keep the number of steps short enough to scan at a glance.",
      "Use Progress for measurable task completion rather than navigable steps.",
    ],
    api: [
      "Count maps to the number of rendered steps.",
      "Current maps to Stepper.currentStep in Code Connect.",
      "Step 1 Text through Step 4 Text map to the steps array.",
      "Completed, current, and upcoming states remain derived from Current.",
    ],
    properties: [
      "Count: 2, 3, 4",
      "Current: 1, 2, 3, 4",
      "Step 1 Text, Step 2 Text, Step 3 Text, Step 4 Text",
    ],
    accessibility: [
      "Product code should expose the current step to assistive technology.",
      "Step labels remain visible beside the visual indicators.",
      "Stepper is non-interactive unless individual steps are composed as controls.",
    ],
  },
  {
    componentName: "Tooltip",
    componentSetName: "Tooltip",
    category: "Overlay",
    summary:
      "Tooltip presents brief contextual help when a control is hovered or receives keyboard focus.",
    usage: [
      "Use for short, supplemental help that does not block the current task.",
      "Keep content concise enough to scan quickly.",
      "Do not place interactive controls inside a Tooltip.",
    ],
    api: [
      "Side maps to TooltipContent.side in Code Connect.",
      "Content Text maps to TooltipContent children.",
      "TooltipProvider and TooltipTrigger remain composition concerns in product code.",
      "Tip renders on the side facing the trigger.",
      "Side Offset uses the shared 4px overlay offset contract.",
    ],
    properties: [
      "Side: Top, Right, Bottom, Left",
      "Content Text",
      "Tip Size: 8",
      "Side Offset: 4",
    ],
    accessibility: [
      "Tooltip content is non-interactive overlay text.",
      "Text, surface, and border contrast pass in Light and Dark modes.",
      "Native platform implementations should preserve hover, focus, and accessible description behavior.",
    ],
  },
  {
    componentName: "Backdrop",
    componentSetName: "Backdrop",
    category: "Overlay",
    summary:
      "Backdrop provides the non-interactive scrim layer behind modal, drawer, and sheet surfaces.",
    usage: [
      "Use Visibility Visible behind blocking overlays when page content should recede.",
      "Use Visibility Hidden as the non-rendering reference that matches hidden runtime behavior.",
      "Keep Backdrop separate from Dialog, Drawer, and BottomSheet content so overlays can share one scrim contract.",
    ],
    api: [
      "Visibility maps to Backdrop.visible in code.",
      "Backdrop does not expose text or icon properties.",
      "Runtime code owns portal placement, animation, and outside-click dismissal.",
    ],
    properties: ["Visibility: Visible, Hidden"],
    accessibility: [
      "Backdrop is not interactive by itself and should not receive focus.",
      "Modal primitives own focus trapping and dismiss behavior.",
      "Scrim color uses shared overlay variables for Light and Dark modes.",
    ],
  },
  {
    componentName: "BottomSheet",
    componentSetName: "BottomSheet",
    category: "Overlay",
    summary:
      "BottomSheet presents mobile-first contextual content from the bottom edge with a drag handle, sheet surface, and optional actions.",
    usage: [
      "Use for compact mobile tasks that should keep the current screen visible.",
      "Use Basic for short details, Form for quick edits, and Footer when the sheet owns confirmation actions.",
      "Prefer Dialog when the workflow must block the whole screen equally across device classes.",
    ],
    api: [
      "Content maps to composed BottomSheetContent anatomy in Code Connect.",
      "Title Text and Description Text map to BottomSheetTitle and BottomSheetDescription children.",
      "Content Slot maps to arbitrary sheet children; Form and Footer variants compose live Input and Button examples.",
      "Footer actions compose live Button instances when Content=Footer.",
      "Backdrop, portal placement, safe-area padding, snap points, and drag gestures remain runtime concerns.",
    ],
    properties: [
      "Content: Basic, Form, Footer",
      "Title Text, Description Text",
      "Content Slot",
      "Secondary Action, Primary Action",
    ],
    accessibility: [
      "Product code should preserve modal focus management through the runtime primitive.",
      "The drag handle is visual only and must not be the only dismiss affordance.",
      "Text, surface, border, and action contrast pass in Light and Dark modes.",
    ],
  },
  {
    componentName: "Dialog",
    componentSetName: "Dialog",
    category: "Overlay",
    summary:
      "Dialog presents focused task content above the page with a scrim, surface, close affordance, and optional footer action.",
    usage: [
      "Use when a user must complete or review a contained task before returning.",
      "Keep title and description concise so the modal context stays clear.",
      "Use Footer only when the dialog owns its confirmation action.",
    ],
    api: [
      "Content maps to composed DialogContent anatomy in Code Connect.",
      "Title Text maps to DialogTitle children.",
      "Description Text maps to DialogDescription children.",
      "Body Text maps to composed dialog body content.",
      "Form content composes live Input instances.",
      "Footer actions compose live Button instances.",
    ],
    properties: [
      "Content: Basic, Form, Footer",
      "Title Text, Description Text, Body Text",
      "Nested Input and Button instances expose their own component properties.",
    ],
    accessibility: [
      "Dialog content uses a mode-aware surface over Overlay/Scrim.",
      "Text, icon, and border contrast pass in Light and Dark modes.",
      "Product code must keep focus trapped and return focus to the trigger.",
    ],
  },
  {
    componentName: "Drawer",
    componentSetName: "Drawer",
    category: "Overlay",
    summary:
      "Drawer presents contextual content from an edge of the viewport without forcing the user into a full modal task.",
    usage: [
      "Use for filters, navigation, map layers, object details, and settings that stay attached to the current screen.",
      "Use Right or Left for side panels and Bottom for mobile-style sheets.",
      "Keep product-specific content in composed dashboard, SDK, or CMS components rather than in Core.",
    ],
    api: [
      "Side maps to DrawerContent.side in Code Connect.",
      "Title Text, Description Text, and Body Text map to composed drawer content.",
      "Footer actions compose live Button instances.",
      "The runtime owns focus trapping, dismiss behavior, and trigger composition.",
    ],
    properties: [
      "Side: Right, Left, Top, Bottom",
      "Title Text, Description Text, Body Text",
    ],
    accessibility: [
      "Drawer content uses Dialog primitives in React and equivalent modal semantics on native platforms.",
      "Product code should label the drawer and return focus to the trigger when dismissed.",
      "Text, surface, and boundary contrast pass in Light and Dark modes.",
    ],
  },
  {
    componentName: "Popover",
    componentSetName: "Popover",
    category: "Overlay",
    summary:
      "Popover presents dismissible contextual content anchored to a trigger without blocking the whole page.",
    usage: [
      "Use for short controls, filters, and supplemental detail tied to one trigger.",
      "Prefer Dialog when the task requires commitment or focus trapping.",
      "Keep content compact enough to dismiss without losing task context.",
    ],
    api: [
      "Side maps to PopoverContent.side in Code Connect.",
      "Title Text and Description Text map to composed children.",
      "Side Offset uses the shared 4px overlay offset contract.",
    ],
    properties: [
      "Side: Top, Right, Bottom, Left",
      "Title Text, Description Text",
      "Side Offset: 4",
    ],
    accessibility: [
      "Popover content is keyboard reachable from its trigger in product code.",
      "Surface, text, icon, and border contrast pass in Light and Dark modes.",
      "Dismiss behavior remains a runtime composition concern.",
    ],
  },
  {
    componentName: "Menu",
    componentSetName: "Menu",
    category: "Overlay",
    summary:
      "Menu presents a compact list of actions or choices from a trigger.",
    usage: [
      "Use for contextual actions where the choices are known and short.",
      "Use Checkbox or Radio content when the menu reflects selectable state.",
      "Avoid long-form content inside Menu; use Popover or Dialog instead.",
    ],
    api: [
      "Content maps to composed MenuContent examples in Code Connect.",
      "Label Text maps to MenuLabel children.",
      "Item text properties map to MenuItem, MenuCheckboxItem, or MenuRadioItem children.",
      "Shortcut Text maps to MenuShortcut children.",
    ],
    properties: [
      "Content: Basic, Checkbox, Radio, Submenu",
      "Label Text, Item 1 Text, Item 2 Text, Item 3 Text, Shortcut Text",
    ],
    accessibility: [
      "Menu items keep a 32px visual row inside a trigger-owned interaction model.",
      "Text, checked marks, and boundaries pass in Light and Dark modes.",
      "Product code must provide roving focus and dismiss behavior through Radix.",
    ],
  },
  {
    componentName: "Toast",
    componentSetName: "Toast",
    category: "Feedback",
    summary:
      "Toast presents transient feedback with title, description, optional action, and close affordance.",
    usage: [
      "Use for non-blocking confirmation or status updates.",
      "Include an action only when there is a clear quick recovery path.",
      "Prefer Alert for persistent status content in page layout.",
    ],
    api: [
      "Content maps to composed Toast children in Code Connect.",
      "Title Text maps to ToastTitle children.",
      "Description Text maps to ToastDescription children.",
      "Action Text maps to ToastAction children when the Action content variant is used.",
    ],
    properties: [
      "Content: Basic, Action",
      "Title Text, Description Text, Action Text",
    ],
    accessibility: [
      "Toast is non-modal status feedback and should use runtime live-region semantics.",
      "Text, action, and close affordance contrast pass in Light and Dark modes.",
      "Viewport placement remains a product-code composition concern.",
    ],
  },
  {
    componentName: "Checkbox",
    componentSetName: "Checkbox",
    category: "Selection",
    summary:
      "Checkbox lets users select one or more independent options, with clear checked, disabled, and error states.",
    usage: [
      "Use for independent settings and multi-select decisions.",
      "Use Error when validation requires user attention.",
      "Keep the label adjacent and specific.",
    ],
    api: [
      "Checked maps to Checkbox.checked.",
      "State maps to disabled and error props in Code Connect.",
      "Label Text maps to Checkbox.label.",
      "Focus follows the React focus-visible ring.",
    ],
    properties: [
      "Checked: Unchecked, Checked",
      "State: Default, Disabled, Error",
      "Label Text, Focus Visible",
    ],
    accessibility: [
      "Rows meet the 44px minimum interactive target.",
      "Control and mark contrast pass in Light and Dark modes.",
      "Focus Visible controls the generated focus ring.",
    ],
  },
  {
    componentName: "Radio",
    componentSetName: "Radio",
    category: "Selection",
    summary:
      "Radio represents one choice inside a mutually exclusive option group.",
    usage: [
      "Use for visible option lists where exactly one choice can be selected.",
      "Use Error when the group requires correction before submission.",
      "Prefer Select when the option list is long or space is constrained.",
    ],
    api: [
      "Checked maps to whether RadioGroup.defaultValue matches RadioGroupItem.value.",
      "State maps to disabled and error props in Code Connect.",
      "Label Text maps to RadioGroupItem.label.",
      "Focus follows the React focus-visible ring.",
    ],
    properties: [
      "Checked: Unchecked, Checked",
      "State: Default, Disabled, Error",
      "Label Text, Focus Visible",
    ],
    accessibility: [
      "Rows meet the 44px minimum interactive target.",
      "Control and checked-dot contrast pass in Light and Dark modes.",
      "Focus Visible controls the generated focus ring.",
    ],
  },
  {
    componentName: "Switch",
    componentSetName: "Switch",
    category: "Selection",
    summary: "Switch toggles an immediate binary setting on or off.",
    usage: [
      "Use for settings that take effect immediately.",
      "Do not use when the choice needs a separate confirmation action.",
      "Use Error sparingly for validation or unavailable system states.",
    ],
    api: [
      "Checked maps to Switch.checked.",
      "State maps to disabled and error props in Code Connect.",
      "Label Text maps to Switch.label.",
      "Focus follows the React focus-visible ring.",
    ],
    properties: [
      "Checked: Unchecked, Checked",
      "State: Default, Disabled, Error",
      "Label Text, Focus Visible",
    ],
    accessibility: [
      "Rows meet the 44px minimum interactive target.",
      "Track and thumb contrast pass in Light and Dark modes.",
      "Focus Visible controls the generated focus ring.",
    ],
  },
  {
    componentName: "Input",
    componentSetName: "Input",
    category: "Forms",
    summary:
      "Input captures a single line of user text with label, placeholder, optional helper text, and validation status.",
    usage: [
      "Use labels for persistent context and placeholders for examples only.",
      "Use helper text for validation guidance or successful completion messages.",
      "Readonly communicates fixed values without inviting edits.",
    ],
    api: [
      "State maps to focus, disabled, and readOnly props in Code Connect.",
      "Status maps to default, error, warning, and success validation tones.",
      "Label Text, Placeholder Text, Helper Text, and Show Helper Text map to Input props.",
      "Focus follows the React focus-visible ring.",
    ],
    properties: [
      "State: Default, Focus, Disabled, Readonly",
      "Status: Default, Error, Warning, Success",
      "Label Text, Placeholder Text, Helper Text, Show Helper Text, Focus Visible",
    ],
    accessibility: [
      "Field height meets the 44px minimum target.",
      "Text, placeholder, helper, and border contrast pass in both modes.",
      "Helper text can be hidden while preserving editable content.",
    ],
  },
  {
    componentName: "PasswordInput",
    componentSetName: "PasswordInput",
    category: "Forms",
    summary:
      "PasswordInput captures a private text value with label, helper text, validation status, and an accessible visibility toggle.",
    usage: [
      "Use for credentials, passcodes, and secret values where temporary reveal helps reduce entry errors.",
      "Keep the reveal action adjacent to the field and preserve the user-entered value.",
      "Use helper text for password requirements instead of relying on placeholder-only guidance.",
    ],
    api: [
      "State maps to focus, disabled, and readOnly props in Code Connect.",
      "Status maps to default, error, warning, and success validation tones.",
      "Visibility maps to PasswordInput.defaultVisible in Code Connect examples.",
      "Label Text, Placeholder Text, Helper Text, and Show Helper Text map to PasswordInput props.",
    ],
    properties: [
      "State: Default, Focus, Disabled, Readonly",
      "Status: Default, Error, Warning, Success",
      "Visibility: Hidden, Visible",
      "Label Text, Placeholder Text, Helper Text, Show Helper Text, Focus Visible",
    ],
    accessibility: [
      "Field and visibility toggle meet the 44px minimum target.",
      "The toggle has an accessible name and pressed state in product code.",
      "Text, helper copy, field boundaries, icons, and focus rings pass contrast in both modes.",
    ],
  },
  {
    componentName: "FormField",
    componentSetName: "FormField",
    category: "Forms",
    summary:
      "FormField wraps a control with shared label, description, helper text, validation, optional, and required anatomy.",
    usage: [
      "Use to give inputs, selects, pickers, uploads, and custom controls the same field structure.",
      "Use Description when persistent guidance should sit above the control.",
      "Use Helper or status messages when validation or completion feedback belongs below the control.",
    ],
    api: [
      "Content maps to Basic, Description, Helper, and Full field anatomy examples.",
      "Status maps to default, error, warning, and success message tones.",
      "Required maps to the required marker versus optional text treatment.",
      "Content Slot maps to FieldWrapper/FormField children in Code Connect.",
    ],
    properties: [
      "Content: Basic, Description, Helper, Full",
      "Status: Default, Error, Warning, Success",
      "Required: False, True",
      "Label Text, Description Text, Optional Text, Helper Text, Content Slot",
    ],
    accessibility: [
      "Labels stay associated with the slotted control in product code.",
      "Descriptions and helper/error text should be wired through aria-describedby.",
      "Dynamic error messages should preserve alert behavior where required.",
    ],
  },
  {
    componentName: "NumberInput",
    componentSetName: "NumberInput",
    category: "Forms",
    summary:
      "NumberInput captures numeric values with optional stepper controls, label, helper text, and validation status.",
    usage: [
      "Use for bounded or incremental numeric values such as quantity, floors, capacity, or radius.",
      "Show steppers when quick one-step adjustment is useful.",
      "Hide steppers when keyboard entry is the primary workflow or precision matters.",
    ],
    api: [
      "State maps to disabled and readOnly props in Code Connect.",
      "Status maps to default, error, warning, and success validation tones.",
      "Steppers maps to NumberInput.showSteppers.",
      "Label Text, Value Text, Placeholder Text, Helper Text, and Show Helper Text map to NumberInput props.",
    ],
    properties: [
      "State: Default, Focus, Disabled, Readonly",
      "Status: Default, Error, Warning, Success",
      "Steppers: True, False",
      "Label Text, Value Text, Placeholder Text, Helper Text, Show Helper Text, Focus Visible",
    ],
    accessibility: [
      "Field and stepper controls meet the 44px minimum target.",
      "Text, icons, helper copy, and field boundaries pass contrast in both modes.",
      "Product code preserves native spinbutton semantics and labelled stepper controls.",
    ],
  },
  {
    componentName: "OTPInput",
    componentSetName: "OTPInput",
    category: "Forms",
    summary:
      "OTPInput captures short one-time passcodes or PIN-like verification codes as separated digit cells.",
    usage: [
      "Use for short numeric verification codes that benefit from auto-advance and paste support.",
      "Use 4 digits for PIN-style flows and 6 digits for common one-time passcodes.",
      "Do not use OTPInput for long secrets; use PasswordInput when the whole value should remain hidden.",
    ],
    api: [
      "Length maps to OTPInput.length.",
      "State maps to disabled, readOnly, and focus examples in Code Connect.",
      "Status maps to default, error, warning, and success validation tones.",
      "Digit text properties combine into OTPInput.value in Code Connect examples.",
    ],
    properties: [
      "Length: 4, 6",
      "State: Default, Focus, Disabled, Readonly",
      "Status: Default, Error, Warning, Success",
      "Label Text, Digit 1-6 Text, Helper Text, Show Helper Text, Focus Visible",
    ],
    accessibility: [
      "Each digit cell meets the 44px minimum interactive target.",
      "Every cell has a positional accessible name and shared validation messaging.",
      "Text, helper copy, cell boundaries, and focus rings pass contrast in both modes.",
    ],
  },
  {
    componentName: "Combobox",
    componentSetName: "Combobox",
    category: "Forms",
    summary:
      "Combobox captures or filters a single selection with a searchable field and listbox popup.",
    usage: [
      "Use when users choose one value from a longer or filterable option set.",
      "Use Select when the list is short and search is unnecessary.",
      "Use Listbox or MultiSelect when choices should stay persistently visible.",
    ],
    api: [
      "State maps to focus, disabled, and readOnly props in Code Connect.",
      "Status maps to default, error, warning, and success validation tones.",
      "Content maps to closed field and open listbox anatomy examples.",
      "Label Text, Placeholder Text, Helper Text, option text properties, and Show Helper Text map to Combobox props and children.",
    ],
    properties: [
      "State: Default, Focus, Disabled, Readonly",
      "Status: Default, Error, Warning, Success",
      "Content: Closed, Open",
      "Label Text, Placeholder Text, Option 1 Text, Option 2 Text, Option 3 Text, Helper Text, Show Helper Text, Focus Visible",
    ],
    accessibility: [
      "Field height and listbox options meet the 44px minimum target.",
      "Text, icons, option rows, helper copy, and field boundaries pass contrast in both modes.",
      "Product code preserves ARIA combobox/listbox semantics, active descendant state, and keyboard navigation.",
    ],
  },
  {
    componentName: "MultiSelect",
    componentSetName: "MultiSelect",
    category: "Forms",
    summary:
      "MultiSelect lets users choose several values from a searchable list and review selections as chips.",
    usage: [
      "Use for filters, categories, assigned users, facilities, and other short multi-value fields.",
      "Use Combobox when exactly one value is selected.",
      "Use Listbox when options should remain visible without a field trigger.",
    ],
    api: [
      "State maps to focus, disabled, and readOnly props in Code Connect.",
      "Status maps to default, error, warning, and success validation tones.",
      "Content maps to empty, selected, and open multiselect examples.",
      "Selected values compose live Chip instances for visual and token parity.",
      "Label Text, Placeholder Text, Helper Text, and option text properties map to MultiSelect props and children.",
    ],
    properties: [
      "State: Default, Focus, Disabled, Readonly",
      "Status: Default, Error, Warning, Success",
      "Content: Empty, Selected, Open",
      "Label Text, Placeholder Text, Option 1 Text, Option 2 Text, Option 3 Text, Helper Text, Show Helper Text, Focus Visible",
    ],
    accessibility: [
      "Field controls and listbox options meet the 44px minimum target.",
      "Selected chips, text, icons, helper copy, and field boundaries pass contrast in both modes.",
      "Product code preserves ARIA combobox/listbox semantics with aria-multiselectable and keyboard removal behavior.",
    ],
  },
  {
    componentName: "Listbox",
    componentSetName: "Listbox",
    category: "Forms",
    summary:
      "Listbox presents visible single- or multi-select options with keyboard navigation.",
    usage: [
      "Use when the available options should stay visible while the user chooses.",
      "Use Multiple when users can select more than one independent option.",
      "Use Select, Combobox, or MultiSelect when vertical space is constrained.",
    ],
    api: [
      "Selection maps to Listbox.multiple.",
      "State maps to focus and disabled examples.",
      "Option text and description properties map to Listbox option children.",
      "Focus Visible controls the generated listbox focus ring.",
    ],
    properties: [
      "Selection: Single, Multiple",
      "State: Default, Focus, Disabled",
      "Option 1 Text, Option 1 Description, Option 2 Text, Option 2 Description, Option 3 Text, Option 3 Description, Focus Visible",
    ],
    accessibility: [
      "Listbox and option rows preserve visible focus and active descendant states.",
      "Option text, descriptions, selected icons, and container boundaries pass contrast in both modes.",
      "Product code preserves role=listbox, role=option, aria-selected, and aria-multiselectable where applicable.",
    ],
  },
  {
    componentName: "DatePicker",
    componentSetName: "DatePicker",
    category: "Forms",
    summary:
      "DatePicker captures one date with shared field anatomy and an optional calendar popover example.",
    usage: [
      "Use for dates that benefit from both typed entry and calendar selection.",
      "Use Open when documenting calendar placement, selected day, and keyboard focus behavior.",
      "Prefer DateRangePicker when the task needs a start and end date.",
    ],
    api: [
      "State maps to focus, disabled, and readOnly props in Code Connect.",
      "Status maps to default, error, warning, and success validation tones.",
      "Content maps to closed field and open calendar anatomy examples.",
      "Label Text, Value Text, Month Text, Helper Text, and Show Helper Text map to DatePicker props and examples.",
    ],
    properties: [
      "State: Default, Focus, Disabled, Readonly",
      "Status: Default, Error, Warning, Success",
      "Content: Closed, Open",
      "Label Text, Value Text, Month Text, Helper Text, Show Helper Text, Focus Visible",
    ],
    accessibility: [
      "The field meets the 44px minimum interactive target.",
      "Calendar popup surface, day states, text, icons, helper copy, and boundaries pass contrast in both modes.",
      "Product code preserves native date input semantics and accessible descriptions.",
    ],
  },
  {
    componentName: "DateRangePicker",
    componentSetName: "DateRangePicker",
    category: "Forms",
    summary:
      "DateRangePicker captures a start and end date with shared field anatomy and calendar range examples.",
    usage: [
      "Use when the user needs to choose a bounded time span.",
      "Use Open when documenting range selection, hover range, and two-month calendar layout.",
      "Use DatePicker when only one date is required.",
    ],
    api: [
      "State maps to focus, disabled, and readOnly props in Code Connect.",
      "Status maps to default, error, warning, and success validation tones.",
      "Content maps to closed two-field and open range calendar anatomy examples.",
      "Label Text, Start Label Text, End Label Text, Start Value Text, End Value Text, Helper Text, and month headings map to DateRangePicker props and examples.",
    ],
    properties: [
      "State: Default, Focus, Disabled, Readonly",
      "Status: Default, Error, Warning, Success",
      "Content: Closed, Open",
      "Label Text, Start Label Text, End Label Text, Start Value Text, End Value Text, Start Month Text, End Month Text, Helper Text, Show Helper Text, Focus Visible",
    ],
    accessibility: [
      "Both fields meet the 44px minimum interactive target.",
      "Range states, calendar popup surface, text, icons, helper copy, and boundaries pass contrast in both modes.",
      "Product code preserves labelled start/end inputs and shared described-by messaging.",
    ],
  },
  {
    componentName: "TimePicker",
    componentSetName: "TimePicker",
    category: "Forms",
    summary:
      "TimePicker captures one time value with shared field anatomy and an optional time-list example.",
    usage: [
      "Use for scheduling, operating hours, reminders, and time-specific filters.",
      "Use Open when documenting available step intervals and keyboard focus behavior.",
      "Use NumberInput only when the value is numeric rather than a clock time.",
    ],
    api: [
      "State maps to focus, disabled, and readOnly props in Code Connect.",
      "Status maps to default, error, warning, and success validation tones.",
      "Content maps to closed field and open time-list anatomy examples.",
      "Label Text, Value Text, Helper Text, and option text properties map to TimePicker props and examples.",
    ],
    properties: [
      "State: Default, Focus, Disabled, Readonly",
      "Status: Default, Error, Warning, Success",
      "Content: Closed, Open",
      "Label Text, Value Text, Option 1 Text, Option 2 Text, Option 3 Text, Option 4 Text, Helper Text, Show Helper Text, Focus Visible",
    ],
    accessibility: [
      "The field and list options meet the 44px minimum interactive target.",
      "Time-list surface, option states, text, icons, helper copy, and boundaries pass contrast in both modes.",
      "Product code preserves native time input semantics and accessible descriptions.",
    ],
  },
  {
    componentName: "FileUpload",
    componentSetName: "FileUpload",
    category: "Forms",
    summary:
      "FileUpload lets users select files with click, keyboard, drag-and-drop, and selected-file review anatomy.",
    usage: [
      "Use for imports, attachments, media uploads, evidence collection, and document submission.",
      "Use Dropzone content for the empty drag target and Files content when selected files need review and removal.",
      "Keep DropZone as a FileUpload anatomy alias so product code and Figma stay aligned.",
    ],
    api: [
      "State maps to focus, dragActive, and disabled behavior in Code Connect examples.",
      "Status maps to default, error, warning, and success FieldWrapper validation tones.",
      "Content maps to empty drop-zone and selected-file list anatomy examples.",
      "Label Text, Browse Text, Drop Text, Description Text, File Name Text, File Meta Text, Helper Text, and Show Helper Text map to FileUpload props and examples.",
    ],
    properties: [
      "State: Default, Focus, Dragging, Disabled",
      "Status: Default, Error, Warning, Success",
      "Content: Dropzone, Files",
      "Label Text, Browse Text, Drop Text, Description Text, File Name Text, File Meta Text, Helper Text, Show Helper Text, Focus Visible",
    ],
    accessibility: [
      "The drop zone and selected file remove action meet the 44px minimum interactive target.",
      "Drop-zone boundary, status colors, selected file rows, iconography, and helper copy pass contrast in both modes.",
      "Product code preserves keyboard activation, aria-describedby, aria-invalid, and disabled states.",
    ],
  },
  {
    componentName: "ColorPicker",
    componentSetName: "ColorPicker",
    category: "Forms",
    summary:
      "ColorPicker captures a color value with a shared field, swatch, format-specific color inputs, picker controls, palette selector, presets, and optional picker popover.",
    usage: [
      "Use for theme editing, data visualization palettes, map layer styling, and brand color settings.",
      "Use Closed when a compact color field is enough; use Open to document picker controls, presets, and keyboard focus behavior.",
      "Use a plain text Input when the user only needs to paste a token or non-color string.",
    ],
    api: [
      "State maps to focus, disabled, and readOnly props in Code Connect.",
      "Status maps to default, error, warning, and success validation tones.",
      "Content maps to closed field and open picker popover anatomy examples.",
      "Label Text, Value Text, Format, palette, Helper Text, and preset metadata map to ColorPicker props and examples.",
    ],
    properties: [
      "State: Default, Focus, Disabled, Readonly",
      "Status: Default, Error, Warning, Success",
      "Content: Closed, Open",
      "Format: HSL, RGB, HEX",
      "Label Text, Value Text, Helper Text, Show Helper Text, Focus Visible",
    ],
    accessibility: [
      "The field meets the 44px minimum interactive target.",
      "Field text, helper copy, picker surface, icons, and boundaries pass contrast in both modes.",
      "Product code labels the color input, exposes the editable value, and provides accessible names for swatches and presets.",
    ],
  },
  {
    componentName: "Textarea",
    componentSetName: "Textarea",
    category: "Forms",
    summary:
      "Textarea captures longer freeform text with label, placeholder, interaction state, and error status.",
    usage: [
      "Use when users need more than one line of text.",
      "Keep placeholder examples short enough to avoid visual clutter.",
      "Use Error when the input requires correction.",
    ],
    api: [
      "State maps to focus, disabled, and readOnly props in Code Connect.",
      "Status maps to Textarea.error in Code Connect.",
      "Label Text maps to Textarea.label.",
      "Placeholder Text maps to Textarea.placeholder.",
    ],
    properties: [
      "State: Default, Focus, Disabled, Readonly",
      "Status: Default, Error",
      "Label Text, Placeholder Text, Focus Visible",
    ],
    accessibility: [
      "Default height exceeds the 44px minimum interactive target.",
      "Text and border contrast pass in Light and Dark modes.",
      "Focus Visible controls the generated focus ring.",
    ],
  },
  {
    componentName: "Search",
    componentSetName: "Search",
    category: "Forms",
    summary:
      "Search captures query text with a leading search icon and the same interaction model as Input.",
    usage: [
      "Use for filtering, search pages, and command-like lookup surfaces.",
      "Keep label text available for accessibility even when visually compact.",
      "Use Readonly only when showing a fixed query state.",
    ],
    api: [
      "State maps to focus, disabled, and readOnly props in Code Connect.",
      "Status maps to Search.error in Code Connect.",
      "Label Text maps to Search.label.",
      "Placeholder Text maps to Search.placeholder.",
    ],
    properties: [
      "State: Default, Focus, Disabled, Readonly",
      "Status: Default, Error",
      "Label Text, Placeholder Text, Focus Visible",
    ],
    accessibility: [
      "Field height meets the 44px minimum target.",
      "The search icon and field boundary pass non-text contrast checks.",
      "Focus Visible controls the generated focus ring.",
    ],
  },
  {
    componentName: "SearchBar",
    componentSetName: "SearchBar",
    category: "Forms",
    summary:
      "SearchBar presents a product-level search affordance for prominent lookup surfaces.",
    usage: [
      "Use Inline inside toolbars, headers, and dense product layouts.",
      "Use Floating when search needs stronger separation from a map, canvas, or mobile surface.",
      "Use Search for regular field-level form input and filtering inside forms.",
    ],
    api: [
      "Variant maps to SearchBar.variant.",
      "State maps to default, focus, filled, and disabled examples.",
      "Placeholder Text maps to the empty state label.",
      "Value Text maps to the filled query content.",
      "Focus Visible controls the generated focus ring.",
    ],
    properties: [
      "Variant: Inline, Floating",
      "State: Default, Focus, Filled, Disabled",
      "Placeholder Text, Value Text, Focus Visible",
    ],
    accessibility: [
      "Product code should expose a search landmark or labelled search input when appropriate.",
      "The control keeps a 44px-or-larger interaction target.",
      "Search icon, clear icon, text, boundary, and focus ring contrast pass in both modes.",
    ],
  },
  {
    componentName: "Select",
    componentSetName: "Select",
    category: "Forms",
    summary:
      "SelectTrigger opens a menu of options and displays the selected value or placeholder.",
    usage: [
      "Use when users choose one value from a compact list.",
      "Prefer Radio when all choices should remain visible.",
      "Use Error to indicate invalid or missing selection.",
    ],
    api: [
      "State maps to focus and disabled props in Code Connect.",
      "Status maps to SelectTrigger.error in Code Connect.",
      "Placeholder Text maps to SelectValue.placeholder.",
      "Focus follows the React focus-visible ring.",
    ],
    properties: [
      "State: Default, Focus, Disabled",
      "Status: Default, Error",
      "Placeholder Text, Focus Visible",
    ],
    accessibility: [
      "Trigger height meets the 44px minimum target.",
      "Text, icon, and boundary contrast pass in both modes.",
      "Focus Visible controls the generated focus ring.",
    ],
  },
  {
    componentName: "Slider",
    componentSetName: "Slider",
    category: "Input",
    summary:
      "Slider lets users adjust a bounded numeric value on a visual range.",
    usage: [
      "Use when approximate adjustment is faster than typing.",
      "Pair with a visible value when precision matters.",
      "Use Error for validation states tied to the selected value.",
    ],
    api: [
      "State maps to focus and disabled props in Code Connect.",
      "Status maps to Slider.error in Code Connect.",
      "Type maps to Slider.defaultValue and thumbCount in Code Connect.",
      "Label Text maps to Slider.label.",
      "Focus follows the React focus-visible ring on the thumb.",
    ],
    properties: [
      "State: Default, Focus, Disabled",
      "Status: Default, Error",
      "Type: Single, Range",
      "Label Text, Focus Visible",
    ],
    accessibility: [
      "Overall control height meets the 44px interaction target.",
      "Track and thumb boundaries pass the 3:1 non-text guidance.",
      "Focus Visible controls the generated thumb focus ring.",
    ],
  },
  {
    componentName: "Rating",
    componentSetName: "Rating",
    category: "Input",
    summary:
      "Rating lets users express a compact preference or score across a fixed star scale.",
    usage: [
      "Use when a quick qualitative score is more useful than precise numeric input.",
      "Keep the scale short; five stars is the default Core example.",
      "Use Readonly when showing an existing score rather than asking for input.",
    ],
    api: [
      "Value maps to Rating.value in Code Connect.",
      "State maps to Rating.readOnly.",
      "Max remains five in the Core component; product code can compose a longer scale if needed.",
    ],
    properties: ["Value: 0, 1, 2, 3, 4, 5", "State: Default, Readonly"],
    accessibility: [
      "Product code exposes the control as a radiogroup with one radio per value.",
      "Each star keeps a 44px interaction cell in the Figma component.",
      "Readonly ratings are static score displays and should not accept input.",
    ],
  },
  {
    componentName: "Progress",
    componentSetName: "Progress",
    category: "Feedback",
    summary:
      "Progress communicates completion for determinate loading or task state.",
    usage: [
      "Use when a task has measurable completion.",
      "Use Spinner instead when progress cannot be estimated.",
      "Avoid using Progress as an interactive input.",
    ],
    api: [
      "Value maps to Progress.value in Code Connect.",
      "Value variants cover 0, 25, 50, 75, and 100 percent examples.",
      "Progress is non-interactive and does not expose focus or disabled states.",
    ],
    properties: ["Value: 0, 25, 50, 75, 100"],
    accessibility: [
      "Track and fill pass non-text contrast guidance on Surface/0.",
      "The visual bar is non-interactive, so it does not need a 44px target.",
      "Code should provide an accessible progress value.",
    ],
  },
  {
    componentName: "Spinner",
    componentSetName: "Spinner",
    category: "Feedback",
    summary:
      "Spinner communicates indeterminate loading when duration or completion cannot be predicted.",
    usage: [
      "Use for short waiting states where progress is unknown.",
      "Prefer Progress when completion is measurable.",
      "Pair with status text for longer waits.",
    ],
    api: [
      "Size maps to Spinner.size in Code Connect.",
      "Sizes map to sm, md, lg, and xl runtime values.",
      "Spinner is status feedback and non-interactive.",
    ],
    properties: ["Size: Small, Medium, Large, XLarge"],
    accessibility: [
      "Spinner stroke contrast passes in Light and Dark modes.",
      "Spinner is non-interactive, so it does not need a 44px target.",
      "Code should expose an appropriate loading status when necessary.",
    ],
  },
  {
    componentName: "Avatar",
    componentSetName: "Avatar",
    category: "Identity",
    summary:
      "Avatar represents a person, account, or entity with image content or fallback initials.",
    usage: [
      "Use Image when a reliable profile image is available.",
      "Use Fallback when the image is unavailable or still loading.",
      "Keep fallback text short, usually one or two initials.",
    ],
    api: [
      "Content describes whether the Figma example shows image content or fallback initials.",
      "Image URL maps to AvatarImage.src.",
      "Alt Text maps to AvatarImage.alt.",
      "Fallback maps to AvatarFallback children in Code Connect.",
    ],
    properties: ["Content: Fallback, Image", "Fallback, Image URL, Alt Text"],
    accessibility: [
      "Fallback text contrast passes in Light and Dark modes.",
      "Avatar is non-interactive unless composed inside another control.",
      "Alt Text must describe the person or entity shown by the image.",
    ],
  },
  {
    componentName: "Alert",
    componentSetName: "Alert",
    category: "Feedback",
    summary:
      "Alert presents contextual status content with title, description, tone, and icon.",
    usage: [
      "Use Default for neutral contextual information.",
      "Use Destructive, Warning, Success, or Info to communicate message tone.",
      "Keep descriptions direct and action-oriented.",
    ],
    api: [
      "Variant maps to Alert.variant in Code Connect.",
      "Title maps to AlertTitle children.",
      "Description maps to AlertDescription children.",
      "Alert is non-interactive status content.",
    ],
    properties: [
      "Variant: Default, Destructive, Success, Warning, Info",
      "Title, Description",
    ],
    accessibility: [
      "Text, icon, and border contrast pass in Light and Dark modes.",
      "Alert is non-interactive, so focus states are not exposed.",
      "Use semantic live-region behavior in product code only when the message is dynamic.",
    ],
  },
  {
    componentName: "EmptyState",
    componentSetName: "EmptyState",
    category: "Feedback",
    summary:
      "EmptyState communicates that a view, list, table, or search result has no content to show.",
    usage: [
      "Use Basic when title and description are enough to explain the empty state.",
      "Use Icon when the view benefits from a quick visual cue.",
      "Use Action when there is a clear next step or recovery action.",
    ],
    api: [
      "Content maps to whether the example includes icon and action content.",
      "Title Text maps to EmptyState.title.",
      "Description Text maps to EmptyState.description.",
      "Action content composes a live Button instance in the Figma example.",
    ],
    properties: [
      "Content: Basic, Icon, Action",
      "Title Text, Description Text",
    ],
    accessibility: [
      "EmptyState is static content unless action content is provided.",
      "Title and description text contrast passes in Light and Dark modes.",
      "Action content must preserve its own keyboard and touch target behavior.",
    ],
  },
];
const FOCUS_VISIBLE_PROPERTY_NAME = "Focus Visible";
const DEFAULT_ICON_COMPONENT_NAME = "Icon / Slot Default";
const ICON_PAGE_NAME = "Icons";
const DEFAULT_CURATED_ICON_NAME = "search-md";
const KOSMOS_ICON_DEFINITIONS = [
  {
    name: "activity",
    figmaName: "activity",
    category: "General",
    componentKey: "f78d9ca48d2a538e6ef669730f34436169faa378",
    description: "Activity and status pulse.",
  },
  {
    name: "alert-circle",
    figmaName: "alert-circle",
    category: "Alerts & feedback",
    componentKey: "681cbc55bcc62ab6c335467487239c06204fc121",
    description: "Circular warning or validation alert.",
  },
  {
    name: "alert-triangle",
    figmaName: "alert-triangle",
    category: "Alerts & feedback",
    componentKey: "838860cebb3d8d48570381580f46086f2cbf31f8",
    description: "Triangular warning alert.",
  },
  {
    name: "arrow-left",
    figmaName: "arrow-left",
    category: "Arrows",
    componentKey: "2004c8e9d68cd9900346b47e76aab19298edf5e5",
    description: "Directional arrow left.",
  },
  {
    name: "arrow-right",
    figmaName: "arrow-right",
    category: "Arrows",
    componentKey: "1528623f23da482c4fd66789bdbf4a55c1bc59d8",
    description: "Directional arrow right.",
  },
  {
    name: "bell-01",
    figmaName: "bell-01",
    category: "General",
    componentKey: "3deb23e1ab8a78324a4272df2d048baa50205f39",
    description: "Notification bell.",
  },
  {
    name: "building-01",
    figmaName: "building-01",
    category: "General",
    componentKey: "adc46aca049a4e59317132be8c73d2cfbd786707",
    description: "Building or venue.",
  },
  {
    name: "bus",
    figmaName: "bus",
    category: "Maps & travel",
    componentKey: "4e7536f3a9bc1e09768482fd33ef28414e6b31d6",
    description: "Bus transit mode.",
  },
  {
    name: "calendar",
    figmaName: "calendar",
    category: "Time",
    componentKey: "d4eb472c7167ff6640f51d715e7fb311890d4792",
    description: "Calendar date picker.",
  },
  {
    name: "check",
    figmaName: "check",
    category: "General",
    componentKey: "f601c3bb92fe6cb3a026af1b2a01759bbe89a806",
    description: "Confirmation check mark.",
  },
  {
    name: "chevron-down",
    figmaName: "chevron-down",
    category: "Arrows",
    componentKey: "e896ae2f00a33d950b0f7312e1128bd1c59d5170",
    description: "Chevron down.",
  },
  {
    name: "chevron-left",
    figmaName: "chevron-left",
    category: "Arrows",
    componentKey: "bbeda37dc1dc91216013fc2ad81c3399f44c0c14",
    description: "Chevron left.",
  },
  {
    name: "chevron-right",
    figmaName: "chevron-right",
    category: "Arrows",
    componentKey: "428b0806cb58e6ebee32b2f7cbd64dd9bd85f0e9",
    description: "Chevron right.",
  },
  {
    name: "chevron-up",
    figmaName: "chevron-up",
    category: "Arrows",
    componentKey: "50e56f602a4b43f9963ae7f695fe13ee2b5fa1d6",
    description: "Chevron up.",
  },
  {
    name: "clock",
    figmaName: "clock",
    category: "Time",
    componentKey: "08adbc77ee4c51b32963ccf1fe72ac6a4d3f08bd",
    description: "Clock or time.",
  },
  {
    name: "compass-01",
    figmaName: "compass-01",
    category: "Maps & travel",
    componentKey: "b262b14178d28d97a4b976b72a70bdae0999e7ff",
    description: "Compass navigation.",
  },
  {
    name: "download-01",
    figmaName: "download-01",
    category: "General",
    componentKey: "29ff9dc6686602a1c235b3dbaf9aa4ae3dfad264",
    description: "Download action.",
  },
  {
    name: "edit-01",
    figmaName: "edit-01",
    category: "General",
    componentKey: "44041483f36de252124b379a891fc00dccb059dd",
    description: "Edit action.",
  },
  {
    name: "home-line",
    figmaName: "home-line",
    category: "General",
    componentKey: "8fe986a008470b554d0b47ac63e030d00cd07a5e",
    description: "Home navigation.",
  },
  {
    name: "info-circle",
    figmaName: "info-circle",
    category: "Alerts & feedback",
    componentKey: "2bd38fbeca52d383efd64031e70c330c21f1b609",
    description: "Informational message.",
  },
  {
    name: "lock-01",
    figmaName: "lock-01",
    category: "Security",
    componentKey: "7d57a4eb976ab7f70712747b4667ae4935fbfc0f",
    description: "Locked or private state.",
  },
  {
    name: "map-01",
    figmaName: "map-01",
    category: "Maps & travel",
    componentKey: "b883e50d71e3500426a2d43c1f00ba3825b06426",
    description: "Map view.",
  },
  {
    name: "marker-pin-01",
    figmaName: "marker-pin-01",
    category: "Maps & travel",
    componentKey: "a0d318bbb6e7634b80dc173a7ea8e1f49393e4c5",
    description: "Map pin or destination marker.",
  },
  {
    name: "menu-01",
    figmaName: "menu-01",
    category: "General",
    componentKey: "2354ba287be0cbacd2de01ed47d9eb284eb89b7e",
    description: "Navigation menu.",
  },
  {
    name: "minus",
    figmaName: "minus",
    category: "General",
    componentKey: "bb51a37b247c2c552f2bcaaba31c4a2f5e41966f",
    description: "Remove, collapse, or decrement.",
  },
  {
    name: "navigation-pointer-01",
    figmaName: "navigation-pointer-01",
    category: "Maps & travel",
    componentKey: "a5702e95e86702733e297d347243e79548ad2c5e",
    description: "Navigation pointer.",
  },
  {
    name: "plus",
    figmaName: "plus",
    category: "General",
    componentKey: "19b316378295e3508135be5eae492eaa1f1afe09",
    description: "Add or expand.",
  },
  {
    name: "qr-code-01",
    figmaName: "qr-code-01",
    category: "Development",
    componentKey: "762d574fb5612498060e08dac0fc8682fb900c16",
    description: "QR code.",
  },
  {
    name: "route",
    figmaName: "route",
    category: "Maps & travel",
    componentKey: "bc439a063f5539dd462417c452c979309a44f0d5",
    description: "Route or path.",
  },
  {
    name: "scan",
    figmaName: "scan",
    category: "Security",
    componentKey: "c1f65d4b3b447c5b7fd195446e6409570a696632",
    description: "Scan frame.",
  },
  {
    name: "search-md",
    figmaName: "search-md",
    category: "General",
    componentKey: "ef51678d08d83cc3645c684bec46c1aa7629ef42",
    description: "Search action.",
  },
  {
    name: "settings-01",
    figmaName: "settings-01",
    category: "General",
    componentKey: "c2a934a796c047d31d851feee464f4b8beab765a",
    description: "Settings.",
  },
  {
    name: "trash-01",
    figmaName: "trash-01",
    category: "General",
    componentKey: "5bb204c3a75db71969d618138e6b3322e342e765",
    description: "Delete action.",
  },
  {
    name: "upload-01",
    figmaName: "upload-01",
    category: "General",
    componentKey: "9ce8b7df1a2d0568a277be65cac91d1a6e01fedb",
    description: "Upload action.",
  },
  {
    name: "user-01",
    figmaName: "user-01",
    category: "Users",
    componentKey: "94049ae03d7c9d56b3a2c4e659d3eaa63cfae05b",
    description: "Single user.",
  },
  {
    name: "users-01",
    figmaName: "users-01",
    category: "Users",
    componentKey: "ca6431b25aebf9ceea8af2f4e21478bc498f4b5c",
    description: "User group.",
  },
  {
    name: "wifi",
    figmaName: "wifi",
    category: "Media & devices",
    componentKey: "c421c14e474eec4e3a9d42def105dd146266ecd1",
    description: "Wireless connection.",
  },
  {
    name: "x-close",
    figmaName: "x-close",
    category: "General",
    componentKey: "6c340143b2eb690526e4b6d2c04c6a0a0c03a369",
    description: "Close or dismiss.",
  },
];
const COMPONENT_COLLECTION_NAME = "Kozmos Components";
const TEXT_STYLE_GROUP_TEXT = "Text";
const TEXT_STYLE_GROUP_HEADING = "Heading";
const BUTTON_LABEL_TEXT_STYLE_NAME = "Button / Label";
let activeTextStyleByKey = {};
let activeTextStyleSpecByKey = {};
let activeTextStylesEnsured = false;
const COMPONENT_FLOAT_TOKENS = [
  { name: "Button/width/small", value: 96, scopes: ["WIDTH_HEIGHT"] },
  { name: "Button/width/default", value: 112, scopes: ["WIDTH_HEIGHT"] },
  { name: "Button/width/large", value: 136, scopes: ["WIDTH_HEIGHT"] },
  { name: "Button/width/icon", value: 44, scopes: ["WIDTH_HEIGHT"] },
  { name: "Button/height/small", value: 44, scopes: ["WIDTH_HEIGHT"] },
  { name: "Button/height/default", value: 44, scopes: ["WIDTH_HEIGHT"] },
  { name: "Button/height/large", value: 44, scopes: ["WIDTH_HEIGHT"] },
  { name: "Button/height/icon", value: 44, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Button/padding/x/small",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "Button/padding/x/default",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  {
    name: "Button/padding/x/large",
    value: 32,
    alias: "Layout/spacing/400",
    scopes: ["GAP"],
  },
  {
    name: "Button/padding/x/icon",
    value: 0,
    alias: "Layout/spacing/0",
    scopes: ["GAP"],
  },
  {
    name: "Button/padding/y",
    value: 0,
    alias: "Layout/spacing/0",
    scopes: ["GAP"],
  },
  {
    name: "Button/gap/default",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "Button/gap/icon",
    value: 0,
    alias: "Layout/spacing/0",
    scopes: ["GAP"],
  },
  {
    name: "Button/radius",
    value: 8,
    alias: "Radius/DEFAULT",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Button/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  { name: "Button/label/font-size", value: 14, scopes: ["FONT_SIZE"] },
  { name: "Button/label/line-height", value: 20, scopes: ["LINE_HEIGHT"] },
  {
    name: "Button/icon/size",
    value: 16,
    alias: "Layout/sizing/200",
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "Button/spinner/size", value: 14, scopes: ["WIDTH_HEIGHT"] },
  { name: "IconButton/size/small", value: 44, scopes: ["WIDTH_HEIGHT"] },
  { name: "IconButton/size/default", value: 44, scopes: ["WIDTH_HEIGHT"] },
  { name: "IconButton/size/large", value: 44, scopes: ["WIDTH_HEIGHT"] },
  { name: "IconButton/icon/size/small", value: 14, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "IconButton/icon/size/default",
    value: 16,
    alias: "Layout/sizing/200",
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "IconButton/icon/size/large", value: 20, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "IconButton/spinner/size/small",
    value: 14,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "IconButton/spinner/size/default",
    value: 14,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "IconButton/spinner/size/large",
    value: 16,
    alias: "Layout/sizing/200",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "IconButton/radius",
    value: 9999,
    alias: "Radius/full",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "IconButton/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  { name: "ToggleButton/width/small", value: 96, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "ToggleButton/width/default",
    value: 112,
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "ToggleButton/width/large", value: 136, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "ToggleButton/height/small",
    value: 44,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "ToggleButton/height/default",
    value: 44,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "ToggleButton/height/large",
    value: 44,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "ToggleButton/padding/x/small",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "ToggleButton/padding/x/default",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  {
    name: "ToggleButton/padding/x/large",
    value: 24,
    alias: "Layout/spacing/300",
    scopes: ["GAP"],
  },
  {
    name: "ToggleButton/padding/y",
    value: 0,
    alias: "Layout/spacing/0",
    scopes: ["GAP"],
  },
  {
    name: "ToggleButton/radius",
    value: 12,
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "ToggleButton/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  { name: "SplitButton/width/small", value: 160, scopes: ["WIDTH_HEIGHT"] },
  { name: "SplitButton/width/default", value: 184, scopes: ["WIDTH_HEIGHT"] },
  { name: "SplitButton/width/large", value: 216, scopes: ["WIDTH_HEIGHT"] },
  { name: "SplitButton/height/small", value: 44, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "SplitButton/height/default",
    value: 44,
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "SplitButton/height/large", value: 44, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "SplitButton/main/padding/x/small",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "SplitButton/main/padding/x/default",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  {
    name: "SplitButton/main/padding/x/large",
    value: 24,
    alias: "Layout/spacing/300",
    scopes: ["GAP"],
  },
  {
    name: "SplitButton/trigger/width/small",
    value: 44,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "SplitButton/trigger/width/default",
    value: 44,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "SplitButton/trigger/width/large",
    value: 44,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "SplitButton/radius",
    value: 8,
    alias: "Button/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "SplitButton/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "SplitButton/divider/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "SplitButton/icon/size",
    value: 16,
    alias: "Layout/sizing/200",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "FloatingActionButton/size/small",
    value: 48,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "FloatingActionButton/size/default",
    value: 56,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "FloatingActionButton/size/large",
    value: 64,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "FloatingActionButton/icon/size/small",
    value: 20,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "FloatingActionButton/icon/size/default",
    value: 24,
    alias: "Layout/sizing/300",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "FloatingActionButton/icon/size/large",
    value: 28,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "FloatingActionButton/radius",
    value: 9999,
    alias: "Radius/full",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "FloatingActionButton/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  { name: "Counter/height/small", value: 18, scopes: ["WIDTH_HEIGHT"] },
  { name: "Counter/height/default", value: 20, scopes: ["WIDTH_HEIGHT"] },
  { name: "Counter/min-width/small", value: 18, scopes: ["WIDTH_HEIGHT"] },
  { name: "Counter/min-width/default", value: 20, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Counter/padding/x/small",
    value: 5,
    alias: "Layout/spacing/50",
    scopes: ["GAP"],
  },
  {
    name: "Counter/padding/x/default",
    value: 6,
    alias: "Layout/spacing/75",
    scopes: ["GAP"],
  },
  {
    name: "Counter/radius",
    value: 9999,
    alias: "Radius/full",
    scopes: ["CORNER_RADIUS"],
  },
  { name: "Counter/font-size/small", value: 11, scopes: ["FONT_SIZE"] },
  { name: "Counter/font-size/default", value: 12, scopes: ["FONT_SIZE"] },
  { name: "Counter/line-height/small", value: 14, scopes: ["LINE_HEIGHT"] },
  { name: "Counter/line-height/default", value: 16, scopes: ["LINE_HEIGHT"] },
  {
    name: "Badge/height/small",
    value: 44,
    alias: "Button/height/small",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Badge/height/default",
    value: 44,
    alias: "Button/height/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Badge/height/large",
    value: 44,
    alias: "Button/height/large",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Badge/height/icon",
    value: 44,
    alias: "Button/height/icon",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Badge/width/icon",
    value: 44,
    alias: "Button/width/icon",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Badge/padding/x/small",
    value: 12,
    alias: "Button/padding/x/small",
    scopes: ["GAP"],
  },
  {
    name: "Badge/padding/x/default",
    value: 16,
    alias: "Button/padding/x/default",
    scopes: ["GAP"],
  },
  {
    name: "Badge/padding/x/large",
    value: 32,
    alias: "Button/padding/x/large",
    scopes: ["GAP"],
  },
  {
    name: "Badge/padding/x/icon",
    value: 0,
    alias: "Button/padding/x/icon",
    scopes: ["GAP"],
  },
  {
    name: "Badge/padding/y",
    value: 0,
    alias: "Button/padding/y",
    scopes: ["GAP"],
  },
  { name: "Badge/gap", value: 4, alias: "Layout/spacing/50", scopes: ["GAP"] },
  {
    name: "Badge/icon/size",
    value: 16,
    alias: "Layout/sizing/200",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Badge/counter/height",
    value: 20,
    alias: "Counter/height/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Badge/counter/padding/x",
    value: 6,
    alias: "Counter/padding/x/default",
    scopes: ["GAP"],
  },
  {
    name: "Badge/counter/radius",
    value: 9999,
    alias: "Counter/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Badge/counter/font-size",
    value: 12,
    alias: "Counter/font-size/default",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Badge/counter/line-height",
    value: 16,
    alias: "Counter/line-height/default",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Badge/radius",
    value: 8,
    alias: "Button/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Badge/stroke/width",
    value: 1,
    alias: "Button/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "Badge/label/font-size",
    value: 14,
    alias: "Button/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Badge/label/line-height",
    value: 20,
    alias: "Button/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Chip/height/small",
    value: 28,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Chip/height/default",
    value: 32,
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "Chip/height/large", value: 36, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Chip/padding/x/small",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "Chip/padding/x/default",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  {
    name: "Chip/padding/x/large",
    value: 20,
    scopes: ["GAP"],
  },
  {
    name: "Chip/padding/y",
    value: 0,
    alias: "Layout/spacing/0",
    scopes: ["GAP"],
  },
  { name: "Chip/gap", value: 6, alias: "Layout/spacing/75", scopes: ["GAP"] },
  {
    name: "Chip/remove/size",
    value: 20,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Chip/remove/icon/size",
    value: 12,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Chip/radius",
    value: 9999,
    alias: "Radius/full",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Chip/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "Chip/label/font-size/small",
    value: 12,
    alias: "Text/font-size/xs",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Chip/label/font-size/default",
    value: 14,
    alias: "Text/font-size/sm",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Chip/label/font-size/large",
    value: 14,
    alias: "Text/font-size/sm",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Chip/label/line-height/small",
    value: 16,
    alias: "Text/line-height/xs",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Chip/label/line-height/default",
    value: 20,
    alias: "Text/line-height/sm",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Chip/label/line-height/large",
    value: 20,
    alias: "Text/line-height/sm",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "SegmentedControl/height/small",
    value: 52,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "SegmentedControl/height/default",
    value: 52,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "SegmentedControl/height/large",
    value: 56,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "SegmentedControl/item-height/small",
    value: 44,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "SegmentedControl/item-height/default",
    value: 44,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "SegmentedControl/item-height/large",
    value: 48,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "SegmentedControl/padding",
    value: 4,
    alias: "Layout/spacing/50",
    scopes: ["GAP"],
  },
  {
    name: "SegmentedControl/item-padding/x/small",
    value: 12,
    alias: "Chip/padding/x/small",
    scopes: ["GAP"],
  },
  {
    name: "SegmentedControl/item-padding/x/default",
    value: 16,
    alias: "Chip/padding/x/default",
    scopes: ["GAP"],
  },
  {
    name: "SegmentedControl/item-padding/x/large",
    value: 20,
    alias: "Chip/padding/x/large",
    scopes: ["GAP"],
  },
  {
    name: "SegmentedControl/radius",
    value: 16,
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "SegmentedControl/item-radius",
    value: 12,
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "SegmentedControl/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "SegmentedControl/label/font-size/small",
    value: 12,
    alias: "Chip/label/font-size/small",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "SegmentedControl/label/font-size/default",
    value: 14,
    alias: "Chip/label/font-size/default",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "SegmentedControl/label/font-size/large",
    value: 14,
    alias: "Chip/label/font-size/large",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "SegmentedControl/label/line-height/small",
    value: 16,
    alias: "Chip/label/line-height/small",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "SegmentedControl/label/line-height/default",
    value: 20,
    alias: "Chip/label/line-height/default",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "SegmentedControl/label/line-height/large",
    value: 20,
    alias: "Chip/label/line-height/large",
    scopes: ["LINE_HEIGHT"],
  },
  { name: "Card/width/default", value: 360, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Card/padding",
    value: 24,
    alias: "Layout/spacing/300",
    scopes: ["GAP"],
  },
  {
    name: "Card/header/gap",
    value: 6,
    alias: "Layout/spacing/75",
    scopes: ["GAP"],
  },
  {
    name: "Card/body/gap",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "Card/footer/gap",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "Card/radius",
    value: 16,
    alias: "Radius/lg",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Card/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  { name: "Card/title/font-size", value: 24, scopes: ["FONT_SIZE"] },
  { name: "Card/title/line-height", value: 24, scopes: ["LINE_HEIGHT"] },
  {
    name: "Card/description/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Card/description/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Card/body/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  { name: "Card/body/line-height", value: 22, scopes: ["LINE_HEIGHT"] },
  { name: "List/width/default", value: 360, scopes: ["WIDTH_HEIGHT"] },
  { name: "List/item/height/default", value: 48, scopes: ["WIDTH_HEIGHT"] },
  { name: "List/item/height/compact", value: 40, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "List/item/padding/x",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  {
    name: "List/item/padding/y/default",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "List/item/padding/y/compact",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "List/radius",
    value: 8,
    alias: "Radius/DEFAULT",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "List/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "List/item/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "List/item/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  // Product / SDK geometry. Aliases reuse Core primitives wherever the value
  // already exists so the lane cannot drift from Core spacing and radii.
  { name: "DirectionStep/width/default", value: 360, scopes: ["WIDTH_HEIGHT"] },
  { name: "DirectionStep/height/default", value: 72, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "DirectionStep/icon/container/size",
    value: 40,
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "DirectionStep/icon/size", value: 24, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "DirectionStep/padding",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "DirectionStep/gap",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "DirectionStep/radius",
    value: 8,
    alias: "Radius/DEFAULT",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "DirectionStep/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "DirectionStep/instruction/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "DirectionStep/instruction/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  { name: "DirectionStep/meta/font-size", value: 12, scopes: ["FONT_SIZE"] },
  {
    name: "DirectionStep/meta/line-height",
    value: 16,
    scopes: ["LINE_HEIGHT"],
  },
  { name: "FloorSelector/width/vertical", value: 64, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "FloorSelector/width/horizontal",
    value: 240,
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "FloorSelector/item/size", value: 44, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "FloorSelector/padding",
    value: 4,
    alias: "Layout/spacing/50",
    scopes: ["GAP"],
  },
  {
    name: "FloorSelector/gap",
    value: 4,
    alias: "Layout/spacing/50",
    scopes: ["GAP"],
  },
  {
    name: "FloorSelector/radius",
    value: 8,
    alias: "Radius/DEFAULT",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "FloorSelector/item/radius",
    value: 6,
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "FloorSelector/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  { name: "FloorSelector/label/font-size", value: 14, scopes: ["FONT_SIZE"] },
  {
    name: "FloorSelector/label/line-height",
    value: 20,
    scopes: ["LINE_HEIGHT"],
  },
  { name: "LocationPin/marker/size", value: 32, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "LocationPin/marker/size/selected",
    value: 40,
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "LocationPin/tip/height", value: 10, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "LocationPin/stroke/width",
    value: 2,
    scopes: ["STROKE_FLOAT"],
  },
  { name: "LocationPin/label/font-size", value: 12, scopes: ["FONT_SIZE"] },
  { name: "LocationPin/label/line-height", value: 16, scopes: ["LINE_HEIGHT"] },
  { name: "MapView/width/default", value: 480, scopes: ["WIDTH_HEIGHT"] },
  { name: "MapView/height/default", value: 320, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "MapView/radius",
    value: 8,
    alias: "Radius/DEFAULT",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "MapView/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "MapView/overlay/padding",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  { name: "MapView/attribution/font-size", value: 10, scopes: ["FONT_SIZE"] },
  {
    name: "MapView/attribution/line-height",
    value: 14,
    scopes: ["LINE_HEIGHT"],
  },
  { name: "POICard/width/default", value: 320, scopes: ["WIDTH_HEIGHT"] },
  { name: "POICard/media/height", value: 140, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "POICard/padding",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  {
    name: "POICard/gap",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "POICard/radius",
    value: 12,
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "POICard/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  { name: "POICard/title/font-size", value: 16, scopes: ["FONT_SIZE"] },
  { name: "POICard/title/line-height", value: 24, scopes: ["LINE_HEIGHT"] },
  { name: "POICard/subtitle/font-size", value: 14, scopes: ["FONT_SIZE"] },
  { name: "POICard/subtitle/line-height", value: 20, scopes: ["LINE_HEIGHT"] },
  {
    name: "WayfindingCard/width/default",
    value: 320,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "WayfindingCard/padding",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  {
    name: "WayfindingCard/gap",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "WayfindingCard/radius",
    value: 12,
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "WayfindingCard/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  { name: "WayfindingCard/title/font-size", value: 16, scopes: ["FONT_SIZE"] },
  {
    name: "WayfindingCard/title/line-height",
    value: 24,
    scopes: ["LINE_HEIGHT"],
  },
  { name: "Tree/width/default", value: 320, scopes: ["WIDTH_HEIGHT"] },
  { name: "Tree/item/height/default", value: 40, scopes: ["WIDTH_HEIGHT"] },
  { name: "Tree/item/height/compact", value: 32, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Tree/item/padding/x",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "Tree/item/indent",
    value: 20,
    scopes: ["GAP"],
  },
  {
    name: "Tree/item/radius",
    value: 8,
    alias: "Radius/DEFAULT",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Tree/icon/size",
    value: 16,
    alias: "Layout/sizing/200",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Tree/item/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Tree/item/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  { name: "NavigationItem/width/side", value: 248, scopes: ["WIDTH_HEIGHT"] },
  { name: "NavigationItem/width/rail", value: 72, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "NavigationItem/height/default",
    value: 44,
    alias: "Button/height/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "NavigationItem/height/compact",
    value: 44,
    alias: "Button/height/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "NavigationItem/height/rail", value: 72, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "NavigationItem/padding/x",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "NavigationItem/gap",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "NavigationItem/radius",
    value: 8,
    alias: "Radius/DEFAULT",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "NavigationItem/label/font-size",
    value: 14,
    alias: "Text/font-size/sm",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "NavigationItem/label/line-height",
    value: 20,
    alias: "Text/line-height/sm",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "NavigationItem/rail/font-size",
    value: 12,
    alias: "Text/font-size/xs",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "NavigationItem/rail/line-height",
    value: 16,
    alias: "Text/line-height/xs",
    scopes: ["LINE_HEIGHT"],
  },
  { name: "Table/width/default", value: 720, scopes: ["WIDTH_HEIGHT"] },
  { name: "Table/row/height/default", value: 48, scopes: ["WIDTH_HEIGHT"] },
  { name: "Table/row/height/compact", value: 40, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Table/cell/padding/x",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  {
    name: "Table/cell/padding/y/default",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "Table/cell/padding/y/compact",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "Table/radius",
    value: 8,
    alias: "Radius/DEFAULT",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Table/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "Table/header/font-size",
    value: 12,
    alias: "Text/font-size/xs",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Table/header/line-height",
    value: 16,
    alias: "Text/line-height/xs",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Table/cell/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Table/cell/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Navbar/width/default",
    value: NAVBAR_DEFAULT_WIDTH,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Navbar/height/default",
    value: NAVBAR_DEFAULT_HEIGHT,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Navbar/padding/x",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  {
    name: "Navbar/radius",
    value: 0,
    alias: "Radius/none",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Navbar/logo/font-size",
    value: 14,
    alias: "Text/font-size/sm",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Navbar/logo/line-height",
    value: 20,
    alias: "Text/line-height/sm",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Navbar/item/font-size",
    value: 14,
    alias: "Text/font-size/sm",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Navbar/item/line-height",
    value: 20,
    alias: "Text/line-height/sm",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Navbar/action/font-size",
    value: 14,
    alias: "Button/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Navbar/action/line-height",
    value: 20,
    alias: "Button/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Sidebar/width/default",
    value: SIDEBAR_DEFAULT_WIDTH,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Sidebar/width/rail",
    value: SIDEBAR_RAIL_WIDTH,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Sidebar/height/default",
    value: SIDEBAR_DEFAULT_HEIGHT,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Sidebar/padding/x",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  {
    name: "Sidebar/padding/x/rail",
    value: 4,
    alias: "Layout/spacing/50",
    scopes: ["GAP"],
  },
  {
    name: "Sidebar/item/height/default",
    value: 40,
    alias: "Tree/item/height/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Sidebar/item/radius",
    value: 8,
    alias: "Radius/DEFAULT",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Sidebar/title/font-size",
    value: 14,
    alias: "Text/font-size/sm",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Sidebar/title/line-height",
    value: 20,
    alias: "Text/line-height/sm",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Sidebar/section/font-size",
    value: 12,
    alias: "Text/font-size/xs",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Sidebar/section/line-height",
    value: 16,
    alias: "Text/line-height/xs",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Sidebar/item/font-size",
    value: 14,
    alias: "Text/font-size/sm",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Sidebar/item/line-height",
    value: 20,
    alias: "Text/line-height/sm",
    scopes: ["LINE_HEIGHT"],
  },
  { name: "Tabs/width/two", value: 320, scopes: ["WIDTH_HEIGHT"] },
  { name: "Tabs/width/three", value: 480, scopes: ["WIDTH_HEIGHT"] },
  { name: "Tabs/width/four", value: 640, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Tabs/list/height",
    value: 44,
    alias: "Button/height/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Tabs/list/padding",
    value: 4,
    alias: "Layout/spacing/50",
    scopes: ["GAP"],
  },
  {
    name: "Tabs/list/gap",
    value: 0,
    alias: "Layout/spacing/0",
    scopes: ["GAP"],
  },
  {
    name: "Tabs/list/radius",
    value: 16,
    alias: "Radius/lg",
    scopes: ["CORNER_RADIUS"],
  },
  { name: "Tabs/trigger/height", value: 36, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Tabs/trigger/padding/x",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  { name: "Tabs/trigger/radius", value: 12, scopes: ["CORNER_RADIUS"] },
  {
    name: "Tabs/trigger/font-size",
    value: 14,
    alias: "Button/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Tabs/trigger/line-height",
    value: 20,
    alias: "Button/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  { name: "Checkbox/width/default", value: 176, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Checkbox/height/default",
    value: 44,
    alias: "Button/height/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "Checkbox/control/size", value: 20, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Checkbox/control/radius",
    value: 4,
    alias: "Radius/sm",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Checkbox/control/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "Checkbox/gap",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "Checkbox/padding/x",
    value: 0,
    alias: "Layout/spacing/0",
    scopes: ["GAP"],
  },
  {
    name: "Checkbox/label/font-size",
    value: 14,
    alias: "Button/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Checkbox/label/line-height",
    value: 20,
    alias: "Button/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Radio/width/default",
    value: 176,
    alias: "Checkbox/width/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Radio/height/default",
    value: 44,
    alias: "Checkbox/height/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Radio/control/size",
    value: 20,
    alias: "Checkbox/control/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "Radio/dot/size", value: 10, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Radio/control/radius",
    value: 9999,
    alias: "Radius/full",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Radio/control/stroke/width",
    value: 1,
    alias: "Checkbox/control/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  { name: "Radio/gap", value: 8, alias: "Checkbox/gap", scopes: ["GAP"] },
  {
    name: "Radio/padding/x",
    value: 0,
    alias: "Checkbox/padding/x",
    scopes: ["GAP"],
  },
  {
    name: "Radio/label/font-size",
    value: 14,
    alias: "Checkbox/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Radio/label/line-height",
    value: 20,
    alias: "Checkbox/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Switch/width/default",
    value: 176,
    alias: "Checkbox/width/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Switch/height/default",
    value: 44,
    alias: "Checkbox/height/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Switch/track/width",
    value: 44,
    alias: "Button/width/icon",
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "Switch/track/height", value: 24, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Switch/thumb/size",
    value: 20,
    alias: "Radio/control/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Switch/track/radius",
    value: 9999,
    alias: "Radius/full",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Switch/thumb/radius",
    value: 9999,
    alias: "Radius/full",
    scopes: ["CORNER_RADIUS"],
  },
  { name: "Switch/track/stroke/width", value: 2, scopes: ["STROKE_FLOAT"] },
  { name: "Switch/gap", value: 8, alias: "Checkbox/gap", scopes: ["GAP"] },
  {
    name: "Switch/padding/x",
    value: 0,
    alias: "Checkbox/padding/x",
    scopes: ["GAP"],
  },
  {
    name: "Switch/label/font-size",
    value: 14,
    alias: "Checkbox/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Switch/label/line-height",
    value: 20,
    alias: "Checkbox/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  { name: "Input/width/default", value: 320, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Input/field/height",
    value: 44,
    alias: "Button/height/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Input/field/padding/x",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "Input/field/padding/y",
    value: 0,
    alias: "Layout/spacing/0",
    scopes: ["GAP"],
  },
  {
    name: "Input/field/radius",
    value: 8,
    alias: "Button/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Input/field/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  { name: "Input/gap", value: 6, alias: "Layout/spacing/75", scopes: ["GAP"] },
  {
    name: "Input/label/font-size",
    value: 14,
    alias: "Checkbox/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Input/label/line-height",
    value: 20,
    alias: "Checkbox/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Input/text/font-size",
    value: 14,
    alias: "Checkbox/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Input/text/line-height",
    value: 20,
    alias: "Checkbox/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "FormField/width/default",
    value: 320,
    alias: "Input/width/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "PasswordInput/width/default",
    value: 320,
    alias: "Input/width/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "PasswordInput/field/height",
    value: 44,
    alias: "Input/field/height",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "PasswordInput/field/padding/x",
    value: 12,
    alias: "Input/field/padding/x",
    scopes: ["GAP"],
  },
  {
    name: "PasswordInput/field/padding/y",
    value: 0,
    alias: "Input/field/padding/y",
    scopes: ["GAP"],
  },
  {
    name: "PasswordInput/field/radius",
    value: 8,
    alias: "Input/field/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "PasswordInput/field/stroke/width",
    value: 1,
    alias: "Input/field/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "PasswordInput/gap",
    value: 6,
    alias: "Input/gap",
    scopes: ["GAP"],
  },
  {
    name: "PasswordInput/toggle/size",
    value: 44,
    alias: "Input/field/height",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "PasswordInput/toggle/icon/size",
    value: 16,
    alias: "Button/icon/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "PasswordInput/toggle/radius",
    value: 8,
    alias: "Input/field/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "PasswordInput/label/font-size",
    value: 14,
    alias: "Input/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "PasswordInput/label/line-height",
    value: 20,
    alias: "Input/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "PasswordInput/text/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "PasswordInput/text/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "FormField/gap",
    value: 6,
    alias: "Input/gap",
    scopes: ["GAP"],
  },
  {
    name: "FormField/header/gap",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "FormField/content-slot/height",
    value: 44,
    alias: "Input/field/height",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "FormField/label/font-size",
    value: 14,
    alias: "Input/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "FormField/label/line-height",
    value: 20,
    alias: "Input/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "FormField/text/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "FormField/text/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "FormField/meta/font-size",
    value: 12,
    alias: "Text/font-size/xs",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "FormField/meta/line-height",
    value: 16,
    alias: "Text/line-height/xs",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "NumberInput/width/default",
    value: 320,
    alias: "Input/width/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "NumberInput/field/height",
    value: 44,
    alias: "Input/field/height",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "NumberInput/field/padding/x",
    value: 12,
    alias: "Input/field/padding/x",
    scopes: ["GAP"],
  },
  {
    name: "NumberInput/field/padding/y",
    value: 0,
    alias: "Input/field/padding/y",
    scopes: ["GAP"],
  },
  {
    name: "NumberInput/field/radius",
    value: 8,
    alias: "Input/field/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "NumberInput/field/stroke/width",
    value: 1,
    alias: "Input/field/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "NumberInput/gap",
    value: 6,
    alias: "Input/gap",
    scopes: ["GAP"],
  },
  {
    name: "NumberInput/stepper/size",
    value: 44,
    alias: "Input/field/height",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "NumberInput/stepper/icon/size",
    value: 16,
    alias: "Button/icon/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "NumberInput/divider/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "NumberInput/label/font-size",
    value: 14,
    alias: "Input/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "NumberInput/label/line-height",
    value: 20,
    alias: "Input/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "NumberInput/text/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "NumberInput/text/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "OTPInput/width/default",
    value: 320,
    alias: "Input/width/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "OTPInput/cell/size",
    value: 44,
    alias: "Input/field/height",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "OTPInput/cell/radius",
    value: 8,
    alias: "Input/field/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "OTPInput/cell/stroke/width",
    value: 1,
    alias: "Input/field/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "OTPInput/gap",
    value: 6,
    alias: "Input/gap",
    scopes: ["GAP"],
  },
  {
    name: "OTPInput/cell/gap",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "OTPInput/label/font-size",
    value: 14,
    alias: "Input/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "OTPInput/label/line-height",
    value: 20,
    alias: "Input/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "OTPInput/text/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "OTPInput/text/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Tooltip/padding/x",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "Tooltip/padding/y",
    value: 6,
    alias: "Layout/spacing/75",
    scopes: ["GAP"],
  },
  {
    name: "Tooltip/radius",
    value: 8,
    alias: "Radius/md",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Tooltip/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "Tooltip/tip/size",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Tooltip/tip/height",
    value: 4,
    alias: "Layout/spacing/50",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Tooltip/side-offset",
    value: 4,
    alias: "Layout/spacing/50",
    scopes: ["GAP"],
  },
  {
    name: "Tooltip/content/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Tooltip/content/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  { name: "Dialog/width/default", value: 512, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Dialog/padding",
    value: 24,
    alias: "Layout/spacing/300",
    scopes: ["GAP"],
  },
  {
    name: "Dialog/gap",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  {
    name: "Dialog/header/gap",
    value: 6,
    alias: "Layout/spacing/75",
    scopes: ["GAP"],
  },
  {
    name: "Dialog/body/gap",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "Dialog/footer/gap",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "Dialog/footer/height",
    value: 44,
    alias: "Button/height/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Dialog/radius",
    value: 16,
    alias: "Radius/lg",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Dialog/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  { name: "Dialog/close/size", value: 32, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Dialog/close/icon/size",
    value: 16,
    alias: "Button/icon/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "Dialog/title/font-size", value: 18, scopes: ["FONT_SIZE"] },
  { name: "Dialog/title/line-height", value: 24, scopes: ["LINE_HEIGHT"] },
  {
    name: "Dialog/description/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Dialog/description/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Dialog/body/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Dialog/body/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  { name: "Drawer/width/default", value: 408, scopes: ["WIDTH_HEIGHT"] },
  { name: "Drawer/height/default", value: 360, scopes: ["WIDTH_HEIGHT"] },
  { name: "Drawer/sheet/width", value: 520, scopes: ["WIDTH_HEIGHT"] },
  { name: "Drawer/sheet/height", value: 320, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Drawer/padding",
    value: 24,
    alias: "Layout/spacing/300",
    scopes: ["GAP"],
  },
  {
    name: "Drawer/gap",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  {
    name: "Drawer/header/gap",
    value: 6,
    alias: "Layout/spacing/75",
    scopes: ["GAP"],
  },
  {
    name: "Drawer/footer/gap",
    value: 8,
    alias: "Dialog/footer/gap",
    scopes: ["GAP"],
  },
  {
    name: "Drawer/footer/height",
    value: 44,
    alias: "Dialog/footer/height",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Drawer/radius",
    value: 8,
    alias: "Radius/DEFAULT",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Drawer/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  { name: "BottomSheet/width/default", value: 360, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "BottomSheet/padding/x",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  {
    name: "BottomSheet/padding/top",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "BottomSheet/padding/bottom",
    value: 24,
    alias: "Layout/spacing/300",
    scopes: ["GAP"],
  },
  {
    name: "BottomSheet/gap",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "BottomSheet/header/gap",
    value: 6,
    alias: "Layout/spacing/75",
    scopes: ["GAP"],
  },
  {
    name: "BottomSheet/footer/height",
    value: 44,
    alias: "Drawer/footer/height",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "BottomSheet/footer/gap",
    value: 8,
    alias: "Drawer/footer/gap",
    scopes: ["GAP"],
  },
  { name: "BottomSheet/handle/width", value: 48, scopes: ["WIDTH_HEIGHT"] },
  { name: "BottomSheet/handle/height", value: 5, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "BottomSheet/radius",
    value: 16,
    alias: "Radius/lg",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "BottomSheet/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "Drawer/close/size",
    value: 44,
    alias: "Button/height/icon",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Drawer/close/icon/size",
    value: 16,
    alias: "Button/icon/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Drawer/title/font-size",
    value: 18,
    alias: "Dialog/title/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Drawer/title/line-height",
    value: 24,
    alias: "Dialog/title/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Drawer/description/font-size",
    value: 14,
    alias: "Dialog/description/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Drawer/description/line-height",
    value: 20,
    alias: "Dialog/description/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Drawer/body/font-size",
    value: 14,
    alias: "Dialog/body/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Drawer/body/line-height",
    value: 20,
    alias: "Dialog/body/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  { name: "Popover/width/default", value: 288, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Popover/padding",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  {
    name: "Popover/gap",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  {
    name: "Popover/header/gap",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "Popover/radius",
    value: 8,
    alias: "Radius/md",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Popover/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "Popover/side-offset",
    value: 4,
    alias: "Layout/spacing/50",
    scopes: ["GAP"],
  },
  {
    name: "Popover/title/font-size",
    value: 14,
    alias: "Input/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Popover/title/line-height",
    value: 20,
    alias: "Input/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Popover/description/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Popover/description/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  { name: "Menu/width/default", value: 192, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Menu/padding",
    value: 4,
    alias: "Layout/spacing/50",
    scopes: ["GAP"],
  },
  {
    name: "Menu/radius",
    value: 8,
    alias: "Radius/md",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Menu/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  { name: "Menu/item/height", value: 32, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Menu/item/padding/x",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "Menu/item/inset/padding-left",
    value: 32,
    alias: "Layout/spacing/400",
    scopes: ["GAP"],
  },
  {
    name: "Menu/item/radius",
    value: 4,
    alias: "Radius/sm",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Menu/item/gap",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "Menu/icon/size",
    value: 16,
    alias: "Button/icon/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Menu/separator/height",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Menu/text/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Menu/text/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  { name: "Menu/shortcut/font-size", value: 12, scopes: ["FONT_SIZE"] },
  { name: "Menu/shortcut/line-height", value: 16, scopes: ["LINE_HEIGHT"] },
  { name: "Toast/width/default", value: 420, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Toast/padding",
    value: 24,
    alias: "Layout/spacing/300",
    scopes: ["GAP"],
  },
  {
    name: "Toast/padding/right",
    value: 32,
    alias: "Layout/spacing/400",
    scopes: ["GAP"],
  },
  {
    name: "Toast/gap",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  {
    name: "Toast/text/gap",
    value: 4,
    alias: "Layout/spacing/50",
    scopes: ["GAP"],
  },
  {
    name: "Toast/radius",
    value: 8,
    alias: "Radius/md",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Toast/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  { name: "Toast/action/height", value: 32, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Toast/action/padding/x",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "Toast/action/radius",
    value: 8,
    alias: "Radius/md",
    scopes: ["CORNER_RADIUS"],
  },
  { name: "Toast/close/size", value: 24, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Toast/close/icon/size",
    value: 16,
    alias: "Button/icon/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Toast/title/font-size",
    value: 14,
    alias: "Input/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Toast/title/line-height",
    value: 20,
    alias: "Input/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Toast/description/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Toast/description/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Textarea/width/default",
    value: 320,
    alias: "Input/width/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "Textarea/field/height", value: 80, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Textarea/field/padding/x",
    value: 12,
    alias: "Input/field/padding/x",
    scopes: ["GAP"],
  },
  {
    name: "Textarea/field/padding/y",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "Textarea/field/radius",
    value: 8,
    alias: "Input/field/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Textarea/field/stroke/width",
    value: 1,
    alias: "Input/field/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  { name: "Textarea/gap", value: 6, alias: "Input/gap", scopes: ["GAP"] },
  {
    name: "Textarea/label/font-size",
    value: 14,
    alias: "Input/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Textarea/label/line-height",
    value: 20,
    alias: "Input/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Textarea/text/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Textarea/text/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Search/width/default",
    value: 320,
    alias: "Input/width/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Search/field/height",
    value: 44,
    alias: "Input/field/height",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Search/field/padding/x",
    value: 12,
    alias: "Input/field/padding/x",
    scopes: ["GAP"],
  },
  {
    name: "Search/field/padding/y",
    value: 0,
    alias: "Input/field/padding/y",
    scopes: ["GAP"],
  },
  {
    name: "Search/field/radius",
    value: 8,
    alias: "Input/field/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Search/field/stroke/width",
    value: 1,
    alias: "Input/field/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  { name: "Search/gap", value: 6, alias: "Input/gap", scopes: ["GAP"] },
  {
    name: "Search/icon/size",
    value: 16,
    alias: "Button/icon/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Search/label/font-size",
    value: 14,
    alias: "Input/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Search/label/line-height",
    value: 20,
    alias: "Input/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Search/text/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Search/text/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Select/width/default",
    value: 320,
    alias: "Input/width/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Select/trigger/height",
    value: 44,
    alias: "Input/field/height",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Select/trigger/padding/x",
    value: 12,
    alias: "Input/field/padding/x",
    scopes: ["GAP"],
  },
  {
    name: "Select/trigger/padding/y",
    value: 0,
    alias: "Input/field/padding/y",
    scopes: ["GAP"],
  },
  {
    name: "Select/trigger/radius",
    value: 8,
    alias: "Input/field/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Select/trigger/stroke/width",
    value: 1,
    alias: "Input/field/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "Select/icon/size",
    value: 16,
    alias: "Button/icon/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Select/text/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Select/text/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Combobox/width/default",
    value: 320,
    alias: "Input/width/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Combobox/field/height",
    value: 44,
    alias: "Input/field/height",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Combobox/field/padding/x",
    value: 12,
    alias: "Input/field/padding/x",
    scopes: ["GAP"],
  },
  {
    name: "Combobox/field/padding/y",
    value: 0,
    alias: "Input/field/padding/y",
    scopes: ["GAP"],
  },
  {
    name: "Combobox/field/radius",
    value: 8,
    alias: "Input/field/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Combobox/field/stroke/width",
    value: 1,
    alias: "Input/field/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "Combobox/gap",
    value: 6,
    alias: "Input/gap",
    scopes: ["GAP"],
  },
  {
    name: "Combobox/icon/size",
    value: 16,
    alias: "Select/icon/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Combobox/listbox/width",
    value: 320,
    alias: "Input/width/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Combobox/listbox/padding",
    value: 4,
    alias: "Layout/spacing/50",
    scopes: ["GAP"],
  },
  {
    name: "Combobox/listbox/gap",
    value: 2,
    scopes: ["GAP"],
  },
  {
    name: "Combobox/listbox/radius",
    value: 8,
    alias: "Radius/DEFAULT",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Combobox/listbox/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "Combobox/option/height",
    value: 44,
    alias: "Button/height/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Combobox/option/padding/x",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "Combobox/option/padding/y",
    value: 0,
    alias: "Layout/spacing/0",
    scopes: ["GAP"],
  },
  {
    name: "Combobox/option/radius",
    value: 4,
    alias: "Radius/sm",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Combobox/label/font-size",
    value: 14,
    alias: "Input/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Combobox/label/line-height",
    value: 20,
    alias: "Input/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Combobox/text/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Combobox/text/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "MultiSelect/width/default",
    value: 320,
    alias: "Input/width/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "MultiSelect/field/min-height",
    value: 44,
    alias: "Input/field/height",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "MultiSelect/field/padding/x",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "MultiSelect/field/padding/y",
    value: 4,
    alias: "Layout/spacing/50",
    scopes: ["GAP"],
  },
  {
    name: "MultiSelect/field/gap",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "MultiSelect/field/radius",
    value: 8,
    alias: "Input/field/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "MultiSelect/field/stroke/width",
    value: 1,
    alias: "Input/field/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "MultiSelect/gap",
    value: 6,
    alias: "Input/gap",
    scopes: ["GAP"],
  },
  {
    name: "MultiSelect/chip/height",
    value: 24,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "MultiSelect/chip/padding/x",
    value: 12,
    alias: "Chip/padding/x/small",
    scopes: ["GAP"],
  },
  {
    name: "MultiSelect/chip/radius",
    value: 9999,
    alias: "Chip/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "MultiSelect/icon/size",
    value: 16,
    alias: "Combobox/icon/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "MultiSelect/listbox/width",
    value: 320,
    alias: "Combobox/listbox/width",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "MultiSelect/listbox/padding",
    value: 4,
    alias: "Combobox/listbox/padding",
    scopes: ["GAP"],
  },
  {
    name: "MultiSelect/listbox/gap",
    value: 2,
    alias: "Combobox/listbox/gap",
    scopes: ["GAP"],
  },
  {
    name: "MultiSelect/listbox/radius",
    value: 8,
    alias: "Combobox/listbox/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "MultiSelect/listbox/stroke/width",
    value: 1,
    alias: "Combobox/listbox/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "MultiSelect/option/height",
    value: 44,
    alias: "Combobox/option/height",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "MultiSelect/option/padding/x",
    value: 12,
    alias: "Combobox/option/padding/x",
    scopes: ["GAP"],
  },
  {
    name: "MultiSelect/option/radius",
    value: 4,
    alias: "Combobox/option/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "MultiSelect/label/font-size",
    value: 14,
    alias: "Input/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "MultiSelect/label/line-height",
    value: 20,
    alias: "Input/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "MultiSelect/text/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "MultiSelect/text/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Listbox/width/default",
    value: 320,
    alias: "Combobox/listbox/width",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Listbox/padding",
    value: 4,
    alias: "Combobox/listbox/padding",
    scopes: ["GAP"],
  },
  {
    name: "Listbox/gap",
    value: 4,
    alias: "Badge/gap",
    scopes: ["GAP"],
  },
  {
    name: "Listbox/radius",
    value: 8,
    alias: "Combobox/listbox/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Listbox/stroke/width",
    value: 1,
    alias: "Combobox/listbox/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "Listbox/option/height",
    value: 56,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Listbox/option/padding/x",
    value: 12,
    alias: "Combobox/option/padding/x",
    scopes: ["GAP"],
  },
  {
    name: "Listbox/option/padding/y",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "Listbox/option/radius",
    value: 4,
    alias: "Combobox/option/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Listbox/icon/size",
    value: 16,
    alias: "Combobox/icon/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Listbox/text/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Listbox/text/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Listbox/description/font-size",
    value: 12,
    alias: "Text/font-size/xs",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Listbox/description/line-height",
    value: 16,
    alias: "Text/line-height/xs",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "DatePicker/width/default",
    value: 320,
    alias: "Input/width/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "DatePicker/field/height",
    value: 44,
    alias: "Input/field/height",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "DatePicker/field/padding/x",
    value: 12,
    alias: "Input/field/padding/x",
    scopes: ["GAP"],
  },
  {
    name: "DatePicker/field/padding/y",
    value: 0,
    alias: "Input/field/padding/y",
    scopes: ["GAP"],
  },
  {
    name: "DatePicker/field/radius",
    value: 8,
    alias: "Input/field/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "DatePicker/field/stroke/width",
    value: 1,
    alias: "Input/field/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "DatePicker/gap",
    value: 6,
    alias: "Input/gap",
    scopes: ["GAP"],
  },
  {
    name: "DatePicker/icon/size",
    value: 16,
    alias: "Combobox/icon/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "DatePicker/calendar/width",
    value: 320,
    alias: "Combobox/listbox/width",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "DatePicker/calendar/padding",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "DatePicker/calendar/gap",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "DatePicker/calendar/radius",
    value: 8,
    alias: "Combobox/listbox/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "DatePicker/calendar/stroke/width",
    value: 1,
    alias: "Combobox/listbox/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "DatePicker/calendar/header/height",
    value: 32,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "DatePicker/calendar/grid/gap",
    value: 4,
    alias: "Layout/spacing/50",
    scopes: ["GAP"],
  },
  {
    name: "DatePicker/calendar/day/size",
    value: 36,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "DatePicker/calendar/day/radius",
    value: 8,
    alias: "Radius/md",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "DatePicker/label/font-size",
    value: 14,
    alias: "Input/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "DatePicker/label/line-height",
    value: 20,
    alias: "Input/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "DatePicker/text/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "DatePicker/text/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "DatePicker/meta/font-size",
    value: 12,
    alias: "Text/font-size/xs",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "DatePicker/meta/line-height",
    value: 16,
    alias: "Text/line-height/xs",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "DateRangePicker/width/default",
    value: 672,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "DateRangePicker/field/width",
    value: 330,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "DateRangePicker/field/height",
    value: 44,
    alias: "DatePicker/field/height",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "DateRangePicker/field/padding/x",
    value: 12,
    alias: "DatePicker/field/padding/x",
    scopes: ["GAP"],
  },
  {
    name: "DateRangePicker/field/padding/y",
    value: 0,
    alias: "DatePicker/field/padding/y",
    scopes: ["GAP"],
  },
  {
    name: "DateRangePicker/field/radius",
    value: 8,
    alias: "DatePicker/field/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "DateRangePicker/field/stroke/width",
    value: 1,
    alias: "DatePicker/field/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "DateRangePicker/gap",
    value: 6,
    alias: "DatePicker/gap",
    scopes: ["GAP"],
  },
  {
    name: "DateRangePicker/field-gap",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "DateRangePicker/field-label/gap",
    value: 6,
    alias: "Input/gap",
    scopes: ["GAP"],
  },
  {
    name: "DateRangePicker/icon/size",
    value: 16,
    alias: "DatePicker/icon/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "DateRangePicker/calendar/width",
    value: 672,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "DateRangePicker/calendar/padding",
    value: 12,
    alias: "DatePicker/calendar/padding",
    scopes: ["GAP"],
  },
  {
    name: "DateRangePicker/calendar/gap",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "DateRangePicker/calendar/radius",
    value: 8,
    alias: "DatePicker/calendar/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "DateRangePicker/calendar/stroke/width",
    value: 1,
    alias: "DatePicker/calendar/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "DateRangePicker/calendar/month/width",
    value: 318,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "DateRangePicker/calendar/day/size",
    value: 40,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "DateRangePicker/calendar/day/radius",
    value: 8,
    alias: "DatePicker/calendar/day/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "DateRangePicker/label/font-size",
    value: 14,
    alias: "DatePicker/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "DateRangePicker/label/line-height",
    value: 20,
    alias: "DatePicker/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "DateRangePicker/text/font-size",
    value: 14,
    alias: "DatePicker/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "DateRangePicker/text/line-height",
    value: 20,
    alias: "DatePicker/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "DateRangePicker/meta/font-size",
    value: 12,
    alias: "DatePicker/meta/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "DateRangePicker/meta/line-height",
    value: 16,
    alias: "DatePicker/meta/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "TimePicker/width/default",
    value: 320,
    alias: "DatePicker/width/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "TimePicker/field/height",
    value: 44,
    alias: "DatePicker/field/height",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "TimePicker/field/padding/x",
    value: 12,
    alias: "DatePicker/field/padding/x",
    scopes: ["GAP"],
  },
  {
    name: "TimePicker/field/padding/y",
    value: 0,
    alias: "DatePicker/field/padding/y",
    scopes: ["GAP"],
  },
  {
    name: "TimePicker/field/radius",
    value: 8,
    alias: "DatePicker/field/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "TimePicker/field/stroke/width",
    value: 1,
    alias: "DatePicker/field/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "TimePicker/gap",
    value: 6,
    alias: "DatePicker/gap",
    scopes: ["GAP"],
  },
  {
    name: "TimePicker/icon/size",
    value: 16,
    alias: "DatePicker/icon/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "TimePicker/listbox/width",
    value: 320,
    alias: "Combobox/listbox/width",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "TimePicker/listbox/padding",
    value: 4,
    alias: "Combobox/listbox/padding",
    scopes: ["GAP"],
  },
  {
    name: "TimePicker/listbox/gap",
    value: 2,
    alias: "Combobox/listbox/gap",
    scopes: ["GAP"],
  },
  {
    name: "TimePicker/listbox/radius",
    value: 8,
    alias: "Combobox/listbox/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "TimePicker/listbox/stroke/width",
    value: 1,
    alias: "Combobox/listbox/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "TimePicker/option/height",
    value: 44,
    alias: "Combobox/option/height",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "TimePicker/option/padding/x",
    value: 12,
    alias: "Combobox/option/padding/x",
    scopes: ["GAP"],
  },
  {
    name: "TimePicker/option/radius",
    value: 4,
    alias: "Combobox/option/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "TimePicker/label/font-size",
    value: 14,
    alias: "DatePicker/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "TimePicker/label/line-height",
    value: 20,
    alias: "DatePicker/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "TimePicker/text/font-size",
    value: 14,
    alias: "DatePicker/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "TimePicker/text/line-height",
    value: 20,
    alias: "DatePicker/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "FileUpload/width/default",
    value: 384,
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "FileUpload/gap", value: 6, alias: "Input/gap", scopes: ["GAP"] },
  {
    name: "FileUpload/dropzone/min-height",
    value: 144,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "FileUpload/dropzone/padding/x",
    value: 16,
    alias: "Input/field/padding/x",
    scopes: ["GAP"],
  },
  {
    name: "FileUpload/dropzone/padding/y",
    value: 24,
    scopes: ["GAP"],
  },
  {
    name: "FileUpload/dropzone/radius",
    value: 8,
    alias: "Combobox/listbox/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "FileUpload/dropzone/stroke/width",
    value: 1,
    alias: "Input/field/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "FileUpload/file-row/height",
    value: 44,
    alias: "Button/height/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "FileUpload/file-row/padding/x",
    value: 12,
    alias: "Input/field/padding/x",
    scopes: ["GAP"],
  },
  {
    name: "FileUpload/file-row/padding/y",
    value: 8,
    scopes: ["GAP"],
  },
  {
    name: "FileUpload/file-row/gap",
    value: 12,
    scopes: ["GAP"],
  },
  {
    name: "FileUpload/file-row/radius",
    value: 8,
    alias: "Combobox/listbox/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "FileUpload/icon/size",
    value: 28,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "FileUpload/file-icon/size",
    value: 20,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "FileUpload/label/font-size",
    value: 14,
    alias: "Input/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "FileUpload/label/line-height",
    value: 20,
    alias: "Input/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "FileUpload/text/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "FileUpload/text/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "FileUpload/meta/font-size",
    value: 12,
    alias: "FormField/meta/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "FileUpload/meta/line-height",
    value: 16,
    alias: "FormField/meta/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "ColorPicker/width/default",
    value: 320,
    alias: "DatePicker/width/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "ColorPicker/field/height",
    value: 44,
    alias: "DatePicker/field/height",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "ColorPicker/field/padding/x",
    value: 12,
    alias: "DatePicker/field/padding/x",
    scopes: ["GAP"],
  },
  {
    name: "ColorPicker/field/padding/y",
    value: 0,
    alias: "DatePicker/field/padding/y",
    scopes: ["GAP"],
  },
  {
    name: "ColorPicker/field/radius",
    value: 8,
    alias: "DatePicker/field/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "ColorPicker/field/stroke/width",
    value: 1,
    alias: "DatePicker/field/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "ColorPicker/gap",
    value: 6,
    alias: "DatePicker/gap",
    scopes: ["GAP"],
  },
  {
    name: "ColorPicker/icon/size",
    value: 16,
    alias: "DatePicker/icon/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "ColorPicker/swatch/size",
    value: 24,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "ColorPicker/swatch/radius",
    value: 6,
    alias: "Radius/sm",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "ColorPicker/swatch/stroke/width",
    value: 1,
    alias: "DatePicker/field/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "ColorPicker/popover/width",
    value: 320,
    alias: "Combobox/listbox/width",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "ColorPicker/popover/padding",
    value: 12,
    alias: "DatePicker/calendar/padding",
    scopes: ["GAP"],
  },
  {
    name: "ColorPicker/popover/gap",
    value: 12,
    alias: "Layout/spacing/150",
    scopes: ["GAP"],
  },
  {
    name: "ColorPicker/popover/radius",
    value: 8,
    alias: "Combobox/listbox/radius",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "ColorPicker/popover/stroke/width",
    value: 1,
    alias: "Combobox/listbox/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "ColorPicker/color-area/height",
    value: 160,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "ColorPicker/color-area/radius",
    value: 8,
    alias: "Radius/md",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "ColorPicker/slider/height",
    value: 24,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "ColorPicker/slider/track/height",
    value: 8,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "ColorPicker/slider/thumb/size",
    value: 20,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "ColorPicker/preset/size",
    value: 30,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "ColorPicker/preset/radius",
    value: 6,
    alias: "Radius/sm",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "ColorPicker/label/font-size",
    value: 14,
    alias: "DatePicker/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "ColorPicker/label/line-height",
    value: 20,
    alias: "DatePicker/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "ColorPicker/text/font-size",
    value: 14,
    alias: "DatePicker/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "ColorPicker/text/line-height",
    value: 20,
    alias: "DatePicker/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "ColorPicker/meta/font-size",
    value: 12,
    alias: "DatePicker/meta/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "ColorPicker/meta/line-height",
    value: 16,
    alias: "DatePicker/meta/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Slider/width/default",
    value: 320,
    alias: "Input/width/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Slider/height/default",
    value: 44,
    alias: "Button/height/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "Slider/track/height", value: 8, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Slider/thumb/size",
    value: 20,
    alias: "Radio/control/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "Slider/gap", value: 6, alias: "Input/gap", scopes: ["GAP"] },
  {
    name: "Slider/label/font-size",
    value: 14,
    alias: "Input/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Slider/label/line-height",
    value: 20,
    alias: "Input/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Progress/width/default",
    value: 320,
    alias: "Input/width/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "Progress/height/default", value: 8, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Progress/radius",
    value: 9999,
    alias: "Radius/full",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Progress/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "Spinner/size/small",
    value: 16,
    alias: "Button/icon/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "Spinner/size/medium", value: 24, scopes: ["WIDTH_HEIGHT"] },
  { name: "Spinner/size/large", value: 32, scopes: ["WIDTH_HEIGHT"] },
  { name: "Spinner/size/xlarge", value: 48, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Spinner/stroke/width",
    value: 2,
    alias: "Switch/track/stroke/width",
    scopes: ["STROKE_FLOAT"],
  },
  { name: "Avatar/size/default", value: 40, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Avatar/radius",
    value: 9999,
    alias: "Radius/full",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Avatar/fallback/font-size",
    value: 14,
    alias: "Button/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Avatar/fallback/line-height",
    value: 20,
    alias: "Button/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  { name: "Alert/width/default", value: 360, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Alert/padding",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  { name: "Alert/gap", value: 8, alias: "Layout/spacing/100", scopes: ["GAP"] },
  {
    name: "Alert/radius",
    value: 12,
    alias: "Radius/lg",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Alert/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "Alert/icon/size",
    value: 16,
    alias: "Button/icon/size",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Alert/title/font-size",
    value: 14,
    alias: "Button/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Alert/title/line-height",
    value: 20,
    alias: "Button/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Alert/description/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Alert/description/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "EmptyState/width/default",
    value: 360,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "EmptyState/padding",
    value: 32,
    alias: "Layout/spacing/400",
    scopes: ["GAP"],
  },
  {
    name: "EmptyState/gap",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  {
    name: "EmptyState/text/gap",
    value: 4,
    alias: "Layout/spacing/50",
    scopes: ["GAP"],
  },
  {
    name: "EmptyState/icon/container/size",
    value: 64,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "EmptyState/icon/container/radius",
    value: 9999,
    alias: "Radius/full",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "EmptyState/icon/size",
    value: 32,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "EmptyState/title/font-size",
    value: 16,
    alias: "Button/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "EmptyState/title/line-height",
    value: 20,
    alias: "Button/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "EmptyState/description/font-size",
    value: 14,
    alias: "Input/text/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "EmptyState/description/line-height",
    value: 20,
    alias: "Input/text/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  { name: "Text/font-size/xs", value: 12, scopes: ["FONT_SIZE"] },
  { name: "Text/line-height/xs", value: 16, scopes: ["LINE_HEIGHT"] },
  {
    name: "Text/font-size/sm",
    value: 14,
    alias: "Button/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Text/line-height/sm",
    value: 20,
    alias: "Button/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Text/font-size/base",
    value: 16,
    alias: "Input/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  { name: "Text/line-height/base", value: 24, scopes: ["LINE_HEIGHT"] },
  { name: "Text/font-size/lg", value: 18, scopes: ["FONT_SIZE"] },
  { name: "Text/line-height/lg", value: 28, scopes: ["LINE_HEIGHT"] },
  {
    name: "Text/font-size/xl",
    value: 20,
    alias: "Card/title/font-size",
    scopes: ["FONT_SIZE"],
  },
  { name: "Text/line-height/xl", value: 28, scopes: ["LINE_HEIGHT"] },
  {
    name: "Text/font-size/2xl",
    value: 24,
    alias: "Dialog/title/font-size",
    scopes: ["FONT_SIZE"],
  },
  { name: "Text/line-height/2xl", value: 32, scopes: ["LINE_HEIGHT"] },
  { name: "Text/font-size/3xl", value: 30, scopes: ["FONT_SIZE"] },
  { name: "Text/line-height/3xl", value: 36, scopes: ["LINE_HEIGHT"] },
  { name: "Text/font-size/4xl", value: 36, scopes: ["FONT_SIZE"] },
  { name: "Text/line-height/4xl", value: 40, scopes: ["LINE_HEIGHT"] },
  { name: "Link/width/default", value: 128, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Link/height/default",
    value: 44,
    alias: "Button/height/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Link/font-size",
    value: 14,
    alias: "Button/label/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Link/line-height",
    value: 20,
    alias: "Button/label/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Label/font-size",
    value: 14,
    alias: "Link/font-size",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Label/line-height",
    value: 20,
    alias: "Link/line-height",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Label/height/default",
    value: 44,
    alias: "Button/height/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "Separator/length/default", value: 320, scopes: ["WIDTH_HEIGHT"] },
  { name: "Separator/thickness", value: 1, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Separator/height/vertical",
    value: 44,
    alias: "Button/height/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "Skeleton/width/line", value: 240, scopes: ["WIDTH_HEIGHT"] },
  { name: "Skeleton/height/line", value: 16, scopes: ["WIDTH_HEIGHT"] },
  { name: "Skeleton/width/block", value: 320, scopes: ["WIDTH_HEIGHT"] },
  { name: "Skeleton/height/block", value: 80, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Skeleton/size/circle",
    value: 40,
    alias: "Avatar/size/default",
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Skeleton/radius/default",
    value: 8,
    alias: "Radius/DEFAULT",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Skeleton/radius/circle",
    value: 9999,
    alias: "Radius/full",
    scopes: ["CORNER_RADIUS"],
  },
  { name: "Box/width/default", value: 320, scopes: ["WIDTH_HEIGHT"] },
  { name: "Box/min-height/default", value: 120, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Box/padding/default",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  {
    name: "Box/radius",
    value: 8,
    alias: "Radius/DEFAULT",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Box/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "Box/text/font-size",
    value: 14,
    alias: "Text/font-size/sm",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Box/text/line-height",
    value: 20,
    alias: "Text/line-height/sm",
    scopes: ["LINE_HEIGHT"],
  },
  { name: "Stack/width/default", value: 320, scopes: ["WIDTH_HEIGHT"] },
  { name: "Stack/height/column", value: 120, scopes: ["WIDTH_HEIGHT"] },
  { name: "Stack/height/row", value: 72, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Stack/gap/2",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "Stack/gap/4",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  { name: "Stack/gap/6", value: 24, scopes: ["GAP"] },
  { name: "Stack/item/width", value: 88, scopes: ["WIDTH_HEIGHT"] },
  { name: "Stack/item/height", value: 32, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Stack/item/radius",
    value: 8,
    alias: "Radius/DEFAULT",
    scopes: ["CORNER_RADIUS"],
  },
  { name: "Grid/width/default", value: 360, scopes: ["WIDTH_HEIGHT"] },
  { name: "Grid/height/2", value: 88, scopes: ["WIDTH_HEIGHT"] },
  { name: "Grid/height/4", value: 96, scopes: ["WIDTH_HEIGHT"] },
  { name: "Grid/height/6", value: 104, scopes: ["WIDTH_HEIGHT"] },
  { name: "Grid/cell/height/default", value: 40, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Grid/gap/2",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "Grid/gap/4",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  { name: "Grid/gap/6", value: 24, scopes: ["GAP"] },
  { name: "Container/width/default", value: 480, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Container/min-height/default",
    value: 120,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Container/padding/x",
    value: 24,
    alias: "Layout/spacing/300",
    scopes: ["GAP"],
  },
  {
    name: "Container/content-width/centered",
    value: 320,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Container/content-width/fluid",
    value: 432,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Container/content-height",
    value: 64,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Container/content-padding",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
  {
    name: "Container/content-radius",
    value: 8,
    alias: "Radius/DEFAULT",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Container/content-stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "Container/text/font-size",
    value: 14,
    alias: "Text/font-size/sm",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Container/text/line-height",
    value: 20,
    alias: "Text/line-height/sm",
    scopes: ["LINE_HEIGHT"],
  },
  { name: "Breadcrumb/width/basic", value: 360, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Breadcrumb/width/ellipsis",
    value: 284,
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "Breadcrumb/height", value: 44, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Breadcrumb/gap",
    value: 8,
    alias: "Layout/spacing/100",
    scopes: ["GAP"],
  },
  {
    name: "Breadcrumb/item-gap",
    value: 6,
    alias: "Layout/spacing/75",
    scopes: ["GAP"],
  },
  {
    name: "Breadcrumb/ellipsis/size",
    value: 36,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Breadcrumb/font-size",
    value: 14,
    alias: "Text/font-size/sm",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Breadcrumb/line-height",
    value: 20,
    alias: "Text/line-height/sm",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Pagination/width/basic/small",
    value: 332,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Pagination/width/basic/default",
    value: 372,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Pagination/width/ellipsis/small",
    value: 428,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Pagination/width/ellipsis/default",
    value: 468,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Pagination/width/compact/small",
    value: 252,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Pagination/width/compact/default",
    value: 304,
    scopes: ["WIDTH_HEIGHT"],
  },
  { name: "Pagination/height", value: 44, scopes: ["WIDTH_HEIGHT"] },
  { name: "Pagination/item/size", value: 44, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Pagination/action/width/small",
    value: 92,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Pagination/action/width/default",
    value: 112,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Pagination/compact-label/width/small",
    value: 60,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Pagination/compact-label/width/default",
    value: 72,
    scopes: ["WIDTH_HEIGHT"],
  },
  {
    name: "Pagination/gap",
    value: 4,
    alias: "Layout/spacing/50",
    scopes: ["GAP"],
  },
  {
    name: "Pagination/item/radius",
    value: 8,
    alias: "Radius/DEFAULT",
    scopes: ["CORNER_RADIUS"],
  },
  {
    name: "Pagination/stroke/width",
    value: 1,
    alias: "Border Width/sm",
    scopes: ["STROKE_FLOAT"],
  },
  {
    name: "Pagination/label/font-size/small",
    value: 12,
    alias: "Text/font-size/xs",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Pagination/label/font-size/default",
    value: 14,
    alias: "Text/font-size/sm",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Pagination/label/line-height/small",
    value: 16,
    alias: "Text/line-height/xs",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Pagination/label/line-height/default",
    value: 20,
    alias: "Text/line-height/sm",
    scopes: ["LINE_HEIGHT"],
  },
  { name: "Pagination/icon/size", value: 16, scopes: ["WIDTH_HEIGHT"] },
  { name: "Accordion/width/default", value: 360, scopes: ["WIDTH_HEIGHT"] },
  { name: "Accordion/height/closed", value: 53, scopes: ["WIDTH_HEIGHT"] },
  { name: "Accordion/height/open", value: 128, scopes: ["WIDTH_HEIGHT"] },
  { name: "Accordion/trigger/height", value: 52, scopes: ["WIDTH_HEIGHT"] },
  { name: "Accordion/content/height", value: 75, scopes: ["WIDTH_HEIGHT"] },
  { name: "Accordion/divider/height", value: 1, scopes: ["WIDTH_HEIGHT"] },
  {
    name: "Accordion/trigger/font-size",
    value: 14,
    alias: "Text/font-size/sm",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Accordion/trigger/line-height",
    value: 20,
    alias: "Text/line-height/sm",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Accordion/content/font-size",
    value: 14,
    alias: "Text/font-size/sm",
    scopes: ["FONT_SIZE"],
  },
  {
    name: "Accordion/content/line-height",
    value: 20,
    alias: "Text/line-height/sm",
    scopes: ["LINE_HEIGHT"],
  },
  {
    name: "Accordion/content/padding-bottom",
    value: 16,
    alias: "Layout/spacing/200",
    scopes: ["GAP"],
  },
];

// Layout-sizing diagnostics.
//
// `setLayoutSizing*` has to swallow errors: it is called on nodes whose runtime
// may not expose the property at all. But swallowing hid a real bug — setting
// FILL on a node not yet inside an auto-layout parent throws, the helper
// returned false, and all 180 call sites ignore the return value, so text
// silently collapsed to one character per line in the built sets. Failures are
// recorded here and drained into whichever run reports next, so they show up in
// the log the operator already reads instead of nowhere.
const layoutSizingFailures = [];
const LAYOUT_SIZING_FAILURE_SAMPLE_LIMIT = 12;

function recordLayoutSizingFailure(axis, node, value, error) {
  layoutSizingFailures.push({
    axis,
    value,
    node: (node && node.name) || "(unnamed)",
    type: (node && node.type) || "(unknown)",
    parentType: (node && node.parent && node.parent.type) || "(detached)",
    reason: error
      ? String((error && error.message) || error)
      : "assignment did not take effect",
  });
}

function resetLayoutSizingFailures() {
  layoutSizingFailures.length = 0;
}

function drainLayoutSizingFailures(stats) {
  if (!stats || typeof stats !== "object") return;
  if (layoutSizingFailures.length === 0) return;

  const failures = layoutSizingFailures.slice();
  resetLayoutSizingFailures();

  if (!Array.isArray(stats.warnings)) stats.warnings = [];
  stats.layoutSizingFailures = failures;

  const sample = failures
    .slice(0, LAYOUT_SIZING_FAILURE_SAMPLE_LIMIT)
    .map(
      (failure) =>
        `${failure.node} (${failure.type}) ${failure.axis}=${failure.value} under ${failure.parentType}: ${failure.reason}`,
    )
    .join("; ");
  const elided =
    failures.length > LAYOUT_SIZING_FAILURE_SAMPLE_LIMIT
      ? ` (+${failures.length - LAYOUT_SIZING_FAILURE_SAMPLE_LIMIT} more)`
      : "";

  stats.warnings.push(
    `${failures.length} layout sizing call(s) did not apply; affected nodes may be mis-sized. ${sample}${elided}`,
  );
}

/**
 * Every handler branch answers the UI with a result object, so this is the one
 * seam where layout-sizing diagnostics can be attached without a new branch
 * being able to forget them.
 */
function postResultToUi(message) {
  if (message && message.result) drainLayoutSizingFailures(message.result);
  figma.ui.postMessage(message);
}

figma.ui.onmessage = async (message) => {
  resetLayoutSizingFailures();

  try {
    configureFontConfig(message && message.fontConfig);

    if (message.type === "inspect") {
      postResultToUi({
        type: "inspect-result",
        result: await inspectFile(),
      });
      return;
    }

    if (message.type === "import-foundations") {
      const result = await importFoundations(
        message.payload,
        message.options || {},
      );
      postResultToUi({ type: "import-result", result });
      return;
    }

    const additionalComponentAction =
      additionalComponentActionHandlers()[message.type];
    if (additionalComponentAction) {
      const result = await additionalComponentAction();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-text") {
      const result = await buildTextComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-text") {
      const result = await updateTextComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-text") {
      const result = await rebuildTextComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-heading") {
      const result = await buildHeadingComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-heading") {
      const result = await updateHeadingComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-heading") {
      const result = await rebuildHeadingComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-link") {
      const result = await buildLinkComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-link") {
      const result = await updateLinkComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-link") {
      const result = await rebuildLinkComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-label") {
      const result = await buildLabelComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-label") {
      const result = await updateLabelComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-label") {
      const result = await rebuildLabelComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-separator") {
      const result = await buildSeparatorComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-separator") {
      const result = await updateSeparatorComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-separator") {
      const result = await rebuildSeparatorComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-skeleton") {
      const result = await buildSkeletonComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-skeleton") {
      const result = await updateSkeletonComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-skeleton") {
      const result = await rebuildSkeletonComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-box") {
      const result = await buildBoxComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-box") {
      const result = await updateBoxComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-box") {
      const result = await rebuildBoxComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-stack") {
      const result = await buildStackComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-stack") {
      const result = await updateStackComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-stack") {
      const result = await rebuildStackComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-grid") {
      const result = await buildGridComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-grid") {
      const result = await updateGridComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-grid") {
      const result = await rebuildGridComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-container") {
      const result = await buildContainerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-container") {
      const result = await updateContainerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-container") {
      const result = await rebuildContainerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-breadcrumb") {
      const result = await buildBreadcrumbComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-breadcrumb") {
      const result = await updateBreadcrumbComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-breadcrumb") {
      const result = await rebuildBreadcrumbComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-pagination") {
      const result = await buildPaginationComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-pagination") {
      const result = await updatePaginationComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-pagination") {
      const result = await rebuildPaginationComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-accordion") {
      const result = await buildAccordionComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-accordion") {
      const result = await updateAccordionComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-accordion") {
      const result = await rebuildAccordionComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-button") {
      const result = await buildButtonComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-button") {
      const result = await updateButtonComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-button") {
      const result = await rebuildButtonComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-icon-button") {
      const result = await buildIconButtonComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-icon-button") {
      const result = await updateIconButtonComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-icon-button") {
      const result = await rebuildIconButtonComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-toggle-button") {
      const result = await buildToggleButtonComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-toggle-button") {
      const result = await updateToggleButtonComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-toggle-button") {
      const result = await rebuildToggleButtonComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-split-button") {
      const result = await buildSplitButtonComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-split-button") {
      const result = await updateSplitButtonComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-split-button") {
      const result = await rebuildSplitButtonComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-floating-action-button") {
      const result = await buildFloatingActionButtonComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-floating-action-button") {
      const result = await updateFloatingActionButtonComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-floating-action-button") {
      const result = await rebuildFloatingActionButtonComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-counter") {
      const result = await buildCounterComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-counter") {
      const result = await updateCounterComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-counter") {
      const result = await rebuildCounterComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-badge") {
      const result = await buildBadgeComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-badge") {
      const result = await updateBadgeComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-badge") {
      const result = await rebuildBadgeComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-chip") {
      const result = await buildChipComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-chip") {
      const result = await updateChipComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-chip") {
      const result = await rebuildChipComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-segmented-control") {
      const result = await buildSegmentedControlComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-segmented-control") {
      const result = await updateSegmentedControlComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-segmented-control") {
      const result = await rebuildSegmentedControlComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-card") {
      const result = await buildCardComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-card") {
      const result = await updateCardComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-card") {
      const result = await rebuildCardComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-list") {
      const result = await buildListComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-list") {
      const result = await updateListComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-list") {
      const result = await rebuildListComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-table") {
      const result = await buildTableComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-table") {
      const result = await updateTableComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-table") {
      const result = await rebuildTableComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-tabs") {
      const result = await buildTabsComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-tabs") {
      const result = await updateTabsComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-tabs") {
      const result = await rebuildTabsComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-tooltip") {
      const result = await buildTooltipComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-tooltip") {
      const result = await updateTooltipComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-tooltip") {
      const result = await rebuildTooltipComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-dialog") {
      const result = await buildDialogComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-dialog") {
      const result = await updateDialogComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-dialog") {
      const result = await rebuildDialogComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-drawer") {
      const result = await buildDrawerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-drawer") {
      const result = await updateDrawerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-drawer") {
      const result = await rebuildDrawerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-popover") {
      const result = await buildPopoverComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-popover") {
      const result = await updatePopoverComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-popover") {
      const result = await rebuildPopoverComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-menu") {
      const result = await buildMenuComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-menu") {
      const result = await updateMenuComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-menu") {
      const result = await rebuildMenuComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-checkbox") {
      const result = await buildCheckboxComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-checkbox") {
      const result = await updateCheckboxComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-checkbox") {
      const result = await rebuildCheckboxComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-radio") {
      const result = await buildRadioComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-radio") {
      const result = await updateRadioComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-radio") {
      const result = await rebuildRadioComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-switch") {
      const result = await buildSwitchComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-switch") {
      const result = await updateSwitchComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-switch") {
      const result = await rebuildSwitchComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-input") {
      const result = await buildInputComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-input") {
      const result = await updateInputComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-input") {
      const result = await rebuildInputComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-password-input") {
      const result = await buildPasswordInputComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-password-input") {
      const result = await updatePasswordInputComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-password-input") {
      const result = await rebuildPasswordInputComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-form-field") {
      const result = await buildFormFieldComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-form-field") {
      const result = await updateFormFieldComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-form-field") {
      const result = await rebuildFormFieldComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-number-input") {
      const result = await buildNumberInputComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-number-input") {
      const result = await updateNumberInputComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-number-input") {
      const result = await rebuildNumberInputComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-otp-input") {
      const result = await buildOTPInputComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-otp-input") {
      const result = await updateOTPInputComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-otp-input") {
      const result = await rebuildOTPInputComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-combobox") {
      const result = await buildComboboxComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-combobox") {
      const result = await updateComboboxComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-combobox") {
      const result = await rebuildComboboxComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-multi-select") {
      const result = await buildMultiSelectComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-multi-select") {
      const result = await updateMultiSelectComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-multi-select") {
      const result = await rebuildMultiSelectComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-listbox") {
      const result = await buildListboxComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-listbox") {
      const result = await updateListboxComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-listbox") {
      const result = await rebuildListboxComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-date-picker") {
      const result = await buildDatePickerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-date-picker") {
      const result = await updateDatePickerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-date-picker") {
      const result = await rebuildDatePickerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-date-range-picker") {
      const result = await buildDateRangePickerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-date-range-picker") {
      const result = await updateDateRangePickerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-date-range-picker") {
      const result = await rebuildDateRangePickerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-time-picker") {
      const result = await buildTimePickerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-time-picker") {
      const result = await updateTimePickerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-time-picker") {
      const result = await rebuildTimePickerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-file-upload") {
      const result = await buildFileUploadComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-file-upload") {
      const result = await updateFileUploadComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-file-upload") {
      const result = await rebuildFileUploadComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-color-picker") {
      const result = await buildColorPickerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-color-picker") {
      const result = await updateColorPickerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-color-picker") {
      const result = await rebuildColorPickerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-textarea") {
      const result = await buildTextareaComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-textarea") {
      const result = await updateTextareaComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-textarea") {
      const result = await rebuildTextareaComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-search") {
      const result = await buildSearchComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-search") {
      const result = await updateSearchComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-search") {
      const result = await rebuildSearchComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-select") {
      const result = await buildSelectComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-select") {
      const result = await updateSelectComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-select") {
      const result = await rebuildSelectComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-slider") {
      const result = await buildSliderComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-slider") {
      const result = await updateSliderComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-slider") {
      const result = await rebuildSliderComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-rating") {
      const result = await buildRatingComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-rating") {
      const result = await updateRatingComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-rating") {
      const result = await rebuildRatingComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-stepper") {
      const result = await buildStepperComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-stepper") {
      const result = await updateStepperComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-stepper") {
      const result = await rebuildStepperComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-progress") {
      const result = await buildProgressComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-progress") {
      const result = await updateProgressComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-progress") {
      const result = await rebuildProgressComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-spinner") {
      const result = await buildSpinnerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-spinner") {
      const result = await updateSpinnerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-spinner") {
      const result = await rebuildSpinnerComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-avatar") {
      const result = await buildAvatarComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-avatar") {
      const result = await updateAvatarComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-avatar") {
      const result = await rebuildAvatarComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-alert") {
      const result = await buildAlertComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-alert") {
      const result = await updateAlertComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-alert") {
      const result = await rebuildAlertComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-empty-state") {
      const result = await buildEmptyStateComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-empty-state") {
      const result = await updateEmptyStateComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-empty-state") {
      const result = await rebuildEmptyStateComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-toast") {
      const result = await buildToastComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-toast") {
      const result = await updateToastComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "rebuild-toast") {
      const result = await rebuildToastComponent();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "audit-library") {
      postAuditProgress(
        "scan",
        "Scanning file structure...",
        "Loading local variables, pages, icons, and QA surfaces.",
      );
      const result = await auditLibrary();
      postAuditProgress(
        "ready",
        "Preparing audit results...",
        "Summarizing warnings and component recommendations.",
      );
      postResultToUi({ type: "audit-result", result });
      return;
    }

    if (message.type === "document-components") {
      const result = await documentComponentLibrary();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "reorganize-components") {
      const result = await reorganizeComponentsPage();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-surface-qa") {
      const result = await buildSurfaceQaPage();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "apply-text-styles") {
      const result = await applyTextStylesToComponentLibrary();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "build-example-dashboard") {
      const result = await buildDashboardExamplePage();
      postResultToUi({ type: "example-result", result });
      return;
    }

    if (message.type === "build-example-settings") {
      const result = await buildSettingsExamplePage();
      postResultToUi({ type: "example-result", result });
      return;
    }

    if (message.type === "build-example-mobile-drawer") {
      const result = await buildMobileDrawerExamplePage();
      postResultToUi({ type: "example-result", result });
      return;
    }

    if (message.type === "build-example-operations-console") {
      const result = await buildOperationsConsoleExamplePage();
      postResultToUi({ type: "example-result", result });
      return;
    }

    if (message.type === "build-example-sdk-route-flow") {
      const result = await buildSdkRouteFlowExamplePage();
      postResultToUi({ type: "example-result", result });
      return;
    }

    if (message.type === "build-example-kiosk-handoff") {
      const result = await buildKioskHandoffExamplePage();
      postResultToUi({ type: "example-result", result });
      return;
    }

    if (message.type === "build-all-examples") {
      const result = await buildAllExamplePages();
      postResultToUi({ type: "example-result", result });
      return;
    }

    if (message.type === "build-icons") {
      const result = await syncIconSourceLibrary();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "update-icons") {
      const result = await syncIconSourceLibrary();
      postResultToUi({ type: "component-result", result });
      return;
    }

    if (message.type === "close") {
      figma.closePlugin();
      return;
    }

    postResultToUi({
      type: "error",
      message: `Unknown plugin action "${message.type}". Reload the Kozmos DS Foundations Importer so the UI and code use the same local files.`,
    });
  } catch (error) {
    postResultToUi({
      type: "error",
      message: error instanceof Error ? error.message : String(error),
      stack: error instanceof Error ? error.stack : undefined,
    });
  }
};

function postAuditProgress(step, title, detail) {
  postResultToUi({
    type: "audit-progress",
    step,
    title,
    detail,
  });
}

// Every Product / SDK and platform set, in picker order. These share the
// productSdkSlot / productSdkControlButton / productSdkText helpers, so a change
// to any of them makes all of these stale at once — which is exactly the
// situation that makes updating them one at a time tedious.
const PRODUCT_SDK_UPDATE_SEQUENCE = [
  ["AdaptiveMapShell", updateAdaptiveMapShellComponent],
  ["BrowseCategoriesPanel", updateBrowseCategoriesPanelComponent],
  ["CategoryTile", updateCategoryTileComponent],
  ["DirectionStep", updateDirectionStepComponent],
  ["FloorSelector", updateFloorSelectorComponent],
  ["LocationPin", updateLocationPinComponent],
  ["MapControlButton", updateMapControlButtonComponent],
  ["MapControlsGroup", updateMapControlsGroupComponent],
  ["MapOverlay", updateMapOverlayComponent],
  ["MapView", updateMapViewComponent],
  ["POICard", updatePOICardComponent],
  ["POIDetailPanel", updatePOIDetailPanelComponent],
  ["POIMediaGallery", updatePOIMediaGalleryComponent],
  ["POIResultCard", updatePOIResultCardComponent],
  ["POIResultList", updatePOIResultListComponent],
  ["RouteOptionCard", updateRouteOptionCardComponent],
  ["RoutePreviewPanel", updateRoutePreviewPanelComponent],
  ["RouteSummary", updateRouteSummaryComponent],
  ["RoutingInputGroup", updateRoutingInputGroupComponent],
  ["SaveLocationCard", updateSaveLocationCardComponent],
  ["UserLocationMarker", updateUserLocationMarkerComponent],
  ["WayfindingCard", updateWayfindingCardComponent],
  ["DynamicIsland", updateDynamicIslandComponent],
  ["FeedbackCard", updateFeedbackCardComponent],
];

async function updateAllProductSdkComponents() {
  const stats = {
    updatedComponents: 0,
    skipped: [],
    failures: [],
    perComponent: [],
    warnings: [],
  };

  suppressAutoReorganize = true;

  try {
    for (
      let index = 0;
      index < PRODUCT_SDK_UPDATE_SEQUENCE.length;
      index += 1
    ) {
      const entry = PRODUCT_SDK_UPDATE_SEQUENCE[index];
      const name = entry[0];
      postAuditProgress(
        index + 1,
        `Updating ${name}`,
        `${index + 1} of ${PRODUCT_SDK_UPDATE_SEQUENCE.length}`,
      );

      try {
        const result = await entry[1]();
        const record = { name, variants: result.variants || 0 };
        if (result.updated) stats.updatedComponents += 1;
        else stats.skipped.push(name);
        if (Array.isArray(result.warnings) && result.warnings.length > 0) {
          record.warnings = result.warnings;
          for (const warning of result.warnings) {
            stats.warnings.push(`${name}: ${warning}`);
          }
        }
        stats.perComponent.push(record);
      } catch (error) {
        // One failing set must not strand the other twenty-three.
        const message = error instanceof Error ? error.message : String(error);
        stats.failures.push(`${name}: ${message}`);
        stats.perComponent.push({ name, failed: message });
      }
    }
  } finally {
    suppressAutoReorganize = false;
  }

  stats.layout = await reorganizeComponentsPage();
  stats.message =
    `Updated ${stats.updatedComponents} of ${PRODUCT_SDK_UPDATE_SEQUENCE.length} Product / SDK set(s) in place, then reorganized once.` +
    (stats.failures.length > 0 ? ` ${stats.failures.length} failed.` : "") +
    (stats.skipped.length > 0
      ? ` ${stats.skipped.length} not present and skipped.`
      : "");
  return stats;
}

function additionalComponentActionHandlers() {
  return {
    "build-tag": buildTagComponent,
    "update-tag": updateTagComponent,
    "rebuild-tag": rebuildTagComponent,
    "build-scroll-area": buildScrollAreaComponent,
    "update-scroll-area": updateScrollAreaComponent,
    "rebuild-scroll-area": rebuildScrollAreaComponent,
    "build-bottom-navigation": buildBottomNavigationComponent,
    "update-bottom-navigation": updateBottomNavigationComponent,
    "rebuild-bottom-navigation": rebuildBottomNavigationComponent,
    "build-navigation-item": buildNavigationItemComponent,
    "update-navigation-item": updateNavigationItemComponent,
    "rebuild-navigation-item": rebuildNavigationItemComponent,
    "build-navbar": buildNavbarComponent,
    "update-navbar": updateNavbarComponent,
    "rebuild-navbar": rebuildNavbarComponent,
    "build-sidebar": buildSidebarComponent,
    "update-sidebar": updateSidebarComponent,
    "rebuild-sidebar": rebuildSidebarComponent,
    "build-backdrop": buildBackdropComponent,
    "update-backdrop": updateBackdropComponent,
    "rebuild-backdrop": rebuildBackdropComponent,
    "build-bottom-sheet": buildBottomSheetComponent,
    "update-bottom-sheet": updateBottomSheetComponent,
    "rebuild-bottom-sheet": rebuildBottomSheetComponent,
    "fix-current-audit-issues": fixCurrentAuditIssues,
    "build-search-bar": buildSearchBarComponent,
    "update-search-bar": updateSearchBarComponent,
    "rebuild-search-bar": rebuildSearchBarComponent,
    "build-tree": buildTreeComponent,
    "update-tree": updateTreeComponent,
    "rebuild-tree": rebuildTreeComponent,
    "build-tree-parent-item": buildTreeParentItemComponent,
    "update-tree-parent-item": updateTreeParentItemComponent,
    "rebuild-tree-parent-item": rebuildTreeParentItemComponent,
    "build-tree-child-item": buildTreeChildItemComponent,
    "update-tree-child-item": updateTreeChildItemComponent,
    "rebuild-tree-child-item": rebuildTreeChildItemComponent,
    "build-tree-item": buildTreeItemComponent,
    "update-tree-item": updateTreeItemComponent,
    "rebuild-tree-item": rebuildTreeItemComponent,
    "build-timeline": buildTimelineComponent,
    "update-timeline": updateTimelineComponent,
    "rebuild-timeline": rebuildTimelineComponent,
    "build-browse-categories-panel": buildBrowseCategoriesPanelComponent,
    "update-browse-categories-panel": updateBrowseCategoriesPanelComponent,
    "rebuild-browse-categories-panel": rebuildBrowseCategoriesPanelComponent,
    "build-category-tile": buildCategoryTileComponent,
    "update-category-tile": updateCategoryTileComponent,
    "rebuild-category-tile": rebuildCategoryTileComponent,
    "build-poi-media-gallery": buildPOIMediaGalleryComponent,
    "update-poi-media-gallery": updatePOIMediaGalleryComponent,
    "rebuild-poi-media-gallery": rebuildPOIMediaGalleryComponent,
    "build-poi-result-card": buildPOIResultCardComponent,
    "update-poi-result-card": updatePOIResultCardComponent,
    "rebuild-poi-result-card": rebuildPOIResultCardComponent,
    "build-poi-result-list": buildPOIResultListComponent,
    "update-poi-result-list": updatePOIResultListComponent,
    "rebuild-poi-result-list": rebuildPOIResultListComponent,
    "build-route-option-card": buildRouteOptionCardComponent,
    "update-route-option-card": updateRouteOptionCardComponent,
    "rebuild-route-option-card": rebuildRouteOptionCardComponent,
    "build-route-preview-panel": buildRoutePreviewPanelComponent,
    "update-route-preview-panel": updateRoutePreviewPanelComponent,
    "rebuild-route-preview-panel": rebuildRoutePreviewPanelComponent,
    "build-route-summary": buildRouteSummaryComponent,
    "update-route-summary": updateRouteSummaryComponent,
    "rebuild-route-summary": rebuildRouteSummaryComponent,
    "build-routing-input-group": buildRoutingInputGroupComponent,
    "update-routing-input-group": updateRoutingInputGroupComponent,
    "rebuild-routing-input-group": rebuildRoutingInputGroupComponent,
    "build-save-location-card": buildSaveLocationCardComponent,
    "update-save-location-card": updateSaveLocationCardComponent,
    "rebuild-save-location-card": rebuildSaveLocationCardComponent,
    "build-user-location-marker": buildUserLocationMarkerComponent,
    "update-user-location-marker": updateUserLocationMarkerComponent,
    "rebuild-user-location-marker": rebuildUserLocationMarkerComponent,
    "build-dynamic-island": buildDynamicIslandComponent,
    "update-dynamic-island": updateDynamicIslandComponent,
    "rebuild-dynamic-island": rebuildDynamicIslandComponent,
    "build-feedback-card": buildFeedbackCardComponent,
    "update-feedback-card": updateFeedbackCardComponent,
    "rebuild-feedback-card": rebuildFeedbackCardComponent,
    "update-all-product-sdk": updateAllProductSdkComponents,
    "build-adaptive-map-shell": buildAdaptiveMapShellComponent,
    "update-adaptive-map-shell": updateAdaptiveMapShellComponent,
    "rebuild-adaptive-map-shell": rebuildAdaptiveMapShellComponent,
    "build-map-control-button": buildMapControlButtonComponent,
    "update-map-control-button": updateMapControlButtonComponent,
    "rebuild-map-control-button": rebuildMapControlButtonComponent,
    "build-map-controls-group": buildMapControlsGroupComponent,
    "update-map-controls-group": updateMapControlsGroupComponent,
    "rebuild-map-controls-group": rebuildMapControlsGroupComponent,
    "build-map-overlay": buildMapOverlayComponent,
    "update-map-overlay": updateMapOverlayComponent,
    "rebuild-map-overlay": rebuildMapOverlayComponent,
    "build-poi-detail-panel": buildPOIDetailPanelComponent,
    "update-poi-detail-panel": updatePOIDetailPanelComponent,
    "rebuild-poi-detail-panel": rebuildPOIDetailPanelComponent,
    "build-direction-step": buildDirectionStepComponent,
    "update-direction-step": updateDirectionStepComponent,
    "rebuild-direction-step": rebuildDirectionStepComponent,
    "build-floor-selector": buildFloorSelectorComponent,
    "update-floor-selector": updateFloorSelectorComponent,
    "rebuild-floor-selector": rebuildFloorSelectorComponent,
    "build-location-pin": buildLocationPinComponent,
    "update-location-pin": updateLocationPinComponent,
    "rebuild-location-pin": rebuildLocationPinComponent,
    "build-map-view": buildMapViewComponent,
    "update-map-view": updateMapViewComponent,
    "rebuild-map-view": rebuildMapViewComponent,
    "build-poi-card": buildPOICardComponent,
    "update-poi-card": updatePOICardComponent,
    "rebuild-poi-card": rebuildPOICardComponent,
    "build-wayfinding-card": buildWayfindingCardComponent,
    "update-wayfinding-card": updateWayfindingCardComponent,
    "rebuild-wayfinding-card": rebuildWayfindingCardComponent,
  };
}

async function fixCurrentAuditIssues() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    warnings: [],
    operations: [],
  };
  const operations = [
    { componentName: "Separator", update: updateSeparatorComponent },
    { componentName: "Slider", update: updateSliderComponent },
    { componentName: "NavigationItem", update: updateNavigationItemComponent },
    { componentName: "Navbar", update: updateNavbarComponent },
    { componentName: "Sidebar", update: updateSidebarComponent },
    { componentName: "Dialog", update: updateDialogComponent },
    { componentName: "BottomSheet", update: updateBottomSheetComponent },
  ];

  for (const operation of operations) {
    let result = null;
    try {
      result = await operation.update();
    } catch (error) {
      const message = `${operation.componentName}: update failed (${messageFor(error)}).`;
      stats.warnings.push(message);
      stats.operations.push({
        componentName: operation.componentName,
        failed: true,
        message,
      });
      continue;
    }

    mergeAuditIssueFixResult(stats, operation.componentName, result);
  }

  const refreshed = stats.operations
    .filter((operation) => operation.changed)
    .map((operation) => operation.componentName);
  if (refreshed.length > 0) {
    stats.updated = true;
    stats.message = `Refreshed ${refreshed.join(", ")}. Run Audit Again to confirm the live file state.`;
  } else {
    stats.message =
      "No known audit issue component was changed. Review the warnings and run the individual component update if needed.";
  }

  return stats;
}

function mergeAuditIssueFixResult(stats, componentName, result) {
  const changed = Boolean(
    result &&
    (result.updated ||
      result.created ||
      result.rebuilt ||
      result.variantsUpdated ||
      result.variantsCreated ||
      result.compositeRepairsApplied ||
      result.compositeInputInstancesRecreated),
  );
  const operation = {
    componentName,
    changed,
    componentSetId: result && result.componentSetId,
    urlNodeId: result && result.urlNodeId,
    message: result && result.message,
    warnings: result && Array.isArray(result.warnings) ? result.warnings : [],
  };

  stats.operations.push(operation);
  if (operation.componentSetId && !stats.componentSetId) {
    stats.componentSetId = operation.componentSetId;
  }
  if (operation.urlNodeId && !stats.urlNodeId) {
    stats.urlNodeId = operation.urlNodeId;
  }

  for (const key of [
    "variantsUpdated",
    "variantsCreated",
    "variantsRemoved",
    "variantsMissing",
    "compositeRepairsApplied",
    "compositeInputInstancesRecreated",
    "componentVariableBindingsSkipped",
  ]) {
    if (result && typeof result[key] === "number") {
      stats[key] = (stats[key] || 0) + result[key];
    }
  }

  for (const warning of operation.warnings) {
    stats.warnings.push(`${componentName}: ${warning}`);
  }
  if (!changed && operation.message) {
    stats.warnings.push(`${componentName}: ${operation.message}`);
  }
}

function createAuditPerformance() {
  return {
    phases: [],
    byName: {},
  };
}

function auditTimerNow() {
  return Date.now();
}

const AUDIT_ROLLUP_TIMING_NAMES = new Set(["component set total"]);

function timeAuditStep(performance, name, metadata, callback) {
  const startedAt = auditTimerNow();
  try {
    return callback();
  } finally {
    recordAuditTiming(performance, name, startedAt, metadata);
  }
}

function indexAuditPropertiesByBaseName(properties) {
  const result = {};
  for (const property of Object.values(properties || {})) {
    if (!property || !property.baseName || result[property.baseName]) continue;
    result[property.baseName] = property;
  }
  return result;
}

function recordAuditTiming(performance, name, startedAt, metadata) {
  if (!performance || !name || typeof startedAt !== "number") return;

  const durationMs = Math.max(0, auditTimerNow() - startedAt);
  const entry = {
    name,
    durationMs,
  };
  if (metadata && Object.keys(metadata).length > 0) {
    entry.metadata = metadata;
  }
  performance.phases.push(entry);

  const aggregate = performance.byName[name] || {
    durationMs: 0,
    count: 0,
    maxDurationMs: 0,
    maxMetadata: null,
  };
  aggregate.durationMs += durationMs;
  aggregate.count += 1;
  if (durationMs >= aggregate.maxDurationMs) {
    aggregate.maxDurationMs = durationMs;
    aggregate.maxMetadata = metadata || null;
  }
  performance.byName[name] = aggregate;
}

function finalizeAuditPerformance(performance, startedAt) {
  const totalMs = Math.max(0, auditTimerNow() - startedAt);
  const byNameEntries = Object.entries(performance.byName).map(
    ([name, aggregate]) => ({
      name,
      durationMs: aggregate.durationMs,
      count: aggregate.count,
      maxDurationMs: aggregate.maxDurationMs,
      maxMetadata: aggregate.maxMetadata,
    }),
  );
  byNameEntries.sort((a, b) => b.durationMs - a.durationMs);
  const slowestPhase =
    byNameEntries.find((entry) => !AUDIT_ROLLUP_TIMING_NAMES.has(entry.name)) ||
    byNameEntries[0] ||
    null;

  return {
    totalMs,
    slowestPhase,
    byName: byNameEntries,
    phases: performance.phases,
  };
}

async function inspectFile() {
  const collections = await figma.variables.getLocalVariableCollectionsAsync();
  const variables = await figma.variables.getLocalVariablesAsync();

  return {
    fileName: figma.root.name,
    pages: figma.root.children.map((page) => page.name),
    variableCollections: collections.map((collection) => ({
      name: collection.name,
      modes: collection.modes.map((mode) => mode.name),
      variableCount: variables.filter(
        (variable) => variable.variableCollectionId === collection.id,
      ).length,
    })),
    variables: variables.length,
  };
}

async function buildDashboardExamplePage() {
  return buildExampleTemplate("dashboard", buildDashboardExampleTemplate);
}

async function buildSettingsExamplePage() {
  return buildExampleTemplate("settings", buildSettingsExampleTemplate);
}

async function buildMobileDrawerExamplePage() {
  return buildExampleTemplate("mobileDrawer", buildMobileDrawerExampleTemplate);
}

async function buildOperationsConsoleExamplePage() {
  return buildExampleTemplate(
    "operationsConsole",
    buildOperationsConsoleExampleTemplate,
  );
}

async function buildSdkRouteFlowExamplePage() {
  return buildExampleTemplate("sdkRouteFlow", buildSdkRouteFlowExampleTemplate);
}

async function buildKioskHandoffExamplePage() {
  return buildExampleTemplate("kioskHandoff", buildKioskHandoffExampleTemplate);
}

async function buildAllExamplePages() {
  const results = [];
  results.push(await buildDashboardExamplePage());
  results.push(await buildSettingsExamplePage());
  results.push(await buildMobileDrawerExamplePage());
  results.push(await buildOperationsConsoleExamplePage());
  results.push(await buildSdkRouteFlowExamplePage());
  results.push(await buildKioskHandoffExamplePage());

  const warnings = [];
  for (const result of results) {
    for (const warning of result.warnings || []) warnings.push(warning);
  }

  return {
    page: EXAMPLES_PAGE_NAME,
    templateName: "All Examples",
    templateCount: results.length,
    templates: results.map((result) => ({
      name: result.templateName,
      id: result.id,
      urlNodeId: result.urlNodeId,
    })),
    warnings,
    message: `Created ${results.length} example templates on the Examples page.`,
  };
}

async function buildExampleTemplate(key, builder) {
  const config = EXAMPLE_TEMPLATE_CONFIGS[key];
  const stats = createExampleStats(config.name);
  const page = await ensurePage(EXAMPLES_PAGE_NAME);
  await figma.setCurrentPageAsync(page);
  const fonts = await loadButtonFonts(stats);
  await ensureKozmosTextStyles(fonts, stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);

  const cleanup = removeExistingExampleTemplate(page, config.name, key);
  const root = await builder({
    config,
    fonts,
    variableByName,
    stats,
  });
  const placement = resolveExampleTemplatePlacement(
    page,
    root,
    config,
    cleanup.untaggedMatchCount > 0,
  );
  if (cleanup.untaggedMatchCount > 0) {
    const placementSummary = placement.moved
      ? `placed the new generated example at (${Math.round(placement.x)}, ${Math.round(placement.y)}) outside existing page content`
      : "kept the new generated example at its configured position because that slot was clear";
    stats.warnings.push(
      `${config.name}: found ${cleanup.untaggedMatchCount} same-name frame(s) without importer example metadata. Left them untouched and ${placementSummary}.`,
    );
  }
  root.x = placement.x;
  root.y = placement.y;
  root.setSharedPluginData(RUN_NAMESPACE, "kind", "example-template");
  root.setSharedPluginData(RUN_NAMESPACE, "example", key);
  page.appendChild(root);
  normalizeExampleDeferredChildSizing(root);
  stats.framesCreated += 1;

  try {
    figma.viewport.scrollAndZoomIntoView([root]);
  } catch (_error) {
    // Viewport APIs are unavailable in some headless/plugin test contexts.
  }

  return {
    page: EXAMPLES_PAGE_NAME,
    templateName: config.name,
    templateCount: 1,
    id: root.id,
    urlNodeId: nodeIdForUrl(root.id),
    framesCreated: stats.framesCreated,
    instancesCreated: stats.instancesCreated,
    placeholdersCreated: stats.placeholdersCreated,
    warnings: stats.warnings,
    message: `Created ${config.name} on the Examples page.`,
  };
}

function createExampleStats(templateName) {
  return {
    page: EXAMPLES_PAGE_NAME,
    templateName,
    framesCreated: 0,
    instancesCreated: 0,
    placeholdersCreated: 0,
    warnings: [],
  };
}

function removeExistingExampleTemplate(page, name, key) {
  const removable = [];
  let untaggedMatchCount = 0;
  for (const child of page.children) {
    if (child.name !== name) continue;

    let kind = "";
    let example = "";
    try {
      kind = child.getSharedPluginData(RUN_NAMESPACE, "kind") || "";
      example = child.getSharedPluginData(RUN_NAMESPACE, "example") || "";
    } catch (_error) {
      kind = "";
      example = "";
    }

    if (kind === "example-template" && example === key) {
      removable.push(child);
    } else {
      untaggedMatchCount += 1;
    }
  }

  for (const child of removable) {
    child.remove();
  }

  return {
    removedCount: removable.length,
    untaggedMatchCount,
  };
}

function resolveExampleTemplatePlacement(page, root, config, avoidCollisions) {
  const desired = { x: config.x, y: config.y };
  if (!avoidCollisions) {
    return {
      x: desired.x,
      y: desired.y,
      moved: false,
    };
  }

  const size = sceneNodeSize(root, config);
  const desiredBounds = {
    x: desired.x,
    y: desired.y,
    width: size.width,
    height: size.height,
  };
  const occupiedBounds = page.children
    .map((child) => sceneNodeBounds(child))
    .filter(Boolean);

  if (
    !occupiedBounds.some((bounds) => rectanglesOverlap(desiredBounds, bounds))
  ) {
    return {
      x: desired.x,
      y: desired.y,
      moved: false,
    };
  }

  const maxBottom = occupiedBounds.reduce(
    (bottom, bounds) => Math.max(bottom, bounds.y + bounds.height),
    config.y,
  );

  return {
    x: config.x,
    y: maxBottom + EXAMPLE_TEMPLATE_COLLISION_GAP,
    moved: true,
  };
}

function sceneNodeSize(node, fallback) {
  const width =
    node && typeof node.width === "number" && Number.isFinite(node.width)
      ? node.width
      : fallback.width;
  const height =
    node && typeof node.height === "number" && Number.isFinite(node.height)
      ? node.height
      : fallback.height;

  return { width, height };
}

function sceneNodeBounds(node) {
  if (!node) return null;
  if (
    typeof node.x !== "number" ||
    typeof node.y !== "number" ||
    typeof node.width !== "number" ||
    typeof node.height !== "number" ||
    !Number.isFinite(node.x) ||
    !Number.isFinite(node.y) ||
    !Number.isFinite(node.width) ||
    !Number.isFinite(node.height)
  ) {
    return null;
  }

  return {
    x: node.x,
    y: node.y,
    width: node.width,
    height: node.height,
  };
}

function rectanglesOverlap(a, b) {
  return (
    a.x < b.x + b.width &&
    a.x + a.width > b.x &&
    a.y < b.y + b.height &&
    a.y + a.height > b.y
  );
}

async function buildDashboardExampleTemplate({
  config,
  fonts,
  variableByName,
  stats,
}) {
  const contentWidth = config.width - 64;
  const tableWidth = contentWidth - 32;
  const root = createExampleFrame({
    name: config.name,
    width: config.width,
    height: config.height,
    padding: 32,
    itemSpacing: 22,
    fillToken: "Surface/100",
    fillFallback: "#F6F7F9",
    variableByName,
    stats,
  });

  const header = createExampleRow("Example Header", 16, contentWidth);
  setVerticalStackChildSizing(header);
  const headerCopy = createExampleStack("Example Header Copy", 6);
  setHorizontalStackFillChildSizing(headerCopy);
  headerCopy.appendChild(
    createExampleText({
      name: "Page Title",
      characters: "Maps dashboard",
      font: fonts.bold,
      fontSize: 26,
      lineHeight: 32,
      colorToken: "Colors/foreground/0",
      colorFallback: "#111111",
      width: contentWidth - 148,
      variableByName,
      stats,
    }),
  );
  headerCopy.appendChild(
    createExampleText({
      name: "Page Description",
      characters:
        "Review map sync, content status, and recent workspace activity.",
      font: fonts.regular,
      fontSize: 14,
      lineHeight: 20,
      colorToken: "Colors/foreground/400",
      colorFallback: "#5F6675",
      width: contentWidth - 148,
      variableByName,
      stats,
    }),
  );
  const createButton = await createExampleInstance({
    componentSetName: "Button",
    variantProperties: {
      Variant: "Default",
      Size: "Default",
      State: "Default",
    },
    name: "Create POI Button",
    textProperties: { "Label Text": "Create POI" },
    width: 140,
    height: 44,
    fonts,
    variableByName,
    stats,
  });
  header.appendChild(headerCopy);
  header.appendChild(createButton);
  root.appendChild(header);

  const metrics = createExampleRow("Example Metrics", 16, contentWidth);
  setVerticalStackChildSizing(metrics);
  const metricWidth = (contentWidth - 32) / 3;
  metrics.appendChild(
    createExampleMetricCard({
      label: "Buildings",
      value: "12",
      width: metricWidth,
      fonts,
      variableByName,
      stats,
    }),
  );
  metrics.appendChild(
    createExampleMetricCard({
      label: "Sync",
      value: "Ready",
      width: metricWidth,
      fonts,
      variableByName,
      stats,
    }),
  );
  metrics.appendChild(
    createExampleMetricCard({
      label: "Issues",
      value: "3",
      width: metricWidth,
      fonts,
      variableByName,
      stats,
    }),
  );
  root.appendChild(metrics);

  const toolbar = createExampleRow("Example Toolbar", 12, contentWidth);
  setVerticalStackChildSizing(toolbar);
  const search = await createExampleInstance({
    componentSetName: "Search",
    variantProperties: {
      State: "Default",
      Status: "Default",
    },
    name: "Search Maps",
    textProperties: {
      "Label Text": "Search",
      "Placeholder Text": "Search maps",
    },
    width: 320,
    height: 70,
    growHorizontal: true,
    fonts,
    variableByName,
    stats,
  });
  const chipReady = await createExampleInstance({
    componentSetName: "Chip",
    variantProperties: {
      Variant: "Brand",
      Size: "Small",
      State: "Selected",
      Removable: "False",
    },
    name: "Ready Chip",
    textProperties: { "Label Text": "Ready" },
    width: 64,
    height: 28,
    fonts,
    variableByName,
    stats,
  });
  const chipDraft = await createExampleInstance({
    componentSetName: "Chip",
    variantProperties: {
      Variant: "Neutral",
      Size: "Small",
      State: "Default",
      Removable: "False",
    },
    name: "Draft Chip",
    textProperties: { "Label Text": "Draft" },
    width: 62,
    height: 28,
    fonts,
    variableByName,
    stats,
  });
  const exportButton = await createExampleInstance({
    componentSetName: "Button",
    variantProperties: {
      Variant: "Outline",
      Size: "Default",
      State: "Default",
    },
    name: "Export Button",
    textProperties: { "Label Text": "Export" },
    width: 104,
    height: 44,
    fonts,
    variableByName,
    stats,
  });
  toolbar.appendChild(search);
  toolbar.appendChild(chipReady);
  toolbar.appendChild(chipDraft);
  toolbar.appendChild(exportButton);
  root.appendChild(toolbar);

  const tablePanel = createExampleSurface({
    name: "Dashboard Table Panel",
    width: contentWidth,
    padding: 16,
    itemSpacing: 14,
    variableByName,
    stats,
  });
  setVerticalStackChildSizing(tablePanel);
  tablePanel.appendChild(
    await createExampleInstance({
      componentSetName: "Table",
      variantProperties: { Density: "Compact" },
      name: "Dashboard Table",
      width: tableWidth,
      height: 160,
      fonts,
      variableByName,
      stats,
    }),
  );

  const paginationRow = createExampleRow(
    "Example Pagination Row",
    0,
    tableWidth,
  );
  paginationRow.primaryAxisAlignItems = "CENTER";
  setVerticalStackChildSizing(paginationRow);
  const pagination = await createExampleInstance({
    componentSetName: "Pagination",
    variantProperties: {
      Content: "Basic",
      Size: "Small",
    },
    name: "Dashboard Pagination",
    width: 332,
    height: 44,
    fonts,
    variableByName,
    stats,
  });
  paginationRow.appendChild(pagination);
  tablePanel.appendChild(paginationRow);
  root.appendChild(tablePanel);
  return root;
}

async function buildSettingsExampleTemplate({
  config,
  fonts,
  variableByName,
  stats,
}) {
  const contentWidth = config.width - 64;
  const fieldWidth = 360;
  const root = createExampleFrame({
    name: config.name,
    width: config.width,
    height: config.height,
    padding: 32,
    itemSpacing: 24,
    fillToken: "Surface/100",
    fillFallback: "#F6F7F9",
    variableByName,
    stats,
  });

  root.appendChild(
    createExampleText({
      name: "Page Title",
      characters: "Workspace settings",
      font: fonts.bold,
      fontSize: 28,
      lineHeight: 36,
      colorToken: "Colors/foreground/0",
      colorFallback: "#111111",
      width: contentWidth,
      variableByName,
      stats,
    }),
  );

  const panel = createExampleSurface({
    name: "Settings Panel",
    width: contentWidth,
    padding: 24,
    itemSpacing: 18,
    variableByName,
    stats,
  });
  setVerticalStackChildSizing(panel);
  panel.appendChild(
    await createExampleInstance({
      componentSetName: "Input",
      variantProperties: {
        State: "Default",
        Status: "Default",
      },
      name: "Project Name Input",
      textProperties: {
        "Label Text": "Project name",
        "Placeholder Text": "Downtown campus",
        "Helper Text": "Visible in dashboard navigation.",
      },
      width: fieldWidth,
      height: 70,
      fonts,
      variableByName,
      stats,
    }),
  );
  panel.appendChild(
    await createExampleInstance({
      componentSetName: "Select",
      variantProperties: {
        State: "Default",
        Status: "Default",
      },
      name: "Default Building Select",
      textProperties: {
        "Placeholder Text": "Default building",
      },
      width: fieldWidth,
      height: 44,
      fonts,
      variableByName,
      stats,
    }),
  );
  panel.appendChild(
    await createExampleInstance({
      componentSetName: "Switch",
      variantProperties: {
        Checked: "Checked",
        State: "Default",
      },
      name: "Enable Indoor Positioning Switch",
      textProperties: {
        "Label Text": "Enable indoor positioning",
      },
      width: 260,
      height: 44,
      fonts,
      variableByName,
      stats,
    }),
  );
  panel.appendChild(
    await createExampleInstance({
      componentSetName: "Textarea",
      variantProperties: {
        State: "Default",
        Status: "Default",
      },
      name: "Notes Textarea",
      textProperties: {
        "Label Text": "Notes",
        "Placeholder Text": "Add implementation notes",
      },
      width: fieldWidth,
      height: 132,
      fonts,
      variableByName,
      stats,
    }),
  );
  root.appendChild(panel);

  const footer = createExampleRow("Settings Footer", 10, contentWidth);
  footer.primaryAxisAlignItems = "MAX";
  setVerticalStackChildSizing(footer);
  footer.appendChild(
    await createExampleInstance({
      componentSetName: "Button",
      variantProperties: {
        Variant: "Outline",
        Size: "Default",
        State: "Default",
      },
      name: "Cancel Button",
      textProperties: { "Label Text": "Cancel" },
      width: 108,
      height: 44,
      fonts,
      variableByName,
      stats,
    }),
  );
  footer.appendChild(
    await createExampleInstance({
      componentSetName: "Button",
      variantProperties: {
        Variant: "Default",
        Size: "Default",
        State: "Default",
      },
      name: "Save Button",
      textProperties: { "Label Text": "Save changes" },
      width: 148,
      height: 44,
      fonts,
      variableByName,
      stats,
    }),
  );
  root.appendChild(footer);
  return root;
}

async function buildMobileDrawerExampleTemplate({
  config,
  fonts,
  variableByName,
  stats,
}) {
  const contentWidth = config.width - 32;
  const drawerContentWidth = contentWidth - 40;
  const root = createExampleFrame({
    name: config.name,
    width: config.width,
    height: config.height,
    padding: 16,
    itemSpacing: 16,
    fillToken: "Surface/100",
    fillFallback: "#F6F7F9",
    variableByName,
    stats,
  });
  root.cornerRadius = 32;

  const map = createExampleFrame({
    name: "Map Preview",
    width: contentWidth,
    height: 340,
    padding: 20,
    itemSpacing: 8,
    fillToken: "Surface/200",
    fillFallback: "#ECEEF2",
    variableByName,
    stats,
  });
  map.primaryAxisAlignItems = "MAX";
  map.counterAxisAlignItems = "MIN";
  setVerticalStackChildSizing(map);
  map.appendChild(
    createExampleText({
      name: "Map Label",
      characters: "Map canvas",
      font: fonts.medium,
      fontSize: 14,
      lineHeight: 20,
      colorToken: "Colors/foreground/400",
      colorFallback: "#5F6675",
      width: contentWidth - 40,
      variableByName,
      stats,
    }),
  );
  root.appendChild(map);

  const drawer = createExampleSurface({
    name: "Bottom Drawer Composition",
    width: contentWidth,
    padding: 20,
    itemSpacing: 16,
    variableByName,
    stats,
  });
  resizeNodeWithoutConstraints(drawer, contentWidth, 430);
  drawer.primaryAxisSizingMode = "FIXED";
  setVerticalFixedFillChildSizing(drawer);
  drawer.appendChild(
    createExampleText({
      name: "Drawer Title",
      characters: "Find a destination",
      font: fonts.bold,
      fontSize: 20,
      lineHeight: 28,
      colorToken: "Colors/foreground/0",
      colorFallback: "#111111",
      width: drawerContentWidth,
      variableByName,
      stats,
    }),
  );
  drawer.appendChild(
    await createExampleInstance({
      componentSetName: "Search",
      variantProperties: {
        State: "Default",
        Status: "Default",
      },
      name: "Destination Search",
      textProperties: {
        "Label Text": "Search",
        "Placeholder Text": "Search places",
      },
      width: drawerContentWidth,
      height: 70,
      fillHorizontal: true,
      fonts,
      variableByName,
      stats,
    }),
  );

  const chipRow = createExampleRow(
    "Destination Filters",
    8,
    drawerContentWidth,
  );
  setVerticalStackChildSizing(chipRow);
  chipRow.appendChild(
    await createExampleInstance({
      componentSetName: "Chip",
      variantProperties: {
        Variant: "Brand",
        Size: "Small",
        State: "Selected",
        Removable: "False",
      },
      name: "Open Now Chip",
      textProperties: { "Label Text": "Open" },
      width: 58,
      height: 28,
      fonts,
      variableByName,
      stats,
    }),
  );
  chipRow.appendChild(
    await createExampleInstance({
      componentSetName: "Chip",
      variantProperties: {
        Variant: "Neutral",
        Size: "Small",
        State: "Default",
        Removable: "False",
      },
      name: "Nearby Chip",
      textProperties: { "Label Text": "Nearby" },
      width: 72,
      height: 28,
      fonts,
      variableByName,
      stats,
    }),
  );
  drawer.appendChild(chipRow);
  drawer.appendChild(
    await createExampleInstance({
      componentSetName: "List",
      variantProperties: { Density: "Compact" },
      name: "Destination Results",
      width: drawerContentWidth,
      height: 120,
      fillHorizontal: true,
      fonts,
      variableByName,
      stats,
    }),
  );

  const footer = createExampleRow(
    "Mobile Drawer Footer",
    10,
    drawerContentWidth,
  );
  footer.primaryAxisAlignItems = "MAX";
  setVerticalStackChildSizing(footer);
  footer.appendChild(
    await createExampleInstance({
      componentSetName: "Button",
      variantProperties: {
        Variant: "Outline",
        Size: "Default",
        State: "Default",
      },
      name: "Clear Button",
      textProperties: { "Label Text": "Clear" },
      width: 96,
      height: 44,
      fonts,
      variableByName,
      stats,
    }),
  );
  footer.appendChild(
    await createExampleInstance({
      componentSetName: "Button",
      variantProperties: {
        Variant: "Default",
        Size: "Default",
        State: "Default",
      },
      name: "Route Button",
      textProperties: { "Label Text": "Start route" },
      width: 128,
      height: 44,
      fonts,
      variableByName,
      stats,
    }),
  );
  drawer.appendChild(footer);
  root.appendChild(drawer);
  return root;
}

async function buildOperationsConsoleExampleTemplate({
  config,
  fonts,
  variableByName,
  stats,
}) {
  const contentWidth = config.width - 64;
  const shellHeight = config.height - 156;
  const sidebarWidth = 248;
  const mainWidth = contentWidth - sidebarWidth - 24;
  const root = createExampleFrame({
    name: config.name,
    width: config.width,
    height: config.height,
    padding: 32,
    itemSpacing: 20,
    fillToken: "Surface/100",
    fillFallback: "#F6F7F9",
    variableByName,
    stats,
  });

  root.appendChild(
    await createExampleInstance({
      componentSetName: "Navbar",
      variantProperties: { Content: "Actions" },
      name: "Operations Navbar",
      width: contentWidth,
      height: 72,
      fillHorizontal: true,
      fonts,
      variableByName,
      stats,
    }),
  );

  const shell = createExampleRow("Operations Shell", 24, contentWidth);
  resizeNodeWithoutConstraints(shell, contentWidth, shellHeight);
  shell.counterAxisSizingMode = "FIXED";
  shell.counterAxisAlignItems = "MIN";
  setVerticalStackChildSizing(shell);

  shell.appendChild(
    await createExampleInstance({
      componentSetName: "Sidebar",
      variantProperties: { Content: "Sections" },
      name: "Operations Sidebar",
      width: sidebarWidth,
      height: shellHeight,
      fonts,
      variableByName,
      stats,
    }),
  );

  const main = createExampleStack("Operations Main", 18);
  resizeNodeWithoutConstraints(main, mainWidth, shellHeight);
  main.counterAxisSizingMode = "FIXED";
  main.primaryAxisSizingMode = "FIXED";
  setHorizontalStackFillChildSizing(main);

  const heading = createExampleRow("Operations Heading", 16, mainWidth);
  setVerticalStackChildSizing(heading);
  const headingCopy = createExampleStack("Operations Heading Copy", 6);
  setHorizontalStackFillChildSizing(headingCopy);
  headingCopy.appendChild(
    createExampleText({
      name: "Operations Title",
      characters: "Venue operations",
      font: fonts.bold,
      fontSize: 28,
      lineHeight: 36,
      colorToken: "Colors/foreground/0",
      colorFallback: "#111111",
      width: mainWidth - 180,
      variableByName,
      stats,
    }),
  );
  headingCopy.appendChild(
    createExampleText({
      name: "Operations Summary",
      characters:
        "Monitor map content, publish status, routing health, and support queues.",
      font: fonts.regular,
      fontSize: 14,
      lineHeight: 20,
      colorToken: "Colors/foreground/400",
      colorFallback: "#5F6675",
      width: mainWidth - 180,
      variableByName,
      stats,
    }),
  );
  heading.appendChild(headingCopy);
  heading.appendChild(
    await createExampleInstance({
      componentSetName: "Button",
      variantProperties: {
        Variant: "Default",
        Size: "Default",
        State: "Default",
      },
      name: "Publish Button",
      textProperties: { "Label Text": "Publish" },
      width: 132,
      height: 44,
      fonts,
      variableByName,
      stats,
    }),
  );
  main.appendChild(heading);

  const alert = await createExampleInstance({
    componentSetName: "Alert",
    variantProperties: { Variant: "Warning" },
    name: "Routing Warning Alert",
    textProperties: {
      Title: "Routing sync delayed",
      Description: "Three venues are waiting for map package review.",
    },
    width: mainWidth,
    height: 86,
    fillHorizontal: true,
    fonts,
    variableByName,
    stats,
  });
  main.appendChild(alert);

  const metrics = createExampleRow("Operations Metrics", 14, mainWidth);
  setVerticalStackChildSizing(metrics);
  const metricWidth = (mainWidth - 42) / 4;
  [
    ["Sites", "42"],
    ["Ready to publish", "8"],
    ["Open issues", "12"],
    ["SLA", "97%"],
  ].forEach(([label, value]) => {
    metrics.appendChild(
      createExampleMetricCard({
        label,
        value,
        width: metricWidth,
        fonts,
        variableByName,
        stats,
      }),
    );
  });
  main.appendChild(metrics);

  const filters = createExampleSurface({
    name: "Operations Filters",
    width: mainWidth,
    padding: 16,
    itemSpacing: 12,
    variableByName,
    stats,
  });
  filters.layoutMode = "HORIZONTAL";
  filters.primaryAxisSizingMode = "FIXED";
  filters.counterAxisSizingMode = "AUTO";
  filters.counterAxisAlignItems = "CENTER";
  filters.appendChild(
    await createExampleInstance({
      componentSetName: "SearchBar",
      variantProperties: { Variant: "Inline", State: "Default" },
      name: "Operations Search",
      textProperties: { "Placeholder Text": "Search venues, maps, issues" },
      width: 340,
      height: 52,
      growHorizontal: true,
      fonts,
      variableByName,
      stats,
    }),
  );
  filters.appendChild(
    await createExampleInstance({
      componentSetName: "DateRangePicker",
      variantProperties: {
        State: "Default",
        Status: "Default",
        Content: "Closed",
      },
      name: "Operations Date Range",
      width: 260,
      height: 68,
      fonts,
      variableByName,
      stats,
    }),
  );
  filters.appendChild(
    await createExampleInstance({
      componentSetName: "Chip",
      variantProperties: {
        Variant: "Brand",
        Size: "Small",
        State: "Selected",
        Removable: "True",
      },
      name: "Live Filter Chip",
      textProperties: { "Label Text": "Live" },
      width: 80,
      height: 32,
      fonts,
      variableByName,
      stats,
    }),
  );
  main.appendChild(filters);

  const content = createExampleRow("Operations Content", 16, mainWidth);
  resizeNodeWithoutConstraints(content, mainWidth, 266);
  content.counterAxisSizingMode = "FIXED";
  content.counterAxisAlignItems = "MIN";
  setVerticalStackChildSizing(content);

  const tablePanel = createExampleSurface({
    name: "Operations Table Panel",
    width: Math.round(mainWidth * 0.62),
    padding: 16,
    itemSpacing: 12,
    variableByName,
    stats,
  });
  setFixedChildSizing(tablePanel);
  tablePanel.appendChild(
    await createExampleInstance({
      componentSetName: "Table",
      variantProperties: { Density: "Compact" },
      name: "Operations Table",
      width: Math.round(mainWidth * 0.62) - 32,
      height: 190,
      fillHorizontal: true,
      fonts,
      variableByName,
      stats,
    }),
  );

  const activityPanel = createExampleSurface({
    name: "Operations Activity Panel",
    width: mainWidth - Math.round(mainWidth * 0.62) - 16,
    padding: 16,
    itemSpacing: 12,
    variableByName,
    stats,
  });
  setFixedChildSizing(activityPanel);
  activityPanel.appendChild(
    createExampleText({
      name: "Activity Title",
      characters: "Recent activity",
      font: fonts.bold,
      fontSize: 16,
      lineHeight: 22,
      colorToken: "Colors/foreground/0",
      colorFallback: "#111111",
      width: activityPanel.width - 32,
      variableByName,
      stats,
    }),
  );
  activityPanel.appendChild(
    await createExampleInstance({
      componentSetName: "Timeline",
      variantProperties: {
        Content: "Detailed",
        Density: "Compact",
      },
      name: "Operations Timeline",
      width: activityPanel.width - 32,
      height: 190,
      fillHorizontal: true,
      fonts,
      variableByName,
      stats,
    }),
  );

  content.appendChild(tablePanel);
  content.appendChild(activityPanel);
  main.appendChild(content);
  shell.appendChild(main);
  root.appendChild(shell);
  return root;
}

async function buildSdkRouteFlowExampleTemplate({
  config,
  fonts,
  variableByName,
  stats,
}) {
  const contentWidth = config.width - 32;
  const root = createExampleFrame({
    name: config.name,
    width: config.width,
    height: config.height,
    padding: 16,
    itemSpacing: 14,
    fillToken: "Surface/100",
    fillFallback: "#F6F7F9",
    variableByName,
    stats,
  });
  root.cornerRadius = 36;

  const map = createExampleFrame({
    name: "SDK Map Composition",
    width: contentWidth,
    height: 420,
    padding: 18,
    itemSpacing: 12,
    fillToken: "Surface/200",
    fillFallback: "#E8EEF5",
    variableByName,
    stats,
  });
  map.primaryAxisAlignItems = "MIN";
  map.counterAxisAlignItems = "CENTER";
  setVerticalStackChildSizing(map);

  map.appendChild(
    await createExampleInstance({
      componentSetName: "SearchBar",
      variantProperties: { Variant: "Floating", State: "Filled" },
      name: "SDK Floating Search",
      textProperties: { "Placeholder Text": "Gate B12 to Coffee" },
      width: contentWidth - 36,
      height: 56,
      fillHorizontal: true,
      fonts,
      variableByName,
      stats,
    }),
  );

  const mapStage = createExampleMapStage({
    name: "SDK Route Map Stage",
    width: contentWidth - 36,
    height: 300,
    fonts,
    variableByName,
    stats,
  });
  map.appendChild(mapStage);
  root.appendChild(map);

  const sheet = createExampleSurface({
    name: "SDK Route Sheet",
    width: contentWidth,
    padding: 20,
    itemSpacing: 14,
    variableByName,
    stats,
  });
  resizeNodeWithoutConstraints(sheet, contentWidth, 378);
  sheet.primaryAxisSizingMode = "FIXED";
  setVerticalFixedFillChildSizing(sheet);
  sheet.appendChild(
    createExampleText({
      name: "Route Sheet Title",
      characters: "Fastest accessible route",
      font: fonts.bold,
      fontSize: 20,
      lineHeight: 28,
      colorToken: "Colors/foreground/0",
      colorFallback: "#111111",
      width: contentWidth - 40,
      variableByName,
      stats,
    }),
  );

  const routeMeta = createExampleRow("Route Metadata", 8, contentWidth - 40);
  setVerticalStackChildSizing(routeMeta);
  routeMeta.appendChild(
    await createExampleInstance({
      componentSetName: "Badge",
      variantProperties: { Variant: "Default", Size: "Default" },
      name: "Route Time Badge",
      textProperties: { "Label Text": "8 min" },
      width: 72,
      height: 28,
      fonts,
      variableByName,
      stats,
    }),
  );
  routeMeta.appendChild(
    await createExampleInstance({
      componentSetName: "Badge",
      variantProperties: { Variant: "Secondary", Size: "Default" },
      name: "Accessible Badge",
      textProperties: { "Label Text": "Step-free" },
      width: 104,
      height: 28,
      fonts,
      variableByName,
      stats,
    }),
  );
  sheet.appendChild(routeMeta);

  sheet.appendChild(
    await createExampleInstance({
      componentSetName: "Progress",
      variantProperties: { Value: "50" },
      name: "Route Progress",
      width: contentWidth - 40,
      height: 16,
      fillHorizontal: true,
      fonts,
      variableByName,
      stats,
    }),
  );
  sheet.appendChild(
    await createExampleInstance({
      componentSetName: "List",
      variantProperties: { Density: "Compact" },
      name: "Route Steps List",
      width: contentWidth - 40,
      height: 132,
      fillHorizontal: true,
      fonts,
      variableByName,
      stats,
    }),
  );

  const actions = createExampleRow("Route Actions", 10, contentWidth - 40);
  actions.primaryAxisAlignItems = "MAX";
  setVerticalStackChildSizing(actions);
  actions.appendChild(
    await createExampleInstance({
      componentSetName: "Button",
      variantProperties: {
        Variant: "Outline",
        Size: "Default",
        State: "Default",
      },
      name: "Share Route Button",
      textProperties: { "Label Text": "Share" },
      width: 96,
      height: 44,
      fonts,
      variableByName,
      stats,
    }),
  );
  actions.appendChild(
    await createExampleInstance({
      componentSetName: "Button",
      variantProperties: {
        Variant: "Default",
        Size: "Default",
        State: "Default",
      },
      name: "Start Navigation Button",
      textProperties: { "Label Text": "Start" },
      width: 108,
      height: 44,
      fonts,
      variableByName,
      stats,
    }),
  );
  sheet.appendChild(actions);
  root.appendChild(sheet);

  root.appendChild(
    await createExampleInstance({
      componentSetName: "BottomNavigation",
      variantProperties: { Count: "Three", Active: "Two" },
      name: "SDK Bottom Navigation",
      width: contentWidth,
      height: 64,
      fillHorizontal: true,
      fonts,
      variableByName,
      stats,
    }),
  );
  return root;
}

async function buildKioskHandoffExampleTemplate({
  config,
  fonts,
  variableByName,
  stats,
}) {
  const contentWidth = config.width - 72;
  const root = createExampleFrame({
    name: config.name,
    width: config.width,
    height: config.height,
    padding: 36,
    itemSpacing: 20,
    fillToken: "Surface/100",
    fillFallback: "#F6F7F9",
    variableByName,
    stats,
  });

  const header = createExampleRow("Kiosk Header", 20, contentWidth);
  setVerticalStackChildSizing(header);
  const titleStack = createExampleStack("Kiosk Header Copy", 6);
  setHorizontalStackFillChildSizing(titleStack);
  titleStack.appendChild(
    createExampleText({
      name: "Kiosk Title",
      characters: "Dubai International",
      font: fonts.bold,
      fontSize: 24,
      lineHeight: 30,
      colorToken: "Colors/foreground/0",
      colorFallback: "#111111",
      width: contentWidth - 286,
      variableByName,
      stats,
    }),
  );
  titleStack.appendChild(
    createExampleText({
      name: "Kiosk Subtitle",
      characters: "Find a route, preview it, and send it to your phone.",
      font: fonts.regular,
      fontSize: 13,
      lineHeight: 20,
      colorToken: "Colors/foreground/400",
      colorFallback: "#5F6675",
      width: contentWidth - 286,
      variableByName,
      stats,
    }),
  );
  header.appendChild(titleStack);
  header.appendChild(
    await createExampleInstance({
      componentSetName: "SegmentedControl",
      variantProperties: {
        Size: "Small",
        Active: "One",
        State: "Default",
      },
      name: "Kiosk View Tabs",
      width: 246,
      height: 36,
      fonts,
      variableByName,
      stats,
    }),
  );
  root.appendChild(header);

  const body = createExampleRow("Kiosk Body", 20, contentWidth);
  resizeNodeWithoutConstraints(body, contentWidth, 456);
  body.counterAxisSizingMode = "FIXED";
  body.counterAxisAlignItems = "MIN";
  setVerticalStackChildSizing(body);

  const routePanel = createExampleSurface({
    name: "Kiosk Route Panel",
    width: 360,
    padding: 18,
    itemSpacing: 14,
    variableByName,
    stats,
  });
  resizeNodeWithoutConstraints(routePanel, 360, 456);
  routePanel.primaryAxisSizingMode = "FIXED";
  setFixedChildSizing(routePanel);
  routePanel.appendChild(
    await createExampleInstance({
      componentSetName: "SearchBar",
      variantProperties: { Variant: "Inline", State: "Default" },
      name: "Kiosk Destination Search",
      textProperties: { "Placeholder Text": "Where are you going?" },
      width: 324,
      height: 44,
      fillHorizontal: true,
      fonts,
      variableByName,
      stats,
    }),
  );
  routePanel.appendChild(
    await createExampleInstance({
      componentSetName: "Combobox",
      variantProperties: {
        State: "Default",
        Status: "Default",
        Content: "Open",
      },
      name: "Kiosk Suggested Destinations",
      textProperties: {
        "Label Text": "Destination",
        "Placeholder Text": "Search stations",
        "Option 1 Text": "Metro station",
        "Option 2 Text": "Bus stop",
        "Option 3 Text": "Bike parking",
      },
      width: 324,
      height: 188,
      fillHorizontal: true,
      fonts,
      variableByName,
      stats,
    }),
  );
  routePanel.appendChild(
    await createExampleInstance({
      componentSetName: "Switch",
      variantProperties: {
        Checked: "Checked",
        State: "Default",
      },
      name: "Accessible Route Switch",
      textProperties: { "Label Text": "Step-free route" },
      width: 220,
      height: 36,
      fonts,
      variableByName,
      stats,
    }),
  );
  routePanel.appendChild(
    await createExampleInstance({
      componentSetName: "Button",
      variantProperties: {
        Variant: "Default",
        Size: "Default",
        State: "Default",
      },
      name: "Kiosk Find Route Button",
      textProperties: { "Label Text": "Find route" },
      width: 324,
      height: 44,
      fillHorizontal: true,
      fonts,
      variableByName,
      stats,
    }),
  );

  const previewPanel = createExampleSurface({
    name: "Kiosk Preview Panel",
    width: contentWidth - 380,
    padding: 18,
    itemSpacing: 14,
    variableByName,
    stats,
  });
  resizeNodeWithoutConstraints(previewPanel, contentWidth - 380, 456);
  previewPanel.primaryAxisSizingMode = "FIXED";
  setFixedChildSizing(previewPanel);
  previewPanel.appendChild(
    createExampleMapStage({
      name: "Kiosk Map Preview",
      width: previewPanel.width - 36,
      height: 224,
      fonts,
      variableByName,
      stats,
    }),
  );
  previewPanel.appendChild(
    await createExampleInstance({
      componentSetName: "Alert",
      variantProperties: { Variant: "Info" },
      name: "Kiosk Handoff Alert",
      textProperties: {
        Title: "Route ready",
        Description: "Scan after preview to continue on your phone.",
      },
      width: previewPanel.width - 36,
      height: 76,
      fillHorizontal: true,
      fonts,
      variableByName,
      stats,
    }),
  );
  const previewActions = createExampleRow(
    "Kiosk Preview Actions",
    12,
    previewPanel.width - 36,
  );
  previewActions.primaryAxisAlignItems = "MAX";
  setVerticalStackChildSizing(previewActions);
  previewActions.appendChild(
    await createExampleInstance({
      componentSetName: "Button",
      variantProperties: {
        Variant: "Outline",
        Size: "Default",
        State: "Default",
      },
      name: "Kiosk Print Button",
      textProperties: { "Label Text": "Print" },
      width: 88,
      height: 44,
      fonts,
      variableByName,
      stats,
    }),
  );
  previewActions.appendChild(
    await createExampleInstance({
      componentSetName: "Button",
      variantProperties: {
        Variant: "Default",
        Size: "Default",
        State: "Default",
      },
      name: "Kiosk Send Button",
      textProperties: { "Label Text": "Show QR" },
      width: 108,
      height: 44,
      fonts,
      variableByName,
      stats,
    }),
  );
  previewPanel.appendChild(previewActions);

  body.appendChild(routePanel);
  body.appendChild(previewPanel);
  root.appendChild(body);
  return root;
}

function createExampleMapStage({
  name,
  width,
  height,
  fonts,
  variableByName,
  stats,
}) {
  const stage = createExampleFrame({
    name,
    width,
    height,
    padding: 20,
    itemSpacing: 10,
    fillToken: "Surface/200",
    fillFallback: "#E8EEF5",
    variableByName,
    stats,
  });
  stage.counterAxisAlignItems = "CENTER";
  stage.primaryAxisAlignItems = "CENTER";

  const path = figma.createFrame();
  path.name = "Route Path Preview";
  path.layoutMode = "HORIZONTAL";
  path.primaryAxisSizingMode = "FIXED";
  path.counterAxisSizingMode = "FIXED";
  path.primaryAxisAlignItems = "CENTER";
  path.counterAxisAlignItems = "CENTER";
  path.itemSpacing = 8;
  const markerSizes = [18, 12, 12, 12, 18];
  const markerWidth = markerSizes.reduce((total, size) => total + size, 0);
  const routeGapWidth = (markerSizes.length * 2 - 2) * path.itemSpacing;
  const maxRouteWidth = Math.max(220, width - 48);
  const segmentWidth = Math.max(
    24,
    Math.floor((maxRouteWidth - markerWidth - routeGapWidth) / 4),
  );
  const routeWidth = markerWidth + segmentWidth * 4 + routeGapWidth;
  path.resizeWithoutConstraints(routeWidth, 56);
  path.fills = [];
  path.strokes = [];

  for (let index = 0; index < 5; index += 1) {
    if (index > 0) {
      const segment = figma.createRectangle();
      segment.name = "Route Segment";
      segment.resizeWithoutConstraints(segmentWidth, 4);
      segment.cornerRadius = 999;
      segment.fills = [
        paintFromVariable("Colors/theme/500", "#135BEC", variableByName, stats),
      ];
      path.appendChild(segment);
    }

    const marker = figma.createEllipse();
    marker.name = index === 0 ? "Origin Marker" : "Route Marker";
    const markerSize = markerSizes[index];
    marker.resizeWithoutConstraints(markerSize, markerSize);
    marker.fills = [
      paintFromVariable(
        index === 4 ? "Colors/emotional/success/500" : "Colors/theme/500",
        index === 4 ? "#16A34A" : "#2563EB",
        variableByName,
        stats,
      ),
    ];
    marker.strokes = [
      paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
    ];
    marker.strokeWeight = 2;
    path.appendChild(marker);
  }

  stage.appendChild(path);
  stage.appendChild(
    createExampleText({
      name: "Map Stage Label",
      characters: "Route preview placeholder",
      font: fonts.medium,
      fontSize: 11,
      lineHeight: 16,
      colorToken: "Colors/foreground/400",
      colorFallback: "#5F6675",
      width: width - 56,
      variableByName,
      stats,
    }),
  );
  return stage;
}

function createExampleFrame({
  name,
  width,
  height,
  padding,
  itemSpacing,
  fillToken,
  fillFallback,
  variableByName,
  stats,
}) {
  const frame = figma.createFrame();
  frame.name = name;
  frame.layoutMode = "VERTICAL";
  frame.primaryAxisSizingMode = "FIXED";
  frame.counterAxisSizingMode = "FIXED";
  frame.primaryAxisAlignItems = "MIN";
  frame.counterAxisAlignItems = "MIN";
  frame.itemSpacing = itemSpacing || 0;
  frame.paddingLeft = padding || 0;
  frame.paddingRight = padding || 0;
  frame.paddingTop = padding || 0;
  frame.paddingBottom = padding || 0;
  frame.resizeWithoutConstraints(width, height);
  frame.fills = [
    paintFromVariable(fillToken, fillFallback, variableByName, stats),
  ];
  frame.strokes = [];
  frame.setSharedPluginData(RUN_NAMESPACE, "kind", "example-frame");
  return frame;
}

function createExampleSurface({
  name,
  width,
  padding,
  itemSpacing,
  variableByName,
  stats,
}) {
  const surface = createExampleFrame({
    name,
    width,
    height: 120,
    padding,
    itemSpacing,
    fillToken: "Surface/0",
    fillFallback: "#FFFFFF",
    variableByName,
    stats,
  });
  surface.primaryAxisSizingMode = "AUTO";
  surface.cornerRadius = 12;
  surface.strokes = [
    paintFromVariable(
      "Colors/background/200",
      "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  surface.strokeWeight = 1;
  return surface;
}

function createExampleStack(name, gap) {
  const frame = figma.createFrame();
  frame.name = name;
  frame.layoutMode = "VERTICAL";
  frame.primaryAxisSizingMode = "AUTO";
  frame.counterAxisSizingMode = "AUTO";
  frame.primaryAxisAlignItems = "MIN";
  frame.counterAxisAlignItems = "MIN";
  frame.itemSpacing = gap || 0;
  frame.fills = [];
  frame.strokes = [];
  return frame;
}

function createExampleRow(name, gap, width) {
  const frame = figma.createFrame();
  frame.name = name;
  frame.layoutMode = "HORIZONTAL";
  frame.primaryAxisSizingMode = width ? "FIXED" : "AUTO";
  frame.counterAxisSizingMode = "AUTO";
  frame.primaryAxisAlignItems = "MIN";
  frame.counterAxisAlignItems = "CENTER";
  frame.itemSpacing = gap || 0;
  frame.fills = [];
  frame.strokes = [];
  if (width) resizeNodeWithoutConstraints(frame, width, 44);
  return frame;
}

function createExampleMetricCard({
  label,
  value,
  width,
  fonts,
  variableByName,
  stats,
}) {
  const card = createExampleSurface({
    name: `${label} Metric`,
    width: 0,
    padding: 16,
    itemSpacing: 6,
    variableByName,
    stats,
  });
  resizeNodeWithoutConstraints(card, width || 240, 84);
  setVerticalStackChildSizing(card);
  card.appendChild(
    createExampleText({
      name: "Metric Label",
      characters: label,
      font: fonts.regular,
      fontSize: 12,
      lineHeight: 16,
      colorToken: "Colors/foreground/400",
      colorFallback: "#5F6675",
      width: (width || 240) - 32,
      variableByName,
      stats,
    }),
  );
  card.appendChild(
    createExampleText({
      name: "Metric Value",
      characters: value,
      font: fonts.bold,
      fontSize: 20,
      lineHeight: 26,
      colorToken: "Colors/foreground/0",
      colorFallback: "#111111",
      width: (width || 240) - 32,
      variableByName,
      stats,
    }),
  );
  return card;
}

function createExampleText({
  name,
  characters,
  font,
  fontSize,
  lineHeight,
  colorToken,
  colorFallback,
  width,
  variableByName,
  stats,
}) {
  const text = figma.createText();
  text.name = name;
  text.fontName = font;
  text.fontSize = fontSize;
  text.lineHeight = { unit: "PIXELS", value: lineHeight };
  text.fills = [
    paintFromVariable(colorToken, colorFallback, variableByName, stats),
  ];
  if (width) resizeNodeWithoutConstraints(text, width, lineHeight);
  setTextAutoResize(text, "HEIGHT");
  setExampleChildSizing(text, "vertical-fill");
  text.characters = characters;
  return text;
}

function setHorizontalFillTextSizing(node) {
  setLayoutSizingHorizontal(node, "FILL");
  setLayoutSizingVertical(node, "HUG");

  try {
    node.layoutGrow = 1;
  } catch (_error) {
    // layoutGrow is unavailable on older plugin runtimes.
  }

  try {
    node.layoutAlign = "CENTER";
  } catch (_error) {
    // layoutAlign is unavailable on older plugin runtimes.
  }
}

function setExampleChildSizing(node, sizingMode) {
  if (!node || !sizingMode) return;

  try {
    node.setSharedPluginData(
      RUN_NAMESPACE,
      EXAMPLE_CHILD_SIZING_DATA_KEY,
      sizingMode,
    );
  } catch (_error) {
    // Shared plugin data is unavailable for a small subset of node-like values.
  }

  applyExampleChildSizing(node, sizingMode);
}

function normalizeExampleDeferredChildSizing(node) {
  if (!node || !node.children) return;

  for (const child of node.children) {
    let sizingMode = "";
    try {
      sizingMode =
        child.getSharedPluginData(
          RUN_NAMESPACE,
          EXAMPLE_CHILD_SIZING_DATA_KEY,
        ) || "";
    } catch (_error) {
      sizingMode = "";
    }

    if (sizingMode) applyExampleChildSizing(child, sizingMode);
    normalizeExampleDeferredChildSizing(child);
  }
}

function applyExampleChildSizing(node, sizingMode) {
  if (sizingMode === "horizontal-fill-fixed") {
    setHorizontalStackFillFixedChildSizing(node);
  } else if (sizingMode === "horizontal-fill-hug") {
    setHorizontalStackFillHugChildSizing(node);
  } else if (sizingMode === "vertical-fill") {
    setVerticalStackChildSizing(node);
  } else if (sizingMode === "fixed") {
    setFixedChildSizing(node);
  } else if (sizingMode === "hug") {
    setHugChildSizing(node);
  }
}

async function createExampleInstance({
  componentSetName,
  variantProperties,
  name,
  textProperties,
  booleanProperties,
  width,
  height,
  fillHorizontal,
  growHorizontal,
  fonts,
  variableByName,
  stats,
}) {
  const created = await createNestedComponentInstance({
    componentSetName,
    variantProperties: variantProperties || {},
    name,
    role: "example",
    stats,
  });

  if (!created) {
    const placeholder = createExamplePlaceholder({
      name,
      label: componentSetName,
      width: width || 180,
      height: height || 44,
      fonts,
      variableByName,
      stats,
    });
    if (growHorizontal) {
      if (height) {
        setExampleChildSizing(placeholder, "horizontal-fill-fixed");
      } else {
        setExampleChildSizing(placeholder, "horizontal-fill-hug");
      }
    } else if (fillHorizontal) {
      setExampleChildSizing(placeholder, "vertical-fill");
    } else if (width && height) {
      setExampleChildSizing(placeholder, "fixed");
    } else {
      setExampleChildSizing(placeholder, "hug");
    }
    return placeholder;
  }

  const { componentSet, instance } = created;
  if (textProperties) {
    for (const [baseName, value] of Object.entries(textProperties)) {
      setInstanceTextProperty(instance, componentSet, baseName, value, stats);
    }
  }
  if (booleanProperties) {
    for (const [baseName, value] of Object.entries(booleanProperties)) {
      setInstanceBooleanProperty(
        instance,
        componentSet,
        baseName,
        value,
        stats,
      );
    }
  }

  if (width && height) resizeNodeWithoutConstraints(instance, width, height);
  if (growHorizontal) {
    if (height) {
      setExampleChildSizing(instance, "horizontal-fill-fixed");
    } else {
      setExampleChildSizing(instance, "horizontal-fill-hug");
    }
  } else if (fillHorizontal) {
    setExampleChildSizing(instance, "vertical-fill");
  } else if (width && height) {
    setExampleChildSizing(instance, "fixed");
  } else {
    setExampleChildSizing(instance, "hug");
  }
  stats.instancesCreated += 1;
  return instance;
}

function createExamplePlaceholder({
  name,
  label,
  width,
  height,
  fonts,
  variableByName,
  stats,
}) {
  const placeholder = figma.createFrame();
  placeholder.name = name || `Missing ${label}`;
  placeholder.layoutMode = "HORIZONTAL";
  placeholder.primaryAxisSizingMode = "FIXED";
  placeholder.counterAxisSizingMode = "FIXED";
  placeholder.primaryAxisAlignItems = "CENTER";
  placeholder.counterAxisAlignItems = "CENTER";
  placeholder.paddingLeft = 12;
  placeholder.paddingRight = 12;
  placeholder.paddingTop = 8;
  placeholder.paddingBottom = 8;
  placeholder.cornerRadius = 8;
  placeholder.resizeWithoutConstraints(width, height);
  placeholder.fills = [
    paintFromVariable("Surface/200", "#ECEEF2", variableByName, stats),
  ];
  placeholder.strokes = [
    paintFromVariable(
      "Colors/background/200",
      "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  placeholder.strokeWeight = 1;
  placeholder.appendChild(
    createExampleText({
      name: "Placeholder Text",
      characters: `Missing ${label}`,
      font: fonts.medium,
      fontSize: 12,
      lineHeight: 16,
      colorToken: "Colors/foreground/400",
      colorFallback: "#5F6675",
      width: Math.max(width - 24, 40),
      variableByName,
      stats,
    }),
  );
  placeholder.setSharedPluginData(RUN_NAMESPACE, "kind", "example-placeholder");
  stats.placeholdersCreated += 1;
  return placeholder;
}

async function documentComponentLibrary() {
  const stats = {
    documented: 0,
    descriptionsUpdated: 0,
    pagesCreated: 0,
    pagesUpdated: 0,
    splitPagesRemoved: 0,
    previewsCreated: 0,
    missingComponentSets: [],
    page: null,
    sections: [],
    warnings: [],
  };

  const fonts = await loadButtonFonts(stats);
  const componentSets = await findComponentSetsByName(COMPONENT_DOCS);
  await removeGeneratedSplitDocPages(stats);

  const existed = Boolean(
    figma.root.children.find((page) => page.name === COMPONENT_DOCS_PAGE_NAME),
  );
  const page = await ensurePage(COMPONENT_DOCS_PAGE_NAME);
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();
  removeGeneratedComponentDocs(page);

  const header = createDocsCatalogHeaderFrame(fonts);
  page.appendChild(header);

  let columnIndex = 0;
  let rowY = DOCS_CANVAS_Y + header.height + 48;
  let rowHeight = 0;

  for (let docIndex = 0; docIndex < COMPONENT_DOCS.length; docIndex += 1) {
    const doc = COMPONENT_DOCS[docIndex];
    const componentSet = componentSets[doc.componentSetName];
    if (!componentSet) {
      stats.missingComponentSets.push(doc.componentSetName);
      continue;
    }

    applyComponentSetDescription(
      componentSet,
      doc.componentSetName,
      false,
      null,
    );
    stats.descriptionsUpdated += 1;

    const section = createComponentDocsRoot(doc, componentSet, fonts, stats);
    section.x =
      DOCS_CANVAS_X + columnIndex * (DOCS_SECTION_WIDTH + DOCS_SECTION_GAP);
    section.y = rowY;
    page.appendChild(section);

    stats.documented += 1;
    stats.sections.push({
      name: doc.componentSetName,
      componentSetId: componentSet.id,
      urlNodeId: nodeIdForUrl(componentSet.id),
      x: section.x,
      y: section.y,
    });

    rowHeight = Math.max(rowHeight, docsMeasuredHeight(section));
    columnIndex += 1;
    if (columnIndex >= DOCS_COLUMNS) {
      columnIndex = 0;
      rowY += rowHeight + DOCS_ROW_GAP;
      rowHeight = 0;
    }
  }

  if (existed) {
    stats.pagesUpdated += 1;
  } else {
    stats.pagesCreated += 1;
  }
  stats.page = {
    name: page.name,
    id: page.id,
  };

  if (stats.missingComponentSets.length > 0) {
    stats.warnings.push(
      `Missing component sets: ${stats.missingComponentSets.join(", ")}`,
    );
  }

  stats.message = `Documented ${stats.documented} component set(s) in a ${DOCS_COLUMNS}-column Docs page grid and updated ${stats.descriptionsUpdated} description field(s).`;
  return stats;
}

async function buildSurfaceQaPage() {
  const stats = {
    page: null,
    panelsCreated: 0,
    instancesCreated: 0,
    modeBindingsApplied: 0,
    missingComponentSets: [],
    missingVariants: [],
    warnings: [],
  };

  const fonts = await loadButtonFonts(stats);
  await ensureKozmosTextStyles(fonts, stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const collections = await figma.variables.getLocalVariableCollectionsAsync();
  const componentSets = await findComponentSetsByName(surfaceQaComponentDocs());
  const existed = Boolean(
    figma.root.children.find((page) => page.name === SURFACE_QA_PAGE_NAME),
  );
  const page = await ensurePage(SURFACE_QA_PAGE_NAME);
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();
  removeGeneratedSurfaceQa(page);

  const header = createSurfaceQaHeader(fonts);
  page.appendChild(header);

  const panels = [
    {
      title: "Surface/0 - Light",
      subtitle: "Transparent variants on the default product surface.",
      modeName: "Light",
      productSurface: false,
      fillToken: "Surface/0",
      fillFallback: "#FFFFFF",
      textTone: "light",
    },
    {
      title: "Surface/0 - Dark",
      subtitle: "The same live instances with the Dark variable mode applied.",
      modeName: "Dark",
      productSurface: false,
      fillToken: "Surface/0",
      fillFallback: "#101114",
      textTone: "dark",
    },
    {
      title: "Product map - Light",
      subtitle:
        "Representative indoor map surface with fill-extrusion and symbol_label layers.",
      modeName: "Light",
      productSurface: true,
      fillFallback: "#EEF3F8",
      textTone: "light",
    },
    {
      title: "Product map - Dark",
      subtitle:
        "Dark product-map host surface for glass, ghost, and transparent controls.",
      modeName: "Dark",
      productSurface: true,
      fillFallback: "#151B22",
      textTone: "dark",
    },
  ];

  for (let index = 0; index < panels.length; index += 1) {
    const panel = createSurfaceQaPanel(
      panels[index],
      componentSets,
      collections,
      variableByName,
      fonts,
      stats,
    );
    panel.x =
      SURFACE_QA_CANVAS_X +
      (index % 2) * (SURFACE_QA_PANEL_WIDTH + SURFACE_QA_PANEL_GAP);
    panel.y = SURFACE_QA_CANVAS_Y + 170 + Math.floor(index / 2) * 760;
    page.appendChild(panel);
    stats.panelsCreated += 1;
  }

  stats.page = {
    name: page.name,
    id: page.id,
    existed,
  };
  stats.message = `Built ${stats.panelsCreated} transparent surface QA panel(s) with ${stats.instancesCreated} live component instance(s).`;
  return stats;
}

function surfaceQaComponentDocs() {
  const requested = {};
  for (const group of SURFACE_QA_COMPONENT_GROUPS) {
    for (const row of group.rows) {
      for (const spec of row) requested[spec.componentSetName] = true;
    }
  }

  return Object.keys(requested).map((componentSetName) => ({
    componentSetName,
  }));
}

/**
 * Split an ordered list of heights into at most `columnCount` contiguous
 * groups, minimising the tallest group.
 *
 * Contiguous matters: each column then holds a consecutive slice of the
 * section's alphabetized list, so reading order stays column-major (down, then
 * across) the way a newspaper reads. Binary-searching the height limit and
 * greedily filling is exact for this problem and cheap at these sizes.
 */
function partitionIntoContiguousColumns(heights, columnCount) {
  if (heights.length === 0) return [];

  const limitCount = Math.max(1, Math.min(columnCount, heights.length));
  if (limitCount === 1) return [heights.map((_value, index) => index)];

  let low = 0;
  let high = 0;

  for (const height of heights) {
    low = Math.max(low, height);
    high += height;
  }

  const fits = (limit) => {
    let used = 1;
    let current = 0;

    for (const height of heights) {
      if (current > 0 && current + height > limit) {
        used += 1;
        current = height;
      } else {
        current += height;
      }
    }

    return used <= limitCount;
  };

  while (low < high) {
    const mid = Math.floor((low + high) / 2);
    if (fits(mid)) high = mid;
    else low = mid + 1;
  }

  const groups = [[]];
  let current = 0;

  for (let index = 0; index < heights.length; index += 1) {
    const group = groups[groups.length - 1];

    if (group.length > 0 && current + heights[index] > low) {
      groups.push([index]);
      current = heights[index];
      continue;
    }

    group.push(index);
    current += heights[index];
  }

  return groups;
}

/**
 * Flow blocks into the column count whose resulting bounding box is closest to
 * square, and return the placement.
 *
 * Reviewability is what this optimises. A tall ribbon and a wide ribbon are
 * equally awkward to scan, and a roughly square block is what Figma's
 * zoom-to-fit is good at. Deriving the count from the measured sizes rather
 * than a target-height constant means it keeps choosing well as sets are added,
 * with nothing to re-tune.
 *
 * Each block is `{ width, height }`; `gap` separates blocks within a column and
 * `columnGap` separates the columns.
 */
function packBlocksIntoColumns(blocks, gap, columnGap, maxColumns) {
  if (blocks.length === 0) {
    return { groups: [], columns: [], width: 0, height: 0 };
  }

  const packHeights = blocks.map((block) => block.height + gap);
  const limit = Math.max(1, Math.min(maxColumns, blocks.length));
  let best = null;

  for (let count = 1; count <= limit; count += 1) {
    const groups = partitionIntoContiguousColumns(packHeights, count);
    const columns = [];
    let width = 0;
    let height = 0;

    for (const group of groups) {
      let columnWidth = 0;
      let columnHeight = 0;

      for (const index of group) {
        columnWidth = Math.max(columnWidth, blocks[index].width);
        columnHeight += packHeights[index];
      }

      columnHeight = Math.max(0, columnHeight - gap);
      columns.push({ x: width, width: columnWidth, height: columnHeight });
      width += columnWidth + columnGap;
      height = Math.max(height, columnHeight);
    }

    width = Math.max(0, width - columnGap);
    const aspect =
      Math.max(width, height) / Math.max(1, Math.min(width, height));

    if (!best || aspect < best.aspect) {
      best = { groups, columns, width, height, aspect };
    }
  }

  return best;
}

function componentSetFootprintHeight(node, bounds) {
  return Math.max(
    bounds.height,
    COMPONENT_PAGE_LAYOUT_MIN_HEIGHTS[canonicalComponentSetName(node.name)] ||
      0,
    COMPONENT_PAGE_LAYOUT_MIN_FOOTPRINT_HEIGHT,
  );
}

/**
 * Lay one section's component sets out in columns, relative to the section's
 * content origin. Column widths are independent because set widths in this
 * library span roughly 200px to several thousand, so a uniform column width
 * would either clip the wide ones or strand whitespace beside the narrow ones.
 */
function layoutComponentSetColumns(nodes) {
  const entries = [];

  for (const node of nodes) {
    const bounds = measureComponentSetLayoutBounds(node);
    entries.push({
      node,
      bounds,
      footprintHeight: componentSetFootprintHeight(node, bounds),
      x: 0,
      y: 0,
    });
  }

  const packed = packBlocksIntoColumns(
    entries.map((entry) => ({
      width: entry.bounds.width,
      height: entry.footprintHeight,
    })),
    COMPONENT_PAGE_LAYOUT_ROW_GAP,
    COMPONENT_PAGE_LAYOUT_COLUMN_GAP,
    COMPONENT_PAGE_LAYOUT_MAX_COLUMNS,
  );

  packed.groups.forEach((group, columnIndex) => {
    const column = packed.columns[columnIndex];
    let columnY = 0;

    for (const index of group) {
      const entry = entries[index];
      entry.x = column.x;
      entry.y = columnY;
      columnY += entry.footprintHeight + COMPONENT_PAGE_LAYOUT_ROW_GAP;
    }

    column.componentCount = group.length;
  });

  return {
    entries,
    columns: packed.columns,
    contentWidth: packed.width,
    contentHeight: packed.height,
  };
}

async function reorganizeComponentsPage() {
  const stats = {
    page: COMPONENTS_PAGE_NAME,
    preservedNodeIds: true,
    movedComponentSets: 0,
    renamedComponentSets: 0,
    unexpectedTopLevelInstancesRemoved: 0,
    skippedComponentSets: [],
    sections: [],
    positions: [],
    warnings: [],
  };
  const page = figma.root.children.find(
    (child) => child.name === COMPONENTS_PAGE_NAME,
  );

  if (!page) {
    stats.message =
      "Components page was not found. Build at least one component set first.";
    return stats;
  }

  await figma.setCurrentPageAsync(page);
  await page.loadAsync();
  removeUnexpectedTopLevelGeneratedInstances(page, stats);
  removeDuplicateGeneratedComponentSetsForKnownComponents(page, stats);

  const fonts = await loadButtonFonts(stats);
  const activeSectionTitles = {};
  const sectionNodes = [];
  for (const section of COMPONENT_PAGE_LAYOUT_SECTIONS) {
    const nodes = [];

    for (const name of section.components) {
      const node = findComponentSetOnPage(page, name);

      if (node) {
        normalizeComponentSetNodeName(node, name, stats);
        nodes.push(node);
      } else {
        stats.skippedComponentSets.push(name);
      }
    }

    if (nodes.length === 0) continue;

    activeSectionTitles[section.title] = true;
    sectionNodes.push({
      title: section.title,
      nodes,
    });
  }

  if (sectionNodes.length === 0) {
    stats.message = "No known component sets were found to reorganize.";
    return stats;
  }

  const useNativeSections = canCreateComponentLayoutSection();
  removeGeneratedComponentSectionHeadings(page, activeSectionTitles, {
    removeActive: useNativeSections,
  });
  removeStaleComponentLayoutSections(page, activeSectionTitles);

  // Measure every section before placing any of it: packing the sections
  // themselves into columns needs all the block sizes up front.
  const measuredSections = [];

  for (const section of sectionNodes) {
    const layout = layoutComponentSetColumns(section.nodes);
    const heading = useNativeSections
      ? null
      : upsertComponentSectionHeading(page, section.title, fonts);
    const contentOffsetY = heading
      ? Math.ceil(heading.height || 0) +
        COMPONENT_PAGE_LAYOUT_SECTION_HEADING_GAP
      : COMPONENT_PAGE_LAYOUT_SECTION_PADDING_TOP;
    const trailing = useNativeSections
      ? COMPONENT_PAGE_LAYOUT_SECTION_PADDING_BOTTOM
      : 0;
    const inset = useNativeSections
      ? COMPONENT_PAGE_LAYOUT_SECTION_PADDING_X
      : 0;

    measuredSections.push({
      section,
      layout,
      heading,
      contentOffsetX: inset,
      contentOffsetY,
      width: layout.contentWidth + inset * 2,
      height: contentOffsetY + layout.contentHeight + trailing,
    });
  }

  const sectionGrid = packBlocksIntoColumns(
    measuredSections,
    COMPONENT_PAGE_LAYOUT_SECTION_GAP,
    COMPONENT_PAGE_LAYOUT_SECTION_COLUMN_GAP,
    COMPONENT_PAGE_LAYOUT_MAX_SECTION_COLUMNS,
  );
  stats.sectionColumns = sectionGrid.columns.length;
  stats.pageWidth = sectionGrid.width;
  stats.pageHeight = sectionGrid.height;

  sectionGrid.groups.forEach((group, columnIndex) => {
    const column = sectionGrid.columns[columnIndex];
    let columnY = COMPONENT_PAGE_LAYOUT_Y;

    for (const index of group) {
      const block = measuredSections[index];
      placeComponentSection(
        page,
        block,
        COMPONENT_PAGE_LAYOUT_X + column.x,
        columnY,
        useNativeSections,
        stats,
      );
      columnY += block.height + COMPONENT_PAGE_LAYOUT_SECTION_GAP;
    }
  });

  if (stats.skippedComponentSets.length > 0) {
    stats.warnings.push(
      `${stats.skippedComponentSets.length} known component set(s) were not present on the Components page.`,
    );
  }

  const renameSummary =
    stats.renamedComponentSets > 0
      ? ` Renamed ${stats.renamedComponentSets} legacy / v1 set(s) in place.`
      : "";
  const cleanupSummary =
    stats.unexpectedTopLevelInstancesRemoved > 0
      ? ` Removed ${stats.unexpectedTopLevelInstancesRemoved} stray top-level generated instance(s).`
      : "";
  stats.message =
    `Reorganized ${stats.movedComponentSets} component set(s) on the Components page into ${stats.sections.length} alphabetized ${useNativeSections ? "native Figma section" : "section heading"} group(s) across ${stats.sectionColumns} column(s), ${stats.pageWidth} x ${stats.pageHeight}px; node IDs were preserved.` +
    renameSummary +
    cleanupSummary;
  return stats;
}

/**
 * Place one measured section and its component sets.
 *
 * Native sections own their children, so those children carry section-relative
 * coordinates; the text-heading fallback leaves everything on the page in
 * absolute coordinates. `stats.positions` always records absolute page
 * coordinates so the build report stays comparable across both paths.
 */
function placeComponentSection(
  page,
  block,
  originX,
  originY,
  useNativeSections,
  stats,
) {
  const sectionRecord = {
    title: block.section.title,
    kind: useNativeSections ? "SECTION" : "TEXT",
    x: originX,
    y: originY,
    width: block.width,
    height: block.height,
    columnCount: block.layout.columns.length,
    componentCount: 0,
    components: [],
  };

  let container = page;
  let contentOriginX = originX + block.contentOffsetX;
  let contentOriginY = originY + block.contentOffsetY;
  let layoutSection = null;

  if (useNativeSections) {
    layoutSection = upsertComponentLayoutSection(page, block.section.title);
    page.appendChild(layoutSection);
    layoutSection.x = originX;
    layoutSection.y = originY;
    container = layoutSection;
    contentOriginX = block.contentOffsetX;
    contentOriginY = block.contentOffsetY;
    sectionRecord.id = layoutSection.id;
    sectionRecord.urlNodeId = nodeIdForUrl(layoutSection.id);
  } else {
    block.heading.x = originX;
    block.heading.y = originY;
    sectionRecord.headingId = block.heading.id;
    sectionRecord.headingUrlNodeId = nodeIdForUrl(block.heading.id);
  }

  for (const entry of block.layout.entries) {
    const node = entry.node;
    const contentX = contentOriginX + entry.x;
    const contentY = contentOriginY + entry.y;

    if (node.parent !== container) container.appendChild(node);
    node.x = contentX - entry.bounds.minX;
    node.y = contentY - entry.bounds.minY;
    stats.movedComponentSets += 1;
    stats.positions.push({
      name: node.name,
      section: block.section.title,
      id: node.id,
      urlNodeId: nodeIdForUrl(node.id),
      x: node.x,
      y: node.y,
      visualX: layoutSection ? originX + contentX : contentX,
      visualY: layoutSection ? originY + contentY : contentY,
      width: entry.bounds.width,
      height: entry.bounds.height,
      footprintHeight: entry.footprintHeight,
    });
    sectionRecord.componentCount += 1;
    sectionRecord.components.push(node.name);
  }

  if (layoutSection) {
    resizeNodeWithoutConstraints(layoutSection, block.width, block.height);
  }

  stats.sections.push(sectionRecord);
  return sectionRecord;
}

function componentPageLayoutOrder(sections) {
  const order = [];

  for (const section of sections) {
    for (const componentName of section.components) {
      order.push(componentName);
    }
  }

  return order;
}

function removeDuplicateGeneratedComponentSetsForKnownComponents(page, stats) {
  const knownNames = componentPageLayoutOrder(COMPONENT_PAGE_LAYOUT_SECTIONS);

  for (const componentSetName of knownNames) {
    const keep = findComponentSetOnPage(page, componentSetName);
    if (!keep || keep.type !== "COMPONENT_SET") continue;

    removeDuplicateGeneratedComponentSetsForName(
      page,
      componentSetName,
      keep,
      stats,
    );
  }
}

function componentSectionHeadingName(title) {
  return `Section / ${title}`;
}

function isComponentSectionHeading(node) {
  if (!node) return false;
  if (
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") ===
      "component-section-heading"
  ) {
    return true;
  }

  return node.type === "TEXT" && /^Section \/ .+/.test(node.name || "");
}

function isComponentLayoutSection(node) {
  if (!node || node.type !== "SECTION") return false;
  if (
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") === "component-section"
  ) {
    return true;
  }

  for (const section of COMPONENT_PAGE_LAYOUT_SECTIONS) {
    if (node.name === section.title) return true;
  }

  return false;
}

function canCreateComponentLayoutSection() {
  return typeof figma.createSection === "function";
}

function removeGeneratedComponentSectionHeadings(
  page,
  activeSectionTitles,
  options,
) {
  const removable = [];

  for (const child of page.children) {
    if (!isComponentSectionHeading(child)) continue;
    const title = String(child.name || "").replace(/^Section \/ /, "");
    if (options && options.removeActive) {
      removable.push(child);
    } else if (!activeSectionTitles[title]) {
      removable.push(child);
    }
  }

  for (const child of removable) {
    child.remove();
  }
}

function removeStaleComponentLayoutSections(page, activeSectionTitles) {
  const removable = [];

  for (const child of page.children) {
    if (!isComponentLayoutSection(child)) continue;
    const title =
      (child.getSharedPluginData &&
        child.getSharedPluginData(RUN_NAMESPACE, "title")) ||
      child.name;
    if (!activeSectionTitles[title]) removable.push(child);
  }

  for (const section of removable) {
    unwrapSectionChildrenToPage(section, page);
    section.remove();
  }
}

function unwrapSectionChildrenToPage(section, page) {
  if (!section || !section.children) return;

  while (section.children.length > 0) {
    const child = section.children[0];
    const position = absoluteNodePosition(child);
    page.appendChild(child);
    child.x = position.x;
    child.y = position.y;
  }
}

function upsertComponentLayoutSection(page, title) {
  let layoutSection = null;

  for (const child of page.children) {
    if (child.type !== "SECTION") continue;
    const sectionTitle =
      (child.getSharedPluginData &&
        child.getSharedPluginData(RUN_NAMESPACE, "title")) ||
      child.name;
    if (sectionTitle === title) {
      layoutSection = child;
      break;
    }
  }

  if (!layoutSection) {
    layoutSection = figma.createSection();
    page.appendChild(layoutSection);
  }

  layoutSection.name = title;
  layoutSection.setSharedPluginData(RUN_NAMESPACE, "kind", "component-section");
  layoutSection.setSharedPluginData(RUN_NAMESPACE, "title", title);
  try {
    layoutSection.sectionContentsHidden = false;
  } catch (_error) {
    // Older runtimes may not expose collapsible section contents.
  }

  return layoutSection;
}

function upsertComponentSectionHeading(page, title, fonts) {
  const name = componentSectionHeadingName(title);
  let heading = null;

  for (const child of page.children) {
    if (child.name === name && child.type === "TEXT") {
      heading = child;
      break;
    }
  }

  if (!heading) {
    heading = figma.createText();
    page.appendChild(heading);
  }

  heading.name = name;
  heading.fontName = fonts.bold;
  heading.fontSize = 24;
  heading.lineHeight = { unit: "PIXELS", value: 32 };
  heading.fills = [paintFromHex("#2D3440")];
  setTextAutoResize(heading, "WIDTH_AND_HEIGHT");
  heading.characters = title;
  heading.setSharedPluginData(
    RUN_NAMESPACE,
    "kind",
    "component-section-heading",
  );
  heading.setSharedPluginData(RUN_NAMESPACE, "title", title);
  return heading;
}

function measureComponentSetLayoutBounds(node) {
  const bounds = {
    minX: 0,
    minY: 0,
    maxX: Math.ceil(node.width || 0),
    maxY: Math.ceil(node.height || 0),
  };

  measureVisibleChildBounds(node, 0, 0, bounds);

  const minX = Math.floor(bounds.minX);
  const minY = Math.floor(bounds.minY);
  const maxX = Math.ceil(bounds.maxX);
  const maxY = Math.ceil(bounds.maxY);

  return {
    minX,
    minY,
    maxX,
    maxY,
    width: maxX - minX,
    height: maxY - minY,
  };
}

function absoluteNodePosition(node) {
  if (node && node.absoluteTransform) {
    return {
      x: node.absoluteTransform[0][2],
      y: node.absoluteTransform[1][2],
    };
  }

  let x = Number((node && node.x) || 0);
  let y = Number((node && node.y) || 0);
  let parent = node && node.parent;

  while (parent && parent.type !== "PAGE" && parent.type !== "DOCUMENT") {
    x += Number(parent.x || 0);
    y += Number(parent.y || 0);
    parent = parent.parent;
  }

  return { x, y };
}

function measureVisibleChildBounds(parent, offsetX, offsetY, bounds) {
  if (!parent || !("children" in parent)) return;

  for (const child of parent.children) {
    if (child.visible === false) continue;

    const childX = offsetX + (child.x || 0);
    const childY = offsetY + (child.y || 0);
    const childWidth = child.width || 0;
    const childHeight = child.height || 0;

    bounds.minX = Math.min(bounds.minX, childX);
    bounds.minY = Math.min(bounds.minY, childY);
    bounds.maxX = Math.max(bounds.maxX, childX + childWidth);
    bounds.maxY = Math.max(bounds.maxY, childY + childHeight);

    measureVisibleChildBounds(child, childX, childY, bounds);
  }
}

function removeGeneratedSurfaceQa(page) {
  const removable = [];
  for (const child of page.children) {
    if (
      child.getSharedPluginData &&
      child.getSharedPluginData(RUN_NAMESPACE, "kind") === "surface-qa"
    ) {
      removable.push(child);
    }
  }

  for (const child of removable) child.remove();
}

function createSurfaceQaHeader(fonts) {
  const header = figma.createFrame();
  header.name = "Kozmos DS / Transparent Surface QA";
  header.x = SURFACE_QA_CANVAS_X;
  header.y = SURFACE_QA_CANVAS_Y;
  header.resize(SURFACE_QA_PANEL_WIDTH * 2 + SURFACE_QA_PANEL_GAP, 120);
  header.layoutMode = "VERTICAL";
  header.primaryAxisSizingMode = "AUTO";
  header.counterAxisSizingMode = "FIXED";
  header.itemSpacing = 10;
  header.paddingTop = 32;
  header.paddingRight = 36;
  header.paddingBottom = 32;
  header.paddingLeft = 36;
  header.cornerRadius = 16;
  header.fills = solidPaint(1, 1, 1);
  header.strokes = solidPaint(0.88, 0.89, 0.92);
  header.strokeWeight = 1;
  header.setSharedPluginData(RUN_NAMESPACE, "kind", "surface-qa");
  header.setSharedPluginData(RUN_NAMESPACE, "role", "header");

  header.appendChild(
    createSurfaceQaText(
      "Title",
      "Transparent Surface QA",
      fonts.medium,
      36,
      44,
      { r: 0.07, g: 0.08, b: 0.1 },
      SURFACE_QA_PANEL_WIDTH * 2 + SURFACE_QA_PANEL_GAP - 72,
    ),
  );
  header.appendChild(
    createSurfaceQaText(
      "Summary",
      "Generated checkpoint for the surface-dependent advisories in the component audit. Review these live instances on Surface/0 and representative product map surfaces before closing Core v1.",
      fonts.regular,
      15,
      24,
      { r: 0.28, g: 0.3, b: 0.36 },
      SURFACE_QA_PANEL_WIDTH * 2 + SURFACE_QA_PANEL_GAP - 72,
    ),
  );

  return header;
}

function createSurfaceQaPanel(
  panelSpec,
  componentSets,
  collections,
  variableByName,
  fonts,
  stats,
) {
  const panel = figma.createFrame();
  panel.name = `Surface QA / ${panelSpec.title}`;
  panel.resize(SURFACE_QA_PANEL_WIDTH, 100);
  panel.layoutMode = "VERTICAL";
  panel.primaryAxisSizingMode = "AUTO";
  panel.counterAxisSizingMode = "FIXED";
  panel.itemSpacing = 22;
  panel.paddingTop = 32;
  panel.paddingRight = 32;
  panel.paddingBottom = 36;
  panel.paddingLeft = 32;
  panel.cornerRadius = 16;
  panel.clipsContent = false;
  panel.fills = panelSpec.fillToken
    ? [
        paintFromVariable(
          panelSpec.fillToken,
          panelSpec.fillFallback,
          variableByName,
          stats,
        ),
      ]
    : [paintFromHex(panelSpec.fillFallback)];
  panel.strokes =
    panelSpec.textTone === "dark"
      ? solidPaint(0.28, 0.31, 0.36)
      : solidPaint(0.78, 0.8, 0.84);
  panel.strokeWeight = 1;
  panel.setSharedPluginData(RUN_NAMESPACE, "kind", "surface-qa");
  panel.setSharedPluginData(RUN_NAMESPACE, "role", "panel");
  panel.setSharedPluginData(RUN_NAMESPACE, "mode", panelSpec.modeName);
  panel.setSharedPluginData(
    RUN_NAMESPACE,
    "surface",
    panelSpec.productSurface ? "product-map" : "surface-0",
  );
  applySurfaceQaVariableMode(panel, collections, panelSpec.modeName, stats);

  panel.appendChild(createSurfaceQaPanelTitle(panelSpec, fonts));
  if (panelSpec.productSurface) {
    panel.appendChild(createProductMapSurfacePreview(panelSpec, fonts));
  }

  for (const group of SURFACE_QA_COMPONENT_GROUPS) {
    panel.appendChild(
      createSurfaceQaComponentGroup(
        group,
        componentSets,
        fonts,
        stats,
        panelSpec.textTone,
      ),
    );
  }

  return panel;
}

function createSurfaceQaPanelTitle(panelSpec, fonts) {
  const titleGroup = figma.createFrame();
  titleGroup.name = "Panel Header";
  titleGroup.resize(SURFACE_QA_PANEL_WIDTH - 64, 80);
  titleGroup.layoutMode = "VERTICAL";
  titleGroup.primaryAxisSizingMode = "AUTO";
  titleGroup.counterAxisSizingMode = "FIXED";
  titleGroup.itemSpacing = 6;
  titleGroup.fills = [];

  const dark = panelSpec.textTone === "dark";
  titleGroup.appendChild(
    createSurfaceQaText(
      "Title",
      panelSpec.title,
      fonts.medium,
      22,
      30,
      dark ? { r: 0.95, g: 0.96, b: 0.98 } : { r: 0.08, g: 0.09, b: 0.12 },
      SURFACE_QA_PANEL_WIDTH - 64,
    ),
  );
  titleGroup.appendChild(
    createSurfaceQaText(
      "Description",
      panelSpec.subtitle,
      fonts.regular,
      13,
      20,
      dark ? { r: 0.72, g: 0.75, b: 0.8 } : { r: 0.34, g: 0.36, b: 0.42 },
      SURFACE_QA_PANEL_WIDTH - 64,
    ),
  );

  return titleGroup;
}

function createProductMapSurfacePreview(panelSpec, fonts) {
  const dark = panelSpec.textTone === "dark";
  const map = figma.createFrame();
  map.name = "Product Surface / map preview";
  map.resize(SURFACE_QA_PANEL_WIDTH - 64, 180);
  map.cornerRadius = 12;
  map.clipsContent = true;
  map.fills = [dark ? paintFromHex("#101821") : paintFromHex("#E8EEF5")];
  map.strokes = [dark ? paintFromHex("#394452") : paintFromHex("#C4CEDB")];
  map.strokeWeight = 1;

  const extrusion = figma.createRectangle();
  extrusion.name = "Map Layer / fill-extrusion";
  extrusion.resize(250, 76);
  extrusion.x = 36;
  extrusion.y = 42;
  extrusion.cornerRadius = 10;
  extrusion.fills = [dark ? paintFromHex("#28384A") : paintFromHex("#CFD9E6")];
  map.appendChild(extrusion);

  const route = figma.createRectangle();
  route.name = "Map Layer / route";
  route.resize(420, 8);
  route.x = 176;
  route.y = 104;
  route.cornerRadius = 9999;
  route.rotation = -8;
  route.fills = [dark ? paintFromHex("#57C7FF") : paintFromHex("#135BEC")];
  map.appendChild(route);

  const label = figma.createText();
  label.name = "Map Layer / symbol_label";
  label.fontName = fonts.medium;
  label.fontSize = 13;
  label.lineHeight = { unit: "PIXELS", value: 18 };
  label.characters = "symbol_label";
  label.x = 372;
  label.y = 58;
  label.fills = [dark ? paintFromHex("#F8FAFC") : paintFromHex("#0D1016")];
  map.appendChild(label);

  const layerLabel = figma.createText();
  layerLabel.name = "Map Layer / fill-extrusion label";
  layerLabel.fontName = fonts.regular;
  layerLabel.fontSize = 12;
  layerLabel.lineHeight = { unit: "PIXELS", value: 18 };
  layerLabel.characters = "fill-extrusion";
  layerLabel.x = 52;
  layerLabel.y = 70;
  layerLabel.fills = [dark ? paintFromHex("#D8DEE8") : paintFromHex("#2D3440")];
  map.appendChild(layerLabel);

  return map;
}

function createSurfaceQaComponentGroup(
  group,
  componentSets,
  fonts,
  stats,
  textTone,
) {
  const wrapper = figma.createFrame();
  wrapper.name = `QA Group / ${group.title}`;
  wrapper.resize(SURFACE_QA_PANEL_WIDTH - 64, 100);
  wrapper.layoutMode = "VERTICAL";
  wrapper.primaryAxisSizingMode = "AUTO";
  wrapper.counterAxisSizingMode = "FIXED";
  wrapper.itemSpacing = SURFACE_QA_GROUP_GAP;
  wrapper.fills = [];
  wrapper.setSharedPluginData(RUN_NAMESPACE, "kind", "surface-qa");
  wrapper.setSharedPluginData(RUN_NAMESPACE, "role", "group");

  wrapper.appendChild(
    createSurfaceQaText(
      "Group Title",
      group.title,
      fonts.medium,
      13,
      18,
      surfaceQaMutedTextColor(textTone),
      SURFACE_QA_PANEL_WIDTH - 64,
    ),
  );

  for (const specs of group.rows) {
    const row = figma.createFrame();
    row.name = "QA Row";
    row.resize(SURFACE_QA_PANEL_WIDTH - 64, 56);
    row.layoutMode = "HORIZONTAL";
    row.primaryAxisSizingMode = "AUTO";
    row.counterAxisSizingMode = "AUTO";
    row.itemSpacing = 12;
    row.fills = [];

    for (const spec of specs) {
      appendSurfaceQaInstance(row, spec, componentSets, fonts, stats);
    }

    wrapper.appendChild(row);
  }

  return wrapper;
}

function surfaceQaMutedTextColor(textTone) {
  return textTone === "dark"
    ? { r: 0.6, g: 0.64, b: 0.72 }
    : { r: 0.43, g: 0.46, b: 0.53 };
}

function appendSurfaceQaInstance(row, spec, componentSets, fonts, stats) {
  const componentSet = componentSets[spec.componentSetName];
  if (!componentSet) {
    pushUnique(stats.missingComponentSets, spec.componentSetName);
    row.appendChild(
      createSurfaceQaMissingNode(fonts, `Missing ${spec.componentSetName}`),
    );
    return;
  }

  const component = componentSet.children.find(
    (child) => child.type === "COMPONENT" && child.name === spec.variantName,
  );
  if (!component) {
    pushUnique(
      stats.missingVariants,
      `${spec.componentSetName} / ${spec.variantName}`,
    );
    row.appendChild(createSurfaceQaMissingNode(fonts, spec.variantName));
    return;
  }

  const instance = component.createInstance();
  instance.name = `QA / ${spec.componentSetName} / ${spec.variantName}`;
  instance.setSharedPluginData(RUN_NAMESPACE, "kind", "surface-qa-instance");
  instance.setSharedPluginData(
    RUN_NAMESPACE,
    "componentSet",
    spec.componentSetName,
  );
  instance.setSharedPluginData(RUN_NAMESPACE, "variant", spec.variantName);

  if (spec.text) {
    for (const baseName of Object.keys(spec.text)) {
      setInstanceTextProperty(
        instance,
        componentSet,
        baseName,
        spec.text[baseName],
        stats,
      );
    }
  }

  row.appendChild(instance);
  stats.instancesCreated += 1;
}

function createSurfaceQaMissingNode(fonts, message) {
  const frame = figma.createFrame();
  frame.name = "QA Missing Instance";
  frame.resize(180, 44);
  frame.layoutMode = "VERTICAL";
  frame.primaryAxisSizingMode = "AUTO";
  frame.counterAxisSizingMode = "FIXED";
  frame.paddingTop = 10;
  frame.paddingRight = 12;
  frame.paddingBottom = 10;
  frame.paddingLeft = 12;
  frame.cornerRadius = 8;
  frame.fills = [paintFromHex("#FFF7ED")];
  frame.strokes = [paintFromHex("#FDBA74")];
  frame.strokeWeight = 1;

  frame.appendChild(
    createSurfaceQaText(
      "Missing",
      message,
      fonts.regular,
      11,
      16,
      { r: 0.54, g: 0.22, b: 0.04 },
      156,
    ),
  );

  return frame;
}

function createSurfaceQaText(
  name,
  characters,
  font,
  fontSize,
  lineHeight,
  color,
  width,
) {
  const text = figma.createText();
  text.name = name;
  text.fontName = font;
  text.fontSize = fontSize;
  text.lineHeight = { unit: "PIXELS", value: lineHeight };
  text.fills = solidPaint(color.r, color.g, color.b);
  text.resizeWithoutConstraints(width, lineHeight);
  setTextAutoResize(text, "HEIGHT");
  setLayoutSizingHorizontal(text, "FILL");
  text.characters = characters;
  return text;
}

function applySurfaceQaVariableMode(node, collections, modeName, stats) {
  if (!node || !node.setExplicitVariableModeForCollection) return;

  for (const collection of collections) {
    const mode = (collection.modes || []).find(
      (candidate) => candidate.name === modeName,
    );
    if (!mode) continue;

    try {
      node.setExplicitVariableModeForCollection(collection.id, mode.modeId);
      stats.modeBindingsApplied += 1;
    } catch (_error) {
      try {
        node.setExplicitVariableModeForCollection(collection, mode.modeId);
        stats.modeBindingsApplied += 1;
      } catch (error) {
        stats.warnings.push(
          `Could not apply ${modeName} mode for ${collection.name} (${messageFor(error)}).`,
        );
      }
    }
  }
}

function pushUnique(values, value) {
  if (values.indexOf(value) === -1) values.push(value);
}

function canonicalComponentSetName(name) {
  if (!name) return name;
  return name.endsWith(COMPONENT_SET_LEGACY_SUFFIX)
    ? name.slice(0, -COMPONENT_SET_LEGACY_SUFFIX.length)
    : name;
}

function componentSetNameMatches(actualName, expectedName) {
  return (
    canonicalComponentSetName(actualName) ===
    canonicalComponentSetName(expectedName)
  );
}

function findComponentSetOnPage(page, name) {
  const canonicalName = canonicalComponentSetName(name);
  let legacyMatch = null;

  for (const componentSet of collectComponentSetsInPage(page)) {
    if (componentSet.name === canonicalName) return componentSet;
    if (
      !legacyMatch &&
      componentSetNameMatches(componentSet.name, canonicalName)
    ) {
      legacyMatch = componentSet;
    }
  }

  return legacyMatch;
}

function collectComponentSetsInPage(page) {
  const componentSets = [];
  if (!page || !page.children) return componentSets;

  collectComponentSetsInNode(page, componentSets);
  return componentSets;
}

function collectComponentSetsInNode(node, componentSets) {
  if (!node) return;
  if (node.type === "COMPONENT_SET") {
    componentSets.push(node);
    return;
  }

  if (!node.children) return;
  for (const child of node.children) {
    collectComponentSetsInNode(child, componentSets);
  }
}

function normalizeComponentSetNodeName(node, canonicalName, stats) {
  if (!node || node.name === canonicalName) return false;

  const oldName = node.name;
  node.name = canonicalName;
  if (stats) {
    incrementStat(stats, "renamedComponentSets");
    if (!stats.renamed) stats.renamed = [];
    stats.renamed.push({
      id: node.id,
      urlNodeId: nodeIdForUrl(node.id),
      oldName,
      newName: canonicalName,
    });
  }
  return true;
}

function removeDuplicateGeneratedComponentSetsForName(
  page,
  componentSetName,
  keepNode,
  stats,
) {
  if (!page) return 0;

  const matches = collectComponentSetsInPage(page).filter((node) =>
    componentSetNameMatches(node.name, componentSetName),
  );

  if (matches.length <= 1) return 0;

  const keep =
    keepNode ||
    matches.find(
      (node) => node.name === canonicalComponentSetName(componentSetName),
    ) ||
    matches[0];
  let removed = 0;

  for (const node of matches) {
    if (keep && node.id === keep.id) continue;

    const oldName = node.name;
    const oldId = node.id;
    try {
      node.remove();
      removed += 1;
      if (stats) {
        if (!stats.duplicateComponentSetsRemovedDetails) {
          stats.duplicateComponentSetsRemovedDetails = [];
        }
        stats.duplicateComponentSetsRemovedDetails.push({
          id: oldId,
          urlNodeId: nodeIdForUrl(oldId),
          oldName,
          keptId: keep && keep.id,
          keptUrlNodeId: keep && nodeIdForUrl(keep.id),
        });
      }
    } catch (error) {
      if (stats && stats.warnings) {
        stats.warnings.push(
          `Could not remove duplicate ${componentSetName} component set "${oldName}" (${messageFor(error)}).`,
        );
      }
    }
  }

  if (removed > 0) {
    incrementStat(stats, "duplicateComponentSetsRemoved", removed);
  }

  return removed;
}

// Set while a bulk update is running. Each single update reorganizes the whole
// Components page, which is fine once and pointless twenty-four times over a
// 94-set page; the bulk runner reorganizes once at the end instead.
let suppressAutoReorganize = false;

async function reorganizeAfterGeneratedComponentMutation(stats) {
  if (suppressAutoReorganize) {
    stats.layoutDeferred = true;
    return null;
  }
  stats.layout = await reorganizeComponentsPage();
  return stats.layout;
}

async function findComponentSetsByName(docs) {
  const requested = {};
  const found = {};

  for (const doc of docs) {
    requested[canonicalComponentSetName(doc.componentSetName)] = true;
  }

  for (const page of figma.root.children) {
    await page.loadAsync();
    for (const componentSet of collectComponentSetsInPage(page)) {
      const canonicalName = canonicalComponentSetName(componentSet.name);
      if (!requested[canonicalName]) continue;
      if (!found[canonicalName] || componentSet.name === canonicalName) {
        found[canonicalName] = componentSet;
      }
    }
  }

  return found;
}

function removeGeneratedComponentDocs(page) {
  const removable = [];

  for (const child of page.children) {
    if (child.getSharedPluginData(RUN_NAMESPACE, "kind") === "component-docs") {
      removable.push(child);
    }
  }

  for (const child of removable) {
    child.remove();
  }
}

async function removeGeneratedSplitDocPages(stats) {
  const removable = [];

  for (const page of figma.root.children) {
    if (page.name.indexOf(COMPONENT_DOC_SPLIT_PAGE_PREFIX) !== 0) continue;

    await page.loadAsync();
    let generatedDocsPage = false;
    for (const child of page.children) {
      if (
        child.getSharedPluginData(RUN_NAMESPACE, "kind") === "component-docs"
      ) {
        generatedDocsPage = true;
        break;
      }
    }

    if (generatedDocsPage) removable.push(page);
  }

  for (const page of removable) {
    try {
      if (figma.currentPage && figma.currentPage.id === page.id) {
        const fallback = figma.root.children.find(
          (candidate) => candidate.id !== page.id,
        );
        if (fallback) await figma.setCurrentPageAsync(fallback);
      }

      page.remove();
      stats.splitPagesRemoved += 1;
    } catch (error) {
      stats.warnings.push(
        `Could not remove generated split docs page "${page.name}" (${messageFor(error)})`,
      );
    }
  }
}

function createDocsCatalogHeaderFrame(fonts) {
  const header = figma.createFrame();
  header.name = "Kozmos DS / Component Documentation";
  header.x = DOCS_CANVAS_X;
  header.y = DOCS_CANVAS_Y;
  header.resize(DOCS_CATALOG_WIDTH, 100);
  header.layoutMode = "VERTICAL";
  header.primaryAxisSizingMode = "AUTO";
  header.counterAxisSizingMode = "FIXED";
  header.itemSpacing = 12;
  header.paddingTop = 40;
  header.paddingRight = 40;
  header.paddingBottom = 40;
  header.paddingLeft = 40;
  header.cornerRadius = 16;
  header.fills = solidPaint(1, 1, 1);
  header.strokes = solidPaint(0.88, 0.89, 0.92);
  header.strokeWeight = 1;
  header.setSharedPluginData(RUN_NAMESPACE, "kind", "component-docs");
  header.setSharedPluginData(RUN_NAMESPACE, "component", "Catalog");

  header.appendChild(
    createDocsText(
      "Eyebrow",
      "Kozmos DS",
      fonts.medium,
      13,
      18,
      { r: 0.36, g: 0.16, b: 0.86 },
      DOCS_CATALOG_WIDTH - 80,
    ),
  );
  header.appendChild(
    createDocsText(
      "Title",
      "Component Documentation",
      fonts.medium,
      44,
      52,
      { r: 0.07, g: 0.08, b: 0.1 },
      DOCS_CATALOG_WIDTH - 80,
    ),
  );
  header.appendChild(
    createDocsText(
      "Summary",
      "Generated reference for the v1 component library. Source component sets remain on the Components page so Code Connect node IDs stay stable.",
      fonts.regular,
      17,
      28,
      { r: 0.23, g: 0.25, b: 0.3 },
      DOCS_CATALOG_WIDTH - 80,
    ),
  );

  return header;
}

function createComponentDocsRoot(doc, componentSet, fonts, stats) {
  const root = figma.createFrame();
  root.name = `Docs Section / ${doc.componentSetName}`;
  root.resize(DOCS_SECTION_WIDTH, 100);
  root.layoutMode = "VERTICAL";
  root.primaryAxisSizingMode = "AUTO";
  root.counterAxisSizingMode = "FIXED";
  root.itemSpacing = 24;
  root.paddingTop = DOCS_SECTION_PADDING;
  root.paddingRight = DOCS_SECTION_PADDING;
  root.paddingBottom = DOCS_SECTION_PADDING_BOTTOM;
  root.paddingLeft = DOCS_SECTION_PADDING;
  root.cornerRadius = 16;
  root.fills = solidPaint(1, 1, 1);
  root.strokes = solidPaint(0.88, 0.89, 0.92);
  root.strokeWeight = 1;
  root.setSharedPluginData(RUN_NAMESPACE, "kind", "component-docs");
  root.setSharedPluginData(RUN_NAMESPACE, "component", doc.componentName);

  const header = createDocsHeader(doc, componentSet, fonts);
  const preview = createDocsPreview(doc, componentSet, fonts, stats);
  const sections = [
    createDocsSection("Usage", doc.usage, fonts),
    createDocsSection("Properties", doc.properties, fonts),
    createDocsSection("Code Connect", doc.api, fonts),
    createDocsSection("Accessibility", doc.accessibility, fonts),
  ];
  const footer = createDocsText(
    "Source",
    [
      `Source component set: ${doc.componentSetName}`,
      `Node ID: ${nodeIdForUrl(componentSet.id)}`,
      "The source component set remains on the Components page so Code Connect links stay stable.",
    ].join("\n"),
    fonts.regular,
    13,
    20,
    { r: 0.32, g: 0.34, b: 0.39 },
  );

  root.appendChild(header);
  root.appendChild(preview);
  for (const section of sections) root.appendChild(section);
  root.appendChild(footer);

  return root;
}

function createDocsHeader(doc, componentSet, fonts) {
  const header = figma.createFrame();
  header.name = "Header";
  header.resize(DOCS_INNER_WIDTH, 100);
  header.layoutMode = "VERTICAL";
  header.primaryAxisSizingMode = "AUTO";
  header.counterAxisSizingMode = "FIXED";
  header.itemSpacing = 12;
  header.fills = [];

  const eyebrow = createDocsText(
    "Category",
    `Kozmos DS / ${doc.category}`,
    fonts.medium,
    13,
    18,
    { r: 0.36, g: 0.16, b: 0.86 },
  );
  const title = createDocsText(
    "Title",
    doc.componentSetName,
    fonts.medium,
    34,
    42,
    { r: 0.07, g: 0.08, b: 0.1 },
  );
  const summary = createDocsText(
    "Description",
    doc.summary,
    fonts.regular,
    15,
    24,
    { r: 0.23, g: 0.25, b: 0.3 },
  );
  const metadata = createDocsText(
    "Metadata",
    [
      `Variants: ${componentSet.children.length}`,
      `Component properties: ${componentPropertyNamesForDocs(componentSet).join(", ") || "None"}`,
      `Right panel description: generated from this documentation source.`,
    ].join("\n"),
    fonts.regular,
    13,
    20,
    { r: 0.38, g: 0.4, b: 0.46 },
  );

  header.appendChild(eyebrow);
  header.appendChild(title);
  header.appendChild(summary);
  header.appendChild(metadata);
  return header;
}

function createDocsPreview(doc, componentSet, fonts, stats) {
  const frame = figma.createFrame();
  frame.name = "Preview";
  frame.resize(DOCS_INNER_WIDTH, 100);
  frame.layoutMode = "VERTICAL";
  frame.primaryAxisSizingMode = "AUTO";
  frame.counterAxisSizingMode = "FIXED";
  frame.itemSpacing = 18;
  frame.paddingTop = 24;
  frame.paddingRight = 24;
  frame.paddingBottom = 32;
  frame.paddingLeft = 24;
  frame.cornerRadius = 12;
  frame.fills = solidPaint(0.97, 0.97, 0.98);
  frame.strokes = solidPaint(0.88, 0.89, 0.92);
  frame.strokeWeight = 1;

  const title = createDocsText(
    "Preview Title",
    "Reference Preview",
    fonts.medium,
    16,
    22,
    { r: 0.09, g: 0.1, b: 0.13 },
  );
  const intro = createDocsText(
    "Preview Description",
    "Representative live instances from the source component set.",
    fonts.regular,
    13,
    20,
    { r: 0.34, g: 0.36, b: 0.42 },
  );
  const row = figma.createFrame();
  row.name = "Preview Instances";
  row.resize(DOCS_INNER_WIDTH - 48, 80);
  row.layoutMode = "HORIZONTAL";
  row.primaryAxisSizingMode = "AUTO";
  row.counterAxisSizingMode = "AUTO";
  row.itemSpacing = 16;
  row.paddingTop = 8;
  row.paddingRight = 8;
  row.paddingBottom = 8;
  row.paddingLeft = 8;
  row.fills = [];

  let previewCount = 0;
  const maxPreviewCount = docsPreviewLimitFor(doc.componentName);
  for (const child of docsPreviewComponentsFor(doc, componentSet)) {
    if (previewCount >= maxPreviewCount) break;

    try {
      const instance = child.createInstance();
      instance.name = `Preview / ${child.name}`;
      applyDocsPreviewOverrides(instance, child, doc, componentSet, stats);
      row.appendChild(instance);
      previewCount += 1;
      stats.previewsCreated += 1;
    } catch (error) {
      stats.warnings.push(
        `${doc.componentSetName}: could not create preview instance (${messageFor(error)})`,
      );
      break;
    }
  }

  if (previewCount === 0) {
    row.appendChild(
      createDocsText(
        "Preview Empty",
        "No preview instance could be generated for this component set.",
        fonts.regular,
        13,
        20,
        { r: 0.44, g: 0.46, b: 0.52 },
      ),
    );
  }

  frame.appendChild(title);
  frame.appendChild(intro);
  frame.appendChild(row);
  return frame;
}

function docsPreviewComponentsFor(doc, componentSet) {
  const components = componentSet.children.filter(
    (child) => child.type === "COMPONENT",
  );

  if (doc.componentName === "Badge") {
    return preferredPreviewComponents(components, [
      "Variant=Default, Size=Default",
      "Variant=Secondary, Size=Default",
      "Variant=Outline, Size=Default",
      "Variant=Destructive, Size=Default",
    ]);
  }

  if (doc.componentName === "Card") {
    return preferredPreviewComponents(components, [
      "Content=Full",
      "Content=Header",
      "Content=Basic",
    ]);
  }

  return components;
}

function preferredPreviewComponents(components, preferredNames) {
  const selected = [];

  for (const preferredName of preferredNames) {
    const match = components.find(
      (component) => component.name === preferredName,
    );
    if (match) selected.push(match);
  }

  for (const component of components) {
    if (selected.indexOf(component) === -1) selected.push(component);
  }

  return selected;
}

function applyDocsPreviewOverrides(
  instance,
  sourceComponent,
  doc,
  componentSet,
  stats,
) {
  if (!instance || !instance.setProperties) return;

  if (doc.componentName === "Badge") {
    const props = parseBadgeVariantName(sourceComponent.name);
    if (!props) return;
    setInstanceTextProperty(
      instance,
      componentSet,
      "Label Text",
      badgeLabelText(props.variant, props.size),
      stats,
    );
  }
}

function setInstanceTextProperty(
  instance,
  componentSet,
  baseName,
  value,
  stats,
) {
  const lookup = componentPropertyLookupByBaseName(
    componentSet,
    baseName,
    "TEXT",
    stats,
    "set text instance property",
  );
  const propertyName = lookup.propertyName;
  if (!propertyName) {
    if (!lookup.readError) {
      recordMissingInstanceProperty(componentSet, baseName, "TEXT", stats);
    }
    return false;
  }

  try {
    instance.setProperties({ [propertyName]: value });
    return true;
  } catch (error) {
    if (stats && stats.warnings) {
      stats.warnings.push(
        `${componentSet.name}: could not set ${baseName} preview property (${messageFor(error)})`,
      );
    }
    return false;
  }
}

function setInstanceBooleanProperty(
  instance,
  componentSet,
  baseName,
  value,
  stats,
) {
  const lookup = componentPropertyLookupByBaseName(
    componentSet,
    baseName,
    "BOOLEAN",
    stats,
    "set boolean instance property",
  );
  const propertyName = lookup.propertyName;
  if (!propertyName) {
    if (!lookup.readError) {
      recordMissingInstanceProperty(componentSet, baseName, "BOOLEAN", stats);
    }
    return false;
  }

  try {
    instance.setProperties({ [propertyName]: value });
    return true;
  } catch (error) {
    if (stats && stats.warnings) {
      stats.warnings.push(
        `${componentSet.name}: could not set ${baseName} instance property (${messageFor(error)})`,
      );
    }
    return false;
  }
}

function setInstanceSwapProperty(
  instance,
  componentSet,
  baseName,
  componentId,
  stats,
) {
  const lookup = componentPropertyLookupByBaseName(
    componentSet,
    baseName,
    "INSTANCE_SWAP",
    stats,
    "set instance-swap property",
  );
  const propertyName = lookup.propertyName;
  if (!propertyName) {
    if (!lookup.readError) {
      recordMissingInstanceProperty(
        componentSet,
        baseName,
        "INSTANCE_SWAP",
        stats,
      );
    }
    return false;
  }
  if (!componentId) return false;

  try {
    instance.setProperties({ [propertyName]: componentId });
    return true;
  } catch (error) {
    if (stats && stats.warnings) {
      stats.warnings.push(
        `${componentSet.name}: could not set ${baseName} instance-swap property (${messageFor(error)})`,
      );
    }
    return false;
  }
}

function componentPropertyNameByBaseName(componentSet, baseName, type) {
  return componentPropertyLookupByBaseName(
    componentSet,
    baseName,
    type,
    null,
    "preview",
  ).propertyName;
}

function componentPropertyLookupByBaseName(
  componentSet,
  baseName,
  type,
  stats,
  context,
) {
  const read = safeComponentPropertyDefinitions(componentSet, stats, context);
  const definitions = read.definitions;
  if (read.error) {
    return {
      propertyName: null,
      readError: read.error,
    };
  }

  for (const propertyName of Object.keys(definitions)) {
    const definition = definitions[propertyName];
    if (definition.type !== type) continue;
    if (propertyName.split("#")[0] === baseName) {
      return {
        propertyName,
        readError: null,
      };
    }
  }

  return {
    propertyName: null,
    readError: null,
  };
}

function recordMissingInstanceProperty(componentSet, baseName, type, stats) {
  if (!stats) return;
  stats.missingInstanceProperties = (stats.missingInstanceProperties || 0) + 1;
  if (!stats.warnings) return;

  const componentName =
    componentSet && componentSet.name ? componentSet.name : "Component";
  const key = `${componentName}:${type}:${baseName}`;
  stats.missingInstancePropertyWarnings =
    stats.missingInstancePropertyWarnings || {};
  if (stats.missingInstancePropertyWarnings[key]) return;
  stats.missingInstancePropertyWarnings[key] = true;
  stats.warnings.push(
    `${componentName}: missing ${type} component property "${baseName}" while applying an instance override. Check generated example/config property names.`,
  );
}

async function findLocalComponentSetByName(name) {
  for (const page of figma.root.children) {
    await page.loadAsync();
    const match = findComponentSetOnPage(page, name);
    if (match && match.type === "COMPONENT_SET") return match;
  }

  return null;
}

function findComponentVariantByProperties(componentSet, properties) {
  if (!componentSet || !componentSet.children) return null;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;
    if (componentVariantNameMatches(child.name, properties)) return child;
  }

  return null;
}

function componentVariantNameMatches(name, properties) {
  const actual = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    actual[key] = value;
  }

  for (const key of Object.keys(properties)) {
    if (actual[key] !== properties[key]) return false;
  }

  return true;
}

async function createNestedComponentInstance(config) {
  const componentSet = await findLocalComponentSetByName(
    config.componentSetName,
  );
  if (!componentSet) {
    if (config.stats && config.stats.warnings) {
      config.stats.warnings.push(
        `Missing ${config.componentSetName}; could not create nested ${config.name} instance.`,
      );
    }
    return null;
  }

  const variant = findComponentVariantByProperties(
    componentSet,
    config.variantProperties,
  );
  if (!variant) {
    if (config.stats && config.stats.warnings) {
      config.stats.warnings.push(
        `${config.componentSetName}: missing variant for nested ${config.name} instance.`,
      );
    }
    return null;
  }

  const instance = variant.createInstance();
  instance.name = config.name;
  markNestedComponentInstance(instance, config.componentSetName, config.role);

  return {
    componentSet,
    instance,
  };
}

function createMissingNestedComponentNode(name, message, stats) {
  const frame = figma.createFrame();
  frame.name = name;
  frame.layoutMode = "HORIZONTAL";
  frame.primaryAxisSizingMode = "AUTO";
  frame.counterAxisSizingMode = "AUTO";
  frame.primaryAxisAlignItems = "CENTER";
  frame.counterAxisAlignItems = "CENTER";
  frame.paddingLeft = 12;
  frame.paddingRight = 12;
  frame.paddingTop = 8;
  frame.paddingBottom = 8;
  frame.cornerRadius = 8;
  frame.fills = [];
  frame.strokes = [];
  frame.resizeWithoutConstraints(180, 44);
  frame.setSharedPluginData(RUN_NAMESPACE, "kind", "missing-nested-component");

  if (stats && stats.warnings) {
    stats.warnings.push(`${name}: ${message}`);
  }

  return frame;
}

function isGeneratedNestedComponentInstance(node) {
  return (
    node &&
    node.type === "INSTANCE" &&
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") ===
      "nested-component-instance"
  );
}

function nodeSourceComponentSet(node) {
  if (!node || node.type !== "INSTANCE" || !node.getSharedPluginData) {
    return null;
  }

  const generatedSource = node.getSharedPluginData(
    RUN_NAMESPACE,
    "sourceComponentSet",
  );
  if (generatedSource) return generatedSource;

  let mainComponent = null;
  try {
    mainComponent = node.mainComponent;
  } catch (_error) {
    mainComponent = null;
  }

  if (
    mainComponent &&
    mainComponent.parent &&
    mainComponent.parent.type === "COMPONENT_SET"
  ) {
    return mainComponent.parent.name;
  }

  return null;
}

function isNestedComponentInstance(node, componentSetName) {
  return (
    node &&
    node.type === "INSTANCE" &&
    componentSetNameMatches(nodeSourceComponentSet(node), componentSetName)
  );
}

function markNestedComponentInstance(node, componentSetName, role) {
  if (!node || node.type !== "INSTANCE" || !node.setSharedPluginData) return;

  node.setSharedPluginData(RUN_NAMESPACE, "kind", "nested-component-instance");
  node.setSharedPluginData(
    RUN_NAMESPACE,
    "sourceComponentSet",
    componentSetName,
  );
  if (role) {
    node.setSharedPluginData(RUN_NAMESPACE, "role", role);
  }
}

function isBadgeCounterNode(node) {
  if (!node || node.name !== "Counter" || !node.getSharedPluginData) {
    return false;
  }

  return (
    node.getSharedPluginData(RUN_NAMESPACE, "role") === "badge-counter" ||
    node.getSharedPluginData(RUN_NAMESPACE, "kind") === "badge-counter" ||
    isNestedComponentInstance(node, "Counter")
  );
}

function docsPreviewLimitFor(componentName) {
  if (componentName === "Button") return 3;
  if (componentName === "IconButton") return 6;
  if (componentName === "Badge") return 4;
  if (componentName === "Chip") return 6;
  if (componentName === "Card") return 1;
  if (componentName === "Tooltip") return 4;
  if (componentName === "Spinner") return 4;
  if (componentName === "Avatar") return 2;
  if (componentName === "Checkbox") return 3;
  if (componentName === "Radio") return 3;
  if (componentName === "Switch") return 3;
  if (componentName === "Input") return 2;
  if (componentName === "PasswordInput") return 2;
  if (componentName === "Textarea") return 2;
  if (componentName === "Search") return 2;
  if (componentName === "Select") return 2;
  if (componentName === "Slider") return 2;
  if (componentName === "Progress") return 2;
  if (componentName === "Alert") return 2;
  return 3;
}

function createDocsSection(title, lines, fonts) {
  const section = figma.createFrame();
  section.name = `Section / ${title}`;
  section.resize(DOCS_INNER_WIDTH, 100);
  section.layoutMode = "VERTICAL";
  section.primaryAxisSizingMode = "AUTO";
  section.counterAxisSizingMode = "FIXED";
  section.itemSpacing = 10;
  section.paddingTop = 24;
  section.paddingRight = 24;
  section.paddingBottom = 32;
  section.paddingLeft = 24;
  section.cornerRadius = 12;
  section.fills = solidPaint(1, 1, 1);
  section.strokes = solidPaint(0.88, 0.89, 0.92);
  section.strokeWeight = 1;

  const heading = createDocsText("Heading", title, fonts.medium, 18, 24, {
    r: 0.07,
    g: 0.08,
    b: 0.1,
  });
  const body = createDocsText(
    "Body",
    bulletLines(lines),
    fonts.regular,
    14,
    22,
    { r: 0.26, g: 0.28, b: 0.33 },
  );

  section.appendChild(heading);
  section.appendChild(body);
  return section;
}

function createDocsText(
  name,
  characters,
  font,
  fontSize,
  lineHeight,
  color,
  width,
) {
  const text = figma.createText();
  text.name = name;
  text.fontName = font;
  text.fontSize = fontSize;
  text.lineHeight = { unit: "PIXELS", value: lineHeight };
  text.fills = solidPaint(color.r, color.g, color.b);
  text.resizeWithoutConstraints(width || DOCS_INNER_WIDTH, lineHeight);
  setTextAutoResize(text, "HEIGHT");
  setLayoutSizingHorizontal(text, "FILL");
  text.characters = characters;
  return text;
}

function docsMeasuredHeight(node) {
  if (node && node.height && node.height > 100) return node.height;
  return 1160;
}

function bulletLines(lines) {
  const output = [];
  for (const line of lines) {
    output.push(`- ${line}`);
  }
  return output.join("\n");
}

function solidPaint(r, g, b) {
  return [{ type: "SOLID", color: { r, g, b } }];
}

function paintFromHex(value) {
  const color = parseColor(value);
  return {
    type: "SOLID",
    color: { r: color.r, g: color.g, b: color.b },
    opacity: color.a,
  };
}

function componentPropertyNamesForDocs(componentSet) {
  const read = safeComponentPropertyDefinitions(componentSet, null, "docs");
  const names = [];

  for (const propertyName in read.definitions) {
    if (!Object.prototype.hasOwnProperty.call(read.definitions, propertyName)) {
      continue;
    }

    names.push(propertyName);
  }

  return names;
}

function applyComponentSetDescription(
  componentSet,
  componentSetName,
  updated,
  fallbackLines,
) {
  const doc = componentDocForComponentSetName(componentSetName);
  const lines = doc
    ? componentSetDescriptionLines(doc)
    : fallbackComponentDescriptionLines(updated, fallbackLines);

  componentSet.description = lines.join("\n");
}

function componentDocForComponentSetName(componentSetName) {
  const canonicalName = canonicalComponentSetName(componentSetName);
  for (const doc of COMPONENT_DOCS) {
    if (doc.componentSetName === canonicalName) return doc;
  }

  return null;
}

function componentSetDescriptionLines(doc) {
  const lines = [doc.summary, "", "Usage:"];

  for (const item of doc.usage) lines.push(`- ${item}`);
  lines.push("");
  lines.push("Code Connect:");
  for (const item of doc.api) lines.push(`- ${item}`);
  lines.push("");
  lines.push("Accessibility:");
  for (const item of doc.accessibility) lines.push(`- ${item}`);
  lines.push("");
  lines.push(
    "Maintenance: update this component set in place to preserve Code Connect node IDs.",
  );
  return lines;
}

function fallbackComponentDescriptionLines(updated, fallbackLines) {
  const lines = [];
  if (fallbackLines) {
    for (const line of fallbackLines) lines.push(line);
  }

  if (updated) {
    lines.push("Updated in place to preserve the Code Connect node ID.");
  }

  return lines;
}

async function auditLibrary() {
  const auditStartedAt = auditTimerNow();
  const performance = createAuditPerformance();
  let phaseStartedAt = auditTimerNow();
  postAuditProgress(
    "scan",
    "Scanning foundations...",
    "Reading local variable collections and variable values.",
  );
  const collections = await figma.variables.getLocalVariableCollectionsAsync();
  const variables = await figma.variables.getLocalVariablesAsync();
  const variableContext = createVariableContext(collections, variables);
  recordAuditTiming(performance, "variables", phaseStartedAt, {
    collections: collections.length,
    variables: variables.length,
  });
  postAuditProgress(
    "scan",
    "Scanning icon sources...",
    "Checking curated icon source components.",
  );
  phaseStartedAt = auditTimerNow();
  const icons = await auditIconSources();
  recordAuditTiming(performance, "icons", phaseStartedAt, {
    found: icons.foundCount,
    expected: icons.expectedCount,
    issues: icons.issueCount,
  });
  postAuditProgress(
    "scan",
    "Scanning transparent surface QA...",
    "Checking QA panels and contrast examples.",
  );
  phaseStartedAt = auditTimerNow();
  const surfaceQa = await auditSurfaceQaPage();
  recordAuditTiming(performance, "surface QA", phaseStartedAt, {
    found: surfaceQa.found,
    issues: surfaceQa.issueCount,
  });
  postAuditProgress(
    "audit",
    "Auditing component sets...",
    `Checking ${figma.root.children.length} page(s) for components, contrast, slots, and layout issues.`,
  );
  const audit = {
    generatedAt: new Date().toISOString(),
    fileName: figma.root.name,
    pages: [],
    variables: auditVariables(collections, variables),
    icons,
    surfaceQa,
    componentSets: [],
    summary: {
      pageCount: figma.root.children.length,
      variableCollectionCount: collections.length,
      variableCount: variables.length,
      iconSourceCount: 0,
      componentSetCount: 0,
      componentCount: 0,
      warningCount: 0,
      advisoryCount: 0,
    },
    warnings: [],
    advisories: [],
  };

  postAuditProgress(
    "audit",
    "Auditing typography config...",
    "Checking local text styles and generated component text against the active font config.",
  );
  phaseStartedAt = auditTimerNow();
  const typography = await auditConfiguredTypography();
  audit.typography = typography;
  if (typography.issueCount > 0) {
    audit.warnings.push(
      `Typography: ${typography.issueCount} font config issue(s) found. Run Apply Text Styles, then update any component sets that still report stale generated text.${formatTypographyIssueDetails(typography)}`,
    );
  }
  recordAuditTiming(performance, "typography", phaseStartedAt, {
    textStyleIssues: typography.textStyles.issueCount,
    componentTextIssues: typography.componentText.issueCount,
    fallbackWarnings: typography.fontWarnings.length,
  });

  phaseStartedAt = auditTimerNow();
  await removeUnexpectedTopLevelArtifactsBeforeAudit(audit);
  recordAuditTiming(performance, "maintenance cleanup", phaseStartedAt, {
    removed:
      audit.maintenance &&
      Array.isArray(audit.maintenance.removedUnexpectedTopLevel)
        ? audit.maintenance.removedUnexpectedTopLevel.length
        : 0,
  });

  if (icons.issueCount > 0) {
    audit.warnings.push(
      `${icons.issueCount} icon source component issue(s) found. Run Update Icons to refresh source sizing and constraints.`,
    );
  }

  if (surfaceQa.issueCount > 0) {
    audit.warnings.push(
      `${surfaceQa.issueCount} transparent surface QA issue(s) found. Fix the affected component contrast, then run Build Surface QA to refresh the generated page.`,
    );
  }

  const pages = figma.root.children;
  for (let pageIndex = 0; pageIndex < pages.length; pageIndex += 1) {
    const page = pages[pageIndex];
    postAuditProgress(
      "audit",
      "Auditing component sets...",
      `Checking ${page.name} (${pageIndex + 1}/${pages.length}).`,
    );
    phaseStartedAt = auditTimerNow();
    await page.loadAsync();
    const pageNodes = collectPageAuditNodes(page);
    const componentSets = pageNodes.componentSets;
    const components = pageNodes.components;
    const textNodes = pageNodes.textNodes;
    const pageRecord = {
      id: page.id,
      name: page.name,
      topLevelChildren: page.children.length,
      topLevelSample: page.children.slice(0, 12).map((node) => ({
        id: node.id,
        urlNodeId: nodeIdForUrl(node.id),
        name: node.name,
        type: node.type,
      })),
      componentSets: componentSets.length,
      components: components.length,
      textNodes: textNodes.length,
    };

    const unexpectedTopLevel = unexpectedTopLevelNodesForPage(page);
    if (unexpectedTopLevel.length > 0) {
      pageRecord.unexpectedTopLevel = unexpectedTopLevel;
      audit.warnings.push(
        `${page.name}: ${unexpectedTopLevel.length} unexpected top-level node(s) found: ${formatUnexpectedTopLevelNodes(unexpectedTopLevel)}. Run Reorganize Components to remove stray generated instances, then audit again.`,
      );
    }

    const layoutIssues = auditComponentsPageLayout(page);
    if (layoutIssues.length > 0) {
      pageRecord.layoutIssues = layoutIssues;
      audit.warnings.push(
        `${page.name}: ${layoutIssues.length} component layout overlap issue(s) found. Run Reorganize Components to refresh the measured component column.`,
      );
    }
    const exampleGeometryIntegrity = auditGeneratedExampleGeometry(page);
    if (exampleGeometryIntegrity.issueCount > 0) {
      pageRecord.exampleGeometryIntegrity = exampleGeometryIntegrity;
      audit.warnings.push(
        `${page.name}: ${exampleGeometryIntegrity.issueCount} collapsed generated example node(s) found.${formatGeometryIssueDetails(exampleGeometryIntegrity.issues) ? ` Details: ${formatGeometryIssueDetails(exampleGeometryIntegrity.issues)}` : ""}`,
      );
    }
    recordAuditTiming(performance, "page scan", phaseStartedAt, {
      page: page.name,
      componentSets: componentSets.length,
      components: components.length,
      textNodes: textNodes.length,
      layoutIssues: layoutIssues.length,
      exampleGeometryIssues: exampleGeometryIntegrity.issueCount,
    });

    audit.pages.push(pageRecord);
    if (!isArchivePageName(page.name)) {
      audit.summary.componentSetCount += componentSets.length;
      audit.summary.componentCount += components.length;
    } else {
      pageRecord.archivedComponentSets = componentSets.length;
      pageRecord.archivedComponents = components.length;
    }

    if (!isArchivePageName(page.name)) {
      for (const componentSet of componentSets) {
        const record = auditComponentSet(
          componentSet,
          page.name,
          variableContext,
          performance,
        );
        audit.componentSets.push(record);
        for (const warning of record.warnings) {
          audit.warnings.push(`${record.name}: ${warning}`);
        }
        for (const advisory of record.advisories) {
          audit.advisories.push(`${record.name}: ${advisory}`);
        }
      }
    }
  }

  audit.summary.iconSourceCount = audit.icons.foundCount;
  postAuditProgress(
    "ready",
    "Preparing audit results...",
    "Summarizing component warnings, advisories, and next actions.",
  );
  phaseStartedAt = auditTimerNow();
  audit.summary.warningCount = audit.warnings.length;
  audit.summary.advisoryCount = audit.advisories.length;
  recordAuditTiming(performance, "summary", phaseStartedAt, {
    warnings: audit.summary.warningCount,
    advisories: audit.summary.advisoryCount,
  });
  audit.performance = finalizeAuditPerformance(performance, auditStartedAt);
  audit.summary.auditDurationMs = audit.performance.totalMs;
  audit.summary.slowestAuditPhase = audit.performance.slowestPhase;
  return audit;
}

function collectPageAuditNodes(page) {
  const result = {
    componentSets: [],
    components: [],
    textNodes: [],
  };

  function walk(node) {
    if (node.type === "COMPONENT_SET") result.componentSets.push(node);
    if (node.type === "COMPONENT") result.components.push(node);
    if (node.type === "TEXT") result.textNodes.push(node);

    if (node.children) {
      for (const child of node.children) walk(child);
    }
  }

  walk(page);
  return result;
}

async function removeUnexpectedTopLevelArtifactsBeforeAudit(audit) {
  const page = figma.root.children.find(
    (child) => child.name === COMPONENTS_PAGE_NAME,
  );
  if (!page) return;

  await page.loadAsync();
  const stats = {
    removedUnexpectedTopLevelInstances: [],
    unexpectedTopLevelInstancesRemoved: 0,
  };
  removeUnexpectedTopLevelGeneratedInstances(page, stats);

  if (stats.removedUnexpectedTopLevelInstances.length > 0) {
    audit.maintenance = audit.maintenance || {};
    audit.maintenance.removedUnexpectedTopLevel =
      stats.removedUnexpectedTopLevelInstances;
  }
}

function isArchivePageName(name) {
  return name === "Archive / Legacy Reference";
}

function unexpectedTopLevelNodesForPage(page) {
  if (page.name !== "Components") return [];

  const expectedComponentSets = new Set(COMPONENT_PAGE_LAYOUT_ORDER);

  const unexpected = [];
  for (const node of page.children) {
    if (isComponentSectionHeading(node)) continue;
    if (isComponentLayoutSection(node)) continue;

    const isGeneratedComponentSet =
      node.type === "COMPONENT_SET" &&
      node.getSharedPluginData &&
      node.getSharedPluginData(RUN_NAMESPACE, "kind") === "component-set";
    if (
      isGeneratedComponentSet ||
      (node.type === "COMPONENT_SET" &&
        expectedComponentSets.has(canonicalComponentSetName(node.name)))
    ) {
      continue;
    }

    unexpected.push({
      id: node.id,
      urlNodeId: nodeIdForUrl(node.id),
      name: node.name,
      type: node.type,
    });
  }

  return unexpected;
}

function knownComponentSetNamesForLayout() {
  return new Set(COMPONENT_PAGE_LAYOUT_ORDER.map(canonicalComponentSetName));
}

function isKnownGeneratedTopLevelInstance(node, knownNames) {
  if (!node || node.type !== "INSTANCE") return false;

  const canonicalName = canonicalComponentSetName(node.name);
  if (knownNames.has(canonicalName)) return true;

  const sourceComponentSet = nodeSourceComponentSet(node);
  return (
    sourceComponentSet &&
    knownNames.has(canonicalComponentSetName(sourceComponentSet))
  );
}

function removeUnexpectedTopLevelGeneratedInstances(page, stats) {
  if (!page || page.name !== COMPONENTS_PAGE_NAME) return;

  const knownNames = knownComponentSetNamesForLayout();
  const stale = [];
  for (const child of page.children.slice()) {
    if (
      isKnownGeneratedTopLevelInstance(child, knownNames) ||
      isKnownGeneratedTopLevelAnatomyArtifact(child)
    ) {
      stale.push(child);
    }
  }

  for (const node of stale) {
    if (!Array.isArray(stats.removedUnexpectedTopLevelInstances)) {
      stats.removedUnexpectedTopLevelInstances = [];
    }

    stats.removedUnexpectedTopLevelInstances.push({
      id: node.id,
      urlNodeId: nodeIdForUrl(node.id),
      name: node.name,
      type: node.type,
      sourceComponentSet: nodeSourceComponentSet(node),
    });
    node.remove();
    incrementStat(stats, "unexpectedTopLevelInstancesRemoved");
  }
}

function isKnownGeneratedTopLevelAnatomyArtifact(node) {
  for (const componentName of COMPONENT_PAGE_LAYOUT_ORDER) {
    if (isTopLevelGeneratedAnatomyArtifact(node, componentName)) return true;
  }

  return false;
}

function auditComponentsPageLayout(page) {
  if (page.name !== COMPONENTS_PAGE_NAME) return [];

  const componentSets = [];
  const knownNames = new Set(COMPONENT_PAGE_LAYOUT_ORDER);

  for (const node of collectComponentSetsInPage(page)) {
    const name = canonicalComponentSetName(node.name);
    const isGenerated =
      node.getSharedPluginData &&
      node.getSharedPluginData(RUN_NAMESPACE, "kind") === "component-set";
    if (!knownNames.has(name) && !isGenerated) continue;

    const bounds = measureComponentSetLayoutBounds(node);
    const position = absoluteNodePosition(node);
    componentSets.push({
      id: node.id,
      urlNodeId: nodeIdForUrl(node.id),
      name: node.name,
      left: position.x + bounds.minX,
      top: position.y + bounds.minY,
      right: position.x + bounds.maxX,
      bottom: position.y + bounds.maxY,
    });
  }

  const issues = [];
  componentSets.sort((a, b) => a.top - b.top || a.left - b.left);

  for (let index = 0; index < componentSets.length; index += 1) {
    const current = componentSets[index];
    for (
      let nextIndex = index + 1;
      nextIndex < componentSets.length;
      nextIndex += 1
    ) {
      const next = componentSets[nextIndex];
      if (next.top >= current.bottom - 1) break;
      if (!rangesOverlap(current.left, current.right, next.left, next.right)) {
        continue;
      }

      issues.push({
        kind: "component-set-overlap",
        first: current.name,
        firstId: current.id,
        firstUrlNodeId: current.urlNodeId,
        second: next.name,
        secondId: next.id,
        secondUrlNodeId: next.urlNodeId,
        overlapY: Math.round(current.bottom - next.top),
      });
    }
  }

  return issues;
}

function rangesOverlap(firstStart, firstEnd, secondStart, secondEnd) {
  return firstStart < secondEnd && secondStart < firstEnd;
}

function formatUnexpectedTopLevelNodes(nodes) {
  const visible = nodes.slice(0, 4).map((node) => {
    return `${node.name} (${node.type}, node-id=${node.urlNodeId})`;
  });
  const remaining = nodes.length - visible.length;
  if (remaining > 0) {
    visible.push(`${remaining} more`);
  }
  return visible.join("; ");
}

function auditVariables(collections, variables) {
  const byCollection = [];

  for (const collection of collections) {
    const collectionVariables = variables.filter(
      (variable) => variable.variableCollectionId === collection.id,
    );
    const byType = {};
    let scopedVariables = 0;
    let unscopedVariables = 0;
    let variablesWithCodeSyntax = 0;

    for (const variable of collectionVariables) {
      byType[variable.resolvedType] = (byType[variable.resolvedType] || 0) + 1;
      if (variable.scopes && variable.scopes.length > 0) {
        scopedVariables += 1;
      } else {
        unscopedVariables += 1;
      }

      if (variable.codeSyntax && Object.keys(variable.codeSyntax).length > 0) {
        variablesWithCodeSyntax += 1;
      }
    }

    byCollection.push({
      id: collection.id,
      name: collection.name,
      modes: collection.modes.map((mode) => mode.name),
      variableCount: collectionVariables.length,
      byType,
      scopedVariables,
      unscopedVariables,
      variablesWithCodeSyntax,
      sample: collectionVariables.slice(0, 12).map((variable) => ({
        id: variable.id,
        name: variable.name,
        type: variable.resolvedType,
        scopes: variable.scopes || [],
      })),
    });
  }

  return {
    total: variables.length,
    byCollection,
  };
}

async function auditConfiguredTypography() {
  const stats = { warnings: [] };
  const config = currentFontConfig();
  let fonts = null;

  try {
    fonts = await resolveConfiguredFonts(stats);
  } catch (error) {
    return {
      configured: config,
      resolved: null,
      fontWarnings: [
        `Could not resolve configured typography (${messageFor(error)}).`,
      ],
      textStyles: {
        checked: 0,
        missingCount: 0,
        staleCount: 0,
        issueCount: 0,
        issues: [],
      },
      componentText: {
        found: false,
        checked: 0,
        staleCount: 0,
        issueCount: 0,
        issues: [],
      },
      issueCount: 1,
    };
  }

  const textStyles = await auditConfiguredTextStyles(fonts);
  const componentText = await auditGeneratedComponentTextFonts(fonts);

  return {
    configured: config,
    resolved: stats.fontConfig || null,
    fontWarnings: stats.warnings.slice(),
    textStyles,
    componentText,
    issueCount:
      stats.warnings.length + textStyles.issueCount + componentText.issueCount,
  };
}

async function auditConfiguredTextStyles(fonts) {
  const styles = await getLocalTextStylesSafe();
  const styleByName = {};
  for (const style of styles) styleByName[style.name] = style;

  const specs = kozmosTextStyleSpecs(fonts);
  const issues = [];
  let missingCount = 0;
  let staleCount = 0;

  for (const spec of specs) {
    const style = styleByName[spec.name];
    if (!style) {
      missingCount += 1;
      pushLimitedTypographyIssue(issues, {
        kind: "missing-text-style",
        name: spec.name,
      });
      continue;
    }

    const staleFields = staleTextStyleFields(style, spec);
    if (staleFields.length > 0) {
      staleCount += 1;
      pushLimitedTypographyIssue(issues, {
        kind: "stale-text-style",
        name: spec.name,
        fields: staleFields,
        actualFont: fontLabel(style.fontName),
        expectedFont: fontLabel(spec.fontName),
      });
    }
  }

  return {
    checked: specs.length,
    missingCount,
    staleCount,
    issueCount: missingCount + staleCount,
    issues,
  };
}

function staleTextStyleFields(style, spec) {
  const fields = [];
  if (!sameFont(style.fontName, spec.fontName)) fields.push("font");
  if (style.fontSize !== spec.fontSize) fields.push("fontSize");

  const lineHeight = lineHeightPixelValue(style.lineHeight);
  if (lineHeight !== spec.lineHeight) fields.push("lineHeight");

  return fields;
}

function lineHeightPixelValue(lineHeight) {
  if (!lineHeight || lineHeight.unit !== "PIXELS") return null;
  return lineHeight.value;
}

async function auditGeneratedComponentTextFonts(fonts) {
  const page = figma.root.children.find(
    (child) => child.name === COMPONENTS_PAGE_NAME,
  );
  if (!page) {
    return {
      found: false,
      checked: 0,
      staleCount: 0,
      issueCount: 0,
      issues: [],
    };
  }

  await page.loadAsync();
  const specByKey = textStyleSpecByKey(fonts);
  const issues = [];
  let checked = 0;
  let staleCount = 0;
  let skippedInInstances = 0;
  let unmatchedCount = 0;

  const componentSets = page.findAll((node) => node.type === "COMPONENT_SET");
  for (const componentSet of componentSets) {
    const isGenerated =
      componentSet.getSharedPluginData &&
      componentSet.getSharedPluginData(RUN_NAMESPACE, "kind") ===
        "component-set";
    const known =
      COMPONENT_PAGE_LAYOUT_ORDER.indexOf(
        canonicalComponentSetName(componentSet.name),
      ) !== -1;
    if (!isGenerated && !known) continue;

    const textNodes = componentSet.findAll((node) => node.type === "TEXT");
    for (const text of textNodes) {
      if (hasAncestorTypeBefore(text, "INSTANCE", componentSet)) {
        skippedInInstances += 1;
        continue;
      }

      const key = inferTextStyleKeyForComponentText(text, componentSet);
      const spec = key ? specByKey[key] : null;
      if (!spec) {
        unmatchedCount += 1;
        continue;
      }

      checked += 1;
      if (!sameFont(text.fontName, spec.fontName)) {
        staleCount += 1;
        pushLimitedTypographyIssue(issues, {
          kind: "stale-component-text",
          componentSet: componentSet.name,
          text: text.name,
          actualFont: fontLabel(text.fontName),
          expectedFont: fontLabel(spec.fontName),
        });
      }
    }
  }

  return {
    found: true,
    checked,
    skippedInInstances,
    unmatchedCount,
    staleCount,
    issueCount: staleCount,
    issues,
  };
}

function textStyleSpecByKey(fonts) {
  const result = {};
  for (const spec of kozmosTextStyleSpecs(fonts)) result[spec.key] = spec;
  return result;
}

function pushLimitedTypographyIssue(issues, issue) {
  if (issues.length < 12) issues.push(issue);
}

function formatTypographyIssueDetails(typography) {
  const details = [];
  for (const warning of typography.fontWarnings || []) {
    if (warning) details.push(warning);
  }

  const styleIssues = typography.textStyles || {};
  if (styleIssues.missingCount > 0) {
    details.push(`${styleIssues.missingCount} text style(s) missing`);
  }
  if (styleIssues.staleCount > 0) {
    details.push(`${styleIssues.staleCount} text style(s) stale`);
  }

  const componentText = typography.componentText || {};
  if (componentText.staleCount > 0) {
    details.push(`${componentText.staleCount} generated text node(s) stale`);
  }

  return details.length > 0 ? ` Details: ${details.join("; ")}.` : "";
}

async function auditIconSources() {
  const expectedNames = KOSMOS_ICON_DEFINITIONS.map(
    (definition) => definition.name,
  );
  const foundNames = [];
  const foundByName = {};
  const issues = [];
  let sourceChildIssues = 0;
  let sourceConstraintIssues = 0;
  let sourceGeometryIssues = 0;
  const page = figma.root.children.find(
    (child) => child.name === ICON_PAGE_NAME,
  );

  if (page) {
    await page.loadAsync();
    const components = page.findAll(
      (node) =>
        node.type === "COMPONENT" &&
        node.getSharedPluginData &&
        node.getSharedPluginData(RUN_NAMESPACE, "kind") === "icon-source",
    );

    for (const component of components) {
      const iconName =
        component.getSharedPluginData(RUN_NAMESPACE, "icon-name") ||
        component.name.replace(/^Icon \/ /, "");
      foundNames.push(iconName);
      foundByName[iconName] = {
        id: component.id,
        urlNodeId: nodeIdForUrl(component.id),
        name: component.name,
        source: auditIconSourceComponent(component, iconName),
      };

      for (const issue of foundByName[iconName].source.issues) {
        issues.push(issue);
        if (issue.kind === "missing-source") sourceChildIssues += 1;
        if (issue.kind === "source-constraints") sourceConstraintIssues += 1;
        if (issue.kind === "source-geometry") sourceGeometryIssues += 1;
      }
    }
  }

  const missing = expectedNames.filter((name) => !foundByName[name]);
  const unexpected = foundNames.filter(
    (name) => expectedNames.indexOf(name) === -1,
  );

  foundNames.sort();

  return {
    page: ICON_PAGE_NAME,
    expectedCount: expectedNames.length,
    foundCount: foundNames.length,
    missingCount: missing.length,
    unexpectedCount: unexpected.length,
    missing,
    unexpected,
    sourceChildIssues,
    sourceConstraintIssues,
    sourceGeometryIssues,
    issueCount: issues.length,
    issues: issues.slice(0, 12),
    sample: foundNames.slice(0, 12).map((name) => foundByName[name]),
  };
}

async function auditSurfaceQaPage() {
  const page = figma.root.children.find(
    (child) => child.name === SURFACE_QA_PAGE_NAME,
  );
  const expectedPanels = 4;
  const expectedInstances = expectedPanels * surfaceQaExpectedInstanceCount();
  const collections = await figma.variables.getLocalVariableCollectionsAsync();
  const variables = await figma.variables.getLocalVariablesAsync();
  const variableContext = createVariableContext(collections, variables);

  if (!page) {
    return {
      page: SURFACE_QA_PAGE_NAME,
      found: false,
      panelCount: 0,
      expectedPanels,
      instanceCount: 0,
      expectedInstances,
      issueCount: 0,
      issues: [],
    };
  }

  await page.loadAsync();
  const panels = page.findAll(
    (node) =>
      node.getSharedPluginData &&
      node.getSharedPluginData(RUN_NAMESPACE, "kind") === "surface-qa" &&
      node.getSharedPluginData(RUN_NAMESPACE, "role") === "panel",
  );
  const instances = page.findAll(
    (node) =>
      node.getSharedPluginData &&
      node.getSharedPluginData(RUN_NAMESPACE, "kind") === "surface-qa-instance",
  );
  const modes = [];
  const surfaces = [];
  const issues = [];
  const contrast = createContrastAuditResult();

  for (const panel of panels) {
    pushUnique(modes, panel.getSharedPluginData(RUN_NAMESPACE, "mode"));
    pushUnique(surfaces, panel.getSharedPluginData(RUN_NAMESPACE, "surface"));
    mergeContrastModeResult(
      contrast,
      auditSurfaceQaPanelContrast(panel, variableContext),
    );
  }

  if (panels.length !== expectedPanels) {
    issues.push({
      kind: "panel-count",
      expected: expectedPanels,
      actual: panels.length,
    });
  }

  if (instances.length < expectedInstances) {
    issues.push({
      kind: "instance-count",
      expectedAtLeast: expectedInstances,
      actual: instances.length,
    });
  }

  for (const modeName of ["Light", "Dark"]) {
    if (modes.indexOf(modeName) === -1) {
      issues.push({ kind: "missing-mode-panel", mode: modeName });
    }
  }

  for (const surfaceName of ["surface-0", "product-map"]) {
    if (surfaces.indexOf(surfaceName) === -1) {
      issues.push({ kind: "missing-surface-panel", surface: surfaceName });
    }
  }

  if (contrast.textFailures > 0 || contrast.nonTextFailures > 0) {
    issues.push({
      kind: "surface-contrast",
      textFailures: contrast.textFailures,
      nonTextFailures: contrast.nonTextFailures,
      failures: contrast.failures,
    });
  }

  if (contrast.minTextContrast !== null) {
    contrast.minTextContrast = Math.round(contrast.minTextContrast * 100) / 100;
  }

  if (contrast.minNonTextContrast !== null) {
    contrast.minNonTextContrast =
      Math.round(contrast.minNonTextContrast * 100) / 100;
  }

  return {
    page: SURFACE_QA_PAGE_NAME,
    id: page.id,
    found: true,
    panelCount: panels.length,
    expectedPanels,
    instanceCount: instances.length,
    expectedInstances,
    modes,
    surfaces,
    contrast,
    issueCount: issues.length,
    issues,
  };
}

function auditSurfaceQaPanelContrast(panel, variableContext) {
  const modeName = panel.getSharedPluginData(RUN_NAMESPACE, "mode") || "Light";
  const surfaceName =
    panel.getSharedPluginData(RUN_NAMESPACE, "surface") || "surface";
  const result = createContrastAuditResult();
  result.mode = `${modeName} / ${surfaceName}`;

  const background = surfaceQaPanelBackground(panel, variableContext, modeName);
  const instances = panel.findAll(
    (node) =>
      node.getSharedPluginData &&
      node.getSharedPluginData(RUN_NAMESPACE, "kind") === "surface-qa-instance",
  );

  for (const instance of instances) {
    auditSurfaceQaInstanceContrast(
      instance,
      background,
      modeName,
      variableContext,
      result,
    );
  }

  return result;
}

function surfaceQaPanelBackground(panel, variableContext, modeName) {
  const fill = solidPaintToRgba(
    firstVisibleSolidPaint(panel.fills),
    variableContext,
    modeName,
  );

  if (fill) return compositeColor(fill, { r: 1, g: 1, b: 1, a: 1 });

  return (
    colorForVariableName(
      "Surface/0",
      modeName === "Dark" ? "#000000" : "#FFFFFF",
      variableContext,
      modeName,
    ) || { r: 1, g: 1, b: 1, a: 1 }
  );
}

function auditSurfaceQaInstanceContrast(
  instance,
  hostBackground,
  modeName,
  variableContext,
  result,
) {
  const variantName =
    instance.getSharedPluginData(RUN_NAMESPACE, "variant") || instance.name;
  const isDisabled = variantName.indexOf("State=Disabled") !== -1;

  auditNodeContrast(
    instance,
    hostBackground,
    isDisabled,
    result,
    variableContext,
    instance.name,
    modeName,
  );
}

function surfaceQaExpectedInstanceCount() {
  let count = 0;
  for (const group of SURFACE_QA_COMPONENT_GROUPS) {
    for (const row of group.rows) count += row.length;
  }
  return count;
}

function auditIconSourceComponent(component, iconName) {
  const source = directChildNamed(component, "Pointr Source");
  const issues = [];

  if (!source || source.type !== "INSTANCE") {
    issues.push({
      icon: iconName,
      kind: "missing-source",
      message:
        "Icon source component is missing a direct Pointr Source instance.",
    });
    return {
      childType: source ? source.type : null,
      constraints: null,
      width: null,
      height: null,
      issues,
    };
  }

  const constraints = source.constraints || {};
  if (
    constraints.horizontal !== "STRETCH" ||
    constraints.vertical !== "STRETCH"
  ) {
    issues.push({
      icon: iconName,
      kind: "source-constraints",
      horizontal: constraints.horizontal || null,
      vertical: constraints.vertical || null,
      expectedHorizontal: "STRETCH",
      expectedVertical: "STRETCH",
    });
  }

  const sameBounds =
    Math.round(source.x) === 0 &&
    Math.round(source.y) === 0 &&
    Math.round(source.width) === Math.round(component.width) &&
    Math.round(source.height) === Math.round(component.height);

  if (!sameBounds) {
    issues.push({
      icon: iconName,
      kind: "source-geometry",
      x: Math.round(source.x),
      y: Math.round(source.y),
      width: Math.round(source.width),
      height: Math.round(source.height),
      expectedWidth: Math.round(component.width),
      expectedHeight: Math.round(component.height),
    });
  }

  return {
    childType: source.type,
    constraints: {
      horizontal: constraints.horizontal || null,
      vertical: constraints.vertical || null,
    },
    width: Math.round(source.width),
    height: Math.round(source.height),
    issues,
  };
}

function auditComponentSet(
  componentSet,
  pageName,
  variableContext,
  performance,
) {
  const componentSetStartedAt = auditTimerNow();
  const timingMetadata = {
    page: pageName,
    componentSet: componentSet.name,
  };
  const variantAudit = timeAuditStep(
    performance,
    "component variants",
    timingMetadata,
    () => {
      const childComponents = componentSet.children.filter(
        (child) => child.type === "COMPONENT",
      );
      const variantAxes = extractVariantAxes(childComponents);
      const accessibility = auditComponentAccessibility(
        componentSet,
        childComponents,
        variantAxes,
      );

      return {
        childComponents,
        variantAxes,
        accessibility,
      };
    },
  );
  const { childComponents, variantAxes, accessibility } = variantAudit;
  const componentTimingMetadata = Object.assign({}, timingMetadata, {
    variants: childComponents.length,
  });
  const boundVariableIds = timeAuditStep(
    performance,
    "bound variable ids",
    componentTimingMetadata,
    () => collectBoundVariableIds(componentSet),
  );
  const propertyAudit = timeAuditStep(
    performance,
    "component properties",
    componentTimingMetadata,
    () => {
      const propertyDefinitionRead = safeComponentPropertyDefinitions(
        componentSet,
        null,
        "audit",
      );
      const propertyDefinitions = propertyDefinitionRead.definitions;
      const expectedVariantAxes = expectedVariantAxesForComponentSetName(
        componentSet.name,
      );
      const variantProperties = auditVariantProperties(
        propertyDefinitions,
        expectedVariantAxes,
      );
      const componentPropertyConflicts =
        auditComponentPropertyConflicts(propertyDefinitions);

      return {
        propertyDefinitionRead,
        propertyDefinitions,
        variantProperties,
        componentPropertyConflicts,
      };
    },
  );
  const {
    propertyDefinitionRead,
    propertyDefinitions,
    variantProperties,
    componentPropertyConflicts,
  } = propertyAudit;
  const instanceSwapSlots = timeAuditStep(
    performance,
    "instance slots",
    componentTimingMetadata,
    () => auditInstanceSwapSlots(componentSet, propertyDefinitions),
  );
  const textProperties = timeAuditStep(
    performance,
    "text properties",
    componentTimingMetadata,
    () => auditTextProperties(componentSet, propertyDefinitions),
  );
  const focusVisibleProperty = timeAuditStep(
    performance,
    "focus bindings",
    componentTimingMetadata,
    () => auditFocusVisibleProperty(componentSet, propertyDefinitions),
  );
  const iconSlotIntegrity = timeAuditStep(
    performance,
    "icon slot integrity",
    componentTimingMetadata,
    () => auditGeneratedIconSlotIntegrity(componentSet),
  );
  const tooltipTipIntegrity = timeAuditStep(
    performance,
    "tooltip tip integrity",
    componentTimingMetadata,
    () => auditTooltipTipIntegrity(componentSet, childComponents),
  );
  const compositionIntegrity = timeAuditStep(
    performance,
    "composition integrity",
    componentTimingMetadata,
    () => auditCompositionIntegrity(componentSet),
  );
  const geometryIntegrity = timeAuditStep(
    performance,
    "geometry integrity",
    componentTimingMetadata,
    () => auditGeneratedGeometryIntegrity(componentSet),
  );
  const badgeContentIntegrity = timeAuditStep(
    performance,
    "badge content integrity",
    componentTimingMetadata,
    () => auditBadgeContentIntegrity(componentSet, childComponents),
  );
  const slotContract = timeAuditStep(
    performance,
    "slot contract",
    componentTimingMetadata,
    () => auditComponentSlotContract(componentSet, propertyDefinitions),
  );
  const boundVariableFields = timeAuditStep(
    performance,
    "bound variable fields",
    componentTimingMetadata,
    () => auditBoundVariableFields(componentSet),
  );
  const contrast = timeAuditStep(
    performance,
    "contrast audit",
    componentTimingMetadata,
    () =>
      auditComponentContrast(componentSet, childComponents, variableContext),
  );
  const record = {
    id: componentSet.id,
    urlNodeId: nodeIdForUrl(componentSet.id),
    page: pageName,
    name: componentSet.name,
    description: componentSet.description || "",
    variantCount: childComponents.length,
    variantAxes,
    componentProperties: Object.keys(propertyDefinitions),
    componentPropertyConflicts,
    variantProperties: variantProperties.properties,
    instanceSwapSlots,
    textProperties,
    focusVisibleProperty,
    iconSlotIntegrity,
    tooltipTipIntegrity,
    compositionIntegrity,
    geometryIntegrity,
    badgeContentIntegrity,
    slotContract,
    boundVariableCount: boundVariableIds.length,
    boundVariableFields,
    width: Math.round(componentSet.width),
    height: Math.round(componentSet.height),
    accessibility,
    contrast,
    warnings: [],
    advisories: [],
  };
  const textPropertyByBaseName = indexAuditPropertiesByBaseName(textProperties);
  const instanceSlotByBaseName =
    indexAuditPropertiesByBaseName(instanceSwapSlots);
  const warningStartedAt = auditTimerNow();

  if (propertyDefinitionRead.error) {
    record.warnings.push(
      `Could not read component property definitions (${propertyDefinitionRead.error}). Run the component updater once to repair existing Figma property errors, then audit again.`,
    );
  }

  if (childComponents.length === 0) {
    record.warnings.push("Component set has no child components.");
  }

  if (record.boundVariableCount === 0) {
    record.warnings.push("No bound variables found in component set.");
  }

  if (
    shouldAuditLayoutBindings(record.name) &&
    boundVariableFields.layoutFieldCount === 0
  ) {
    record.warnings.push(
      "No bound layout, spacing, radius, or sizing variables found. Run the component updater to bind non-color tokens.",
    );
  }

  if (variantProperties.issueCount > 0) {
    record.warnings.push(
      `${variantProperties.issueCount} stale or unexpected variant property definition(s) found. Run the component updater to normalize the instance property panel.`,
    );
    record.variantPropertyIssues = variantProperties.issues;
  }

  if (componentPropertyConflicts.issueCount > 0) {
    record.warnings.push(
      `${componentPropertyConflicts.issueCount} conflicting component property name(s) found. Run the component updater to consolidate duplicate exposed properties.`,
    );
  }

  if (slotContract.issueCount > 0) {
    const issueDetails = formatSlotContractIssueDetails(slotContract.issues);
    record.warnings.push(
      `${slotContract.issueCount} slot contract issue(s) found. Run the component updater so shell slots are exposed as shared Figma SLOT properties and bound to generated slot frames.${issueDetails ? ` Details: ${issueDetails}` : ""}`,
    );
  }

  if (
    shouldAuditTypographyBindings(record.name) &&
    boundVariableFields.textNodesWithoutTextStyle > 0
  ) {
    record.warnings.push(
      `${boundVariableFields.textNodesWithoutTextStyle} text node(s) are missing Figma text styles. Run Apply Text Styles or the component updater to attach the shared typography styles.`,
    );
  }

  if (
    shouldAuditTypographyBindings(record.name) &&
    boundVariableFields.textNodes > 0 &&
    boundVariableFields.typographyFieldCount === 0 &&
    boundVariableFields.textStyleNodes < boundVariableFields.textNodes
  ) {
    record.warnings.push(
      `${boundVariableFields.textNodes - boundVariableFields.textStyleNodes} text node(s) are missing typography token bindings or Figma text styles.`,
    );
  }

  if (shouldAuditFocusBindings(record.name)) {
    if (
      !focusVisibleProperty.propertyName ||
      focusVisibleProperty.boundNodes === 0
    ) {
      record.warnings.push(
        "Focus Visible boolean property is missing or not bound to generated focus ring layers.",
      );
    }

    if (focusVisibleProperty.geometryIssueCount > 0) {
      record.warnings.push(
        `${focusVisibleProperty.geometryIssueCount} focus ring geometry issue(s) found. Run the component updater to refresh absolute focus ring placement.`,
      );
    }
  }

  if (
    record.name === "Button" ||
    record.name === "ToggleButton" ||
    record.name === "SplitButton" ||
    record.name === "Badge" ||
    record.name === "Chip" ||
    record.name === "Checkbox" ||
    record.name === "Radio" ||
    record.name === "Switch" ||
    record.name === "Input" ||
    record.name === "PasswordInput" ||
    record.name === "NumberInput" ||
    record.name === "Combobox" ||
    record.name === "MultiSelect" ||
    record.name === "Textarea" ||
    record.name === "Search" ||
    record.name === "Slider"
  ) {
    const labelTextProperty = textPropertyByBaseName["Label Text"];
    if (!labelTextProperty || labelTextProperty.boundTextNodes === 0) {
      record.warnings.push(
        "Label Text component property is missing or not bound to label text nodes; Code Connect children mapping will not be editable from Figma.",
      );
    }

    if (
      record.name === "Input" ||
      record.name === "PasswordInput" ||
      record.name === "NumberInput" ||
      record.name === "Combobox" ||
      record.name === "MultiSelect" ||
      record.name === "Textarea" ||
      record.name === "Search" ||
      record.name === "Select"
    ) {
      const placeholderTextProperty =
        textPropertyByBaseName["Placeholder Text"];
      if (
        !placeholderTextProperty ||
        placeholderTextProperty.boundTextNodes === 0
      ) {
        record.warnings.push(
          "Placeholder Text component property is missing or not bound to placeholder text nodes; Code Connect placeholder mapping will not be editable from Figma.",
        );
      }

      if (
        record.name === "Input" ||
        record.name === "PasswordInput" ||
        record.name === "NumberInput" ||
        record.name === "Combobox" ||
        record.name === "MultiSelect"
      ) {
        const helperTextProperty = textPropertyByBaseName["Helper Text"];
        if (!helperTextProperty || helperTextProperty.boundTextNodes === 0) {
          record.warnings.push(
            "Helper Text component property is missing or not bound to helper text nodes; validation and hint copy will not be editable from Figma.",
          );
        }
      }

      if (record.name === "Combobox") {
        for (const baseName of [
          "Option 1 Text",
          "Option 2 Text",
          "Option 3 Text",
        ]) {
          const optionTextProperty = textPropertyByBaseName[baseName];
          if (!optionTextProperty || optionTextProperty.boundTextNodes === 0) {
            record.warnings.push(
              `${baseName} component property is missing or not bound to generated Combobox option text nodes.`,
            );
          }
        }
      }

      if (record.name === "MultiSelect") {
        for (const baseName of [
          "Option 1 Text",
          "Option 2 Text",
          "Option 3 Text",
        ]) {
          const textProperty = textPropertyByBaseName[baseName];
          if (!textProperty || textProperty.boundTextNodes === 0) {
            record.warnings.push(
              `${baseName} component property is missing or not bound to generated MultiSelect text nodes.`,
            );
          }
        }
      }
    }
  }

  if (record.name === "Listbox") {
    for (const baseName of [
      "Option 1 Text",
      "Option 1 Description",
      "Option 2 Text",
      "Option 2 Description",
      "Option 3 Text",
      "Option 3 Description",
    ]) {
      const textProperty = textPropertyByBaseName[baseName];
      if (!textProperty || textProperty.boundTextNodes === 0) {
        record.warnings.push(
          `${baseName} component property is missing or not bound to generated Listbox option nodes.`,
        );
      }
    }
  }

  if (record.name === "SegmentedControl") {
    for (const baseName of ["Item 1 Text", "Item 2 Text", "Item 3 Text"]) {
      const textProperty = textPropertyByBaseName[baseName];
      if (!textProperty || textProperty.boundTextNodes === 0) {
        record.warnings.push(
          `${baseName} component property is missing or not bound to generated SegmentedControl text nodes.`,
        );
      }
    }
  }

  if (record.name === "Counter") {
    const counterTextProperty = textPropertyByBaseName["Counter Text"];
    if (!counterTextProperty || counterTextProperty.boundTextNodes === 0) {
      record.warnings.push(
        "Counter Text component property is missing or not bound to generated Counter text nodes.",
      );
    }
  }

  if (record.name === "Badge") {
    const hasShowCounterProperty = Object.keys(propertyDefinitions).some(
      (propertyName) => {
        const definition = propertyDefinitions[propertyName];
        return (
          propertyName.split("#")[0] === "Show Counter" &&
          definition.type === "BOOLEAN"
        );
      },
    );
    if (!hasShowCounterProperty) {
      record.warnings.push(
        "Show Counter boolean property is missing; Badge counters should be hidden by default and opt-in per instance.",
      );
    }

    const iconSlot = instanceSlotByBaseName.Icon;
    if (!iconSlot || iconSlot.boundInstances === 0) {
      record.warnings.push(
        "Icon instance-swap property is missing or not bound to Badge icon-size variants.",
      );
    }

    if (badgeContentIntegrity.issueCount > 0) {
      record.warnings.push(
        `${badgeContentIntegrity.issueCount} Badge content integrity issue(s) found. Run the Badge updater so icon-size variants use Icon slots and label variants keep hidden counter capsules.`,
      );
    }
  }

  if (record.name === "Card") {
    for (const baseName of ["Title Text", "Description Text", "Body Text"]) {
      const textProperty = textPropertyByBaseName[baseName];
      if (!textProperty || textProperty.boundTextNodes === 0) {
        record.warnings.push(
          `${baseName} component property is missing or not bound to generated Card text nodes.`,
        );
      }
    }
  }

  if (record.name === "Box") {
    if (
      !componentSetHasSlotPropertyOrNode(
        componentSet,
        propertyDefinitions,
        "Content Slot",
      )
    ) {
      record.warnings.push(
        "Content Slot is missing; Box should expose arbitrary children as a native Figma slot or fallback slot frame.",
      );
    }
  }

  if (record.name === "Container") {
    if (
      !componentSetHasSlotPropertyOrNode(
        componentSet,
        propertyDefinitions,
        "Content Slot",
      )
    ) {
      record.warnings.push(
        "Content Slot is missing; Container should expose arbitrary children as a native Figma slot or fallback slot frame.",
      );
    }
  }

  if (record.name === "Stack") {
    if (
      !componentSetHasSlotPropertyOrNode(
        componentSet,
        propertyDefinitions,
        "Content Slot",
      )
    ) {
      record.warnings.push(
        "Content Slot is missing; Stack should expose arbitrary children as a native Figma slot or fallback slot frame.",
      );
    }
  }

  if (record.name === "Grid") {
    if (!gridHasCellSlots(componentSet, propertyDefinitions)) {
      record.warnings.push(
        "Grid cell slots are missing; Grid should expose replaceable Cell slots in row-major order.",
      );
    }
    for (const issue of gridCellSlotIntegrityIssues(componentSet)) {
      record.warnings.push(issue);
    }
  }

  if (record.name === "Tree") {
    if (
      !componentSetHasSlotPropertyOrNode(
        componentSet,
        propertyDefinitions,
        "Content Slot",
      )
    ) {
      record.warnings.push(
        "Content Slot is missing; Tree should expose composed TreeParentItem and TreeChildItem rows through a slot instead of fixed example rows.",
      );
    }
  }

  if (record.name === "Breadcrumb") {
    for (const baseName of [
      "Item 1 Text",
      "Item 2 Text",
      "Current Page Text",
    ]) {
      const textProperty = textPropertyByBaseName[baseName];
      if (!textProperty || textProperty.boundTextNodes === 0) {
        record.warnings.push(
          `${baseName} component property is missing or not bound to generated Breadcrumb text nodes.`,
        );
      }
    }
  }

  if (record.name === "Accordion") {
    for (const baseName of ["Trigger Text", "Content Text"]) {
      const textProperty = textPropertyByBaseName[baseName];
      if (!textProperty || textProperty.boundTextNodes === 0) {
        record.warnings.push(
          `${baseName} component property is missing or not bound to generated Accordion text nodes.`,
        );
      }
    }
  }

  if (compositionIntegrity.issueCount > 0) {
    const issueDetails = formatCompositionIssueDetails(
      compositionIntegrity.issues,
    );
    record.warnings.push(
      `${compositionIntegrity.issueCount} composite component integrity issue(s) found. Composite components must use live nested instances with valid auto-layout sizing.${issueDetails ? ` Details: ${issueDetails}` : ""}`,
    );
  }

  if (geometryIntegrity.issueCount > 0) {
    const issueDetails = formatGeometryIssueDetails(geometryIntegrity.issues);
    record.warnings.push(
      `${geometryIntegrity.issueCount} collapsed generated node(s) found. Run the component updater to refresh collapsed frames or text nodes.${issueDetails ? ` Details: ${issueDetails}` : ""}`,
    );
  }

  if (record.name === "Tabs") {
    for (const baseName of [
      "Tab 1 Text",
      "Tab 2 Text",
      "Tab 3 Text",
      "Tab 4 Text",
    ]) {
      const textProperty = textPropertyByBaseName[baseName];
      if (!textProperty || textProperty.boundTextNodes === 0) {
        record.warnings.push(
          `${baseName} component property is missing or not bound to generated Tabs trigger text nodes.`,
        );
      }
    }
  }

  if (record.name === "Tooltip") {
    const textProperty = textPropertyByBaseName["Content Text"];
    if (!textProperty || textProperty.boundTextNodes === 0) {
      record.warnings.push(
        "Content Text component property is missing or not bound to generated Tooltip text nodes.",
      );
    }

    if (tooltipTipIntegrity.tipNodes !== childComponents.length) {
      record.warnings.push(
        `Expected one generated Tooltip tip per side variant, found ${tooltipTipIntegrity.tipNodes}/${childComponents.length}. Run the Tooltip updater to refresh side indicators.`,
      );
    }
    if (tooltipTipIntegrity.triangleTipNodes !== childComponents.length) {
      record.warnings.push(
        `Expected every Tooltip tip to be a generated triangle, found ${tooltipTipIntegrity.triangleTipNodes}/${childComponents.length}. Run the Tooltip updater to refresh side indicators.`,
      );
    }
  }

  if (
    shouldRequireDisabledState(record.name) &&
    accessibility.hasStateAxis &&
    !accessibility.hasDisabledState
  ) {
    record.warnings.push("State axis exists but Disabled state is missing.");
  }

  if (shouldAuditFocusBindings(record.name) && !accessibility.hasFocusState) {
    record.warnings.push("Focus state or focus documentation is missing.");
  }

  if (
    shouldAuditTouchTarget(record.name) &&
    !accessibility.minimumTouchTargetPass
  ) {
    if (
      accessibility.compactVisualSize ||
      shouldAllowCompactVisualTouchTarget(record.name)
    ) {
      record.advisories.push(
        `Compact visual size is ${accessibility.minimumInteractiveSize}px. Keep for desktop-dense controls only; use a 44px hit area or larger visual size for touch contexts.`,
      );
    } else {
      record.warnings.push(
        `Minimum touch target is ${accessibility.minimumInteractiveSize}px; touch guidance prefers at least 44px.`,
      );
    }
  }

  if (record.contrast.textFailures > 0) {
    const failureDetails = formatContrastFailureDetails(
      record.contrast.failures,
      "text",
    );
    record.warnings.push(
      `${record.contrast.textFailures} text contrast pair(s) fail WCAG AA 4.5:1.${failureDetails ? ` Details: ${failureDetails}` : ""}`,
    );
  }

  if (record.contrast.nonTextFailures > 0) {
    const failureDetails = formatContrastFailureDetails(
      record.contrast.failures,
      "non-text",
    );
    record.warnings.push(
      `${record.contrast.nonTextFailures} icon/control contrast pair(s) fail the 3:1 non-text guidance.${failureDetails ? ` Details: ${failureDetails}` : ""}`,
    );
  }

  if (iconSlotIntegrity.maskWrappedSlots > 0) {
    record.warnings.push(
      `${iconSlotIntegrity.maskWrappedSlots} generated Icon slot(s) still use the legacy mask wrapper. Run the component updater to migrate them to direct instance-swap slots.`,
    );
  }

  if (iconSlotIntegrity.colorOverrideIssues > 0) {
    record.warnings.push(
      `${iconSlotIntegrity.colorOverrideIssues} generated Icon slot(s) do not expose tintable fill or stroke layers for direct color overrides.`,
    );
  }

  if (iconSlotIntegrity.unknownSlotIssues > 0) {
    record.warnings.push(
      `${iconSlotIntegrity.unknownSlotIssues} generated Icon slot(s) have an unexpected node structure.`,
    );
  }

  if (record.contrast.surfaceDependent) {
    record.advisories.push(
      "Transparent variants depend on the host surface. Test them on Surface/0 in each mode and on any product surfaces where they will be used.",
    );
  }

  recordAuditTiming(
    performance,
    "component warnings",
    warningStartedAt,
    Object.assign({}, componentTimingMetadata, {
      warnings: record.warnings.length,
      advisories: record.advisories.length,
    }),
  );

  recordAuditTiming(performance, "component set total", componentSetStartedAt, {
    page: pageName,
    componentSet: componentSet.name,
    variants: childComponents.length,
    warnings: record.warnings.length,
    advisories: record.advisories.length,
  });

  return record;
}

function shouldAuditLayoutBindings(name) {
  return (
    [
      "Link",
      "Separator",
      "Skeleton",
      "Box",
      "Stack",
      "Container",
      "Breadcrumb",
      "Pagination",
      "Accordion",
      "Button",
      "IconButton",
      "ToggleButton",
      "SplitButton",
      "FloatingActionButton",
      "Counter",
      "Badge",
      "Chip",
      "SegmentedControl",
      "Card",
      "List",
      "TreeParentItem",
      "TreeChildItem",
      "TreeItem",
      "Tree",
      "Table",
      "NavigationItem",
      "Navbar",
      "Sidebar",
      "Tabs",
      "Tooltip",
      "Dialog",
      "Popover",
      "Menu",
      "Toast",
      "Checkbox",
      "Radio",
      "Switch",
      "Input",
      "PasswordInput",
      "Combobox",
      "MultiSelect",
      "Listbox",
      "Textarea",
      "Search",
      "Select",
      "Slider",
      "Progress",
      "Spinner",
      "Avatar",
      "Alert",
    ].indexOf(name) !== -1
  );
}

function shouldRequireDisabledState(name) {
  return (
    [
      "Button",
      "IconButton",
      "ToggleButton",
      "SplitButton",
      "FloatingActionButton",
      "Chip",
      "SegmentedControl",
      "NavigationItem",
      "Checkbox",
      "Radio",
      "Switch",
      "Input",
      "PasswordInput",
      "Combobox",
      "MultiSelect",
      "Listbox",
      "Textarea",
      "Search",
      "SearchBar",
      "Select",
      "Slider",
    ].indexOf(name) !== -1
  );
}

function shouldAuditTypographyBindings(name) {
  return (
    [
      "Link",
      "Box",
      "Container",
      "Breadcrumb",
      "Pagination",
      "Accordion",
      "Button",
      "ToggleButton",
      "SplitButton",
      "Counter",
      "Badge",
      "Tag",
      "Chip",
      "SegmentedControl",
      "Card",
      "List",
      "TreeParentItem",
      "TreeChildItem",
      "TreeItem",
      "Tree",
      "Table",
      "Timeline",
      "BottomNavigation",
      "NavigationItem",
      "Navbar",
      "Sidebar",
      "Tabs",
      "Tooltip",
      "Checkbox",
      "Radio",
      "Switch",
      "Input",
      "PasswordInput",
      "Combobox",
      "MultiSelect",
      "Listbox",
      "Textarea",
      "Search",
      "SearchBar",
      "Select",
      "Slider",
      "Avatar",
      "Alert",
    ].indexOf(name) !== -1
  );
}

function shouldAuditFocusBindings(name) {
  return (
    [
      "Link",
      "Button",
      "IconButton",
      "ToggleButton",
      "SplitButton",
      "FloatingActionButton",
      "Chip",
      "SegmentedControl",
      "Checkbox",
      "Radio",
      "Switch",
      "Tabs",
      "NavigationItem",
      "TreeParentItem",
      "TreeChildItem",
      "TreeItem",
      "Input",
      "PasswordInput",
      "Combobox",
      "MultiSelect",
      "Listbox",
      "Textarea",
      "Search",
      "SearchBar",
      "Select",
      "Slider",
    ].indexOf(name) !== -1
  );
}

function shouldAuditTouchTarget(name) {
  return (
    [
      "Button",
      "IconButton",
      "ToggleButton",
      "SplitButton",
      "FloatingActionButton",
      "Badge",
      "Pagination",
      "Chip",
      "SegmentedControl",
      "BottomNavigation",
      "NavigationItem",
      "Tabs",
      "Checkbox",
      "Radio",
      "Switch",
      "Input",
      "PasswordInput",
      "Combobox",
      "MultiSelect",
      "Listbox",
      "Textarea",
      "Search",
      "SearchBar",
      "Select",
      "Slider",
    ].indexOf(name) !== -1
  );
}

function shouldAllowCompactVisualTouchTarget(name) {
  return name === "Chip";
}

function extractVariantAxes(childComponents) {
  const axes = {};
  for (const component of childComponents) {
    const parts = component.name.split(",");
    for (const part of parts) {
      const index = part.indexOf("=");
      if (index === -1) continue;
      const axis = part.slice(0, index).trim();
      const value = part.slice(index + 1).trim();
      if (!axes[axis]) axes[axis] = [];
      if (axes[axis].indexOf(value) === -1) axes[axis].push(value);
    }
  }

  for (const key of Object.keys(axes)) {
    axes[key].sort();
  }

  return axes;
}

function expectedVariantAxesForComponentSetName(name) {
  const canonicalName = canonicalComponentSetName(name);

  if (canonicalName === "Text") {
    return {
      Size: TEXT_SIZES,
      Weight: TEXT_WEIGHTS,
      Tone: TEXT_TONES,
    };
  }

  if (canonicalName === "Heading") {
    return {
      Level: HEADING_LEVELS,
    };
  }

  if (canonicalName === "Link") {
    return {
      Variant: LINK_VARIANTS,
      State: LINK_STATES,
    };
  }

  if (canonicalName === "Label") {
    return {
      State: LABEL_STATES,
    };
  }

  if (canonicalName === "AdaptiveMapShell") {
    return {
      PanelPlacement: ADAPTIVE_MAP_SHELL_PANEL_PLACEMENTS,
    };
  }

  if (canonicalName === "MapControlButton") {
    return {
      Presentation: MAP_CONTROL_BUTTON_PRESENTATIONS,
    };
  }

  if (canonicalName === "MapControlsGroup") {
    return {
      LocationPresentation: MAP_CONTROLS_GROUP_PRESENTATIONS,
    };
  }

  if (canonicalName === "MapOverlay") {
    return {
      Width: MAP_OVERLAY_WIDTHS,
    };
  }

  if (canonicalName === "POIDetailPanel") {
    return {
      Presentation: POI_DETAIL_PANEL_PRESENTATIONS,
    };
  }

  if (canonicalName === "BrowseCategoriesPanel") {
    return {
      Content: BROWSE_CATEGORIES_PANEL_CONTENT,
    };
  }

  if (canonicalName === "CategoryTile") {
    return {
      State: CATEGORY_TILE_STATES,
    };
  }

  if (canonicalName === "POIMediaGallery") {
    return {
      Content: POI_MEDIA_GALLERY_CONTENT,
    };
  }

  if (canonicalName === "POIResultCard") {
    return {
      State: POI_RESULT_CARD_STATES,
    };
  }

  if (canonicalName === "POIResultList") {
    return {
      Content: POI_RESULT_LIST_CONTENT,
    };
  }

  if (canonicalName === "RouteOptionCard") {
    return {
      State: ROUTE_OPTION_CARD_STATES,
    };
  }

  if (canonicalName === "RoutePreviewPanel") {
    return {
      Status: ROUTE_PREVIEW_PANEL_STATUSES,
    };
  }

  if (canonicalName === "RouteSummary") {
    return {
      State: ROUTE_SUMMARY_STATES,
    };
  }

  if (canonicalName === "RoutingInputGroup") {
    return {
      Content: ROUTING_INPUT_GROUP_CONTENT,
    };
  }

  if (canonicalName === "SaveLocationCard") {
    return {
      State: SAVE_LOCATION_CARD_STATES,
    };
  }

  if (canonicalName === "UserLocationMarker") {
    return {
      Heading: USER_LOCATION_MARKER_HEADINGS,
    };
  }

  if (canonicalName === "DynamicIsland") {
    return {
      State: DYNAMIC_ISLAND_STATES,
    };
  }

  if (canonicalName === "FeedbackCard") {
    return {
      State: FEEDBACK_CARD_STATES,
    };
  }

  if (canonicalName === "DirectionStep") {
    return {
      Type: DIRECTION_STEP_TYPES,
    };
  }

  if (canonicalName === "FloorSelector") {
    return {
      Variant: FLOOR_SELECTOR_VARIANTS,
    };
  }

  if (canonicalName === "LocationPin") {
    return {
      State: LOCATION_PIN_STATES,
      Size: LOCATION_PIN_SIZES,
    };
  }

  if (canonicalName === "MapView") {
    return {
      Content: MAP_VIEW_CONTENT,
    };
  }

  if (canonicalName === "POICard") {
    return {
      Content: POI_CARD_CONTENT,
    };
  }

  if (canonicalName === "WayfindingCard") {
    return {
      Content: WAYFINDING_CARD_CONTENT,
    };
  }

  if (canonicalName === "Separator") {
    return {
      Orientation: SEPARATOR_ORIENTATIONS,
    };
  }

  if (canonicalName === "Skeleton") {
    return {
      Shape: SKELETON_SHAPES,
    };
  }

  if (canonicalName === "Box") {
    return {
      Surface: BOX_SURFACES,
    };
  }

  if (canonicalName === "Stack") {
    return {
      Direction: STACK_DIRECTIONS,
      Gap: STACK_GAPS,
    };
  }

  if (canonicalName === "Grid") {
    return {
      Columns: GRID_COLUMNS,
      Gap: GRID_GAPS,
    };
  }

  if (canonicalName === "ScrollArea") {
    return {
      Orientation: SCROLL_AREA_ORIENTATIONS,
      Scrollbar: SCROLL_AREA_SCROLLBARS,
    };
  }

  if (canonicalName === "Container") {
    return {
      Centered: CONTAINER_CENTERED,
    };
  }

  if (canonicalName === "Breadcrumb") {
    return {
      Content: BREADCRUMB_CONTENT,
    };
  }

  if (canonicalName === "Pagination") {
    return {
      Content: PAGINATION_CONTENT,
      Size: PAGINATION_SIZES,
    };
  }

  if (canonicalName === "Accordion") {
    return {
      State: ACCORDION_STATES,
    };
  }

  if (canonicalName === "Button") {
    return {
      Variant: BUTTON_VARIANTS,
      Size: BUTTON_SIZES,
      State: BUTTON_STATES,
    };
  }

  if (canonicalName === "IconButton") {
    return {
      Variant: BUTTON_VARIANTS,
      Size: ICON_BUTTON_SIZES,
      State: BUTTON_STATES,
    };
  }

  if (canonicalName === "ToggleButton") {
    return {
      Variant: TOGGLE_BUTTON_VARIANTS,
      Size: TOGGLE_BUTTON_SIZES,
      State: TOGGLE_BUTTON_STATES,
    };
  }

  if (canonicalName === "SplitButton") {
    return {
      Variant: SPLIT_BUTTON_VARIANTS,
      Size: SPLIT_BUTTON_SIZES,
      State: SPLIT_BUTTON_STATES,
    };
  }

  if (canonicalName === "FloatingActionButton") {
    return {
      Variant: FLOATING_ACTION_BUTTON_VARIANTS,
      Size: FLOATING_ACTION_BUTTON_SIZES,
      State: FLOATING_ACTION_BUTTON_STATES,
    };
  }

  if (canonicalName === "Counter") {
    return {
      Tone: COUNTER_TONES,
      Size: COUNTER_SIZES,
    };
  }

  if (canonicalName === "Tag") {
    return {
      Variant: TAG_VARIANTS,
      Removable: TAG_REMOVABLE,
    };
  }

  if (canonicalName === "Badge") {
    return {
      Variant: BADGE_VARIANTS,
      Size: BADGE_SIZES,
    };
  }

  if (canonicalName === "Chip") {
    return {
      Variant: CHIP_VARIANTS,
      Size: CHIP_SIZES,
      State: CHIP_STATES,
      Removable: CHIP_REMOVABLE,
    };
  }

  if (canonicalName === "SegmentedControl") {
    return {
      Size: SEGMENTED_CONTROL_SIZES,
      Active: SEGMENTED_CONTROL_ACTIVE,
      State: SEGMENTED_CONTROL_STATES,
    };
  }

  if (canonicalName === "Card") {
    return {
      Content: CARD_CONTENT,
    };
  }

  if (canonicalName === "List") {
    return {
      Density: LIST_DENSITIES,
    };
  }

  if (canonicalName === "Table") {
    return {
      Density: TABLE_DENSITIES,
    };
  }

  if (canonicalName === "Tree") {
    return {
      Density: TREE_DENSITIES,
    };
  }

  if (canonicalName === "TreeParentItem") {
    return {
      Content: TREE_CONTENT,
      Expanded: TREE_EXPANDED,
      Depth: TREE_ITEM_DEPTHS,
      Density: TREE_DENSITIES,
      State: TREE_STATES,
    };
  }

  if (canonicalName === "TreeChildItem") {
    return {
      Content: TREE_CONTENT,
      Depth: TREE_ITEM_DEPTHS,
      Density: TREE_DENSITIES,
      State: TREE_STATES,
    };
  }

  if (canonicalName === "TreeItem") {
    return {
      Type: TREE_ITEM_TYPES,
      Content: TREE_CONTENT,
      Expanded: TREE_EXPANDED,
      Depth: TREE_ITEM_DEPTHS,
      Density: TREE_DENSITIES,
      State: TREE_STATES,
    };
  }

  if (canonicalName === "Timeline") {
    return {
      Content: TIMELINE_CONTENT,
      Density: TIMELINE_DENSITIES,
    };
  }

  if (canonicalName === "Tabs") {
    return {
      Count: TABS_COUNTS,
      Active: TABS_ACTIVE,
      State: TABS_STATES,
    };
  }

  if (canonicalName === "BottomNavigation") {
    return {
      Count: BOTTOM_NAVIGATION_COUNTS,
      Active: BOTTOM_NAVIGATION_ACTIVE,
    };
  }

  if (canonicalName === "NavigationItem") {
    return {
      Placement: NAVIGATION_ITEM_PLACEMENTS,
      Content: NAVIGATION_ITEM_CONTENT,
      Density: NAVIGATION_ITEM_DENSITIES,
      State: NAVIGATION_ITEM_STATES,
    };
  }

  if (canonicalName === "Navbar") {
    return {
      Content: NAVBAR_CONTENT,
    };
  }

  if (canonicalName === "Sidebar") {
    return {
      Content: SIDEBAR_CONTENT,
    };
  }

  if (canonicalName === "Tooltip") {
    return {
      Side: TOOLTIP_SIDES,
    };
  }

  if (canonicalName === "Backdrop") {
    return {
      Visibility: BACKDROP_VISIBILITY,
    };
  }

  if (canonicalName === "BottomSheet") {
    return {
      Content: BOTTOM_SHEET_CONTENT,
    };
  }

  if (canonicalName === "Dialog") {
    return {
      Content: DIALOG_CONTENT,
    };
  }

  if (canonicalName === "Drawer") {
    return {
      Side: DRAWER_SIDES,
    };
  }

  if (canonicalName === "Popover") {
    return {
      Side: POPOVER_SIDES,
    };
  }

  if (canonicalName === "Menu") {
    return {
      Content: MENU_CONTENT,
    };
  }

  if (canonicalName === "Toast") {
    return {
      Content: TOAST_CONTENT,
    };
  }

  if (canonicalName === "Checkbox") {
    return {
      Checked: CHECKBOX_CHECKED,
      State: CHECKBOX_STATES,
    };
  }

  if (canonicalName === "Radio") {
    return {
      Checked: RADIO_CHECKED,
      State: RADIO_STATES,
    };
  }

  if (canonicalName === "Switch") {
    return {
      Checked: SWITCH_CHECKED,
      State: SWITCH_STATES,
    };
  }

  if (canonicalName === "Input") {
    return {
      State: INPUT_STATES,
      Status: INPUT_STATUSES,
    };
  }

  if (canonicalName === "PasswordInput") {
    return {
      State: INPUT_STATES,
      Status: INPUT_STATUSES,
      Visibility: PASSWORD_INPUT_VISIBILITY,
    };
  }

  if (canonicalName === "FormField") {
    return {
      Content: FORM_FIELD_CONTENT,
      Status: FORM_FIELD_STATUSES,
      Required: FORM_FIELD_REQUIRED,
    };
  }

  if (canonicalName === "NumberInput") {
    return {
      State: INPUT_STATES,
      Status: INPUT_STATUSES,
      Steppers: NUMBER_INPUT_STEPPERS,
    };
  }

  if (canonicalName === "OTPInput") {
    return {
      Length: OTP_INPUT_LENGTHS,
      State: INPUT_STATES,
      Status: INPUT_STATUSES,
    };
  }

  if (canonicalName === "Combobox") {
    return {
      State: COMBOBOX_STATES,
      Status: COMBOBOX_STATUSES,
      Content: COMBOBOX_CONTENT,
    };
  }

  if (canonicalName === "MultiSelect") {
    return {
      State: MULTI_SELECT_STATES,
      Status: MULTI_SELECT_STATUSES,
      Content: MULTI_SELECT_CONTENT,
    };
  }

  if (canonicalName === "Listbox") {
    return {
      Selection: LISTBOX_SELECTION,
      State: LISTBOX_STATES,
    };
  }

  if (canonicalName === "DatePicker") {
    return {
      State: DATE_PICKER_STATES,
      Status: DATE_PICKER_STATUSES,
      Content: DATE_PICKER_CONTENT,
    };
  }

  if (canonicalName === "DateRangePicker") {
    return {
      State: DATE_RANGE_PICKER_STATES,
      Status: DATE_RANGE_PICKER_STATUSES,
      Content: DATE_RANGE_PICKER_CONTENT,
    };
  }

  if (canonicalName === "TimePicker") {
    return {
      State: TIME_PICKER_STATES,
      Status: TIME_PICKER_STATUSES,
      Content: TIME_PICKER_CONTENT,
    };
  }

  if (canonicalName === "FileUpload") {
    return {
      State: FILE_UPLOAD_STATES,
      Status: FILE_UPLOAD_STATUSES,
      Content: FILE_UPLOAD_CONTENT,
    };
  }

  if (canonicalName === "ColorPicker") {
    return {
      State: COLOR_PICKER_STATES,
      Status: COLOR_PICKER_STATUSES,
      Content: COLOR_PICKER_CONTENT,
      Format: COLOR_PICKER_FORMATS,
    };
  }

  if (canonicalName === "Textarea") {
    return {
      State: TEXTAREA_STATES,
      Status: TEXTAREA_STATUSES,
    };
  }

  if (canonicalName === "Search") {
    return {
      State: SEARCH_STATES,
      Status: SEARCH_STATUSES,
    };
  }

  if (canonicalName === "SearchBar") {
    return {
      Variant: SEARCH_BAR_VARIANTS,
      State: SEARCH_BAR_STATES,
    };
  }

  if (canonicalName === "Select") {
    return {
      State: SELECT_STATES,
      Status: SELECT_STATUSES,
    };
  }

  if (canonicalName === "Slider") {
    return {
      State: SLIDER_STATES,
      Status: SLIDER_STATUSES,
      Type: SLIDER_TYPES,
    };
  }

  if (canonicalName === "Rating") {
    return {
      Value: RATING_VALUES,
      State: RATING_STATES,
    };
  }

  if (canonicalName === "Stepper") {
    return {
      Count: STEPPER_COUNTS,
      Current: STEPPER_CURRENT,
    };
  }

  if (canonicalName === "Progress") {
    return {
      Value: PROGRESS_VALUES,
    };
  }

  if (canonicalName === "Spinner") {
    return {
      Size: SPINNER_SIZES,
    };
  }

  if (canonicalName === "Avatar") {
    return {
      Content: AVATAR_CONTENT,
    };
  }

  if (canonicalName === "Alert") {
    return {
      Variant: ALERT_VARIANTS,
    };
  }

  if (canonicalName === "EmptyState") {
    return {
      Content: EMPTY_STATE_CONTENT,
    };
  }

  return null;
}

function auditComponentPropertyConflicts(propertyDefinitions) {
  const groups = {};

  for (const propertyName of Object.keys(propertyDefinitions || {})) {
    const definition = propertyDefinitions[propertyName];
    if (!definition || !definition.type || definition.type === "VARIANT") {
      continue;
    }

    const baseName = propertyName.split("#")[0];
    const key = `${definition.type}:${baseName}`;
    if (!groups[key]) {
      groups[key] = {
        baseName,
        type: definition.type,
        propertyNames: [],
      };
    }
    groups[key].propertyNames.push(propertyName);
  }

  const issues = Object.values(groups)
    .filter((group) => group.propertyNames.length > 1)
    .map((group) => ({
      baseName: group.baseName,
      type: group.type,
      propertyNames: group.propertyNames,
    }));

  return {
    issueCount: issues.length,
    issues,
  };
}

function auditVariantProperties(propertyDefinitions, expectedAxes) {
  const properties = {};
  const issues = [];
  const expectedNames = expectedAxes ? Object.keys(expectedAxes) : [];

  for (const propertyName of Object.keys(propertyDefinitions)) {
    const definition = propertyDefinitions[propertyName];
    if (definition.type !== "VARIANT") continue;

    const baseName = propertyName.split("#")[0];
    const options = Array.isArray(definition.variantOptions)
      ? definition.variantOptions.slice()
      : [];
    const propertyIssues = [];

    if (expectedAxes && !expectedAxes[baseName]) {
      propertyIssues.push("unexpected-axis");
      issues.push({
        propertyName,
        baseName,
        kind: "unexpected-axis",
        expectedAxes: expectedNames,
      });
    } else if (expectedAxes && expectedAxes[baseName] && options.length > 0) {
      const expectedOptions = expectedAxes[baseName];
      const missing = expectedOptions.filter(
        (option) => options.indexOf(option) === -1,
      );
      const unexpected = options.filter(
        (option) => expectedOptions.indexOf(option) === -1,
      );

      if (missing.length > 0 || unexpected.length > 0) {
        propertyIssues.push("option-mismatch");
        issues.push({
          propertyName,
          baseName,
          kind: "option-mismatch",
          missing,
          unexpected,
        });
      }
    }

    properties[propertyName] = {
      baseName,
      options,
      defaultValue:
        typeof definition.defaultValue === "string"
          ? definition.defaultValue
          : null,
      issues: propertyIssues,
    };
  }

  if (expectedAxes) {
    for (const expectedName of expectedNames) {
      const exists = Object.keys(properties).some(
        (propertyName) => properties[propertyName].baseName === expectedName,
      );
      if (!exists) {
        issues.push({
          propertyName: null,
          baseName: expectedName,
          kind: "missing-axis",
        });
      }
    }
  }

  return {
    properties,
    issueCount: issues.length,
    issues,
  };
}

function auditInstanceSwapSlots(componentSet, propertyDefinitions) {
  const slots = {};

  for (const propertyName of Object.keys(propertyDefinitions)) {
    const definition = propertyDefinitions[propertyName];
    if (definition.type !== "INSTANCE_SWAP") continue;

    slots[propertyName] = {
      baseName: propertyName.split("#")[0],
      boundInstances: 0,
      preferredValues: Array.isArray(definition.preferredValues)
        ? definition.preferredValues.length
        : 0,
    };
  }

  function walk(node) {
    if (isGeneratedNestedComponentInstance(node)) return;

    if (
      node.type === "INSTANCE" &&
      node.componentPropertyReferences &&
      node.componentPropertyReferences.mainComponent
    ) {
      const propertyName = node.componentPropertyReferences.mainComponent;
      if (slots[propertyName]) {
        slots[propertyName].boundInstances += 1;
      }
    }

    if (node.children) {
      for (const child of node.children) walk(child);
    }
  }

  walk(componentSet);
  return slots;
}

function auditTextProperties(componentSet, propertyDefinitions) {
  const properties = {};

  for (const propertyName of Object.keys(propertyDefinitions)) {
    const definition = propertyDefinitions[propertyName];
    if (definition.type !== "TEXT") continue;

    properties[propertyName] = {
      baseName: propertyName.split("#")[0],
      boundTextNodes: 0,
    };
  }

  function walk(node) {
    if (isGeneratedNestedComponentInstance(node)) return;

    if (
      node.type === "TEXT" &&
      node.componentPropertyReferences &&
      node.componentPropertyReferences.characters
    ) {
      const propertyName = node.componentPropertyReferences.characters;
      if (properties[propertyName]) {
        properties[propertyName].boundTextNodes += 1;
      }
    }

    if (node.children) {
      for (const child of node.children) walk(child);
    }
  }

  walk(componentSet);
  return properties;
}

function auditComponentSlotContract(componentSet, propertyDefinitions) {
  const componentName = canonicalComponentSetName(componentSet.name);
  const slotNames = requiredSlotPropertyNamesForComponent(componentName);
  const result = {
    slots: {},
    issueCount: 0,
    issues: [],
  };

  if (!slotNames) return result;

  for (const slotName of slotNames) {
    const propertyName = slotPropertyNameByBaseName(
      propertyDefinitions,
      slotName,
    );
    result.slots[slotName] = {
      propertyName,
      nodeCount: 0,
      boundNodes: 0,
    };

    if (!propertyName) {
      result.issues.push({
        kind: "missing-slot-property",
        component: componentName,
        slotName,
      });
    }
  }

  for (const child of componentSet.children || []) {
    if (child.type !== "COMPONENT") continue;

    const expectedSlotNames = requiredSlotNamesForVariant(
      componentName,
      child.name,
    );
    if (!expectedSlotNames) continue;

    for (const slotName of expectedSlotNames) {
      const nodes = descendantNodesNamed(child, slotName).filter(
        isSlotReferenceCandidate,
      );
      const slotRecord = result.slots[slotName];
      const propertyName = slotRecord ? slotRecord.propertyName : null;
      const boundNodes = propertyName
        ? nodes.filter((node) =>
            nodeReferencesComponentProperty(node, propertyName),
          ).length
        : 0;

      if (slotRecord) {
        slotRecord.nodeCount += nodes.length;
        slotRecord.boundNodes += boundNodes;
      }

      if (nodes.length === 0) {
        result.issues.push({
          kind: "missing-slot-node",
          component: componentName,
          variant: child.name,
          slotName,
        });
      } else if (propertyName && boundNodes === 0) {
        result.issues.push({
          kind: "unbound-slot-node",
          component: componentName,
          variant: child.name,
          slotName,
          propertyName,
        });
      }
    }
  }

  for (const staleSlotName of staleSlotNamesForComponent(componentName) || []) {
    const staleNodes = descendantNodesNamed(componentSet, staleSlotName);
    if (staleNodes.length > 0) {
      result.issues.push({
        kind: "stale-slot-node",
        component: componentName,
        slotName: staleSlotName,
        nodeCount: staleNodes.length,
      });
    }
  }

  const stalePropertyNames =
    stalePropertyNamesForComponent(componentName) || [];
  for (const stalePropertyName of stalePropertyNames) {
    const propertyName = componentPropertyDefinitionNameByBaseName(
      propertyDefinitions,
      stalePropertyName,
    );
    if (propertyName) {
      result.issues.push({
        kind: "stale-property",
        component: componentName,
        propertyName,
        baseName: stalePropertyName,
      });
    }
  }

  result.issueCount = result.issues.length;
  return result;
}

function requiredSlotPropertyNamesForComponent(componentName) {
  if (componentName === "Navbar") return NAVBAR_SLOT_NAMES;
  if (componentName === "Sidebar") return SIDEBAR_SLOT_NAMES;
  return null;
}

function requiredSlotNamesForVariant(componentName, variantName) {
  if (componentName === "Navbar") {
    const props = parseNavbarVariantName(variantName);
    if (!props) return null;
    return requiredNavbarSlotNamesForContent(props.content);
  }

  if (componentName === "Sidebar") {
    const props = parseSidebarVariantName(variantName);
    if (!props) return null;
    return requiredSidebarSlotNamesForContent(props.content);
  }

  return null;
}

function requiredNavbarSlotNamesForContent(content) {
  if (content === "Contextual") {
    return [
      "Logo Slot",
      "Context Slot",
      "Navigation Slot",
      "Primary Action Slot",
      "Utility Slot",
      "Account Slot",
    ];
  }
  if (content === "Actions") {
    return ["Logo Slot", "Navigation Slot", "Actions Slot"];
  }
  return ["Logo Slot", "Navigation Slot"];
}

function requiredSidebarSlotNamesForContent(content) {
  if (content === "Tools") {
    return ["Header Slot", "Navigation Slot", "Tools Slot", "Footer Slot"];
  }
  return ["Header Slot", "Navigation Slot", "Footer Slot"];
}

function staleSlotNamesForComponent(componentName) {
  if (componentName === "Navbar") {
    return ["Site Slot", "Site Selector"];
  }
  if (componentName === "Sidebar") {
    return ["Content Slot"];
  }
  return null;
}

function stalePropertyNamesForComponent(componentName) {
  if (componentName === "Navbar") {
    return [
      "Site Slot",
      "Site Label Text",
      "Site Text",
      "Logo Text",
      "Context Label Text",
      "Context Text",
      "Action Text",
      "Primary Action Text",
      "Item 1 Text",
      "Item 2 Text",
      "Item 3 Text",
    ];
  }
  if (componentName === "Sidebar") {
    return [
      "Content Slot",
      "Title Text",
      "Section Text",
      "Footer Text",
      "Item 1 Text",
      "Item 2 Text",
      "Item 3 Text",
      "Tool Text",
      "Settings Text",
    ];
  }
  return null;
}

function slotPropertyNameByBaseName(propertyDefinitions, slotName) {
  for (const propertyName of Object.keys(propertyDefinitions || {})) {
    const definition = propertyDefinitions[propertyName];
    if (
      definition &&
      definition.type === "SLOT" &&
      propertyName.split("#")[0] === slotName
    ) {
      return propertyName;
    }
  }
  return null;
}

function componentPropertyDefinitionNameByBaseName(
  propertyDefinitions,
  baseName,
) {
  for (const propertyName of Object.keys(propertyDefinitions || {})) {
    if (propertyName.split("#")[0] === baseName) return propertyName;
  }
  return null;
}

function descendantNodesNamed(node, name) {
  const matches = [];

  function walk(current) {
    if (!current || !current.children) return;

    for (const child of current.children) {
      if (child.name === name) matches.push(child);
      if (!isGeneratedNestedComponentInstance(child)) walk(child);
    }
  }

  walk(node);
  return matches;
}

function nodeReferencesComponentProperty(node, propertyName) {
  if (!node || !node.componentPropertyReferences) return false;
  return Object.keys(node.componentPropertyReferences).some(
    (referenceKey) =>
      node.componentPropertyReferences[referenceKey] === propertyName,
  );
}

function formatSlotContractIssueDetails(issues) {
  return issues
    .slice(0, 6)
    .map((issue) => {
      const variant = issue.variant ? `${issue.variant} / ` : "";
      if (issue.kind === "missing-slot-property") {
        return `${issue.component} / ${issue.slotName} is missing a SLOT property`;
      }
      if (issue.kind === "missing-slot-node") {
        return `${issue.component} / ${variant}${issue.slotName} has no slot frame`;
      }
      if (issue.kind === "unbound-slot-node") {
        return `${issue.component} / ${variant}${issue.slotName} is not bound to its SLOT property`;
      }
      if (issue.kind === "stale-slot-node") {
        return `${issue.component} still contains stale ${issue.slotName} node(s)`;
      }
      if (issue.kind === "stale-property") {
        return `${issue.component} still exposes stale ${issue.baseName} property`;
      }
      return `${issue.component} / ${issue.slotName}`;
    })
    .join("; ");
}

function auditFocusVisibleProperty(componentSet, propertyDefinitions) {
  const result = {
    propertyName: null,
    defaultValue: null,
    boundNodes: 0,
    ringNodes: 0,
    geometryIssueCount: 0,
    geometryIssues: [],
  };

  for (const propertyName of Object.keys(propertyDefinitions)) {
    const definition = propertyDefinitions[propertyName];
    const baseName = propertyName.split("#")[0];
    if (
      baseName === FOCUS_VISIBLE_PROPERTY_NAME &&
      definition.type === "BOOLEAN"
    ) {
      result.propertyName = propertyName;
      result.defaultValue =
        typeof definition.defaultValue === "boolean"
          ? definition.defaultValue
          : null;
      break;
    }
  }

  function walk(node, parent) {
    if (isGeneratedNestedComponentInstance(node)) return;

    if (
      result.propertyName &&
      node.name === "Focus Ring" &&
      node.componentPropertyReferences &&
      node.componentPropertyReferences.visible === result.propertyName
    ) {
      result.boundNodes += 1;
    }

    if (
      node.name === "Focus Ring" &&
      node.getSharedPluginData(RUN_NAMESPACE, "kind") === "focus-ring"
    ) {
      result.ringNodes += 1;

      const issues = [];
      const parentUsesAutoLayout =
        parent &&
        parent.layoutMode !== undefined &&
        parent.layoutMode !== "NONE";

      if (parentUsesAutoLayout && node.layoutPositioning !== "ABSOLUTE") {
        issues.push("not absolute");
      }

      if (parent) {
        const expectedOffset = -4;
        const expectedWidth = parent.width + 8;
        const expectedHeight = parent.height + 8;
        const tolerance = 0.5;

        if (Math.abs(node.x - expectedOffset) > tolerance) {
          issues.push(`x=${Math.round(node.x)} expected ${expectedOffset}`);
        }
        if (Math.abs(node.y - expectedOffset) > tolerance) {
          issues.push(`y=${Math.round(node.y)} expected ${expectedOffset}`);
        }
        if (Math.abs(node.width - expectedWidth) > tolerance) {
          issues.push(
            `width=${Math.round(node.width)} expected ${Math.round(expectedWidth)}`,
          );
        }
        if (Math.abs(node.height - expectedHeight) > tolerance) {
          issues.push(
            `height=${Math.round(node.height)} expected ${Math.round(expectedHeight)}`,
          );
        }
      }

      if (issues.length > 0) {
        result.geometryIssues.push({
          node: node.name,
          parent: parent ? parent.name : null,
          issues,
        });
      }
    }

    if (node.children) {
      for (const child of node.children) walk(child, node);
    }
  }

  walk(componentSet, null);
  result.geometryIssueCount = result.geometryIssues.length;
  return result;
}

function auditGeneratedIconSlotIntegrity(componentSet) {
  const result = {
    generatedSlots: 0,
    directInstanceSlots: 0,
    maskWrappedSlots: 0,
    colorOverrideIssues: 0,
    unknownSlotIssues: 0,
  };

  function walk(node) {
    if (isGeneratedNestedComponentInstance(node)) return;

    if (
      node &&
      node.name === "Icon" &&
      node.getSharedPluginData &&
      node.getSharedPluginData(RUN_NAMESPACE, "kind") === "icon-slot-instance"
    ) {
      result.generatedSlots += 1;

      if (node.type === "INSTANCE") {
        result.directInstanceSlots += 1;
        if (!hasTintableIconPaint(node)) {
          result.colorOverrideIssues += 1;
        }
      } else if (node.type === "FRAME") {
        result.maskWrappedSlots += 1;
      } else {
        result.unknownSlotIssues += 1;
      }
    }

    if (node.children) {
      for (const child of node.children) walk(child);
    }
  }

  walk(componentSet);
  return result;
}

function auditTooltipTipIntegrity(componentSet, childComponents) {
  const result = {
    expectedTipNodes:
      componentSet.name === "Tooltip" ? childComponents.length : 0,
    tipNodes: 0,
    absoluteTipNodes: 0,
    triangleTipNodes: 0,
  };

  if (componentSet.name !== "Tooltip") {
    return result;
  }

  function walk(node) {
    if (isGeneratedNestedComponentInstance(node)) return;

    if (
      node &&
      node.name === "Tip" &&
      node.getSharedPluginData &&
      node.getSharedPluginData(RUN_NAMESPACE, "kind") === "tooltip-tip"
    ) {
      result.tipNodes += 1;
      if (node.layoutPositioning === "ABSOLUTE") {
        result.absoluteTipNodes += 1;
      }
      if (node.getSharedPluginData(RUN_NAMESPACE, "shape") === "triangle") {
        result.triangleTipNodes += 1;
      }
    }

    if (node.children) {
      for (const child of node.children) walk(child);
    }
  }

  walk(componentSet);
  return result;
}

function auditCompositionIntegrity(componentSet) {
  const issues = [];

  if (componentSet.name === "Card") {
    auditExpectedNestedInstance(
      componentSet,
      issues,
      "Secondary Action",
      "Button",
    );
    auditExpectedNestedInstance(
      componentSet,
      issues,
      "Primary Action",
      "Button",
    );
  }

  if (componentSet.name === "Dialog") {
    auditDialogAutoLayoutIntegrity(componentSet, issues);
  }

  if (componentSet.name === "Accordion") {
    auditAccordionAutoLayoutIntegrity(componentSet, issues);
  }

  if (componentSet.name === "Drawer") {
    auditExpectedNestedInstance(
      componentSet,
      issues,
      "Secondary Action",
      "Button",
    );
    auditExpectedNestedInstance(
      componentSet,
      issues,
      "Primary Action",
      "Button",
    );
    auditDrawerAutoLayoutIntegrity(componentSet, issues);
  }

  if (componentSet.name === "BottomSheet") {
    auditBottomSheetAutoLayoutIntegrity(componentSet, issues);
  }

  if (["Box", "Container", "Stack"].indexOf(componentSet.name) !== -1) {
    auditContentSlotOverflowIntegrity(componentSet, issues);
  }

  if (componentSet.name === "List") {
    auditListAutoLayoutIntegrity(componentSet, issues);
  }

  if (componentSet.name === "Table") {
    auditTableAutoLayoutIntegrity(componentSet, issues);
  }

  if (componentSet.name === "Pagination") {
    auditPaginationAutoLayoutIntegrity(componentSet, issues);
  }

  if (componentSet.name === "Navbar") {
    auditNavbarAutoLayoutIntegrity(componentSet, issues);
  }

  if (componentSet.name === "Sidebar") {
    auditSidebarAutoLayoutIntegrity(componentSet, issues);
  }

  if (componentSet.name === "ColorPicker") {
    auditExpectedNestedInstance(componentSet, issues, "Hue Slider", "Slider");
    auditExpectedNestedInstance(
      componentSet,
      issues,
      "Opacity Slider",
      "Slider",
    );
    auditExpectedNestedInstance(
      componentSet,
      issues,
      "ColorPicker Palette Select",
      "Select",
    );
  }

  return {
    issueCount: issues.length,
    issues,
  };
}

function auditGeneratedExampleGeometry(page) {
  const issues = [];
  let issueCount = 0;

  if (!page || !page.children) {
    return {
      issueCount,
      issues,
    };
  }

  for (const child of page.children) {
    let kind = "";
    try {
      kind =
        child.getSharedPluginData &&
        child.getSharedPluginData(RUN_NAMESPACE, "kind");
    } catch (_error) {
      kind = "";
    }

    if (kind !== "example-template") continue;

    const result = auditGeneratedGeometryIntegrity(child, {
      context: `Example / ${child.name}`,
      limit: Math.max(0, 24 - issues.length),
    });
    issueCount += result.issueCount;
    for (const issue of result.issues) issues.push(issue);
  }

  return {
    issueCount,
    issues,
  };
}

function auditGeneratedGeometryIntegrity(root, options = {}) {
  const issues = [];
  let issueCount = 0;
  const limit = typeof options.limit === "number" ? options.limit : 24;
  const context = options.context || root.name || "Generated component";

  function pushIssue(issue) {
    issueCount += 1;
    if (issues.length >= limit) return;
    issues.push(issue);
  }

  function walk(node, variantName) {
    if (!node || node.visible === false) return;

    const nextVariantName = node.type === "COMPONENT" ? node.name : variantName;
    auditGeneratedGeometryNode({
      context,
      node,
      pushIssue,
      variant: nextVariantName,
    });

    if (isGeneratedNestedComponentInstance(node)) return;
    if (!node.children) return;

    for (const child of node.children) {
      walk(child, nextVariantName);
    }
  }

  walk(root, root.type === "COMPONENT" ? root.name : null);

  return {
    issueCount,
    issues,
  };
}

function auditGeneratedGeometryNode({ context, node, pushIssue, variant }) {
  if (!shouldAuditGeneratedGeometryNode(node)) return;

  const width = Number(node.width);
  const height = Number(node.height);
  if (!Number.isFinite(width) || !Number.isFinite(height)) return;

  const minimum = generatedGeometryMinimumForNode(node);
  if (!minimum) return;
  if (width >= minimum.width && height >= minimum.height) return;

  pushIssue({
    kind: "collapsed-generated-geometry",
    context,
    variant,
    node: node.name,
    nodeType: node.type,
    nodeId: node.id,
    urlNodeId: nodeIdForUrl(node.id),
    expected: `>= ${minimum.width}x${minimum.height}`,
    actual: `${Math.round(width)}x${Math.round(height)}`,
  });
}

function shouldAuditGeneratedGeometryNode(node) {
  if (!node || node.visible === false) return false;
  if (isIntentionalThinGeometryNode(node)) return false;

  return (
    node.type === "COMPONENT_SET" ||
    node.type === "COMPONENT" ||
    node.type === "INSTANCE" ||
    node.type === "FRAME" ||
    node.type === "TEXT"
  );
}

function generatedGeometryMinimumForNode(node) {
  if (node.type === "TEXT") {
    if (!node.characters || String(node.characters).length === 0) return null;
    return {
      width: 4,
      height: 4,
    };
  }

  if (
    node.type === "COMPONENT_SET" ||
    node.type === "COMPONENT" ||
    node.type === "INSTANCE" ||
    node.type === "FRAME"
  ) {
    return {
      width: 2,
      height: 2,
    };
  }

  return null;
}

function isIntentionalThinGeometryNode(node) {
  const name = node && node.name ? node.name : "";
  if (/separator|divider|hairline|rule|route segment/i.test(name)) return true;
  if (nodeBelongsToGeneratedComponentSet(node, "Separator")) return true;

  const sourceComponentSet = nodeSourceComponentSet(node);
  if (sourceComponentSet === "Separator") return true;

  return false;
}

function nodeBelongsToGeneratedComponentSet(node, componentSetName) {
  if (!node || !componentSetName) return false;

  try {
    if (
      node.getSharedPluginData &&
      node.getSharedPluginData(RUN_NAMESPACE, "component") === componentSetName
    ) {
      return true;
    }
  } catch (_error) {
    // Shared plugin data is not available on every node in older runtimes.
  }

  let parent = node.parent || null;
  while (parent) {
    if (
      parent.type === "COMPONENT_SET" &&
      canonicalComponentSetName(parent.name) === componentSetName
    ) {
      return true;
    }
    parent = parent.parent || null;
  }

  return false;
}

function formatGeometryIssueDetails(issues) {
  if (!issues || issues.length === 0) return "";

  const visible = issues.slice(0, 4).map((issue) => {
    const location = [
      issue.context,
      issue.variant,
      issue.node,
      issue.urlNodeId ? `node-id=${issue.urlNodeId}` : "",
    ]
      .filter(Boolean)
      .join(" / ");
    return `${location} ${issue.actual} expected ${issue.expected}`;
  });

  const remaining = issues.length - visible.length;
  if (remaining > 0) visible.push(`${remaining} more`);
  return visible.join("; ");
}

function auditListAutoLayoutIntegrity(componentSet, issues) {
  const components = componentSet.children || [];

  for (const component of components) {
    if (component.type !== "COMPONENT") continue;
    const props = parseListVariantName(component.name);
    if (!props) continue;

    const items = directChildrenNamedByPattern(component, /^List Item \d+$/);
    if (items.length !== 3) {
      issues.push({
        kind: "list-item-count",
        variant: component.name,
        nodeId: component.id,
        urlNodeId: nodeIdForUrl(component.id),
        expected: 3,
        actual: items.length,
      });
    }

    for (const item of items) {
      auditCompositeRowGeometry({
        component,
        row: item,
        issues,
        variant: component.name,
        kindPrefix: "list-item",
        minHeight: 32,
      });

      if (directChildNamed(item, "List Item Separator")) {
        issues.push({
          kind: "list-legacy-separator-node",
          variant: component.name,
          node: item.name,
          nodeId: item.id,
          urlNodeId: nodeIdForUrl(item.id),
          expected:
            "Row dividers should be bottom strokes, not layout children.",
        });
      }
    }
  }
}

function auditTableAutoLayoutIntegrity(componentSet, issues) {
  const components = componentSet.children || [];

  for (const component of components) {
    if (component.type !== "COMPONENT") continue;
    const props = parseTableVariantName(component.name);
    if (!props) continue;

    const rows = directChildrenNamedByPattern(
      component,
      /^(Table Header|Table Row \d+)$/,
    );
    if (rows.length !== 4) {
      issues.push({
        kind: "table-row-count",
        variant: component.name,
        nodeId: component.id,
        urlNodeId: nodeIdForUrl(component.id),
        expected: 4,
        actual: rows.length,
      });
    }

    for (const row of rows) {
      auditCompositeRowGeometry({
        component,
        row,
        issues,
        variant: component.name,
        kindPrefix: "table-row",
        minHeight: 32,
      });

      if (directChildNamed(row, "Table Row Separator")) {
        issues.push({
          kind: "table-legacy-separator-node",
          variant: component.name,
          node: row.name,
          nodeId: row.id,
          urlNodeId: nodeIdForUrl(row.id),
          expected:
            "Row dividers should be bottom strokes, not layout children.",
        });
      }
    }
  }
}

function auditPaginationAutoLayoutIntegrity(componentSet, issues) {
  const expectedCounts = {
    Basic: 5,
    Ellipsis: 7,
    Compact: 3,
  };
  const components = componentSet.children || [];

  for (const component of components) {
    if (component.type !== "COMPONENT") continue;
    const props = parsePaginationVariantName(component.name);
    if (!props) continue;

    const controls = directChildrenNamedByPattern(component, /^Pagination /);
    const expectedCount = expectedCounts[props.content];
    if (controls.length !== expectedCount) {
      issues.push({
        kind: "pagination-control-count",
        variant: component.name,
        nodeId: component.id,
        urlNodeId: nodeIdForUrl(component.id),
        expected: expectedCount,
        actual: controls.length,
      });
    }

    for (const control of controls) {
      auditPaginationControlGeometry({
        control,
        issues,
        variant: component.name,
        minHeight: 44,
      });
      auditVisualOverflow(
        component.name,
        control,
        issues,
        "pagination-control-visual-overflow",
      );
    }
  }
}

function auditPaginationControlGeometry(options) {
  const width = Number(options.control.width || 0);
  const height = Number(options.control.height || 0);

  if (options.control.type !== "FRAME") {
    options.issues.push({
      kind: "pagination-control-frame-type",
      variant: options.variant,
      node: options.control.name,
      nodeId: options.control.id,
      urlNodeId: nodeIdForUrl(options.control.id),
      expected: "FRAME",
      actual: options.control.type,
    });
  }

  if (options.control.layoutMode !== "HORIZONTAL") {
    options.issues.push({
      kind: "pagination-control-layout-mode",
      variant: options.variant,
      node: options.control.name,
      nodeId: options.control.id,
      urlNodeId: nodeIdForUrl(options.control.id),
      expected: "HORIZONTAL",
      actual: options.control.layoutMode || null,
    });
  }

  if (width <= 0) {
    options.issues.push({
      kind: "pagination-control-width",
      variant: options.variant,
      node: options.control.name,
      nodeId: options.control.id,
      urlNodeId: nodeIdForUrl(options.control.id),
      expected: "> 0",
      actual: Math.round(width),
    });
  }

  if (height < options.minHeight) {
    options.issues.push({
      kind: "pagination-control-height",
      variant: options.variant,
      node: options.control.name,
      nodeId: options.control.id,
      urlNodeId: nodeIdForUrl(options.control.id),
      expected: `>= ${options.minHeight}`,
      actual: Math.round(height),
    });
  }
}

function auditAccordionAutoLayoutIntegrity(componentSet, issues) {
  const components = componentSet.children || [];

  for (const component of components) {
    if (component.type !== "COMPONENT") continue;

    const props = parseAccordionVariantName(component.name);
    if (!props) continue;

    const trigger = directChildNamed(component, "Accordion Trigger");
    if (!trigger || trigger.type !== "FRAME") {
      issues.push({
        kind: "accordion-trigger-missing",
        variant: component.name,
        nodeId: component.id,
        urlNodeId: nodeIdForUrl(component.id),
      });
    } else {
      auditHorizontalFillSizing({
        node: trigger,
        issues,
        kind: "accordion-trigger-horizontal-sizing",
        variant: component.name,
      });
      auditAutoLayoutSizing({
        node: trigger,
        issues,
        kind: "accordion-trigger-vertical-sizing",
        field: "layoutSizingVertical",
        expected: "FIXED",
        actual: trigger.layoutSizingVertical,
        variant: component.name,
      });
      auditVisualOverflow(
        component.name,
        trigger,
        issues,
        "accordion-trigger-visual-overflow",
      );
    }

    const content = directChildNamed(component, "Accordion Content");
    if (props.state === "Open") {
      if (!content || content.type !== "FRAME") {
        issues.push({
          kind: "accordion-content-missing",
          variant: component.name,
          nodeId: component.id,
          urlNodeId: nodeIdForUrl(component.id),
        });
      } else {
        auditHorizontalFillSizing({
          node: content,
          issues,
          kind: "accordion-content-horizontal-sizing",
          variant: component.name,
        });
        auditAutoLayoutSizing({
          node: content,
          issues,
          kind: "accordion-content-vertical-sizing",
          field: "layoutSizingVertical",
          expected: "HUG",
          actual: content.layoutSizingVertical,
          variant: component.name,
        });
        auditVisualOverflow(
          component.name,
          content,
          issues,
          "accordion-content-visual-overflow",
        );
      }
    } else if (content) {
      issues.push({
        kind: "accordion-closed-content-visible",
        variant: component.name,
        node: content.name,
        nodeType: content.type,
        nodeId: content.id,
        urlNodeId: nodeIdForUrl(content.id),
      });
    }

    const divider = directChildNamed(component, "Accordion Divider");
    if (divider) {
      issues.push({
        kind: "accordion-legacy-divider-node",
        variant: component.name,
        node: divider.name,
        nodeType: divider.type,
        nodeId: divider.id,
        urlNodeId: nodeIdForUrl(divider.id),
        expected:
          "Accordion should use its own bottom stroke instead of an internal divider node.",
      });
    }

    if (!component.strokes || component.strokes.length === 0) {
      issues.push({
        kind: "accordion-bottom-stroke-missing",
        variant: component.name,
        node: component.name,
        nodeType: component.type,
        nodeId: component.id,
        urlNodeId: nodeIdForUrl(component.id),
      });
    }

    if (
      typeof component.strokeBottomWeight === "number" &&
      component.strokeBottomWeight !== 1
    ) {
      issues.push({
        kind: "accordion-bottom-stroke-weight",
        variant: component.name,
        node: component.name,
        nodeType: component.type,
        nodeId: component.id,
        urlNodeId: nodeIdForUrl(component.id),
        field: "strokeBottomWeight",
        expected: 1,
        actual: component.strokeBottomWeight,
      });
    }
  }
}

function auditCompositeRowGeometry(options) {
  const componentWidth = Number(options.component.width || 0);
  const rowWidth = Number(options.row.width || 0);
  const rowX = Number(options.row.x || 0);

  if (options.row.type !== "FRAME") {
    options.issues.push({
      kind: `${options.kindPrefix}-frame-type`,
      variant: options.variant,
      node: options.row.name,
      nodeId: options.row.id,
      urlNodeId: nodeIdForUrl(options.row.id),
      expected: "FRAME",
      actual: options.row.type,
    });
  }

  if (options.row.layoutMode !== "HORIZONTAL") {
    options.issues.push({
      kind: `${options.kindPrefix}-layout-mode`,
      variant: options.variant,
      node: options.row.name,
      nodeId: options.row.id,
      urlNodeId: nodeIdForUrl(options.row.id),
      expected: "HORIZONTAL",
      actual: options.row.layoutMode || null,
    });
  }

  if (Math.abs(rowX) > 1) {
    options.issues.push({
      kind: `${options.kindPrefix}-x-position`,
      variant: options.variant,
      node: options.row.name,
      nodeId: options.row.id,
      urlNodeId: nodeIdForUrl(options.row.id),
      expected: 0,
      actual: Math.round(rowX),
    });
  }

  if (componentWidth > 0 && Math.abs(rowWidth - componentWidth) > 1) {
    options.issues.push({
      kind: `${options.kindPrefix}-width`,
      variant: options.variant,
      node: options.row.name,
      nodeId: options.row.id,
      urlNodeId: nodeIdForUrl(options.row.id),
      expected: Math.round(componentWidth),
      actual: Math.round(rowWidth),
    });
  }

  if (Number(options.row.height || 0) < options.minHeight) {
    options.issues.push({
      kind: `${options.kindPrefix}-height`,
      variant: options.variant,
      node: options.row.name,
      nodeId: options.row.id,
      urlNodeId: nodeIdForUrl(options.row.id),
      expected: `>= ${options.minHeight}`,
      actual: Math.round(Number(options.row.height || 0)),
    });
  }
}

function directChildrenNamedByPattern(parent, pattern) {
  const matches = [];
  if (!parent || !parent.children) return matches;

  for (const child of parent.children) {
    if (pattern.test(child.name || "")) {
      matches.push(child);
    }
  }

  return matches;
}

function nodeHasDescendantNamed(node, name) {
  if (!node || !node.children) return false;

  for (const child of node.children) {
    if (child.name === name) return true;
    if (nodeHasDescendantNamed(child, name)) return true;
  }

  return false;
}

function componentSetHasSlotPropertyOrNode(
  componentSet,
  propertyDefinitions,
  slotName,
) {
  const hasSlotProperty = !!slotPropertyNameByBaseName(
    propertyDefinitions,
    slotName,
  );

  return hasSlotProperty || nodeHasDescendantNamed(componentSet, slotName);
}

function componentSetHasSlotPropertyOrNodeMatching(
  componentSet,
  propertyDefinitions,
  predicate,
) {
  const hasSlotProperty = Object.keys(propertyDefinitions || {}).some(
    (propertyName) => {
      const definition = propertyDefinitions[propertyName];
      return (
        definition &&
        definition.type === "SLOT" &&
        predicate(propertyName.split("#")[0])
      );
    },
  );

  if (hasSlotProperty) return true;

  function walk(node) {
    if (!node) return false;
    if (predicate(node.name || "")) return true;
    if (!node.children) return false;
    for (const child of node.children) {
      if (walk(child)) return true;
    }
    return false;
  }

  return walk(componentSet);
}

function gridHasCellSlots(componentSet, propertyDefinitions) {
  return componentSetHasSlotPropertyOrNodeMatching(
    componentSet,
    propertyDefinitions,
    isGridCellSlotName,
  );
}

function gridCellSlotIntegrityIssues(componentSet) {
  const issues = [];
  if (!componentSet || !componentSet.children) return issues;

  for (const component of componentSet.children) {
    if (component.type !== "COMPONENT") continue;
    const props = parseGridVariantName(component.name);
    if (!props) continue;

    const columnCount = Number(props.columns);
    const expectedNames = gridCellSlotNames(columnCount, GRID_PREVIEW_ROWS);
    const foundNames = new Set();
    let staleContentSlot = false;

    function walk(node) {
      if (!node) return;
      if (node.name === "Content Slot") {
        staleContentSlot = true;
      } else if (isGridCellSlotName(node.name)) {
        foundNames.add(node.name);
      }
      if (!node.children) return;
      for (const child of node.children) {
        walk(child);
      }
    }

    walk(component);

    for (let rowIndex = 0; rowIndex < GRID_PREVIEW_ROWS; rowIndex += 1) {
      const row = directChildNamed(component, `Grid Row ${rowIndex + 1}`);
      if (!row) {
        issues.push(`${component.name}: missing Grid Row ${rowIndex + 1}.`);
        continue;
      }
      if (row.layoutMode && row.layoutMode !== "HORIZONTAL") {
        issues.push(
          `${component.name}: Grid Row ${rowIndex + 1} should use horizontal auto layout.`,
        );
      }

      for (let columnIndex = 0; columnIndex < columnCount; columnIndex += 1) {
        const slotName = `Cell ${rowIndex * columnCount + columnIndex + 1} Slot`;
        if (!directChildNamed(row, slotName)) {
          issues.push(
            `${component.name}: ${slotName} is not inside Grid Row ${rowIndex + 1}.`,
          );
        }
      }
    }

    const missing = expectedNames.filter((name) => !foundNames.has(name));
    const unexpected = Array.from(foundNames).filter(
      (name) => expectedNames.indexOf(name) === -1,
    );

    if (missing.length > 0) {
      issues.push(`${component.name}: missing ${missing.join(", ")}.`);
    }
    if (unexpected.length > 0) {
      issues.push(`${component.name}: unexpected ${unexpected.join(", ")}.`);
    }
    if (staleContentSlot) {
      issues.push(
        `${component.name}: stale Content Slot remains; Grid should use per-cell slots.`,
      );
    }
  }

  return issues;
}

function auditContentSlotOverflowIntegrity(componentSet, issues) {
  function walk(node, variantName) {
    const nextVariantName =
      node && node.type === "COMPONENT" ? node.name : variantName;

    if (
      node &&
      (node.name === "Content Slot" || isGridCellSlotName(node.name))
    ) {
      auditSlotContentOverflow({
        issues,
        kind: "content-slot-overflow",
        slot: node,
        variant: nextVariantName || componentSet.name,
      });
    }

    if (!node || !node.children) return;
    for (const child of node.children) {
      walk(child, nextVariantName);
    }
  }

  walk(componentSet, componentSet.name);
}

function auditNavbarAutoLayoutIntegrity(componentSet, issues) {
  for (const component of componentSet.children || []) {
    if (component.type !== "COMPONENT") continue;
    const props = parseNavbarVariantName(component.name);
    if (!props || props.content !== "Contextual") continue;

    if (Number(component.width || 0) < NAVBAR_DEFAULT_WIDTH) {
      issues.push({
        kind: "navbar-contextual-width",
        variant: component.name,
        node: component.name,
        nodeType: component.type,
        nodeId: component.id,
        urlNodeId: nodeIdForUrl(component.id),
        expected: `>= ${NAVBAR_DEFAULT_WIDTH}`,
        actual: Math.round(Number(component.width || 0)),
      });
    }

    const contextSlot = directChildNamed(component, "Context Slot");
    if (!contextSlot) {
      issues.push({
        kind: "navbar-context-slot-missing",
        variant: component.name,
        nodeId: component.id,
        urlNodeId: nodeIdForUrl(component.id),
      });
      continue;
    }

    auditAutoLayoutSizing({
      node: contextSlot,
      issues,
      kind: "navbar-context-slot-horizontal-sizing",
      field: "layoutSizingHorizontal",
      expected: "FIXED",
      actual: contextSlot.layoutSizingHorizontal,
      variant: component.name,
    });

    if (Number(contextSlot.width || 0) < NAVBAR_CONTEXT_SLOT_WIDTH) {
      issues.push({
        kind: "navbar-context-slot-width",
        variant: component.name,
        node: contextSlot.name,
        nodeType: contextSlot.type,
        nodeId: contextSlot.id,
        urlNodeId: nodeIdForUrl(contextSlot.id),
        expected: `>= ${NAVBAR_CONTEXT_SLOT_WIDTH}`,
        actual: Math.round(Number(contextSlot.width || 0)),
      });
    }

    const selector = directChildNamed(contextSlot, "Context Selector");
    if (!selector) {
      issues.push({
        kind: "navbar-context-selector-missing",
        variant: component.name,
        node: contextSlot.name,
        nodeId: contextSlot.id,
        urlNodeId: nodeIdForUrl(contextSlot.id),
      });
      continue;
    }

    if (Number(selector.width || 0) < NAVBAR_CONTEXT_SLOT_WIDTH) {
      issues.push({
        kind: "navbar-context-selector-width",
        variant: component.name,
        node: selector.name,
        nodeType: selector.type,
        nodeId: selector.id,
        urlNodeId: nodeIdForUrl(selector.id),
        expected: `>= ${NAVBAR_CONTEXT_SLOT_WIDTH}`,
        actual: Math.round(Number(selector.width || 0)),
      });
    }

    const contextText = directChildNamed(selector, "Context Text");
    if (!contextText) {
      issues.push({
        kind: "navbar-context-text-missing",
        variant: component.name,
        node: selector.name,
        nodeId: selector.id,
        urlNodeId: nodeIdForUrl(selector.id),
      });
      continue;
    }

    if (Number(contextText.width || 0) < NAVBAR_CONTEXT_TEXT_WIDTH) {
      issues.push({
        kind: "navbar-context-text-width",
        variant: component.name,
        node: contextText.name,
        nodeType: contextText.type,
        nodeId: contextText.id,
        urlNodeId: nodeIdForUrl(contextText.id),
        expected: `>= ${NAVBAR_CONTEXT_TEXT_WIDTH}`,
        actual: Math.round(Number(contextText.width || 0)),
      });
    }

    auditVisualOverflow(
      component.name,
      contextSlot,
      issues,
      "navbar-context-slot-visual-overflow",
      { horizontal: true, vertical: true },
    );
  }
}

function auditSidebarAutoLayoutIntegrity(componentSet, issues) {
  for (const component of componentSet.children || []) {
    if (component.type !== "COMPONENT") continue;
    const props = parseSidebarVariantName(component.name);
    if (!props) continue;

    const isRail = props.content === "Rail";
    const expectedComponentWidth = isRail
      ? SIDEBAR_RAIL_WIDTH
      : SIDEBAR_DEFAULT_WIDTH;
    const expectedSlotWidth = isRail
      ? SIDEBAR_RAIL_SLOT_WIDTH
      : SIDEBAR_SLOT_WIDTH;

    auditMinimumNodeDimension({
      node: component,
      issues,
      kind: "sidebar-component-width",
      field: "width",
      expected: expectedComponentWidth,
      actual: component.width,
      variant: component.name,
    });
    auditMinimumNodeDimension({
      node: component,
      issues,
      kind: "sidebar-component-height",
      field: "height",
      expected: SIDEBAR_DEFAULT_HEIGHT,
      actual: component.height,
      variant: component.name,
    });

    auditSidebarSlotGeometry({
      component,
      issues,
      slotName: "Header Slot",
      expectedWidth: expectedSlotWidth,
      expectedHorizontalSizing: "FILL",
    });
    auditSidebarSlotGeometry({
      component,
      issues,
      slotName: "Navigation Slot",
      expectedWidth: expectedSlotWidth,
      expectedHorizontalSizing: "FILL",
      expectedVerticalSizing: "FILL",
      expectedLayoutGrow: 1,
    });

    if (props.content === "Tools") {
      auditSidebarSlotGeometry({
        component,
        issues,
        slotName: "Tools Slot",
        expectedWidth: expectedSlotWidth,
        expectedHorizontalSizing: "FILL",
      });
    }

    auditSidebarSlotGeometry({
      component,
      issues,
      slotName: "Footer Slot",
      expectedWidth: expectedSlotWidth,
      expectedHorizontalSizing: "FILL",
    });
  }
}

function auditSidebarSlotGeometry({
  component,
  expectedHorizontalSizing,
  expectedLayoutGrow,
  expectedVerticalSizing,
  expectedWidth,
  issues,
  slotName,
}) {
  const slot = directChildNamed(component, slotName);
  const slug = slugifyIssueName(slotName);
  if (!slot) {
    issues.push({
      kind: `sidebar-${slug}-missing`,
      variant: component.name,
      nodeId: component.id,
      urlNodeId: nodeIdForUrl(component.id),
    });
    return;
  }

  if (expectedHorizontalSizing === "FILL") {
    auditHorizontalFillSizing({
      node: slot,
      issues,
      kind: `sidebar-${slug}-horizontal-sizing`,
      variant: component.name,
    });
  } else if (expectedHorizontalSizing) {
    auditAutoLayoutSizing({
      node: slot,
      issues,
      kind: `sidebar-${slug}-horizontal-sizing`,
      field: "layoutSizingHorizontal",
      expected: expectedHorizontalSizing,
      actual: slot.layoutSizingHorizontal,
      variant: component.name,
    });
  }

  if (expectedVerticalSizing) {
    auditAutoLayoutSizing({
      node: slot,
      issues,
      kind: `sidebar-${slug}-vertical-sizing`,
      field: "layoutSizingVertical",
      expected: expectedVerticalSizing,
      actual: slot.layoutSizingVertical,
      variant: component.name,
    });
  }

  if (
    typeof expectedLayoutGrow === "number" &&
    typeof slot.layoutGrow === "number" &&
    slot.layoutGrow !== expectedLayoutGrow
  ) {
    issues.push({
      kind: `sidebar-${slug}-layout-grow`,
      variant: component.name,
      node: slot.name,
      nodeType: slot.type,
      nodeId: slot.id,
      urlNodeId: nodeIdForUrl(slot.id),
      field: "layoutGrow",
      expected: expectedLayoutGrow,
      actual: slot.layoutGrow,
    });
  }

  auditMinimumNodeDimension({
    node: slot,
    issues,
    kind: `sidebar-${slug}-width`,
    field: "width",
    expected: expectedWidth,
    actual: slot.width,
    variant: component.name,
  });
  auditVisualOverflow(
    component.name,
    slot,
    issues,
    `sidebar-${slug}-overflow`,
    {
      horizontal: true,
      ignoreNestedInstanceOuterBounds: true,
      vertical: true,
    },
  );
}

function auditMinimumNodeDimension({
  actual,
  expected,
  field,
  issues,
  kind,
  node,
  variant,
}) {
  if (Number(actual || 0) >= expected) return;

  issues.push({
    kind,
    variant,
    node: node.name,
    nodeType: node.type,
    nodeId: node.id,
    urlNodeId: nodeIdForUrl(node.id),
    field,
    expected: `>= ${expected}`,
    actual: Math.round(Number(actual || 0)),
  });
}

function slugifyIssueName(value) {
  return String(value || "")
    .trim()
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/^-|-$/g, "");
}

function auditSlotContentOverflow({ issues, kind, slot, variant }) {
  if (!slot || !slot.children) return;

  const slotBounds = slot.absoluteBoundingBox;
  if (!slotBounds) return;

  let childBounds = null;
  for (const child of slot.children) {
    if (child.visible === false) continue;
    if (
      child.getSharedPluginData &&
      child.getSharedPluginData(RUN_NAMESPACE, "kind") === "focus-ring"
    ) {
      continue;
    }

    const bounds = child.absoluteBoundingBox;
    if (!bounds) continue;

    const next = {
      x1: bounds.x,
      y1: bounds.y,
      x2: bounds.x + bounds.width,
      y2: bounds.y + bounds.height,
    };

    if (!childBounds) {
      childBounds = next;
    } else {
      childBounds.x1 = Math.min(childBounds.x1, next.x1);
      childBounds.y1 = Math.min(childBounds.y1, next.y1);
      childBounds.x2 = Math.max(childBounds.x2, next.x2);
      childBounds.y2 = Math.max(childBounds.y2, next.y2);
    }
  }

  if (!childBounds) return;

  const tolerance = 1;
  const slotX2 = slotBounds.x + slotBounds.width;
  const slotY2 = slotBounds.y + slotBounds.height;
  const overflows =
    childBounds.x1 < slotBounds.x - tolerance ||
    childBounds.y1 < slotBounds.y - tolerance ||
    childBounds.x2 > slotX2 + tolerance ||
    childBounds.y2 > slotY2 + tolerance;

  if (!overflows) return;

  issues.push({
    kind,
    variant,
    node: slot.name,
    nodeType: slot.type,
    nodeId: slot.id,
    urlNodeId: nodeIdForUrl(slot.id),
    expected: "inserted slot content to fit inside the slot bounds",
    actual: {
      contentHeight: Math.round(childBounds.y2 - childBounds.y1),
      contentWidth: Math.round(childBounds.x2 - childBounds.x1),
      slotHeight: Math.round(slotBounds.height),
      slotWidth: Math.round(slotBounds.width),
    },
  });
}

function auditDrawerAutoLayoutIntegrity(componentSet, issues) {
  const components = componentSet.children || [];

  for (const component of components) {
    if (component.type !== "COMPONENT") continue;

    const props = parseDrawerVariantName(component.name);
    if (!props) continue;

    const body = directChildNamed(component, "Drawer Body");
    if (!body || body.type !== "FRAME") {
      issues.push({
        kind: "drawer-body-missing",
        variant: component.name,
        nodeId: component.id,
        urlNodeId: nodeIdForUrl(component.id),
      });
      continue;
    }

    auditAutoLayoutSizing({
      node: body,
      issues,
      kind: "drawer-body-horizontal-sizing",
      field: "layoutSizingHorizontal",
      expected: "FILL",
      actual: body.layoutSizingHorizontal,
      variant: component.name,
    });
    auditAutoLayoutSizing({
      node: body,
      issues,
      kind: "drawer-body-vertical-sizing",
      field: "layoutSizingVertical",
      expected: "FILL",
      actual: body.layoutSizingVertical,
      variant: component.name,
    });

    if (typeof body.layoutGrow === "number" && body.layoutGrow !== 1) {
      issues.push({
        kind: "drawer-body-primary-axis-fill",
        variant: component.name,
        node: body.name,
        nodeType: body.type,
        nodeId: body.id,
        urlNodeId: nodeIdForUrl(body.id),
        field: "layoutGrow",
        expected: 1,
        actual: body.layoutGrow,
      });
    }

    const contentSlot = directChildNamed(body, "Content Slot");
    if (!contentSlot) {
      issues.push({
        kind: "drawer-content-slot-missing",
        variant: component.name,
        node: body.name,
        nodeId: body.id,
        urlNodeId: nodeIdForUrl(body.id),
        expected: "Drawer Body should contain a fill-height Content Slot.",
      });
    } else {
      auditAutoLayoutSizing({
        node: contentSlot,
        issues,
        kind: "drawer-content-slot-horizontal-sizing",
        field: "layoutSizingHorizontal",
        expected: "FILL",
        actual: contentSlot.layoutSizingHorizontal,
        variant: component.name,
      });
      auditAutoLayoutSizing({
        node: contentSlot,
        issues,
        kind: "drawer-content-slot-vertical-sizing",
        field: "layoutSizingVertical",
        expected: "FILL",
        actual: contentSlot.layoutSizingVertical,
        variant: component.name,
      });

      if (
        typeof contentSlot.layoutGrow === "number" &&
        contentSlot.layoutGrow !== 1
      ) {
        issues.push({
          kind: "drawer-content-slot-primary-axis-fill",
          variant: component.name,
          node: contentSlot.name,
          nodeType: contentSlot.type,
          nodeId: contentSlot.id,
          urlNodeId: nodeIdForUrl(contentSlot.id),
          field: "layoutGrow",
          expected: 1,
          actual: contentSlot.layoutGrow,
        });
      }

      auditSlotContentOverflow({
        issues,
        kind: "drawer-content-slot-overflow",
        slot: contentSlot,
        variant: component.name,
      });
    }

    auditDrawerFooterSizing(component, issues);
  }
}

function auditDrawerFooterSizing(component, issues) {
  const footer = directChildNamed(component, "Drawer Footer");
  if (!footer || footer.type !== "FRAME") {
    issues.push({
      kind: "drawer-footer-missing",
      variant: component.name,
      nodeId: component.id,
      urlNodeId: nodeIdForUrl(component.id),
    });
    return;
  }

  auditAutoLayoutSizing({
    node: footer,
    issues,
    kind: "drawer-footer-horizontal-sizing",
    field: "layoutSizingHorizontal",
    expected: "FILL",
    actual: footer.layoutSizingHorizontal,
    variant: component.name,
  });
  auditAutoLayoutSizing({
    node: footer,
    issues,
    kind: "drawer-footer-vertical-sizing",
    field: "layoutSizingVertical",
    expected: "FIXED",
    actual: footer.layoutSizingVertical,
    variant: component.name,
  });

  if (typeof footer.layoutGrow === "number" && footer.layoutGrow !== 0) {
    issues.push({
      kind: "drawer-footer-primary-axis-fill",
      variant: component.name,
      node: footer.name,
      nodeType: footer.type,
      nodeId: footer.id,
      urlNodeId: nodeIdForUrl(footer.id),
      field: "layoutGrow",
      expected: 0,
      actual: footer.layoutGrow,
    });
  }
}

function auditBottomSheetAutoLayoutIntegrity(componentSet, issues) {
  const components = componentSet.children || [];

  for (const component of components) {
    if (component.type !== "COMPONENT") continue;

    const props = parseBottomSheetVariantName(component.name);
    if (!props) continue;

    if (component.clipsContent === true) {
      issues.push({
        kind: "bottom-sheet-clips-content",
        variant: component.name,
        node: component.name,
        nodeType: component.type,
        nodeId: component.id,
        urlNodeId: nodeIdForUrl(component.id),
        expected: false,
        actual: true,
      });
    }

    const body = directChildNamed(component, "BottomSheet Body");
    if (!body || body.type !== "FRAME") {
      issues.push({
        kind: "bottom-sheet-body-missing",
        variant: component.name,
        nodeId: component.id,
        urlNodeId: nodeIdForUrl(component.id),
      });
      continue;
    }

    auditHorizontalFillSizing({
      node: body,
      issues,
      kind: "bottom-sheet-body-horizontal-sizing",
      variant: component.name,
    });
    auditAutoLayoutSizing({
      node: body,
      issues,
      kind: "bottom-sheet-body-vertical-sizing",
      field: "layoutSizingVertical",
      expected: "HUG",
      actual: body.layoutSizingVertical,
      variant: component.name,
    });

    const contentSlot = directChildNamed(body, "Content Slot");
    if (!contentSlot) {
      issues.push({
        kind: "bottom-sheet-content-slot-missing",
        variant: component.name,
        node: body.name,
        nodeId: body.id,
        urlNodeId: nodeIdForUrl(body.id),
        expected: "BottomSheet Body should contain a shared Content Slot.",
      });
    } else {
      auditHorizontalFillSizing({
        node: contentSlot,
        issues,
        kind: "bottom-sheet-content-slot-horizontal-sizing",
        variant: component.name,
      });
      auditAutoLayoutSizing({
        node: contentSlot,
        issues,
        kind: "bottom-sheet-content-slot-vertical-sizing",
        field: "layoutSizingVertical",
        expected: "HUG",
        actual: contentSlot.layoutSizingVertical,
        variant: component.name,
      });
      auditSlotContentOverflow({
        issues,
        kind: "bottom-sheet-content-slot-overflow",
        slot: contentSlot,
        variant: component.name,
      });
    }

    if (props.content === "Form") {
      for (const formInput of BOTTOM_SHEET_FORM_INPUTS) {
        auditBottomSheetFormInputSizing(
          component,
          body,
          formInput.name,
          issues,
        );
      }
    }

    if (props.content === "Footer") {
      auditBottomSheetFooterSizing(component, issues);
    }
  }
}

function auditBottomSheetFormInputSizing(component, body, nodeName, issues) {
  const input = directChildNamed(body, nodeName);
  if (!input) {
    issues.push({
      kind: "bottom-sheet-form-input-missing",
      variant: component.name,
      node: nodeName,
      nodeId: body.id,
      urlNodeId: nodeIdForUrl(body.id),
    });
    return;
  }
  if (!isNestedComponentInstance(input, "Input")) {
    issues.push({
      kind: "cloned-subcomponent-frame",
      variant: component.name,
      node: input.name,
      nodeType: input.type,
      nodeId: input.id,
      urlNodeId: nodeIdForUrl(input.id),
      expectedSource: "Input",
    });
    return;
  }

  auditHorizontalFillSizing({
    node: input,
    issues,
    kind: "bottom-sheet-form-input-horizontal-sizing",
    variant: component.name,
  });
  auditAutoLayoutSizing({
    node: input,
    issues,
    kind: "bottom-sheet-form-input-vertical-sizing",
    field: "layoutSizingVertical",
    expected: "HUG",
    actual: input.layoutSizingVertical,
    variant: component.name,
  });
  auditVisualOverflow(
    component.name,
    input,
    issues,
    "bottom-sheet-form-input-visual-overflow",
  );
}

function auditBottomSheetFooterSizing(component, issues) {
  const footer = directChildNamed(component, "BottomSheet Footer");
  if (!footer || footer.type !== "FRAME") {
    issues.push({
      kind: "bottom-sheet-footer-missing",
      variant: component.name,
      nodeId: component.id,
      urlNodeId: nodeIdForUrl(component.id),
    });
    return;
  }

  auditHorizontalFillSizing({
    node: footer,
    issues,
    kind: "bottom-sheet-footer-horizontal-sizing",
    variant: component.name,
  });
  auditAutoLayoutSizing({
    node: footer,
    issues,
    kind: "bottom-sheet-footer-vertical-sizing",
    field: "layoutSizingVertical",
    expected: "FIXED",
    actual: footer.layoutSizingVertical,
    variant: component.name,
  });

  for (const nodeName of ["Secondary Action", "Primary Action"]) {
    const action = directChildNamed(footer, nodeName);
    if (!action) {
      issues.push({
        kind: "bottom-sheet-footer-action-missing",
        variant: component.name,
        node: nodeName,
        nodeId: footer.id,
        urlNodeId: nodeIdForUrl(footer.id),
      });
      continue;
    }
    if (!isNestedComponentInstance(action, "Button")) {
      issues.push({
        kind: "cloned-subcomponent-frame",
        variant: component.name,
        node: action.name,
        nodeType: action.type,
        nodeId: action.id,
        urlNodeId: nodeIdForUrl(action.id),
        expectedSource: "Button",
      });
      continue;
    }

    auditAutoLayoutSizing({
      node: action,
      issues,
      kind: "bottom-sheet-footer-action-horizontal-sizing",
      field: "layoutSizingHorizontal",
      expected: "FIXED",
      actual: action.layoutSizingHorizontal,
      variant: component.name,
    });
    auditAutoLayoutSizing({
      node: action,
      issues,
      kind: "bottom-sheet-footer-action-vertical-sizing",
      field: "layoutSizingVertical",
      expected: "FIXED",
      actual: action.layoutSizingVertical,
      variant: component.name,
    });
    auditVisualOverflow(
      component.name,
      action,
      issues,
      "bottom-sheet-footer-action-visual-overflow",
    );
  }
}

function counterTextNodeFor(counter) {
  if (!counter || !counter.children) return null;
  return directChildNamed(counter, "Counter Text");
}

function instanceHasComponentProperty(instance, baseName, type) {
  if (!instance || !instance.componentProperties) return false;

  for (const propertyName of Object.keys(instance.componentProperties)) {
    const property = instance.componentProperties[propertyName];
    if (propertyName.split("#")[0] !== baseName) continue;
    if (type && property.type !== type) continue;
    return true;
  }

  return false;
}

function auditBadgeContentIntegrity(componentSet, childComponents) {
  const issues = [];

  if (componentSet.name !== "Badge") {
    return {
      issueCount: 0,
      issues,
    };
  }

  function pushIssue(component, kind, node, expected) {
    issues.push({
      kind,
      variant: component.name,
      node: node ? node.name : null,
      nodeType: node ? node.type : null,
      nodeId: node ? node.id : component.id,
      urlNodeId: node ? nodeIdForUrl(node.id) : nodeIdForUrl(component.id),
      expected,
    });
  }

  for (const component of childComponents) {
    const props = parseBadgeVariantName(component.name);
    if (!props) continue;

    const icon = directChildNamed(component, "Icon");
    const label = directChildNamed(component, "Label Text");
    const legacyCounterText = directChildNamed(component, "Counter Text");
    const counter = directChildNamed(component, "Counter");
    const counterText = counterTextNodeFor(counter);

    if (props.size === "Icon") {
      if (label) {
        pushIssue(
          component,
          "badge-icon-size-label-text",
          label,
          "Icon-size Badge variants should not include Label Text.",
        );
      }

      if (counter) {
        pushIssue(
          component,
          "badge-icon-size-counter",
          counter,
          "Icon-size Badge variants should not include Counter.",
        );
      }

      if (legacyCounterText) {
        pushIssue(
          component,
          "badge-icon-size-legacy-counter-text",
          legacyCounterText,
          "Icon-size Badge variants should not include legacy direct Counter Text.",
        );
      }

      if (
        !icon ||
        icon.type !== "INSTANCE" ||
        !icon.getSharedPluginData ||
        icon.getSharedPluginData(RUN_NAMESPACE, "kind") !== "icon-slot-instance"
      ) {
        pushIssue(
          component,
          "badge-icon-size-icon-slot",
          icon,
          "Icon-size Badge variants should include a direct generated Icon instance-swap slot.",
        );
      }

      continue;
    }

    if (legacyCounterText) {
      pushIssue(
        component,
        "badge-legacy-counter-text",
        legacyCounterText,
        "Non-icon Badge variants should expose count text through nested Counter.",
      );
    }

    if (!label || label.type !== "TEXT") {
      pushIssue(
        component,
        "badge-label-text-missing",
        label,
        "Non-icon Badge variants should include editable Label Text.",
      );
    }

    if (!counter || !isNestedComponentInstance(counter, "Counter")) {
      pushIssue(
        component,
        "badge-counter-missing",
        counter,
        "Non-icon Badge variants should include a hidden nested Counter instance.",
      );
    } else if (counter.visible !== false) {
      pushIssue(
        component,
        "badge-counter-visible-by-default",
        counter,
        "Counter should be hidden by default and shown through Show Counter.",
      );
    } else if (counter.isExposedInstance !== true) {
      pushIssue(
        component,
        "badge-counter-not-exposed",
        counter,
        "Nested Counter should be exposed so its Counter Text property remains editable.",
      );
    }

    if (
      !counterText &&
      !instanceHasComponentProperty(counter, "Counter Text", "TEXT")
    ) {
      pushIssue(
        component,
        "badge-counter-text-missing",
        counterText,
        "Nested Counter should expose editable Counter Text.",
      );
    }

    if (icon) {
      pushIssue(
        component,
        "badge-label-size-icon-slot",
        icon,
        "Non-icon Badge variants should not include the icon-size Icon slot.",
      );
    }
  }

  return {
    issueCount: issues.length,
    issues,
  };
}

function formatCompositionIssueDetails(issues) {
  if (!issues || issues.length === 0) return "";

  const visible = issues.slice(0, 3).map((issue) => {
    const location = [issue.variant, issue.node].filter(Boolean).join(" / ");
    const expectation = issue.expectedSource
      ? `expected live ${issue.expectedSource} instance`
      : issue.field
        ? `${issue.field} expected ${issue.expected}, got ${issue.actual}`
        : issue.expected
          ? `expected ${issue.expected}`
          : "";
    const parts = [issue.kind];
    if (location) parts.push(`(${location})`);
    if (expectation) parts.push(expectation);
    return parts.join(" ");
  });

  const remaining = issues.length - visible.length;
  if (remaining > 0) visible.push(`${remaining} more`);
  return visible.join("; ");
}

function formatContrastFailureDetails(failures, kind) {
  if (!Array.isArray(failures) || failures.length === 0) return "";

  const matching = failures.filter((failure) => failure.kind === kind);
  const visible = matching.slice(0, 3).map((failure) => {
    const parts = [failure.mode, failure.node].filter(Boolean);
    const location = parts.join(" ");
    return `${location} ratio ${failure.ratio} < ${failure.required}`;
  });

  const remaining = matching.length - visible.length;
  if (remaining > 0) visible.push(`${remaining} more`);
  return visible.join("; ");
}

function auditDialogAutoLayoutIntegrity(componentSet, issues) {
  const components = componentSet.children || [];

  for (const component of components) {
    if (component.type !== "COMPONENT") continue;

    const props = parseDialogVariantName(component.name);
    if (!props) continue;

    if (props.value === "Form") {
      const body = directChildNamed(component, "Dialog Body");
      if (!body || body.type !== "FRAME") {
        issues.push({
          kind: "dialog-form-body-missing",
          variant: component.name,
          nodeId: component.id,
          urlNodeId: nodeIdForUrl(component.id),
        });
        continue;
      }

      if (body.layoutMode !== "VERTICAL") {
        issues.push({
          kind: "dialog-form-body-layout-mode",
          variant: component.name,
          node: body.name,
          nodeId: body.id,
          urlNodeId: nodeIdForUrl(body.id),
          expected: "VERTICAL",
          actual: body.layoutMode || null,
        });
      }

      if (body.primaryAxisSizingMode !== "AUTO") {
        issues.push({
          kind: "dialog-form-body-height-sizing",
          variant: component.name,
          node: body.name,
          nodeId: body.id,
          urlNodeId: nodeIdForUrl(body.id),
          expected: "AUTO",
          actual: body.primaryAxisSizingMode || null,
        });
      }

      for (const legacyName of ["Name Field", "Username Field"]) {
        const legacy = directChildNamed(body, legacyName);
        if (!legacy) continue;
        issues.push({
          kind: "legacy-composition-frame",
          variant: component.name,
          node: legacy.name,
          nodeType: legacy.type,
          nodeId: legacy.id,
          urlNodeId: nodeIdForUrl(legacy.id),
          expectedSource: "Input",
        });
      }

      for (const nodeName of ["Name Input", "Username Input"]) {
        const input = directChildNamed(body, nodeName);
        if (!input) {
          issues.push({
            kind: "dialog-form-input-missing",
            variant: component.name,
            node: nodeName,
            nodeId: body.id,
            urlNodeId: nodeIdForUrl(body.id),
          });
          continue;
        }
        if (!isNestedComponentInstance(input, "Input")) {
          issues.push({
            kind: "cloned-subcomponent-frame",
            variant: component.name,
            node: input.name,
            nodeType: input.type,
            nodeId: input.id,
            urlNodeId: nodeIdForUrl(input.id),
            expectedSource: "Input",
          });
          continue;
        }

        auditHorizontalFillSizing({
          node: input,
          issues,
          kind: "dialog-form-input-horizontal-sizing",
          variant: component.name,
        });
        auditAutoLayoutSizing({
          node: input,
          issues,
          kind: "dialog-form-input-vertical-sizing",
          field: "layoutSizingVertical",
          expected: "HUG",
          actual: input.layoutSizingVertical,
          variant: component.name,
        });

        if (typeof input.layoutGrow === "number" && input.layoutGrow !== 0) {
          issues.push({
            kind: "dialog-form-input-primary-axis-fill",
            variant: component.name,
            node: input.name,
            nodeType: input.type,
            nodeId: input.id,
            urlNodeId: nodeIdForUrl(input.id),
            field: "layoutGrow",
            expected: 0,
            actual: input.layoutGrow,
          });
        }

        auditVisualOverflow(component.name, input, issues);
      }
    }

    if (props.value === "Form" || props.value === "Footer") {
      auditDialogFooterActionSizing(component, issues);
    }
  }
}

function auditDialogFooterActionSizing(component, issues) {
  const footer = directChildNamed(component, "Dialog Footer");
  if (!footer || footer.type !== "FRAME") {
    issues.push({
      kind: "dialog-footer-missing",
      variant: component.name,
      nodeId: component.id,
      urlNodeId: nodeIdForUrl(component.id),
    });
    return;
  }

  if (footer.layoutMode !== "HORIZONTAL") {
    issues.push({
      kind: "dialog-footer-layout-mode",
      variant: component.name,
      node: footer.name,
      nodeId: footer.id,
      urlNodeId: nodeIdForUrl(footer.id),
      expected: "HORIZONTAL",
      actual: footer.layoutMode || null,
    });
  }

  auditAutoLayoutSizing({
    node: footer,
    issues,
    kind: "dialog-footer-horizontal-sizing",
    field: "layoutSizingHorizontal",
    expected: "FILL",
    actual: footer.layoutSizingHorizontal,
    variant: component.name,
  });
  auditAutoLayoutSizing({
    node: footer,
    issues,
    kind: "dialog-footer-vertical-sizing",
    field: "layoutSizingVertical",
    expected: "FIXED",
    actual: footer.layoutSizingVertical,
    variant: component.name,
  });

  if (typeof footer.height === "number" && footer.height < 44) {
    issues.push({
      kind: "dialog-footer-min-height",
      variant: component.name,
      node: footer.name,
      nodeId: footer.id,
      urlNodeId: nodeIdForUrl(footer.id),
      expected: ">= 44",
      actual: footer.height,
    });
  }

  for (const nodeName of ["Secondary Action", "Primary Action"]) {
    const action = directChildNamed(footer, nodeName);
    if (!action) {
      issues.push({
        kind: "dialog-footer-action-missing",
        variant: component.name,
        node: nodeName,
        nodeId: footer.id,
        urlNodeId: nodeIdForUrl(footer.id),
      });
      continue;
    }
    if (!isNestedComponentInstance(action, "Button")) {
      issues.push({
        kind: "cloned-subcomponent-frame",
        variant: component.name,
        node: action.name,
        nodeType: action.type,
        nodeId: action.id,
        urlNodeId: nodeIdForUrl(action.id),
        expectedSource: "Button",
      });
      continue;
    }

    auditAutoLayoutSizing({
      node: action,
      issues,
      kind: "dialog-footer-action-horizontal-sizing",
      field: "layoutSizingHorizontal",
      expected: "FIXED",
      actual: action.layoutSizingHorizontal,
      variant: component.name,
    });
    auditAutoLayoutSizing({
      node: action,
      issues,
      kind: "dialog-footer-action-vertical-sizing",
      field: "layoutSizingVertical",
      expected: "FIXED",
      actual: action.layoutSizingVertical,
      variant: component.name,
    });

    if (typeof action.layoutGrow === "number" && action.layoutGrow !== 0) {
      issues.push({
        kind: "dialog-footer-action-primary-axis-fill",
        variant: component.name,
        node: action.name,
        nodeType: action.type,
        nodeId: action.id,
        urlNodeId: nodeIdForUrl(action.id),
        field: "layoutGrow",
        expected: 0,
        actual: action.layoutGrow,
      });
    }

    const inheritsCentered =
      action.layoutAlign === "INHERIT" &&
      footer.counterAxisAlignItems === "CENTER";
    if (
      action.layoutAlign &&
      action.layoutAlign !== "CENTER" &&
      !inheritsCentered
    ) {
      issues.push({
        kind: "dialog-footer-action-cross-axis-align",
        variant: component.name,
        node: action.name,
        nodeType: action.type,
        nodeId: action.id,
        urlNodeId: nodeIdForUrl(action.id),
        field: "layoutAlign",
        expected: "CENTER",
        actual: action.layoutAlign,
      });
    }

    const expectedSize = nodeName === "Primary Action" ? "Large" : "Default";
    const metrics = buttonMetrics(expectedSize);
    const minWidth = expectedDialogFooterActionWidth(
      nodeName === "Primary Action" ? "Save changes" : "Cancel",
      expectedSize,
    );

    if (typeof action.width === "number" && action.width < minWidth) {
      issues.push({
        kind: "dialog-footer-action-min-width",
        variant: component.name,
        node: action.name,
        nodeType: action.type,
        nodeId: action.id,
        urlNodeId: nodeIdForUrl(action.id),
        expected: `>= ${minWidth}`,
        actual: action.width,
      });
    }

    if (typeof action.height === "number" && action.height < metrics.height) {
      issues.push({
        kind: "dialog-footer-action-min-height",
        variant: component.name,
        node: action.name,
        nodeType: action.type,
        nodeId: action.id,
        urlNodeId: nodeIdForUrl(action.id),
        expected: `>= ${metrics.height}`,
        actual: action.height,
      });
    }

    auditVisualOverflow(
      component.name,
      action,
      issues,
      "dialog-footer-action-visual-overflow",
    );
  }
}

function auditAutoLayoutSizing(options) {
  if (typeof options.actual === "undefined") return;
  if (options.actual === options.expected) return;

  issuesPushAutoLayoutSizing(options);
}

function auditHorizontalFillSizing(options) {
  const layoutSizing = options.node.layoutSizingHorizontal;
  const layoutAlign = options.node.layoutAlign;
  if (layoutSizing === "FILL" || layoutAlign === "STRETCH") {
    return;
  }

  issuesPushAutoLayoutSizing({
    node: options.node,
    issues: options.issues,
    kind: options.kind,
    variant: options.variant,
    field: "layoutSizingHorizontal/layoutAlign",
    expected: "FILL or STRETCH",
    actual: compositeSizingActualSummary(options.node),
  });
}

function issuesPushAutoLayoutSizing(options) {
  options.issues.push({
    kind: options.kind,
    variant: options.variant,
    node: options.node.name,
    nodeType: options.node.type,
    nodeId: options.node.id,
    urlNodeId: nodeIdForUrl(options.node.id),
    field: options.field,
    expected: options.expected,
    actual: options.actual || null,
  });
}

function auditVisualOverflow(
  variant,
  node,
  issues,
  kind = "dialog-form-input-visual-overflow",
  options = {},
) {
  const ownBounds = node.absoluteBoundingBox;
  if (!ownBounds || !node.children) return;
  const checkHorizontal = options.horizontal === true;
  const checkVertical = options.vertical !== false;

  let childBounds = null;

  function mergeBounds(bounds) {
    if (!bounds) return;
    const next = {
      x1: bounds.x,
      y1: bounds.y,
      x2: bounds.x + bounds.width,
      y2: bounds.y + bounds.height,
    };

    if (!childBounds) {
      childBounds = next;
      return;
    }

    childBounds.x1 = Math.min(childBounds.x1, next.x1);
    childBounds.y1 = Math.min(childBounds.y1, next.y1);
    childBounds.x2 = Math.max(childBounds.x2, next.x2);
    childBounds.y2 = Math.max(childBounds.y2, next.y2);
  }

  function walk(current) {
    if (current.visible === false) return;
    if (isGeneratedFocusRingNode(current)) return;
    if (
      options.ignoreNestedInstanceOuterBounds === true &&
      isGeneratedNestedComponentInstance(current) &&
      current.children &&
      current.children.length > 0
    ) {
      for (const child of current.children) walk(child);
      return;
    }

    mergeBounds(current.absoluteBoundingBox);

    if (current.children) {
      for (const child of current.children) walk(child);
    }
  }

  for (const child of node.children) walk(child);
  if (!childBounds) return;

  const tolerance = 1;
  if (checkHorizontal) {
    const ownRight = ownBounds.x + ownBounds.width;
    const overflowsLeft = childBounds.x1 < ownBounds.x - tolerance;
    const overflowsRight = childBounds.x2 > ownRight + tolerance;
    if (overflowsLeft || overflowsRight) {
      issues.push({
        kind,
        variant,
        node: node.name,
        nodeType: node.type,
        nodeId: node.id,
        urlNodeId: nodeIdForUrl(node.id),
        expectedMinLeft: Math.round(ownBounds.x),
        actualContentLeft: Math.round(childBounds.x1),
        expectedMaxRight: Math.round(ownRight),
        actualContentRight: Math.round(childBounds.x2),
      });
      return;
    }
  }

  if (!checkVertical) return;
  const ownBottom = ownBounds.y + ownBounds.height;
  if (childBounds.y2 <= ownBottom + tolerance) return;

  issues.push({
    kind,
    variant,
    node: node.name,
    nodeType: node.type,
    nodeId: node.id,
    urlNodeId: nodeIdForUrl(node.id),
    expectedMaxBottom: Math.round(ownBottom),
    actualContentBottom: Math.round(childBounds.y2),
  });
}

function isGeneratedFocusRingNode(node) {
  if (!node || !node.getSharedPluginData) return false;
  try {
    return node.getSharedPluginData(RUN_NAMESPACE, "kind") === "focus-ring";
  } catch (_error) {
    return false;
  }
}

function auditExpectedNestedInstance(
  componentSet,
  issues,
  nodeName,
  sourceComponentSet,
) {
  const nodes = componentSet.findAll((node) => node.name === nodeName);
  for (const node of nodes) {
    if (isNestedComponentInstance(node, sourceComponentSet)) {
      continue;
    }

    issues.push({
      kind: "cloned-subcomponent-frame",
      node: node.name,
      nodeType: node.type,
      nodeId: node.id,
      urlNodeId: nodeIdForUrl(node.id),
      expectedSource: sourceComponentSet,
    });
  }
}

function auditLegacyFrameClone(
  componentSet,
  issues,
  nodeName,
  sourceComponentSet,
) {
  const nodes = componentSet.findAll(
    (node) => node.name === nodeName && node.type !== "INSTANCE",
  );
  for (const node of nodes) {
    issues.push({
      kind: "legacy-composition-frame",
      node: node.name,
      nodeType: node.type,
      nodeId: node.id,
      urlNodeId: nodeIdForUrl(node.id),
      expectedSource: sourceComponentSet,
    });
  }
}

function hasTintableIconPaint(node) {
  if (!node) return false;
  if (
    firstVisibleSolidPaint(node.fills) ||
    firstVisibleSolidPaint(node.strokes)
  ) {
    return true;
  }

  if (node.children) {
    for (const child of node.children) {
      if (hasTintableIconPaint(child)) return true;
    }
  }

  return false;
}

function auditComponentAccessibility(
  componentSet,
  childComponents,
  variantAxes,
) {
  let minWidth = Infinity;
  let minHeight = Infinity;
  let minimumInteractiveSize = Infinity;
  const stateValues = variantAxes.State || [];
  const hasStateAxis = stateValues.length > 0;
  const hasDisabledState = stateValues.indexOf("Disabled") !== -1;
  const hasLoadingState = stateValues.indexOf("Loading") !== -1;
  const hasFocusState =
    stateValues.indexOf("Focus") !== -1 ||
    stateValues.indexOf("Focused") !== -1 ||
    hasOwnedFocusNode(componentSet);

  for (const component of childComponents) {
    minWidth = Math.min(minWidth, component.width);
    minHeight = Math.min(minHeight, component.height);
    minimumInteractiveSize = Math.min(
      minimumInteractiveSize,
      Math.min(component.width, component.height),
    );
  }

  if (childComponents.length === 0) {
    minWidth = 0;
    minHeight = 0;
    minimumInteractiveSize = 0;
  }

  return {
    hasStateAxis,
    hasDisabledState,
    hasLoadingState,
    hasFocusState,
    minWidth: Math.round(minWidth),
    minHeight: Math.round(minHeight),
    minimumInteractiveSize: Math.round(minimumInteractiveSize),
    compactVisualSize:
      minimumInteractiveSize >= 36 && minimumInteractiveSize < 44,
    recommendedTouchTarget: 44,
    minimumTouchTargetPass: minimumInteractiveSize >= 44,
  };
}

function hasOwnedFocusNode(node) {
  if (!node) return false;
  if (isGeneratedNestedComponentInstance(node)) return false;
  if (/focus/i.test(node.name)) return true;

  if (node.children) {
    for (const child of node.children) {
      if (hasOwnedFocusNode(child)) return true;
    }
  }

  return false;
}

function auditComponentContrast(
  componentSet,
  childComponents,
  variableContext,
) {
  const result = createContrastAuditResult();
  const modeNames = contrastModeNames(variableContext);

  for (const modeName of modeNames) {
    const modeResult = auditComponentContrastForMode(
      componentSet,
      childComponents,
      variableContext,
      modeName,
    );
    mergeContrastModeResult(result, modeResult);
  }

  if (result.minTextContrast !== null) {
    result.minTextContrast = Math.round(result.minTextContrast * 100) / 100;
  }

  if (result.minNonTextContrast !== null) {
    result.minNonTextContrast =
      Math.round(result.minNonTextContrast * 100) / 100;
  }

  return result;
}

function createContrastAuditResult() {
  return {
    textPairs: 0,
    textFailures: 0,
    nonTextPairs: 0,
    nonTextFailures: 0,
    minTextContrast: null,
    minNonTextContrast: null,
    surfaceDependent: false,
    modesAudited: [],
    byMode: [],
    failures: [],
  };
}

function contrastModeNames(variableContext) {
  const modeNames =
    variableContext && Array.isArray(variableContext.modeNames)
      ? variableContext.modeNames
      : [];

  const preferred = ["Light", "Dark"].filter(
    (modeName) => modeNames.indexOf(modeName) !== -1,
  );

  if (preferred.length > 0) return preferred;
  return modeNames.length > 0 ? modeNames : ["Default"];
}

function mergeContrastModeResult(result, modeResult) {
  result.modesAudited.push(modeResult.mode);
  result.byMode.push({
    mode: modeResult.mode,
    textPairs: modeResult.textPairs,
    textFailures: modeResult.textFailures,
    nonTextPairs: modeResult.nonTextPairs,
    nonTextFailures: modeResult.nonTextFailures,
    minTextContrast:
      modeResult.minTextContrast === null
        ? null
        : Math.round(modeResult.minTextContrast * 100) / 100,
    minNonTextContrast:
      modeResult.minNonTextContrast === null
        ? null
        : Math.round(modeResult.minNonTextContrast * 100) / 100,
    surfaceDependent: modeResult.surfaceDependent,
  });

  result.textPairs = Math.max(result.textPairs, modeResult.textPairs);
  result.nonTextPairs = Math.max(result.nonTextPairs, modeResult.nonTextPairs);
  result.textFailures += modeResult.textFailures;
  result.nonTextFailures += modeResult.nonTextFailures;
  result.surfaceDependent =
    result.surfaceDependent || modeResult.surfaceDependent;

  if (modeResult.minTextContrast !== null) {
    result.minTextContrast =
      result.minTextContrast === null
        ? modeResult.minTextContrast
        : Math.min(result.minTextContrast, modeResult.minTextContrast);
  }

  if (modeResult.minNonTextContrast !== null) {
    result.minNonTextContrast =
      result.minNonTextContrast === null
        ? modeResult.minNonTextContrast
        : Math.min(result.minNonTextContrast, modeResult.minNonTextContrast);
  }

  for (const failure of modeResult.failures) {
    if (result.failures.length >= 12) break;
    result.failures.push(failure);
  }
}

function auditComponentContrastForMode(
  componentSet,
  childComponents,
  variableContext,
  modeName,
) {
  const fallbackSurface = colorForVariableName(
    "Surface/0",
    "#FFFFFF",
    variableContext,
    modeName,
  ) || { r: 1, g: 1, b: 1, a: 1 };
  const result = createContrastAuditResult();
  result.mode = modeName;

  for (const component of childComponents) {
    const props =
      parseTextVariantName(component.name) ||
      parseHeadingVariantName(component.name) ||
      parseLinkVariantName(component.name) ||
      parseLabelVariantName(component.name) ||
      parseBoxVariantName(component.name) ||
      parseGridVariantName(component.name) ||
      parseButtonVariantName(component.name) ||
      parseIconButtonVariantName(component.name) ||
      parseCounterVariantName(component.name) ||
      parseBadgeVariantName(component.name) ||
      parseCardVariantName(component.name) ||
      parseTabsVariantName(component.name) ||
      parseCheckboxVariantName(component.name) ||
      parseRadioVariantName(component.name) ||
      parseSwitchVariantName(component.name) ||
      parseInputVariantName(component.name) ||
      parsePasswordInputVariantName(component.name) ||
      parseNumberInputVariantName(component.name) ||
      parseOTPInputVariantName(component.name) ||
      parseComboboxVariantName(component.name) ||
      parseMultiSelectVariantName(component.name) ||
      parseListboxVariantName(component.name) ||
      parseDatePickerVariantName(component.name) ||
      parseDateRangePickerVariantName(component.name) ||
      parseTimePickerVariantName(component.name) ||
      parseFileUploadVariantName(component.name) ||
      parseColorPickerVariantName(component.name) ||
      parseTextareaVariantName(component.name) ||
      parseSearchVariantName(component.name) ||
      parseSelectVariantName(component.name) ||
      parseSliderVariantName(component.name) ||
      parseProgressVariantName(component.name) ||
      parseSpinnerVariantName(component.name) ||
      parseAvatarVariantName(component.name) ||
      parseAlertVariantName(component.name) ||
      parseEmptyStateVariantName(component.name) ||
      parseSegmentedControlVariantName(component.name) ||
      {};
    if (props.variant === "Glass") {
      result.surfaceDependent = true;
      continue;
    }

    const isDisabled = props.state === "Disabled";
    const bgPaint = solidPaintToRgba(
      firstVisibleSolidPaint(component.fills),
      variableContext,
      modeName,
    );
    if (!bgPaint) result.surfaceDependent = true;
    auditNodeContrast(
      component,
      fallbackSurface,
      isDisabled,
      result,
      variableContext,
      component.name,
      modeName,
    );
  }

  return result;
}

function auditNodeContrast(
  node,
  background,
  isDisabled,
  result,
  variableContext,
  ownerName,
  modeName,
) {
  if (node && node.visible === false) return;
  if (isGeneratedNestedComponentInstance(node)) return;

  if (isGeneratedDirectIconSlot(node)) {
    auditActualIconSlotPaints(
      node,
      background,
      isDisabled,
      result,
      variableContext,
      ownerName,
      modeName,
    );
    return;
  }

  if (isGeneratedCheckboxControl(node)) {
    auditCheckboxControlPaints(
      node,
      background,
      isDisabled,
      result,
      variableContext,
      ownerName,
      modeName,
    );
    return;
  }

  if (isGeneratedRadioControl(node)) {
    auditRadioControlPaints(
      node,
      background,
      isDisabled,
      result,
      variableContext,
      ownerName,
      modeName,
    );
    return;
  }

  if (isGeneratedSwitchTrack(node)) {
    auditSwitchTrackPaints(
      node,
      background,
      isDisabled,
      result,
      variableContext,
      ownerName,
      modeName,
    );
    return;
  }

  if (isGeneratedInputField(node)) {
    auditInputFieldPaints(
      node,
      background,
      isDisabled,
      result,
      variableContext,
      ownerName,
      modeName,
    );
    return;
  }

  if (isGeneratedCardAction(node)) {
    auditCardActionPaints(
      node,
      background,
      isDisabled,
      result,
      variableContext,
      ownerName,
      modeName,
    );
    return;
  }

  if (isGeneratedSliderTrack(node)) {
    auditSliderTrackPaints(
      node,
      background,
      isDisabled,
      result,
      variableContext,
      ownerName,
      modeName,
    );
    return;
  }

  if (isGeneratedSliderThumb(node)) {
    auditSliderThumbPaints(
      node,
      background,
      isDisabled,
      result,
      variableContext,
      ownerName,
      modeName,
    );
    return;
  }

  if (isGeneratedProgressTrack(node)) {
    auditSliderTrackPaints(
      node,
      background,
      isDisabled,
      result,
      variableContext,
      ownerName,
      modeName,
    );
    return;
  }

  if (isGeneratedSegmentedControlSegment(node)) {
    auditSegmentedControlSegmentPaints(
      node,
      background,
      isDisabled,
      result,
      variableContext,
      ownerName,
      modeName,
    );
    return;
  }

  if (
    isGeneratedDatePickerCalendarDay(node) ||
    isGeneratedDateRangePickerCalendarDay(node) ||
    isGeneratedTimePickerOption(node)
  ) {
    auditGeneratedFilledChildSurfacePaints(
      node,
      background,
      isDisabled,
      result,
      variableContext,
      ownerName,
      modeName,
    );
    return;
  }

  if (isGeneratedCheckboxMark(node)) {
    auditActualIconSlotPaints(
      node,
      background,
      isDisabled,
      result,
      variableContext,
      ownerName,
      modeName,
    );
    return;
  }

  const generatedIconPaint = generatedIconSlotPaint(
    node,
    variableContext,
    modeName,
  );
  if (generatedIconPaint) {
    auditNonTextPaint(
      node,
      generatedIconPaint,
      background,
      isDisabled,
      result,
      ownerName,
      modeName,
    );
    return;
  }

  if (node.type === "TEXT") {
    const paint = solidPaintToRgba(
      firstVisibleSolidPaint(node.fills),
      variableContext,
      modeName,
    );
    if (paint) {
      const foreground = compositeColor(paint, background);
      const ratio = contrastRatio(foreground, background);
      result.textPairs += 1;
      if (!isDisabled) {
        result.minTextContrast =
          result.minTextContrast === null
            ? ratio
            : Math.min(result.minTextContrast, ratio);
      }

      if (!isDisabled && ratio < 4.5) {
        result.textFailures += 1;
        addContrastFailure(
          result,
          node,
          ratio,
          "text",
          4.5,
          ownerName,
          modeName,
        );
      }
    }
  } else if (isLikelyNonTextIndicator(node)) {
    const paints = [];
    const fill = solidPaintToRgba(
      firstVisibleSolidPaint(node.fills),
      variableContext,
      modeName,
    );
    const stroke = solidPaintToRgba(
      firstVisibleSolidPaint(node.strokes),
      variableContext,
      modeName,
    );
    if (fill) paints.push(fill);
    if (stroke) paints.push(stroke);

    for (const paint of paints) {
      auditNonTextPaint(
        node,
        paint,
        background,
        isDisabled,
        result,
        ownerName,
        modeName,
      );
    }
  }

  if (node.children) {
    const childBackground = contrastChildBackground(
      node,
      background,
      variableContext,
      modeName,
    );
    for (const child of node.children) {
      auditNodeContrast(
        child,
        childBackground,
        isDisabled,
        result,
        variableContext,
        ownerName,
        modeName,
      );
    }
  }
}

function contrastChildBackground(node, background, variableContext, modeName) {
  const fill = solidPaintToRgba(
    firstVisibleSolidPaint(node.fills),
    variableContext,
    modeName,
  );

  return fill ? compositeColor(fill, background) : background;
}

function isGeneratedCheckboxControl(node) {
  return (
    node &&
    node.name === "Checkbox Control" &&
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") === "checkbox-control"
  );
}

function isGeneratedCheckboxMark(node) {
  return (
    node &&
    node.name === "Checkbox Mark" &&
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") === "checkbox-mark"
  );
}

function isGeneratedRadioControl(node) {
  return (
    node &&
    node.name === "Radio Control" &&
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") === "radio-control"
  );
}

function isGeneratedRadioDot(node) {
  return (
    node &&
    node.name === "Radio Dot" &&
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") === "radio-dot"
  );
}

function isGeneratedSwitchTrack(node) {
  return (
    node &&
    node.name === "Switch Track" &&
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") === "switch-track"
  );
}

function isGeneratedSwitchThumb(node) {
  return (
    node &&
    node.name === "Switch Thumb" &&
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") === "switch-thumb"
  );
}

function isGeneratedInputField(node) {
  return (
    node &&
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") === "input-field"
  );
}

function isGeneratedSliderTrack(node) {
  return (
    node &&
    node.name === "Slider Track" &&
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") === "slider-track"
  );
}

function isGeneratedSliderThumb(node) {
  return (
    node &&
    /^Slider Thumb/.test(node.name || "") &&
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") === "slider-thumb"
  );
}

function isGeneratedSliderThumbAnchor(node) {
  return (
    node &&
    /^Slider Thumb Anchor/.test(node.name || "") &&
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") === "slider-thumb-anchor"
  );
}

function isGeneratedProgressTrack(node) {
  return (
    node &&
    node.name === "Progress Track" &&
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") === "progress-track"
  );
}

function isGeneratedSegmentedControlSegment(node) {
  return (
    node &&
    /^Segment \d+$/.test(node.name || "") &&
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") ===
      "segmented-control-segment"
  );
}

function isGeneratedDatePickerCalendarDay(node) {
  return (
    node &&
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") === "datepicker-day"
  );
}

function isGeneratedDateRangePickerCalendarDay(node) {
  return (
    node &&
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") === "daterange-day"
  );
}

function isGeneratedTimePickerOption(node) {
  return (
    node &&
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") === "timepicker-option"
  );
}

function isGeneratedCardAction(node) {
  if (!node || !node.getSharedPluginData) return false;

  const kind = node.getSharedPluginData(RUN_NAMESPACE, "kind");
  return (
    ((kind === "card-action" || kind === "dialog-action") &&
      (node.name === "Primary Action" || node.name === "Secondary Action")) ||
    (kind === "toast-action" && node.name === "Toast Action")
  );
}

function auditSegmentedControlSegmentPaints(
  node,
  background,
  isDisabled,
  result,
  variableContext,
  ownerName,
  modeName,
) {
  const fill = solidPaintToRgba(
    firstVisibleSolidPaint(node.fills),
    variableContext,
    modeName,
  );
  const nextBackground = fill ? compositeColor(fill, background) : background;

  if (node.children) {
    for (const child of node.children) {
      auditNodeContrast(
        child,
        nextBackground,
        isDisabled,
        result,
        variableContext,
        ownerName,
        modeName,
      );
    }
  }
}

function auditGeneratedFilledChildSurfacePaints(
  node,
  background,
  isDisabled,
  result,
  variableContext,
  ownerName,
  modeName,
) {
  const fill = solidPaintToRgba(
    firstVisibleSolidPaint(node.fills),
    variableContext,
    modeName,
  );
  const nextBackground = fill ? compositeColor(fill, background) : background;
  const unavailable =
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "availability") === "unavailable";
  const childDisabled = isDisabled || unavailable;

  if (node.children) {
    for (const child of node.children) {
      auditNodeContrast(
        child,
        nextBackground,
        childDisabled,
        result,
        variableContext,
        ownerName,
        modeName,
      );
    }
  }
}

function auditCheckboxControlPaints(
  node,
  background,
  isDisabled,
  result,
  variableContext,
  ownerName,
  modeName,
) {
  const fill = solidPaintToRgba(
    firstVisibleSolidPaint(node.fills),
    variableContext,
    modeName,
  );
  const stroke = solidPaintToRgba(
    firstVisibleSolidPaint(node.strokes),
    variableContext,
    modeName,
  );
  const nextBackground = fill ? compositeColor(fill, background) : background;

  if (fill) {
    auditNonTextPaint(
      node,
      fill,
      background,
      isDisabled,
      result,
      ownerName,
      modeName,
    );
  }

  if (stroke) {
    auditNonTextPaint(
      node,
      stroke,
      background,
      isDisabled,
      result,
      ownerName,
      modeName,
    );
  }

  if (node.children) {
    for (const child of node.children) {
      auditNodeContrast(
        child,
        nextBackground,
        isDisabled,
        result,
        variableContext,
        ownerName,
        modeName,
      );
    }
  }
}

function auditRadioControlPaints(
  node,
  background,
  isDisabled,
  result,
  variableContext,
  ownerName,
  modeName,
) {
  auditCheckboxControlPaints(
    node,
    background,
    isDisabled,
    result,
    variableContext,
    ownerName,
    modeName,
  );
}

function auditSwitchTrackPaints(
  node,
  background,
  isDisabled,
  result,
  variableContext,
  ownerName,
  modeName,
) {
  auditCheckboxControlPaints(
    node,
    background,
    isDisabled,
    result,
    variableContext,
    ownerName,
    modeName,
  );
}

function auditSliderTrackPaints(
  node,
  background,
  isDisabled,
  result,
  variableContext,
  ownerName,
  modeName,
) {
  const fill = solidPaintToRgba(
    firstVisibleSolidPaint(node.fills),
    variableContext,
    modeName,
  );
  const stroke = solidPaintToRgba(
    firstVisibleSolidPaint(node.strokes),
    variableContext,
    modeName,
  );
  const nextBackground = fill ? compositeColor(fill, background) : background;
  const boundaryPaint = stroke || fill;

  if (boundaryPaint) {
    auditNonTextPaint(
      node,
      boundaryPaint,
      background,
      isDisabled,
      result,
      ownerName,
      modeName,
    );
  }

  if (node.children) {
    for (const child of node.children) {
      auditNodeContrast(
        child,
        nextBackground,
        isDisabled,
        result,
        variableContext,
        ownerName,
        modeName,
      );
    }
  }
}

function auditSliderThumbPaints(
  node,
  background,
  isDisabled,
  result,
  variableContext,
  ownerName,
  modeName,
) {
  const fill = solidPaintToRgba(
    firstVisibleSolidPaint(node.fills),
    variableContext,
    modeName,
  );
  const stroke = solidPaintToRgba(
    firstVisibleSolidPaint(node.strokes),
    variableContext,
    modeName,
  );
  const boundaryPaint = stroke || fill;
  const nextBackground = fill ? compositeColor(fill, background) : background;

  if (boundaryPaint) {
    auditNonTextPaint(
      node,
      boundaryPaint,
      background,
      isDisabled,
      result,
      ownerName,
      modeName,
    );
  }

  if (node.children) {
    for (const child of node.children) {
      auditNodeContrast(
        child,
        nextBackground,
        isDisabled,
        result,
        variableContext,
        ownerName,
        modeName,
      );
    }
  }
}

function auditCardActionPaints(
  node,
  background,
  isDisabled,
  result,
  variableContext,
  ownerName,
  modeName,
) {
  const fill = solidPaintToRgba(
    firstVisibleSolidPaint(node.fills),
    variableContext,
    modeName,
  );
  const stroke = solidPaintToRgba(
    firstVisibleSolidPaint(node.strokes),
    variableContext,
    modeName,
  );
  const nextBackground = fill ? compositeColor(fill, background) : background;
  const boundaryPaint = stroke || fill;

  if (boundaryPaint) {
    auditNonTextPaint(
      node,
      boundaryPaint,
      background,
      isDisabled,
      result,
      ownerName,
      modeName,
    );
  }

  if (node.children) {
    for (const child of node.children) {
      auditNodeContrast(
        child,
        nextBackground,
        isDisabled,
        result,
        variableContext,
        ownerName,
        modeName,
      );
    }
  }
}

function auditInputFieldPaints(
  node,
  background,
  isDisabled,
  result,
  variableContext,
  ownerName,
  modeName,
) {
  const fill = solidPaintToRgba(
    firstVisibleSolidPaint(node.fills),
    variableContext,
    modeName,
  );
  const stroke = solidPaintToRgba(
    firstVisibleSolidPaint(node.strokes),
    variableContext,
    modeName,
  );
  const nextBackground = fill ? compositeColor(fill, background) : background;

  if (stroke) {
    auditNonTextPaint(
      node,
      stroke,
      background,
      isDisabled,
      result,
      ownerName,
      modeName,
    );
  }

  if (node.children) {
    for (const child of node.children) {
      auditNodeContrast(
        child,
        nextBackground,
        isDisabled,
        result,
        variableContext,
        ownerName,
        modeName,
      );
    }
  }
}

function isGeneratedDirectIconSlot(node) {
  return (
    node &&
    node.name === "Icon" &&
    node.type === "INSTANCE" &&
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") === "icon-slot-instance"
  );
}

function auditActualIconSlotPaints(
  node,
  background,
  isDisabled,
  result,
  variableContext,
  ownerName,
  modeName,
) {
  function walk(current) {
    const fill = solidPaintToRgba(
      firstVisibleSolidPaint(current.fills),
      variableContext,
      modeName,
    );
    const stroke = solidPaintToRgba(
      firstVisibleSolidPaint(current.strokes),
      variableContext,
      modeName,
    );

    if (fill) {
      auditNonTextPaint(
        current,
        fill,
        background,
        isDisabled,
        result,
        ownerName,
        modeName,
      );
    }

    if (stroke) {
      auditNonTextPaint(
        current,
        stroke,
        background,
        isDisabled,
        result,
        ownerName,
        modeName,
      );
    }

    if (current.children) {
      for (const child of current.children) walk(child);
    }
  }

  walk(node);
}

function auditNonTextPaint(
  node,
  paint,
  background,
  isDisabled,
  result,
  ownerName,
  modeName,
) {
  const foreground = compositeColor(paint, background);
  const ratio = contrastRatio(foreground, background);
  result.nonTextPairs += 1;
  if (!isDisabled) {
    result.minNonTextContrast =
      result.minNonTextContrast === null
        ? ratio
        : Math.min(result.minNonTextContrast, ratio);
  }

  if (!isDisabled && ratio < 3) {
    result.nonTextFailures += 1;
    addContrastFailure(result, node, ratio, "non-text", 3, ownerName, modeName);
  }
}

function generatedIconSlotPaint(node, variableContext, modeName) {
  if (!node || !node.getSharedPluginData) return null;
  if (node.name !== "Icon") return null;
  if (
    node.getSharedPluginData(RUN_NAMESPACE, "kind") !== "icon-slot-instance"
  ) {
    return null;
  }

  const variableName = node.getSharedPluginData(
    RUN_NAMESPACE,
    "foreground-token",
  );
  const fallback = node.getSharedPluginData(
    RUN_NAMESPACE,
    "foreground-fallback",
  );

  return colorForVariableName(
    variableName,
    fallback,
    variableContext,
    modeName,
  );
}

function isLikelyNonTextIndicator(node) {
  if (!node || !node.name) return false;
  if (node.type === "COMPONENT" || node.type === "COMPONENT_SET") return false;
  if (node.type === "FRAME" || node.type === "GROUP") return false;
  if (node.name.indexOf("Variant=") === 0) return false;

  return (
    node.name === "Icon" ||
    node.name === "Loading Indicator" ||
    node.name === "Icon Circle" ||
    node.name === "Icon Handle" ||
    node.name === "Icon Mark" ||
    node.name === "Checkbox Control" ||
    /^Checkbox Mark/.test(node.name) ||
    node.name === "Radio Control" ||
    node.name === "Radio Dot" ||
    isGeneratedRadioDot(node) ||
    node.name === "Switch Track" ||
    node.name === "Switch Thumb" ||
    isGeneratedSwitchThumb(node) ||
    node.name === "Input Field" ||
    isGeneratedInputField(node) ||
    node.name === "Slider Track" ||
    node.name === "Slider Range" ||
    /^Slider Thumb/.test(node.name || "") ||
    isGeneratedSliderTrack(node) ||
    isGeneratedSliderThumb(node) ||
    node.name === "Progress Track" ||
    node.name === "Progress Range" ||
    isGeneratedProgressTrack(node) ||
    /spinner|glyph/i.test(node.name)
  );
}

function addContrastFailure(
  result,
  node,
  ratio,
  kind,
  required,
  ownerName,
  modeName,
) {
  if (result.failures.length >= 12) return;

  result.failures.push({
    mode: modeName,
    node: ownerName
      ? `${ownerName} / ${node.name || node.type}`
      : node.name || node.type,
    kind,
    ratio: Math.round(ratio * 100) / 100,
    required,
  });
}

function firstVisibleSolidPaint(paints) {
  if (!Array.isArray(paints)) return null;

  for (const paint of paints) {
    if (!paint || paint.visible === false) continue;
    if (paint.type === "SOLID") return paint;
  }

  return null;
}

function solidPaintToRgba(paint, variableContext, modeName) {
  if (!paint) return null;
  const boundColor = resolveBoundPaintColor(paint, variableContext, modeName);
  const color = boundColor || paint.color;
  if (!color) return null;
  return {
    r: color.r,
    g: color.g,
    b: color.b,
    a: paint.opacity === undefined ? 1 : paint.opacity,
  };
}

function createVariableContext(collections, variables) {
  const collectionById = {};
  const variableById = {};
  const variableByName = {};
  const modeNames = [];

  for (const collection of collections) {
    collectionById[collection.id] = collection;
    for (const mode of collection.modes || []) {
      if (modeNames.indexOf(mode.name) === -1) modeNames.push(mode.name);
    }
  }

  for (const variable of variables) {
    variableById[variable.id] = variable;
    variableByName[variable.name] = variable;
  }

  return {
    collectionById,
    variableById,
    variableByName,
    modeNames,
    colorCache: {},
  };
}

function resolveBoundPaintColor(paint, variableContext, modeName) {
  if (!paint || !paint.boundVariables || !variableContext) return null;
  const alias = paint.boundVariables.color;
  if (!alias || !alias.id) return null;
  return resolveVariableColor(alias.id, variableContext, [], modeName);
}

function resolveVariableColor(variableId, variableContext, seenIds, modeName) {
  if (!variableId || seenIds.indexOf(variableId) !== -1) return null;
  const cacheKey = `${variableId}::${modeName || ""}`;
  if (
    variableContext.colorCache &&
    Object.prototype.hasOwnProperty.call(variableContext.colorCache, cacheKey)
  ) {
    return variableContext.colorCache[cacheKey];
  }

  const variable = variableContext.variableById[variableId];
  if (!variable || variable.resolvedType !== "COLOR") return null;

  seenIds.push(variableId);
  const collection =
    variableContext.collectionById[variable.variableCollectionId];
  const modeId = firstModeId(collection, variable, modeName);
  if (!modeId || !variable.valuesByMode) return null;

  const resolved = resolveVariableColorValue(
    variable.valuesByMode[modeId],
    variableContext,
    seenIds,
    modeName,
  );
  if (variableContext.colorCache) {
    variableContext.colorCache[cacheKey] = resolved;
  }
  return resolved;
}

function firstModeId(collection, variable, modeName) {
  if (collection && collection.modes && collection.modes.length > 0) {
    if (modeName) {
      const matchingMode = collection.modes.find(
        (mode) => mode.name === modeName,
      );
      if (matchingMode) return matchingMode.modeId;
    }

    return collection.modes[0].modeId;
  }

  if (!variable || !variable.valuesByMode) return null;
  const modeIds = Object.keys(variable.valuesByMode);
  return modeIds.length > 0 ? modeIds[0] : null;
}

function resolveVariableColorValue(value, variableContext, seenIds, modeName) {
  if (!value) return null;

  if (value.type === "VARIABLE_ALIAS" && value.id) {
    return resolveVariableColor(value.id, variableContext, seenIds, modeName);
  }

  if (
    typeof value.r === "number" &&
    typeof value.g === "number" &&
    typeof value.b === "number"
  ) {
    return {
      r: value.r,
      g: value.g,
      b: value.b,
    };
  }

  return null;
}

function colorForVariableName(
  variableName,
  fallback,
  variableContext,
  modeName,
) {
  if (variableName && variableContext && variableContext.variableByName) {
    const variable = variableContext.variableByName[variableName];
    if (variable && variable.id) {
      const resolved = resolveVariableColor(
        variable.id,
        variableContext,
        [],
        modeName,
      );
      if (resolved) {
        return {
          r: resolved.r,
          g: resolved.g,
          b: resolved.b,
          a: 1,
        };
      }
    }
  }

  if (!fallback) return null;

  try {
    const color = parseColor(fallback);
    return {
      r: color.r,
      g: color.g,
      b: color.b,
      a: color.a,
    };
  } catch (_error) {
    return null;
  }
}

function compositeColor(foreground, background) {
  const alpha = foreground.a === undefined ? 1 : foreground.a;
  if (alpha >= 1) {
    return {
      r: foreground.r,
      g: foreground.g,
      b: foreground.b,
      a: 1,
    };
  }

  return {
    r: foreground.r * alpha + background.r * (1 - alpha),
    g: foreground.g * alpha + background.g * (1 - alpha),
    b: foreground.b * alpha + background.b * (1 - alpha),
    a: 1,
  };
}

function contrastRatio(a, b) {
  const l1 = relativeLuminance(a);
  const l2 = relativeLuminance(b);
  const lighter = Math.max(l1, l2);
  const darker = Math.min(l1, l2);
  return (lighter + 0.05) / (darker + 0.05);
}

function relativeLuminance(color) {
  return (
    0.2126 * linearRgb(color.r) +
    0.7152 * linearRgb(color.g) +
    0.0722 * linearRgb(color.b)
  );
}

function linearRgb(value) {
  return value <= 0.03928
    ? value / 12.92
    : Math.pow((value + 0.055) / 1.055, 2.4);
}

function collectBoundVariableIds(root) {
  const ids = [];

  function addId(id) {
    if (id && ids.indexOf(id) === -1) ids.push(id);
  }

  function scanValue(value) {
    if (!value) return;
    if (Array.isArray(value)) {
      for (const item of value) scanValue(item);
      return;
    }

    if (typeof value !== "object") return;

    if (
      typeof value.id === "string" &&
      (value.type === "VARIABLE_ALIAS" || value.id.indexOf("VariableID:") === 0)
    ) {
      addId(value.id);
    }

    if (value.boundVariables) scanValue(value.boundVariables);

    for (const key of Object.keys(value)) {
      if (key === "parent" || key === "children") continue;
      scanValue(value[key]);
    }
  }

  function walk(node) {
    if (isGeneratedNestedComponentInstance(node)) return;

    scanValue(node.boundVariables);
    scanValue(node.fills);
    scanValue(node.strokes);
    scanValue(node.effects);
    if (node.children) {
      for (const child of node.children) walk(child);
    }
  }

  walk(root);
  return ids;
}

function auditBoundVariableFields(root) {
  const layoutFields = new Set([
    "width",
    "height",
    "paddingLeft",
    "paddingRight",
    "paddingTop",
    "paddingBottom",
    "itemSpacing",
    "cornerRadius",
    "strokeWeight",
  ]);
  const typographyFields = new Set(["fontSize", "lineHeight"]);
  const usage = {
    totalFields: 0,
    layoutFieldCount: 0,
    typographyFieldCount: 0,
    colorFieldCount: 0,
    byField: {},
    textNodes: 0,
    textStyleNodes: 0,
    textNodesWithoutTextStyle: 0,
  };

  function addField(field) {
    usage.totalFields += 1;
    usage.byField[field] = (usage.byField[field] || 0) + 1;

    if (layoutFields.has(field)) {
      usage.layoutFieldCount += 1;
    } else if (typographyFields.has(field)) {
      usage.typographyFieldCount += 1;
    } else if (field === "fill.color" || field === "stroke.color") {
      usage.colorFieldCount += 1;
    }
  }

  function isVariableAlias(value) {
    return (
      value &&
      typeof value === "object" &&
      typeof value.id === "string" &&
      (value.type === "VARIABLE_ALIAS" || value.id.indexOf("VariableID:") === 0)
    );
  }

  function scanBoundVariables(value, field) {
    if (!value) return;
    if (isVariableAlias(value)) {
      addField(field);
      return;
    }

    if (Array.isArray(value)) {
      for (const item of value) scanBoundVariables(item, field);
      return;
    }

    if (typeof value !== "object") return;

    for (const key of Object.keys(value)) {
      scanBoundVariables(value[key], key);
    }
  }

  function scanPaints(paints, field) {
    if (!Array.isArray(paints)) return;

    for (const paint of paints) {
      if (!paint || !paint.boundVariables) continue;
      if (isVariableAlias(paint.boundVariables.color)) {
        addField(field);
      } else {
        scanBoundVariables(paint.boundVariables, field);
      }
    }
  }

  function hasTextStyle(node) {
    const directStyleId = node.textStyleId;
    if (
      typeof directStyleId === "string" &&
      directStyleId.length > 0 &&
      directStyleId !== figma.mixed
    ) {
      return true;
    }

    if (
      node.getRangeTextStyleId &&
      typeof node.characters === "string" &&
      node.characters.length > 0
    ) {
      try {
        const rangeStyleId = node.getRangeTextStyleId(
          0,
          node.characters.length,
        );
        return (
          typeof rangeStyleId === "string" &&
          rangeStyleId.length > 0 &&
          rangeStyleId !== figma.mixed
        );
      } catch (_error) {
        return false;
      }
    }

    return (
      typeof directStyleId === "string" &&
      directStyleId.length > 0 &&
      directStyleId !== figma.mixed
    );
  }

  function walk(node) {
    if (isGeneratedNestedComponentInstance(node)) return;

    scanBoundVariables(node.boundVariables, "");
    scanPaints(node.fills, "fill.color");
    scanPaints(node.strokes, "stroke.color");

    if (node.type === "TEXT") {
      usage.textNodes += 1;
      if (hasTextStyle(node)) {
        usage.textStyleNodes += 1;
      } else {
        usage.textNodesWithoutTextStyle += 1;
      }
    }

    if (node.children) {
      for (const child of node.children) walk(child);
    }
  }

  walk(root);
  return usage;
}

async function importFoundations(payload, options) {
  validatePayload(payload);

  const stats = {
    pagesCreated: 0,
    pagesRenamed: 0,
    pagesExisting: 0,
    collectionsCreated: 0,
    collectionsExisting: 0,
    modesCreated: 0,
    modesExisting: 0,
    variablesCreated: 0,
    variablesExisting: 0,
    variableValuesSet: 0,
    aliasesSet: 0,
    scopesSet: 0,
    syntaxSet: 0,
    docsCreated: 0,
    skipped: [],
    warnings: [],
  };

  const pages =
    options.createPages === false
      ? new Map()
      : await ensurePages(payload.pages || [], stats);

  if (options.createVariables !== false) {
    await ensureVariables(payload, stats);
  }

  if (options.createDocs !== false) {
    await ensureDocumentation(payload, pages, stats);
  }

  return {
    target: payload.target,
    summary: payload.summary,
    stats,
    file: await inspectFile(),
  };
}

function validatePayload(payload) {
  if (!payload || typeof payload !== "object") {
    throw new Error("Payload must be a JSON object.");
  }

  if (!Array.isArray(payload.variables)) {
    throw new Error("Payload is missing a variables array.");
  }

  if (!Array.isArray(payload.collections)) {
    throw new Error("Payload is missing a collections array.");
  }

  for (const variable of payload.variables) {
    if (
      !variable.canonicalName ||
      !variable.figmaName ||
      !variable.collection ||
      !variable.figmaType
    ) {
      throw new Error(
        `Invalid variable record: ${JSON.stringify(variable).slice(0, 200)}`,
      );
    }

    if (!VALID_VARIABLE_TYPES.has(variable.figmaType)) {
      throw new Error(
        `Unsupported Figma variable type "${variable.figmaType}" for ${variable.canonicalName}.`,
      );
    }
  }
}

async function ensurePages(pageNames, stats) {
  const pages = new Map(figma.root.children.map((page) => [page.name, page]));

  for (let index = 0; index < pageNames.length; index += 1) {
    const name = pageNames[index];
    if (pages.has(name)) {
      stats.pagesExisting += 1;
      continue;
    }

    let page = null;
    const defaultPage = figma.root.children.find(
      (child) => child.name === "Page 1",
    );

    if (index === 0 && defaultPage) {
      await defaultPage.loadAsync();
      if (defaultPage.children.length === 0) {
        defaultPage.name = name;
        page = defaultPage;
        stats.pagesRenamed += 1;
      }
    }

    if (!page) {
      page = figma.createPage();
      page.name = name;
      stats.pagesCreated += 1;
    }

    page.setSharedPluginData(RUN_NAMESPACE, "kind", "page");
    pages.set(name, page);
  }

  return pages;
}

async function ensureVariables(payload, stats) {
  const collections = await ensureCollections(payload.collections, stats);
  const existingVariables = await figma.variables.getLocalVariablesAsync();
  const variableByCollectionAndName = new Map();
  const variableByCanonicalName = new Map();

  for (const variable of existingVariables) {
    variableByCollectionAndName.set(
      `${variable.variableCollectionId}:${variable.name}`,
      variable,
    );
  }

  for (const item of payload.variables) {
    const collection = collections.get(item.collection);
    if (!collection) {
      stats.skipped.push(
        `${item.canonicalName}: missing collection ${item.collection}`,
      );
      continue;
    }

    const key = `${collection.id}:${item.figmaName}`;
    let variable = variableByCollectionAndName.get(key);

    if (!variable) {
      variable = figma.variables.createVariable(
        item.figmaName,
        collection,
        item.figmaType,
      );
      variable.setSharedPluginData(
        RUN_NAMESPACE,
        "canonicalName",
        item.canonicalName,
      );
      variable.setSharedPluginData(
        RUN_NAMESPACE,
        "cssVariable",
        item.cssVariable || "",
      );
      variableByCollectionAndName.set(key, variable);
      stats.variablesCreated += 1;
    } else {
      stats.variablesExisting += 1;
    }

    variableByCanonicalName.set(item.canonicalName, variable);

    if (
      Array.isArray(item.suggestedScopes) &&
      item.suggestedScopes.length > 0
    ) {
      try {
        variable.scopes = item.suggestedScopes;
        stats.scopesSet += 1;
      } catch (error) {
        stats.warnings.push(
          `${item.canonicalName}: could not set scopes (${messageFor(error)})`,
        );
      }
    }

    setCodeSyntax(variable, item, stats);
  }

  for (const item of payload.variables) {
    const variable = variableByCanonicalName.get(item.canonicalName);
    const collection = collections.get(item.collection);
    if (!variable || !collection) continue;

    const modeIds = modeIdsFor(collection);
    await setModeValue(
      variable,
      modeIds.Light,
      item.values.light,
      item,
      variableByCanonicalName,
      stats,
    );
    await setModeValue(
      variable,
      modeIds.Dark,
      item.values.dark,
      item,
      variableByCanonicalName,
      stats,
    );
  }
}

async function ensureCollections(collectionRecords, stats) {
  const existing = await figma.variables.getLocalVariableCollectionsAsync();
  const byName = new Map(
    existing.map((collection) => [collection.name, collection]),
  );
  const collections = new Map();

  for (const record of collectionRecords) {
    let collection = byName.get(record.name);

    if (!collection) {
      collection = figma.variables.createVariableCollection(record.name);
      collection.setSharedPluginData(RUN_NAMESPACE, "kind", "collection");
      stats.collectionsCreated += 1;
    } else {
      stats.collectionsExisting += 1;
    }

    ensureModes(collection, record.modes || ["Light", "Dark"], stats);
    collections.set(record.name, collection);
  }

  return collections;
}

function ensureModes(collection, modeNames, stats) {
  const desiredModes = modeNames.length > 0 ? modeNames : ["Light", "Dark"];
  let modes = collection.modes;

  if (!modes.some((mode) => mode.name === desiredModes[0])) {
    collection.renameMode(modes[0].modeId, desiredModes[0]);
    modes = collection.modes;
  }

  for (const modeName of desiredModes) {
    if (collection.modes.some((mode) => mode.name === modeName)) {
      stats.modesExisting += 1;
      continue;
    }

    collection.addMode(modeName);
    stats.modesCreated += 1;
  }
}

function modeIdsFor(collection) {
  const light =
    collection.modes.find((mode) => mode.name === "Light") ||
    collection.modes[0];
  const dark = collection.modes.find((mode) => mode.name === "Dark") || light;

  return {
    Light: light.modeId,
    Dark: dark.modeId,
  };
}

async function setModeValue(
  variable,
  modeId,
  valueRecord,
  item,
  variableByCanonicalName,
  stats,
) {
  if (!modeId || !valueRecord) return;

  try {
    const value = await toFigmaVariableValue(
      valueRecord,
      item,
      variableByCanonicalName,
    );
    variable.setValueForMode(modeId, value);
    stats.variableValuesSet += 1;
    if (valueRecord.kind === "alias") stats.aliasesSet += 1;
  } catch (error) {
    stats.warnings.push(
      `${item.canonicalName}: could not set ${modeId} (${messageFor(error)})`,
    );
  }
}

async function toFigmaVariableValue(
  valueRecord,
  item,
  variableByCanonicalName,
) {
  if (valueRecord.kind === "alias") {
    const target = variableByCanonicalName.get(valueRecord.path);
    if (!target) {
      throw new Error(`missing alias target ${valueRecord.path}`);
    }

    if (figma.variables.createVariableAliasByIdAsync) {
      return figma.variables.createVariableAliasByIdAsync(target.id);
    }

    return { type: "VARIABLE_ALIAS", id: target.id };
  }

  if (item.figmaType === "COLOR") {
    return parseColor(valueRecord.value);
  }

  if (item.figmaType === "FLOAT") {
    const numberValue =
      typeof valueRecord.value === "number"
        ? valueRecord.value
        : Number.parseFloat(String(valueRecord.value));

    if (Number.isNaN(numberValue)) {
      throw new Error(`expected number, received ${valueRecord.value}`);
    }

    return numberValue;
  }

  if (item.figmaType === "BOOLEAN") {
    return Boolean(valueRecord.value);
  }

  return String(valueRecord.value);
}

function parseColor(value) {
  if (typeof value !== "string") {
    throw new Error(`expected color string, received ${typeof value}`);
  }

  const raw = value.trim().toLowerCase();
  if (raw === "transparent") {
    return { r: 0, g: 0, b: 0, a: 0 };
  }

  const hex = raw.replace(/^#/, "");
  if (/^[0-9a-f]{3}$/.test(hex)) {
    return {
      r: Number.parseInt(hex[0] + hex[0], 16) / 255,
      g: Number.parseInt(hex[1] + hex[1], 16) / 255,
      b: Number.parseInt(hex[2] + hex[2], 16) / 255,
      a: 1,
    };
  }

  if (/^[0-9a-f]{6}([0-9a-f]{2})?$/.test(hex)) {
    return {
      r: Number.parseInt(hex.slice(0, 2), 16) / 255,
      g: Number.parseInt(hex.slice(2, 4), 16) / 255,
      b: Number.parseInt(hex.slice(4, 6), 16) / 255,
      a: hex.length === 8 ? Number.parseInt(hex.slice(6, 8), 16) / 255 : 1,
    };
  }

  const rgba = raw.match(/^rgba?\(([^)]+)\)$/);
  if (rgba) {
    const parts = rgba[1].split(",").map((part) => part.trim());
    if (parts.length < 3) throw new Error(`invalid rgb color ${value}`);
    return {
      r: Number.parseFloat(parts[0]) / 255,
      g: Number.parseFloat(parts[1]) / 255,
      b: Number.parseFloat(parts[2]) / 255,
      a: parts[3] === undefined ? 1 : Number.parseFloat(parts[3]),
    };
  }

  throw new Error(`unsupported color ${value}`);
}

function setCodeSyntax(variable, item, stats) {
  if (!variable.setVariableCodeSyntax || !item.syntax) return;

  const mappings = [
    ["WEB", item.syntax.web],
    ["iOS", item.syntax.ios],
    ["ANDROID", item.syntax.android],
  ];

  for (const [platform, syntax] of mappings) {
    if (!syntax) continue;

    try {
      variable.setVariableCodeSyntax(platform, syntax);
      stats.syntaxSet += 1;
    } catch (error) {
      stats.warnings.push(
        `${item.canonicalName}: could not set ${platform} syntax (${messageFor(error)})`,
      );
    }
  }
}

async function ensureDocumentation(payload, pages, stats) {
  const foundationsPage =
    pages.get("Foundations") ||
    figma.root.children.find((page) => page.name === "Foundations");
  if (!foundationsPage) return;

  await foundationsPage.loadAsync();
  await figma.setCurrentPageAsync(foundationsPage);

  if (
    foundationsPage.findOne(
      (node) => node.name === "Kozmos DS / Foundations Overview",
    )
  ) {
    return;
  }

  let fonts;
  try {
    fonts = await loadButtonFonts(stats);
  } catch (error) {
    stats.warnings.push(
      `Could not create documentation text (${messageFor(error)})`,
    );
    return;
  }

  const frame = figma.createFrame();
  frame.name = "Kozmos DS / Foundations Overview";
  frame.resize(960, 720);
  frame.x = 80;
  frame.y = 80;
  frame.fills = [{ type: "SOLID", color: { r: 1, g: 1, b: 1 } }];
  frame.layoutMode = "VERTICAL";
  frame.itemSpacing = 20;
  frame.paddingTop = 48;
  frame.paddingRight = 48;
  frame.paddingBottom = 48;
  frame.paddingLeft = 48;
  frame.primaryAxisSizingMode = "AUTO";
  frame.counterAxisSizingMode = "FIXED";
  frame.setSharedPluginData(RUN_NAMESPACE, "kind", "documentation");

  const title = figma.createText();
  title.name = "Title";
  title.fontName = fonts.bold;
  title.fontSize = 32;
  title.characters = "Kozmos Foundations";

  const summary = figma.createText();
  summary.name = "Summary";
  summary.fontName = fonts.regular;
  summary.fontSize = 16;
  summary.lineHeight = { unit: "PIXELS", value: 24 };
  summary.characters = [
    `Target file: ${payload.target && payload.target.fileName ? payload.target.fileName : figma.root.name}`,
    `Modes: ${(payload.target && payload.target.modes ? payload.target.modes : ["Light", "Dark"]).join(", ")}`,
    `Variable candidates: ${payload.summary ? payload.summary.variableTokens : payload.variables.length}`,
    `Style/documentation-only tokens: ${payload.summary ? payload.summary.styleOnlyTokens : 0}`,
    "",
    "This page was generated from docs/figma-foundations-payload.json. Treat code tokens as the source of truth.",
  ].join("\n");

  frame.appendChild(title);
  frame.appendChild(summary);
  foundationsPage.appendChild(frame);
  stats.docsCreated += 1;
}

function messageFor(error) {
  return error instanceof Error ? error.message : String(error);
}

function compactConfigValue(value, fallback) {
  if (typeof value !== "string") return fallback;
  const trimmed = value.trim();
  return trimmed || fallback;
}

function normalizeFontConfig(config) {
  const source = config && typeof config === "object" ? config : {};
  return {
    family: compactConfigValue(source.family, DEFAULT_FONT_CONFIG.family),
    regularStyle: compactConfigValue(
      source.regularStyle,
      DEFAULT_FONT_CONFIG.regularStyle,
    ),
    mediumStyle: compactConfigValue(
      source.mediumStyle,
      DEFAULT_FONT_CONFIG.mediumStyle,
    ),
    boldStyle: compactConfigValue(
      source.boldStyle,
      DEFAULT_FONT_CONFIG.boldStyle,
    ),
  };
}

function fontConfigKey(config) {
  const normalized = normalizeFontConfig(config);
  return [
    normalized.family,
    normalized.regularStyle,
    normalized.mediumStyle,
    normalized.boldStyle,
  ].join("\u0001");
}

function resetTextStyleCache() {
  activeTextStyleByKey = {};
  activeTextStyleSpecByKey = {};
  activeTextStylesEnsured = false;
}

function configureFontConfig(config) {
  const nextConfig = normalizeFontConfig(config);
  const nextKey = fontConfigKey(nextConfig);
  if (nextKey !== activeFontConfigKey) {
    resetTextStyleCache();
  }
  activeFontConfig = nextConfig;
  activeFontConfigKey = nextKey;
  return activeFontConfig;
}

function currentFontConfig() {
  return activeFontConfig || DEFAULT_FONT_CONFIG;
}

function fontLabel(font) {
  if (!font || typeof font !== "object" || !font.family || !font.style) {
    return "Mixed or unknown font";
  }
  return `${font.family} ${font.style}`;
}

function sameFont(left, right) {
  return (
    !!left &&
    !!right &&
    left.family === right.family &&
    left.style === right.style
  );
}

function compactFontCandidates(candidates) {
  const result = [];
  const seen = new Set();
  for (const candidate of candidates) {
    if (!candidate || !candidate.family || !candidate.style) continue;
    const key = `${candidate.family}\u0001${candidate.style}`;
    if (seen.has(key)) continue;
    seen.add(key);
    result.push({ family: candidate.family, style: candidate.style });
  }
  return result;
}

function fontCandidatesForRole(role, fallbackFont) {
  const config = currentFontConfig();
  const family = config.family;

  if (role === "regular") {
    return compactFontCandidates([
      { family, style: config.regularStyle },
      { family, style: "Regular" },
      { family, style: "Book" },
      { family, style: "Medium" },
      {
        family: DEFAULT_FONT_CONFIG.family,
        style: DEFAULT_FONT_CONFIG.regularStyle,
      },
      { family: "Inter", style: "Regular" },
      { family: "Inter", style: "Medium" },
      { family: "Arial", style: "Regular" },
    ]);
  }

  if (role === "medium") {
    return compactFontCandidates([
      { family, style: config.mediumStyle },
      { family, style: "Medium" },
      { family, style: "SemiBold" },
      { family, style: "Semi Bold" },
      { family, style: config.regularStyle },
      { family, style: "Regular" },
      fallbackFont,
      {
        family: DEFAULT_FONT_CONFIG.family,
        style: DEFAULT_FONT_CONFIG.mediumStyle,
      },
      { family: DEFAULT_FONT_CONFIG.family, style: "SemiBold" },
      { family: DEFAULT_FONT_CONFIG.family, style: "Regular" },
      { family: "Inter", style: "Medium" },
      { family: "Inter", style: "Semi Bold" },
      { family: "Inter", style: "Bold" },
      fallbackFont,
    ]);
  }

  return compactFontCandidates([
    { family, style: config.boldStyle },
    { family, style: "Bold" },
    { family, style: "SemiBold" },
    { family, style: "Semi Bold" },
    { family, style: "Medium" },
    fallbackFont,
    {
      family: DEFAULT_FONT_CONFIG.family,
      style: DEFAULT_FONT_CONFIG.boldStyle,
    },
    { family: DEFAULT_FONT_CONFIG.family, style: "SemiBold" },
    { family: DEFAULT_FONT_CONFIG.family, style: "Medium" },
    { family: "Inter", style: "Bold" },
    { family: "Inter", style: "Semi Bold" },
    fallbackFont,
  ]);
}

function incrementStat(stats, key, amount) {
  if (!stats) return;
  stats[key] = (stats[key] || 0) + (amount || 1);
}

function pushUniqueWarning(stats, key, message) {
  if (!stats) return;
  if (!stats.__warningKeys) {
    Object.defineProperty(stats, "__warningKeys", {
      value: {},
      enumerable: false,
    });
  }

  if (stats.__warningKeys[key]) return;
  stats.__warningKeys[key] = true;
  stats.warnings.push(message);
}

async function ensureComponentRuntimeVariables(stats) {
  const collection = await ensureComponentVariableCollection(stats);
  const modeIds = modeIdsFor(collection);
  const existingVariables = await figma.variables.getLocalVariablesAsync();
  const variableByCollectionAndName = new Map();
  const variableByName = new Map();

  for (const variable of existingVariables) {
    variableByCollectionAndName.set(
      `${variable.variableCollectionId}:${variable.name}`,
      variable,
    );
    variableByName.set(variable.name, variable);
  }

  for (const token of COMPONENT_FLOAT_TOKENS) {
    const key = `${collection.id}:${token.name}`;
    let variable = variableByCollectionAndName.get(key);

    if (!variable) {
      variable = figma.variables.createVariable(
        token.name,
        collection,
        "FLOAT",
      );
      variable.setSharedPluginData(RUN_NAMESPACE, "kind", "component-token");
      variable.setSharedPluginData(
        RUN_NAMESPACE,
        "cssVariable",
        cssVariableForComponentToken(token.name),
      );
      variableByCollectionAndName.set(key, variable);
      variableByName.set(token.name, variable);
      incrementStat(stats, "componentVariablesCreated");
    } else {
      incrementStat(stats, "componentVariablesExisting");
    }

    setVariableScopes(variable, token.scopes || [], token.name, stats);
    setVariableWebSyntax(
      variable,
      `var(${cssVariableForComponentToken(token.name)})`,
      token.name,
      stats,
    );

    for (const modeId of [modeIds.Light, modeIds.Dark]) {
      if (!modeId) continue;
      try {
        const value = await componentTokenModeValue(
          token,
          variableByName,
          stats,
        );
        variable.setValueForMode(modeId, value);
        incrementStat(stats, "componentVariableValuesSet");
        if (token.alias && value && value.type === "VARIABLE_ALIAS") {
          incrementStat(stats, "componentVariableAliasesSet");
        }
      } catch (error) {
        stats.warnings.push(
          `${token.name}: could not set component token value (${messageFor(error)})`,
        );
      }
    }
  }

  const variables = await figma.variables.getLocalVariablesAsync();
  return new Map(variables.map((variable) => [variable.name, variable]));
}

async function componentTokenModeValue(token, variableByName, stats) {
  if (!token.alias) return token.value;

  const target = variableByName.get(token.alias);
  if (!target) {
    pushUniqueWarning(
      stats,
      `missing-component-alias:${token.name}:${token.alias}`,
      `${token.name}: alias target "${token.alias}" was not found; using ${token.value}.`,
    );
    return token.value;
  }

  if (figma.variables.createVariableAliasByIdAsync) {
    try {
      return await figma.variables.createVariableAliasByIdAsync(target.id);
    } catch (error) {
      pushUniqueWarning(
        stats,
        `component-alias-failed:${token.name}:${token.alias}`,
        `${token.name}: could not alias to "${token.alias}" (${messageFor(error)}); using ${token.value}.`,
      );
      return token.value;
    }
  }

  return { type: "VARIABLE_ALIAS", id: target.id };
}

async function ensureComponentVariableCollection(stats) {
  const collections = await figma.variables.getLocalVariableCollectionsAsync();
  let collection = collections.find(
    (candidate) => candidate.name === COMPONENT_COLLECTION_NAME,
  );

  if (!collection) {
    collection = figma.variables.createVariableCollection(
      COMPONENT_COLLECTION_NAME,
    );
    collection.setSharedPluginData(RUN_NAMESPACE, "kind", "collection");
    incrementStat(stats, "componentCollectionsCreated");
  }

  ensureModeNames(collection, ["Light", "Dark"], stats);
  return collection;
}

function ensureModeNames(collection, names, stats) {
  const desired = names && names.length > 0 ? names : ["Light", "Dark"];
  let modes = collection.modes;

  if (!modes.some((mode) => mode.name === desired[0])) {
    try {
      collection.renameMode(modes[0].modeId, desired[0]);
      modes = collection.modes;
    } catch (error) {
      stats.warnings.push(
        `${collection.name}: could not rename first mode (${messageFor(error)})`,
      );
    }
  }

  for (const name of desired) {
    if (collection.modes.some((mode) => mode.name === name)) {
      incrementStat(stats, "componentModesExisting");
      continue;
    }

    try {
      collection.addMode(name);
      incrementStat(stats, "componentModesCreated");
    } catch (error) {
      stats.warnings.push(
        `${collection.name}: could not add ${name} mode (${messageFor(error)})`,
      );
    }
  }
}

function setVariableScopes(variable, scopes, name, stats) {
  if (!scopes || scopes.length === 0) return;

  try {
    variable.scopes = scopes;
    incrementStat(stats, "componentVariableScopesSet");
  } catch (error) {
    stats.warnings.push(
      `${name}: could not set component token scopes (${messageFor(error)})`,
    );
  }
}

function setVariableWebSyntax(variable, syntax, name, stats) {
  if (!variable.setVariableCodeSyntax || !syntax) return;

  try {
    variable.setVariableCodeSyntax("WEB", syntax);
    incrementStat(stats, "componentVariableSyntaxSet");
  } catch (error) {
    stats.warnings.push(
      `${name}: could not set component token code syntax (${messageFor(error)})`,
    );
  }
}

function cssVariableForComponentToken(name) {
  return `--components-${name
    .replace(/([a-z])([A-Z])/g, "$1-$2")
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/^-|-$/g, "")}`;
}

async function getLocalTextStylesSafe() {
  if (figma.getLocalTextStylesAsync) {
    return figma.getLocalTextStylesAsync();
  }

  if (figma.getLocalTextStyles) {
    return figma.getLocalTextStyles();
  }

  return [];
}

async function ensureButtonLabelTextStyle(fonts, stats) {
  const styles = await ensureKozmosTextStyles(fonts, stats);
  return styles.buttonLabel || null;
}

async function ensureKozmosTextStyles(fonts, stats) {
  if (activeTextStylesEnsured) return activeTextStyleByKey;

  activeTextStyleByKey = {};
  activeTextStyleSpecByKey = {};
  const specs = kozmosTextStyleSpecs(fonts);
  for (const spec of specs) {
    activeTextStyleSpecByKey[spec.key] = spec;
  }

  if (!figma.createTextStyle) {
    pushUniqueWarning(
      stats,
      "missing-create-text-style",
      "This Figma runtime does not expose createTextStyle; component text will use direct typography values.",
    );
    activeTextStylesEnsured = true;
    return activeTextStyleByKey;
  }

  const existingStyles = await getLocalTextStylesSafe();
  const styleByName = {};
  for (const style of existingStyles) {
    styleByName[style.name] = style;
  }

  for (const spec of specs) {
    let style = styleByName[spec.name];
    if (!style) {
      style = figma.createTextStyle();
      style.name = spec.name;
      incrementStat(stats, "textStylesCreated");
    } else {
      incrementStat(stats, "textStylesExisting");
    }

    try {
      style.description = spec.description;
      style.fontName = spec.fontName;
      style.fontSize = spec.fontSize;
      style.lineHeight = { unit: "PIXELS", value: spec.lineHeight };
      style.letterSpacing = { unit: "PERCENT", value: 0 };
      style.paragraphSpacing = 0;
      activeTextStyleByKey[spec.key] = style;
      incrementStat(stats, "textStylesUpdated");
    } catch (error) {
      pushUniqueWarning(
        stats,
        `text-style-update:${spec.name}`,
        `${spec.name}: could not update text style (${messageFor(error)})`,
      );
    }

    if (activeTextStyleByKey[spec.key] && "textDecoration" in style) {
      try {
        style.textDecoration = spec.textDecoration || "NONE";
      } catch (error) {
        pushUniqueWarning(
          stats,
          `text-style-decoration:${spec.name}`,
          `${spec.name}: could not update text decoration (${messageFor(error)}).`,
        );
      }
    }
  }

  activeTextStylesEnsured = true;
  return activeTextStyleByKey;
}

function kozmosTextStyleSpecs(fonts) {
  const specs = [];

  for (const size of TEXT_SIZES) {
    const metrics = textMetrics(size);
    for (const weight of TEXT_WEIGHTS) {
      specs.push({
        key: textStyleKeyForText(size, weight),
        name: `${TEXT_STYLE_GROUP_TEXT} / ${size} / ${weight}`,
        fontName: textFontForWeight(weight, fonts),
        fontSize: metrics.fontSize,
        lineHeight: metrics.lineHeight,
        description:
          "Kozmos text typography style. Apply color separately through semantic foreground variables.",
      });
    }
  }

  for (const level of HEADING_LEVELS) {
    const metrics = headingMetrics(level);
    specs.push({
      key: textStyleKeyForHeading(level),
      name: `${TEXT_STYLE_GROUP_HEADING} / ${level}`,
      fontName: fonts.bold || fonts.medium || fonts.regular,
      fontSize: metrics.fontSize,
      lineHeight: metrics.lineHeight,
      description:
        "Kozmos heading typography style. Heading level semantics are handled by code; this style controls visual type only.",
    });
  }

  addComponentTextStyleSpecs(specs, fonts);
  return specs;
}

function addComponentTextStyleSpecs(specs, fonts) {
  addTextStyleSpec(
    specs,
    "buttonLabel",
    BUTTON_LABEL_TEXT_STYLE_NAME,
    fonts.medium,
    14,
    20,
    "Typography contract for Kozmos Button labels.",
  );
  addTextStyleSpec(
    specs,
    "badgeLabel",
    "Badge / Label",
    fonts.medium,
    14,
    20,
    "Typography contract for Kozmos Badge labels.",
  );
  addTextStyleSpec(
    specs,
    "chipLabelSmall",
    "Chip / Label Small",
    fonts.medium,
    12,
    16,
    "Typography contract for small Kozmos Chip labels.",
  );
  addTextStyleSpec(
    specs,
    "chipLabel",
    "Chip / Label",
    fonts.medium,
    14,
    20,
    "Typography contract for Kozmos Chip labels.",
  );
  addTextStyleSpec(
    specs,
    "segmentedControlLabelSmall",
    "SegmentedControl / Label Small",
    fonts.medium,
    12,
    16,
    "Typography contract for small Kozmos SegmentedControl labels.",
  );
  addTextStyleSpec(
    specs,
    "segmentedControlLabel",
    "SegmentedControl / Label",
    fonts.medium,
    14,
    20,
    "Typography contract for Kozmos SegmentedControl labels.",
  );
  addTextStyleSpec(
    specs,
    "counterSmall",
    "Counter / Small",
    fonts.medium,
    11,
    14,
    "Typography contract for small Kozmos counters.",
  );
  addTextStyleSpec(
    specs,
    "counterDefault",
    "Counter / Default",
    fonts.medium,
    12,
    16,
    "Typography contract for default Kozmos counters.",
  );
  addTextStyleSpec(specs, "cardTitle", "Card / Title", fonts.medium, 24, 24);
  addTextStyleSpec(
    specs,
    "cardDescription",
    "Card / Description",
    fonts.regular,
    14,
    20,
  );
  addTextStyleSpec(specs, "cardBody", "Card / Body", fonts.regular, 14, 22);
  addTextStyleSpec(specs, "listItem", "List / Item", fonts.regular, 14, 20);
  addTextStyleSpec(specs, "treeItem", "Tree / Item", fonts.regular, 14, 20);
  addTextStyleSpec(
    specs,
    "navigationItem",
    "NavigationItem / Label",
    fonts.medium,
    14,
    20,
  );
  addTextStyleSpec(
    specs,
    "navigationItemRail",
    "NavigationItem / Rail Label",
    fonts.medium,
    12,
    16,
  );
  addTextStyleSpec(specs, "navbarLogo", "Navbar / Logo", fonts.medium, 14, 20);
  addTextStyleSpec(specs, "navbarItem", "Navbar / Item", fonts.medium, 14, 20);
  addTextStyleSpec(
    specs,
    "navbarAction",
    "Navbar / Action",
    fonts.medium,
    14,
    20,
  );
  addTextStyleSpec(
    specs,
    "sidebarTitle",
    "Sidebar / Title",
    fonts.medium,
    14,
    20,
  );
  addTextStyleSpec(
    specs,
    "sidebarSection",
    "Sidebar / Section",
    fonts.medium,
    12,
    16,
  );
  addTextStyleSpec(
    specs,
    "sidebarItem",
    "Sidebar / Item",
    fonts.regular,
    14,
    20,
  );
  addTextStyleSpec(
    specs,
    "tableHeader",
    "Table / Header",
    fonts.medium,
    12,
    16,
  );
  addTextStyleSpec(specs, "tableCell", "Table / Cell", fonts.regular, 14, 20);
  addTextStyleSpec(
    specs,
    "tabsTrigger",
    "Tabs / Trigger",
    fonts.medium,
    14,
    20,
  );
  addTextStyleSpec(
    specs,
    "tooltipContent",
    "Tooltip / Content",
    fonts.regular,
    14,
    20,
  );
  addTextStyleSpec(
    specs,
    "dialogTitle",
    "Dialog / Title",
    fonts.medium,
    18,
    24,
  );
  addTextStyleSpec(
    specs,
    "dialogDescription",
    "Dialog / Description",
    fonts.regular,
    14,
    20,
  );
  addTextStyleSpec(specs, "dialogBody", "Dialog / Body", fonts.regular, 14, 20);
  addTextStyleSpec(
    specs,
    "popoverTitle",
    "Popover / Title",
    fonts.medium,
    14,
    20,
  );
  addTextStyleSpec(
    specs,
    "popoverDescription",
    "Popover / Description",
    fonts.regular,
    14,
    20,
  );
  addTextStyleSpec(specs, "menuItem", "Menu / Item", fonts.regular, 14, 20);
  addTextStyleSpec(
    specs,
    "menuItemStrong",
    "Menu / Item Strong",
    fonts.medium,
    14,
    20,
  );
  addTextStyleSpec(
    specs,
    "menuShortcut",
    "Menu / Shortcut",
    fonts.medium,
    12,
    16,
  );
  addTextStyleSpec(specs, "toastTitle", "Toast / Title", fonts.medium, 14, 20);
  addTextStyleSpec(
    specs,
    "toastDescription",
    "Toast / Description",
    fonts.regular,
    14,
    20,
  );
  addTextStyleSpec(
    specs,
    "controlLabel",
    "Control / Label",
    fonts.medium,
    14,
    20,
  );
  addTextStyleSpec(specs, "fieldLabel", "Field / Label", fonts.medium, 14, 20);
  addTextStyleSpec(specs, "fieldText", "Field / Text", fonts.regular, 14, 20);
  addTextStyleSpec(specs, "fieldMeta", "Field / Meta", fonts.regular, 12, 16);
  addTextStyleSpec(
    specs,
    "avatarFallback",
    "Avatar / Fallback",
    fonts.medium,
    14,
    20,
  );
  addTextStyleSpec(specs, "alertTitle", "Alert / Title", fonts.medium, 14, 20);
  addTextStyleSpec(
    specs,
    "alertDescription",
    "Alert / Description",
    fonts.regular,
    14,
    20,
  );
  addTextStyleSpec(
    specs,
    "emptyStateTitle",
    "EmptyState / Title",
    fonts.medium,
    16,
    20,
  );
  addTextStyleSpec(
    specs,
    "emptyStateDescription",
    "EmptyState / Description",
    fonts.regular,
    14,
    20,
  );
  addTextStyleSpec(
    specs,
    "linkText",
    "Link / Text",
    fonts.medium,
    14,
    20,
    "Typography contract for Kozmos Link labels.",
    "UNDERLINE",
  );
  addTextStyleSpec(specs, "labelText", "Label / Text", fonts.medium, 14, 20);
  addTextStyleSpec(specs, "boxText", "Box / Text", fonts.regular, 14, 20);
  addTextStyleSpec(
    specs,
    "containerText",
    "Container / Text",
    fonts.regular,
    14,
    20,
  );
  addTextStyleSpec(
    specs,
    "breadcrumbItem",
    "Breadcrumb / Item",
    fonts.regular,
    14,
    20,
  );
  addTextStyleSpec(
    specs,
    "paginationLabelSmall",
    "Pagination / Label Small",
    fonts.medium,
    12,
    16,
    "Typography contract for small Kozmos Pagination labels.",
  );
  addTextStyleSpec(
    specs,
    "paginationLabel",
    "Pagination / Label",
    fonts.medium,
    14,
    20,
    "Typography contract for Kozmos Pagination labels.",
  );
  addTextStyleSpec(
    specs,
    "accordionTrigger",
    "Accordion / Trigger",
    fonts.regular,
    14,
    20,
  );
  addTextStyleSpec(
    specs,
    "accordionContent",
    "Accordion / Content",
    fonts.regular,
    14,
    20,
  );
}

function addTextStyleSpec(
  specs,
  key,
  name,
  fontName,
  fontSize,
  lineHeight,
  description,
  textDecoration,
) {
  specs.push({
    key,
    name,
    fontName,
    fontSize,
    lineHeight,
    description:
      description ||
      "Kozmos component typography style. Apply color separately through semantic foreground variables.",
    textDecoration,
  });
}

function textStyleKeyForText(size, weight) {
  return `text.${size}.${weight}`;
}

function textStyleKeyForHeading(level) {
  return `heading.${level}`;
}

async function applyTextStyleToNodeAsync(text, key, stats) {
  const style = activeTextStyleByKey[key];
  const spec = activeTextStyleSpecByKey[key];
  if ((!style || !style.id) && !spec) return false;

  let applied = false;

  if (spec) {
    try {
      text.fontName = spec.fontName;
      text.fontSize = spec.fontSize;
      text.lineHeight = { unit: "PIXELS", value: spec.lineHeight };
      text.letterSpacing = { unit: "PERCENT", value: 0 };
      if ("textDecoration" in text) {
        text.textDecoration = spec.textDecoration || "NONE";
      }
      incrementStat(stats, "textStyleTypographyFieldsApplied");
      applied = true;
    } catch (error) {
      pushUniqueWarning(
        stats,
        `text-style-direct-fields:${key}`,
        `Could not apply typography fields for text style "${key}" (${messageFor(error)}).`,
      );
    }
  }

  if (!style || !style.id) return applied;

  try {
    if (text.setTextStyleIdAsync) {
      await text.setTextStyleIdAsync(style.id);
    } else {
      text.textStyleId = style.id;
    }
    incrementStat(stats, "textStyleBindingsApplied");
    applied = true;
  } catch (error) {
    pushUniqueWarning(
      stats,
      `text-style-bind:${key}`,
      `Could not apply text style "${style.name}" (${messageFor(error)}).`,
    );
  }

  return applied;
}

async function applyTextStylesToComponentLibrary() {
  const stats = {
    updated: false,
    componentSetsVisited: 0,
    textNodesVisited: 0,
    textNodesStyled: 0,
    textNodesSkippedInInstances: 0,
    textNodesUnmatched: 0,
    warnings: [],
  };

  await loadButtonFonts(stats);

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  const componentSets = page.findAll((node) => node.type === "COMPONENT_SET");

  for (const componentSet of componentSets) {
    stats.componentSetsVisited += 1;
    const textNodes = componentSet.findAll((node) => node.type === "TEXT");

    for (const text of textNodes) {
      stats.textNodesVisited += 1;

      if (hasAncestorTypeBefore(text, "INSTANCE", componentSet)) {
        stats.textNodesSkippedInInstances += 1;
        continue;
      }

      const key = inferTextStyleKeyForComponentText(text, componentSet);
      if (key && (await applyTextStyleToNodeAsync(text, key, stats))) {
        stats.textNodesStyled += 1;
      } else {
        stats.textNodesUnmatched += 1;
      }
    }
  }

  stats.updated = true;
  stats.message = `Applied text styles to ${stats.textNodesStyled} component text node(s).`;
  return stats;
}

function hasAncestorTypeBefore(node, type, boundary) {
  let parent = node.parent;
  while (parent && parent !== boundary) {
    if (parent.type === type) return true;
    parent = parent.parent;
  }
  return false;
}

function nearestAncestorOfTypeBefore(node, type, boundary) {
  let parent = node.parent;
  while (parent && parent !== boundary) {
    if (parent.type === type) return parent;
    parent = parent.parent;
  }
  return null;
}

function textStyleKeyForNavbarText(textName) {
  if (textName === "Logo Text") return "navbarLogo";
  if (textName === "Context Label Text") return "sidebarSection";
  if (textName === "Context Text") return "navbarItem";
  if (textName === "Primary Action Text" || textName === "Action Text") {
    return "navbarAction";
  }
  if (/^Item \d+ Text$/.test(textName)) return "navigationItem";
  return null;
}

function textStyleKeyForSidebarText(textName, component) {
  if (textName === "Title Text") return "sidebarTitle";
  if (textName === "Section Text") return "sidebarSection";
  if (textName === "Footer Text") return "sidebarItem";

  const props =
    component && component.name
      ? parseSidebarVariantName(component.name)
      : null;
  const navigationKey =
    props && props.content === "Rail" ? "navigationItemRail" : "navigationItem";
  if (/^Item \d+ Text$/.test(textName)) return navigationKey;
  if (textName === "Tool Text" || textName === "Settings Text") {
    return navigationKey;
  }
  return null;
}

function textStyleKeyForNavigationItemText(component) {
  const props =
    component && component.name
      ? parseNavigationItemVariantName(component.name)
      : null;
  if (props && props.placement === "Rail") return "navigationItemRail";
  return "navigationItem";
}

function inferTextStyleKeyForComponentText(text, componentSet) {
  const component = nearestAncestorOfTypeBefore(
    text,
    "COMPONENT",
    componentSet,
  );
  const setName = componentSet.name;
  const textName = text.name;

  if (setName === "Text" && component) {
    const textProps = parseTextVariantName(component.name);
    if (textProps) return textStyleKeyForText(textProps.size, textProps.weight);
  }

  if (setName === "Heading" && component) {
    const headingProps = parseHeadingVariantName(component.name);
    if (headingProps) return textStyleKeyForHeading(headingProps.value);
  }

  if (setName === "Counter" && component) {
    const counterProps = parseCounterVariantName(component.name);
    if (counterProps && counterProps.size === "Small") return "counterSmall";
    if (counterProps) return "counterDefault";
  }

  if (setName === "Chip" && component) {
    const chipProps = parseChipVariantName(component.name);
    if (chipProps && chipProps.size === "Small") return "chipLabelSmall";
    if (chipProps) return "chipLabel";
  }

  if (setName === "SegmentedControl" && component) {
    const segmentedControlProps = parseSegmentedControlVariantName(
      component.name,
    );
    if (segmentedControlProps && segmentedControlProps.size === "Small") {
      return "segmentedControlLabelSmall";
    }
    if (segmentedControlProps) return "segmentedControlLabel";
  }

  if (setName === "Button") return "buttonLabel";
  if (setName === "Badge") return "badgeLabel";
  if (setName === "Link") return "linkText";
  if (setName === "Label") return "labelText";
  if (setName === "Box") return "boxText";
  if (setName === "Container") return "containerText";
  if (setName === "Breadcrumb") return "breadcrumbItem";
  if (setName === "Pagination" && component) {
    const paginationProps = parsePaginationVariantName(component.name);
    if (paginationProps && paginationProps.size === "Small") {
      return "paginationLabelSmall";
    }
    if (paginationProps) return "paginationLabel";
  }
  if (setName === "Stepper") {
    return textName === "Step Number" ? "fieldText" : "fieldMeta";
  }
  if (setName === "Accordion") {
    return textName === "Trigger Text"
      ? "accordionTrigger"
      : "accordionContent";
  }
  if (setName === "BottomNavigation") return "fieldMeta";
  if (setName === "NavigationItem") {
    return textStyleKeyForNavigationItemText(component);
  }
  if (setName === "Navbar") {
    return textStyleKeyForNavbarText(textName);
  }
  if (setName === "Sidebar") {
    return textStyleKeyForSidebarText(textName, component);
  }
  if (setName === "Tabs") return "tabsTrigger";
  if (setName === "Tooltip") return "tooltipContent";
  if (setName === "Avatar") return "avatarFallback";
  if (setName === "Alert") {
    return textName === "Title" ? "alertTitle" : "alertDescription";
  }
  if (setName === "EmptyState") {
    return textName === "Title Text"
      ? "emptyStateTitle"
      : "emptyStateDescription";
  }
  if (setName === "Card") {
    if (textName === "Title Text") return "cardTitle";
    if (textName === "Description Text") return "cardDescription";
    return "cardBody";
  }
  if (setName === "List") return "listItem";
  if (
    setName === "Tree" ||
    setName === "TreeItem" ||
    setName === "TreeParentItem" ||
    setName === "TreeChildItem"
  ) {
    return "treeItem";
  }
  if (setName === "Table") {
    if (/^Header \d+ Text$/.test(textName)) return "tableHeader";
    return "tableCell";
  }
  if (setName === "Timeline") {
    if (/^Item \d+ Time$/.test(textName)) return "fieldMeta";
    return "listItem";
  }
  if (setName === "Dialog") {
    if (textName === "Title Text") return "dialogTitle";
    if (textName === "Description Text") return "dialogDescription";
    return "dialogBody";
  }
  if (setName === "Popover") {
    if (textName === "Title Text") return "popoverTitle";
    return "popoverDescription";
  }
  if (setName === "Menu") {
    if (textName === "Shortcut Text") return "menuShortcut";
    if (textName === "Label Text") return "menuItemStrong";
    return "menuItem";
  }
  if (setName === "Toast") {
    if (textName === "Description Text") return "toastDescription";
    return "toastTitle";
  }
  if (setName === "Checkbox" || setName === "Radio" || setName === "Switch") {
    return "controlLabel";
  }
  if (
    setName === "FormField" ||
    setName === "Input" ||
    setName === "PasswordInput" ||
    setName === "NumberInput" ||
    setName === "Textarea" ||
    setName === "Search" ||
    setName === "Select" ||
    setName === "DatePicker" ||
    setName === "DateRangePicker" ||
    setName === "TimePicker" ||
    setName === "FileUpload" ||
    setName === "ColorPicker" ||
    setName === "Slider"
  ) {
    if (textName === "Optional Text") return "fieldMeta";
    return textName === "Label Text" ? "fieldLabel" : "fieldText";
  }

  return null;
}

async function applyButtonLabelTypography(
  label,
  fonts,
  textStyle,
  variableByName,
  stats,
) {
  label.fontName = fonts.medium;
  label.fontSize = 14;
  label.lineHeight = { unit: "PIXELS", value: 20 };
  label.textAutoResize = "WIDTH_AND_HEIGHT";

  if (textStyle && textStyle.id) {
    try {
      if (label.setTextStyleIdAsync) {
        await label.setTextStyleIdAsync(textStyle.id);
      } else {
        label.textStyleId = textStyle.id;
      }
      incrementStat(stats, "textStyleBindingsApplied");
    } catch (error) {
      stats.warnings.push(
        `Could not apply ${BUTTON_LABEL_TEXT_STYLE_NAME} to Button label (${messageFor(error)}).`,
      );
    }
  }

  bindFloatVariable(
    label,
    "fontSize",
    "Button/label/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    label,
    "lineHeight",
    "Button/label/line-height",
    variableByName,
    stats,
  );
}

async function applyTextTypography(
  text,
  size,
  weight,
  fonts,
  variableByName,
  stats,
) {
  const metrics = textMetrics(size);
  text.fontName = textFontForWeight(weight, fonts);
  text.fontSize = metrics.fontSize;
  text.lineHeight = { unit: "PIXELS", value: metrics.lineHeight };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(
    text,
    textStyleKeyForText(size, weight),
    stats,
  );

  bindFloatVariable(
    text,
    "fontSize",
    `Text/font-size/${metrics.token}`,
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    `Text/line-height/${metrics.token}`,
    variableByName,
    stats,
  );
}

async function applyBadgeLabelTypography(label, fonts, variableByName, stats) {
  label.fontName = fonts.medium;
  label.fontSize = 14;
  label.lineHeight = { unit: "PIXELS", value: 20 };
  label.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(label, "badgeLabel", stats);

  bindFloatVariable(
    label,
    "fontSize",
    "Badge/label/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    label,
    "lineHeight",
    "Badge/label/line-height",
    variableByName,
    stats,
  );
}

async function applyChipLabelTypography(
  label,
  size,
  fonts,
  variableByName,
  stats,
) {
  const metrics = chipMetrics(size);
  label.fontName = fonts.medium;
  label.fontSize = metrics.fontSize;
  label.lineHeight = { unit: "PIXELS", value: metrics.lineHeight };
  label.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(
    label,
    size === "Small" ? "chipLabelSmall" : "chipLabel",
    stats,
  );

  bindFloatVariable(
    label,
    "fontSize",
    metrics.fontSizeToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    label,
    "lineHeight",
    metrics.lineHeightToken,
    variableByName,
    stats,
  );
}

async function applySegmentedControlLabelTypography(
  label,
  metrics,
  fonts,
  variableByName,
  stats,
) {
  label.fontName = fonts.medium;
  label.fontSize = metrics.fontSize;
  label.lineHeight = { unit: "PIXELS", value: metrics.lineHeight };
  label.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(
    label,
    metrics.size === "Small"
      ? "segmentedControlLabelSmall"
      : "segmentedControlLabel",
    stats,
  );

  bindFloatVariable(
    label,
    "fontSize",
    metrics.fontSizeToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    label,
    "lineHeight",
    metrics.lineHeightToken,
    variableByName,
    stats,
  );
}

async function applyCounterTypography(
  label,
  size,
  fonts,
  variableByName,
  stats,
) {
  const metrics = counterMetrics(size);
  label.fontName = fonts.medium;
  label.fontSize = metrics.fontSize;
  label.lineHeight = { unit: "PIXELS", value: metrics.lineHeight };
  label.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(
    label,
    size === "Small" ? "counterSmall" : "counterDefault",
    stats,
  );

  bindFloatVariable(
    label,
    "fontSize",
    metrics.fontSizeToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    label,
    "lineHeight",
    metrics.lineHeightToken,
    variableByName,
    stats,
  );
}

async function applyCardTitleTypography(text, fonts, variableByName, stats) {
  text.fontName = fonts.medium;
  text.fontSize = 24;
  text.lineHeight = { unit: "PIXELS", value: 24 };
  text.textAutoResize = "HEIGHT";
  await applyTextStyleToNodeAsync(text, "cardTitle", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Card/title/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Card/title/line-height",
    variableByName,
    stats,
  );
}

async function applyCardDescriptionTypography(
  text,
  fonts,
  variableByName,
  stats,
) {
  text.fontName = fonts.regular;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "HEIGHT";
  await applyTextStyleToNodeAsync(text, "cardDescription", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Card/description/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Card/description/line-height",
    variableByName,
    stats,
  );
}

async function applyCardBodyTypography(text, fonts, variableByName, stats) {
  text.fontName = fonts.regular;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 22 };
  text.textAutoResize = "HEIGHT";
  await applyTextStyleToNodeAsync(text, "cardBody", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Card/body/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Card/body/line-height",
    variableByName,
    stats,
  );
}

async function applyListItemTypography(text, fonts, variableByName, stats) {
  text.fontName = fonts.regular;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "TRUNCATE";
  await applyTextStyleToNodeAsync(text, "listItem", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "List/item/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "List/item/line-height",
    variableByName,
    stats,
  );
}

async function applyTreeItemTypography(text, fonts, variableByName, stats) {
  text.fontName = fonts.regular;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "TRUNCATE";
  await applyTextStyleToNodeAsync(text, "treeItem", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Tree/item/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Tree/item/line-height",
    variableByName,
    stats,
  );
}

async function applyNavigationTextTypography(
  text,
  fonts,
  {
    fontWeight,
    fontSize,
    lineHeight,
    textStyleKey,
    fontSizeToken,
    lineHeightToken,
  },
  variableByName,
  stats,
) {
  text.fontName = fontWeight === "medium" ? fonts.medium : fonts.regular;
  text.fontSize = fontSize;
  text.lineHeight = { unit: "PIXELS", value: lineHeight };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(text, textStyleKey, stats);

  bindFloatVariable(text, "fontSize", fontSizeToken, variableByName, stats);
  bindFloatVariable(text, "lineHeight", lineHeightToken, variableByName, stats);
}

async function applyNavigationItemTypography(
  text,
  fonts,
  placement,
  variableByName,
  stats,
) {
  const rail = placement === "Rail";
  await applyNavigationTextTypography(
    text,
    fonts,
    {
      fontWeight: "medium",
      fontSize: rail ? 12 : 14,
      lineHeight: rail ? 16 : 20,
      textStyleKey: rail ? "navigationItemRail" : "navigationItem",
      fontSizeToken: rail
        ? "NavigationItem/rail/font-size"
        : "NavigationItem/label/font-size",
      lineHeightToken: rail
        ? "NavigationItem/rail/line-height"
        : "NavigationItem/label/line-height",
    },
    variableByName,
    stats,
  );
}

async function applyNavbarLogoTypography(text, fonts, variableByName, stats) {
  await applyNavigationTextTypography(
    text,
    fonts,
    {
      fontWeight: "medium",
      fontSize: 14,
      lineHeight: 20,
      textStyleKey: "navbarLogo",
      fontSizeToken: "Navbar/logo/font-size",
      lineHeightToken: "Navbar/logo/line-height",
    },
    variableByName,
    stats,
  );
}

async function applyNavbarItemTypography(text, fonts, variableByName, stats) {
  await applyNavigationTextTypography(
    text,
    fonts,
    {
      fontWeight: "medium",
      fontSize: 14,
      lineHeight: 20,
      textStyleKey: "navbarItem",
      fontSizeToken: "Navbar/item/font-size",
      lineHeightToken: "Navbar/item/line-height",
    },
    variableByName,
    stats,
  );
}

async function applyNavbarActionTypography(text, fonts, variableByName, stats) {
  await applyNavigationTextTypography(
    text,
    fonts,
    {
      fontWeight: "medium",
      fontSize: 14,
      lineHeight: 20,
      textStyleKey: "navbarAction",
      fontSizeToken: "Navbar/action/font-size",
      lineHeightToken: "Navbar/action/line-height",
    },
    variableByName,
    stats,
  );
}

async function applySidebarTitleTypography(text, fonts, variableByName, stats) {
  await applyNavigationTextTypography(
    text,
    fonts,
    {
      fontWeight: "medium",
      fontSize: 14,
      lineHeight: 20,
      textStyleKey: "sidebarTitle",
      fontSizeToken: "Sidebar/title/font-size",
      lineHeightToken: "Sidebar/title/line-height",
    },
    variableByName,
    stats,
  );
}

async function applySidebarSectionTypography(
  text,
  fonts,
  variableByName,
  stats,
) {
  await applyNavigationTextTypography(
    text,
    fonts,
    {
      fontWeight: "medium",
      fontSize: 12,
      lineHeight: 16,
      textStyleKey: "sidebarSection",
      fontSizeToken: "Sidebar/section/font-size",
      lineHeightToken: "Sidebar/section/line-height",
    },
    variableByName,
    stats,
  );
}

async function applySidebarItemTypography(text, fonts, variableByName, stats) {
  await applyNavigationTextTypography(
    text,
    fonts,
    {
      fontWeight: "regular",
      fontSize: 14,
      lineHeight: 20,
      textStyleKey: "sidebarItem",
      fontSizeToken: "Sidebar/item/font-size",
      lineHeightToken: "Sidebar/item/line-height",
    },
    variableByName,
    stats,
  );
}

async function applyTimelineTitleTypography(
  text,
  fonts,
  variableByName,
  stats,
) {
  await applyListItemTypography(text, fonts, variableByName, stats);
}

async function applyTimelineDescriptionTypography(
  text,
  fonts,
  variableByName,
  stats,
) {
  await applyListItemTypography(text, fonts, variableByName, stats);
}

async function applyTimelineTimeTypography(text, fonts, variableByName, stats) {
  await applyFormFieldMetaTypography(text, fonts, variableByName, stats);
}

async function applyTableHeaderTypography(text, fonts, variableByName, stats) {
  text.fontName = fonts.medium;
  text.fontSize = 12;
  text.lineHeight = { unit: "PIXELS", value: 16 };
  text.textAutoResize = "TRUNCATE";
  await applyTextStyleToNodeAsync(text, "tableHeader", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Table/header/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Table/header/line-height",
    variableByName,
    stats,
  );
}

async function applyTableCellTypography(text, fonts, variableByName, stats) {
  text.fontName = fonts.regular;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "TRUNCATE";
  await applyTextStyleToNodeAsync(text, "tableCell", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Table/cell/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Table/cell/line-height",
    variableByName,
    stats,
  );
}

async function applyPaginationLabelTypography(
  text,
  metrics,
  fonts,
  variableByName,
  stats,
) {
  text.fontName = fonts.medium;
  text.fontSize = metrics.fontSize;
  text.lineHeight = { unit: "PIXELS", value: metrics.lineHeight };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(
    text,
    metrics.size === "Small" ? "paginationLabelSmall" : "paginationLabel",
    stats,
  );

  bindFloatVariable(
    text,
    "fontSize",
    metrics.fontSizeToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    metrics.lineHeightToken,
    variableByName,
    stats,
  );
}

async function applyTabsTriggerTypography(text, fonts, variableByName, stats) {
  text.fontName = fonts.medium;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(text, "tabsTrigger", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Tabs/trigger/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Tabs/trigger/line-height",
    variableByName,
    stats,
  );
}

async function applyTooltipContentTypography(
  text,
  fonts,
  variableByName,
  stats,
) {
  text.fontName = fonts.regular;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(text, "tooltipContent", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Tooltip/content/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Tooltip/content/line-height",
    variableByName,
    stats,
  );
}

async function applyDialogTitleTypography(text, fonts, variableByName, stats) {
  text.fontName = fonts.medium;
  text.fontSize = 18;
  text.lineHeight = { unit: "PIXELS", value: 24 };
  text.textAutoResize = "HEIGHT";
  await applyTextStyleToNodeAsync(text, "dialogTitle", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Dialog/title/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Dialog/title/line-height",
    variableByName,
    stats,
  );
}

async function applyDialogDescriptionTypography(
  text,
  fonts,
  variableByName,
  stats,
) {
  text.fontName = fonts.regular;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "HEIGHT";
  await applyTextStyleToNodeAsync(text, "dialogDescription", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Dialog/description/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Dialog/description/line-height",
    variableByName,
    stats,
  );
}

async function applyDialogBodyTypography(text, fonts, variableByName, stats) {
  text.fontName = fonts.regular;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "HEIGHT";
  await applyTextStyleToNodeAsync(text, "dialogBody", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Dialog/body/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Dialog/body/line-height",
    variableByName,
    stats,
  );
}

async function applyDrawerTitleTypography(text, fonts, variableByName, stats) {
  text.fontName = fonts.medium;
  text.fontSize = 18;
  text.lineHeight = { unit: "PIXELS", value: 24 };
  text.textAutoResize = "HEIGHT";
  await applyTextStyleToNodeAsync(text, "dialogTitle", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Drawer/title/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Drawer/title/line-height",
    variableByName,
    stats,
  );
}

async function applyDrawerDescriptionTypography(
  text,
  fonts,
  variableByName,
  stats,
) {
  text.fontName = fonts.regular;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "HEIGHT";
  await applyTextStyleToNodeAsync(text, "dialogDescription", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Drawer/description/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Drawer/description/line-height",
    variableByName,
    stats,
  );
}

async function applyDrawerBodyTypography(text, fonts, variableByName, stats) {
  text.fontName = fonts.regular;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "HEIGHT";
  await applyTextStyleToNodeAsync(text, "dialogBody", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Drawer/body/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Drawer/body/line-height",
    variableByName,
    stats,
  );
}

async function applyPopoverTitleTypography(text, fonts, variableByName, stats) {
  text.fontName = fonts.medium;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "HEIGHT";
  await applyTextStyleToNodeAsync(text, "popoverTitle", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Popover/title/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Popover/title/line-height",
    variableByName,
    stats,
  );
}

async function applyPopoverDescriptionTypography(
  text,
  fonts,
  variableByName,
  stats,
) {
  text.fontName = fonts.regular;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "HEIGHT";
  await applyTextStyleToNodeAsync(text, "popoverDescription", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Popover/description/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Popover/description/line-height",
    variableByName,
    stats,
  );
}

async function applyMenuItemTypography(
  text,
  fonts,
  variableByName,
  stats,
  strong,
) {
  text.fontName = strong ? fonts.medium : fonts.regular;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(
    text,
    strong ? "menuItemStrong" : "menuItem",
    stats,
  );

  bindFloatVariable(
    text,
    "fontSize",
    "Menu/text/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Menu/text/line-height",
    variableByName,
    stats,
  );
}

async function applyMenuShortcutTypography(text, fonts, variableByName, stats) {
  text.fontName = fonts.medium;
  text.fontSize = 12;
  text.lineHeight = { unit: "PIXELS", value: 16 };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(text, "menuShortcut", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Menu/shortcut/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Menu/shortcut/line-height",
    variableByName,
    stats,
  );
}

async function applyToastTitleTypography(text, fonts, variableByName, stats) {
  text.fontName = fonts.medium;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "HEIGHT";
  await applyTextStyleToNodeAsync(text, "toastTitle", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Toast/title/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Toast/title/line-height",
    variableByName,
    stats,
  );
}

async function applyToastDescriptionTypography(
  text,
  fonts,
  variableByName,
  stats,
) {
  text.fontName = fonts.regular;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "HEIGHT";
  await applyTextStyleToNodeAsync(text, "toastDescription", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Toast/description/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Toast/description/line-height",
    variableByName,
    stats,
  );
}

async function applyCheckboxLabelTypography(
  label,
  fonts,
  variableByName,
  stats,
) {
  label.fontName = fonts.medium;
  label.fontSize = 14;
  label.lineHeight = { unit: "PIXELS", value: 20 };
  label.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(label, "controlLabel", stats);

  bindFloatVariable(
    label,
    "fontSize",
    "Checkbox/label/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    label,
    "lineHeight",
    "Checkbox/label/line-height",
    variableByName,
    stats,
  );
}

async function applyRadioLabelTypography(label, fonts, variableByName, stats) {
  label.fontName = fonts.medium;
  label.fontSize = 14;
  label.lineHeight = { unit: "PIXELS", value: 20 };
  label.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(label, "controlLabel", stats);

  bindFloatVariable(
    label,
    "fontSize",
    "Radio/label/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    label,
    "lineHeight",
    "Radio/label/line-height",
    variableByName,
    stats,
  );
}

async function applySwitchLabelTypography(label, fonts, variableByName, stats) {
  label.fontName = fonts.medium;
  label.fontSize = 14;
  label.lineHeight = { unit: "PIXELS", value: 20 };
  label.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(label, "controlLabel", stats);

  bindFloatVariable(
    label,
    "fontSize",
    "Switch/label/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    label,
    "lineHeight",
    "Switch/label/line-height",
    variableByName,
    stats,
  );
}

async function applyInputLabelTypography(label, fonts, variableByName, stats) {
  label.fontName = fonts.medium;
  label.fontSize = 14;
  label.lineHeight = { unit: "PIXELS", value: 20 };
  label.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(label, "fieldLabel", stats);

  bindFloatVariable(
    label,
    "fontSize",
    "Input/label/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    label,
    "lineHeight",
    "Input/label/line-height",
    variableByName,
    stats,
  );
}

async function applyInputTextTypography(text, fonts, variableByName, stats) {
  text.fontName = fonts.regular;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(text, "fieldText", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Input/text/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Input/text/line-height",
    variableByName,
    stats,
  );
}

async function applyFieldLabelTypography(
  label,
  fonts,
  tokenPrefix,
  variableByName,
  stats,
) {
  label.fontName = fonts.medium;
  label.fontSize = 14;
  label.lineHeight = { unit: "PIXELS", value: 20 };
  label.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(label, "fieldLabel", stats);

  bindFloatVariable(
    label,
    "fontSize",
    `${tokenPrefix}/label/font-size`,
    variableByName,
    stats,
  );
  bindFloatVariable(
    label,
    "lineHeight",
    `${tokenPrefix}/label/line-height`,
    variableByName,
    stats,
  );
}

async function applyFieldTextTypography(
  text,
  fonts,
  tokenPrefix,
  variableByName,
  stats,
) {
  text.fontName = fonts.regular;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(text, "fieldText", stats);

  bindFloatVariable(
    text,
    "fontSize",
    `${tokenPrefix}/text/font-size`,
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    `${tokenPrefix}/text/line-height`,
    variableByName,
    stats,
  );
}

async function applyFormFieldMetaTypography(
  text,
  fonts,
  variableByName,
  stats,
) {
  text.fontName = fonts.regular;
  text.fontSize = 12;
  text.lineHeight = { unit: "PIXELS", value: 16 };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(text, "fieldMeta", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "FormField/meta/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "FormField/meta/line-height",
    variableByName,
    stats,
  );
}

async function applyDatePickerMetaTypography(
  text,
  fonts,
  variableByName,
  stats,
) {
  text.fontName = fonts.regular;
  text.fontSize = 12;
  text.lineHeight = { unit: "PIXELS", value: 16 };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(text, "fieldMeta", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "DatePicker/meta/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "DatePicker/meta/line-height",
    variableByName,
    stats,
  );
}

async function applyDateRangePickerMetaTypography(
  text,
  fonts,
  variableByName,
  stats,
) {
  text.fontName = fonts.regular;
  text.fontSize = 12;
  text.lineHeight = { unit: "PIXELS", value: 16 };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(text, "fieldMeta", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "DateRangePicker/meta/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "DateRangePicker/meta/line-height",
    variableByName,
    stats,
  );
}

async function applyFileUploadMetaTypography(
  text,
  fonts,
  variableByName,
  stats,
) {
  text.fontName = fonts.regular;
  text.fontSize = 12;
  text.lineHeight = { unit: "PIXELS", value: 16 };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(text, "fieldMeta", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "FileUpload/meta/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "FileUpload/meta/line-height",
    variableByName,
    stats,
  );
}

async function applyColorPickerMetaTypography(
  text,
  fonts,
  variableByName,
  stats,
) {
  text.fontName = fonts.regular;
  text.fontSize = 12;
  text.lineHeight = { unit: "PIXELS", value: 16 };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(text, "fieldMeta", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "ColorPicker/meta/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "ColorPicker/meta/line-height",
    variableByName,
    stats,
  );
}

async function applyAvatarFallbackTypography(
  text,
  fonts,
  variableByName,
  stats,
) {
  text.fontName = fonts.medium;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(text, "avatarFallback", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Avatar/fallback/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Avatar/fallback/line-height",
    variableByName,
    stats,
  );
}

async function applyAlertTitleTypography(text, fonts, variableByName, stats) {
  text.fontName = fonts.medium;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(text, "alertTitle", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Alert/title/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Alert/title/line-height",
    variableByName,
    stats,
  );
}

async function applyAlertDescriptionTypography(
  text,
  fonts,
  variableByName,
  stats,
) {
  text.fontName = fonts.regular;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(text, "alertDescription", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Alert/description/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Alert/description/line-height",
    variableByName,
    stats,
  );
}

async function applyEmptyStateTitleTypography(
  text,
  fonts,
  variableByName,
  stats,
) {
  text.fontName = fonts.medium;
  text.fontSize = 16;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "HEIGHT";
  await applyTextStyleToNodeAsync(text, "emptyStateTitle", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "EmptyState/title/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "EmptyState/title/line-height",
    variableByName,
    stats,
  );
}

async function applyEmptyStateDescriptionTypography(
  text,
  fonts,
  variableByName,
  stats,
) {
  text.fontName = fonts.regular;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "HEIGHT";
  await applyTextStyleToNodeAsync(text, "emptyStateDescription", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "EmptyState/description/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "EmptyState/description/line-height",
    variableByName,
    stats,
  );
}

function bindFloatVariable(
  node,
  propertyName,
  variableName,
  variableByName,
  stats,
) {
  const variable = variableByName.get(variableName);
  if (!variable) {
    pushUniqueWarning(
      stats,
      `missing-component-variable:${variableName}`,
      `Missing component variable "${variableName}".`,
    );
    return false;
  }

  if (!node || !node.setBoundVariable) {
    incrementStat(stats, "componentVariableBindingsSkipped");
    return false;
  }

  try {
    node.setBoundVariable(propertyName, variable);
    incrementStat(stats, "componentVariableBindingsApplied");
    return true;
  } catch (error) {
    incrementStat(stats, "componentVariableBindingsFailed");
    pushUniqueWarning(
      stats,
      `component-binding:${propertyName}:${variableName}`,
      `Could not bind ${variableName} to ${propertyName} (${messageFor(error)})`,
    );
    return false;
  }
}

function clearBoundVariable(node, propertyName, stats) {
  if (!node || !node.setBoundVariable) return false;

  try {
    node.setBoundVariable(propertyName, null);
    return true;
  } catch (_error) {
    if (stats) incrementStat(stats, "componentVariableBindingsSkipped");
    return false;
  }
}

function bindSizeVariables(
  node,
  widthToken,
  heightToken,
  variableByName,
  stats,
) {
  bindFloatVariable(node, "width", widthToken, variableByName, stats);
  bindFloatVariable(
    node,
    "height",
    heightToken || widthToken,
    variableByName,
    stats,
  );
}

function bindButtonGeometryVariables(
  component,
  metrics,
  config,
  variableByName,
  stats,
) {
  bindSizeVariables(
    component,
    metrics.widthToken,
    metrics.heightToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    metrics.paddingXToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    metrics.paddingXToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "Button/padding/y",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "Button/padding/y",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    metrics.gapToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "Button/radius",
    variableByName,
    stats,
  );

  if (config && config.stroke) {
    bindFloatVariable(
      component,
      "strokeWeight",
      "Button/stroke/width",
      variableByName,
      stats,
    );
  }
}

function bindIconButtonGeometryVariables(
  component,
  metrics,
  config,
  variableByName,
  stats,
) {
  bindSizeVariables(
    component,
    metrics.sizeToken,
    metrics.sizeToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    "Button/padding/y",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    "Button/padding/y",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "Button/padding/y",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "Button/padding/y",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Button/gap/icon",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "IconButton/radius",
    variableByName,
    stats,
  );

  if (config && config.stroke) {
    bindFloatVariable(
      component,
      "strokeWeight",
      "IconButton/stroke/width",
      variableByName,
      stats,
    );
  }
}

function bindToggleButtonGeometryVariables(
  component,
  metrics,
  config,
  variableByName,
  stats,
) {
  bindSizeVariables(
    component,
    metrics.widthToken,
    metrics.heightToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    metrics.paddingXToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    metrics.paddingXToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "ToggleButton/padding/y",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "ToggleButton/padding/y",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "ToggleButton/radius",
    variableByName,
    stats,
  );

  if (config && config.stroke) {
    bindFloatVariable(
      component,
      "strokeWeight",
      "ToggleButton/stroke/width",
      variableByName,
      stats,
    );
  }
}

function bindSplitButtonGeometryVariables(
  component,
  mainAction,
  trigger,
  divider,
  metrics,
  config,
  variableByName,
  stats,
) {
  bindSizeVariables(
    component,
    metrics.widthToken,
    metrics.heightToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "SplitButton/radius",
    variableByName,
    stats,
  );

  if (config && config.stroke) {
    bindFloatVariable(
      component,
      "strokeWeight",
      "SplitButton/stroke/width",
      variableByName,
      stats,
    );
  }

  if (mainAction) {
    bindFloatVariable(
      mainAction,
      "paddingLeft",
      metrics.mainPaddingXToken,
      variableByName,
      stats,
    );
    bindFloatVariable(
      mainAction,
      "paddingRight",
      metrics.mainPaddingXToken,
      variableByName,
      stats,
    );
  }

  if (trigger) {
    bindFloatVariable(
      trigger,
      "width",
      metrics.triggerWidthToken,
      variableByName,
      stats,
    );
  }

  if (divider) {
    bindFloatVariable(
      divider,
      "width",
      "SplitButton/divider/width",
      variableByName,
      stats,
    );
  }
}

function bindFloatingActionButtonGeometryVariables(
  component,
  metrics,
  config,
  variableByName,
  stats,
) {
  bindSizeVariables(
    component,
    metrics.sizeToken,
    metrics.sizeToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "FloatingActionButton/radius",
    variableByName,
    stats,
  );

  if (config && config.stroke) {
    bindFloatVariable(
      component,
      "strokeWeight",
      "FloatingActionButton/stroke/width",
      variableByName,
      stats,
    );
  }
}

function bindBadgeGeometryVariables(
  component,
  metrics,
  config,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "height",
    metrics.heightToken,
    variableByName,
    stats,
  );
  if (metrics.widthToken) {
    bindFloatVariable(
      component,
      "width",
      metrics.widthToken,
      variableByName,
      stats,
    );
  }
  bindFloatVariable(
    component,
    "paddingLeft",
    metrics.paddingXToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    metrics.paddingXToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "Badge/padding/y",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "Badge/padding/y",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Badge/gap",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "Badge/radius",
    variableByName,
    stats,
  );

  if (config && config.stroke) {
    bindFloatVariable(
      component,
      "strokeWeight",
      "Badge/stroke/width",
      variableByName,
      stats,
    );
  }
}

function bindChipGeometryVariables(component, metrics, variableByName, stats) {
  bindFloatVariable(
    component,
    "height",
    metrics.heightToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    metrics.paddingXToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    metrics.paddingXToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "Chip/padding/y",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "Chip/padding/y",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Chip/gap",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "Chip/radius",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "strokeWeight",
    "Chip/stroke/width",
    variableByName,
    stats,
  );
}

function bindSegmentedControlGeometryVariables(
  component,
  metrics,
  variableByName,
  stats,
) {
  clearBoundVariable(component, "width", stats);
  bindFloatVariable(
    component,
    "height",
    metrics.heightToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    "SegmentedControl/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    "SegmentedControl/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "SegmentedControl/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "SegmentedControl/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "SegmentedControl/radius",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "strokeWeight",
    "SegmentedControl/stroke/width",
    variableByName,
    stats,
  );
}

function bindCounterGeometryVariables(counter, metrics, variableByName, stats) {
  bindFloatVariable(
    counter,
    "height",
    metrics.heightToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    counter,
    "paddingLeft",
    metrics.paddingXToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    counter,
    "paddingRight",
    metrics.paddingXToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    counter,
    "cornerRadius",
    "Counter/radius",
    variableByName,
    stats,
  );
}

function bindCardGeometryVariables(
  component,
  header,
  body,
  footer,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "Card/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "Card/radius",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "strokeWeight",
    "Card/stroke/width",
    variableByName,
    stats,
  );

  const sections = [header, body, footer].filter(Boolean);
  for (const section of sections) {
    bindFloatVariable(
      section,
      "paddingLeft",
      "Card/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      section,
      "paddingRight",
      "Card/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      section,
      "paddingBottom",
      "Card/padding",
      variableByName,
      stats,
    );
  }

  if (header) {
    bindFloatVariable(
      header,
      "paddingTop",
      "Card/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      header,
      "itemSpacing",
      "Card/header/gap",
      variableByName,
      stats,
    );
  }

  if (body) {
    bindFloatVariable(
      body,
      "itemSpacing",
      "Card/body/gap",
      variableByName,
      stats,
    );
  }

  if (footer) {
    bindFloatVariable(
      footer,
      "itemSpacing",
      "Card/footer/gap",
      variableByName,
      stats,
    );
  }
}

function bindListGeometryVariables(
  component,
  density,
  items,
  variableByName,
  stats,
) {
  const key = String(density).toLowerCase();
  bindFloatVariable(
    component,
    "width",
    "List/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "List/radius",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "strokeWeight",
    "List/stroke/width",
    variableByName,
    stats,
  );

  for (const item of items) {
    bindFloatVariable(
      item,
      "height",
      `List/item/height/${key}`,
      variableByName,
      stats,
    );
    bindFloatVariable(
      item,
      "paddingLeft",
      "List/item/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      item,
      "paddingRight",
      "List/item/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      item,
      "paddingTop",
      `List/item/padding/y/${key}`,
      variableByName,
      stats,
    );
    bindFloatVariable(
      item,
      "paddingBottom",
      `List/item/padding/y/${key}`,
      variableByName,
      stats,
    );
  }
}

function bindTreeGeometryVariables(
  component,
  density,
  rows,
  icons,
  variableByName,
  stats,
) {
  const key = String(density).toLowerCase();
  bindFloatVariable(
    component,
    "width",
    "Tree/width/default",
    variableByName,
    stats,
  );

  for (const row of rows) {
    bindFloatVariable(
      row,
      "height",
      `Tree/item/height/${key}`,
      variableByName,
      stats,
    );
    bindFloatVariable(
      row,
      "paddingRight",
      "Tree/item/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      row,
      "cornerRadius",
      "Tree/item/radius",
      variableByName,
      stats,
    );
  }

  for (const icon of icons) {
    bindFloatVariable(icon, "width", "Tree/icon/size", variableByName, stats);
    bindFloatVariable(icon, "height", "Tree/icon/size", variableByName, stats);
  }
}

function bindTableGeometryVariables(
  component,
  density,
  rows,
  cells,
  variableByName,
  stats,
) {
  const key = String(density).toLowerCase();
  bindFloatVariable(
    component,
    "width",
    "Table/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "Table/radius",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "strokeWeight",
    "Table/stroke/width",
    variableByName,
    stats,
  );

  for (const row of rows) {
    bindFloatVariable(
      row,
      "height",
      `Table/row/height/${key}`,
      variableByName,
      stats,
    );
  }

  for (const cell of cells) {
    bindFloatVariable(
      cell,
      "paddingLeft",
      "Table/cell/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      cell,
      "paddingRight",
      "Table/cell/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      cell,
      "paddingTop",
      `Table/cell/padding/y/${key}`,
      variableByName,
      stats,
    );
    bindFloatVariable(
      cell,
      "paddingBottom",
      `Table/cell/padding/y/${key}`,
      variableByName,
      stats,
    );
  }
}

function bindPaginationGeometryVariables(
  component,
  metrics,
  controls,
  variableByName,
  stats,
) {
  bindSizeVariables(
    component,
    metrics.widthToken,
    "Pagination/height",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Pagination/gap",
    variableByName,
    stats,
  );

  for (const control of controls) {
    if (!control || !control.node) continue;

    bindSizeVariables(
      control.node,
      control.widthToken || "Pagination/item/size",
      "Pagination/item/size",
      variableByName,
      stats,
    );
    bindFloatVariable(
      control.node,
      "cornerRadius",
      "Pagination/item/radius",
      variableByName,
      stats,
    );
    if (control.hasStroke) {
      bindFloatVariable(
        control.node,
        "strokeWeight",
        "Pagination/stroke/width",
        variableByName,
        stats,
      );
    }
  }
}

function bindTabsGeometryVariables(
  component,
  count,
  triggers,
  variableByName,
  stats,
) {
  const widthToken = `Tabs/width/${String(count).toLowerCase()}`;
  bindSizeVariables(
    component,
    widthToken,
    "Tabs/list/height",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    "Tabs/list/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    "Tabs/list/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "Tabs/list/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "Tabs/list/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Tabs/list/gap",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "Tabs/list/radius",
    variableByName,
    stats,
  );

  for (const trigger of triggers) {
    bindFloatVariable(
      trigger,
      "height",
      "Tabs/trigger/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      trigger,
      "paddingLeft",
      "Tabs/trigger/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      trigger,
      "paddingRight",
      "Tabs/trigger/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      trigger,
      "cornerRadius",
      "Tabs/trigger/radius",
      variableByName,
      stats,
    );
  }
}

function bindTooltipGeometryVariables(component, variableByName, stats) {
  bindFloatVariable(
    component,
    "paddingLeft",
    "Tooltip/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    "Tooltip/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "Tooltip/padding/y",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "Tooltip/padding/y",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "Tooltip/radius",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "strokeWeight",
    "Tooltip/stroke/width",
    variableByName,
    stats,
  );
}

function bindTooltipTipVariables(tip, side, variableByName, stats) {
  if (side === "Left" || side === "Right") {
    bindSizeVariables(
      tip,
      "Tooltip/tip/height",
      "Tooltip/tip/size",
      variableByName,
      stats,
    );
    return;
  }
  bindSizeVariables(
    tip,
    "Tooltip/tip/size",
    "Tooltip/tip/height",
    variableByName,
    stats,
  );
}

function bindDialogGeometryVariables(
  component,
  header,
  body,
  footer,
  close,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "Dialog/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    "Dialog/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    "Dialog/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "Dialog/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "Dialog/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Dialog/gap",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "Dialog/radius",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "strokeWeight",
    "Dialog/stroke/width",
    variableByName,
    stats,
  );

  if (header) {
    const headerContent = directChildNamed(header, "Dialog Header Content");
    bindFloatVariable(
      headerContent || header,
      "itemSpacing",
      "Dialog/header/gap",
      variableByName,
      stats,
    );
  }

  if (body) {
    bindFloatVariable(
      body,
      "itemSpacing",
      "Dialog/body/gap",
      variableByName,
      stats,
    );
  }

  if (footer) {
    bindFloatVariable(
      footer,
      "itemSpacing",
      "Dialog/footer/gap",
      variableByName,
      stats,
    );
  }

  if (close) {
    bindSizeVariables(
      close,
      "Dialog/close/size",
      "Dialog/close/size",
      variableByName,
      stats,
    );
  }
}

function bindDrawerGeometryVariables(
  component,
  side,
  header,
  body,
  footer,
  close,
  variableByName,
  stats,
) {
  const horizontal = side === "Left" || side === "Right";
  bindFloatVariable(
    component,
    "width",
    horizontal ? "Drawer/width/default" : "Drawer/sheet/width",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "height",
    horizontal ? "Drawer/height/default" : "Drawer/sheet/height",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    "Drawer/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    "Drawer/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "Drawer/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "Drawer/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Drawer/gap",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "Drawer/radius",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "strokeWeight",
    "Drawer/stroke/width",
    variableByName,
    stats,
  );

  if (header) {
    const headerContent = directChildNamed(header, "Drawer Header Content");
    bindFloatVariable(
      headerContent || header,
      "itemSpacing",
      "Drawer/header/gap",
      variableByName,
      stats,
    );
  }

  if (body) {
    bindFloatVariable(body, "itemSpacing", "Drawer/gap", variableByName, stats);
  }

  if (footer) {
    bindFloatVariable(
      footer,
      "height",
      "BottomSheet/footer/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      footer,
      "itemSpacing",
      "BottomSheet/footer/gap",
      variableByName,
      stats,
    );
  }

  if (close) {
    bindSizeVariables(
      close,
      "Drawer/close/size",
      "Drawer/close/size",
      variableByName,
      stats,
    );
  }
}

function bindBottomSheetGeometryVariables(
  component,
  header,
  body,
  footer,
  close,
  handle,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "BottomSheet/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    "BottomSheet/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    "BottomSheet/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "BottomSheet/padding/top",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "BottomSheet/padding/bottom",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "BottomSheet/gap",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "topLeftRadius",
    "BottomSheet/radius",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "topRightRadius",
    "BottomSheet/radius",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "strokeWeight",
    "BottomSheet/stroke/width",
    variableByName,
    stats,
  );

  if (header) {
    const headerContent = directChildNamed(
      header,
      "BottomSheet Header Content",
    );
    bindFloatVariable(
      headerContent || header,
      "itemSpacing",
      "BottomSheet/header/gap",
      variableByName,
      stats,
    );
  }

  if (body) {
    bindFloatVariable(
      body,
      "itemSpacing",
      "BottomSheet/gap",
      variableByName,
      stats,
    );
  }

  if (footer) {
    bindFloatVariable(
      footer,
      "height",
      "BottomSheet/footer/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      footer,
      "itemSpacing",
      "BottomSheet/footer/gap",
      variableByName,
      stats,
    );
  }

  if (close) {
    bindSizeVariables(
      close,
      "Drawer/close/size",
      "Drawer/close/size",
      variableByName,
      stats,
    );
  }

  if (handle) {
    bindSizeVariables(
      handle,
      "BottomSheet/handle/width",
      "BottomSheet/handle/height",
      variableByName,
      stats,
    );
  }
}

function bindPopoverGeometryVariables(
  component,
  header,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "Popover/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    "Popover/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    "Popover/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "Popover/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "Popover/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Popover/gap",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "Popover/radius",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "strokeWeight",
    "Popover/stroke/width",
    variableByName,
    stats,
  );

  if (header) {
    bindFloatVariable(
      header,
      "itemSpacing",
      "Popover/header/gap",
      variableByName,
      stats,
    );
  }
}

function bindMenuGeometryVariables(component, items, variableByName, stats) {
  bindFloatVariable(
    component,
    "width",
    "Menu/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    "Menu/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    "Menu/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "Menu/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "Menu/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "Menu/radius",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "strokeWeight",
    "Menu/stroke/width",
    variableByName,
    stats,
  );

  for (const item of items) {
    if (!item || item.removed || item.type !== "FRAME") continue;
    bindFloatVariable(
      item,
      "height",
      "Menu/item/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      item,
      "paddingRight",
      "Menu/item/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      item,
      "cornerRadius",
      "Menu/item/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      item,
      "itemSpacing",
      "Menu/item/gap",
      variableByName,
      stats,
    );
  }
}

function bindToastGeometryVariables(
  component,
  textGroup,
  action,
  close,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "Toast/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    "Toast/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    "Toast/padding/right",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "Toast/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "Toast/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Toast/gap",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "Toast/radius",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "strokeWeight",
    "Toast/stroke/width",
    variableByName,
    stats,
  );

  if (textGroup) {
    bindFloatVariable(
      textGroup,
      "itemSpacing",
      "Toast/text/gap",
      variableByName,
      stats,
    );
  }

  if (action) {
    bindFloatVariable(
      action,
      "height",
      "Toast/action/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      action,
      "paddingLeft",
      "Toast/action/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      action,
      "paddingRight",
      "Toast/action/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      action,
      "cornerRadius",
      "Toast/action/radius",
      variableByName,
      stats,
    );
  }

  if (close) {
    bindSizeVariables(
      close,
      "Toast/close/size",
      "Toast/close/size",
      variableByName,
      stats,
    );
  }
}

function bindCheckboxGeometryVariables(
  component,
  control,
  variableByName,
  stats,
) {
  bindSizeVariables(
    component,
    "Checkbox/width/default",
    "Checkbox/height/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    "Checkbox/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    "Checkbox/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "Checkbox/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "Checkbox/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Checkbox/gap",
    variableByName,
    stats,
  );

  if (control) {
    bindSizeVariables(
      control,
      "Checkbox/control/size",
      "Checkbox/control/size",
      variableByName,
      stats,
    );
    bindFloatVariable(
      control,
      "cornerRadius",
      "Checkbox/control/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      control,
      "strokeWeight",
      "Checkbox/control/stroke/width",
      variableByName,
      stats,
    );
  }
}

function bindRadioGeometryVariables(
  component,
  control,
  dot,
  variableByName,
  stats,
) {
  bindSizeVariables(
    component,
    "Radio/width/default",
    "Radio/height/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    "Radio/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    "Radio/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "Radio/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "Radio/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Radio/gap",
    variableByName,
    stats,
  );

  if (control) {
    bindSizeVariables(
      control,
      "Radio/control/size",
      "Radio/control/size",
      variableByName,
      stats,
    );
    bindFloatVariable(
      control,
      "cornerRadius",
      "Radio/control/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      control,
      "strokeWeight",
      "Radio/control/stroke/width",
      variableByName,
      stats,
    );
  }

  if (dot) {
    bindSizeVariables(
      dot,
      "Radio/dot/size",
      "Radio/dot/size",
      variableByName,
      stats,
    );
  }
}

function bindSwitchGeometryVariables(
  component,
  track,
  thumb,
  variableByName,
  stats,
) {
  bindSizeVariables(
    component,
    "Switch/width/default",
    "Switch/height/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    "Switch/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    "Switch/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "Switch/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "Switch/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Switch/gap",
    variableByName,
    stats,
  );

  if (track) {
    bindSizeVariables(
      track,
      "Switch/track/width",
      "Switch/track/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      track,
      "cornerRadius",
      "Switch/track/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      track,
      "strokeWeight",
      "Switch/track/stroke/width",
      variableByName,
      stats,
    );
  }

  if (thumb) {
    bindSizeVariables(
      thumb,
      "Switch/thumb/size",
      "Switch/thumb/size",
      variableByName,
      stats,
    );
    bindFloatVariable(
      thumb,
      "cornerRadius",
      "Switch/thumb/radius",
      variableByName,
      stats,
    );
  }
}

function bindInputGeometryVariables(component, field, variableByName, stats) {
  bindFloatVariable(
    component,
    "width",
    "Input/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Input/gap",
    variableByName,
    stats,
  );

  if (field) {
    bindFloatVariable(
      field,
      "height",
      "Input/field/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingLeft",
      "Input/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingRight",
      "Input/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingTop",
      "Input/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingBottom",
      "Input/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "cornerRadius",
      "Input/field/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "strokeWeight",
      "Input/field/stroke/width",
      variableByName,
      stats,
    );
  }
}

function bindPasswordInputGeometryVariables(
  component,
  field,
  valueSlot,
  toggle,
  icon,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "PasswordInput/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "PasswordInput/gap",
    variableByName,
    stats,
  );

  if (field) {
    bindFloatVariable(
      field,
      "height",
      "PasswordInput/field/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "cornerRadius",
      "PasswordInput/field/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "strokeWeight",
      "PasswordInput/field/stroke/width",
      variableByName,
      stats,
    );
  }

  if (valueSlot) {
    bindFloatVariable(
      valueSlot,
      "paddingLeft",
      "PasswordInput/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      valueSlot,
      "paddingRight",
      "PasswordInput/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      valueSlot,
      "paddingTop",
      "PasswordInput/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      valueSlot,
      "paddingBottom",
      "PasswordInput/field/padding/y",
      variableByName,
      stats,
    );
  }

  if (toggle) {
    bindSizeVariables(
      toggle,
      "PasswordInput/toggle/size",
      "PasswordInput/toggle/size",
      variableByName,
      stats,
    );
    bindFloatVariable(
      toggle,
      "cornerRadius",
      "PasswordInput/toggle/radius",
      variableByName,
      stats,
    );
  }

  if (icon) {
    bindSizeVariables(
      icon,
      "PasswordInput/toggle/icon/size",
      "PasswordInput/toggle/icon/size",
      variableByName,
      stats,
    );
  }
}

function bindFormFieldGeometryVariables(
  component,
  header,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "FormField/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "FormField/gap",
    variableByName,
    stats,
  );

  if (header) {
    bindFloatVariable(
      header,
      "itemSpacing",
      "FormField/header/gap",
      variableByName,
      stats,
    );
  }
}

function bindNumberInputGeometryVariables(
  component,
  field,
  valueSlot,
  stepperNodes,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "NumberInput/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "NumberInput/gap",
    variableByName,
    stats,
  );

  if (field) {
    bindFloatVariable(
      field,
      "height",
      "NumberInput/field/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "cornerRadius",
      "NumberInput/field/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "strokeWeight",
      "NumberInput/field/stroke/width",
      variableByName,
      stats,
    );
  }

  if (valueSlot) {
    bindFloatVariable(
      valueSlot,
      "paddingLeft",
      "NumberInput/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      valueSlot,
      "paddingRight",
      "NumberInput/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      valueSlot,
      "paddingTop",
      "NumberInput/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      valueSlot,
      "paddingBottom",
      "NumberInput/field/padding/y",
      variableByName,
      stats,
    );
  }

  for (const stepper of stepperNodes || []) {
    bindSizeVariables(
      stepper,
      "NumberInput/stepper/size",
      "NumberInput/stepper/size",
      variableByName,
      stats,
    );
  }
}

function bindTextareaGeometryVariables(
  component,
  field,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "Textarea/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Textarea/gap",
    variableByName,
    stats,
  );

  if (field) {
    bindFloatVariable(
      field,
      "height",
      "Textarea/field/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingLeft",
      "Textarea/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingRight",
      "Textarea/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingTop",
      "Textarea/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingBottom",
      "Textarea/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "cornerRadius",
      "Textarea/field/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "strokeWeight",
      "Textarea/field/stroke/width",
      variableByName,
      stats,
    );
  }
}

function bindSearchGeometryVariables(
  component,
  field,
  icon,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "Search/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Search/gap",
    variableByName,
    stats,
  );

  if (field) {
    bindFloatVariable(
      field,
      "height",
      "Search/field/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingLeft",
      "Search/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingRight",
      "Search/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingTop",
      "Search/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingBottom",
      "Search/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "cornerRadius",
      "Search/field/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "strokeWeight",
      "Search/field/stroke/width",
      variableByName,
      stats,
    );
  }

  if (icon) {
    bindSizeVariables(
      icon,
      "Search/icon/size",
      "Search/icon/size",
      variableByName,
      stats,
    );
  }
}

function bindSelectGeometryVariables(
  component,
  trigger,
  icon,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "Select/width/default",
    variableByName,
    stats,
  );

  if (trigger) {
    bindFloatVariable(
      trigger,
      "height",
      "Select/trigger/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      trigger,
      "paddingLeft",
      "Select/trigger/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      trigger,
      "paddingRight",
      "Select/trigger/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      trigger,
      "paddingTop",
      "Select/trigger/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      trigger,
      "paddingBottom",
      "Select/trigger/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      trigger,
      "cornerRadius",
      "Select/trigger/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      trigger,
      "strokeWeight",
      "Select/trigger/stroke/width",
      variableByName,
      stats,
    );
  }

  if (icon) {
    bindSizeVariables(
      icon,
      "Select/icon/size",
      "Select/icon/size",
      variableByName,
      stats,
    );
  }
}

function bindComboboxGeometryVariables(
  component,
  field,
  listbox,
  optionNodes,
  icons,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "Combobox/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Combobox/gap",
    variableByName,
    stats,
  );

  if (field) {
    bindFloatVariable(
      field,
      "height",
      "Combobox/field/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingLeft",
      "Combobox/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingRight",
      "Combobox/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingTop",
      "Combobox/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingBottom",
      "Combobox/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "cornerRadius",
      "Combobox/field/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "strokeWeight",
      "Combobox/field/stroke/width",
      variableByName,
      stats,
    );
  }

  if (listbox) {
    bindFloatVariable(
      listbox,
      "width",
      "Combobox/listbox/width",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "paddingLeft",
      "Combobox/listbox/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "paddingRight",
      "Combobox/listbox/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "paddingTop",
      "Combobox/listbox/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "paddingBottom",
      "Combobox/listbox/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "itemSpacing",
      "Combobox/listbox/gap",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "cornerRadius",
      "Combobox/listbox/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "strokeWeight",
      "Combobox/listbox/stroke/width",
      variableByName,
      stats,
    );
  }

  for (const option of optionNodes || []) {
    bindFloatVariable(
      option,
      "height",
      "Combobox/option/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      option,
      "paddingLeft",
      "Combobox/option/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      option,
      "paddingRight",
      "Combobox/option/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      option,
      "paddingTop",
      "Combobox/option/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      option,
      "paddingBottom",
      "Combobox/option/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      option,
      "cornerRadius",
      "Combobox/option/radius",
      variableByName,
      stats,
    );
  }

  for (const icon of icons || []) {
    bindSizeVariables(
      icon,
      "Combobox/icon/size",
      "Combobox/icon/size",
      variableByName,
      stats,
    );
  }
}

function bindMultiSelectGeometryVariables(
  component,
  field,
  chips,
  listbox,
  optionNodes,
  icons,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "MultiSelect/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "MultiSelect/gap",
    variableByName,
    stats,
  );

  if (field) {
    bindFloatVariable(
      field,
      "height",
      "MultiSelect/field/min-height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingLeft",
      "MultiSelect/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingRight",
      "MultiSelect/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingTop",
      "MultiSelect/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingBottom",
      "MultiSelect/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "itemSpacing",
      "MultiSelect/field/gap",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "cornerRadius",
      "MultiSelect/field/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "strokeWeight",
      "MultiSelect/field/stroke/width",
      variableByName,
      stats,
    );
  }

  for (const chip of chips || []) {
    if (isNestedComponentInstance(chip, "Chip")) {
      continue;
    }
    bindFloatVariable(
      chip,
      "height",
      "MultiSelect/chip/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      chip,
      "paddingLeft",
      "MultiSelect/chip/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      chip,
      "paddingRight",
      "MultiSelect/chip/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      chip,
      "cornerRadius",
      "MultiSelect/chip/radius",
      variableByName,
      stats,
    );
  }

  if (listbox) {
    bindFloatVariable(
      listbox,
      "width",
      "MultiSelect/listbox/width",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "paddingLeft",
      "MultiSelect/listbox/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "paddingRight",
      "MultiSelect/listbox/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "paddingTop",
      "MultiSelect/listbox/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "paddingBottom",
      "MultiSelect/listbox/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "itemSpacing",
      "MultiSelect/listbox/gap",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "cornerRadius",
      "MultiSelect/listbox/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "strokeWeight",
      "MultiSelect/listbox/stroke/width",
      variableByName,
      stats,
    );
  }

  for (const option of optionNodes || []) {
    bindFloatVariable(
      option,
      "height",
      "MultiSelect/option/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      option,
      "paddingLeft",
      "MultiSelect/option/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      option,
      "paddingRight",
      "MultiSelect/option/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      option,
      "cornerRadius",
      "MultiSelect/option/radius",
      variableByName,
      stats,
    );
  }

  for (const icon of icons || []) {
    bindSizeVariables(
      icon,
      "MultiSelect/icon/size",
      "MultiSelect/icon/size",
      variableByName,
      stats,
    );
  }
}

function bindListboxGeometryVariables(
  component,
  optionNodes,
  icons,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "Listbox/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    "Listbox/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    "Listbox/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "Listbox/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "Listbox/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Listbox/gap",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "Listbox/radius",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "strokeWeight",
    "Listbox/stroke/width",
    variableByName,
    stats,
  );

  for (const option of optionNodes || []) {
    bindFloatVariable(
      option,
      "height",
      "Listbox/option/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      option,
      "paddingLeft",
      "Listbox/option/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      option,
      "paddingRight",
      "Listbox/option/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      option,
      "paddingTop",
      "Listbox/option/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      option,
      "paddingBottom",
      "Listbox/option/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      option,
      "cornerRadius",
      "Listbox/option/radius",
      variableByName,
      stats,
    );
  }

  for (const icon of icons || []) {
    bindSizeVariables(
      icon,
      "Listbox/icon/size",
      "Listbox/icon/size",
      variableByName,
      stats,
    );
  }
}

function bindDatePickerGeometryVariables(
  component,
  field,
  calendar,
  header,
  dayNodes,
  icons,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "DatePicker/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "DatePicker/gap",
    variableByName,
    stats,
  );

  if (field) {
    bindFloatVariable(
      field,
      "height",
      "DatePicker/field/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingLeft",
      "DatePicker/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingRight",
      "DatePicker/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingTop",
      "DatePicker/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingBottom",
      "DatePicker/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "cornerRadius",
      "DatePicker/field/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "strokeWeight",
      "DatePicker/field/stroke/width",
      variableByName,
      stats,
    );
  }

  if (calendar) {
    bindFloatVariable(
      calendar,
      "width",
      "DatePicker/calendar/width",
      variableByName,
      stats,
    );
    bindFloatVariable(
      calendar,
      "paddingLeft",
      "DatePicker/calendar/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      calendar,
      "paddingRight",
      "DatePicker/calendar/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      calendar,
      "paddingTop",
      "DatePicker/calendar/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      calendar,
      "paddingBottom",
      "DatePicker/calendar/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      calendar,
      "itemSpacing",
      "DatePicker/calendar/gap",
      variableByName,
      stats,
    );
    bindFloatVariable(
      calendar,
      "cornerRadius",
      "DatePicker/calendar/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      calendar,
      "strokeWeight",
      "DatePicker/calendar/stroke/width",
      variableByName,
      stats,
    );
  }

  if (header) {
    bindFloatVariable(
      header,
      "height",
      "DatePicker/calendar/header/height",
      variableByName,
      stats,
    );
  }

  for (const day of dayNodes || []) {
    bindSizeVariables(
      day,
      "DatePicker/calendar/day/size",
      "DatePicker/calendar/day/size",
      variableByName,
      stats,
    );
    bindFloatVariable(
      day,
      "cornerRadius",
      "DatePicker/calendar/day/radius",
      variableByName,
      stats,
    );
  }

  for (const icon of icons || []) {
    bindSizeVariables(
      icon,
      "DatePicker/icon/size",
      "DatePicker/icon/size",
      variableByName,
      stats,
    );
  }
}

function bindDateRangePickerGeometryVariables(
  component,
  fieldsRow,
  fieldGroups,
  fields,
  calendar,
  monthNodes,
  dayNodes,
  icons,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "DateRangePicker/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "DateRangePicker/gap",
    variableByName,
    stats,
  );

  if (fieldsRow) {
    bindFloatVariable(
      fieldsRow,
      "itemSpacing",
      "DateRangePicker/field-gap",
      variableByName,
      stats,
    );
  }

  for (const group of fieldGroups || []) {
    bindFloatVariable(
      group,
      "width",
      "DateRangePicker/field/width",
      variableByName,
      stats,
    );
    bindFloatVariable(
      group,
      "itemSpacing",
      "DateRangePicker/field-label/gap",
      variableByName,
      stats,
    );
  }

  for (const field of fields || []) {
    bindFloatVariable(
      field,
      "height",
      "DateRangePicker/field/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingLeft",
      "DateRangePicker/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingRight",
      "DateRangePicker/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingTop",
      "DateRangePicker/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingBottom",
      "DateRangePicker/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "cornerRadius",
      "DateRangePicker/field/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "strokeWeight",
      "DateRangePicker/field/stroke/width",
      variableByName,
      stats,
    );
  }

  if (calendar) {
    bindFloatVariable(
      calendar,
      "width",
      "DateRangePicker/calendar/width",
      variableByName,
      stats,
    );
    bindFloatVariable(
      calendar,
      "paddingLeft",
      "DateRangePicker/calendar/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      calendar,
      "paddingRight",
      "DateRangePicker/calendar/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      calendar,
      "paddingTop",
      "DateRangePicker/calendar/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      calendar,
      "paddingBottom",
      "DateRangePicker/calendar/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      calendar,
      "itemSpacing",
      "DateRangePicker/calendar/gap",
      variableByName,
      stats,
    );
    bindFloatVariable(
      calendar,
      "cornerRadius",
      "DateRangePicker/calendar/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      calendar,
      "strokeWeight",
      "DateRangePicker/calendar/stroke/width",
      variableByName,
      stats,
    );
  }

  for (const month of monthNodes || []) {
    bindFloatVariable(
      month,
      "width",
      "DateRangePicker/calendar/month/width",
      variableByName,
      stats,
    );
  }

  for (const day of dayNodes || []) {
    bindSizeVariables(
      day,
      "DateRangePicker/calendar/day/size",
      "DateRangePicker/calendar/day/size",
      variableByName,
      stats,
    );
    bindFloatVariable(
      day,
      "cornerRadius",
      "DateRangePicker/calendar/day/radius",
      variableByName,
      stats,
    );
  }

  for (const icon of icons || []) {
    bindSizeVariables(
      icon,
      "DateRangePicker/icon/size",
      "DateRangePicker/icon/size",
      variableByName,
      stats,
    );
  }
}

function bindTimePickerGeometryVariables(
  component,
  field,
  listbox,
  optionNodes,
  icons,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "TimePicker/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "TimePicker/gap",
    variableByName,
    stats,
  );

  if (field) {
    bindFloatVariable(
      field,
      "height",
      "TimePicker/field/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingLeft",
      "TimePicker/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingRight",
      "TimePicker/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingTop",
      "TimePicker/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingBottom",
      "TimePicker/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "cornerRadius",
      "TimePicker/field/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "strokeWeight",
      "TimePicker/field/stroke/width",
      variableByName,
      stats,
    );
  }

  if (listbox) {
    bindFloatVariable(
      listbox,
      "width",
      "TimePicker/listbox/width",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "paddingLeft",
      "TimePicker/listbox/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "paddingRight",
      "TimePicker/listbox/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "paddingTop",
      "TimePicker/listbox/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "paddingBottom",
      "TimePicker/listbox/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "itemSpacing",
      "TimePicker/listbox/gap",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "cornerRadius",
      "TimePicker/listbox/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      listbox,
      "strokeWeight",
      "TimePicker/listbox/stroke/width",
      variableByName,
      stats,
    );
  }

  for (const option of optionNodes || []) {
    bindFloatVariable(
      option,
      "height",
      "TimePicker/option/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      option,
      "paddingLeft",
      "TimePicker/option/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      option,
      "paddingRight",
      "TimePicker/option/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      option,
      "cornerRadius",
      "TimePicker/option/radius",
      variableByName,
      stats,
    );
  }

  for (const icon of icons || []) {
    bindSizeVariables(
      icon,
      "TimePicker/icon/size",
      "TimePicker/icon/size",
      variableByName,
      stats,
    );
  }
}

function bindFileUploadGeometryVariables(
  component,
  dropzone,
  fileRows,
  icons,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "FileUpload/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "FileUpload/gap",
    variableByName,
    stats,
  );

  if (dropzone) {
    bindFloatVariable(
      dropzone,
      "height",
      "FileUpload/dropzone/min-height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      dropzone,
      "paddingLeft",
      "FileUpload/dropzone/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      dropzone,
      "paddingRight",
      "FileUpload/dropzone/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      dropzone,
      "paddingTop",
      "FileUpload/dropzone/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      dropzone,
      "paddingBottom",
      "FileUpload/dropzone/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      dropzone,
      "cornerRadius",
      "FileUpload/dropzone/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      dropzone,
      "strokeWeight",
      "FileUpload/dropzone/stroke/width",
      variableByName,
      stats,
    );
  }

  for (const row of fileRows || []) {
    bindFloatVariable(
      row,
      "height",
      "FileUpload/file-row/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      row,
      "paddingLeft",
      "FileUpload/file-row/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      row,
      "paddingRight",
      "FileUpload/file-row/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      row,
      "paddingTop",
      "FileUpload/file-row/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      row,
      "paddingBottom",
      "FileUpload/file-row/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      row,
      "itemSpacing",
      "FileUpload/file-row/gap",
      variableByName,
      stats,
    );
    bindFloatVariable(
      row,
      "cornerRadius",
      "FileUpload/file-row/radius",
      variableByName,
      stats,
    );
  }

  for (const icon of icons || []) {
    const token =
      icon.getSharedPluginData &&
      icon.getSharedPluginData(RUN_NAMESPACE, "role") === "file-row-icon"
        ? "FileUpload/file-icon/size"
        : "FileUpload/icon/size";
    bindSizeVariables(icon, token, token, variableByName, stats);
  }
}

function bindColorPickerGeometryVariables(
  component,
  field,
  swatches,
  popover,
  colorArea,
  sliderRails,
  sliderTracks,
  sliderThumbs,
  presets,
  icons,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "ColorPicker/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "ColorPicker/gap",
    variableByName,
    stats,
  );

  if (field) {
    bindFloatVariable(
      field,
      "height",
      "ColorPicker/field/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingLeft",
      "ColorPicker/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingRight",
      "ColorPicker/field/padding/x",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingTop",
      "ColorPicker/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "paddingBottom",
      "ColorPicker/field/padding/y",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "cornerRadius",
      "ColorPicker/field/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      field,
      "strokeWeight",
      "ColorPicker/field/stroke/width",
      variableByName,
      stats,
    );
  }

  for (const swatch of swatches || []) {
    bindSizeVariables(
      swatch,
      "ColorPicker/swatch/size",
      "ColorPicker/swatch/size",
      variableByName,
      stats,
    );
    bindFloatVariable(
      swatch,
      "cornerRadius",
      "ColorPicker/swatch/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      swatch,
      "strokeWeight",
      "ColorPicker/swatch/stroke/width",
      variableByName,
      stats,
    );
  }

  if (popover) {
    bindFloatVariable(
      popover,
      "width",
      "ColorPicker/popover/width",
      variableByName,
      stats,
    );
    bindFloatVariable(
      popover,
      "paddingLeft",
      "ColorPicker/popover/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      popover,
      "paddingRight",
      "ColorPicker/popover/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      popover,
      "paddingTop",
      "ColorPicker/popover/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      popover,
      "paddingBottom",
      "ColorPicker/popover/padding",
      variableByName,
      stats,
    );
    bindFloatVariable(
      popover,
      "itemSpacing",
      "ColorPicker/popover/gap",
      variableByName,
      stats,
    );
    bindFloatVariable(
      popover,
      "cornerRadius",
      "ColorPicker/popover/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      popover,
      "strokeWeight",
      "ColorPicker/popover/stroke/width",
      variableByName,
      stats,
    );
  }

  if (colorArea) {
    bindFloatVariable(
      colorArea,
      "height",
      "ColorPicker/color-area/height",
      variableByName,
      stats,
    );
    bindFloatVariable(
      colorArea,
      "cornerRadius",
      "ColorPicker/color-area/radius",
      variableByName,
      stats,
    );
  }

  for (const rail of sliderRails || []) {
    bindFloatVariable(
      rail,
      "height",
      "ColorPicker/slider/height",
      variableByName,
      stats,
    );
  }

  for (const track of sliderTracks || []) {
    bindFloatVariable(
      track,
      "height",
      "ColorPicker/slider/track/height",
      variableByName,
      stats,
    );
  }

  for (const thumb of sliderThumbs || []) {
    bindSizeVariables(
      thumb,
      "ColorPicker/slider/thumb/size",
      "ColorPicker/slider/thumb/size",
      variableByName,
      stats,
    );
  }

  for (const preset of presets || []) {
    bindSizeVariables(
      preset,
      "ColorPicker/preset/size",
      "ColorPicker/preset/size",
      variableByName,
      stats,
    );
    bindFloatVariable(
      preset,
      "cornerRadius",
      "ColorPicker/preset/radius",
      variableByName,
      stats,
    );
  }

  for (const icon of icons || []) {
    bindSizeVariables(
      icon,
      "ColorPicker/icon/size",
      "ColorPicker/icon/size",
      variableByName,
      stats,
    );
  }
}

function bindSliderGeometryVariables(
  component,
  root,
  track,
  thumbs,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "Slider/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Slider/gap",
    variableByName,
    stats,
  );

  if (root) {
    bindSizeVariables(
      root,
      "Slider/width/default",
      "Slider/height/default",
      variableByName,
      stats,
    );
  }

  if (track) {
    bindFloatVariable(
      track,
      "width",
      "Slider/width/default",
      variableByName,
      stats,
    );
    bindFloatVariable(
      track,
      "height",
      "Slider/track/height",
      variableByName,
      stats,
    );
  }

  const thumbNodes = Array.isArray(thumbs) ? thumbs : [thumbs];
  for (const thumb of thumbNodes) {
    if (!thumb) continue;
    bindSizeVariables(
      thumb,
      "Slider/thumb/size",
      "Slider/thumb/size",
      variableByName,
      stats,
    );
  }
}

function bindProgressGeometryVariables(
  component,
  track,
  range,
  variableByName,
  stats,
) {
  bindSizeVariables(
    component,
    "Progress/width/default",
    "Progress/height/default",
    variableByName,
    stats,
  );

  if (track) {
    bindSizeVariables(
      track,
      "Progress/width/default",
      "Progress/height/default",
      variableByName,
      stats,
    );
    bindFloatVariable(
      track,
      "cornerRadius",
      "Progress/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      track,
      "strokeWeight",
      "Progress/stroke/width",
      variableByName,
      stats,
    );
  }

  if (range) {
    bindFloatVariable(
      range,
      "height",
      "Progress/height/default",
      variableByName,
      stats,
    );
    bindFloatVariable(
      range,
      "cornerRadius",
      "Progress/radius",
      variableByName,
      stats,
    );
  }
}

function bindAvatarGeometryVariables(component, variableByName, stats) {
  bindSizeVariables(
    component,
    "Avatar/size/default",
    "Avatar/size/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "Avatar/radius",
    variableByName,
    stats,
  );
}

function bindAlertGeometryVariables(component, icon, variableByName, stats) {
  bindFloatVariable(
    component,
    "width",
    "Alert/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    "Alert/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    "Alert/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "Alert/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "Alert/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Alert/gap",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "Alert/radius",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "strokeWeight",
    "Alert/stroke/width",
    variableByName,
    stats,
  );

  if (icon) {
    bindSizeVariables(
      icon,
      "Alert/icon/size",
      "Alert/icon/size",
      variableByName,
      stats,
    );
  }
}

function bindEmptyStateGeometryVariables(
  component,
  iconContainer,
  textStack,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "EmptyState/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    "EmptyState/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    "EmptyState/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingTop",
    "EmptyState/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingBottom",
    "EmptyState/padding",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "EmptyState/gap",
    variableByName,
    stats,
  );

  if (textStack) {
    bindFloatVariable(
      textStack,
      "itemSpacing",
      "EmptyState/text/gap",
      variableByName,
      stats,
    );
  }

  if (iconContainer) {
    bindSizeVariables(
      iconContainer,
      "EmptyState/icon/container/size",
      "EmptyState/icon/container/size",
      variableByName,
      stats,
    );
    bindFloatVariable(
      iconContainer,
      "cornerRadius",
      "EmptyState/icon/container/radius",
      variableByName,
      stats,
    );
  }
}

async function buildButtonComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    iconSlotsBound: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Button", stats);

  const existing = findComponentSetOnPage(page, "Button");
  if (existing) {
    normalizeComponentSetNodeName(existing, "Button", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "Button already exists. Delete or rename it before rebuilding.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const textStyle = await ensureButtonLabelTextStyle(fonts, stats);
  const components = [];

  let index = 0;
  for (const variant of BUTTON_VARIANTS) {
    for (const size of BUTTON_SIZES) {
      for (const state of BUTTON_STATES) {
        const component = await createButtonVariant({
          variant,
          size,
          state,
          variableByName,
          fonts,
          textStyle,
          stats,
        });
        component.x = (index % 12) * 180;
        component.y = Math.floor(index / 12) * 96;
        page.appendChild(component);
        components.push(component);
        index += 1;
      }
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "Button";
  componentSet.x = 80;
  componentSet.y = 80;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "Button");
  applyComponentSetDescription(componentSet, "Button", false, [
    "Kozmos Button component set generated from React Button API.",
    "Variant maps to Button.variant.",
    "Size maps to Button.size.",
    "State maps to disabled/isLoading in Code Connect.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureLabelTextProperty(componentSet, "Button", stats);
  configureFocusVisibleProperty(componentSet, stats);
  await configureButtonIconSlot(componentSet, variableByName, stats);
  return stats;
}

async function updateButtonComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsMissing: 0,
    iconSlotsBound: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Button", stats);

  const existing = findComponentSetOnPage(page, "Button");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "Button was not found. Run Build Button first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "Button", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const textStyle = await ensureButtonLabelTextStyle(fonts, stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "Button");
  applyComponentSetDescription(existing, "Button", true, [
    "Kozmos Button component set generated from React Button API.",
    "Variant maps to Button.variant.",
    "Size maps to Button.size.",
    "State maps to disabled/isLoading in Code Connect.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseButtonVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized Button variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[`${props.variant}/${props.size}/${props.state}`] = true;
    await updateButtonVariant(child, {
      variant: props.variant,
      size: props.size,
      state: props.state,
      variableByName,
      fonts,
      textStyle,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const variant of BUTTON_VARIANTS) {
    for (const size of BUTTON_SIZES) {
      for (const state of BUTTON_STATES) {
        const key = `${variant}/${size}/${state}`;
        if (!seenKeys[key]) {
          stats.variantsMissing += 1;
          stats.warnings.push(`Missing Button variant ${key}.`);
        }
      }
    }
  }

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureLabelTextProperty(existing, "Button", stats);
  configureFocusVisibleProperty(existing, stats);
  await configureButtonIconSlot(existing, variableByName, stats);
  await refreshIconButtonSlotsIfPresent(page, variableByName, stats);
  await pruneLegacyGeneratedIconSlotComponents(stats);
  return stats;
}

async function buildIconButtonComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    iconSlotsBound: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "IconButton", stats);

  const existing = findComponentSetOnPage(page, "IconButton");
  if (existing) {
    normalizeComponentSetNodeName(existing, "IconButton", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "IconButton already exists. Use Update IconButton to preserve its node ID.";
    return stats;
  }

  const variableByName = await ensureComponentRuntimeVariables(stats);
  const iconComponent = await resolveDefaultIconSourceComponent(
    variableByName,
    stats,
  );
  await figma.setCurrentPageAsync(page);

  const components = [];
  let index = 0;
  for (const variant of BUTTON_VARIANTS) {
    for (const size of ICON_BUTTON_SIZES) {
      for (const state of BUTTON_STATES) {
        const component = await createIconButtonVariant({
          variant,
          size,
          state,
          variableByName,
          stats,
        });
        component.x = (index % 9) * 120;
        component.y = Math.floor(index / 9) * 88;
        page.appendChild(component);
        components.push(component);
        index += 1;
      }
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "IconButton";
  componentSet.x = 80;
  componentSet.y = 780;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "IconButton");
  applyComponentSetDescription(componentSet, "IconButton", false, [
    "Kozmos IconButton component set generated from React IconButton API.",
    "Variant maps to IconButton.variant.",
    "Size maps to IconButton.size: Default maps to icon, Small maps to sm, Large maps to lg.",
    "State maps to disabled/isLoading in Code Connect.",
    "Icon is an instance-swap slot. Swap with Pointr Icon Library symbols.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  await configureIconButtonSlot(
    componentSet,
    iconComponent,
    variableByName,
    stats,
  );
  configureFocusVisibleProperty(componentSet, stats);
  return stats;
}

async function updateIconButtonComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsMissing: 0,
    iconSlotsBound: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "IconButton", stats);

  const existing = findComponentSetOnPage(page, "IconButton");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "IconButton was not found. Run Build IconButton first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "IconButton", stats);

  const variableByName = await ensureComponentRuntimeVariables(stats);
  const iconComponent = await resolveDefaultIconSourceComponent(
    variableByName,
    stats,
  );
  await figma.setCurrentPageAsync(page);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "IconButton");
  applyComponentSetDescription(existing, "IconButton", true, [
    "Kozmos IconButton component set generated from React IconButton API.",
    "Variant maps to IconButton.variant.",
    "Size maps to IconButton.size: Default maps to icon, Small maps to sm, Large maps to lg.",
    "State maps to disabled/isLoading in Code Connect.",
    "Icon is an instance-swap slot. Swap with Pointr Icon Library symbols.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseIconButtonVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized IconButton variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[`${props.variant}/${props.size}/${props.state}`] = true;
    await updateIconButtonVariant(child, {
      variant: props.variant,
      size: props.size,
      state: props.state,
      variableByName,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const variant of BUTTON_VARIANTS) {
    for (const size of ICON_BUTTON_SIZES) {
      for (const state of BUTTON_STATES) {
        const key = `${variant}/${size}/${state}`;
        if (!seenKeys[key]) {
          stats.variantsMissing += 1;
          stats.warnings.push(`Missing IconButton variant ${key}.`);
        }
      }
    }
  }

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  await configureIconButtonSlot(existing, iconComponent, variableByName, stats);
  configureFocusVisibleProperty(existing, stats);
  await refreshButtonSlotsIfPresent(page, variableByName, stats);
  await pruneLegacyGeneratedIconSlotComponents(stats);
  return stats;
}

async function buildToggleButtonComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "ToggleButton", stats);

  const existing = findComponentSetOnPage(page, "ToggleButton");
  if (existing) {
    normalizeComponentSetNodeName(existing, "ToggleButton", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "ToggleButton already exists. Use Update ToggleButton to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const textStyle = await ensureButtonLabelTextStyle(fonts, stats);
  const components = [];

  for (const variant of TOGGLE_BUTTON_VARIANTS) {
    for (const size of TOGGLE_BUTTON_SIZES) {
      for (const state of TOGGLE_BUTTON_STATES) {
        const component = await createToggleButtonVariant({
          variant,
          size,
          state,
          variableByName,
          fonts,
          textStyle,
          stats,
        });
        page.appendChild(component);
        components.push(component);
      }
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "ToggleButton";
  componentSet.x = 80;
  componentSet.y = 1440;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "ToggleButton");
  applyComponentSetDescription(componentSet, "ToggleButton", false, [
    "Kozmos ToggleButton component set generated from React ToggleButton API.",
    "Variant maps to ToggleButton.variant.",
    "Size maps to ToggleButton.size.",
    "State maps to pressed and disabled in Code Connect.",
    "Focus Visible controls the generated focus ring.",
  ]);
  clearComponentSetContainerFill(componentSet);
  layoutToggleButtonVariants(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureToggleButtonProperties(componentSet, stats);
  return stats;
}

async function updateToggleButtonComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "ToggleButton", stats);

  const existing = findComponentSetOnPage(page, "ToggleButton");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "ToggleButton was not found. Run Build ToggleButton first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "ToggleButton", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const textStyle = await ensureButtonLabelTextStyle(fonts, stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "ToggleButton");
  applyComponentSetDescription(existing, "ToggleButton", true, [
    "Kozmos ToggleButton component set generated from React ToggleButton API.",
    "Variant maps to ToggleButton.variant.",
    "Size maps to ToggleButton.size.",
    "State maps to pressed and disabled in Code Connect.",
    "Focus Visible controls the generated focus ring.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseToggleButtonVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized ToggleButton variant "${child.name}".`,
      );
      continue;
    }

    const key = `${props.variant}/${props.size}/${props.state}`;
    seenKeys[key] = true;
    await updateToggleButtonVariant(child, {
      variant: props.variant,
      size: props.size,
      state: props.state,
      variableByName,
      fonts,
      textStyle,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const variant of TOGGLE_BUTTON_VARIANTS) {
    for (const size of TOGGLE_BUTTON_SIZES) {
      for (const state of TOGGLE_BUTTON_STATES) {
        const key = `${variant}/${size}/${state}`;
        if (seenKeys[key]) continue;
        const component = await createToggleButtonVariant({
          variant,
          size,
          state,
          variableByName,
          fonts,
          textStyle,
          stats,
        });
        existing.appendChild(component);
        seenKeys[key] = true;
        stats.variantsCreated += 1;
      }
    }
  }

  layoutToggleButtonVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureToggleButtonProperties(existing, stats);
  return stats;
}

async function buildSplitButtonComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "SplitButton", stats);

  const existing = findComponentSetOnPage(page, "SplitButton");
  if (existing) {
    normalizeComponentSetNodeName(existing, "SplitButton", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "SplitButton already exists. Use Update SplitButton to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const textStyle = await ensureButtonLabelTextStyle(fonts, stats);
  const components = [];

  for (const variant of SPLIT_BUTTON_VARIANTS) {
    for (const size of SPLIT_BUTTON_SIZES) {
      for (const state of SPLIT_BUTTON_STATES) {
        const component = await createSplitButtonVariant({
          variant,
          size,
          state,
          variableByName,
          fonts,
          textStyle,
          stats,
        });
        page.appendChild(component);
        components.push(component);
      }
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "SplitButton";
  componentSet.x = 80;
  componentSet.y = 2080;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "SplitButton");
  applyComponentSetDescription(componentSet, "SplitButton", false, [
    "Kozmos SplitButton component set generated from React SplitButton API.",
    "Variant maps to SplitButton.variant.",
    "Size maps to SplitButton.size.",
    "State maps to disabled and focus examples in Code Connect.",
    "Label Text maps to children; the chevron trigger remains component anatomy.",
  ]);
  clearComponentSetContainerFill(componentSet);
  layoutSplitButtonVariants(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureSplitButtonProperties(componentSet, stats);
  return stats;
}

async function updateSplitButtonComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "SplitButton", stats);

  const existing = findComponentSetOnPage(page, "SplitButton");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "SplitButton was not found. Run Build SplitButton first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "SplitButton", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const textStyle = await ensureButtonLabelTextStyle(fonts, stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "SplitButton");
  applyComponentSetDescription(existing, "SplitButton", true, [
    "Kozmos SplitButton component set generated from React SplitButton API.",
    "Variant maps to SplitButton.variant.",
    "Size maps to SplitButton.size.",
    "State maps to disabled and focus examples in Code Connect.",
    "Label Text maps to children; the chevron trigger remains component anatomy.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseSplitButtonVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized SplitButton variant "${child.name}".`,
      );
      continue;
    }

    const key = `${props.variant}/${props.size}/${props.state}`;
    seenKeys[key] = true;
    await updateSplitButtonVariant(child, {
      variant: props.variant,
      size: props.size,
      state: props.state,
      variableByName,
      fonts,
      textStyle,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const variant of SPLIT_BUTTON_VARIANTS) {
    for (const size of SPLIT_BUTTON_SIZES) {
      for (const state of SPLIT_BUTTON_STATES) {
        const key = `${variant}/${size}/${state}`;
        if (seenKeys[key]) continue;
        const component = await createSplitButtonVariant({
          variant,
          size,
          state,
          variableByName,
          fonts,
          textStyle,
          stats,
        });
        existing.appendChild(component);
        seenKeys[key] = true;
        stats.variantsCreated += 1;
      }
    }
  }

  layoutSplitButtonVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureSplitButtonProperties(existing, stats);
  return stats;
}

async function buildFloatingActionButtonComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    iconSlotsBound: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "FloatingActionButton", stats);

  const existing = findComponentSetOnPage(page, "FloatingActionButton");
  if (existing) {
    normalizeComponentSetNodeName(existing, "FloatingActionButton", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "FloatingActionButton already exists. Use Update FloatingActionButton to preserve its node ID.";
    return stats;
  }

  const variableByName = await ensureComponentRuntimeVariables(stats);
  const iconComponent = await resolveDefaultIconSourceComponent(
    variableByName,
    stats,
  );
  await figma.setCurrentPageAsync(page);
  const components = [];

  for (const variant of FLOATING_ACTION_BUTTON_VARIANTS) {
    for (const size of FLOATING_ACTION_BUTTON_SIZES) {
      for (const state of FLOATING_ACTION_BUTTON_STATES) {
        const component = await createFloatingActionButtonVariant({
          variant,
          size,
          state,
          iconComponent,
          variableByName,
          stats,
        });
        page.appendChild(component);
        components.push(component);
      }
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "FloatingActionButton";
  componentSet.x = 80;
  componentSet.y = 2800;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(
    RUN_NAMESPACE,
    "component",
    "FloatingActionButton",
  );
  applyComponentSetDescription(componentSet, "FloatingActionButton", false, [
    "Kozmos FloatingActionButton component set generated from React FloatingActionButton API.",
    "Variant maps to FloatingActionButton.variant.",
    "Size maps to FloatingActionButton.size.",
    "State maps to disabled and focus examples in Code Connect.",
    "Icon is an instance-swap slot. Placement belongs to consuming layouts.",
  ]);
  clearComponentSetContainerFill(componentSet);
  layoutFloatingActionButtonVariants(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  await configureFloatingActionButtonProperties(
    componentSet,
    iconComponent,
    variableByName,
    stats,
  );
  return stats;
}

async function updateFloatingActionButtonComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    iconSlotsBound: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "FloatingActionButton", stats);

  const existing = findComponentSetOnPage(page, "FloatingActionButton");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message =
      "FloatingActionButton was not found. Run Build FloatingActionButton first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "FloatingActionButton", stats);

  const variableByName = await ensureComponentRuntimeVariables(stats);
  const iconComponent = await resolveDefaultIconSourceComponent(
    variableByName,
    stats,
  );
  await figma.setCurrentPageAsync(page);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(
    RUN_NAMESPACE,
    "component",
    "FloatingActionButton",
  );
  applyComponentSetDescription(existing, "FloatingActionButton", true, [
    "Kozmos FloatingActionButton component set generated from React FloatingActionButton API.",
    "Variant maps to FloatingActionButton.variant.",
    "Size maps to FloatingActionButton.size.",
    "State maps to disabled and focus examples in Code Connect.",
    "Icon is an instance-swap slot. Placement belongs to consuming layouts.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseFloatingActionButtonVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized FloatingActionButton variant "${child.name}".`,
      );
      continue;
    }

    const key = `${props.variant}/${props.size}/${props.state}`;
    seenKeys[key] = true;
    await updateFloatingActionButtonVariant(child, {
      variant: props.variant,
      size: props.size,
      state: props.state,
      iconComponent,
      variableByName,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const variant of FLOATING_ACTION_BUTTON_VARIANTS) {
    for (const size of FLOATING_ACTION_BUTTON_SIZES) {
      for (const state of FLOATING_ACTION_BUTTON_STATES) {
        const key = `${variant}/${size}/${state}`;
        if (seenKeys[key]) continue;
        const component = await createFloatingActionButtonVariant({
          variant,
          size,
          state,
          iconComponent,
          variableByName,
          stats,
        });
        existing.appendChild(component);
        seenKeys[key] = true;
        stats.variantsCreated += 1;
      }
    }
  }

  layoutFloatingActionButtonVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  await configureFloatingActionButtonProperties(
    existing,
    iconComponent,
    variableByName,
    stats,
  );
  return stats;
}

async function buildTextComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Text", stats);

  const existing = findComponentSetOnPage(page, "Text");
  if (existing) {
    normalizeComponentSetNodeName(existing, "Text", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "Text already exists. Use Update Text to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  await ensureKozmosTextStyles(fonts, stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (const combination of textVariantCombinations()) {
    const component = await createTextVariant({
      size: combination.size,
      weight: combination.weight,
      tone: combination.tone,
      variableByName,
      fonts,
      stats,
    });
    page.appendChild(component);
    components.push(component);
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "Text";
  componentSet.x = 80;
  componentSet.y = 7600;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "Text");
  applyComponentSetDescription(componentSet, "Text", false, [
    "Kozmos Text component set generated from React Text API.",
    "Size maps to Text.size.",
    "Weight maps to Text.weight.",
    "Tone maps to Text.color in Code Connect.",
    "Text maps to children in Code Connect.",
  ]);
  clearComponentSetContainerFill(componentSet);
  layoutTextVariants(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureTextProperties(componentSet, stats);
  return stats;
}

async function updateTextComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Text", stats);

  const existing = findComponentSetOnPage(page, "Text");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "Text was not found. Run Build Text first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "Text", stats);

  const fonts = await loadButtonFonts(stats);
  await ensureKozmosTextStyles(fonts, stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "Text");
  applyComponentSetDescription(existing, "Text", true, [
    "Kozmos Text component set generated from React Text API.",
    "Size maps to Text.size.",
    "Weight maps to Text.weight.",
    "Tone maps to Text.color in Code Connect.",
    "Text maps to children in Code Connect.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseTextVariantName(child.name);
    if (!props) {
      stats.warnings.push(`Skipped unrecognized Text variant "${child.name}".`);
      continue;
    }

    seenKeys[textVariantKey(props)] = true;
    await updateTextVariant(child, {
      size: props.size,
      weight: props.weight,
      tone: props.tone,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const combination of textVariantCombinations()) {
    const key = textVariantKey(combination);
    if (seenKeys[key]) continue;

    const component = await createTextVariant({
      size: combination.size,
      weight: combination.weight,
      tone: combination.tone,
      variableByName,
      fonts,
      stats,
    });
    existing.appendChild(component);
    seenKeys[key] = true;
    stats.variantsCreated += 1;
  }

  layoutTextVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureTextProperties(existing, stats);
  return stats;
}

async function buildHeadingComponent() {
  return buildSingleAxisComponent({
    componentName: "Heading",
    componentSetName: "Heading",
    axisName: "Level",
    values: HEADING_LEVELS,
    x: 80,
    y: 8660,
    xStep: 260,
    createVariant: createHeadingVariant,
    configureProperties: configureHeadingProperties,
    description: [
      "Kozmos Heading component set generated from React Heading API.",
      "Level maps to Heading.level.",
      "Heading Text maps to children in Code Connect.",
      "Heading typography reuses Text scale variables.",
    ],
  });
}

async function updateHeadingComponent() {
  return updateSingleAxisComponent({
    componentName: "Heading",
    componentSetName: "Heading",
    axisName: "Level",
    values: HEADING_LEVELS,
    xStep: 260,
    createVariant: createHeadingVariant,
    updateVariant: updateHeadingVariant,
    parseVariantName: parseHeadingVariantName,
    configureProperties: configureHeadingProperties,
    description: [
      "Kozmos Heading component set generated from React Heading API.",
      "Level maps to Heading.level.",
      "Heading Text maps to children in Code Connect.",
      "Heading typography reuses Text scale variables.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildLinkComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Link", stats);

  const existing = findComponentSetOnPage(page, "Link");
  if (existing) {
    normalizeComponentSetNodeName(existing, "Link", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "Link already exists. Use Update Link to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  let index = 0;
  for (const variant of LINK_VARIANTS) {
    for (const state of LINK_STATES) {
      const component = await createLinkVariant({
        variant,
        state,
        variableByName,
        fonts,
        stats,
      });
      component.x = index * 160;
      component.y = 0;
      page.appendChild(component);
      components.push(component);
      index += 1;
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "Link";
  componentSet.x = 80;
  componentSet.y = 9100;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "Link");
  applyComponentSetDescription(componentSet, "Link", false, [
    "Kozmos Link component set generated from React Link API.",
    "Variant maps to Link.variant.",
    "State provides focus-visible examples.",
    "Link Text maps to children in Code Connect.",
    "Focus Visible controls the generated focus ring.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureLinkProperties(componentSet, stats);
  return stats;
}

async function updateLinkComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Link", stats);

  const existing = findComponentSetOnPage(page, "Link");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "Link was not found. Run Build Link first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "Link", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "Link");
  applyComponentSetDescription(existing, "Link", true, [
    "Kozmos Link component set generated from React Link API.",
    "Variant maps to Link.variant.",
    "State provides focus-visible examples.",
    "Link Text maps to children in Code Connect.",
    "Focus Visible controls the generated focus ring.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseLinkVariantName(child.name);
    if (!props) {
      stats.warnings.push(`Skipped unrecognized Link variant "${child.name}".`);
      continue;
    }

    seenKeys[`${props.variant}/${props.state}`] = true;
    await updateLinkVariant(child, {
      variant: props.variant,
      state: props.state,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const variant of LINK_VARIANTS) {
    for (const state of LINK_STATES) {
      const key = `${variant}/${state}`;
      if (!seenKeys[key]) {
        stats.variantsMissing += 1;
        stats.warnings.push(`Missing Link variant ${key}.`);
      }
    }
  }

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureLinkProperties(existing, stats);
  return stats;
}

async function buildLabelComponent() {
  return buildSingleAxisComponent({
    componentName: "Label",
    componentSetName: "Label",
    axisName: "State",
    values: LABEL_STATES,
    x: 80,
    y: 9420,
    xStep: 220,
    createVariant: createLabelVariant,
    configureProperties: configureLabelProperties,
    description: [
      "Kozmos Label component set generated from React Label API.",
      "State maps to disabled examples.",
      "Label Text maps to children in Code Connect.",
      "Typography aligns to the shared form label scale.",
    ],
  });
}

async function updateLabelComponent() {
  return updateSingleAxisComponent({
    componentName: "Label",
    componentSetName: "Label",
    axisName: "State",
    values: LABEL_STATES,
    xStep: 220,
    createVariant: createLabelVariant,
    updateVariant: updateLabelVariant,
    parseVariantName: parseLabelVariantName,
    configureProperties: configureLabelProperties,
    description: [
      "Kozmos Label component set generated from React Label API.",
      "State maps to disabled examples.",
      "Label Text maps to children in Code Connect.",
      "Typography aligns to the shared form label scale.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildSeparatorComponent() {
  return buildSingleAxisComponent({
    componentName: "Separator",
    componentSetName: "Separator",
    axisName: "Orientation",
    values: SEPARATOR_ORIENTATIONS,
    x: 80,
    y: 9740,
    xStep: 360,
    createVariant: createSeparatorVariant,
    configureProperties: configureSeparatorProperties,
    description: [
      "Kozmos Separator component set generated from React Separator API.",
      "Orientation maps to Separator.orientation.",
      "Sizing uses component float variables.",
      "Separator is non-interactive and decorative unless product code says otherwise.",
    ],
  });
}

async function updateSeparatorComponent() {
  return updateSingleAxisComponent({
    componentName: "Separator",
    componentSetName: "Separator",
    axisName: "Orientation",
    values: SEPARATOR_ORIENTATIONS,
    xStep: 360,
    createVariant: createSeparatorVariant,
    updateVariant: updateSeparatorVariant,
    parseVariantName: parseSeparatorVariantName,
    configureProperties: configureSeparatorProperties,
    description: [
      "Kozmos Separator component set generated from React Separator API.",
      "Orientation maps to Separator.orientation.",
      "Sizing uses component float variables.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildSkeletonComponent() {
  return buildSingleAxisComponent({
    componentName: "Skeleton",
    componentSetName: "Skeleton",
    axisName: "Shape",
    values: SKELETON_SHAPES,
    x: 80,
    y: 10060,
    xStep: 360,
    createVariant: createSkeletonVariant,
    configureProperties: configureSkeletonProperties,
    description: [
      "Kozmos Skeleton component set generated from React Skeleton API.",
      "Shape maps to common placeholder compositions.",
      "Animation remains a runtime concern.",
      "Sizing uses component float variables.",
    ],
  });
}

async function updateSkeletonComponent() {
  return updateSingleAxisComponent({
    componentName: "Skeleton",
    componentSetName: "Skeleton",
    axisName: "Shape",
    values: SKELETON_SHAPES,
    xStep: 360,
    createVariant: createSkeletonVariant,
    updateVariant: updateSkeletonVariant,
    parseVariantName: parseSkeletonVariantName,
    configureProperties: configureSkeletonProperties,
    description: [
      "Kozmos Skeleton component set generated from React Skeleton API.",
      "Shape maps to common placeholder compositions.",
      "Animation remains a runtime concern.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildBoxComponent() {
  return buildSingleAxisComponent({
    componentName: "Box",
    componentSetName: "Box",
    axisName: "Surface",
    values: BOX_SURFACES,
    x: 80,
    y: 10380,
    xStep: 360,
    createVariant: createBoxVariant,
    configureProperties: configureBoxProperties,
    description: [
      "Kozmos Box component set generated from React Box API.",
      "Surface maps to common composition examples.",
      "Content Slot maps to children in Code Connect.",
      "Runtime Box remains a lightweight polymorphic wrapper.",
    ],
  });
}

async function updateBoxComponent() {
  return updateSingleAxisComponent({
    componentName: "Box",
    componentSetName: "Box",
    axisName: "Surface",
    values: BOX_SURFACES,
    xStep: 360,
    createVariant: createBoxVariant,
    updateVariant: updateBoxVariant,
    parseVariantName: parseBoxVariantName,
    configureProperties: configureBoxProperties,
    description: [
      "Kozmos Box component set generated from React Box API.",
      "Surface maps to common composition examples.",
      "Content Slot maps to children in Code Connect.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildStackComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Stack", stats);

  const existing = findComponentSetOnPage(page, "Stack");
  if (existing) {
    normalizeComponentSetNodeName(existing, "Stack", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "Stack already exists. Use Update Stack to preserve its node ID.";
    return stats;
  }

  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (const combination of stackVariantCombinations()) {
    const component = await createStackVariant({
      direction: combination.direction,
      gap: combination.gap,
      variableByName,
      stats,
    });
    page.appendChild(component);
    components.push(component);
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "Stack";
  componentSet.x = 80;
  componentSet.y = 10820;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "Stack");
  applyComponentSetDescription(componentSet, "Stack", false, [
    "Kozmos Stack component set generated from React Stack API.",
    "Direction maps to Stack.direction.",
    "Gap maps to Stack.gap.",
    "Content Slot maps to children in Code Connect.",
    "Alignment, justification, and wrapping remain product-code composition choices.",
  ]);
  clearComponentSetContainerFill(componentSet);
  layoutStackVariants(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureStackProperties(componentSet, stats);
  return stats;
}

async function updateStackComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Stack", stats);

  const existing = findComponentSetOnPage(page, "Stack");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "Stack was not found. Run Build Stack first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "Stack", stats);

  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "Stack");
  applyComponentSetDescription(existing, "Stack", true, [
    "Kozmos Stack component set generated from React Stack API.",
    "Direction maps to Stack.direction.",
    "Gap maps to Stack.gap.",
    "Content Slot maps to children in Code Connect.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseStackVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized Stack variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[stackVariantKey(props)] = true;
    await updateStackVariant(child, {
      direction: props.direction,
      gap: props.gap,
      variableByName,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const combination of stackVariantCombinations()) {
    const key = stackVariantKey(combination);
    if (seenKeys[key]) continue;

    const component = await createStackVariant({
      direction: combination.direction,
      gap: combination.gap,
      variableByName,
      stats,
    });
    existing.appendChild(component);
    seenKeys[key] = true;
    stats.variantsCreated += 1;
  }

  layoutStackVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureStackProperties(existing, stats);
  return stats;
}

async function buildGridComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Grid", stats);

  const existing = findComponentSetOnPage(page, "Grid");
  if (existing) {
    normalizeComponentSetNodeName(existing, "Grid", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "Grid already exists. Use Update Grid to preserve its node ID.";
    return stats;
  }

  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (const combination of gridVariantCombinations()) {
    const component = await createGridVariant({
      columns: combination.columns,
      gap: combination.gap,
      variableByName,
      stats,
    });
    page.appendChild(component);
    components.push(component);
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "Grid";
  componentSet.x = 80;
  componentSet.y = 11240;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "Grid");
  applyComponentSetDescription(componentSet, "Grid", false, [
    "Kozmos Grid component set generated from React Grid API.",
    "Columns maps to Grid.cols.",
    "Gap maps to Grid.gap.",
    "Content Slot maps to children in Code Connect.",
    "Alignment, spans, and responsive behavior remain product-code composition choices.",
  ]);
  clearComponentSetContainerFill(componentSet);
  layoutGridVariants(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureGridProperties(componentSet, stats);
  return stats;
}

async function updateGridComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Grid", stats);

  const existing = findComponentSetOnPage(page, "Grid");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "Grid was not found. Run Build Grid first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "Grid", stats);

  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "Grid");
  applyComponentSetDescription(existing, "Grid", true, [
    "Kozmos Grid component set generated from React Grid API.",
    "Columns maps to Grid.cols.",
    "Gap maps to Grid.gap.",
    "Content Slot maps to children in Code Connect.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseGridVariantName(child.name);
    if (!props) {
      stats.warnings.push(`Skipped unrecognized Grid variant "${child.name}".`);
      continue;
    }

    seenKeys[gridVariantKey(props)] = true;
    await updateGridVariant(child, {
      columns: props.columns,
      gap: props.gap,
      variableByName,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const combination of gridVariantCombinations()) {
    const key = gridVariantKey(combination);
    if (seenKeys[key]) continue;

    const component = await createGridVariant({
      columns: combination.columns,
      gap: combination.gap,
      variableByName,
      stats,
    });
    existing.appendChild(component);
    seenKeys[key] = true;
    stats.variantsCreated += 1;
  }

  layoutGridVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureGridProperties(existing, stats);
  return stats;
}

async function buildContainerComponent() {
  return buildSingleAxisComponent({
    componentName: "Container",
    componentSetName: "Container",
    axisName: "Centered",
    values: CONTAINER_CENTERED,
    x: 80,
    y: 11380,
    xStep: 560,
    createVariant: createContainerVariant,
    configureProperties: configureContainerProperties,
    description: [
      "Kozmos Container component set generated from React Container API.",
      "Centered maps to Container.centered.",
      "Content Slot maps to children in Code Connect.",
      "Responsive padding and max-width behavior remain runtime CSS concerns.",
    ],
  });
}

async function updateContainerComponent() {
  return updateSingleAxisComponent({
    componentName: "Container",
    componentSetName: "Container",
    axisName: "Centered",
    values: CONTAINER_CENTERED,
    xStep: 560,
    createVariant: createContainerVariant,
    updateVariant: updateContainerVariant,
    parseVariantName: parseContainerVariantName,
    configureProperties: configureContainerProperties,
    description: [
      "Kozmos Container component set generated from React Container API.",
      "Centered maps to Container.centered.",
      "Content Slot maps to children in Code Connect.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildBreadcrumbComponent() {
  return buildSingleAxisComponent({
    componentName: "Breadcrumb",
    componentSetName: "Breadcrumb",
    axisName: "Content",
    values: BREADCRUMB_CONTENT,
    x: 80,
    y: 11780,
    xStep: 440,
    createVariant: createBreadcrumbVariant,
    configureProperties: configureBreadcrumbProperties,
    description: [
      "Kozmos Breadcrumb component set generated from React Breadcrumb anatomy.",
      "Content maps to composed BreadcrumbList examples.",
      "Item text properties map to BreadcrumbLink and BreadcrumbPage children.",
      "Separators and ellipsis remain product-code composition primitives.",
    ],
  });
}

async function updateBreadcrumbComponent() {
  return updateSingleAxisComponent({
    componentName: "Breadcrumb",
    componentSetName: "Breadcrumb",
    axisName: "Content",
    values: BREADCRUMB_CONTENT,
    xStep: 440,
    createVariant: createBreadcrumbVariant,
    updateVariant: updateBreadcrumbVariant,
    parseVariantName: parseBreadcrumbVariantName,
    configureProperties: configureBreadcrumbProperties,
    description: [
      "Kozmos Breadcrumb component set generated from React Breadcrumb anatomy.",
      "Content maps to composed BreadcrumbList examples.",
      "Item text properties map to BreadcrumbLink and BreadcrumbPage children.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildPaginationComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Pagination", stats);

  const existing = findComponentSetOnPage(page, "Pagination");
  if (existing) {
    normalizeComponentSetNodeName(existing, "Pagination", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "Pagination already exists. Use Update Pagination to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  await ensureKozmosTextStyles(fonts, stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (const content of PAGINATION_CONTENT) {
    for (const size of PAGINATION_SIZES) {
      const component = await createPaginationVariant({
        content,
        size,
        variableByName,
        fonts,
        stats,
      });
      page.appendChild(component);
      components.push(component);
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "Pagination";
  componentSet.x = 80;
  componentSet.y = 12060;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "Pagination");
  applyComponentSetDescription(componentSet, "Pagination", false, [
    "Kozmos Pagination component set generated from React Pagination anatomy.",
    "Content maps to composed PaginationContent examples.",
    "Size maps to PaginationLink.size for page links and previous/next controls.",
    "Page text properties map to PaginationLink children.",
    "Product code owns hrefs, routing, total page count, and disabled edge states.",
  ]);
  clearComponentSetContainerFill(componentSet);
  layoutPaginationVariants(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configurePaginationProperties(componentSet, stats);
  return stats;
}

async function updatePaginationComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Pagination", stats);

  const existing = findComponentSetOnPage(page, "Pagination");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "Pagination was not found. Run Build Pagination first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "Pagination", stats);

  const fonts = await loadButtonFonts(stats);
  await ensureKozmosTextStyles(fonts, stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "Pagination");
  applyComponentSetDescription(existing, "Pagination", true, [
    "Kozmos Pagination component set generated from React Pagination anatomy.",
    "Content maps to composed PaginationContent examples.",
    "Size maps to PaginationLink.size for page links and previous/next controls.",
    "Page text properties map to PaginationLink children.",
    "Product code owns hrefs, routing, total page count, and disabled edge states.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parsePaginationVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized Pagination variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[paginationVariantKey(props)] = true;
    await updatePaginationVariant(child, {
      content: props.content,
      size: props.size,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const content of PAGINATION_CONTENT) {
    for (const size of PAGINATION_SIZES) {
      const props = { content, size };
      const key = paginationVariantKey(props);
      if (seenKeys[key]) continue;

      const component = await createPaginationVariant({
        content,
        size,
        variableByName,
        fonts,
        stats,
      });
      existing.appendChild(component);
      seenKeys[key] = true;
      stats.variantsCreated += 1;
    }
  }

  layoutPaginationVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configurePaginationProperties(existing, stats);
  return stats;
}

async function buildAccordionComponent() {
  return buildSingleAxisComponent({
    componentName: "Accordion",
    componentSetName: "Accordion",
    axisName: "State",
    values: ACCORDION_STATES,
    x: 80,
    y: 12100,
    xStep: 440,
    createVariant: createAccordionVariant,
    configureProperties: configureAccordionProperties,
    description: [
      "Kozmos Accordion component set generated from React Accordion anatomy.",
      "State maps to closed and open data-state examples.",
      "Trigger Text maps to AccordionTrigger children.",
      "Content Text maps to AccordionContent children when open.",
    ],
  });
}

async function updateAccordionComponent() {
  return updateSingleAxisComponent({
    componentName: "Accordion",
    componentSetName: "Accordion",
    axisName: "State",
    values: ACCORDION_STATES,
    xStep: 440,
    createVariant: createAccordionVariant,
    updateVariant: updateAccordionVariant,
    parseVariantName: parseAccordionVariantName,
    configureProperties: configureAccordionProperties,
    description: [
      "Kozmos Accordion component set generated from React Accordion anatomy.",
      "State maps to closed and open data-state examples.",
      "Trigger Text maps to AccordionTrigger children.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildCounterComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Counter", stats);

  const existing = findComponentSetOnPage(page, "Counter");
  if (existing) {
    normalizeComponentSetNodeName(existing, "Counter", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "Counter already exists. Use Update Counter to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  let index = 0;
  for (const tone of COUNTER_TONES) {
    for (const size of COUNTER_SIZES) {
      const component = await createCounterVariant({
        tone,
        size,
        variableByName,
        fonts,
        stats,
      });
      component.x = (index % 4) * 112;
      component.y = Math.floor(index / 4) * 72;
      page.appendChild(component);
      components.push(component);
      index += 1;
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "Counter";
  componentSet.x = 80;
  componentSet.y = 1280;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "Counter");
  applyComponentSetDescription(componentSet, "Counter", false, [
    "Kozmos Counter component set generated from React Counter API.",
    "Tone maps to Counter.tone.",
    "Size maps to Counter.size.",
    "Counter Text maps to Counter children in Code Connect.",
    "Badge composes Counter as a hidden nested instance for opt-in count display.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Counter Text",
    "Counter Text",
    "2",
    stats,
  );
  return stats;
}

async function updateCounterComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Counter", stats);

  const existing = findComponentSetOnPage(page, "Counter");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "Counter was not found. Run Build Counter first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "Counter", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "Counter");
  applyComponentSetDescription(existing, "Counter", true, [
    "Kozmos Counter component set generated from React Counter API.",
    "Tone maps to Counter.tone.",
    "Size maps to Counter.size.",
    "Counter Text maps to Counter children in Code Connect.",
    "Badge composes Counter as a hidden nested instance for opt-in count display.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseCounterVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized Counter variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[`${props.tone}/${props.size}`] = true;
    await updateCounterVariant(child, {
      tone: props.tone,
      size: props.size,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const tone of COUNTER_TONES) {
    for (const size of COUNTER_SIZES) {
      const key = `${tone}/${size}`;
      if (!seenKeys[key]) {
        stats.variantsMissing += 1;
        stats.warnings.push(`Missing Counter variant ${key}.`);
      }
    }
  }

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureNamedTextProperty(
    existing,
    "Counter Text",
    "Counter Text",
    "2",
    stats,
  );
  return stats;
}

async function buildBadgeComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Badge", stats);

  const existing = findComponentSetOnPage(page, "Badge");
  if (existing) {
    normalizeComponentSetNodeName(existing, "Badge", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "Badge already exists. Use Update Badge to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  await ensureCounterComponentForBadge(stats);
  const iconComponent = await resolveDefaultIconSourceComponent(
    variableByName,
    stats,
  );
  const components = [];

  let index = 0;
  for (const variant of BADGE_VARIANTS) {
    for (const size of BADGE_SIZES) {
      const component = await createBadgeVariant({
        variant,
        size,
        variableByName,
        iconComponent,
        fonts,
        stats,
      });
      component.x = (index % 8) * 148;
      component.y = Math.floor(index / 8) * 88;
      page.appendChild(component);
      components.push(component);
      index += 1;
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "Badge";
  componentSet.x = 80;
  componentSet.y = 1440;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "Badge");
  applyComponentSetDescription(componentSet, "Badge", false, [
    "Kozmos Badge component set generated from React Badge API.",
    "Variant maps to Badge.variant.",
    "Size maps to Badge.size.",
    "Label Text maps to Badge children in Code Connect.",
    "Icon maps to Badge.icon for icon-sized badges.",
    "Show Counter toggles a hidden nested Counter instance for opt-in count display.",
    "Counter text is edited on the exposed nested Counter instance.",
    "Badge is non-interactive by default; React includes focus-visible classes for composed interactive usage.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureLabelTextProperty(componentSet, "Badge", stats);
  deleteComponentPropertiesByBaseName(
    componentSet,
    ["Counter Text"],
    ["TEXT"],
    stats,
  );
  configureBadgeCounterVisibilityProperty(componentSet, stats);
  await configureBadgeIconSlot(
    componentSet,
    iconComponent,
    variableByName,
    stats,
  );
  return stats;
}

async function updateBadgeComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Badge", stats);

  const existing = findComponentSetOnPage(page, "Badge");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "Badge was not found. Run Build Badge first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "Badge", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  await ensureCounterComponentForBadge(stats);
  const iconComponent = await resolveDefaultIconSourceComponent(
    variableByName,
    stats,
  );
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "Badge");
  applyComponentSetDescription(existing, "Badge", true, [
    "Kozmos Badge component set generated from React Badge API.",
    "Variant maps to Badge.variant.",
    "Size maps to Badge.size.",
    "Label Text maps to Badge children in Code Connect.",
    "Icon maps to Badge.icon for icon-sized badges.",
    "Show Counter toggles a hidden nested Counter instance for opt-in count display.",
    "Counter text is edited on the exposed nested Counter instance.",
    "Badge is non-interactive by default; React includes focus-visible classes for composed interactive usage.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseBadgeVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized Badge variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[`${props.variant}/${props.size}`] = true;
    await updateBadgeVariant(child, {
      variant: props.variant,
      size: props.size,
      variableByName,
      iconComponent,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const variant of BADGE_VARIANTS) {
    for (const size of BADGE_SIZES) {
      const key = `${variant}/${size}`;
      if (!seenKeys[key]) {
        stats.variantsMissing += 1;
        stats.warnings.push(`Missing Badge variant ${key}.`);
      }
    }
  }

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureLabelTextProperty(existing, "Badge", stats);
  deleteComponentPropertiesByBaseName(
    existing,
    ["Counter Text"],
    ["TEXT"],
    stats,
  );
  configureBadgeCounterVisibilityProperty(existing, stats);
  await configureBadgeIconSlot(existing, iconComponent, variableByName, stats);
  return stats;
}

async function buildChipComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Chip", stats);

  const existing = findComponentSetOnPage(page, "Chip");
  if (existing) {
    normalizeComponentSetNodeName(existing, "Chip", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "Chip already exists. Use Update Chip to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  await ensureKozmosTextStyles(fonts, stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  let index = 0;
  for (const variant of CHIP_VARIANTS) {
    for (const size of CHIP_SIZES) {
      for (const state of CHIP_STATES) {
        for (const removable of CHIP_REMOVABLE) {
          const component = await createChipVariant({
            variant,
            size,
            state,
            removable,
            variableByName,
            fonts,
            stats,
          });
          component.x = (index % 6) * 180;
          component.y = Math.floor(index / 6) * 76;
          page.appendChild(component);
          components.push(component);
          index += 1;
        }
      }
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "Chip";
  componentSet.x = 80;
  componentSet.y = 1600;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "Chip");
  applyComponentSetDescription(componentSet, "Chip", false, [
    "Kozmos Chip component set generated from React Chip API.",
    "Variant maps to Chip.variant.",
    "Size maps to Chip.size.",
    "State maps to selected and disabled props in Code Connect.",
    "Removable maps to the presence of Chip.onRemove.",
    "Label Text maps to Chip children in Code Connect.",
    "Focus follows the React focus-visible ring.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureChipProperties(componentSet, stats);
  return stats;
}

async function updateChipComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Chip", stats);

  const existing = findComponentSetOnPage(page, "Chip");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "Chip was not found. Run Build Chip first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "Chip", stats);

  const fonts = await loadButtonFonts(stats);
  await ensureKozmosTextStyles(fonts, stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "Chip");
  applyComponentSetDescription(existing, "Chip", true, [
    "Kozmos Chip component set generated from React Chip API.",
    "Variant maps to Chip.variant.",
    "Size maps to Chip.size.",
    "State maps to selected and disabled props in Code Connect.",
    "Removable maps to the presence of Chip.onRemove.",
    "Label Text maps to Chip children in Code Connect.",
    "Focus follows the React focus-visible ring.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseChipVariantName(child.name);
    if (!props) {
      stats.warnings.push(`Skipped unrecognized Chip variant "${child.name}".`);
      continue;
    }

    seenKeys[chipVariantKey(props)] = true;
    await updateChipVariant(child, {
      variant: props.variant,
      size: props.size,
      state: props.state,
      removable: props.removable,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const variant of CHIP_VARIANTS) {
    for (const size of CHIP_SIZES) {
      for (const state of CHIP_STATES) {
        for (const removable of CHIP_REMOVABLE) {
          const props = { variant, size, state, removable };
          const key = chipVariantKey(props);
          if (seenKeys[key]) continue;

          const component = await createChipVariant({
            variant,
            size,
            state,
            removable,
            variableByName,
            fonts,
            stats,
          });
          existing.appendChild(component);
          seenKeys[key] = true;
          stats.variantsCreated += 1;
        }
      }
    }
  }

  layoutChipVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureChipProperties(existing, stats);
  return stats;
}

async function buildSegmentedControlComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "SegmentedControl", stats);

  const existing = findComponentSetOnPage(page, "SegmentedControl");
  if (existing) {
    normalizeComponentSetNodeName(existing, "SegmentedControl", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "SegmentedControl already exists. Use Update SegmentedControl to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  await ensureKozmosTextStyles(fonts, stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (const size of SEGMENTED_CONTROL_SIZES) {
    for (const active of SEGMENTED_CONTROL_ACTIVE) {
      for (const state of SEGMENTED_CONTROL_STATES) {
        const component = await createSegmentedControlVariant({
          size,
          active,
          state,
          variableByName,
          fonts,
          stats,
        });
        page.appendChild(component);
        components.push(component);
      }
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "SegmentedControl";
  componentSet.x = 80;
  componentSet.y = 1840;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(
    RUN_NAMESPACE,
    "component",
    "SegmentedControl",
  );
  applyComponentSetDescription(componentSet, "SegmentedControl", false, [
    "Kozmos SegmentedControl component set generated from React SegmentedControl API.",
    "Size maps to SegmentedControl.size.",
    "Active maps to the selected/default value.",
    "State maps to disabled, focus, and error examples.",
    "Item text properties map to segment labels.",
  ]);
  clearComponentSetContainerFill(componentSet);
  layoutSegmentedControlVariants(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureSegmentedControlProperties(componentSet, stats);
  return stats;
}

async function updateSegmentedControlComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "SegmentedControl", stats);

  const existing = findComponentSetOnPage(page, "SegmentedControl");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message =
      "SegmentedControl was not found. Run Build SegmentedControl first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "SegmentedControl", stats);

  const fonts = await loadButtonFonts(stats);
  await ensureKozmosTextStyles(fonts, stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "SegmentedControl");
  applyComponentSetDescription(existing, "SegmentedControl", true, [
    "Kozmos SegmentedControl component set generated from React SegmentedControl API.",
    "Size maps to SegmentedControl.size.",
    "Active maps to the selected/default value.",
    "State maps to disabled, focus, and error examples.",
    "Item text properties map to segment labels.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseSegmentedControlVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized SegmentedControl variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[segmentedControlVariantKey(props)] = true;
    await updateSegmentedControlVariant(child, {
      size: props.size,
      active: props.active,
      state: props.state,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const size of SEGMENTED_CONTROL_SIZES) {
    for (const active of SEGMENTED_CONTROL_ACTIVE) {
      for (const state of SEGMENTED_CONTROL_STATES) {
        const props = { size, active, state };
        const key = segmentedControlVariantKey(props);
        if (seenKeys[key]) continue;

        const component = await createSegmentedControlVariant({
          size,
          active,
          state,
          variableByName,
          fonts,
          stats,
        });
        existing.appendChild(component);
        seenKeys[key] = true;
        stats.variantsCreated += 1;
      }
    }
  }

  layoutSegmentedControlVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureSegmentedControlProperties(existing, stats);
  return stats;
}

async function ensureCounterComponentForBadge(stats) {
  const existing = await findLocalComponentSetByName("Counter");
  if (existing) return existing;

  const result = await buildCounterComponent();
  if (result.warnings) {
    for (const warning of result.warnings) {
      stats.warnings.push(`Counter: ${warning}`);
    }
  }

  const created = await findLocalComponentSetByName("Counter");
  if (!created) {
    stats.warnings.push(
      "Counter is missing; Badge counters will use placeholder nodes until Counter is built.",
    );
  } else {
    stats.counterComponentCreated = true;
  }

  return created;
}

async function buildCardComponent() {
  return buildSingleAxisComponent({
    componentName: "Card",
    componentSetName: "Card",
    axisName: "Content",
    values: CARD_CONTENT,
    x: 80,
    y: 4300,
    xStep: 420,
    createVariant: createCardVariant,
    configureProperties: configureCardProperties,
    description: [
      "Kozmos Card component set generated from React Card API.",
      "Content maps to composed Card anatomy in Code Connect.",
      "Title Text maps to CardTitle children.",
      "Description Text maps to CardDescription children.",
      "Body Text maps to CardContent children.",
    ],
  });
}

async function updateCardComponent() {
  return updateSingleAxisComponent({
    componentName: "Card",
    componentSetName: "Card",
    axisName: "Content",
    values: CARD_CONTENT,
    xStep: 420,
    createVariant: createCardVariant,
    updateVariant: updateCardVariant,
    parseVariantName: parseCardVariantName,
    configureProperties: configureCardProperties,
    description: [
      "Kozmos Card component set generated from React Card API.",
      "Content maps to composed Card anatomy in Code Connect.",
      "Title Text maps to CardTitle children.",
      "Description Text maps to CardDescription children.",
      "Body Text maps to CardContent children.",
      "Full cards compose live Button instances for footer actions.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildListComponent() {
  return buildSingleAxisComponent({
    componentName: "List",
    componentSetName: "List",
    axisName: "Density",
    values: LIST_DENSITIES,
    x: 80,
    y: 4300,
    xStep: 420,
    createVariant: createListVariant,
    configureProperties: configureListProperties,
    autoReorganize: true,
    description: [
      "Kozmos List component set generated from React List API.",
      "Density maps to default and compact row spacing examples.",
      "Item text properties map to ListItem children.",
      "Domain-specific row content remains a composition concern.",
    ],
  });
}

async function updateListComponent() {
  return updateSingleAxisComponent({
    componentName: "List",
    componentSetName: "List",
    axisName: "Density",
    values: LIST_DENSITIES,
    xStep: 420,
    createVariant: createListVariant,
    updateVariant: updateListVariant,
    parseVariantName: parseListVariantName,
    configureProperties: configureListProperties,
    autoReorganize: true,
    description: [
      "Kozmos List component set generated from React List API.",
      "Density maps to default and compact row spacing examples.",
      "Item text properties map to ListItem children.",
      "Domain-specific row content remains a composition concern.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildTableComponent() {
  return buildSingleAxisComponent({
    componentName: "Table",
    componentSetName: "Table",
    axisName: "Density",
    values: TABLE_DENSITIES,
    x: 80,
    y: 4560,
    xStep: 780,
    createVariant: createTableVariant,
    configureProperties: configureTableProperties,
    autoReorganize: true,
    description: [
      "Kozmos Table component set generated from React Table API.",
      "Density maps to default and compact row spacing examples.",
      "Header and cell text properties map to TableHead and TableCell children.",
      "Sorting, selection, pagination, and empty states remain separate composition concerns.",
    ],
  });
}

async function updateTableComponent() {
  return updateSingleAxisComponent({
    componentName: "Table",
    componentSetName: "Table",
    axisName: "Density",
    values: TABLE_DENSITIES,
    xStep: 780,
    createVariant: createTableVariant,
    updateVariant: updateTableVariant,
    parseVariantName: parseTableVariantName,
    configureProperties: configureTableProperties,
    autoReorganize: true,
    description: [
      "Kozmos Table component set generated from React Table API.",
      "Density maps to default and compact row spacing examples.",
      "Header and cell text properties map to TableHead and TableCell children.",
      "Sorting, selection, pagination, and empty states remain separate composition concerns.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildTabsComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Tabs", stats);

  const existing = findComponentSetOnPage(page, "Tabs");
  if (existing) {
    normalizeComponentSetNodeName(existing, "Tabs", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "Tabs already exists. Use Update Tabs to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const combinations = tabsVariantCombinations();
  const components = [];

  for (const combination of combinations) {
    const component = await createTabsVariant({
      count: combination.count,
      active: combination.active,
      state: combination.state,
      variableByName,
      fonts,
      stats,
    });
    page.appendChild(component);
    components.push(component);
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "Tabs";
  componentSet.x = 80;
  componentSet.y = 4620;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "Tabs");
  applyComponentSetDescription(componentSet, "Tabs", false, [
    "Kozmos Tabs component set generated from React Tabs API.",
    "Count maps to the number of TabsTrigger examples.",
    "Active maps to Tabs.defaultValue.",
    "State maps to disabled and focus examples.",
    "Tab text properties map to TabsTrigger children.",
  ]);
  clearComponentSetContainerFill(componentSet);
  layoutTabsVariants(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureTabsProperties(componentSet, stats);
  return stats;
}

async function updateTabsComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Tabs", stats);

  const existing = findComponentSetOnPage(page, "Tabs");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "Tabs was not found. Run Build Tabs first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "Tabs", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "Tabs");
  applyComponentSetDescription(existing, "Tabs", true, [
    "Kozmos Tabs component set generated from React Tabs API.",
    "Count maps to the number of TabsTrigger examples.",
    "Active maps to Tabs.defaultValue.",
    "State maps to disabled and focus examples.",
    "Tab text properties map to TabsTrigger children.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseTabsVariantName(child.name);
    if (!props) {
      stats.warnings.push(`Skipped unrecognized Tabs variant "${child.name}".`);
      continue;
    }

    seenKeys[`${props.count}/${props.active}/${props.state}`] = true;
    await updateTabsVariant(child, {
      count: props.count,
      active: props.active,
      state: props.state,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const combination of tabsVariantCombinations()) {
    const key = `${combination.count}/${combination.active}/${combination.state}`;
    if (!seenKeys[key]) {
      stats.variantsMissing += 1;
      stats.warnings.push(`Missing Tabs variant ${key}.`);
    }
  }

  layoutTabsVariants(existing);
  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureTabsProperties(existing, stats);
  return stats;
}

async function buildTooltipComponent() {
  return buildSingleAxisComponent({
    componentName: "Tooltip",
    componentSetName: "Tooltip",
    axisName: "Side",
    values: TOOLTIP_SIDES,
    x: 80,
    y: 5360,
    xStep: 220,
    createVariant: createTooltipVariant,
    configureProperties: configureTooltipProperties,
    description: [
      "Kozmos Tooltip component set generated from React TooltipContent API.",
      "Side maps to TooltipContent.side.",
      "Content Text maps to TooltipContent children.",
      "Tip renders on the side facing the trigger.",
      "TooltipProvider and TooltipTrigger remain composition concerns in product code.",
    ],
  });
}

async function updateTooltipComponent() {
  return updateSingleAxisComponent({
    componentName: "Tooltip",
    componentSetName: "Tooltip",
    axisName: "Side",
    values: TOOLTIP_SIDES,
    xStep: 220,
    createVariant: createTooltipVariant,
    updateVariant: updateTooltipVariant,
    parseVariantName: parseTooltipVariantName,
    configureProperties: configureTooltipProperties,
    description: [
      "Kozmos Tooltip component set generated from React TooltipContent API.",
      "Side maps to TooltipContent.side.",
      "Content Text maps to TooltipContent children.",
      "Tip renders on the side facing the trigger.",
      "TooltipProvider and TooltipTrigger remain composition concerns in product code.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildDialogComponent() {
  return buildSingleAxisComponent({
    componentName: "Dialog",
    componentSetName: "Dialog",
    axisName: "Content",
    values: DIALOG_CONTENT,
    x: 80,
    y: 5600,
    xStep: 560,
    createVariant: createDialogVariant,
    configureProperties: configureDialogProperties,
    description: [
      "Kozmos Dialog component set generated from React DialogContent API.",
      "Content maps to composed DialogContent anatomy.",
      "Title Text maps to DialogTitle children.",
      "Description Text maps to DialogDescription children.",
      "Form content composes live Input instances.",
      "Footer actions compose live Button instances.",
    ],
  });
}

async function updateDialogComponent() {
  const stats = await updateSingleAxisComponent({
    componentName: "Dialog",
    componentSetName: "Dialog",
    axisName: "Content",
    values: DIALOG_CONTENT,
    xStep: 560,
    createVariant: createDialogVariant,
    updateVariant: updateDialogVariant,
    parseVariantName: parseDialogVariantName,
    configureProperties: configureDialogProperties,
    description: [
      "Kozmos Dialog component set generated from React DialogContent API.",
      "Content maps to composed DialogContent anatomy.",
      "Title Text maps to DialogTitle children.",
      "Description Text maps to DialogDescription children.",
      "Form content composes live Input instances.",
      "Footer actions compose live Button instances.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
  return repairCompositeComponentAfterUpdate({
    stats,
    componentName: "Dialog",
    componentSetName: "Dialog",
  });
}

async function buildDrawerComponent() {
  return buildSingleAxisComponent({
    componentName: "Drawer",
    componentSetName: "Drawer",
    axisName: "Side",
    values: DRAWER_SIDES,
    x: 80,
    y: 5840,
    xStep: 560,
    createVariant: createDrawerVariant,
    configureProperties: configureDrawerProperties,
    description: [
      "Kozmos Drawer component set generated from React DrawerContent API.",
      "Side maps to DrawerContent.side.",
      "Title Text, Description Text, and Body Text map to composed drawer content.",
      "Footer actions compose live Button instances.",
      "Drawer is a generic edge sheet; product-specific navigation, filters, and map content remain composition concerns.",
    ],
  });
}

async function updateDrawerComponent() {
  return updateSingleAxisComponent({
    componentName: "Drawer",
    componentSetName: "Drawer",
    axisName: "Side",
    values: DRAWER_SIDES,
    xStep: 560,
    createVariant: createDrawerVariant,
    updateVariant: updateDrawerVariant,
    parseVariantName: parseDrawerVariantName,
    configureProperties: configureDrawerProperties,
    description: [
      "Kozmos Drawer component set generated from React DrawerContent API.",
      "Side maps to DrawerContent.side.",
      "Title Text, Description Text, and Body Text map to composed drawer content.",
      "Footer actions compose live Button instances.",
      "Drawer is a generic edge sheet; product-specific navigation, filters, and map content remain composition concerns.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildPopoverComponent() {
  return buildSingleAxisComponent({
    componentName: "Popover",
    componentSetName: "Popover",
    axisName: "Side",
    values: POPOVER_SIDES,
    x: 80,
    y: 5960,
    xStep: 340,
    createVariant: createPopoverVariant,
    configureProperties: configurePopoverProperties,
    description: [
      "Kozmos Popover component set generated from React PopoverContent API.",
      "Side maps to PopoverContent.side.",
      "Title Text and Description Text map to composed children.",
      "Side Offset follows the shared 4px overlay offset contract.",
    ],
  });
}

async function updatePopoverComponent() {
  return updateSingleAxisComponent({
    componentName: "Popover",
    componentSetName: "Popover",
    axisName: "Side",
    values: POPOVER_SIDES,
    xStep: 340,
    createVariant: createPopoverVariant,
    updateVariant: updatePopoverVariant,
    parseVariantName: parsePopoverVariantName,
    configureProperties: configurePopoverProperties,
    description: [
      "Kozmos Popover component set generated from React PopoverContent API.",
      "Side maps to PopoverContent.side.",
      "Title Text and Description Text map to composed children.",
      "Side Offset follows the shared 4px overlay offset contract.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildMenuComponent() {
  return buildSingleAxisComponent({
    componentName: "Menu",
    componentSetName: "Menu",
    axisName: "Content",
    values: MENU_CONTENT,
    x: 80,
    y: 6200,
    xStep: 240,
    createVariant: createMenuVariant,
    configureProperties: configureMenuProperties,
    description: [
      "Kozmos Menu component set generated from React MenuContent API.",
      "Content maps to composed MenuContent examples.",
      "Label Text maps to MenuLabel children.",
      "Item text properties map to Menu item children.",
    ],
  });
}

async function updateMenuComponent() {
  return updateSingleAxisComponent({
    componentName: "Menu",
    componentSetName: "Menu",
    axisName: "Content",
    values: MENU_CONTENT,
    xStep: 240,
    createVariant: createMenuVariant,
    updateVariant: updateMenuVariant,
    parseVariantName: parseMenuVariantName,
    configureProperties: configureMenuProperties,
    description: [
      "Kozmos Menu component set generated from React MenuContent API.",
      "Content maps to composed MenuContent examples.",
      "Label Text maps to MenuLabel children.",
      "Item text properties map to Menu item children.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildCheckboxComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Checkbox", stats);

  const existing = findComponentSetOnPage(page, "Checkbox");
  if (existing) {
    normalizeComponentSetNodeName(existing, "Checkbox", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "Checkbox already exists. Use Update Checkbox to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  let index = 0;
  for (const checked of CHECKBOX_CHECKED) {
    for (const state of CHECKBOX_STATES) {
      const component = await createCheckboxVariant({
        checked,
        state,
        variableByName,
        fonts,
        stats,
      });
      component.x = (index % 3) * 220;
      component.y = Math.floor(index / 3) * 88;
      page.appendChild(component);
      components.push(component);
      index += 1;
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "Checkbox";
  componentSet.x = 80;
  componentSet.y = 1740;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "Checkbox");
  applyComponentSetDescription(componentSet, "Checkbox", false, [
    "Kozmos Checkbox component set generated from React Checkbox API.",
    "Checked maps to Checkbox.checked.",
    "State maps to disabled/error props in Code Connect.",
    "Label Text maps to Checkbox.label.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureLabelTextProperty(componentSet, "Checkbox", stats);
  configureFocusVisibleProperty(componentSet, stats);
  return stats;
}

async function updateCheckboxComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Checkbox", stats);

  const existing = findComponentSetOnPage(page, "Checkbox");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "Checkbox was not found. Run Build Checkbox first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "Checkbox", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "Checkbox");
  applyComponentSetDescription(existing, "Checkbox", true, [
    "Kozmos Checkbox component set generated from React Checkbox API.",
    "Checked maps to Checkbox.checked.",
    "State maps to disabled/error props in Code Connect.",
    "Label Text maps to Checkbox.label.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseCheckboxVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized Checkbox variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[`${props.checked}/${props.state}`] = true;
    await updateCheckboxVariant(child, {
      checked: props.checked,
      state: props.state,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const checked of CHECKBOX_CHECKED) {
    for (const state of CHECKBOX_STATES) {
      const key = `${checked}/${state}`;
      if (!seenKeys[key]) {
        stats.variantsMissing += 1;
        stats.warnings.push(`Missing Checkbox variant ${key}.`);
      }
    }
  }

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureLabelTextProperty(existing, "Checkbox", stats);
  configureFocusVisibleProperty(existing, stats);
  return stats;
}

async function buildRadioComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Radio", stats);

  const existing = findComponentSetOnPage(page, "Radio");
  if (existing) {
    normalizeComponentSetNodeName(existing, "Radio", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "Radio already exists. Use Update Radio to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  let index = 0;
  for (const checked of RADIO_CHECKED) {
    for (const state of RADIO_STATES) {
      const component = await createRadioVariant({
        checked,
        state,
        variableByName,
        fonts,
        stats,
      });
      component.x = (index % 3) * 220;
      component.y = Math.floor(index / 3) * 88;
      page.appendChild(component);
      components.push(component);
      index += 1;
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "Radio";
  componentSet.x = 80;
  componentSet.y = 1900;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "Radio");
  applyComponentSetDescription(componentSet, "Radio", false, [
    "Kozmos Radio component set generated from React RadioGroupItem API.",
    "Checked maps to whether RadioGroup.defaultValue matches RadioGroupItem.value.",
    "State maps to disabled/error props in Code Connect.",
    "Label Text maps to RadioGroupItem.label.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureLabelTextProperty(componentSet, "Radio", stats);
  configureFocusVisibleProperty(componentSet, stats);
  return stats;
}

async function updateRadioComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Radio", stats);

  const existing = findComponentSetOnPage(page, "Radio");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "Radio was not found. Run Build Radio first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "Radio", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "Radio");
  applyComponentSetDescription(existing, "Radio", true, [
    "Kozmos Radio component set generated from React RadioGroupItem API.",
    "Checked maps to whether RadioGroup.defaultValue matches RadioGroupItem.value.",
    "State maps to disabled/error props in Code Connect.",
    "Label Text maps to RadioGroupItem.label.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseRadioVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized Radio variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[`${props.checked}/${props.state}`] = true;
    await updateRadioVariant(child, {
      checked: props.checked,
      state: props.state,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const checked of RADIO_CHECKED) {
    for (const state of RADIO_STATES) {
      const key = `${checked}/${state}`;
      if (!seenKeys[key]) {
        stats.variantsMissing += 1;
        stats.warnings.push(`Missing Radio variant ${key}.`);
      }
    }
  }

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureLabelTextProperty(existing, "Radio", stats);
  configureFocusVisibleProperty(existing, stats);
  return stats;
}

async function buildSwitchComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Switch", stats);

  const existing = findComponentSetOnPage(page, "Switch");
  if (existing) {
    normalizeComponentSetNodeName(existing, "Switch", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "Switch already exists. Use Update Switch to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  let index = 0;
  for (const checked of SWITCH_CHECKED) {
    for (const state of SWITCH_STATES) {
      const component = await createSwitchVariant({
        checked,
        state,
        variableByName,
        fonts,
        stats,
      });
      component.x = (index % 3) * 220;
      component.y = Math.floor(index / 3) * 88;
      page.appendChild(component);
      components.push(component);
      index += 1;
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "Switch";
  componentSet.x = 80;
  componentSet.y = 2060;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "Switch");
  applyComponentSetDescription(componentSet, "Switch", false, [
    "Kozmos Switch component set generated from React Switch API.",
    "Checked maps to Switch.checked.",
    "State maps to disabled/error props in Code Connect.",
    "Label Text maps to Switch.label.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureLabelTextProperty(componentSet, "Switch", stats);
  configureFocusVisibleProperty(componentSet, stats);
  return stats;
}

async function updateSwitchComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Switch", stats);

  const existing = findComponentSetOnPage(page, "Switch");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "Switch was not found. Run Build Switch first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "Switch", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "Switch");
  applyComponentSetDescription(existing, "Switch", true, [
    "Kozmos Switch component set generated from React Switch API.",
    "Checked maps to Switch.checked.",
    "State maps to disabled/error props in Code Connect.",
    "Label Text maps to Switch.label.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseSwitchVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized Switch variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[`${props.checked}/${props.state}`] = true;
    await updateSwitchVariant(child, {
      checked: props.checked,
      state: props.state,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const checked of SWITCH_CHECKED) {
    for (const state of SWITCH_STATES) {
      const key = `${checked}/${state}`;
      if (!seenKeys[key]) {
        stats.variantsMissing += 1;
        stats.warnings.push(`Missing Switch variant ${key}.`);
      }
    }
  }

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureLabelTextProperty(existing, "Switch", stats);
  configureFocusVisibleProperty(existing, stats);
  return stats;
}

async function buildInputComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Input", stats);

  const existing = findComponentSetOnPage(page, "Input");
  if (existing) {
    normalizeComponentSetNodeName(existing, "Input", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "Input already exists. Use Update Input to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (let stateIndex = 0; stateIndex < INPUT_STATES.length; stateIndex += 1) {
    const state = INPUT_STATES[stateIndex];
    for (
      let statusIndex = 0;
      statusIndex < INPUT_STATUSES.length;
      statusIndex += 1
    ) {
      const status = INPUT_STATUSES[statusIndex];
      const component = await createInputVariant({
        state,
        status,
        variableByName,
        fonts,
        stats,
      });
      component.x = statusIndex * 360;
      component.y = stateIndex * 126;
      page.appendChild(component);
      components.push(component);
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "Input";
  componentSet.x = 80;
  componentSet.y = 2220;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "Input");
  applyComponentSetDescription(componentSet, "Input", false, [
    "Kozmos Input component set generated from React Input API.",
    "State maps to focus, disabled, and readOnly props in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Label Text maps to Input.label.",
    "Placeholder Text maps to Input.placeholder.",
    "Helper Text maps to Input.helperText.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureLabelTextProperty(componentSet, "Input", stats);
  configurePlaceholderTextProperty(componentSet, "Placeholder", stats);
  configureHelperTextProperty(componentSet, "Helper text", stats);
  configureHelperVisibilityProperty(componentSet, stats);
  configureFocusVisibleProperty(componentSet, stats);
  return stats;
}

async function updateInputComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Input", stats);

  const existing = findComponentSetOnPage(page, "Input");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "Input was not found. Run Build Input first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "Input", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "Input");
  applyComponentSetDescription(existing, "Input", true, [
    "Kozmos Input component set generated from React Input API.",
    "State maps to focus, disabled, and readOnly props in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Label Text maps to Input.label.",
    "Placeholder Text maps to Input.placeholder.",
    "Helper Text maps to Input.helperText.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseInputVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized Input variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[`${props.state}/${props.status}`] = true;
    await updateInputVariant(child, {
      state: props.state,
      status: props.status,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const state of INPUT_STATES) {
    for (const status of INPUT_STATUSES) {
      const key = `${state}/${status}`;
      if (seenKeys[key]) continue;

      const component = await createInputVariant({
        state,
        status,
        variableByName,
        fonts,
        stats,
      });
      existing.appendChild(component);
      seenKeys[key] = true;
      stats.variantsCreated += 1;
    }
  }

  layoutInputVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureLabelTextProperty(existing, "Input", stats);
  configurePlaceholderTextProperty(existing, "Placeholder", stats);
  configureHelperTextProperty(existing, "Helper text", stats);
  configureHelperVisibilityProperty(existing, stats);
  configureFocusVisibleProperty(existing, stats);
  return stats;
}

async function buildPasswordInputComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "PasswordInput", stats);

  const existing = findComponentSetOnPage(page, "PasswordInput");
  if (existing) {
    normalizeComponentSetNodeName(existing, "PasswordInput", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "PasswordInput already exists. Use Update PasswordInput to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (let stateIndex = 0; stateIndex < INPUT_STATES.length; stateIndex += 1) {
    const state = INPUT_STATES[stateIndex];
    for (
      let statusIndex = 0;
      statusIndex < INPUT_STATUSES.length;
      statusIndex += 1
    ) {
      const status = INPUT_STATUSES[statusIndex];
      for (
        let visibilityIndex = 0;
        visibilityIndex < PASSWORD_INPUT_VISIBILITY.length;
        visibilityIndex += 1
      ) {
        const visibility = PASSWORD_INPUT_VISIBILITY[visibilityIndex];
        const component = await createPasswordInputVariant({
          state,
          status,
          visibility,
          variableByName,
          fonts,
          stats,
        });
        component.x = statusIndex * 720 + visibilityIndex * 360;
        component.y = stateIndex * 126;
        page.appendChild(component);
        components.push(component);
      }
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "PasswordInput";
  componentSet.x = 80;
  componentSet.y = 3320;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "PasswordInput");
  applyComponentSetDescription(componentSet, "PasswordInput", false, [
    "Kozmos PasswordInput component set generated from React PasswordInput API.",
    "State maps to focus, disabled, and readOnly props in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Visibility maps to PasswordInput.defaultVisible in Code Connect examples.",
    "Label Text maps to PasswordInput.label.",
    "Placeholder Text maps to PasswordInput.placeholder.",
    "Helper Text maps to PasswordInput.helperText.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureLabelTextProperty(componentSet, "Password", stats);
  configurePlaceholderTextProperty(componentSet, "Password", stats);
  configureHelperTextProperty(componentSet, "Password requirements", stats);
  configureHelperVisibilityProperty(componentSet, stats);
  configureFocusVisibleProperty(componentSet, stats);
  return stats;
}

async function updatePasswordInputComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "PasswordInput", stats);

  const existing = findComponentSetOnPage(page, "PasswordInput");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message =
      "PasswordInput was not found. Run Build PasswordInput first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "PasswordInput", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "PasswordInput");
  applyComponentSetDescription(existing, "PasswordInput", true, [
    "Kozmos PasswordInput component set generated from React PasswordInput API.",
    "State maps to focus, disabled, and readOnly props in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Visibility maps to PasswordInput.defaultVisible in Code Connect examples.",
    "Label Text maps to PasswordInput.label.",
    "Placeholder Text maps to PasswordInput.placeholder.",
    "Helper Text maps to PasswordInput.helperText.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parsePasswordInputVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized PasswordInput variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[`${props.state}/${props.status}/${props.visibility}`] = true;
    await updatePasswordInputVariant(child, {
      state: props.state,
      status: props.status,
      visibility: props.visibility,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const state of INPUT_STATES) {
    for (const status of INPUT_STATUSES) {
      for (const visibility of PASSWORD_INPUT_VISIBILITY) {
        const key = `${state}/${status}/${visibility}`;
        if (seenKeys[key]) continue;

        const component = await createPasswordInputVariant({
          state,
          status,
          visibility,
          variableByName,
          fonts,
          stats,
        });
        existing.appendChild(component);
        seenKeys[key] = true;
        stats.variantsCreated += 1;
      }
    }
  }

  layoutPasswordInputVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureLabelTextProperty(existing, "Password", stats);
  configurePlaceholderTextProperty(existing, "Password", stats);
  configureHelperTextProperty(existing, "Password requirements", stats);
  configureHelperVisibilityProperty(existing, stats);
  configureFocusVisibleProperty(existing, stats);
  return stats;
}

async function buildFormFieldComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "FormField", stats);

  const existing = findComponentSetOnPage(page, "FormField");
  if (existing) {
    normalizeComponentSetNodeName(existing, "FormField", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "FormField already exists. Use Update FormField to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (
    let contentIndex = 0;
    contentIndex < FORM_FIELD_CONTENT.length;
    contentIndex += 1
  ) {
    const content = FORM_FIELD_CONTENT[contentIndex];
    for (
      let statusIndex = 0;
      statusIndex < FORM_FIELD_STATUSES.length;
      statusIndex += 1
    ) {
      const status = FORM_FIELD_STATUSES[statusIndex];
      for (
        let requiredIndex = 0;
        requiredIndex < FORM_FIELD_REQUIRED.length;
        requiredIndex += 1
      ) {
        const required = FORM_FIELD_REQUIRED[requiredIndex];
        const component = await createFormFieldVariant({
          content,
          status,
          required,
          variableByName,
          fonts,
          stats,
        });
        component.x = statusIndex * 760 + requiredIndex * 380;
        component.y = contentIndex * 150;
        page.appendChild(component);
        components.push(component);
      }
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "FormField";
  componentSet.x = 80;
  componentSet.y = 5880;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "FormField");
  applyComponentSetDescription(componentSet, "FormField", false, [
    "Kozmos FormField component set generated from React FieldWrapper/FormField API.",
    "Content maps to shared label, description, helper, and full field anatomy examples.",
    "Status maps to default, error, warning, and success message tones.",
    "Required maps to required mark and optional text examples.",
    "Content Slot maps to FieldWrapper children in Code Connect.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureFormFieldProperties(componentSet, stats);
  return stats;
}

async function updateFormFieldComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "FormField", stats);

  const existing = findComponentSetOnPage(page, "FormField");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "FormField was not found. Run Build FormField first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "FormField", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "FormField");
  applyComponentSetDescription(existing, "FormField", true, [
    "Kozmos FormField component set generated from React FieldWrapper/FormField API.",
    "Content maps to shared label, description, helper, and full field anatomy examples.",
    "Status maps to default, error, warning, and success message tones.",
    "Required maps to required mark and optional text examples.",
    "Content Slot maps to FieldWrapper children in Code Connect.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseFormFieldVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized FormField variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[`${props.content}/${props.status}/${props.required}`] = true;
    await updateFormFieldVariant(child, {
      content: props.content,
      status: props.status,
      required: props.required,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const content of FORM_FIELD_CONTENT) {
    for (const status of FORM_FIELD_STATUSES) {
      for (const required of FORM_FIELD_REQUIRED) {
        const key = `${content}/${status}/${required}`;
        if (seenKeys[key]) continue;

        const component = await createFormFieldVariant({
          content,
          status,
          required,
          variableByName,
          fonts,
          stats,
        });
        existing.appendChild(component);
        seenKeys[key] = true;
        stats.variantsCreated += 1;
      }
    }
  }

  layoutFormFieldVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureFormFieldProperties(existing, stats);
  return stats;
}

async function rebuildTextComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Text",
    componentSetName: "Text",
    build: buildTextComponent,
  });
}

async function rebuildHeadingComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Heading",
    componentSetName: "Heading",
    build: buildHeadingComponent,
  });
}

async function rebuildLinkComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Link",
    componentSetName: "Link",
    build: buildLinkComponent,
  });
}

async function rebuildLabelComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Label",
    componentSetName: "Label",
    build: buildLabelComponent,
  });
}

async function rebuildSeparatorComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Separator",
    componentSetName: "Separator",
    build: buildSeparatorComponent,
  });
}

async function rebuildSkeletonComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Skeleton",
    componentSetName: "Skeleton",
    build: buildSkeletonComponent,
  });
}

async function rebuildBoxComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Box",
    componentSetName: "Box",
    build: buildBoxComponent,
  });
}

async function rebuildStackComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Stack",
    componentSetName: "Stack",
    build: buildStackComponent,
  });
}

async function rebuildGridComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Grid",
    componentSetName: "Grid",
    build: buildGridComponent,
  });
}

async function rebuildContainerComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Container",
    componentSetName: "Container",
    build: buildContainerComponent,
  });
}

async function rebuildBreadcrumbComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Breadcrumb",
    componentSetName: "Breadcrumb",
    build: buildBreadcrumbComponent,
  });
}

async function rebuildPaginationComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Pagination",
    componentSetName: "Pagination",
    build: buildPaginationComponent,
  });
}

async function rebuildAccordionComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Accordion",
    componentSetName: "Accordion",
    build: buildAccordionComponent,
  });
}

async function rebuildButtonComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Button",
    componentSetName: "Button",
    build: buildButtonComponent,
  });
}

async function rebuildIconButtonComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "IconButton",
    componentSetName: "IconButton",
    build: buildIconButtonComponent,
  });
}

async function rebuildToggleButtonComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "ToggleButton",
    componentSetName: "ToggleButton",
    build: buildToggleButtonComponent,
  });
}

async function rebuildSplitButtonComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "SplitButton",
    componentSetName: "SplitButton",
    build: buildSplitButtonComponent,
  });
}

async function rebuildFloatingActionButtonComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "FloatingActionButton",
    componentSetName: "FloatingActionButton",
    build: buildFloatingActionButtonComponent,
  });
}

async function rebuildCounterComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Counter",
    componentSetName: "Counter",
    build: buildCounterComponent,
  });
}

async function rebuildBadgeComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Badge",
    componentSetName: "Badge",
    build: buildBadgeComponent,
  });
}

async function rebuildChipComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Chip",
    componentSetName: "Chip",
    build: buildChipComponent,
  });
}

async function rebuildSegmentedControlComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "SegmentedControl",
    componentSetName: "SegmentedControl",
    build: buildSegmentedControlComponent,
  });
}

async function rebuildCheckboxComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Checkbox",
    componentSetName: "Checkbox",
    build: buildCheckboxComponent,
  });
}

async function rebuildRadioComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Radio",
    componentSetName: "Radio",
    build: buildRadioComponent,
  });
}

async function rebuildSwitchComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Switch",
    componentSetName: "Switch",
    build: buildSwitchComponent,
  });
}

async function rebuildInputComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Input",
    componentSetName: "Input",
    build: buildInputComponent,
  });
}

async function rebuildPasswordInputComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "PasswordInput",
    componentSetName: "PasswordInput",
    build: buildPasswordInputComponent,
  });
}

async function rebuildFormFieldComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "FormField",
    componentSetName: "FormField",
    build: buildFormFieldComponent,
  });
}

async function buildNumberInputComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "NumberInput", stats);

  const existing = findComponentSetOnPage(page, "NumberInput");
  if (existing) {
    normalizeComponentSetNodeName(existing, "NumberInput", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "NumberInput already exists. Use Update NumberInput to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (let stateIndex = 0; stateIndex < INPUT_STATES.length; stateIndex += 1) {
    const state = INPUT_STATES[stateIndex];
    for (
      let statusIndex = 0;
      statusIndex < INPUT_STATUSES.length;
      statusIndex += 1
    ) {
      const status = INPUT_STATUSES[statusIndex];
      for (
        let stepperIndex = 0;
        stepperIndex < NUMBER_INPUT_STEPPERS.length;
        stepperIndex += 1
      ) {
        const steppers = NUMBER_INPUT_STEPPERS[stepperIndex];
        const component = await createNumberInputVariant({
          state,
          status,
          steppers,
          variableByName,
          fonts,
          stats,
        });
        component.x = statusIndex * 720 + stepperIndex * 360;
        component.y = stateIndex * 126;
        page.appendChild(component);
        components.push(component);
      }
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "NumberInput";
  componentSet.x = 440;
  componentSet.y = 2220;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "NumberInput");
  applyComponentSetDescription(componentSet, "NumberInput", false, [
    "Kozmos NumberInput component set generated from React NumberInput API.",
    "State maps to disabled and readOnly props in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Steppers maps to NumberInput.showSteppers.",
    "Label Text maps to NumberInput.label.",
    "Value Text maps to NumberInput.defaultValue in Code Connect examples.",
    "Placeholder Text maps to NumberInput.placeholder.",
    "Helper Text maps to NumberInput.helperText.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureNumberInputProperties(componentSet, stats);
  return stats;
}

async function updateNumberInputComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "NumberInput", stats);

  const existing = findComponentSetOnPage(page, "NumberInput");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "NumberInput was not found. Run Build NumberInput first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "NumberInput", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "NumberInput");
  applyComponentSetDescription(existing, "NumberInput", true, [
    "Kozmos NumberInput component set generated from React NumberInput API.",
    "State maps to disabled and readOnly props in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Steppers maps to NumberInput.showSteppers.",
    "Label Text maps to NumberInput.label.",
    "Value Text maps to NumberInput.defaultValue in Code Connect examples.",
    "Placeholder Text maps to NumberInput.placeholder.",
    "Helper Text maps to NumberInput.helperText.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseNumberInputVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized NumberInput variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[`${props.state}/${props.status}/${props.steppers}`] = true;
    await updateNumberInputVariant(child, {
      state: props.state,
      status: props.status,
      steppers: props.steppers,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const state of INPUT_STATES) {
    for (const status of INPUT_STATUSES) {
      for (const steppers of NUMBER_INPUT_STEPPERS) {
        const key = `${state}/${status}/${steppers}`;
        if (seenKeys[key]) continue;

        const component = await createNumberInputVariant({
          state,
          status,
          steppers,
          variableByName,
          fonts,
          stats,
        });
        existing.appendChild(component);
        seenKeys[key] = true;
        stats.variantsCreated += 1;
      }
    }
  }

  layoutNumberInputVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureNumberInputProperties(existing, stats);
  return stats;
}

async function rebuildNumberInputComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "NumberInput",
    componentSetName: "NumberInput",
    build: buildNumberInputComponent,
  });
}

async function buildOTPInputComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "OTPInput", stats);

  const existing = findComponentSetOnPage(page, "OTPInput");
  if (existing) {
    normalizeComponentSetNodeName(existing, "OTPInput", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "OTPInput already exists. Use Update OTPInput to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (let stateIndex = 0; stateIndex < INPUT_STATES.length; stateIndex += 1) {
    const state = INPUT_STATES[stateIndex];
    for (
      let statusIndex = 0;
      statusIndex < INPUT_STATUSES.length;
      statusIndex += 1
    ) {
      const status = INPUT_STATUSES[statusIndex];
      for (
        let lengthIndex = 0;
        lengthIndex < OTP_INPUT_LENGTHS.length;
        lengthIndex += 1
      ) {
        const length = OTP_INPUT_LENGTHS[lengthIndex];
        const component = await createOTPInputVariant({
          length,
          state,
          status,
          variableByName,
          fonts,
          stats,
        });
        component.x = statusIndex * 760 + lengthIndex * 380;
        component.y = stateIndex * 126;
        page.appendChild(component);
        components.push(component);
      }
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "OTPInput";
  componentSet.x = 800;
  componentSet.y = 3320;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "OTPInput");
  applyComponentSetDescription(componentSet, "OTPInput", false, [
    "Kozmos OTPInput component set generated from React OTPInput API.",
    "Length maps to OTPInput.length.",
    "State maps to disabled, readOnly, and focus examples in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Digit text properties map to the composed OTPInput.value example.",
    "Helper Text maps to OTPInput.helperText.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(componentSet);
  layoutOTPInputVariants(componentSet);
  resizeComponentSetToContainChildren(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureOTPInputProperties(componentSet, stats);
  return stats;
}

async function updateOTPInputComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "OTPInput", stats);

  const existing = findComponentSetOnPage(page, "OTPInput");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "OTPInput was not found. Run Build OTPInput first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "OTPInput", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "OTPInput");
  applyComponentSetDescription(existing, "OTPInput", true, [
    "Kozmos OTPInput component set generated from React OTPInput API.",
    "Length maps to OTPInput.length.",
    "State maps to disabled, readOnly, and focus examples in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Digit text properties map to the composed OTPInput.value example.",
    "Helper Text maps to OTPInput.helperText.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseOTPInputVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized OTPInput variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[`${props.length}/${props.state}/${props.status}`] = true;
    await updateOTPInputVariant(child, {
      length: props.length,
      state: props.state,
      status: props.status,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const length of OTP_INPUT_LENGTHS) {
    for (const state of INPUT_STATES) {
      for (const status of INPUT_STATUSES) {
        const key = `${length}/${state}/${status}`;
        if (seenKeys[key]) continue;

        const component = await createOTPInputVariant({
          length,
          state,
          status,
          variableByName,
          fonts,
          stats,
        });
        existing.appendChild(component);
        seenKeys[key] = true;
        stats.variantsCreated += 1;
      }
    }
  }

  layoutOTPInputVariants(existing);
  resizeComponentSetToContainChildren(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureOTPInputProperties(existing, stats);
  return stats;
}

async function rebuildOTPInputComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "OTPInput",
    componentSetName: "OTPInput",
    build: buildOTPInputComponent,
  });
}

async function buildComboboxComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Combobox", stats);

  const existing = findComponentSetOnPage(page, "Combobox");
  if (existing) {
    normalizeComponentSetNodeName(existing, "Combobox", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "Combobox already exists. Use Update Combobox to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (
    let stateIndex = 0;
    stateIndex < COMBOBOX_STATES.length;
    stateIndex += 1
  ) {
    const state = COMBOBOX_STATES[stateIndex];
    for (
      let statusIndex = 0;
      statusIndex < COMBOBOX_STATUSES.length;
      statusIndex += 1
    ) {
      const status = COMBOBOX_STATUSES[statusIndex];
      for (
        let contentIndex = 0;
        contentIndex < COMBOBOX_CONTENT.length;
        contentIndex += 1
      ) {
        const content = COMBOBOX_CONTENT[contentIndex];
        const component = await createComboboxVariant({
          state,
          status,
          content,
          variableByName,
          fonts,
          stats,
        });
        component.x = statusIndex * 760 + contentIndex * 380;
        component.y = stateIndex * 260;
        page.appendChild(component);
        components.push(component);
      }
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "Combobox";
  componentSet.x = 800;
  componentSet.y = 2220;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "Combobox");
  applyComponentSetDescription(componentSet, "Combobox", false, [
    "Kozmos Combobox component set generated from React Combobox API.",
    "State maps to focus, disabled, and readOnly props in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Content maps to closed field and open listbox examples.",
    "Label Text maps to Combobox.label.",
    "Placeholder Text maps to Combobox.placeholder.",
    "Option text properties map to Combobox option children in Code Connect examples.",
    "Helper Text maps to Combobox.helperText.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureComboboxProperties(componentSet, stats);
  return stats;
}

async function updateComboboxComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Combobox", stats);

  const existing = findComponentSetOnPage(page, "Combobox");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "Combobox was not found. Run Build Combobox first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "Combobox", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "Combobox");
  applyComponentSetDescription(existing, "Combobox", true, [
    "Kozmos Combobox component set generated from React Combobox API.",
    "State maps to focus, disabled, and readOnly props in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Content maps to closed field and open listbox examples.",
    "Label Text maps to Combobox.label.",
    "Placeholder Text maps to Combobox.placeholder.",
    "Option text properties map to Combobox option children in Code Connect examples.",
    "Helper Text maps to Combobox.helperText.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseComboboxVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized Combobox variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[`${props.state}/${props.status}/${props.content}`] = true;
    await updateComboboxVariant(child, {
      state: props.state,
      status: props.status,
      content: props.content,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const state of COMBOBOX_STATES) {
    for (const status of COMBOBOX_STATUSES) {
      for (const content of COMBOBOX_CONTENT) {
        const key = `${state}/${status}/${content}`;
        if (seenKeys[key]) continue;

        const component = await createComboboxVariant({
          state,
          status,
          content,
          variableByName,
          fonts,
          stats,
        });
        existing.appendChild(component);
        seenKeys[key] = true;
        stats.variantsCreated += 1;
      }
    }
  }

  layoutComboboxVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureComboboxProperties(existing, stats);
  return stats;
}

async function rebuildComboboxComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Combobox",
    componentSetName: "Combobox",
    build: buildComboboxComponent,
  });
}

async function buildMultiSelectComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "MultiSelect", stats);

  const existing = findComponentSetOnPage(page, "MultiSelect");
  if (existing) {
    normalizeComponentSetNodeName(existing, "MultiSelect", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "MultiSelect already exists. Use Update MultiSelect to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (
    let stateIndex = 0;
    stateIndex < MULTI_SELECT_STATES.length;
    stateIndex += 1
  ) {
    const state = MULTI_SELECT_STATES[stateIndex];
    for (
      let statusIndex = 0;
      statusIndex < MULTI_SELECT_STATUSES.length;
      statusIndex += 1
    ) {
      const status = MULTI_SELECT_STATUSES[statusIndex];
      for (
        let contentIndex = 0;
        contentIndex < MULTI_SELECT_CONTENT.length;
        contentIndex += 1
      ) {
        const content = MULTI_SELECT_CONTENT[contentIndex];
        const component = await createMultiSelectVariant({
          state,
          status,
          content,
          variableByName,
          fonts,
          stats,
        });
        component.x = statusIndex * 1140 + contentIndex * 380;
        component.y = stateIndex * 300;
        page.appendChild(component);
        components.push(component);
      }
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "MultiSelect";
  componentSet.x = 80;
  componentSet.y = 3820;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "MultiSelect");
  applyComponentSetDescription(componentSet, "MultiSelect", false, [
    "Kozmos MultiSelect component set generated from React MultiSelect API.",
    "State maps to focus, disabled, and readOnly props in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Content maps to empty, selected, and open multiselect examples.",
    "Label Text maps to MultiSelect.label.",
    "Placeholder Text maps to MultiSelect.placeholder.",
    "Selected values compose live Chip instances for visual and token parity.",
    "Option text properties map to MultiSelect option children in Code Connect examples.",
    "Helper Text maps to MultiSelect.helperText.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureMultiSelectProperties(componentSet, stats);
  return stats;
}

async function updateMultiSelectComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "MultiSelect", stats);

  const existing = findComponentSetOnPage(page, "MultiSelect");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "MultiSelect was not found. Run Build MultiSelect first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "MultiSelect", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "MultiSelect");
  applyComponentSetDescription(existing, "MultiSelect", true, [
    "Kozmos MultiSelect component set generated from React MultiSelect API.",
    "State maps to focus, disabled, and readOnly props in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Content maps to empty, selected, and open multiselect examples.",
    "Label Text maps to MultiSelect.label.",
    "Placeholder Text maps to MultiSelect.placeholder.",
    "Selected values compose live Chip instances for visual and token parity.",
    "Option text properties map to MultiSelect option children in Code Connect examples.",
    "Helper Text maps to MultiSelect.helperText.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseMultiSelectVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized MultiSelect variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[`${props.state}/${props.status}/${props.content}`] = true;
    await updateMultiSelectVariant(child, {
      state: props.state,
      status: props.status,
      content: props.content,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const state of MULTI_SELECT_STATES) {
    for (const status of MULTI_SELECT_STATUSES) {
      for (const content of MULTI_SELECT_CONTENT) {
        const key = `${state}/${status}/${content}`;
        if (seenKeys[key]) continue;

        const component = await createMultiSelectVariant({
          state,
          status,
          content,
          variableByName,
          fonts,
          stats,
        });
        existing.appendChild(component);
        seenKeys[key] = true;
        stats.variantsCreated += 1;
      }
    }
  }

  layoutMultiSelectVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureMultiSelectProperties(existing, stats);
  return stats;
}

async function rebuildMultiSelectComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "MultiSelect",
    componentSetName: "MultiSelect",
    build: buildMultiSelectComponent,
  });
}

async function buildListboxComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Listbox", stats);

  const existing = findComponentSetOnPage(page, "Listbox");
  if (existing) {
    normalizeComponentSetNodeName(existing, "Listbox", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "Listbox already exists. Use Update Listbox to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (
    let stateIndex = 0;
    stateIndex < LISTBOX_STATES.length;
    stateIndex += 1
  ) {
    const state = LISTBOX_STATES[stateIndex];
    for (
      let selectionIndex = 0;
      selectionIndex < LISTBOX_SELECTION.length;
      selectionIndex += 1
    ) {
      const selection = LISTBOX_SELECTION[selectionIndex];
      const component = await createListboxVariant({
        selection,
        state,
        variableByName,
        fonts,
        stats,
      });
      component.x = selectionIndex * 360;
      component.y = stateIndex * 220;
      page.appendChild(component);
      components.push(component);
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "Listbox";
  componentSet.x = 80;
  componentSet.y = 5120;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "Listbox");
  applyComponentSetDescription(componentSet, "Listbox", false, [
    "Kozmos Listbox component set generated from React Listbox API.",
    "Selection maps to Listbox.multiple.",
    "State maps to focus and disabled examples.",
    "Option text and description properties map to Listbox option children.",
    "Focus Visible controls the generated listbox focus ring.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureListboxProperties(componentSet, stats);
  return stats;
}

async function updateListboxComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Listbox", stats);

  const existing = findComponentSetOnPage(page, "Listbox");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "Listbox was not found. Run Build Listbox first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "Listbox", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "Listbox");
  applyComponentSetDescription(existing, "Listbox", true, [
    "Kozmos Listbox component set generated from React Listbox API.",
    "Selection maps to Listbox.multiple.",
    "State maps to focus and disabled examples.",
    "Option text and description properties map to Listbox option children.",
    "Focus Visible controls the generated listbox focus ring.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseListboxVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized Listbox variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[`${props.selection}/${props.state}`] = true;
    await updateListboxVariant(child, {
      selection: props.selection,
      state: props.state,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const state of LISTBOX_STATES) {
    for (const selection of LISTBOX_SELECTION) {
      const key = `${selection}/${state}`;
      if (seenKeys[key]) continue;

      const component = await createListboxVariant({
        selection,
        state,
        variableByName,
        fonts,
        stats,
      });
      existing.appendChild(component);
      seenKeys[key] = true;
      stats.variantsCreated += 1;
    }
  }

  layoutListboxVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureListboxProperties(existing, stats);
  return stats;
}

async function rebuildListboxComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Listbox",
    componentSetName: "Listbox",
    build: buildListboxComponent,
  });
}

async function buildDatePickerComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "DatePicker", stats);

  const existing = findComponentSetOnPage(page, "DatePicker");
  if (existing) {
    normalizeComponentSetNodeName(existing, "DatePicker", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "DatePicker already exists. Use Update DatePicker to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (
    let stateIndex = 0;
    stateIndex < DATE_PICKER_STATES.length;
    stateIndex += 1
  ) {
    const state = DATE_PICKER_STATES[stateIndex];
    for (
      let statusIndex = 0;
      statusIndex < DATE_PICKER_STATUSES.length;
      statusIndex += 1
    ) {
      const status = DATE_PICKER_STATUSES[statusIndex];
      for (
        let contentIndex = 0;
        contentIndex < DATE_PICKER_CONTENT.length;
        contentIndex += 1
      ) {
        const content = DATE_PICKER_CONTENT[contentIndex];
        const component = await createDatePickerVariant({
          state,
          status,
          content,
          variableByName,
          fonts,
          stats,
        });
        component.x = statusIndex * 760 + contentIndex * 380;
        component.y = stateIndex * DATE_PICKER_STATE_ROW_STEP;
        page.appendChild(component);
        components.push(component);
      }
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "DatePicker";
  componentSet.x = 80;
  componentSet.y = 6220;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "DatePicker");
  applyComponentSetDescription(componentSet, "DatePicker", false, [
    "Kozmos DatePicker component set generated from React DatePicker API.",
    "State maps to focus, disabled, and readOnly props in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Content maps to closed field and open calendar anatomy examples.",
    "Label Text maps to DatePicker.label.",
    "Value Text maps to the date input value in Code Connect examples.",
    "Month Text maps to calendar example heading.",
    "Helper Text maps to DatePicker.helperText.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureDatePickerProperties(componentSet, stats);
  return stats;
}

async function updateDatePickerComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "DatePicker", stats);

  const existing = findComponentSetOnPage(page, "DatePicker");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "DatePicker was not found. Run Build DatePicker first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "DatePicker", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "DatePicker");
  applyComponentSetDescription(existing, "DatePicker", true, [
    "Kozmos DatePicker component set generated from React DatePicker API.",
    "State maps to focus, disabled, and readOnly props in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Content maps to closed field and open calendar anatomy examples.",
    "Label Text maps to DatePicker.label.",
    "Value Text maps to the date input value in Code Connect examples.",
    "Month Text maps to calendar example heading.",
    "Helper Text maps to DatePicker.helperText.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseDatePickerVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized DatePicker variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[`${props.state}/${props.status}/${props.content}`] = true;
    await updateDatePickerVariant(child, {
      state: props.state,
      status: props.status,
      content: props.content,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const state of DATE_PICKER_STATES) {
    for (const status of DATE_PICKER_STATUSES) {
      for (const content of DATE_PICKER_CONTENT) {
        const key = `${state}/${status}/${content}`;
        if (seenKeys[key]) continue;

        const component = await createDatePickerVariant({
          state,
          status,
          content,
          variableByName,
          fonts,
          stats,
        });
        existing.appendChild(component);
        seenKeys[key] = true;
        stats.variantsCreated += 1;
      }
    }
  }

  layoutDatePickerVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureDatePickerProperties(existing, stats);
  return stats;
}

async function rebuildDatePickerComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "DatePicker",
    componentSetName: "DatePicker",
    build: buildDatePickerComponent,
  });
}

async function buildDateRangePickerComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "DateRangePicker", stats);

  const existing = findComponentSetOnPage(page, "DateRangePicker");
  if (existing) {
    normalizeComponentSetNodeName(existing, "DateRangePicker", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "DateRangePicker already exists. Use Update DateRangePicker to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (
    let stateIndex = 0;
    stateIndex < DATE_RANGE_PICKER_STATES.length;
    stateIndex += 1
  ) {
    const state = DATE_RANGE_PICKER_STATES[stateIndex];
    for (
      let statusIndex = 0;
      statusIndex < DATE_RANGE_PICKER_STATUSES.length;
      statusIndex += 1
    ) {
      const status = DATE_RANGE_PICKER_STATUSES[statusIndex];
      for (
        let contentIndex = 0;
        contentIndex < DATE_RANGE_PICKER_CONTENT.length;
        contentIndex += 1
      ) {
        const content = DATE_RANGE_PICKER_CONTENT[contentIndex];
        const component = await createDateRangePickerVariant({
          state,
          status,
          content,
          variableByName,
          fonts,
          stats,
        });
        component.x = statusIndex * 1440 + contentIndex * 720;
        component.y = stateIndex * DATE_RANGE_PICKER_STATE_ROW_STEP;
        page.appendChild(component);
        components.push(component);
      }
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "DateRangePicker";
  componentSet.x = 80;
  componentSet.y = 7960;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(
    RUN_NAMESPACE,
    "component",
    "DateRangePicker",
  );
  applyComponentSetDescription(componentSet, "DateRangePicker", false, [
    "Kozmos DateRangePicker component set generated from React DateRangePicker API.",
    "State maps to focus, disabled, and readOnly props in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Content maps to closed two-field and open range calendar anatomy examples.",
    "Label Text maps to DateRangePicker.label.",
    "Start/End Label and Value text map to DateRangePicker field props.",
    "Start/End Month Text map to calendar example headings.",
    "Helper Text maps to DateRangePicker.helperText.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureDateRangePickerProperties(componentSet, stats);
  return stats;
}

async function updateDateRangePickerComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "DateRangePicker", stats);

  const existing = findComponentSetOnPage(page, "DateRangePicker");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message =
      "DateRangePicker was not found. Run Build DateRangePicker first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "DateRangePicker", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "DateRangePicker");
  applyComponentSetDescription(existing, "DateRangePicker", true, [
    "Kozmos DateRangePicker component set generated from React DateRangePicker API.",
    "State maps to focus, disabled, and readOnly props in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Content maps to closed two-field and open range calendar anatomy examples.",
    "Label Text maps to DateRangePicker.label.",
    "Start/End Label and Value text map to DateRangePicker field props.",
    "Start/End Month Text map to calendar example headings.",
    "Helper Text maps to DateRangePicker.helperText.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseDateRangePickerVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized DateRangePicker variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[`${props.state}/${props.status}/${props.content}`] = true;
    await updateDateRangePickerVariant(child, {
      state: props.state,
      status: props.status,
      content: props.content,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const state of DATE_RANGE_PICKER_STATES) {
    for (const status of DATE_RANGE_PICKER_STATUSES) {
      for (const content of DATE_RANGE_PICKER_CONTENT) {
        const key = `${state}/${status}/${content}`;
        if (seenKeys[key]) continue;

        const component = await createDateRangePickerVariant({
          state,
          status,
          content,
          variableByName,
          fonts,
          stats,
        });
        existing.appendChild(component);
        seenKeys[key] = true;
        stats.variantsCreated += 1;
      }
    }
  }

  layoutDateRangePickerVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureDateRangePickerProperties(existing, stats);
  return stats;
}

async function rebuildDateRangePickerComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "DateRangePicker",
    componentSetName: "DateRangePicker",
    build: buildDateRangePickerComponent,
  });
}

async function buildTimePickerComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "TimePicker", stats);

  const existing = findComponentSetOnPage(page, "TimePicker");
  if (existing) {
    normalizeComponentSetNodeName(existing, "TimePicker", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "TimePicker already exists. Use Update TimePicker to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (
    let stateIndex = 0;
    stateIndex < TIME_PICKER_STATES.length;
    stateIndex += 1
  ) {
    const state = TIME_PICKER_STATES[stateIndex];
    for (
      let statusIndex = 0;
      statusIndex < TIME_PICKER_STATUSES.length;
      statusIndex += 1
    ) {
      const status = TIME_PICKER_STATUSES[statusIndex];
      for (
        let contentIndex = 0;
        contentIndex < TIME_PICKER_CONTENT.length;
        contentIndex += 1
      ) {
        const content = TIME_PICKER_CONTENT[contentIndex];
        const component = await createTimePickerVariant({
          state,
          status,
          content,
          variableByName,
          fonts,
          stats,
        });
        component.x = statusIndex * 760 + contentIndex * 380;
        component.y = stateIndex * 320;
        page.appendChild(component);
        components.push(component);
      }
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "TimePicker";
  componentSet.x = 80;
  componentSet.y = 9840;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "TimePicker");
  applyComponentSetDescription(componentSet, "TimePicker", false, [
    "Kozmos TimePicker component set generated from React TimePicker API.",
    "State maps to focus, disabled, and readOnly props in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Content maps to closed field and open time-list anatomy examples.",
    "Label Text maps to TimePicker.label.",
    "Value Text maps to the time input value in Code Connect examples.",
    "Option text properties map to time-list examples.",
    "Helper Text maps to TimePicker.helperText.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureTimePickerProperties(componentSet, stats);
  return stats;
}

async function updateTimePickerComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "TimePicker", stats);

  const existing = findComponentSetOnPage(page, "TimePicker");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "TimePicker was not found. Run Build TimePicker first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "TimePicker", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "TimePicker");
  applyComponentSetDescription(existing, "TimePicker", true, [
    "Kozmos TimePicker component set generated from React TimePicker API.",
    "State maps to focus, disabled, and readOnly props in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Content maps to closed field and open time-list anatomy examples.",
    "Label Text maps to TimePicker.label.",
    "Value Text maps to the time input value in Code Connect examples.",
    "Option text properties map to time-list examples.",
    "Helper Text maps to TimePicker.helperText.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseTimePickerVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized TimePicker variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[`${props.state}/${props.status}/${props.content}`] = true;
    await updateTimePickerVariant(child, {
      state: props.state,
      status: props.status,
      content: props.content,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const state of TIME_PICKER_STATES) {
    for (const status of TIME_PICKER_STATUSES) {
      for (const content of TIME_PICKER_CONTENT) {
        const key = `${state}/${status}/${content}`;
        if (seenKeys[key]) continue;

        const component = await createTimePickerVariant({
          state,
          status,
          content,
          variableByName,
          fonts,
          stats,
        });
        existing.appendChild(component);
        seenKeys[key] = true;
        stats.variantsCreated += 1;
      }
    }
  }

  layoutTimePickerVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureTimePickerProperties(existing, stats);
  return stats;
}

async function rebuildTimePickerComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "TimePicker",
    componentSetName: "TimePicker",
    build: buildTimePickerComponent,
  });
}

async function buildFileUploadComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "FileUpload", stats);

  const existing = findComponentSetOnPage(page, "FileUpload");
  if (existing) {
    normalizeComponentSetNodeName(existing, "FileUpload", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "FileUpload already exists. Use Update FileUpload to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (
    let stateIndex = 0;
    stateIndex < FILE_UPLOAD_STATES.length;
    stateIndex += 1
  ) {
    const state = FILE_UPLOAD_STATES[stateIndex];
    for (
      let statusIndex = 0;
      statusIndex < FILE_UPLOAD_STATUSES.length;
      statusIndex += 1
    ) {
      const status = FILE_UPLOAD_STATUSES[statusIndex];
      for (
        let contentIndex = 0;
        contentIndex < FILE_UPLOAD_CONTENT.length;
        contentIndex += 1
      ) {
        const content = FILE_UPLOAD_CONTENT[contentIndex];
        const component = await createFileUploadVariant({
          state,
          status,
          content,
          variableByName,
          fonts,
          stats,
        });
        component.x = statusIndex * 880 + contentIndex * 440;
        component.y = stateIndex * 390;
        page.appendChild(component);
        components.push(component);
      }
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "FileUpload";
  componentSet.x = 80;
  componentSet.y = 11280;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "FileUpload");
  applyComponentSetDescription(componentSet, "FileUpload", false, [
    "Kozmos FileUpload component set generated from React FileUpload API.",
    "State maps to focus, dragActive, and disabled behavior in Code Connect examples.",
    "Status maps to FieldWrapper validation tone: default, error, warning, and success.",
    "Content maps to drop-zone and selected-file list anatomy examples.",
    "DropZone is represented by the Content=Dropzone variant because React exports DropZone as FileUpload.",
    "File rows use curated icon sources for file and remove affordances.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureFileUploadProperties(componentSet, stats);
  return stats;
}

async function updateFileUploadComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "FileUpload", stats);

  const existing = findComponentSetOnPage(page, "FileUpload");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "FileUpload was not found. Run Build FileUpload first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "FileUpload", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "FileUpload");
  applyComponentSetDescription(existing, "FileUpload", true, [
    "Kozmos FileUpload component set generated from React FileUpload API.",
    "State maps to focus, dragActive, and disabled behavior in Code Connect examples.",
    "Status maps to FieldWrapper validation tone: default, error, warning, and success.",
    "Content maps to drop-zone and selected-file list anatomy examples.",
    "DropZone is represented by the Content=Dropzone variant because React exports DropZone as FileUpload.",
    "File rows use curated icon sources for file and remove affordances.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseFileUploadVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized FileUpload variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[`${props.state}/${props.status}/${props.content}`] = true;
    await updateFileUploadVariant(child, {
      state: props.state,
      status: props.status,
      content: props.content,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const state of FILE_UPLOAD_STATES) {
    for (const status of FILE_UPLOAD_STATUSES) {
      for (const content of FILE_UPLOAD_CONTENT) {
        const key = `${state}/${status}/${content}`;
        if (seenKeys[key]) continue;

        const component = await createFileUploadVariant({
          state,
          status,
          content,
          variableByName,
          fonts,
          stats,
        });
        existing.appendChild(component);
        seenKeys[key] = true;
        stats.variantsCreated += 1;
      }
    }
  }

  layoutFileUploadVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureFileUploadProperties(existing, stats);
  return stats;
}

async function rebuildFileUploadComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "FileUpload",
    componentSetName: "FileUpload",
    build: buildFileUploadComponent,
  });
}

async function buildColorPickerComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "ColorPicker", stats);

  const existing = findComponentSetOnPage(page, "ColorPicker");
  if (existing) {
    normalizeComponentSetNodeName(existing, "ColorPicker", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "ColorPicker already exists. Use Update ColorPicker to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (
    let stateIndex = 0;
    stateIndex < COLOR_PICKER_STATES.length;
    stateIndex += 1
  ) {
    const state = COLOR_PICKER_STATES[stateIndex];
    for (
      let statusIndex = 0;
      statusIndex < COLOR_PICKER_STATUSES.length;
      statusIndex += 1
    ) {
      const status = COLOR_PICKER_STATUSES[statusIndex];
      for (
        let contentIndex = 0;
        contentIndex < COLOR_PICKER_CONTENT.length;
        contentIndex += 1
      ) {
        const content = COLOR_PICKER_CONTENT[contentIndex];
        for (
          let formatIndex = 0;
          formatIndex < COLOR_PICKER_FORMATS.length;
          formatIndex += 1
        ) {
          const format = COLOR_PICKER_FORMATS[formatIndex];
          const component = await createColorPickerVariant({
            state,
            status,
            content,
            format,
            variableByName,
            fonts,
            stats,
          });
          component.x = statusIndex * 1140 + contentIndex * 380;
          component.y = stateIndex * 2300 + formatIndex * 720;
          page.appendChild(component);
          components.push(component);
        }
      }
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "ColorPicker";
  componentSet.x = 80;
  componentSet.y = 12840;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "ColorPicker");
  applyComponentSetDescription(componentSet, "ColorPicker", false, [
    "Kozmos ColorPicker component set generated from React ColorPicker API.",
    "State maps to focus, disabled, and readOnly props in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Content maps to closed field and open picker popover anatomy examples.",
    "Format maps to ColorPicker.format and controls whether HEX, RGB, or HSL inputs are visible.",
    "Label Text maps to ColorPicker.label.",
    "Value Text maps to the selected color value in Code Connect examples.",
    "Preset swatches represent ColorPicker.presets; product code supplies accessible names.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
  ]);
  clearComponentSetContainerFill(componentSet);
  layoutColorPickerVariants(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureColorPickerProperties(componentSet, stats);
  return stats;
}

async function updateColorPickerComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "ColorPicker", stats);

  const existing = findComponentSetOnPage(page, "ColorPicker");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "ColorPicker was not found. Run Build ColorPicker first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "ColorPicker", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "ColorPicker");
  applyComponentSetDescription(existing, "ColorPicker", true, [
    "Kozmos ColorPicker component set generated from React ColorPicker API.",
    "State maps to focus, disabled, and readOnly props in Code Connect.",
    "Status maps to validation tone: default, error, warning, and success.",
    "Content maps to closed field and open picker popover anatomy examples.",
    "Format maps to ColorPicker.format and controls whether HEX, RGB, or HSL inputs are visible.",
    "Label Text maps to ColorPicker.label.",
    "Value Text maps to the selected color value in Code Connect examples.",
    "Preset swatches represent ColorPicker.presets; product code supplies accessible names.",
    "Focus follows the React focus-visible ring; documented in metadata for v1.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseColorPickerVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized ColorPicker variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[
      `${props.state}/${props.status}/${props.content}/${props.format}`
    ] = true;
    await updateColorPickerVariant(child, {
      state: props.state,
      status: props.status,
      content: props.content,
      format: props.format,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const state of COLOR_PICKER_STATES) {
    for (const status of COLOR_PICKER_STATUSES) {
      for (const content of COLOR_PICKER_CONTENT) {
        for (const format of COLOR_PICKER_FORMATS) {
          const key = `${state}/${status}/${content}/${format}`;
          if (seenKeys[key]) continue;

          const component = await createColorPickerVariant({
            state,
            status,
            content,
            format,
            variableByName,
            fonts,
            stats,
          });
          existing.appendChild(component);
          seenKeys[key] = true;
          stats.variantsCreated += 1;
        }
      }
    }
  }

  layoutColorPickerVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureColorPickerProperties(existing, stats);
  return stats;
}

async function rebuildColorPickerComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "ColorPicker",
    componentSetName: "ColorPicker",
    build: buildColorPickerComponent,
  });
}

async function buildTextareaComponent() {
  return buildStateStatusComponent({
    componentName: "Textarea",
    componentSetName: "Textarea",
    states: TEXTAREA_STATES,
    statuses: TEXTAREA_STATUSES,
    x: 1520,
    y: 2220,
    xStep: 360,
    yStep: 160,
    createVariant: createTextareaVariant,
    configureProperties: configureTextareaProperties,
    description: [
      "Kozmos Textarea component set generated from React Textarea API.",
      "State maps to focus, disabled, and readOnly props in Code Connect.",
      "Status maps to Textarea.error in Code Connect.",
      "Label Text maps to Textarea.label.",
      "Placeholder Text maps to Textarea.placeholder.",
      "Focus follows the React focus-visible ring; documented in metadata for v1.",
    ],
  });
}

async function updateTextareaComponent() {
  return updateStateStatusComponent({
    componentName: "Textarea",
    componentSetName: "Textarea",
    states: TEXTAREA_STATES,
    statuses: TEXTAREA_STATUSES,
    xStep: 360,
    yStep: 160,
    createVariant: createTextareaVariant,
    updateVariant: updateTextareaVariant,
    parseVariantName: parseTextareaVariantName,
    configureProperties: configureTextareaProperties,
    description: [
      "Kozmos Textarea component set generated from React Textarea API.",
      "State maps to focus, disabled, and readOnly props in Code Connect.",
      "Status maps to Textarea.error in Code Connect.",
      "Label Text maps to Textarea.label.",
      "Placeholder Text maps to Textarea.placeholder.",
      "Focus follows the React focus-visible ring; documented in metadata for v1.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildSearchComponent() {
  return buildStateStatusComponent({
    componentName: "Search",
    componentSetName: "Search",
    states: SEARCH_STATES,
    statuses: SEARCH_STATUSES,
    x: 80,
    y: 2740,
    xStep: 360,
    yStep: 126,
    createVariant: createSearchVariant,
    configureProperties: configureSearchProperties,
    description: [
      "Kozmos Search component set generated from React Search API.",
      "State maps to focus, disabled, and readOnly props in Code Connect.",
      "Status maps to Search.error in Code Connect.",
      "Label Text maps to Search.label.",
      "Placeholder Text maps to Search.placeholder.",
      "Focus follows the React focus-visible ring; documented in metadata for v1.",
    ],
  });
}

async function updateSearchComponent() {
  return updateStateStatusComponent({
    componentName: "Search",
    componentSetName: "Search",
    states: SEARCH_STATES,
    statuses: SEARCH_STATUSES,
    xStep: 360,
    yStep: 126,
    createVariant: createSearchVariant,
    updateVariant: updateSearchVariant,
    parseVariantName: parseSearchVariantName,
    configureProperties: configureSearchProperties,
    description: [
      "Kozmos Search component set generated from React Search API.",
      "State maps to focus, disabled, and readOnly props in Code Connect.",
      "Status maps to Search.error in Code Connect.",
      "Label Text maps to Search.label.",
      "Placeholder Text maps to Search.placeholder.",
      "Focus follows the React focus-visible ring; documented in metadata for v1.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildSelectComponent() {
  return buildStateStatusComponent({
    componentName: "Select",
    componentSetName: "Select",
    states: SELECT_STATES,
    statuses: SELECT_STATUSES,
    x: 80,
    y: 3280,
    xStep: 360,
    yStep: 96,
    createVariant: createSelectVariant,
    configureProperties: configureSelectProperties,
    description: [
      "Kozmos SelectTrigger component set generated from React Select API.",
      "State maps to focus and disabled props in Code Connect.",
      "Status maps to SelectTrigger.error in Code Connect.",
      "Placeholder Text maps to SelectValue.placeholder.",
      "Focus follows the React focus-visible ring; documented in metadata for v1.",
    ],
  });
}

async function updateSelectComponent() {
  return updateStateStatusComponent({
    componentName: "Select",
    componentSetName: "Select",
    states: SELECT_STATES,
    statuses: SELECT_STATUSES,
    xStep: 360,
    yStep: 96,
    createVariant: createSelectVariant,
    updateVariant: updateSelectVariant,
    parseVariantName: parseSelectVariantName,
    configureProperties: configureSelectProperties,
    description: [
      "Kozmos SelectTrigger component set generated from React Select API.",
      "State maps to focus and disabled props in Code Connect.",
      "Status maps to SelectTrigger.error in Code Connect.",
      "Placeholder Text maps to SelectValue.placeholder.",
      "Focus follows the React focus-visible ring; documented in metadata for v1.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildSliderComponent() {
  return buildStateStatusComponent({
    componentName: "Slider",
    componentSetName: "Slider",
    states: SLIDER_STATES,
    statuses: SLIDER_STATUSES,
    types: SLIDER_TYPES,
    x: 1160,
    y: 3280,
    xStep: 360,
    yStep: 112,
    createVariant: createSliderVariant,
    configureProperties: configureSliderProperties,
    description: [
      "Kozmos Slider component set generated from React Slider API.",
      "State maps to focus and disabled props in Code Connect.",
      "Status maps to Slider.error in Code Connect.",
      "Type maps to Slider.defaultValue and thumbCount in Code Connect.",
      "Label Text maps to Slider.label.",
      "Focus follows the React focus-visible ring; documented in metadata for v1.",
    ],
  });
}

async function updateSliderComponent() {
  return updateStateStatusComponent({
    componentName: "Slider",
    componentSetName: "Slider",
    states: SLIDER_STATES,
    statuses: SLIDER_STATUSES,
    types: SLIDER_TYPES,
    xStep: 360,
    yStep: 112,
    createVariant: createSliderVariant,
    updateVariant: updateSliderVariant,
    parseVariantName: parseSliderVariantName,
    configureProperties: configureSliderProperties,
    description: [
      "Kozmos Slider component set generated from React Slider API.",
      "State maps to focus and disabled props in Code Connect.",
      "Status maps to Slider.error in Code Connect.",
      "Type maps to Slider.defaultValue and thumbCount in Code Connect.",
      "Label Text maps to Slider.label.",
      "Focus follows the React focus-visible ring; documented in metadata for v1.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildStateStatusComponent(config) {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, config.componentName, stats);

  const existing = findComponentSetOnPage(page, config.componentSetName);
  if (existing) {
    normalizeComponentSetNodeName(existing, config.componentSetName, stats);
    const duplicateCount = removeDuplicateGeneratedComponentSetsForName(
      page,
      config.componentSetName,
      existing,
      stats,
    );
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message = `${config.componentSetName} already exists. Use Update ${config.componentName} to preserve its node ID.`;
    if (duplicateCount > 0) {
      await reorganizeAfterGeneratedComponentMutation(stats);
    }
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];
  const types = config.types || [null];

  for (let stateIndex = 0; stateIndex < config.states.length; stateIndex += 1) {
    const state = config.states[stateIndex];
    for (
      let statusIndex = 0;
      statusIndex < config.statuses.length;
      statusIndex += 1
    ) {
      const status = config.statuses[statusIndex];
      for (let typeIndex = 0; typeIndex < types.length; typeIndex += 1) {
        const type = types[typeIndex];
        const component = await config.createVariant({
          state,
          status,
          type,
          variableByName,
          fonts,
          stats,
        });
        component.x = (statusIndex * types.length + typeIndex) * config.xStep;
        component.y = stateIndex * config.yStep;
        page.appendChild(component);
        components.push(component);
      }
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = config.componentSetName;
  componentSet.x = config.x;
  componentSet.y = config.y;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(
    RUN_NAMESPACE,
    "component",
    config.componentName,
  );
  applyComponentSetDescription(
    componentSet,
    config.componentSetName,
    false,
    config.description,
  );
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  await config.configureProperties(componentSet, stats, variableByName);
  await reorganizeAfterGeneratedComponentMutation(stats);
  return stats;
}

async function updateStateStatusComponent(config) {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, config.componentName, stats);

  const existing = findComponentSetOnPage(page, config.componentSetName);
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = `${config.componentSetName} was not found. Run Build ${config.componentName} first.`;
    return stats;
  }
  normalizeComponentSetNodeName(existing, config.componentSetName, stats);
  removeDuplicateGeneratedComponentSetsForName(
    page,
    config.componentSetName,
    existing,
    stats,
  );

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};
  const types = config.types || [null];

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(
    RUN_NAMESPACE,
    "component",
    config.componentName,
  );
  applyComponentSetDescription(
    existing,
    config.componentSetName,
    true,
    config.description,
  );
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = config.parseVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized ${config.componentName} variant "${child.name}".`,
      );
      continue;
    }

    const type = props.type || null;
    seenKeys[stateStatusComponentKey(props.state, props.status, type)] = true;
    await config.updateVariant(child, {
      state: props.state,
      status: props.status,
      type,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const state of config.states) {
    for (const status of config.statuses) {
      for (const type of types) {
        const key = stateStatusComponentKey(state, status, type);
        if (seenKeys[key]) continue;

        const component = await config.createVariant({
          state,
          status,
          type,
          variableByName,
          fonts,
          stats,
        });
        existing.appendChild(component);
        seenKeys[key] = true;
        stats.variantsCreated += 1;
      }
    }
  }

  layoutStateStatusVariants(existing, config);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  await config.configureProperties(existing, stats, variableByName);
  await reorganizeAfterGeneratedComponentMutation(stats);
  return stats;
}

function layoutStateStatusVariants(componentSet, config) {
  if (!componentSet || !componentSet.children) return;

  const types = config.types || [null];
  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = config.parseVariantName(child.name);
    if (!props) continue;

    const stateIndex = config.states.indexOf(props.state);
    const statusIndex = config.statuses.indexOf(props.status);
    const typeIndex = types.indexOf(props.type || null);
    child.x =
      (statusIndex * types.length + Math.max(0, typeIndex)) * config.xStep;
    child.y = stateIndex * config.yStep;
  }
}

function stateStatusComponentKey(state, status, type) {
  return type ? `${state}/${status}/${type}` : `${state}/${status}`;
}

async function buildPlannedMatrixComponent(config) {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, config.componentName, stats);

  const existing = findComponentSetOnPage(page, config.componentSetName);
  if (existing) {
    normalizeComponentSetNodeName(existing, config.componentSetName, stats);
    const duplicateCount = removeDuplicateGeneratedComponentSetsForName(
      page,
      config.componentSetName,
      existing,
      stats,
    );
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message = `${config.componentSetName} already exists. Use Update ${config.componentName} to preserve its node ID.`;
    if (duplicateCount > 0) {
      await reorganizeAfterGeneratedComponentMutation(stats);
    }
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  await ensureKozmosTextStyles(fonts, stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (const props of config.combinations()) {
    const component = await config.createVariant({
      props,
      variableByName,
      fonts,
      stats,
    });
    page.appendChild(component);
    components.push(component);
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = config.componentSetName;
  componentSet.x = config.x;
  componentSet.y = config.y;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(
    RUN_NAMESPACE,
    "component",
    config.componentName,
  );
  applyComponentSetDescription(
    componentSet,
    config.componentSetName,
    false,
    config.description,
  );
  clearComponentSetContainerFill(componentSet);

  if (typeof config.layoutVariants === "function") {
    config.layoutVariants(componentSet);
  }

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  await config.configureProperties(componentSet, stats, variableByName);
  runGeneratedComponentPostUpdateMaintenance(
    componentSet,
    config.componentName,
    stats,
  );
  await reorganizeAfterGeneratedComponentMutation(stats);
  await runGeneratedComponentPostLayoutMaintenance({
    componentName: config.componentName,
    componentSetName: config.componentSetName,
    stats,
  });
  return stats;
}

async function updatePlannedMatrixComponent(config) {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    variantsRemoved: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, config.componentName, stats);

  const existing = findComponentSetOnPage(page, config.componentSetName);
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = `${config.componentSetName} was not found. Run Build ${config.componentName} first.`;
    return stats;
  }
  normalizeComponentSetNodeName(existing, config.componentSetName, stats);
  removeDuplicateGeneratedComponentSetsForName(
    page,
    config.componentSetName,
    existing,
    stats,
  );

  const fonts = await loadButtonFonts(stats);
  await ensureKozmosTextStyles(fonts, stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(
    RUN_NAMESPACE,
    "component",
    config.componentName,
  );
  applyComponentSetDescription(
    existing,
    config.componentSetName,
    true,
    config.description,
  );
  clearComponentSetContainerFill(existing);

  const desiredKeys = {};
  if (config.removeUnexpectedVariants || config.removeDuplicateVariantKeys) {
    for (const props of config.combinations()) {
      desiredKeys[config.keyForProps(props)] = true;
    }
  }

  const existingChildren = Array.from(existing.children || []);
  for (const child of existingChildren) {
    if (child.type !== "COMPONENT") continue;

    const props = config.parseVariantName(child.name);
    if (!props) {
      if (config.removeUnexpectedVariants) {
        try {
          child.remove();
          stats.variantsRemoved += 1;
        } catch (error) {
          stats.warnings.push(
            `Could not remove unrecognized ${config.componentName} variant "${child.name}" (${messageFor(error)}).`,
          );
        }
        continue;
      }
      stats.warnings.push(
        `Skipped unrecognized ${config.componentName} variant "${child.name}".`,
      );
      continue;
    }

    const key = config.keyForProps(props);
    if (config.removeUnexpectedVariants && !desiredKeys[key]) {
      try {
        child.remove();
        stats.variantsRemoved += 1;
      } catch (error) {
        stats.warnings.push(
          `Could not remove stale ${config.componentName} variant "${child.name}" (${messageFor(error)}).`,
        );
      }
      continue;
    }

    if (seenKeys[key]) {
      if (config.removeDuplicateVariantKeys) {
        try {
          child.remove();
          stats.variantsRemoved += 1;
        } catch (error) {
          stats.warnings.push(
            `Could not remove duplicate ${config.componentName} variant "${child.name}" (${messageFor(error)}).`,
          );
        }
        continue;
      }
      stats.warnings.push(
        `Skipped duplicate ${config.componentName} variant "${child.name}".`,
      );
      continue;
    }

    seenKeys[key] = true;
    await config.updateVariant(child, {
      props,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const props of config.combinations()) {
    const key = config.keyForProps(props);
    if (seenKeys[key]) continue;

    const component = await config.createVariant({
      props,
      variableByName,
      fonts,
      stats,
    });
    existing.appendChild(component);
    seenKeys[key] = true;
    stats.variantsCreated += 1;
  }

  if (typeof config.layoutVariants === "function") {
    config.layoutVariants(existing);
  }

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  await config.configureProperties(existing, stats, variableByName);
  runGeneratedComponentPostUpdateMaintenance(
    existing,
    config.componentName,
    stats,
  );
  await reorganizeAfterGeneratedComponentMutation(stats);
  await runGeneratedComponentPostLayoutMaintenance({
    componentName: config.componentName,
    componentSetName: config.componentSetName,
    stats,
  });
  return stats;
}

function parseVariantValueMap(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  return values;
}

function buildTagComponent() {
  return buildPlannedMatrixComponent(tagComponentConfig());
}

function updateTagComponent() {
  return updatePlannedMatrixComponent(tagComponentConfig());
}

async function rebuildTagComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Tag",
    componentSetName: "Tag",
    build: buildTagComponent,
  });
}

function buildScrollAreaComponent() {
  return buildPlannedMatrixComponent(scrollAreaComponentConfig());
}

function updateScrollAreaComponent() {
  return updatePlannedMatrixComponent(scrollAreaComponentConfig());
}

async function rebuildScrollAreaComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "ScrollArea",
    componentSetName: "ScrollArea",
    build: buildScrollAreaComponent,
  });
}

function buildBottomNavigationComponent() {
  return buildPlannedMatrixComponent(bottomNavigationComponentConfig());
}

function updateBottomNavigationComponent() {
  return updatePlannedMatrixComponent(bottomNavigationComponentConfig());
}

async function rebuildBottomNavigationComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "BottomNavigation",
    componentSetName: "BottomNavigation",
    build: buildBottomNavigationComponent,
  });
}

function buildNavigationItemComponent() {
  return buildPlannedMatrixComponent(navigationItemComponentConfig());
}

function updateNavigationItemComponent() {
  return updatePlannedMatrixComponent(navigationItemComponentConfig());
}

async function rebuildNavigationItemComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "NavigationItem",
    componentSetName: "NavigationItem",
    build: buildNavigationItemComponent,
  });
}

function buildNavbarComponent() {
  return buildPlannedMatrixComponent(navbarComponentConfig());
}

function updateNavbarComponent() {
  return updatePlannedMatrixComponent(navbarComponentConfig());
}

async function rebuildNavbarComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Navbar",
    componentSetName: "Navbar",
    build: buildNavbarComponent,
  });
}

function buildSidebarComponent() {
  return buildPlannedMatrixComponent(sidebarComponentConfig());
}

function updateSidebarComponent() {
  return updatePlannedMatrixComponent(sidebarComponentConfig());
}

async function rebuildSidebarComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Sidebar",
    componentSetName: "Sidebar",
    build: buildSidebarComponent,
  });
}

function buildBackdropComponent() {
  return buildPlannedMatrixComponent(backdropComponentConfig());
}

function updateBackdropComponent() {
  return updatePlannedMatrixComponent(backdropComponentConfig());
}

async function rebuildBackdropComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Backdrop",
    componentSetName: "Backdrop",
    build: buildBackdropComponent,
  });
}

function buildBottomSheetComponent() {
  return buildPlannedMatrixComponent(bottomSheetComponentConfig());
}

function updateBottomSheetComponent() {
  return updateBottomSheetComponentAsync();
}

async function updateBottomSheetComponentAsync() {
  const stats = await updatePlannedMatrixComponent(
    bottomSheetComponentConfig(),
  );
  return repairCompositeComponentAfterUpdate({
    stats,
    componentName: "BottomSheet",
    componentSetName: "BottomSheet",
  });
}

async function rebuildBottomSheetComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "BottomSheet",
    componentSetName: "BottomSheet",
    build: buildBottomSheetComponent,
  });
}

function buildSearchBarComponent() {
  return buildPlannedMatrixComponent(searchBarComponentConfig());
}

function updateSearchBarComponent() {
  return updatePlannedMatrixComponent(searchBarComponentConfig());
}

async function rebuildSearchBarComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "SearchBar",
    componentSetName: "SearchBar",
    build: buildSearchBarComponent,
  });
}

function buildTreeComponent() {
  return buildPlannedMatrixComponent(treeComponentConfig());
}

function updateTreeComponent() {
  return updatePlannedMatrixComponent(treeComponentConfig());
}

async function rebuildTreeComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Tree",
    componentSetName: "Tree",
    build: buildTreeComponent,
  });
}

function buildTreeParentItemComponent() {
  return buildPlannedMatrixComponent(treeParentItemComponentConfig());
}

function updateTreeParentItemComponent() {
  return updateRepairableTreeItemMatrixComponent({
    config: treeParentItemComponentConfig(),
    build: buildTreeParentItemComponent,
  });
}

async function rebuildTreeParentItemComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "TreeParentItem",
    componentSetName: "TreeParentItem",
    build: buildTreeParentItemComponent,
  });
}

function buildTreeChildItemComponent() {
  return buildPlannedMatrixComponent(treeChildItemComponentConfig());
}

function updateTreeChildItemComponent() {
  return updateRepairableTreeItemMatrixComponent({
    config: treeChildItemComponentConfig(),
    build: buildTreeChildItemComponent,
  });
}

async function rebuildTreeChildItemComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "TreeChildItem",
    componentSetName: "TreeChildItem",
    build: buildTreeChildItemComponent,
  });
}

function buildTreeItemComponent() {
  return buildPlannedMatrixComponent(treeItemComponentConfig());
}

function updateTreeItemComponent() {
  return updateRepairableTreeItemMatrixComponent({
    config: treeItemComponentConfig(),
    build: buildTreeItemComponent,
  });
}

async function rebuildTreeItemComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "TreeItem",
    componentSetName: "TreeItem",
    build: buildTreeItemComponent,
  });
}

async function updateRepairableTreeItemMatrixComponent({ config, build }) {
  const preflight = await componentSetPropertyDefinitionReadStatus(
    config.componentSetName,
  );
  if (preflight.error) {
    return rebuildUnreadableComponentSet({
      build,
      config,
      readError: preflight.error,
    });
  }

  const stats = await updatePlannedMatrixComponent(config);
  const repairStatus = await treeItemComponentSetRepairStatus(
    config.componentSetName,
  );
  if (!repairStatus.needsRepair) return stats;

  return rebuildUnreadableComponentSet({
    build,
    config,
    readError:
      repairStatus.reasons.join("; ") ||
      (hasComponentPropertyDefinitionReadWarning(stats)
        ? "post-update component property definitions are unreadable"
        : ""),
  });
}

async function componentSetPropertyDefinitionReadStatus(componentSetName) {
  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  const existing = findComponentSetOnPage(page, componentSetName);
  if (!existing || existing.type !== "COMPONENT_SET") {
    return { definitions: {}, error: null };
  }

  return safeComponentPropertyDefinitions(
    existing,
    null,
    `${componentSetName} preflight`,
  );
}

async function rebuildUnreadableComponentSet({ config, build, readError }) {
  const rebuilt = await rebuildGeneratedComponentSet({
    componentName: config.componentName,
    componentSetName: config.componentSetName,
    build,
  });
  rebuilt.repairedPropertyDefinitions = true;
  rebuilt.warnings = rebuilt.warnings || [];
  const errorSuffix = readError ? ` (${readError})` : "";
  rebuilt.warnings.push(
    `${config.componentSetName} had unreadable Figma component property definitions${errorSuffix}, so the updater rebuilt it with a fresh component set and clean property panel.`,
  );
  return rebuilt;
}

function hasComponentPropertyDefinitionReadWarning(stats) {
  if (!stats || !Array.isArray(stats.warnings)) return false;

  return stats.warnings.some((warning) => {
    if (typeof warning !== "string") return false;
    return warning
      .toLowerCase()
      .includes("could not read component property definitions");
  });
}

async function treeItemComponentSetRepairStatus(componentSetName) {
  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  const existing = findComponentSetOnPage(page, componentSetName);
  if (!existing || existing.type !== "COMPONENT_SET") {
    return { needsRepair: false, reasons: [] };
  }

  const read = safeComponentPropertyDefinitions(
    existing,
    null,
    `${componentSetName} repair check`,
  );
  if (read.error) {
    return {
      needsRepair: true,
      reasons: [
        `component property definitions are unreadable (${read.error})`,
      ],
    };
  }

  const reasons = [];
  const expectedAxes = expectedVariantAxesForComponentSetName(existing.name);
  const variantProperties = auditVariantProperties(
    read.definitions,
    expectedAxes,
  );
  if (variantProperties.issueCount > 0) {
    reasons.push(
      `${variantProperties.issueCount} stale or unexpected variant property definition(s) remain`,
    );
  }

  const conflicts = auditComponentPropertyConflicts(read.definitions);
  if (conflicts.issueCount > 0) {
    reasons.push(
      `${conflicts.issueCount} conflicting component property name(s) remain`,
    );
  }

  const focusVisible = auditFocusVisibleProperty(existing, read.definitions);
  if (!focusVisible.propertyName || focusVisible.boundNodes === 0) {
    reasons.push("Focus Visible is missing or not bound to focus rings");
  }

  return {
    needsRepair: reasons.length > 0,
    reasons,
  };
}

function buildTimelineComponent() {
  return buildPlannedMatrixComponent(timelineComponentConfig());
}

function updateTimelineComponent() {
  return updatePlannedMatrixComponent(timelineComponentConfig());
}

async function rebuildTimelineComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Timeline",
    componentSetName: "Timeline",
    build: buildTimelineComponent,
  });
}

function tagComponentConfig() {
  return {
    componentName: "Tag",
    componentSetName: "Tag",
    x: 80,
    y: 11860,
    combinations: tagVariantCombinations,
    keyForProps: tagVariantKey,
    parseVariantName: parseTagVariantName,
    createVariant: createTagVariant,
    updateVariant: updateTagVariant,
    layoutVariants: layoutTagVariants,
    configureProperties: configureTagProperties,
    description: [
      "Kozmos Tag component set generated from React Tag API.",
      "Variant maps to Tag.variant.",
      "Removable maps to whether remove affordance is present.",
      "Label Text maps to children in Code Connect.",
      "Tag is static metadata unless product code wraps it in a control.",
    ],
  };
}

function timelineComponentConfig() {
  return {
    componentName: "Timeline",
    componentSetName: "Timeline",
    x: 80,
    y: 14620,
    combinations: timelineVariantCombinations,
    keyForProps: timelineVariantKey,
    parseVariantName: parseTimelineVariantName,
    createVariant: createTimelineVariant,
    updateVariant: updateTimelineVariant,
    layoutVariants: layoutTimelineVariants,
    configureProperties: configureTimelineProperties,
    description: [
      "Kozmos Timeline component set generated from React Timeline anatomy.",
      "Content maps to simple or detailed event examples.",
      "Density maps to vertical spacing and row rhythm.",
      "Item time, title, and description text map to composed Timeline children.",
      "Timeline is non-interactive by default; product-specific event content remains composition-owned.",
    ],
  };
}

function scrollAreaComponentConfig() {
  return {
    componentName: "ScrollArea",
    componentSetName: "ScrollArea",
    x: 80,
    y: 12280,
    combinations: scrollAreaVariantCombinations,
    keyForProps: scrollAreaVariantKey,
    parseVariantName: parseScrollAreaVariantName,
    createVariant: createScrollAreaVariant,
    updateVariant: updateScrollAreaVariant,
    layoutVariants: layoutScrollAreaVariants,
    configureProperties: configureScrollAreaProperties,
    description: [
      "Kozmos ScrollArea component set generated from React ScrollArea API.",
      "Orientation maps to ScrollArea.orientation.",
      "Scrollbar maps to hideScrollbar in Code Connect examples.",
      "Content Slot maps to ScrollArea children.",
      "Native scroll mechanics, momentum, and viewport behavior remain runtime concerns.",
    ],
  };
}

function bottomNavigationComponentConfig() {
  return {
    componentName: "BottomNavigation",
    componentSetName: "BottomNavigation",
    x: 80,
    y: 12760,
    combinations: bottomNavigationVariantCombinations,
    keyForProps: bottomNavigationVariantKey,
    parseVariantName: parseBottomNavigationVariantName,
    createVariant: createBottomNavigationVariant,
    updateVariant: updateBottomNavigationVariant,
    layoutVariants: layoutBottomNavigationVariants,
    configureProperties: configureBottomNavigationProperties,
    description: [
      "Kozmos BottomNavigation component set generated from React BottomNavigation API.",
      "Count maps to the number of destination items.",
      "Active maps to the selected destination example.",
      "Item text properties map to each BottomNavigation item label.",
      "Safe-area padding and native platform placement remain runtime concerns.",
    ],
  };
}

function navigationItemComponentConfig() {
  return {
    componentName: "NavigationItem",
    componentSetName: "NavigationItem",
    x: 80,
    y: 12480,
    combinations: navigationItemVariantCombinations,
    keyForProps: navigationItemVariantKey,
    parseVariantName: parseNavigationItemVariantName,
    createVariant: createNavigationItemVariant,
    updateVariant: updateNavigationItemVariant,
    layoutVariants: layoutNavigationItemVariants,
    configureProperties: configureNavigationItemProperties,
    description: [
      "Kozmos NavigationItem component set generated from React NavigationItem API.",
      "Placement maps to Top, Side, and Rail item anatomy.",
      "Content maps to label, icon-label, icon-only, badge, and trailing slot examples.",
      "Leading Icon maps to the default icon instance-swap nested inside Leading Icon Slot.",
      "Density maps to default and compact row rhythm.",
      "State maps to default, hover, selected, focus, and disabled visual examples.",
      "Focus Visible controls the generated focus ring.",
      "Use NavigationItem instances inside Navbar and Sidebar slots.",
    ],
  };
}

function navbarComponentConfig() {
  return {
    componentName: "Navbar",
    componentSetName: "Navbar",
    x: 520,
    y: 12760,
    combinations: navbarVariantCombinations,
    keyForProps: navbarVariantKey,
    parseVariantName: parseNavbarVariantName,
    createVariant: createNavbarVariant,
    updateVariant: updateNavbarVariant,
    layoutVariants: layoutNavbarVariants,
    configureProperties: configureNavbarProperties,
    removeUnexpectedVariants: true,
    removeDuplicateVariantKeys: true,
    description: [
      "Kozmos Navbar component set generated from React Navbar API.",
      "Content maps to Basic, Actions, and Contextual navigation shell examples.",
      "Logo Slot maps to the leading brand area.",
      "Context Slot maps to workspace, tenant, project, or scope switcher content.",
      "Navigation Slot maps to Navbar children.",
      "Primary Action Slot maps to the main product command.",
      "Actions Slot maps to grouped action content in Actions examples.",
      "Utility Slot maps to secondary icon actions.",
      "Account Slot maps to the user/account menu affordance.",
      "Runtime owns route state, sticky behavior, responsive collapse, and navigation semantics.",
    ],
  };
}

function sidebarComponentConfig() {
  return {
    componentName: "Sidebar",
    componentSetName: "Sidebar",
    x: 520,
    y: 13240,
    combinations: sidebarVariantCombinations,
    keyForProps: sidebarVariantKey,
    parseVariantName: parseSidebarVariantName,
    createVariant: createSidebarVariant,
    updateVariant: updateSidebarVariant,
    layoutVariants: layoutSidebarVariants,
    configureProperties: configureSidebarProperties,
    removeUnexpectedVariants: true,
    removeDuplicateVariantKeys: true,
    description: [
      "Kozmos Sidebar component set generated from React Sidebar API.",
      "Content maps to Basic, Sections, Tools, and Rail navigation shell examples.",
      "Header Slot maps to brand, account, or workspace content.",
      "Navigation Slot maps to the primary Sidebar navigation content.",
      "Tools Slot maps to secondary actions or utility rows.",
      "Footer Slot maps to pinned account, status, or support content.",
      "Runtime owns responsive collapse, selected routes, and scrolling.",
    ],
  };
}

function backdropComponentConfig() {
  return {
    componentName: "Backdrop",
    componentSetName: "Backdrop",
    x: 80,
    y: 13280,
    combinations: backdropVariantCombinations,
    keyForProps: backdropVariantKey,
    parseVariantName: parseBackdropVariantName,
    createVariant: createBackdropVariant,
    updateVariant: updateBackdropVariant,
    layoutVariants: layoutBackdropVariants,
    configureProperties: configureBackdropProperties,
    description: [
      "Kozmos Backdrop component set generated from React Backdrop API.",
      "Visibility maps to Backdrop.visible.",
      "Visibility=Hidden is empty in Figma because runtime code renders nothing.",
      "Backdrop is a non-interactive composition layer behind modal content.",
      "Runtime code owns portal placement and dismiss behavior.",
    ],
  };
}

function bottomSheetComponentConfig() {
  return {
    componentName: "BottomSheet",
    componentSetName: "BottomSheet",
    x: 80,
    y: 13640,
    combinations: bottomSheetVariantCombinations,
    keyForProps: bottomSheetVariantKey,
    parseVariantName: parseBottomSheetVariantName,
    createVariant: createBottomSheetVariant,
    updateVariant: updateBottomSheetVariant,
    layoutVariants: layoutBottomSheetVariants,
    configureProperties: configureBottomSheetProperties,
    description: [
      "Kozmos BottomSheet component set generated from React BottomSheetContent API.",
      "Content maps to composed BottomSheet anatomy.",
      "Title Text and Description Text map to composed sheet header content.",
      "Content Slot maps to arbitrary sheet children; Form and Footer variants compose live Input and Button examples.",
      "Footer actions compose live Button instances.",
      "Backdrop, portal placement, safe-area behavior, snap points, and drag gestures remain runtime concerns.",
    ],
  };
}

function searchBarComponentConfig() {
  return {
    componentName: "SearchBar",
    componentSetName: "SearchBar",
    x: 80,
    y: 14120,
    combinations: searchBarVariantCombinations,
    keyForProps: searchBarVariantKey,
    parseVariantName: parseSearchBarVariantName,
    createVariant: createSearchBarVariant,
    updateVariant: updateSearchBarVariant,
    layoutVariants: layoutSearchBarVariants,
    configureProperties: configureSearchBarProperties,
    description: [
      "Kozmos SearchBar component set generated from React SearchBar API.",
      "Variant maps to SearchBar.variant.",
      "State maps to focus, filled, and disabled examples.",
      "Placeholder Text and Value Text map to SearchBar placeholder/value content.",
      "SearchBar is optimized for product search surfaces rather than form field groups.",
    ],
  };
}

function treeComponentConfig() {
  return {
    componentName: "Tree",
    componentSetName: "Tree",
    x: 80,
    y: 17920,
    combinations: treeVariantCombinations,
    keyForProps: treeVariantKey,
    parseVariantName: parseTreeVariantName,
    createVariant: createTreeVariant,
    updateVariant: updateTreeVariant,
    layoutVariants: layoutTreeVariants,
    configureProperties: configureTreeProperties,
    removeUnexpectedVariants: true,
    removeDuplicateVariantKeys: true,
    description: [
      "Kozmos Tree component set generated from React Tree API.",
      "Tree is a composition shell with a Content Slot for TreeParentItem and TreeChildItem rows.",
      "Density maps to default and compact row rhythm for composed row instances.",
      "Runtime code owns data, keyboard navigation, expansion state, selection callbacks, and nested row actions.",
    ],
  };
}

function treeParentItemComponentConfig() {
  return {
    componentName: "TreeParentItem",
    componentSetName: "TreeParentItem",
    x: 80,
    y: 15200,
    combinations: treeParentItemVariantCombinations,
    keyForProps: treeStandaloneItemVariantKey,
    parseVariantName: parseTreeParentItemVariantName,
    createVariant: createTreeParentItemVariant,
    updateVariant: updateTreeParentItemVariant,
    layoutVariants: layoutTreeParentItemVariants,
    configureProperties: configureTreeItemProperties,
    removeUnexpectedVariants: true,
    removeDuplicateVariantKeys: true,
    description: [
      "Kozmos TreeParentItem component set generated from React TreeParentItemRow anatomy.",
      "Content maps to Basic, Count, and Actions examples.",
      "Expanded maps to parent disclosure icon state.",
      "Depth maps to indentation examples for composed trees.",
      "Density maps to row height and vertical rhythm.",
      "State maps to idle, hover, selected, and focus-visible row treatment.",
      "Use TreeParentItem with TreeChildItem to compose product-specific trees.",
    ],
  };
}

function treeChildItemComponentConfig() {
  return {
    componentName: "TreeChildItem",
    componentSetName: "TreeChildItem",
    x: 80,
    y: 16480,
    combinations: treeChildItemVariantCombinations,
    keyForProps: treeStandaloneItemVariantKey,
    parseVariantName: parseTreeChildItemVariantName,
    createVariant: createTreeChildItemVariant,
    updateVariant: updateTreeChildItemVariant,
    layoutVariants: layoutTreeChildItemVariants,
    configureProperties: configureTreeItemProperties,
    removeUnexpectedVariants: true,
    removeDuplicateVariantKeys: true,
    description: [
      "Kozmos TreeChildItem component set generated from React TreeChildItemRow anatomy.",
      "Content maps to Basic, Count, and Actions examples.",
      "Depth maps to indentation examples for composed trees.",
      "Density maps to row height and vertical rhythm.",
      "State maps to idle, hover, selected, and focus-visible row treatment.",
      "Use TreeChildItem with TreeParentItem to compose product-specific trees.",
    ],
  };
}

function treeItemComponentConfig() {
  return {
    componentName: "TreeItem",
    componentSetName: "TreeItem",
    x: 80,
    y: 17760,
    combinations: treeItemVariantCombinations,
    keyForProps: treeItemVariantKey,
    parseVariantName: parseTreeItemVariantName,
    createVariant: createTreeItemVariant,
    updateVariant: updateTreeItemVariant,
    layoutVariants: layoutTreeItemVariants,
    configureProperties: configureTreeItemProperties,
    removeUnexpectedVariants: true,
    removeDuplicateVariantKeys: true,
    description: [
      "Kozmos TreeItem component set generated from React TreeItemRow anatomy.",
      "Type maps to parent or child row anatomy.",
      "Content maps to Basic, Count, and Actions examples.",
      "Expanded maps to parent disclosure icon state; Child variants keep Expanded False.",
      "Depth maps to indentation examples for composed trees.",
      "Density maps to row height and vertical rhythm.",
      "State maps to idle, hover, selected, and focus-visible row treatment.",
      "Prefer TreeParentItem and TreeChildItem for new product trees; TreeItem stays as a switchable compatibility row.",
    ],
  };
}

function tagVariantCombinations() {
  const combinations = [];
  for (const variant of TAG_VARIANTS) {
    for (const removable of TAG_REMOVABLE) {
      combinations.push({ variant, removable });
    }
  }
  return combinations;
}

function tagVariantKey(props) {
  return `${props.variant}/${props.removable}`;
}

function parseTagVariantName(name) {
  const values = parseVariantValueMap(name);
  if (
    TAG_VARIANTS.indexOf(values.Variant) === -1 ||
    TAG_REMOVABLE.indexOf(values.Removable) === -1
  ) {
    return null;
  }
  return { variant: values.Variant, removable: values.Removable };
}

function scrollAreaVariantCombinations() {
  const combinations = [];
  for (const orientation of SCROLL_AREA_ORIENTATIONS) {
    for (const scrollbar of SCROLL_AREA_SCROLLBARS) {
      combinations.push({ orientation, scrollbar });
    }
  }
  return combinations;
}

function scrollAreaVariantKey(props) {
  return `${props.orientation}/${props.scrollbar}`;
}

function parseScrollAreaVariantName(name) {
  const values = parseVariantValueMap(name);
  if (
    SCROLL_AREA_ORIENTATIONS.indexOf(values.Orientation) === -1 ||
    SCROLL_AREA_SCROLLBARS.indexOf(values.Scrollbar) === -1
  ) {
    return null;
  }
  return { orientation: values.Orientation, scrollbar: values.Scrollbar };
}

function bottomNavigationVariantCombinations() {
  const combinations = [];
  for (const count of BOTTOM_NAVIGATION_COUNTS) {
    for (const active of BOTTOM_NAVIGATION_ACTIVE) {
      if (!bottomNavigationActiveIsValidForCount(active, count)) continue;
      combinations.push({ count, active });
    }
  }
  return combinations;
}

function bottomNavigationVariantKey(props) {
  return `${props.count}/${props.active}`;
}

function parseBottomNavigationVariantName(name) {
  const values = parseVariantValueMap(name);
  if (
    BOTTOM_NAVIGATION_COUNTS.indexOf(values.Count) === -1 ||
    BOTTOM_NAVIGATION_ACTIVE.indexOf(values.Active) === -1 ||
    !bottomNavigationActiveIsValidForCount(values.Active, values.Count)
  ) {
    return null;
  }
  return { count: values.Count, active: values.Active };
}

function navigationItemVariantCombinations() {
  const combinations = [];
  for (const placement of NAVIGATION_ITEM_PLACEMENTS) {
    for (const content of NAVIGATION_ITEM_CONTENT) {
      for (const density of NAVIGATION_ITEM_DENSITIES) {
        for (const state of NAVIGATION_ITEM_STATES) {
          combinations.push({ placement, content, density, state });
        }
      }
    }
  }
  return combinations;
}

function navigationItemVariantKey(props) {
  return `${props.placement}/${props.content}/${props.density}/${props.state}`;
}

function parseNavigationItemVariantName(name) {
  const values = parseVariantValueMap(name);
  if (
    NAVIGATION_ITEM_PLACEMENTS.indexOf(values.Placement) === -1 ||
    NAVIGATION_ITEM_CONTENT.indexOf(values.Content) === -1 ||
    NAVIGATION_ITEM_DENSITIES.indexOf(values.Density) === -1 ||
    NAVIGATION_ITEM_STATES.indexOf(values.State) === -1
  ) {
    return null;
  }
  return {
    placement: values.Placement,
    content: values.Content,
    density: values.Density,
    state: values.State,
  };
}

function navbarVariantCombinations() {
  return NAVBAR_CONTENT.map((content) => ({ content }));
}

function navbarVariantKey(props) {
  return props.content;
}

function parseNavbarVariantName(name) {
  const values = parseVariantValueMap(name);
  if (values.Content === "Product") {
    return { content: "Contextual" };
  }
  if (NAVBAR_CONTENT.indexOf(values.Content) === -1) {
    return null;
  }
  return { content: values.Content };
}

function sidebarVariantCombinations() {
  return SIDEBAR_CONTENT.map((content) => ({ content }));
}

function sidebarVariantKey(props) {
  return props.content;
}

function parseSidebarVariantName(name) {
  const values = parseVariantValueMap(name);
  if (SIDEBAR_CONTENT.indexOf(values.Content) === -1) {
    return null;
  }
  return { content: values.Content };
}

function backdropVariantCombinations() {
  return BACKDROP_VISIBILITY.map((visibility) => ({ visibility }));
}

function backdropVariantKey(props) {
  return props.visibility;
}

function parseBackdropVariantName(name) {
  const values = parseVariantValueMap(name);
  if (BACKDROP_VISIBILITY.indexOf(values.Visibility) !== -1) {
    return { visibility: values.Visibility };
  }
  if (values.Visible === "True") return { visibility: "Visible" };
  if (values.Visible === "False") return { visibility: "Hidden" };
  if (values.State === "Visible") return { visibility: "Visible" };
  if (values.State === "Hidden") return { visibility: "Hidden" };
  return null;
}

function bottomSheetVariantCombinations() {
  return BOTTOM_SHEET_CONTENT.map((content) => ({ content }));
}

function bottomSheetVariantKey(props) {
  return props.content;
}

function parseBottomSheetVariantName(name) {
  const values = parseVariantValueMap(name);
  if (BOTTOM_SHEET_CONTENT.indexOf(values.Content) === -1) {
    return null;
  }
  return { content: values.Content };
}

function searchBarVariantCombinations() {
  const combinations = [];
  for (const variant of SEARCH_BAR_VARIANTS) {
    for (const state of SEARCH_BAR_STATES) {
      combinations.push({ variant, state });
    }
  }
  return combinations;
}

function searchBarVariantKey(props) {
  return `${props.variant}/${props.state}`;
}

function parseSearchBarVariantName(name) {
  const values = parseVariantValueMap(name);
  if (
    SEARCH_BAR_VARIANTS.indexOf(values.Variant) === -1 ||
    SEARCH_BAR_STATES.indexOf(values.State) === -1
  ) {
    return null;
  }
  return { variant: values.Variant, state: values.State };
}

function treeVariantCombinations() {
  return TREE_DENSITIES.map((density) => ({ density }));
}

function treeVariantKey(props) {
  return props.density;
}

function parseTreeVariantName(name) {
  const values = parseVariantValueMap(name);
  const density = values.Density || name;
  if (TREE_DENSITIES.indexOf(density) === -1) {
    return null;
  }
  return { density };
}

function treeParentItemVariantCombinations() {
  return treeItemVariantCombinationsForType("Parent");
}

function treeChildItemVariantCombinations() {
  return treeItemVariantCombinationsForType("Child");
}

function treeItemVariantCombinations() {
  const combinations = [];
  for (const type of TREE_ITEM_TYPES) {
    const variants = treeItemVariantCombinationsForType(type);
    for (const variant of variants) combinations.push(variant);
  }
  return combinations;
}

function treeItemVariantCombinationsForType(type) {
  const combinations = [];
  for (const content of TREE_CONTENT) {
    const expandedOptions = type === "Parent" ? TREE_EXPANDED : ["False"];
    for (const expanded of expandedOptions) {
      for (const depth of TREE_ITEM_DEPTHS) {
        for (const density of TREE_DENSITIES) {
          for (const state of TREE_STATES) {
            combinations.push({
              type,
              content,
              expanded,
              depth,
              density,
              state,
            });
          }
        }
      }
    }
  }
  return combinations;
}

function treeItemVariantKey(props) {
  return `${props.type}/${props.content}/${props.expanded}/${props.depth}/${props.density}/${props.state}`;
}

function treeStandaloneItemVariantKey(props) {
  return `${props.content}/${props.expanded}/${props.depth}/${props.density}/${props.state}`;
}

function parseTreeItemVariantName(name) {
  const values = parseVariantValueMap(name);
  const expanded = values.Expanded || "False";
  const type = normalizeTreeItemType(values.Type);
  const state = normalizeTreeState(values.State);
  if (
    TREE_ITEM_TYPES.indexOf(type) === -1 ||
    TREE_CONTENT.indexOf(values.Content) === -1 ||
    TREE_EXPANDED.indexOf(expanded) === -1 ||
    (type === "Child" && expanded !== "False") ||
    TREE_ITEM_DEPTHS.indexOf(values.Depth) === -1 ||
    TREE_DENSITIES.indexOf(values.Density) === -1 ||
    TREE_STATES.indexOf(state) === -1
  ) {
    return null;
  }
  return {
    type,
    content: values.Content,
    expanded,
    depth: values.Depth,
    density: values.Density,
    state,
  };
}

function parseTreeParentItemVariantName(name) {
  return parseTreeStandaloneItemVariantName(name, "Parent");
}

function parseTreeChildItemVariantName(name) {
  return parseTreeStandaloneItemVariantName(name, "Child");
}

function parseTreeStandaloneItemVariantName(name, type) {
  const values = parseVariantValueMap(name);
  const content = values.Content || "Basic";
  const expanded = type === "Parent" ? values.Expanded || "False" : "False";
  const depth = values.Depth || "0";
  const density = values.Density || "Default";
  const state = normalizeTreeState(values.State);
  const legacyType = values.Type ? normalizeTreeItemType(values.Type) : type;

  if (
    legacyType !== type ||
    TREE_CONTENT.indexOf(content) === -1 ||
    TREE_EXPANDED.indexOf(expanded) === -1 ||
    TREE_ITEM_DEPTHS.indexOf(depth) === -1 ||
    TREE_DENSITIES.indexOf(density) === -1 ||
    TREE_STATES.indexOf(state) === -1
  ) {
    return null;
  }

  return {
    type,
    content,
    expanded,
    depth,
    density,
    state,
  };
}

function normalizeTreeState(value) {
  if (value === "Default") return "Idle";
  if (TREE_STATES.indexOf(value) !== -1) return value;
  return value || "Idle";
}

function normalizeTreeItemType(value) {
  if (value === "Branch") return "Parent";
  if (value === "Leaf") return "Child";
  if (TREE_ITEM_TYPES.indexOf(value) !== -1) return value;
  return value || "Parent";
}

function timelineVariantCombinations() {
  const combinations = [];
  for (const content of TIMELINE_CONTENT) {
    for (const density of TIMELINE_DENSITIES) {
      combinations.push({ content, density });
    }
  }
  return combinations;
}

function timelineVariantKey(props) {
  return `${props.content}/${props.density}`;
}

function parseTimelineVariantName(name) {
  const values = parseVariantValueMap(name);
  if (
    TIMELINE_CONTENT.indexOf(values.Content) === -1 ||
    TIMELINE_DENSITIES.indexOf(values.Density) === -1
  ) {
    return null;
  }
  return { content: values.Content, density: values.Density };
}

async function createTagVariant({ props, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateTagVariant(component, {
    props,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

async function updateTagVariant(
  component,
  { props, variableByName, fonts, stats },
) {
  const config = tagConfig(props.variant);
  const removable = props.removable === "True";

  component.name = `Variant=${props.variant}, Removable=${props.removable}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 6;
  component.paddingLeft = 12;
  component.paddingRight = removable ? 8 : 12;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(64, 32);
  component.cornerRadius = 9999;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Tag");

  component.fills = config.background
    ? [
        paintFromVariable(
          config.background,
          config.backgroundFallback,
          variableByName,
          stats,
        ),
      ]
    : [];
  component.strokes = config.stroke
    ? [
        paintFromVariable(
          config.stroke,
          config.strokeFallback,
          variableByName,
          stats,
        ),
      ]
    : [];
  component.strokeWeight = config.stroke ? 1 : 0;

  removeDirectChildren(component);

  const label = figma.createText();
  label.name = "Label Text";
  await applyBadgeLabelTypography(label, fonts, variableByName, stats);
  label.characters = tagLabelText(props.variant);
  label.fills = [
    paintFromVariable(
      config.foreground,
      config.foregroundFallback,
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";
  setTextAutoResize(label, "WIDTH_AND_HEIGHT");
  component.appendChild(label);
  setHugChildSizing(label);

  if (removable) {
    const icon = await createFixedIconInstance(
      "x-close",
      config.foreground,
      config.foregroundFallback,
      variableByName,
      stats,
      14,
    );
    icon.name = "Remove Icon";
    component.appendChild(icon);
    setHugChildSizing(icon);
  }
}

async function createTreeItemVariant({ props, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateTreeItemVariant(component, {
    props,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

async function createTreeParentItemVariant({
  props,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateTreeParentItemVariant(component, {
    props,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

async function updateTreeParentItemVariant(
  component,
  { props, variableByName, fonts, stats },
) {
  await updateTreeItemVariant(component, {
    props: Object.assign({}, props, { type: "Parent" }),
    variableByName,
    fonts,
    stats,
    componentName: "TreeParentItem",
    includeTypeAxis: false,
  });
}

async function createTreeChildItemVariant({
  props,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateTreeChildItemVariant(component, {
    props,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

async function updateTreeChildItemVariant(
  component,
  { props, variableByName, fonts, stats },
) {
  await updateTreeItemVariant(component, {
    props: Object.assign({}, props, {
      type: "Child",
      expanded: "False",
    }),
    variableByName,
    fonts,
    stats,
    componentName: "TreeChildItem",
    includeTypeAxis: false,
  });
}

async function updateTreeItemVariant(
  component,
  {
    props,
    variableByName,
    fonts,
    stats,
    componentName = "TreeItem",
    includeTypeAxis = true,
  },
) {
  const metrics = treeMetrics({
    density: props.density,
    expanded: "False",
  });
  const row = treeItemRow(props);

  await syncTreeRowNode(component, {
    row,
    props,
    metrics,
    variableByName,
    fonts,
    stats,
    componentName,
    asComponentVariant: true,
    includeTypeAxis,
  });

  bindTreeGeometryVariables(
    component,
    props.density,
    [component],
    [component].flatMap((node) => treeIconNodesForBinding(node)),
    variableByName,
    stats,
  );
}

function treeItemRow(props) {
  const type = normalizeTreeItemType(props.type);
  const parent = type === "Parent";
  const content = props.content || "Basic";
  const withCounts = content === "Count" || content === "Actions";
  const withActions = content === "Actions";
  const state = normalizeTreeState(props.state);
  const actions =
    withActions && parent
      ? [
          { name: "Hide Action", iconName: "x-close" },
          { name: "Lock Action", iconName: "lock-01" },
        ]
      : withActions
        ? [{ name: "Edit Action", iconName: "edit-01" }]
        : [];

  return {
    key: "tree-item",
    type,
    textName: "Label Text",
    text: parent ? "Map content" : "Place item",
    countName: "Count Text",
    count: withCounts ? (parent ? "23" : "4") : null,
    actions,
    depth: Number(props.depth) || 0,
    expandable: parent,
    expanded: parent && props.expanded === "True",
    focused: state === "Focus",
    hovered: state === "Hover",
    iconName: parent ? "map-01" : "marker-pin-01",
    selected: state === "Selected",
    state,
  };
}

function treeIconNodesForBinding(root) {
  const icons = [];

  function walk(node) {
    if (
      node.getSharedPluginData &&
      (node.getSharedPluginData(RUN_NAMESPACE, "kind") ===
        "tree-disclosure-icon" ||
        node.getSharedPluginData(RUN_NAMESPACE, "kind") ===
          "icon-slot-instance")
    ) {
      icons.push(node);
    }

    if (node.children) {
      for (const child of node.children) walk(child);
    }
  }

  walk(root);
  return icons;
}

async function createTreeVariant({ props, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateTreeVariant(component, {
    props,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

async function updateTreeVariant(
  component,
  { props, variableByName, fonts, stats },
) {
  const metrics = treeMetrics({ density: props.density, expanded: "True" });
  const reusableContentSlot = extractDescendantNamed(
    component,
    "Content Slot",
    stats,
    "Tree content slot",
  );

  component.name = `Density=${props.density}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 0;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(metrics.width, metrics.height);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Tree");

  removeDirectChildrenExcept(component, reusableContentSlot);

  let contentSlot = reusableContentSlot;
  if (contentSlot) {
    configureContentSlotNode(contentSlot, {
      height: metrics.height,
      itemSpacing: 0,
      layoutMode: "VERTICAL",
      slotKind:
        contentSlot.getSharedPluginData &&
        contentSlot.getSharedPluginData(RUN_NAMESPACE, "slot-kind")
          ? contentSlot.getSharedPluginData(RUN_NAMESPACE, "slot-kind")
          : "native",
      slotName: "Content Slot",
      width: metrics.width,
    });
    component.appendChild(contentSlot);
  } else {
    contentSlot = appendComponentContentSlot({
      component,
      height: metrics.height,
      itemSpacing: 0,
      layoutMode: "VERTICAL",
      ownerName: "Tree",
      parent: component,
      slotName: "Content Slot",
      stats,
      width: metrics.width,
    });
  }

  setTreeContentSlotSizing(contentSlot);
  bindFloatVariable(
    component,
    "width",
    "Tree/width/default",
    variableByName,
    stats,
  );
}

function setTreeContentSlotSizing(contentSlot) {
  if (!contentSlot) return;

  setNodePropertyIfSupported(contentSlot, "primaryAxisSizingMode", "AUTO");
  setNodePropertyIfSupported(contentSlot, "counterAxisSizingMode", "FIXED");
  setVerticalStackChildSizing(contentSlot);
}

function treeMetrics(props) {
  const compact = props.density === "Compact";
  const rowCount = props.expanded === "True" ? 4 : 2;
  const rowHeight = compact ? 32 : 40;
  return {
    width: 320,
    height: rowCount * rowHeight,
    rowHeight,
    iconSize: 16,
    gap: 8,
    indent: 20,
    paddingX: 8,
    actionSize: 28,
    actionIconSize: 14,
  };
}

function treeRows(props) {
  const expanded = props.expanded === "True";
  const content = props.content || "Basic";
  const withCounts = content === "Count" || content === "Actions";
  const withActions = content === "Actions";
  const state = normalizeTreeState(props.state);
  const selectedKey = expanded ? "leaf" : "root";
  const interactiveKey = expanded ? "child" : "root";
  const activeKey =
    state === "Selected"
      ? selectedKey
      : state === "Focus" || state === "Hover"
        ? interactiveKey
        : null;
  const rows = [
    {
      key: "root",
      textName: "Root Text",
      text: "Map content",
      countName: "Root Count Text",
      count: withCounts ? "23" : null,
      actions: withActions
        ? [
            { name: "Hide Action", iconName: "x-close" },
            { name: "Lock Action", iconName: "lock-01" },
          ]
        : [],
      depth: 0,
      expandable: true,
      expanded,
    },
  ];

  if (expanded) {
    rows.push(
      {
        key: "child",
        textName: "Child Text",
        text: "Content group",
        countName: "Child Count Text",
        count: withCounts ? "164" : null,
        actions: withActions
          ? [{ name: "Lock Action", iconName: "lock-01" }]
          : [],
        depth: 1,
        expandable: true,
        expanded: true,
      },
      {
        key: "leaf",
        textName: "Leaf Text",
        text: "Place item",
        countName: "Leaf Count Text",
        count: withCounts ? "4" : null,
        actions: withActions
          ? [{ name: "Edit Action", iconName: "edit-01" }]
          : [],
        depth: 2,
        expandable: false,
        expanded: false,
      },
    );
  }

  rows.push({
    key: "sibling",
    textName: "Sibling Text",
    text: "Settings",
    countName: "Sibling Count Text",
    count: withCounts ? "12" : null,
    actions: withActions ? [{ name: "Edit Action", iconName: "edit-01" }] : [],
    iconName: "settings-01",
    depth: 0,
    expandable: false,
    expanded: false,
  });

  for (const row of rows) {
    row.selected = state === "Selected" && row.key === activeKey;
    row.focused = state === "Focus" && row.key === activeKey;
    row.hovered = state === "Hover" && row.key === activeKey;
    row.state = state;
  }

  return rows;
}

async function createTreeRow({
  row,
  props,
  metrics,
  variableByName,
  fonts,
  stats,
}) {
  const rowNode = figma.createFrame();
  const result = await syncTreeRowNode(rowNode, {
    row,
    props,
    metrics,
    variableByName,
    fonts,
    stats,
    componentName: "Tree",
  });
  return { rowNode, iconNodes: result.iconNodes };
}

async function syncTreeRowNode(
  rowNode,
  {
    row,
    props,
    metrics,
    variableByName,
    fonts,
    stats,
    componentName,
    asComponentVariant = false,
    includeTypeAxis = true,
  },
) {
  const type = normalizeTreeItemType(
    row.type || (row.expandable ? "Parent" : "Child"),
  );
  const expandedValue = row.expanded ? "True" : "False";
  const rowState = normalizeTreeState(row.state || props.state);
  const highlighted = row.selected || row.hovered;
  const includeExpandedAxis = includeTypeAxis || type === "Parent";
  const variantNameParts = [
    includeTypeAxis ? `Type=${type}` : null,
    `Content=${props.content || "Basic"}`,
    includeExpandedAxis ? `Expanded=${expandedValue}` : null,
    `Depth=${String(row.depth)}`,
    `Density=${props.density}`,
    `State=${rowState}`,
  ].filter(Boolean);

  rowNode.name = asComponentVariant
    ? variantNameParts.join(", ")
    : `Tree Item / ${row.text}`;
  rowNode.layoutMode = "HORIZONTAL";
  rowNode.primaryAxisSizingMode = "FIXED";
  rowNode.counterAxisSizingMode = "FIXED";
  rowNode.primaryAxisAlignItems = "MIN";
  rowNode.counterAxisAlignItems = "CENTER";
  rowNode.itemSpacing = metrics.gap;
  rowNode.paddingLeft = metrics.paddingX + row.depth * metrics.indent;
  rowNode.paddingRight = metrics.paddingX;
  rowNode.paddingTop = 0;
  rowNode.paddingBottom = 0;
  rowNode.resizeWithoutConstraints(metrics.width, metrics.rowHeight);
  rowNode.cornerRadius = 8;
  rowNode.clipsContent = false;
  rowNode.fills = highlighted
    ? [
        paintFromVariable(
          "Colors/background/100",
          "#E3E4E8",
          variableByName,
          stats,
        ),
      ]
    : [];
  rowNode.strokes = [];
  rowNode.setSharedPluginData(
    RUN_NAMESPACE,
    "kind",
    asComponentVariant ? "component-variant" : "tree-item",
  );
  rowNode.setSharedPluginData(RUN_NAMESPACE, "component", componentName);
  rowNode.setSharedPluginData(RUN_NAMESPACE, "state", rowState);

  removeDirectChildren(rowNode);

  const iconNodes = [];
  const chevron = await createTreeChevron(row, variableByName, stats);
  rowNode.appendChild(chevron);
  setFixedChildSizing(chevron);
  iconNodes.push(chevron);

  const typeIcon = await createFixedIconInstance(
    row.iconName || (row.expandable ? "map-01" : "marker-pin-01"),
    row.selected ? "Colors/theme/600" : "Colors/foreground/400",
    row.selected ? "#1051E8" : "#5D626F",
    variableByName,
    stats,
    metrics.iconSize,
    "Tree/icon/size",
  );
  typeIcon.name = "Leading Icon";
  rowNode.appendChild(typeIcon);
  setFixedChildSizing(typeIcon);
  iconNodes.push(typeIcon);

  const labelGroup = figma.createFrame();
  labelGroup.name = "Tree Item Label";
  labelGroup.layoutMode = "HORIZONTAL";
  labelGroup.primaryAxisSizingMode = "AUTO";
  labelGroup.counterAxisSizingMode = "AUTO";
  labelGroup.primaryAxisAlignItems = "MIN";
  labelGroup.counterAxisAlignItems = "CENTER";
  labelGroup.itemSpacing = 6;
  labelGroup.paddingLeft = 0;
  labelGroup.paddingRight = 0;
  labelGroup.paddingTop = 0;
  labelGroup.paddingBottom = 0;
  labelGroup.resizeWithoutConstraints(120, metrics.rowHeight);
  labelGroup.cornerRadius = 0;
  labelGroup.clipsContent = false;
  labelGroup.fills = [];
  labelGroup.strokes = [];

  const text = figma.createText();
  text.name = row.textName;
  await applyTreeItemTypography(text, fonts, variableByName, stats);
  text.characters = row.text;
  text.fills = [
    paintFromVariable(
      row.selected ? "Colors/theme/600" : "Colors/foreground/0",
      row.selected ? "#1051E8" : "#111111",
      variableByName,
      stats,
    ),
  ];
  text.textAlignVertical = "CENTER";
  setTextAutoResize(text, "TRUNCATE");
  text.resizeWithoutConstraints(treeLabelTextWidth(row, metrics), 20);
  labelGroup.appendChild(text);
  setLayoutSizingHorizontal(text, "FIXED");
  setLayoutSizingVertical(text, "HUG");

  if (row.count) {
    const count = await createTreeCount({
      name: row.countName,
      value: row.count,
      variableByName,
      fonts,
      stats,
    });
    labelGroup.appendChild(count);
    setHugChildSizing(count);
  }

  rowNode.appendChild(labelGroup);
  setHugChildSizing(labelGroup);

  const spacer = createTreeItemSpacer(metrics);
  rowNode.appendChild(spacer);
  setHorizontalStackFillHugChildSizing(spacer);

  if (row.actions && row.actions.length > 0 && shouldShowTreeActions(row)) {
    const actions = await createTreeActions({
      actions: row.actions,
      metrics,
      variableByName,
      stats,
    });
    rowNode.appendChild(actions.node);
    setHugChildSizing(actions.node);
    for (const icon of actions.iconNodes) iconNodes.push(icon);
  }

  syncFocusRing(rowNode, {
    enabled: row.focused,
    width: metrics.width,
    height: metrics.rowHeight,
    radius: 8,
    variableName: "Colors/theme/500",
    fallback: "#135BEC",
    variableByName,
    stats,
  });

  const focusRing = directChildNamed(rowNode, "Focus Ring");
  if (row.focused && focusRing) {
    focusRing.visible = true;
  }

  return { iconNodes };
}

function shouldShowTreeActions(row) {
  return Boolean(row.selected || row.hovered || row.focused);
}

function treeLabelTextWidth(row, metrics) {
  const rowPaddingLeft = metrics.paddingX + row.depth * metrics.indent;
  const rowPaddingRight = metrics.paddingX;
  const iconWidth = 16 + metrics.iconSize;
  const actionsVisible = shouldShowTreeActions(row);
  const actionsWidth =
    actionsVisible && row.actions && row.actions.length > 0
      ? row.actions.length * metrics.actionSize +
        Math.max(0, row.actions.length - 1) * 2
      : 0;
  const directChildGapCount =
    actionsVisible && row.actions && row.actions.length > 0 ? 4 : 3;
  const countMetrics = counterMetrics("Small");
  const countWidth = row.count
    ? Math.max(
        countMetrics.minWidth,
        String(row.count).length * 7 + countMetrics.paddingX * 2,
      ) + 6
    : 0;
  const availableWidth =
    metrics.width -
    rowPaddingLeft -
    rowPaddingRight -
    iconWidth -
    actionsWidth -
    directChildGapCount * metrics.gap -
    countWidth;
  const naturalWidth = String(row.text).length * 7.25 + 2;

  return Math.max(40, Math.min(Math.floor(availableWidth), naturalWidth));
}

function createTreeItemSpacer(metrics) {
  const spacer = figma.createFrame();
  spacer.name = "Tree Item Spacer";
  spacer.layoutMode = "HORIZONTAL";
  spacer.primaryAxisSizingMode = "AUTO";
  spacer.counterAxisSizingMode = "FIXED";
  spacer.resizeWithoutConstraints(1, metrics.rowHeight);
  spacer.fills = [];
  spacer.strokes = [];
  spacer.clipsContent = false;
  spacer.setSharedPluginData(RUN_NAMESPACE, "kind", "tree-item-spacer");
  return spacer;
}

async function createTreeCount({ name, value, variableByName, fonts, stats }) {
  const metrics = counterMetrics("Small");
  const count = figma.createFrame();
  count.name = "Count";
  count.layoutMode = "HORIZONTAL";
  count.primaryAxisSizingMode = "AUTO";
  count.counterAxisSizingMode = "FIXED";
  count.primaryAxisAlignItems = "CENTER";
  count.counterAxisAlignItems = "CENTER";
  count.paddingLeft = metrics.paddingX;
  count.paddingRight = metrics.paddingX;
  count.paddingTop = 0;
  count.paddingBottom = 0;
  count.itemSpacing = 0;
  count.resizeWithoutConstraints(metrics.minWidth, metrics.height);
  count.cornerRadius = 9999;
  count.clipsContent = false;
  count.fills = [
    paintFromVariable(
      "Colors/background/100",
      "#E3E4E8",
      variableByName,
      stats,
    ),
  ];
  count.strokes = [
    paintFromVariable(
      "Colors/foreground/300",
      "#A9AFBB",
      variableByName,
      stats,
    ),
  ];
  count.strokeWeight = 1;
  count.setSharedPluginData(RUN_NAMESPACE, "kind", "tree-count");

  const label = figma.createText();
  label.name = name;
  await applyCounterTypography(label, "Small", fonts, variableByName, stats);
  label.characters = value;
  label.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  label.textAlignVertical = "CENTER";
  count.appendChild(label);
  setHugChildSizing(label);

  return count;
}

async function createTreeActions({ actions, metrics, variableByName, stats }) {
  const group = figma.createFrame();
  group.name = "Tree Actions";
  group.layoutMode = "HORIZONTAL";
  group.primaryAxisSizingMode = "AUTO";
  group.counterAxisSizingMode = "FIXED";
  group.primaryAxisAlignItems = "CENTER";
  group.counterAxisAlignItems = "CENTER";
  group.itemSpacing = 2;
  group.paddingLeft = 0;
  group.paddingRight = 0;
  group.paddingTop = 0;
  group.paddingBottom = 0;
  group.resizeWithoutConstraints(
    actions.length * metrics.actionSize + Math.max(0, actions.length - 1) * 2,
    metrics.actionSize,
  );
  group.cornerRadius = 0;
  group.clipsContent = false;
  group.fills = [];
  group.strokes = [];
  group.setSharedPluginData(RUN_NAMESPACE, "kind", "tree-actions");

  const iconNodes = [];
  for (let index = 0; index < actions.length; index += 1) {
    const action = actions[index];
    const button = figma.createFrame();
    button.name = action.name;
    button.layoutMode = "HORIZONTAL";
    button.primaryAxisSizingMode = "FIXED";
    button.counterAxisSizingMode = "FIXED";
    button.primaryAxisAlignItems = "CENTER";
    button.counterAxisAlignItems = "CENTER";
    button.resizeWithoutConstraints(metrics.actionSize, metrics.actionSize);
    button.cornerRadius = 6;
    button.clipsContent = false;
    button.fills = [];
    button.strokes = [];
    button.setSharedPluginData(RUN_NAMESPACE, "kind", "tree-action-button");

    const icon = await createFixedIconInstance(
      action.iconName,
      "Colors/foreground/400",
      "#5D626F",
      variableByName,
      stats,
      metrics.actionIconSize,
      "Tree/icon/size",
    );
    icon.name = `Action ${index + 1} Icon`;
    button.appendChild(icon);
    setFixedChildSizing(icon);
    iconNodes.push(icon);

    group.appendChild(button);
    setFixedChildSizing(button);
  }

  return { node: group, iconNodes };
}

async function createTreeChevron(row, variableByName, stats) {
  if (!row.expandable) {
    const spacer = figma.createFrame();
    spacer.name = "Disclosure Spacer";
    spacer.layoutMode = "NONE";
    spacer.resizeWithoutConstraints(16, 16);
    spacer.fills = [];
    spacer.strokes = [];
    spacer.setSharedPluginData(RUN_NAMESPACE, "kind", "tree-icon-spacer");
    return spacer;
  }

  const chevron = await createFixedIconInstance(
    row.expanded ? "chevron-down" : "chevron-right",
    row.selected ? "Colors/theme/600" : "Colors/foreground/400",
    row.selected ? "#1051E8" : "#5D626F",
    variableByName,
    stats,
    16,
    "Tree/icon/size",
  );
  chevron.name = row.expanded
    ? "Disclosure Icon / expanded"
    : "Disclosure Icon";
  chevron.setSharedPluginData(RUN_NAMESPACE, "kind", "tree-disclosure-icon");
  return chevron;
}

async function createTimelineVariant({ props, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateTimelineVariant(component, {
    props,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

async function updateTimelineVariant(
  component,
  { props, variableByName, fonts, stats },
) {
  const metrics = timelineMetrics(props);

  component.name = `Content=${props.content}, Density=${props.density}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 0;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(metrics.width, metrics.height);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Timeline");

  removeDirectChildren(component);

  for (let index = 0; index < 3; index += 1) {
    const item = await createTimelineItem({
      index,
      isLast: index === 2,
      props,
      metrics,
      variableByName,
      fonts,
      stats,
    });
    component.appendChild(item);
    setVerticalStackChildSizing(item);
  }
}

async function createScrollAreaVariant({ props, variableByName, stats }) {
  const component = figma.createComponent();
  await updateScrollAreaVariant(component, {
    props,
    variableByName,
    stats,
  });
  return component;
}

async function updateScrollAreaVariant(
  component,
  { props, variableByName, stats },
) {
  const metrics = scrollAreaMetrics(props.orientation);
  const showScrollbar = props.scrollbar === "Visible";
  const hasHorizontalScrollbar =
    showScrollbar && props.orientation !== "Vertical";
  const paddingLeft = 12;
  const paddingRight = 12;
  const paddingTop = 12;
  const paddingBottom = hasHorizontalScrollbar ? 32 : 12;
  const viewportWidth = metrics.width - paddingLeft - paddingRight;
  const viewportHeight = metrics.height - paddingTop - paddingBottom;

  component.name = `Orientation=${props.orientation}, Scrollbar=${props.scrollbar}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 0;
  component.paddingLeft = paddingLeft;
  component.paddingRight = paddingRight;
  component.paddingTop = paddingTop;
  component.paddingBottom = paddingBottom;
  component.resizeWithoutConstraints(metrics.width, metrics.height);
  component.cornerRadius = 8;
  component.clipsContent = false;
  component.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  component.strokes = [
    paintFromVariable(
      "Colors/background/200",
      "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "ScrollArea");

  removeDirectChildren(component);

  const viewport = figma.createFrame();
  viewport.name = "ScrollArea Viewport";
  viewport.layoutMode = "VERTICAL";
  viewport.primaryAxisSizingMode = "FIXED";
  viewport.counterAxisSizingMode = "FIXED";
  viewport.primaryAxisAlignItems = "MIN";
  viewport.counterAxisAlignItems = "MIN";
  viewport.itemSpacing = 12;
  viewport.paddingLeft = 0;
  viewport.paddingRight = 0;
  viewport.paddingTop = 0;
  viewport.paddingBottom = 0;
  viewport.resizeWithoutConstraints(viewportWidth, viewportHeight);
  viewport.cornerRadius = 6;
  viewport.clipsContent = true;
  viewport.fills = [];
  viewport.strokes = [];
  viewport.setSharedPluginData(RUN_NAMESPACE, "kind", "scrollarea-viewport");
  component.appendChild(viewport);
  setLayoutSizingHorizontal(viewport, "FILL");
  setLayoutSizingVertical(viewport, "FILL");
  try {
    viewport.layoutAlign = "STRETCH";
    viewport.layoutGrow = 1;
  } catch (_error) {
    // Older Figma runtimes may reject fill sizing on frame nodes.
  }

  const contentSlot = appendComponentContentSlot({
    component,
    height: viewportHeight,
    itemSpacing: 12,
    layoutMode: "VERTICAL",
    ownerName: "ScrollArea",
    parent: viewport,
    slotName: "Content Slot",
    stats,
    width: viewportWidth,
  });
  contentSlot.x = 0;
  contentSlot.y = 0;
  setLayoutSizingHorizontal(contentSlot, "FILL");
  setLayoutSizingVertical(contentSlot, "FILL");
  try {
    contentSlot.layoutPositioning = "AUTO";
    contentSlot.layoutAlign = "STRETCH";
    contentSlot.layoutGrow = 1;
    contentSlot.constraints = { horizontal: "STRETCH", vertical: "STRETCH" };
  } catch (_error) {
    // Older Figma runtimes may reject fill sizing on slot nodes.
  }

  if (showScrollbar && props.orientation !== "Horizontal") {
    appendScrollAreaScrollbar({
      component,
      name: "Vertical Scrollbar",
      vertical: true,
      metrics,
      variableByName,
      stats,
    });
  }

  if (showScrollbar && props.orientation !== "Vertical") {
    appendScrollAreaScrollbar({
      component,
      name: "Horizontal Scrollbar",
      vertical: false,
      metrics,
      variableByName,
      stats,
    });
  }
}

async function createBottomNavigationVariant({
  props,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateBottomNavigationVariant(component, {
    props,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

async function updateBottomNavigationVariant(
  component,
  { props, variableByName, fonts, stats },
) {
  const itemCount = bottomNavigationCountToNumber(props.count);
  const activeIndex = BOTTOM_NAVIGATION_ACTIVE.indexOf(props.active);
  const width = 360;
  const height = 64;

  component.name = `Count=${props.count}, Active=${props.active}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 0;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(width, height);
  component.cornerRadius = 16;
  component.clipsContent = false;
  component.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  component.strokes = [
    paintFromVariable(
      "Colors/background/200",
      "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.effects = [
    {
      type: "DROP_SHADOW",
      color: { r: 0, g: 0, b: 0, a: 0.08 },
      offset: { x: 0, y: -1 },
      radius: 8,
      spread: 0,
      visible: true,
      blendMode: "NORMAL",
    },
  ];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "BottomNavigation");

  removeDirectChildren(component);

  for (let index = 0; index < itemCount; index += 1) {
    const item = await createBottomNavigationItem({
      index,
      itemCount,
      active: index === activeIndex,
      width,
      variableByName,
      fonts,
      stats,
    });
    component.appendChild(item);
  }
}

async function createNavigationItemVariant({
  props,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateNavigationItemVariant(component, {
    props,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

async function updateNavigationItemVariant(
  component,
  { props, variableByName, fonts, stats },
) {
  const metrics = navigationItemMetrics(props);
  const rail = props.placement === "Rail";
  const iconOnly = props.content === "Icon Only";
  const hasIcon = props.content !== "Label";
  const hasBadge = props.content === "Badge";
  const hasTrailing = props.content === "Trailing";
  const selected = props.state === "Selected";
  const disabled = props.state === "Disabled";
  const hover = props.state === "Hover";

  component.name = `Placement=${props.placement}, Content=${props.content}, Density=${props.density}, State=${props.state}`;
  component.layoutMode = rail ? "VERTICAL" : "HORIZONTAL";
  component.primaryAxisSizingMode =
    props.placement === "Top" ? "AUTO" : "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = rail ? "CENTER" : "MIN";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = metrics.gap;
  component.paddingLeft = metrics.paddingX;
  component.paddingRight = metrics.paddingX;
  component.paddingTop = metrics.paddingY;
  component.paddingBottom = metrics.paddingY;
  component.resizeWithoutConstraints(metrics.width, metrics.height);
  component.cornerRadius = 8;
  component.clipsContent = false;
  component.opacity = 1;
  component.fills =
    selected || hover
      ? [
          paintFromVariable(
            "Colors/background/100",
            hover ? "#F7F8FA" : "#F1F2F4",
            variableByName,
            stats,
          ),
        ]
      : [];
  component.strokes = [];
  component.strokeWeight = 0;
  component.effects = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "NavigationItem");

  bindNavigationItemGeometryVariables(component, props, variableByName, stats);
  removeDirectChildren(component);

  if (hasIcon) {
    const iconSlot = await createNavigationItemLeadingIconSlot({
      active: selected,
      disabled,
      rail,
      variableByName,
      stats,
    });
    component.appendChild(iconSlot);
    setHugChildSizing(iconSlot);
  }

  if (!iconOnly) {
    const label = figma.createText();
    label.name = "Label Text";
    await applyNavigationItemTypography(
      label,
      fonts,
      props.placement,
      variableByName,
      stats,
    );
    label.characters = navigationItemDefaultLabel(props);
    label.fills = [
      paintFromVariable(
        navigationItemTextColorToken(props),
        navigationItemTextColorFallback(props),
        variableByName,
        stats,
      ),
    ];
    label.textAlignHorizontal = rail ? "CENTER" : "LEFT";
    label.textAlignVertical = "CENTER";
    label.resizeWithoutConstraints(metrics.labelWidth, rail ? 16 : 20);
    setTextAutoResize(label, "TRUNCATE");
    component.appendChild(label);
    if (rail || props.placement === "Top") {
      setHugChildSizing(label);
    } else {
      setHorizontalStackFillHugChildSizing(label);
    }
  }

  if (hasBadge) {
    const badgeSlot = await createNavigationItemBadgeSlot({
      variableByName,
      fonts,
      stats,
    });
    component.appendChild(badgeSlot);
    setHugChildSizing(badgeSlot);
  }

  if (hasTrailing) {
    const trailingSlot = await createNavigationItemTrailingSlot({
      variableByName,
      stats,
    });
    component.appendChild(trailingSlot);
    setHugChildSizing(trailingSlot);
  }

  syncFocusRing(component, {
    enabled: !disabled,
    width: metrics.width,
    height: metrics.height,
    radius: 8,
    variableName: "Colors/theme/500",
    fallback: "#135BEC",
    variableByName,
    stats,
  });
}

function navigationItemMetrics(props) {
  const rail = props.placement === "Rail";
  const top = props.placement === "Top";
  const compact = props.density === "Compact";
  const height = rail ? 72 : 44;
  const paddingX = rail ? 8 : compact ? 10 : 12;
  const paddingY = rail ? 8 : 0;
  const gap = rail ? 4 : compact ? 6 : 8;
  let width = top ? 112 : 248;
  if (rail) width = 72;
  if (top && props.content === "Icon Only") width = 44;
  if (top && props.content === "Badge") width = 132;
  if (top && props.content === "Trailing") width = 148;
  if (top && props.content === "Icon Label") width = 128;
  const labelWidth = rail ? 56 : top ? Math.max(56, width - 40) : 176;

  return {
    gap,
    height,
    labelWidth,
    paddingX,
    paddingY,
    width,
  };
}

function navigationItemDefaultLabel(props) {
  if (props.placement === "Rail") return "Search";
  if (props.content === "Badge") return "Updates";
  if (props.content === "Trailing") return "Settings";
  return "Overview";
}

function navigationItemTextColorToken(props) {
  if (props.state === "Disabled") return "Colors/foreground/400";
  if (props.state === "Selected") return "Colors/theme/700";
  return "Colors/foreground/0";
}

function navigationItemTextColorFallback(props) {
  if (props.state === "Disabled") return "#5E6575";
  if (props.state === "Selected") return "#0B3E9D";
  return "#000000";
}

function bindNavigationItemGeometryVariables(
  component,
  props,
  variableByName,
  stats,
) {
  if (props.placement === "Side") {
    bindFloatVariable(
      component,
      "width",
      "NavigationItem/width/side",
      variableByName,
      stats,
    );
  }
  if (props.placement === "Rail") {
    bindFloatVariable(
      component,
      "width",
      "NavigationItem/width/rail",
      variableByName,
      stats,
    );
    bindFloatVariable(
      component,
      "height",
      "NavigationItem/height/rail",
      variableByName,
      stats,
    );
  } else {
    bindFloatVariable(
      component,
      "height",
      props.density === "Compact"
        ? "NavigationItem/height/compact"
        : "NavigationItem/height/default",
      variableByName,
      stats,
    );
  }
  bindFloatVariable(
    component,
    "cornerRadius",
    "NavigationItem/radius",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    "NavigationItem/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    "NavigationItem/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "NavigationItem/gap",
    variableByName,
    stats,
  );
}

async function createNavigationItemLeadingIconSlot({
  active,
  disabled,
  rail,
  variableByName,
  stats,
}) {
  const slot = createSampleSlotFrame({
    name: "Leading Icon Slot",
    width: rail ? 24 : 20,
    height: rail ? 24 : 20,
    layoutMode: "HORIZONTAL",
    itemSpacing: 0,
  });
  slot.primaryAxisAlignItems = "CENTER";
  slot.counterAxisAlignItems = "CENTER";

  const icon = await createFixedIconInstance(
    "home-line",
    active
      ? "Colors/theme/500"
      : disabled
        ? "Colors/foreground/400"
        : "Colors/foreground/400",
    active ? "#135BEC" : "#5E6575",
    variableByName,
    stats,
    rail ? 20 : 18,
  );
  icon.name = "Leading Icon";
  slot.appendChild(icon);
  setHugChildSizing(icon);
  return slot;
}

async function createNavigationItemBadgeSlot({ variableByName, fonts, stats }) {
  const slot = createSampleSlotFrame({
    name: "Badge Slot",
    width: 24,
    height: 20,
    layoutMode: "HORIZONTAL",
    itemSpacing: 0,
  });
  slot.primaryAxisAlignItems = "CENTER";
  slot.counterAxisAlignItems = "CENTER";
  slot.paddingLeft = 6;
  slot.paddingRight = 6;
  slot.cornerRadius = 10;
  slot.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  slot.strokes = [
    paintFromVariable(
      "Colors/background/200",
      "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  slot.strokeWeight = 1;

  const text = figma.createText();
  text.name = "Badge Text";
  await applySidebarSectionTypography(text, fonts, variableByName, stats);
  text.characters = "3";
  text.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  text.textAlignHorizontal = "CENTER";
  text.textAlignVertical = "CENTER";
  setTextAutoResize(text, "WIDTH_AND_HEIGHT");
  slot.appendChild(text);
  setHugChildSizing(text);
  return slot;
}

async function createNavigationItemTrailingSlot({ variableByName, stats }) {
  const slot = createSampleSlotFrame({
    name: "Trailing Slot",
    width: 20,
    height: 20,
    layoutMode: "HORIZONTAL",
    itemSpacing: 0,
  });
  slot.primaryAxisAlignItems = "CENTER";
  slot.counterAxisAlignItems = "CENTER";

  const icon = await createFixedIconInstance(
    "chevron-right",
    "Colors/foreground/400",
    "#5D626F",
    variableByName,
    stats,
    16,
  );
  icon.name = "Trailing Icon";
  slot.appendChild(icon);
  setHugChildSizing(icon);
  return slot;
}

async function createNavbarVariant({ props, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateNavbarVariant(component, {
    props,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

async function updateNavbarVariant(
  component,
  { props, variableByName, fonts, stats },
) {
  const hasActions = props.content === "Actions";
  const isContextual = props.content === "Contextual";
  component.name = `Content=${props.content}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = isContextual ? 10 : 16;
  component.paddingLeft = 16;
  component.paddingRight = 16;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(
    NAVBAR_DEFAULT_WIDTH,
    NAVBAR_DEFAULT_HEIGHT,
  );
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  component.strokes = [
    paintFromVariable(
      "Colors/background/200",
      "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Navbar");

  bindSizeVariables(
    component,
    "Navbar/width/default",
    "Navbar/height/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    "Navbar/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    "Navbar/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    "Navbar/radius",
    variableByName,
    stats,
  );

  const reusableSlots = extractReusableSlotsByName(
    component,
    requiredNavbarSlotNamesForContent(props.content),
    stats,
    "Navbar",
  );
  removeDirectChildrenExceptMany(component, Object.values(reusableSlots));

  const logoSlot = await createNavbarLogoSlot({
    component,
    compact: isContextual,
    reusableSlot: reusableSlotNamed(reusableSlots, "Logo Slot"),
    variableByName,
    fonts,
    stats,
  });
  component.appendChild(logoSlot);
  setHugChildSizing(logoSlot);

  if (isContextual) {
    const contextSlot = await createNavbarContextSlot({
      component,
      reusableSlot: reusableSlotNamed(reusableSlots, "Context Slot"),
      variableByName,
      fonts,
      stats,
    });
    component.appendChild(contextSlot);
    setFixedChildSizing(contextSlot);

    const primaryActionSlot = await createNavbarPrimaryActionSlot({
      component,
      reusableSlot: reusableSlotNamed(reusableSlots, "Primary Action Slot"),
      variableByName,
      fonts,
      stats,
    });
    component.appendChild(primaryActionSlot);
    setHugChildSizing(primaryActionSlot);
  }

  const navigationSlot = await createNavbarNavigationSlot({
    component,
    content: props.content,
    reusableSlot: reusableSlotNamed(reusableSlots, "Navigation Slot"),
    variableByName,
    fonts,
    stats,
  });
  component.appendChild(navigationSlot);
  setHorizontalStackFillHugChildSizing(navigationSlot);

  if (hasActions) {
    const actionsSlot = await createNavbarActionsSlot({
      component,
      reusableSlot: reusableSlotNamed(reusableSlots, "Actions Slot"),
      variableByName,
      fonts,
      stats,
    });
    component.appendChild(actionsSlot);
    setHugChildSizing(actionsSlot);
  }

  if (isContextual) {
    const utilitySlot = await createNavbarUtilitySlot({
      component,
      reusableSlot: reusableSlotNamed(reusableSlots, "Utility Slot"),
      variableByName,
      stats,
    });
    component.appendChild(utilitySlot);
    setHugChildSizing(utilitySlot);

    const accountSlot = await createNavbarAccountSlot({
      component,
      reusableSlot: reusableSlotNamed(reusableSlots, "Account Slot"),
      variableByName,
      stats,
    });
    component.appendChild(accountSlot);
    setHugChildSizing(accountSlot);
  }
}

async function createNavbarLogoSlot({
  component,
  compact = false,
  reusableSlot,
  variableByName,
  fonts,
  stats,
}) {
  const shell = createOrReuseShellSlot({
    component,
    reusableSlot,
    ownerName: "Navbar",
    stats,
    slotName: "Logo Slot",
    width: compact ? 40 : 132,
    height: 40,
    layoutMode: "HORIZONTAL",
    itemSpacing: 8,
  });
  const slot = shell.node;
  if (shell.reusedContent) return slot;

  const mark = createBrandMark(variableByName, stats, 32);
  slot.appendChild(mark);
  setHugChildSizing(mark);

  if (compact) {
    return slot;
  }

  const label = figma.createText();
  label.name = "Logo Text";
  await applyNavbarLogoTypography(label, fonts, variableByName, stats);
  label.characters = "Kozmos";
  label.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  label.textAlignVertical = "CENTER";
  label.resizeWithoutConstraints(84, 20);
  setTextAutoResize(label, "TRUNCATE");
  slot.appendChild(label);
  setHugChildSizing(label);

  return slot;
}

async function createNavbarContextSlot({
  component,
  reusableSlot,
  variableByName,
  fonts,
  stats,
}) {
  const shell = createOrReuseShellSlot({
    component,
    reusableSlot,
    ownerName: "Navbar",
    stats,
    slotName: "Context Slot",
    width: NAVBAR_CONTEXT_SLOT_WIDTH,
    height: NAVBAR_CONTEXT_SLOT_HEIGHT,
    layoutMode: "VERTICAL",
    itemSpacing: 2,
  });
  const slot = shell.node;
  if (shell.reusedContent) return slot;

  const label = figma.createText();
  label.name = "Context Label Text";
  await applySidebarSectionTypography(label, fonts, variableByName, stats);
  label.characters = "Context";
  label.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  label.textAlignVertical = "CENTER";
  label.resizeWithoutConstraints(NAVBAR_CONTEXT_SLOT_WIDTH, 16);
  setTextAutoResize(label, "TRUNCATE");
  slot.appendChild(label);
  setVerticalStackChildSizing(label);

  const row = figma.createFrame();
  row.name = "Context Selector";
  row.layoutMode = "HORIZONTAL";
  row.primaryAxisSizingMode = "FIXED";
  row.counterAxisSizingMode = "AUTO";
  row.primaryAxisAlignItems = "MIN";
  row.counterAxisAlignItems = "CENTER";
  row.itemSpacing = 6;
  row.paddingLeft = 0;
  row.paddingRight = 0;
  row.paddingTop = 0;
  row.paddingBottom = 0;
  row.resizeWithoutConstraints(NAVBAR_CONTEXT_SLOT_WIDTH, 22);
  row.fills = [];
  row.strokes = [];
  row.clipsContent = false;

  const contextText = figma.createText();
  contextText.name = "Context Text";
  await applyNavbarItemTypography(contextText, fonts, variableByName, stats);
  contextText.characters = "Workspace";
  contextText.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  contextText.textAlignVertical = "CENTER";
  contextText.resizeWithoutConstraints(NAVBAR_CONTEXT_TEXT_WIDTH, 20);
  setTextAutoResize(contextText, "TRUNCATE");
  row.appendChild(contextText);
  setHorizontalStackFillHugChildSizing(contextText);

  const chevron = await createFixedIconInstance(
    "chevron-down",
    "Colors/foreground/400",
    "#5D626F",
    variableByName,
    stats,
    16,
  );
  chevron.name = "Context Chevron";
  row.appendChild(chevron);
  setHugChildSizing(chevron);

  slot.appendChild(row);
  setVerticalStackChildSizing(row);

  return slot;
}

async function createNavbarNavigationSlot({
  component,
  content,
  reusableSlot,
  variableByName,
  fonts,
  stats,
}) {
  const shell = createOrReuseShellSlot({
    component,
    reusableSlot,
    ownerName: "Navbar",
    stats,
    slotName: "Navigation Slot",
    width: content === "Contextual" ? 236 : 356,
    height: 40,
    layoutMode: "HORIZONTAL",
    itemSpacing: 4,
  });
  const slot = shell.node;
  if (shell.reusedContent) return slot;

  const items =
    content === "Contextual"
      ? [
          { name: "Item 1 Text", label: "Overview", active: true },
          { name: "Item 2 Text", label: "Explore", active: false },
          { name: "Item 3 Text", label: "Settings", active: false },
        ]
      : [
          { name: "Item 1 Text", label: "Overview", active: true },
          { name: "Item 2 Text", label: "Explore", active: false },
          { name: "Item 3 Text", label: "Settings", active: false },
        ];

  for (const item of items) {
    const navItem = await createNavbarNavigationItem({
      item,
      variableByName,
      fonts,
      stats,
    });
    slot.appendChild(navItem);
    setHugChildSizing(navItem);
  }

  return slot;
}

async function createNavbarPrimaryActionSlot({
  component,
  reusableSlot,
  variableByName,
  fonts,
  stats,
}) {
  const shell = createOrReuseShellSlot({
    component,
    reusableSlot,
    ownerName: "Navbar",
    stats,
    slotName: "Primary Action Slot",
    width: 92,
    height: 40,
    layoutMode: "HORIZONTAL",
    itemSpacing: 0,
  });
  const slot = shell.node;
  if (shell.reusedContent) return slot;

  const action = figma.createFrame();
  action.name = "Primary Action";
  action.layoutMode = "HORIZONTAL";
  action.primaryAxisSizingMode = "AUTO";
  action.counterAxisSizingMode = "FIXED";
  action.primaryAxisAlignItems = "CENTER";
  action.counterAxisAlignItems = "CENTER";
  action.itemSpacing = 0;
  action.paddingLeft = 14;
  action.paddingRight = 14;
  action.paddingTop = 0;
  action.paddingBottom = 0;
  action.resizeWithoutConstraints(92, 40);
  action.cornerRadius = 8;
  action.clipsContent = false;
  action.fills = [
    paintFromVariable("Colors/theme/600", "#1051E8", variableByName, stats),
  ];
  action.strokes = [];

  const label = figma.createText();
  label.name = "Primary Action Text";
  await applyNavbarActionTypography(label, fonts, variableByName, stats);
  label.characters = "Create";
  label.fills = [
    paintFromVariable(
      "Colors/foreground/1000",
      "#FFFFFF",
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";
  setTextAutoResize(label, "WIDTH_AND_HEIGHT");
  action.appendChild(label);
  setHugChildSizing(label);

  slot.appendChild(action);
  setHugChildSizing(action);
  return slot;
}

async function createNavbarNavigationItem({
  item,
  variableByName,
  fonts,
  stats,
}) {
  return createNavigationItemNestedInstance({
    content: "Label",
    density: "Default",
    fallbackName: `${item.name} Frame`,
    label: item.label,
    placement: "Top",
    role: "navbar-navigation-item",
    state: item.active ? "Selected" : "Default",
    textNodeName: item.name,
    variableByName,
    fonts,
    stats,
  });
}

async function createNavigationItemNestedInstance({
  content,
  density,
  fallbackName,
  iconName,
  label,
  placement,
  role,
  state,
  textNodeName,
  variableByName,
  fonts,
  stats,
}) {
  const created = await createNestedComponentInstance({
    componentSetName: "NavigationItem",
    variantProperties: {
      Placement: placement,
      Content: content,
      Density: density,
      State: state,
    },
    name: fallbackName,
    role,
    stats,
  });

  if (created) {
    const instance = created.instance;
    setInstanceTextProperty(
      instance,
      created.componentSet,
      "Label Text",
      label,
      stats,
    );
    if (content !== "Label" && iconName) {
      const iconComponent = await findKozmosIconSourceComponent(iconName);
      setInstanceSwapProperty(
        instance,
        created.componentSet,
        "Leading Icon",
        iconComponent ? iconComponent.id : null,
        stats,
      );
    }
    markNestedComponentInstance(instance, "NavigationItem", role);
    return instance;
  }

  const frame = figma.createFrame();
  frame.name = fallbackName;
  frame.layoutMode = placement === "Rail" ? "VERTICAL" : "HORIZONTAL";
  frame.primaryAxisSizingMode = placement === "Top" ? "AUTO" : "FIXED";
  frame.counterAxisSizingMode = "FIXED";
  frame.primaryAxisAlignItems = placement === "Rail" ? "CENTER" : "MIN";
  frame.counterAxisAlignItems = "CENTER";
  frame.itemSpacing = placement === "Rail" ? 4 : 8;
  frame.paddingLeft = placement === "Rail" ? 8 : 12;
  frame.paddingRight = placement === "Rail" ? 8 : 12;
  frame.paddingTop = placement === "Rail" ? 8 : 0;
  frame.paddingBottom = placement === "Rail" ? 8 : 0;
  frame.resizeWithoutConstraints(
    placement === "Rail" ? 72 : 112,
    placement === "Rail" ? 72 : 44,
  );
  frame.cornerRadius = 8;
  frame.clipsContent = false;
  frame.fills =
    state === "Selected"
      ? [
          paintFromVariable(
            "Colors/background/100",
            "#E3E4E8",
            variableByName,
            stats,
          ),
        ]
      : [];
  frame.strokes = [];
  frame.setSharedPluginData(RUN_NAMESPACE, "kind", "missing-nested-component");

  if (content !== "Label") {
    const icon = await createFixedIconInstance(
      iconName || "home-line",
      state === "Selected" ? "Colors/theme/500" : "Colors/foreground/400",
      state === "Selected" ? "#135BEC" : "#5E6575",
      variableByName,
      stats,
      placement === "Rail" ? 20 : 18,
    );
    icon.name = `${textNodeName} Icon`;
    frame.appendChild(icon);
    setHugChildSizing(icon);
  }

  if (content !== "Icon Only") {
    const text = figma.createText();
    text.name = textNodeName;
    await applyNavigationItemTypography(
      text,
      fonts,
      placement,
      variableByName,
      stats,
    );
    text.characters = label;
    text.fills = [
      paintFromVariable(
        state === "Selected" ? "Colors/theme/700" : "Colors/foreground/0",
        state === "Selected" ? "#0B3E9D" : "#000000",
        variableByName,
        stats,
      ),
    ];
    text.textAlignHorizontal = placement === "Rail" ? "CENTER" : "LEFT";
    text.textAlignVertical = "CENTER";
    text.resizeWithoutConstraints(placement === "Rail" ? 56 : 80, 20);
    setTextAutoResize(text, "TRUNCATE");
    frame.appendChild(text);
    if (placement === "Side") {
      setHorizontalStackFillHugChildSizing(text);
    } else {
      setHugChildSizing(text);
    }
  }

  if (stats && stats.warnings) {
    stats.warnings.push(
      `${fallbackName}: build NavigationItem before updating Navbar or Sidebar to use live nested navigation rows.`,
    );
  }
  return frame;
}

async function createNavbarActionsSlot({
  component,
  reusableSlot,
  variableByName,
  fonts,
  stats,
}) {
  const shell = createOrReuseShellSlot({
    component,
    reusableSlot,
    ownerName: "Navbar",
    stats,
    slotName: "Actions Slot",
    width: 148,
    height: 40,
    layoutMode: "HORIZONTAL",
    itemSpacing: 8,
  });
  const slot = shell.node;
  if (shell.reusedContent) return slot;

  const action = figma.createFrame();
  action.name = "Action";
  action.layoutMode = "HORIZONTAL";
  action.primaryAxisSizingMode = "AUTO";
  action.counterAxisSizingMode = "FIXED";
  action.primaryAxisAlignItems = "CENTER";
  action.counterAxisAlignItems = "CENTER";
  action.itemSpacing = 0;
  action.paddingLeft = 14;
  action.paddingRight = 14;
  action.paddingTop = 0;
  action.paddingBottom = 0;
  action.resizeWithoutConstraints(80, 40);
  action.cornerRadius = 8;
  action.clipsContent = false;
  action.fills = [];
  action.strokes = [
    paintFromVariable("Colors/theme/600", "#1051E8", variableByName, stats),
  ];
  action.strokeWeight = 1;

  const label = figma.createText();
  label.name = "Action Text";
  await applyNavbarActionTypography(label, fonts, variableByName, stats);
  label.characters = "Action";
  label.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  label.textAlignVertical = "CENTER";
  setTextAutoResize(label, "WIDTH_AND_HEIGHT");
  action.appendChild(label);
  setHugChildSizing(label);
  slot.appendChild(action);
  setHugChildSizing(action);

  const userIcon = await createFixedIconInstance(
    "user-01",
    "Colors/foreground/400",
    "#5D626F",
    variableByName,
    stats,
    20,
  );
  userIcon.name = "Account Icon";
  slot.appendChild(userIcon);
  setHugChildSizing(userIcon);

  return slot;
}

async function createNavbarUtilitySlot({
  component,
  reusableSlot,
  variableByName,
  stats,
}) {
  const shell = createOrReuseShellSlot({
    component,
    reusableSlot,
    ownerName: "Navbar",
    stats,
    slotName: "Utility Slot",
    width: 44,
    height: 40,
    layoutMode: "HORIZONTAL",
    itemSpacing: 0,
  });
  const slot = shell.node;
  if (shell.reusedContent) return slot;

  const button = figma.createFrame();
  button.name = "Utility Button";
  button.layoutMode = "HORIZONTAL";
  button.primaryAxisSizingMode = "FIXED";
  button.counterAxisSizingMode = "FIXED";
  button.primaryAxisAlignItems = "CENTER";
  button.counterAxisAlignItems = "CENTER";
  button.itemSpacing = 0;
  button.paddingLeft = 0;
  button.paddingRight = 0;
  button.paddingTop = 0;
  button.paddingBottom = 0;
  button.resizeWithoutConstraints(40, 40);
  button.cornerRadius = 8;
  button.clipsContent = false;
  button.fills = [];
  button.strokes = [];

  const icon = await createFixedIconInstance(
    "bell-01",
    "Colors/foreground/400",
    "#5D626F",
    variableByName,
    stats,
    20,
  );
  icon.name = "Utility Icon";
  button.appendChild(icon);
  setHugChildSizing(icon);

  slot.appendChild(button);
  setHugChildSizing(button);
  return slot;
}

async function createNavbarAccountSlot({
  component,
  reusableSlot,
  variableByName,
  stats,
}) {
  const shell = createOrReuseShellSlot({
    component,
    reusableSlot,
    ownerName: "Navbar",
    stats,
    slotName: "Account Slot",
    width: 64,
    height: 40,
    layoutMode: "HORIZONTAL",
    itemSpacing: 6,
  });
  const slot = shell.node;
  if (shell.reusedContent) return slot;

  const avatar = figma.createFrame();
  avatar.name = "Account Avatar";
  avatar.layoutMode = "HORIZONTAL";
  avatar.primaryAxisSizingMode = "FIXED";
  avatar.counterAxisSizingMode = "FIXED";
  avatar.primaryAxisAlignItems = "CENTER";
  avatar.counterAxisAlignItems = "CENTER";
  avatar.itemSpacing = 0;
  avatar.paddingLeft = 0;
  avatar.paddingRight = 0;
  avatar.paddingTop = 0;
  avatar.paddingBottom = 0;
  avatar.resizeWithoutConstraints(32, 32);
  avatar.cornerRadius = 16;
  avatar.clipsContent = false;
  avatar.fills = [
    paintFromVariable(
      "Colors/background/100",
      "#E3E4E8",
      variableByName,
      stats,
    ),
  ];
  avatar.strokes = [
    paintFromVariable(
      "Colors/background/300",
      "#AEB4C0",
      variableByName,
      stats,
    ),
  ];
  avatar.strokeWeight = 1;

  const userIcon = await createFixedIconInstance(
    "user-01",
    "Colors/foreground/400",
    "#5D626F",
    variableByName,
    stats,
    20,
  );
  userIcon.name = "Account Icon";
  avatar.appendChild(userIcon);
  setHugChildSizing(userIcon);
  slot.appendChild(avatar);
  setHugChildSizing(avatar);

  const chevron = await createFixedIconInstance(
    "chevron-down",
    "Colors/foreground/400",
    "#5D626F",
    variableByName,
    stats,
    14,
  );
  chevron.name = "Account Chevron";
  slot.appendChild(chevron);
  setHugChildSizing(chevron);

  return slot;
}

async function createSidebarVariant({ props, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateSidebarVariant(component, {
    props,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

async function updateSidebarVariant(
  component,
  { props, variableByName, fonts, stats },
) {
  const isRail = props.content === "Rail";
  const hasTools = props.content === "Tools";
  component.name = `Content=${props.content}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = isRail ? "CENTER" : "MIN";
  component.itemSpacing = isRail ? 12 : 16;
  component.paddingLeft = isRail ? 4 : 16;
  component.paddingRight = isRail ? 4 : 16;
  component.paddingTop = 16;
  component.paddingBottom = 16;
  component.resizeWithoutConstraints(
    isRail ? SIDEBAR_RAIL_WIDTH : SIDEBAR_DEFAULT_WIDTH,
    SIDEBAR_DEFAULT_HEIGHT,
  );
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  component.strokes = [
    paintFromVariable(
      "Colors/background/200",
      "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Sidebar");

  bindSizeVariables(
    component,
    isRail ? "Sidebar/width/rail" : "Sidebar/width/default",
    "Sidebar/height/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    isRail ? "Sidebar/padding/x/rail" : "Sidebar/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    isRail ? "Sidebar/padding/x/rail" : "Sidebar/padding/x",
    variableByName,
    stats,
  );

  const reusableSlots = extractReusableSlotsByName(
    component,
    requiredSidebarSlotNamesForContent(props.content),
    stats,
    "Sidebar",
  );
  removeDirectChildrenExceptMany(component, Object.values(reusableSlots));

  const header = await createSidebarHeaderSlot({
    component,
    compact: isRail,
    reusableSlot: reusableSlotNamed(reusableSlots, "Header Slot"),
    variableByName,
    fonts,
    stats,
  });
  component.appendChild(header);
  setVerticalStackChildSizing(header);

  const navigation = await createSidebarNavigationSlot({
    component,
    content: props.content,
    reusableSlot: reusableSlotNamed(reusableSlots, "Navigation Slot"),
    variableByName,
    fonts,
    stats,
  });
  component.appendChild(navigation);
  setVerticalStackFillChildSizing(navigation);

  if (hasTools) {
    const tools = await createSidebarToolsSlot({
      component,
      reusableSlot: reusableSlotNamed(reusableSlots, "Tools Slot"),
      variableByName,
      fonts,
      stats,
    });
    component.appendChild(tools);
    setVerticalStackChildSizing(tools);
  }

  const footer = await createSidebarFooterSlot({
    component,
    compact: isRail,
    reusableSlot: reusableSlotNamed(reusableSlots, "Footer Slot"),
    variableByName,
    fonts,
    stats,
  });
  component.appendChild(footer);
  setVerticalStackChildSizing(footer);
}

async function createSidebarHeaderSlot({
  component,
  compact = false,
  reusableSlot,
  variableByName,
  fonts,
  stats,
}) {
  const shell = createOrReuseShellSlot({
    component,
    reusableSlot,
    ownerName: "Sidebar",
    stats,
    slotName: "Header Slot",
    width: compact ? SIDEBAR_RAIL_SLOT_WIDTH : SIDEBAR_SLOT_WIDTH,
    height: SIDEBAR_HEADER_FOOTER_HEIGHT,
    layoutMode: "HORIZONTAL",
    itemSpacing: 10,
  });
  const slot = shell.node;
  if (shell.reusedContent) return slot;

  const mark = createBrandMark(variableByName, stats, 32);
  slot.appendChild(mark);
  setHugChildSizing(mark);

  if (compact) {
    return slot;
  }

  const title = figma.createText();
  title.name = "Title Text";
  await applySidebarTitleTypography(title, fonts, variableByName, stats);
  title.characters = "Workspace";
  title.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  title.textAlignVertical = "CENTER";
  title.resizeWithoutConstraints(SIDEBAR_SLOT_WIDTH - 72, 20);
  setTextAutoResize(title, "TRUNCATE");
  slot.appendChild(title);
  setHorizontalStackFillHugChildSizing(title);

  return slot;
}

async function createSidebarNavigationSlot({
  component,
  content,
  reusableSlot,
  variableByName,
  fonts,
  stats,
}) {
  if (content === "Rail") {
    return createSidebarRailNavigationSlot({
      component,
      reusableSlot,
      variableByName,
      fonts,
      stats,
    });
  }

  const shell = createOrReuseShellSlot({
    component,
    reusableSlot,
    ownerName: "Sidebar",
    stats,
    slotName: "Navigation Slot",
    width: SIDEBAR_SLOT_WIDTH,
    height:
      content === "Tools"
        ? SIDEBAR_TOOLS_NAVIGATION_HEIGHT
        : SIDEBAR_NAVIGATION_HEIGHT,
    layoutMode: "VERTICAL",
    itemSpacing: 4,
  });
  const slot = shell.node;
  if (shell.reusedContent) return slot;

  if (content === "Sections") {
    const label = figma.createText();
    label.name = "Section Text";
    await applySidebarSectionTypography(label, fonts, variableByName, stats);
    label.characters = "Navigation";
    label.fills = [
      paintFromVariable(
        "Colors/foreground/400",
        "#5D626F",
        variableByName,
        stats,
      ),
    ];
    label.textAlignVertical = "CENTER";
    label.resizeWithoutConstraints(SIDEBAR_SLOT_WIDTH, 20);
    setTextAutoResize(label, "TRUNCATE");
    slot.appendChild(label);
    setVerticalStackChildSizing(label);
  }

  const rows = [
    {
      name: "Item 1 Text",
      label: "Overview",
      icon: "home-line",
      active: true,
    },
    { name: "Item 2 Text", label: "Explore", icon: "search-md", active: false },
    {
      name: "Item 3 Text",
      label: "Settings",
      icon: "settings-01",
      active: false,
    },
  ];

  for (const row of rows) {
    const item = await createSidebarNavigationRow({
      row,
      variableByName,
      fonts,
      stats,
    });
    slot.appendChild(item);
    setVerticalFixedFillChildSizing(item);
  }

  return slot;
}

async function createSidebarRailNavigationSlot({
  component,
  reusableSlot,
  variableByName,
  fonts,
  stats,
}) {
  const shell = createOrReuseShellSlot({
    component,
    reusableSlot,
    ownerName: "Sidebar",
    stats,
    slotName: "Navigation Slot",
    width: SIDEBAR_RAIL_SLOT_WIDTH,
    height: SIDEBAR_RAIL_NAVIGATION_HEIGHT,
    layoutMode: "VERTICAL",
    itemSpacing: 8,
  });
  const slot = shell.node;
  if (shell.reusedContent) return slot;

  const rows = [
    {
      name: "Item 1 Text",
      label: "Home",
      icon: "home-line",
      active: false,
    },
    { name: "Item 2 Text", label: "Search", icon: "search-md", active: true },
    {
      name: "Item 3 Text",
      label: "Settings",
      icon: "settings-01",
      active: false,
    },
  ];

  for (const row of rows) {
    const item = await createSidebarRailNavigationRow({
      row,
      variableByName,
      fonts,
      stats,
    });
    slot.appendChild(item);
    setVerticalFixedFillChildSizing(item);
  }

  return slot;
}

async function createSidebarNavigationRow({
  row,
  variableByName,
  fonts,
  stats,
}) {
  return createNavigationItemNestedInstance({
    content: "Icon Label",
    density: "Default",
    fallbackName: `${row.name} Row`,
    iconName: row.icon,
    label: row.label,
    placement: "Side",
    role: "sidebar-navigation-item",
    state: row.active ? "Selected" : "Default",
    textNodeName: row.name,
    variableByName,
    fonts,
    stats,
  });
}

async function createSidebarRailNavigationRow({
  row,
  variableByName,
  fonts,
  stats,
}) {
  return createNavigationItemNestedInstance({
    content: "Icon Label",
    density: "Default",
    fallbackName: `${row.name} Rail Row`,
    iconName: row.icon,
    label: row.label,
    placement: "Rail",
    role: "sidebar-rail-navigation-item",
    state: row.active ? "Selected" : "Default",
    textNodeName: row.name,
    variableByName,
    fonts,
    stats,
  });
}

async function createSidebarToolsSlot({
  component,
  reusableSlot,
  variableByName,
  fonts,
  stats,
}) {
  const shell = createOrReuseShellSlot({
    component,
    reusableSlot,
    ownerName: "Sidebar",
    stats,
    slotName: "Tools Slot",
    width: SIDEBAR_SLOT_WIDTH,
    height: SIDEBAR_TOOLS_SLOT_HEIGHT,
    layoutMode: "VERTICAL",
    itemSpacing: 4,
  });
  const slot = shell.node;
  if (shell.reusedContent) return slot;

  const rows = [
    {
      name: "Tool Text",
      label: "Notifications",
      icon: "bell-01",
      active: false,
    },
    {
      name: "Settings Text",
      label: "Settings",
      icon: "settings-01",
      active: false,
    },
  ];

  for (const row of rows) {
    const item = await createSidebarNavigationRow({
      row,
      variableByName,
      fonts,
      stats,
    });
    slot.appendChild(item);
    setVerticalFixedFillChildSizing(item);
  }

  return slot;
}

async function createSidebarFooterSlot({
  component,
  compact = false,
  reusableSlot,
  variableByName,
  fonts,
  stats,
}) {
  const shell = createOrReuseShellSlot({
    component,
    reusableSlot,
    ownerName: "Sidebar",
    stats,
    slotName: "Footer Slot",
    width: compact ? SIDEBAR_RAIL_SLOT_WIDTH : SIDEBAR_SLOT_WIDTH,
    height: SIDEBAR_HEADER_FOOTER_HEIGHT,
    layoutMode: "HORIZONTAL",
    itemSpacing: 10,
  });
  const slot = shell.node;
  if (shell.reusedContent) return slot;

  const userIcon = await createFixedIconInstance(
    "user-01",
    "Colors/foreground/400",
    "#5D626F",
    variableByName,
    stats,
    20,
  );
  userIcon.name = "Footer Icon";
  slot.appendChild(userIcon);
  setHugChildSizing(userIcon);

  if (compact) {
    return slot;
  }

  const label = figma.createText();
  label.name = "Footer Text";
  await applySidebarItemTypography(label, fonts, variableByName, stats);
  label.characters = "Account";
  label.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  label.textAlignVertical = "CENTER";
  label.resizeWithoutConstraints(SIDEBAR_SLOT_WIDTH - 42, 20);
  setTextAutoResize(label, "TRUNCATE");
  slot.appendChild(label);
  setHorizontalStackFillHugChildSizing(label);

  return slot;
}

function createSampleSlotFrame({
  name,
  width,
  height,
  layoutMode,
  itemSpacing,
}) {
  const slot = figma.createFrame();
  slot.name = name;
  slot.layoutMode = layoutMode;
  slot.primaryAxisSizingMode = "FIXED";
  slot.counterAxisSizingMode = "FIXED";
  slot.primaryAxisAlignItems = "MIN";
  slot.counterAxisAlignItems = layoutMode === "VERTICAL" ? "MIN" : "CENTER";
  slot.itemSpacing = itemSpacing;
  slot.paddingLeft = 0;
  slot.paddingRight = 0;
  slot.paddingTop = 0;
  slot.paddingBottom = 0;
  slot.resizeWithoutConstraints(width, height);
  slot.fills = [];
  slot.strokes = [];
  slot.clipsContent = false;
  slot.setSharedPluginData(RUN_NAMESPACE, "kind", "content-slot");
  slot.setSharedPluginData(RUN_NAMESPACE, "slot-kind", "frame-default");
  return slot;
}

function createBrandMark(variableByName, stats, size) {
  const mark = figma.createFrame();
  mark.name = "Brand Mark";
  mark.layoutMode = "HORIZONTAL";
  mark.primaryAxisSizingMode = "FIXED";
  mark.counterAxisSizingMode = "FIXED";
  mark.primaryAxisAlignItems = "CENTER";
  mark.counterAxisAlignItems = "CENTER";
  mark.itemSpacing = 0;
  mark.paddingLeft = 0;
  mark.paddingRight = 0;
  mark.paddingTop = 0;
  mark.paddingBottom = 0;
  mark.resizeWithoutConstraints(size, size);
  mark.cornerRadius = 8;
  mark.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  mark.strokes = [];
  mark.clipsContent = false;
  return mark;
}

async function createBackdropVariant({ props, variableByName, stats }) {
  const component = figma.createComponent();
  await updateBackdropVariant(component, {
    props,
    variableByName,
    stats,
  });
  return component;
}

async function updateBackdropVariant(
  component,
  { props, variableByName, stats },
) {
  const visible = props.visibility === "Visible";
  component.name = `Visibility=${props.visibility}`;
  component.layoutMode = "NONE";
  component.resizeWithoutConstraints(320, 180);
  component.cornerRadius = 12;
  component.clipsContent = false;
  component.fills = visible
    ? [paintFromVariable("Overlay/Scrim", "#00000080", variableByName, stats)]
    : [];
  component.strokes = [];
  component.strokeWeight = 0;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Backdrop");
  removeDirectChildren(component);
}

async function createBottomSheetVariant({
  props,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateBottomSheetVariant(component, {
    props,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

async function updateBottomSheetVariant(
  component,
  { props, variableByName, fonts, stats },
) {
  component.name = `Content=${props.content}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 8;
  component.paddingLeft = 16;
  component.paddingRight = 16;
  component.paddingTop = 8;
  component.paddingBottom = 24;
  component.resizeWithoutConstraints(360, 280);
  try {
    component.topLeftRadius = 16;
    component.topRightRadius = 16;
    component.bottomLeftRadius = 0;
    component.bottomRightRadius = 0;
  } catch (_error) {
    component.cornerRadius = 16;
  }
  component.clipsContent = false;
  component.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  component.strokes = [
    paintFromVariable(
      "Colors/background/200",
      "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.effects = tooltipShadowEffects();
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "BottomSheet");

  await syncBottomSheetVariantChildren({
    component,
    content: props.content,
    variableByName,
    fonts,
    stats,
  });
  normalizeBottomSheetVariantCompositeSizing(component);
}

async function createSearchBarVariant({ props, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateSearchBarVariant(component, {
    props,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

async function updateSearchBarVariant(
  component,
  { props, variableByName, fonts, stats },
) {
  const config = searchBarConfig(props.variant, props.state);
  const filled = props.state === "Filled";

  component.name = `Variant=${props.variant}, State=${props.state}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 10;
  component.paddingLeft = 16;
  component.paddingRight = 14;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(config.width, config.height);
  component.cornerRadius = config.radius;
  component.clipsContent = false;
  component.fills = [
    paintFromVariable(
      config.background,
      config.backgroundFallback,
      variableByName,
      stats,
    ),
  ];
  component.strokes = [
    paintFromVariable(
      config.stroke,
      config.strokeFallback,
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.effects = config.effects;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "SearchBar");

  removeDirectChildren(component);

  const icon = await createFixedIconInstance(
    "search-md",
    config.icon,
    config.iconFallback,
    variableByName,
    stats,
    20,
  );
  icon.name = "Search Icon";
  component.appendChild(icon);
  setHugChildSizing(icon);

  const text = figma.createText();
  text.name = filled ? "Value Text" : "Placeholder Text";
  await applyInputTextTypography(text, fonts, variableByName, stats);
  text.characters = filled ? "Selected item" : "Search items";
  text.fills = [
    paintFromVariable(
      filled ? config.value : config.placeholder,
      filled ? config.valueFallback : config.placeholderFallback,
      variableByName,
      stats,
    ),
  ];
  text.textAlignVertical = "CENTER";
  setHorizontalFillTextSizing(text);
  setTextAutoResize(text, "TRUNCATE");
  component.appendChild(text);

  if (filled) {
    const clearIcon = await createFixedIconInstance(
      "x-close",
      config.icon,
      config.iconFallback,
      variableByName,
      stats,
      18,
    );
    clearIcon.name = "Clear Icon";
    component.appendChild(clearIcon);
    setHugChildSizing(clearIcon);
  }

  syncFocusRing(component, {
    enabled: props.state !== "Disabled",
    width: config.width,
    height: config.height,
    radius: config.radius,
    variableName: "Colors/theme/500",
    fallback: "#135BEC",
    variableByName,
    stats,
  });
}

function layoutTagVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;
    const props = parseTagVariantName(child.name);
    if (!props) continue;
    const variantIndex = TAG_VARIANTS.indexOf(props.variant);
    const removableIndex = TAG_REMOVABLE.indexOf(props.removable);
    child.x = variantIndex * 180;
    child.y = removableIndex * 64;
  }

  resizeComponentSetToContainChildren(componentSet);
}

function layoutScrollAreaVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;
    const props = parseScrollAreaVariantName(child.name);
    if (!props) continue;
    child.x = SCROLL_AREA_ORIENTATIONS.indexOf(props.orientation) * 420;
    child.y = SCROLL_AREA_SCROLLBARS.indexOf(props.scrollbar) * 260;
  }

  resizeComponentSetToContainChildren(componentSet);
}

function layoutBottomNavigationVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;
    const props = parseBottomNavigationVariantName(child.name);
    if (!props) continue;
    const countIndex = BOTTOM_NAVIGATION_COUNTS.indexOf(props.count);
    const activeIndex = BOTTOM_NAVIGATION_ACTIVE.indexOf(props.active);
    child.x = activeIndex * 420;
    child.y = countIndex * 96;
  }

  resizeComponentSetToContainChildren(componentSet);
}

function layoutNavigationItemVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;
    const props = parseNavigationItemVariantName(child.name);
    if (!props) continue;
    const placementIndex = NAVIGATION_ITEM_PLACEMENTS.indexOf(props.placement);
    const contentIndex = NAVIGATION_ITEM_CONTENT.indexOf(props.content);
    const densityIndex = NAVIGATION_ITEM_DENSITIES.indexOf(props.density);
    const stateIndex = NAVIGATION_ITEM_STATES.indexOf(props.state);
    if (
      placementIndex === -1 ||
      contentIndex === -1 ||
      densityIndex === -1 ||
      stateIndex === -1
    ) {
      continue;
    }

    child.x = stateIndex * NAVIGATION_ITEM_VARIANT_LAYOUT.stateColumnStep;
    child.y =
      navigationItemPlacementStartY(props.placement) +
      navigationItemContentStartY(props.placement, props.content) +
      densityIndex * navigationItemDensityRowStep(props.placement);
  }

  resizeComponentSetToContainChildren(
    componentSet,
    NAVIGATION_ITEM_VARIANT_LAYOUT.outerPadding,
  );
}

function navigationItemPlacementStartY(placement) {
  let y = 0;

  for (const candidate of NAVIGATION_ITEM_PLACEMENTS) {
    if (candidate === placement) return y;
    y +=
      navigationItemPlacementGroupHeight(candidate) +
      NAVIGATION_ITEM_VARIANT_LAYOUT.placementGroupGap;
  }

  return y;
}

function navigationItemPlacementGroupHeight(placement) {
  const contentCount = NAVIGATION_ITEM_CONTENT.length;
  return (
    contentCount * navigationItemContentGroupHeight(placement) +
    Math.max(0, contentCount - 1) *
      NAVIGATION_ITEM_VARIANT_LAYOUT.contentGroupGap
  );
}

function navigationItemContentStartY(placement, content) {
  const contentIndex = NAVIGATION_ITEM_CONTENT.indexOf(content);
  if (contentIndex <= 0) return 0;

  return (
    contentIndex *
    (navigationItemContentGroupHeight(placement) +
      NAVIGATION_ITEM_VARIANT_LAYOUT.contentGroupGap)
  );
}

function navigationItemContentGroupHeight(placement) {
  const densityCount = NAVIGATION_ITEM_DENSITIES.length;
  return (
    densityCount * navigationItemRowHeight(placement) +
    Math.max(0, densityCount - 1) * NAVIGATION_ITEM_VARIANT_LAYOUT.densityRowGap
  );
}

function navigationItemDensityRowStep(placement) {
  return (
    navigationItemRowHeight(placement) +
    NAVIGATION_ITEM_VARIANT_LAYOUT.densityRowGap
  );
}

function navigationItemRowHeight(placement) {
  return (
    NAVIGATION_ITEM_VARIANT_LAYOUT.rowHeights[placement] ||
    NAVIGATION_ITEM_VARIANT_LAYOUT.rowHeights.Side
  );
}

function layoutNavbarVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;
    const props = parseNavbarVariantName(child.name);
    if (!props) continue;
    child.x = NAVBAR_CONTENT.indexOf(props.content) * 780;
    child.y = 0;
  }

  resizeComponentSetToContainChildren(componentSet);
}

function layoutSidebarVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;
    const props = parseSidebarVariantName(child.name);
    if (!props) continue;
    child.x = SIDEBAR_CONTENT.indexOf(props.content) * 340;
    child.y = 0;
  }

  resizeComponentSetToContainChildren(componentSet);
}

function layoutBackdropVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;
    const props = parseBackdropVariantName(child.name);
    if (!props) continue;
    child.x = BACKDROP_VISIBILITY.indexOf(props.visibility) * 360;
    child.y = 0;
  }

  resizeComponentSetToContainChildren(componentSet);
}

function layoutBottomSheetVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;
    const props = parseBottomSheetVariantName(child.name);
    if (!props) continue;
    child.x = BOTTOM_SHEET_CONTENT.indexOf(props.content) * 420;
    child.y = 0;
  }

  resizeComponentSetToContainChildren(componentSet);
}

function layoutSearchBarVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;
    const props = parseSearchBarVariantName(child.name);
    if (!props) continue;
    child.x = SEARCH_BAR_VARIANTS.indexOf(props.variant) * 420;
    child.y = SEARCH_BAR_STATES.indexOf(props.state) * 96;
  }

  resizeComponentSetToContainChildren(componentSet);
}

function layoutTimelineVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;
    const props = parseTimelineVariantName(child.name);
    if (!props) continue;
    child.x = TIMELINE_CONTENT.indexOf(props.content) * 500;
    child.y = TIMELINE_DENSITIES.indexOf(props.density) * 320;
  }

  resizeComponentSetToContainChildren(componentSet);
}

function layoutTreeVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;
    const props = parseTreeVariantName(child.name);
    if (!props) continue;
    const densityIndex = TREE_DENSITIES.indexOf(props.density);
    child.x = densityIndex * 380;
    child.y = 0;
  }

  resizeComponentSetToContainChildren(componentSet);
}

function layoutTreeItemVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;
    const props = parseTreeItemVariantName(child.name);
    if (!props) continue;
    const typeIndex = TREE_ITEM_TYPES.indexOf(props.type);
    const contentIndex = TREE_CONTENT.indexOf(props.content);
    const expandedIndex =
      props.type === "Parent" ? TREE_EXPANDED.indexOf(props.expanded) : 0;
    const depthIndex = TREE_ITEM_DEPTHS.indexOf(props.depth);
    const densityIndex = TREE_DENSITIES.indexOf(props.density);
    const stateIndex = TREE_STATES.indexOf(props.state);

    child.x = (contentIndex * TREE_STATES.length + stateIndex) * 380;
    child.y =
      (((typeIndex * TREE_ITEM_DEPTHS.length + depthIndex) *
        TREE_DENSITIES.length +
        densityIndex) *
        2 +
        expandedIndex) *
      72;
  }

  resizeComponentSetToContainChildren(componentSet);
}

function layoutTreeParentItemVariants(componentSet) {
  layoutStandaloneTreeItemVariants(
    componentSet,
    parseTreeParentItemVariantName,
  );
}

function layoutTreeChildItemVariants(componentSet) {
  layoutStandaloneTreeItemVariants(componentSet, parseTreeChildItemVariantName);
}

function layoutStandaloneTreeItemVariants(componentSet, parseVariantName) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;
    const props = parseVariantName(child.name);
    if (!props) continue;
    const contentIndex = TREE_CONTENT.indexOf(props.content);
    const expandedIndex =
      props.type === "Parent" ? TREE_EXPANDED.indexOf(props.expanded) : 0;
    const depthIndex = TREE_ITEM_DEPTHS.indexOf(props.depth);
    const densityIndex = TREE_DENSITIES.indexOf(props.density);
    const stateIndex = TREE_STATES.indexOf(props.state);

    child.x = (contentIndex * TREE_STATES.length + stateIndex) * 380;
    child.y =
      ((depthIndex * TREE_DENSITIES.length + densityIndex) *
        (props.type === "Parent" ? 2 : 1) +
        expandedIndex) *
      72;
  }

  resizeComponentSetToContainChildren(componentSet);
}

function configureTagProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Label Text",
    "Label Text",
    "Tag",
    stats,
  );
}

function configureScrollAreaProperties(componentSet, stats) {
  configureSharedSlotProperties(componentSet, ["Content Slot"], stats);
}

function configureBottomNavigationProperties(componentSet, stats) {
  for (let index = 1; index <= 5; index += 1) {
    const label = `Item ${index} Text`;
    configureNamedTextProperty(
      componentSet,
      label,
      label,
      bottomNavigationLabel(index - 1),
      stats,
    );
  }
}

async function configureNavigationItemProperties(
  componentSet,
  stats,
  variableByName,
) {
  configureNamedTextProperty(
    componentSet,
    "Label Text",
    "Label Text",
    "Overview",
    stats,
  );
  configureSharedSlotProperties(
    componentSet,
    ["Leading Icon Slot", "Badge Slot", "Trailing Slot"],
    stats,
  );
  configureFocusVisibleProperty(componentSet, stats);
  await configureNavigationItemIconSlots(componentSet, variableByName, stats);
}

async function configureNavigationItemIconSlots(
  componentSet,
  variableByName,
  stats,
) {
  if (!componentSet || !componentSet.children) return;

  const iconSourceComponents = await findKozmosIconSourceComponents();
  const preferredValues = iconPreferredValues(iconSourceComponents);
  const defaultIcon =
    (await findKozmosIconSourceComponent("home-line")) ||
    (await resolveDefaultIconSourceComponent(variableByName, stats));
  if (!defaultIcon) return;

  const propertyName = ensureInstanceSwapProperty(
    componentSet,
    "Leading Icon",
    defaultIcon.id,
    stats,
    preferredValues,
  );
  if (!propertyName) return;

  let boundCount = 0;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const iconSlot = directChildNamed(child, "Leading Icon Slot");
    const icon = directChildNamed(iconSlot || { children: [] }, "Leading Icon");
    if (icon && icon.type === "INSTANCE") {
      bindInstanceSwapProperty(icon, propertyName, stats);
      boundCount += 1;
    }
  }

  stats.navigationItemIconBindings = boundCount;
}

function configureNavbarProperties(componentSet, stats) {
  configureSharedSlotProperties(componentSet, NAVBAR_SLOT_NAMES, stats);
  deleteComponentPropertiesByBaseName(
    componentSet,
    [
      "Site Slot",
      "Site Label Text",
      "Site Text",
      "Logo Text",
      "Context Label Text",
      "Context Text",
      "Action Text",
      "Primary Action Text",
      "Item 1 Text",
      "Item 2 Text",
      "Item 3 Text",
    ],
    ["SLOT", "TEXT"],
    stats,
  );
}

function configureSidebarProperties(componentSet, stats) {
  configureSharedSlotProperties(componentSet, SIDEBAR_SLOT_NAMES, stats);
  deleteComponentPropertiesByBaseName(
    componentSet,
    ["Content Slot"],
    ["SLOT"],
    stats,
  );
  deleteComponentPropertiesByBaseName(
    componentSet,
    [
      "Title Text",
      "Section Text",
      "Footer Text",
      "Item 1 Text",
      "Item 2 Text",
      "Item 3 Text",
      "Tool Text",
      "Settings Text",
    ],
    ["TEXT"],
    stats,
  );
}

function configureTreeProperties(componentSet, stats) {
  deleteComponentPropertiesByBaseName(
    componentSet,
    [
      "Root Text",
      "Child Text",
      "Leaf Text",
      "Sibling Text",
      "Root Count Text",
      "Child Count Text",
      "Leaf Count Text",
      "Sibling Count Text",
      "Focus Visible",
    ],
    ["TEXT", "BOOLEAN"],
    stats,
  );
  configureSharedSlotProperties(componentSet, ["Content Slot"], stats);
}

async function configureTreeItemProperties(
  componentSet,
  stats,
  variableByName,
) {
  const setName = componentSet ? componentSet.name : "";
  const isChildSet = setName === "TreeChildItem";
  configureNamedTextProperty(
    componentSet,
    "Label Text",
    "Label Text",
    isChildSet ? "Place item" : "Map content",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Count Text",
    "Count Text",
    isChildSet ? "4" : "23",
    stats,
  );
  configureFocusVisibleProperty(componentSet, stats);
  await configureTreeItemIconSlots(componentSet, variableByName, stats);
}

async function configureTreeItemIconSlots(componentSet, variableByName, stats) {
  if (!componentSet || !componentSet.children) return;

  const iconSourceComponents = await findKozmosIconSourceComponents();
  const preferredValues = iconPreferredValues(iconSourceComponents);
  const defaultIcon = await resolveDefaultIconSourceComponent(
    variableByName,
    stats,
  );
  const isChildSet = componentSet.name === "TreeChildItem";
  const leadingDefault =
    (await findKozmosIconSourceComponent(
      isChildSet ? "marker-pin-01" : "map-01",
    )) || defaultIcon;
  const actionOneDefault =
    (await findKozmosIconSourceComponent(isChildSet ? "edit-01" : "x-close")) ||
    defaultIcon;
  const actionTwoDefault =
    (await findKozmosIconSourceComponent("lock-01")) || defaultIcon;

  const slots = [
    {
      nodeName: "Leading Icon",
      propertyName: "Leading Icon",
      defaultComponent: leadingDefault,
    },
    {
      nodeName: "Action 1 Icon",
      propertyName: "Action 1 Icon",
      defaultComponent: actionOneDefault,
    },
    {
      nodeName: "Action 2 Icon",
      propertyName: "Action 2 Icon",
      defaultComponent: actionTwoDefault,
    },
  ];

  for (const slot of slots) {
    if (!slot.defaultComponent) continue;

    const componentPropertyName = ensureInstanceSwapProperty(
      componentSet,
      slot.propertyName,
      slot.defaultComponent.id,
      stats,
      preferredValues,
    );
    if (!componentPropertyName) continue;

    bindTreeItemIconSlotInstances(
      componentSet,
      slot.nodeName,
      componentPropertyName,
      stats,
    );
  }
}

function bindTreeItemIconSlotInstances(
  componentSet,
  nodeName,
  propertyName,
  stats,
) {
  if (!componentSet || !componentSet.children || !propertyName) return;

  function walk(node) {
    if (node.name === nodeName && node.type === "INSTANCE") {
      bindInstanceSwapProperty(node, propertyName, stats);
      stats.iconSlotsBound = (stats.iconSlotsBound || 0) + 1;
    }

    if (!node.children) return;
    for (const child of node.children) walk(child);
  }

  for (const child of componentSet.children) {
    if (child.type === "COMPONENT") walk(child);
  }
}

function configureBackdropProperties(_componentSet, _stats) {}

function configureBottomSheetProperties(componentSet, stats) {
  deleteComponentPropertiesByBaseName(
    componentSet,
    ["Body Text"],
    ["TEXT"],
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Title Text",
    "Title Text",
    "Sheet title",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Description Text",
    "Description Text",
    "Use this sheet for a focused mobile task.",
    stats,
  );
  configureSharedSlotProperties(componentSet, ["Content Slot"], stats);
  normalizeBottomSheetCompositeSizing(componentSet, stats);
}

function normalizeBottomSheetCompositeSizing(componentSet, stats) {
  if (!componentSet || !componentSet.children) return;

  for (const component of componentSet.children) {
    if (component.type !== "COMPONENT") continue;
    normalizeBottomSheetVariantCompositeSizing(component, stats);
  }
}

function normalizeDialogCompositeSizing(componentSet, stats) {
  if (!componentSet || !componentSet.children) return;

  for (const component of componentSet.children) {
    if (component.type !== "COMPONENT") continue;
    normalizeDialogVariantCompositeSizing(component, stats);
  }
}

function normalizeDialogVariantCompositeSizing(component, stats) {
  const props = parseDialogVariantName(component.name);
  if (!props || props.value !== "Form") return;

  const body = directChildNamed(component, "Dialog Body");
  if (!body || body.type !== "FRAME") return;

  setCompositeFormInputParentSizingContext(body);
  for (const nodeName of ["Name Input", "Username Input"]) {
    setCompositeFormInputChildSizing(directChildNamed(body, nodeName), stats);
  }
}

function normalizeBottomSheetVariantCompositeSizing(component, stats) {
  const props = parseBottomSheetVariantName(component.name);
  if (!props || props.content !== "Form") return;

  const body = directChildNamed(component, "BottomSheet Body");
  if (!body || body.type !== "FRAME") return;

  setCompositeFormInputParentSizingContext(body);
  for (const formInput of BOTTOM_SHEET_FORM_INPUTS) {
    setCompositeFormInputChildSizing(
      directChildNamed(body, formInput.name),
      stats,
    );
  }
}

function runGeneratedComponentPostUpdateMaintenance(
  componentSet,
  componentName,
  stats,
) {
  if (!componentSet || componentSet.type !== "COMPONENT_SET") return;

  if (componentName !== "Dialog" && componentName !== "BottomSheet") return;

  const before = auditCompositionIntegrity(componentSet);
  if (componentName === "Dialog") {
    normalizeDialogCompositeSizing(componentSet, stats);
  } else {
    normalizeBottomSheetCompositeSizing(componentSet, stats);
  }
  const repaired = repairCompositeFormInputSizingInPlace(
    componentSet,
    componentName,
  );
  const after = auditCompositionIntegrity(componentSet);

  if (before.issueCount > after.issueCount || repaired > 0) {
    stats.compositeRepairsApplied =
      (stats.compositeRepairsApplied || 0) + repaired;
  }
}

async function runGeneratedComponentPostLayoutMaintenance({
  componentName,
  componentSetName,
  stats,
}) {
  if (componentName !== "Dialog" && componentName !== "BottomSheet") return;

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  const componentSet = findComponentSetOnPage(page, componentSetName);
  if (!componentSet || componentSet.type !== "COMPONENT_SET") return;

  runGeneratedComponentPostUpdateMaintenance(
    componentSet,
    componentName,
    stats,
  );
}

async function repairCompositeComponentAfterUpdate({
  stats,
  componentName,
  componentSetName,
}) {
  if (!stats || !stats.updated) return stats;

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  const componentSet = findComponentSetOnPage(page, componentSetName);
  if (!componentSet || componentSet.type !== "COMPONENT_SET") return stats;

  const before = auditCompositionIntegrity(componentSet);
  if (before.issueCount === 0) return stats;

  const repaired = repairCompositeFormInputSizingInPlace(
    componentSet,
    componentName,
  );
  const after = auditCompositionIntegrity(componentSet);
  if (after.issueCount === 0) {
    stats.compositeRepairsApplied =
      (stats.compositeRepairsApplied || 0) + repaired;
    return stats;
  }

  if (repaired > 0) {
    stats.compositeRepairsApplied =
      (stats.compositeRepairsApplied || 0) + repaired;
  }
  stats.compositeRepairIssueCount = after.issueCount;
  pushCompositeRepairFailureWarning(stats, componentName, before, after);
  return stats;
}

function pushCompositeRepairFailureWarning(
  stats,
  componentName,
  before,
  after,
) {
  const firstIssue = after.issues && after.issues[0];
  const issueSummary = firstIssue
    ? ` First remaining issue: ${compositionIssueSummary(firstIssue)}.`
    : "";
  pushUniqueWarning(
    stats,
    `composite-repair:${componentName}:${after.issueCount}`,
    `${componentName}: ${after.issueCount} composite sizing issue(s) remain after in-place repair; update did not rebuild the component set, so Code Connect node IDs were preserved. Before repair there were ${before.issueCount} issue(s).${issueSummary}`,
  );
}

function compositionIssueSummary(issue) {
  const location = [issue.kind, issue.variant, issue.node]
    .filter(Boolean)
    .join(" / ");
  const expected =
    typeof issue.expected === "undefined" ? "unspecified" : issue.expected;
  const actual = typeof issue.actual === "undefined" ? "unset" : issue.actual;

  return `${location || "unknown"} expected ${expected}, actual ${actual}`;
}

function repairCompositeFormInputSizingInPlace(componentSet, componentName) {
  let repaired = 0;

  for (const component of componentSet.children || []) {
    if (component.type !== "COMPONENT") continue;

    if (componentName === "Dialog") {
      const props = parseDialogVariantName(component.name);
      if (!props || props.value !== "Form") continue;

      const body = directChildNamed(component, "Dialog Body");
      if (!body || body.type !== "FRAME") continue;

      for (const nodeName of ["Name Input", "Username Input"]) {
        const input = directChildNamed(body, nodeName);
        if (
          isNestedComponentInstance(input, "Input") &&
          setCompositeFormInputChildSizing(input, null)
        ) {
          repaired += 1;
        }
      }
      continue;
    }

    if (componentName === "BottomSheet") {
      const props = parseBottomSheetVariantName(component.name);
      if (!props || props.content !== "Form") continue;

      const body = directChildNamed(component, "BottomSheet Body");
      if (!body || body.type !== "FRAME") continue;

      for (const formInput of BOTTOM_SHEET_FORM_INPUTS) {
        const input = directChildNamed(body, formInput.name);
        if (
          isNestedComponentInstance(input, "Input") &&
          setCompositeFormInputChildSizing(input, null)
        ) {
          repaired += 1;
        }
      }
    }
  }

  return repaired;
}

function configureSearchBarProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Placeholder Text",
    "Placeholder Text",
    "Search items",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Value Text",
    "Value Text",
    "Selected item",
    stats,
  );
  configureFocusVisibleProperty(componentSet, stats);
}

function configureTimelineProperties(componentSet, stats) {
  for (let index = 1; index <= 3; index += 1) {
    configureNamedTextProperty(
      componentSet,
      `Item ${index} Time`,
      `Item ${index} Time`,
      timelineTimeText(index - 1),
      stats,
    );
    configureNamedTextProperty(
      componentSet,
      `Item ${index} Title`,
      `Item ${index} Title`,
      timelineTitleText(index - 1),
      stats,
    );
    configureNamedTextProperty(
      componentSet,
      `Item ${index} Description`,
      `Item ${index} Description`,
      timelineDescriptionText(index - 1),
      stats,
    );
  }
}

function tagConfig(variant) {
  if (variant === "Destructive") {
    return {
      background: "Colors/emotional/danger/600",
      foreground: "Colors/foreground/1000",
      backgroundFallback: "#D41C42",
      foregroundFallback: "#FFFFFF",
    };
  }

  if (variant === "Secondary") {
    return {
      background: "Colors/background/200",
      foreground: "Colors/foreground/0",
      backgroundFallback: "#C7CAD1",
      foregroundFallback: "#000000",
    };
  }

  if (variant === "Outline") {
    return {
      background: "Surface/0",
      foreground: "Colors/foreground/0",
      stroke: "Colors/background/200",
      backgroundFallback: "#FFFFFF",
      foregroundFallback: "#000000",
      strokeFallback: "#C7CAD1",
    };
  }

  return {
    background: "Colors/theme/600",
    foreground: "Colors/foreground/1000",
    backgroundFallback: "#1051E8",
    foregroundFallback: "#FFFFFF",
  };
}

function tagLabelText(variant) {
  if (variant === "Destructive") return "Blocked";
  if (variant === "Secondary") return "Draft";
  if (variant === "Outline") return "Optional";
  return "Live";
}

function scrollAreaMetrics(orientation) {
  if (orientation === "Horizontal") {
    return {
      width: 360,
      height: 132,
      contentWidth: 336,
      contentHeight: 88,
    };
  }

  return {
    width: 360,
    height: 220,
    contentWidth: 336,
    contentHeight: 196,
  };
}

function appendScrollAreaScrollbar({
  component,
  name,
  vertical,
  metrics,
  variableByName,
  stats,
}) {
  const track = figma.createRectangle();
  track.name = name;
  track.resizeWithoutConstraints(
    vertical ? 6 : metrics.width - 32,
    vertical ? metrics.height - 32 : 6,
  );
  track.cornerRadius = 9999;
  track.fills = [
    paintFromVariableWithOpacity(
      "Colors/foreground/500",
      "#747B8B",
      0.32,
      variableByName,
      stats,
    ),
  ];
  track.strokes = [];
  track.setSharedPluginData(RUN_NAMESPACE, "kind", "scrollarea-scrollbar");
  component.appendChild(track);
  try {
    track.layoutPositioning = "ABSOLUTE";
    track.constraints = vertical
      ? { horizontal: "MAX", vertical: "STRETCH" }
      : { horizontal: "STRETCH", vertical: "MAX" };
  } catch (_error) {
    // Older Figma runtimes may reject absolute constraints on rectangles.
  }
  track.x = vertical ? metrics.width - 12 : 16;
  track.y = vertical ? 16 : metrics.height - 12;

  const thumb = figma.createRectangle();
  thumb.name = vertical ? "Vertical Thumb" : "Horizontal Thumb";
  thumb.resizeWithoutConstraints(vertical ? 6 : 120, vertical ? 72 : 6);
  thumb.cornerRadius = 9999;
  thumb.fills = [
    paintFromVariable(
      "Colors/foreground/400",
      "#5D626F",
      variableByName,
      stats,
    ),
  ];
  thumb.strokes = [];
  thumb.setSharedPluginData(RUN_NAMESPACE, "kind", "scrollarea-thumb");
  component.appendChild(thumb);
  try {
    thumb.layoutPositioning = "ABSOLUTE";
    thumb.constraints = vertical
      ? { horizontal: "MAX", vertical: "SCALE" }
      : { horizontal: "SCALE", vertical: "MAX" };
  } catch (_error) {
    // Older Figma runtimes may reject absolute constraints on rectangles.
  }
  thumb.x = vertical ? metrics.width - 12 : 16;
  thumb.y = vertical ? 28 : metrics.height - 12;
}

function bottomNavigationCountToNumber(count) {
  if (count === "Five") return 5;
  if (count === "Four") return 4;
  return 3;
}

function bottomNavigationActiveIsValidForCount(active, count) {
  const itemCount = bottomNavigationCountToNumber(count);
  const activeIndex = BOTTOM_NAVIGATION_ACTIVE.indexOf(active);
  return activeIndex >= 0 && activeIndex < itemCount;
}

function bottomNavigationLabel(index) {
  return ["Home", "Search", "Routes", "Alerts", "More"][index] || "Item";
}

function bottomNavigationIcon(index) {
  return (
    ["building-01", "search-md", "route", "bell-01", "menu-01"][index] ||
    DEFAULT_CURATED_ICON_NAME
  );
}

function timelineMetrics(props) {
  const compact = props.density === "Compact";
  const detailed = props.content === "Detailed";
  const itemHeight = compact ? (detailed ? 76 : 64) : detailed ? 96 : 80;
  return {
    width: 420,
    height: itemHeight * 3,
    itemHeight,
    contentGap: compact ? 2 : 4,
    railWidth: 28,
  };
}

function timelineTimeText(index) {
  return ["09:00", "10:30", "Now"][index] || "Now";
}

function timelineTitleText(index) {
  return ["Created", "Reviewed", "Published"][index] || "Event";
}

function timelineDescriptionText(index) {
  return (
    [
      "Initial event captured.",
      "Status changed by the team.",
      "Latest update is ready.",
    ][index] || "Timeline detail."
  );
}

async function createTimelineItem({
  index,
  isLast,
  props,
  metrics,
  variableByName,
  fonts,
  stats,
}) {
  const active = index === 0;
  const detailed = props.content === "Detailed";

  const item = figma.createFrame();
  item.name = `Item ${index + 1}`;
  item.layoutMode = "HORIZONTAL";
  item.primaryAxisSizingMode = "FIXED";
  item.counterAxisSizingMode = "FIXED";
  item.primaryAxisAlignItems = "MIN";
  item.counterAxisAlignItems = "MIN";
  item.itemSpacing = 8;
  item.paddingLeft = 0;
  item.paddingRight = 0;
  item.paddingTop = 0;
  item.paddingBottom = 0;
  item.resizeWithoutConstraints(metrics.width, metrics.itemHeight);
  item.cornerRadius = 0;
  item.clipsContent = false;
  item.fills = [];
  item.strokes = [];
  item.setSharedPluginData(RUN_NAMESPACE, "kind", "timeline-item");

  const rail = createTimelineRail({
    active,
    isLast,
    height: metrics.itemHeight,
    width: metrics.railWidth,
    variableByName,
    stats,
  });
  item.appendChild(rail);
  setHugChildSizing(rail);

  const content = figma.createFrame();
  content.name = `Item ${index + 1} Content`;
  content.layoutMode = "VERTICAL";
  content.primaryAxisSizingMode = "AUTO";
  content.counterAxisSizingMode = "FIXED";
  content.primaryAxisAlignItems = "MIN";
  content.counterAxisAlignItems = "MIN";
  content.itemSpacing = metrics.contentGap;
  content.paddingLeft = 0;
  content.paddingRight = 0;
  content.paddingTop = active ? 0 : 2;
  content.paddingBottom = 0;
  content.resizeWithoutConstraints(
    metrics.width - metrics.railWidth - 8,
    metrics.itemHeight,
  );
  content.fills = [];
  content.strokes = [];
  content.clipsContent = false;
  item.appendChild(content);
  setHorizontalStackFillHugChildSizing(content);

  if (detailed) {
    const time = figma.createText();
    time.name = `Item ${index + 1} Time`;
    await applyTimelineTimeTypography(time, fonts, variableByName, stats);
    time.characters = timelineTimeText(index);
    time.fills = [
      paintFromVariable(
        "Colors/foreground/0",
        "#000000",
        variableByName,
        stats,
      ),
    ];
    setTextAutoResize(time, "WIDTH_AND_HEIGHT");
    content.appendChild(time);
    setVerticalStackChildSizing(time);
  }

  const title = figma.createText();
  title.name = `Item ${index + 1} Title`;
  await applyTimelineTitleTypography(title, fonts, variableByName, stats);
  title.characters = timelineTitleText(index);
  title.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  setTextAutoResize(title, "TRUNCATE");
  title.resizeWithoutConstraints(metrics.width - metrics.railWidth - 8, 20);
  content.appendChild(title);
  setVerticalStackChildSizing(title);

  const description = figma.createText();
  description.name = `Item ${index + 1} Description`;
  await applyTimelineDescriptionTypography(
    description,
    fonts,
    variableByName,
    stats,
  );
  description.characters = timelineDescriptionText(index);
  description.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  setTextAutoResize(description, "TRUNCATE");
  description.resizeWithoutConstraints(
    metrics.width - metrics.railWidth - 8,
    20,
  );
  content.appendChild(description);
  setVerticalStackChildSizing(description);

  return item;
}

function createTimelineRail({
  active,
  isLast,
  height,
  width,
  variableByName,
  stats,
}) {
  const rail = figma.createFrame();
  rail.name = "Timeline Rail";
  rail.layoutMode = "NONE";
  rail.resizeWithoutConstraints(width, height);
  rail.fills = [];
  rail.strokes = [];
  rail.clipsContent = false;
  rail.setSharedPluginData(RUN_NAMESPACE, "kind", "timeline-rail");

  if (!isLast) {
    const connector = figma.createRectangle();
    connector.name = "Timeline Connector";
    connector.resizeWithoutConstraints(2, Math.max(1, height - 18));
    connector.x = (width - 2) / 2;
    connector.y = 18;
    connector.cornerRadius = 9999;
    connector.fills = [
      paintFromVariable(
        active ? "Colors/theme/500" : "Colors/background/200",
        active ? "#135BEC" : "#C7CAD1",
        variableByName,
        stats,
      ),
    ];
    connector.strokes = [];
    rail.appendChild(connector);
  }

  const marker = figma.createEllipse();
  marker.name = "Timeline Marker";
  marker.resizeWithoutConstraints(10, 10);
  marker.x = (width - 10) / 2;
  marker.y = 6;
  marker.fills = [
    paintFromVariable(
      active ? "Colors/theme/500" : "Surface/0",
      active ? "#135BEC" : "#FFFFFF",
      variableByName,
      stats,
    ),
  ];
  marker.strokes = [
    paintFromVariable(
      active ? "Colors/theme/500" : "Colors/background/200",
      active ? "#135BEC" : "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  marker.strokeWeight = active ? 0 : 1;
  rail.appendChild(marker);

  return rail;
}

async function createBottomNavigationItem({
  index,
  itemCount,
  active,
  width,
  variableByName,
  fonts,
  stats,
}) {
  const item = figma.createFrame();
  item.name = `Item ${index + 1}`;
  item.layoutMode = "VERTICAL";
  item.primaryAxisSizingMode = "FIXED";
  item.counterAxisSizingMode = "FIXED";
  item.primaryAxisAlignItems = "CENTER";
  item.counterAxisAlignItems = "CENTER";
  item.itemSpacing = 3;
  item.paddingLeft = 4;
  item.paddingRight = 4;
  item.paddingTop = 8;
  item.paddingBottom = 6;
  item.resizeWithoutConstraints(width / itemCount, 64);
  item.cornerRadius = 12;
  item.clipsContent = false;
  item.fills = [];
  item.strokes = [];
  item.setSharedPluginData(RUN_NAMESPACE, "kind", "bottom-navigation-item");
  setLayoutSizingHorizontal(item, "FILL");
  try {
    item.layoutGrow = 1;
  } catch (_error) {
    // layoutGrow is unavailable on older plugin runtimes.
  }

  const foreground = active ? "Colors/theme/600" : "Colors/foreground/400";
  const foregroundFallback = active ? "#1051E8" : "#5D626F";

  const icon = await createFixedIconInstance(
    bottomNavigationIcon(index),
    foreground,
    foregroundFallback,
    variableByName,
    stats,
    20,
  );
  icon.name = `Item ${index + 1} Icon`;
  item.appendChild(icon);
  setHugChildSizing(icon);

  const label = figma.createText();
  label.name = `Item ${index + 1} Text`;
  label.fontName = active ? fonts.medium : fonts.regular;
  label.fontSize = 12;
  label.lineHeight = { unit: "PIXELS", value: 16 };
  await applyTextStyleToNodeAsync(label, "fieldMeta", stats);
  label.characters = bottomNavigationLabel(index);
  label.textAlignHorizontal = "CENTER";
  label.textAlignVertical = "CENTER";
  label.fills = [
    paintFromVariable(foreground, foregroundFallback, variableByName, stats),
  ];
  setTextAutoResize(label, "TRUNCATE");
  label.resizeWithoutConstraints(width / itemCount - 16, 16);
  item.appendChild(label);
  setLayoutSizingHorizontal(label, "FILL");
  setLayoutSizingVertical(label, "HUG");

  return item;
}

function searchBarConfig(variant, state) {
  const disabled = state === "Disabled";
  const focused = state === "Focus";
  const floating = variant === "Floating";

  return {
    width: floating ? 360 : 320,
    height: floating ? 56 : 52,
    radius: floating ? 18 : 16,
    background: disabled ? "Colors/background/100" : "Colors/background/0",
    stroke: focused ? "Colors/theme/500" : "Colors/foreground/500",
    icon: disabled ? "Colors/foreground/500" : "Colors/foreground/400",
    placeholder: disabled ? "Colors/foreground/500" : "Colors/foreground/400",
    value: disabled ? "Colors/foreground/500" : "Colors/foreground/0",
    backgroundFallback: disabled ? "#E3E4E8" : "#FFFFFF",
    strokeFallback: focused ? "#135BEC" : "#747B8B",
    iconFallback: disabled ? "#747B8B" : "#5D626F",
    placeholderFallback: disabled ? "#747B8B" : "#5D626F",
    valueFallback: disabled ? "#747B8B" : "#000000",
    effects: floating
      ? [
          {
            type: "DROP_SHADOW",
            color: { r: 0, g: 0, b: 0, a: 0.08 },
            offset: { x: 0, y: 4 },
            radius: 16,
            spread: 0,
            visible: true,
            blendMode: "NORMAL",
          },
        ]
      : [],
  };
}

function configureTextareaProperties(componentSet, stats) {
  configureLabelTextProperty(componentSet, "Textarea", stats);
  configurePlaceholderTextProperty(componentSet, "Placeholder", stats);
  configureFocusVisibleProperty(componentSet, stats);
}

function configureSearchProperties(componentSet, stats) {
  configureLabelTextProperty(componentSet, "Search", stats);
  configurePlaceholderTextProperty(componentSet, "Search", stats);
  configureFocusVisibleProperty(componentSet, stats);
}

function configureSelectProperties(componentSet, stats) {
  configurePlaceholderTextProperty(componentSet, "Select an option", stats);
  configureFocusVisibleProperty(componentSet, stats);
}

function configureFormFieldProperties(componentSet, stats) {
  configureLabelTextProperty(componentSet, "Label", stats);
  configureNamedTextProperty(
    componentSet,
    "Description Text",
    "Description Text",
    "Use clear supporting guidance when the field needs it.",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Optional Text",
    "Optional Text",
    "Optional",
    stats,
  );
  configureHelperTextProperty(componentSet, "Helper text", stats);
  configureSharedSlotProperties(componentSet, ["Content Slot"], stats);
}

function configureNumberInputProperties(componentSet, stats) {
  configureLabelTextProperty(componentSet, "Number", stats);
  configureNamedTextProperty(
    componentSet,
    "Value Text",
    "Value Text",
    "12",
    stats,
  );
  configurePlaceholderTextProperty(componentSet, "0", stats);
  configureHelperTextProperty(componentSet, "Use a whole number.", stats);
  configureHelperVisibilityProperty(componentSet, stats);
  configureFocusVisibleProperty(componentSet, stats);
}

function configureOTPInputProperties(componentSet, stats) {
  configureLabelTextProperty(componentSet, "Verification code", stats);
  configureNamedTextProperty(
    componentSet,
    "Digit 1 Text",
    "Digit 1 Text",
    "1",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Digit 2 Text",
    "Digit 2 Text",
    "3",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Digit 3 Text",
    "Digit 3 Text",
    "5",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Digit 4 Text",
    "Digit 4 Text",
    "8",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Digit 5 Text",
    "Digit 5 Text",
    "2",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Digit 6 Text",
    "Digit 6 Text",
    "6",
    stats,
  );
  configureHelperTextProperty(componentSet, "Enter the code we sent.", stats);
  configureHelperVisibilityProperty(componentSet, stats);
  configureFocusVisibleProperty(componentSet, stats);
}

function configureComboboxProperties(componentSet, stats) {
  configureLabelTextProperty(componentSet, "Combobox", stats);
  configurePlaceholderTextProperty(componentSet, "Search or select", stats);
  configureNamedTextProperty(
    componentSet,
    "Option 1 Text",
    "Option 1 Text",
    "Metro Station",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Option 2 Text",
    "Option 2 Text",
    "Bus Stop",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Option 3 Text",
    "Option 3 Text",
    "Bike Parking",
    stats,
  );
  configureHelperTextProperty(componentSet, "Choose one option.", stats);
  configureHelperVisibilityProperty(componentSet, stats);
  configureFocusVisibleProperty(componentSet, stats);
}

function configureMultiSelectProperties(componentSet, stats) {
  configureLabelTextProperty(componentSet, "Facilities", stats);
  configurePlaceholderTextProperty(componentSet, "Select options", stats);
  configureNamedTextProperty(
    componentSet,
    "Option 1 Text",
    "Option 1 Text",
    "Metro Station",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Option 2 Text",
    "Option 2 Text",
    "Bus Stop",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Option 3 Text",
    "Option 3 Text",
    "Bike Parking",
    stats,
  );
  configureHelperTextProperty(
    componentSet,
    "Choose one or more options.",
    stats,
  );
  configureHelperVisibilityProperty(componentSet, stats);
  configureFocusVisibleProperty(componentSet, stats);
}

function configureListboxProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Option 1 Text",
    "Option 1 Text",
    "Metro Station",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Option 1 Description",
    "Option 1 Description",
    "Fast transit connections",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Option 2 Text",
    "Option 2 Text",
    "Bus Stop",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Option 2 Description",
    "Option 2 Description",
    "Frequent local service",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Option 3 Text",
    "Option 3 Text",
    "Bike Parking",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Option 3 Description",
    "Option 3 Description",
    "Secure cycle storage",
    stats,
  );
  configureFocusVisibleProperty(componentSet, stats);
}

function configureDatePickerProperties(componentSet, stats) {
  configureLabelTextProperty(componentSet, "Date", stats);
  configureNamedTextProperty(
    componentSet,
    "Value Text",
    "Value Text",
    "May 25, 2026",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Month Text",
    "Month Text",
    "May 2026",
    stats,
  );
  configureHelperTextProperty(componentSet, "Choose a date.", stats);
  configureHelperVisibilityProperty(componentSet, stats);
  configureFocusVisibleProperty(componentSet, stats);
}

function configureDateRangePickerProperties(componentSet, stats) {
  configureLabelTextProperty(componentSet, "Date range", stats);
  configureNamedTextProperty(
    componentSet,
    "Start Label Text",
    "Start Label Text",
    "Start date",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "End Label Text",
    "End Label Text",
    "End date",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Start Value Text",
    "Start Value Text",
    "May 21, 2026",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "End Value Text",
    "End Value Text",
    "May 25, 2026",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Start Month Text",
    "Start Month Text",
    "May 2026",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "End Month Text",
    "End Month Text",
    "June 2026",
    stats,
  );
  configureHelperTextProperty(componentSet, "Choose a date range.", stats);
  configureHelperVisibilityProperty(componentSet, stats);
  configureFocusVisibleProperty(componentSet, stats);
}

function configureTimePickerProperties(componentSet, stats) {
  configureLabelTextProperty(componentSet, "Time", stats);
  configureNamedTextProperty(
    componentSet,
    "Value Text",
    "Value Text",
    "09:30",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Option 1 Text",
    "Option 1 Text",
    "08:00",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Option 2 Text",
    "Option 2 Text",
    "08:30",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Option 3 Text",
    "Option 3 Text",
    "09:00",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Option 4 Text",
    "Option 4 Text",
    "09:30",
    stats,
  );
  configureHelperTextProperty(componentSet, "Choose a time.", stats);
  configureHelperVisibilityProperty(componentSet, stats);
  configureFocusVisibleProperty(componentSet, stats);
}

function configureFileUploadProperties(componentSet, stats) {
  configureLabelTextProperty(componentSet, "Attachment", stats);
  configureNamedTextProperty(
    componentSet,
    "Browse Text",
    "Browse Text",
    "Click to upload",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Drop Text",
    "Drop Text",
    "or drag and drop",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Description Text",
    "Description Text",
    "Images and PDFs up to 8 MB.",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "File Name Text",
    "File Name Text",
    "floor-plan.pdf",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "File Meta Text",
    "File Meta Text",
    "2.4 MB",
    stats,
  );
  configureHelperTextProperty(componentSet, "Upload a PDF or image.", stats);
  configureHelperVisibilityProperty(componentSet, stats);
  configureFocusVisibleProperty(componentSet, stats);
}

function configureColorPickerProperties(componentSet, stats) {
  configureLabelTextProperty(componentSet, "Brand color", stats);
  configureNamedTextProperty(
    componentSet,
    "Value Text",
    "Value Text",
    "#135BEC",
    stats,
  );
  deleteComponentPropertiesByBaseName(
    componentSet,
    ["Mode Text", "Hue Label Text", "Alpha Label Text"],
    ["TEXT"],
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Palette Text",
    "Palette Text",
    "Kozmos Design System 2.0",
    stats,
  );
  for (const [nodeName, defaultValue] of [
    ["Hue Value Text", "0"],
    ["Saturation Value Text", "0"],
    ["Lightness Value Text", "85"],
    ["Red Value Text", "19"],
    ["Green Value Text", "91"],
    ["Blue Value Text", "236"],
    ["Alpha Value Text", "100"],
    ["Alpha Unit Text", "%"],
  ]) {
    configureNamedTextProperty(
      componentSet,
      nodeName,
      nodeName,
      defaultValue,
      stats,
    );
  }
  configureHelperTextProperty(componentSet, "Choose a color.", stats);
  configureHelperVisibilityProperty(componentSet, stats);
  configureFocusVisibleProperty(componentSet, stats);
}

function configureSliderProperties(componentSet, stats) {
  configureLabelTextProperty(componentSet, "Slider", stats);
  configureFocusVisibleProperty(componentSet, stats);
}

function configureRatingProperties(_componentSet, _stats) {}

function configureStepperProperties(componentSet, stats) {
  for (let index = 1; index <= 4; index += 1) {
    configureNamedTextProperty(
      componentSet,
      `Step ${index} Text`,
      `Step ${index} Text`,
      `Step ${index}`,
      stats,
    );
  }
}

async function rebuildTextareaComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Textarea",
    componentSetName: "Textarea",
    build: buildTextareaComponent,
  });
}

async function rebuildSearchComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Search",
    componentSetName: "Search",
    build: buildSearchComponent,
  });
}

async function rebuildSelectComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Select",
    componentSetName: "Select",
    build: buildSelectComponent,
  });
}

async function rebuildSliderComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Slider",
    componentSetName: "Slider",
    build: buildSliderComponent,
  });
}

async function buildRatingComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Rating", stats);

  const existing = findComponentSetOnPage(page, "Rating");
  if (existing) {
    normalizeComponentSetNodeName(existing, "Rating", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "Rating already exists. Use Update Rating to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (const state of RATING_STATES) {
    for (const value of RATING_VALUES) {
      const component = await createRatingVariant({
        value,
        state,
        variableByName,
        fonts,
        stats,
      });
      page.appendChild(component);
      components.push(component);
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "Rating";
  componentSet.x = 80;
  componentSet.y = 7040;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "Rating");
  applyComponentSetDescription(componentSet, "Rating", false, [
    "Kozmos Rating component set generated from React Rating API.",
    "Value maps to Rating.value in Code Connect.",
    "State maps to Rating.readOnly.",
    "Max is fixed to five for the Core component.",
  ]);
  clearComponentSetContainerFill(componentSet);
  layoutRatingVariants(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureRatingProperties(componentSet, stats);
  return stats;
}

async function updateRatingComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Rating", stats);

  const existing = findComponentSetOnPage(page, "Rating");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "Rating was not found. Run Build Rating first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "Rating", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "Rating");
  applyComponentSetDescription(existing, "Rating", true, [
    "Kozmos Rating component set generated from React Rating API.",
    "Value maps to Rating.value in Code Connect.",
    "State maps to Rating.readOnly.",
    "Max is fixed to five for the Core component.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseRatingVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized Rating variant "${child.name}".`,
      );
      continue;
    }

    const key = `${props.value}/${props.state}`;
    seenKeys[key] = true;
    await updateRatingVariant(child, {
      value: props.value,
      state: props.state,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const state of RATING_STATES) {
    for (const value of RATING_VALUES) {
      const key = `${value}/${state}`;
      if (seenKeys[key]) continue;

      const component = await createRatingVariant({
        value,
        state,
        variableByName,
        fonts,
        stats,
      });
      existing.appendChild(component);
      seenKeys[key] = true;
      stats.variantsCreated += 1;
    }
  }

  layoutRatingVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureRatingProperties(existing, stats);
  return stats;
}

async function rebuildRatingComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Rating",
    componentSetName: "Rating",
    build: buildRatingComponent,
  });
}

async function buildStepperComponent() {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Stepper", stats);

  const existing = findComponentSetOnPage(page, "Stepper");
  if (existing) {
    normalizeComponentSetNodeName(existing, "Stepper", stats);
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message =
      "Stepper already exists. Use Update Stepper to preserve its node ID.";
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (const count of STEPPER_COUNTS) {
    for (const current of STEPPER_CURRENT) {
      if (stepperCurrentIndex(current) >= stepperCountNumber(count)) continue;

      const component = await createStepperVariant({
        count,
        current,
        variableByName,
        fonts,
        stats,
      });
      page.appendChild(component);
      components.push(component);
    }
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = "Stepper";
  componentSet.x = 80;
  componentSet.y = 7200;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(RUN_NAMESPACE, "component", "Stepper");
  applyComponentSetDescription(componentSet, "Stepper", false, [
    "Kozmos Stepper component set generated from React Stepper API.",
    "Count maps to the rendered steps array length.",
    "Current maps to Stepper.currentStep in Code Connect.",
    "Step text properties map to the steps array.",
  ]);
  clearComponentSetContainerFill(componentSet);
  layoutStepperVariants(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  configureStepperProperties(componentSet, stats);
  return stats;
}

async function updateStepperComponent() {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, "Stepper", stats);

  const existing = findComponentSetOnPage(page, "Stepper");
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = "Stepper was not found. Run Build Stepper first.";
    return stats;
  }
  normalizeComponentSetNodeName(existing, "Stepper", stats);

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenKeys = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(RUN_NAMESPACE, "component", "Stepper");
  applyComponentSetDescription(existing, "Stepper", true, [
    "Kozmos Stepper component set generated from React Stepper API.",
    "Count maps to the rendered steps array length.",
    "Current maps to Stepper.currentStep in Code Connect.",
    "Step text properties map to the steps array.",
    "Updated in place to preserve the Code Connect node ID.",
  ]);
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseStepperVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized Stepper variant "${child.name}".`,
      );
      continue;
    }

    const key = `${props.count}/${props.current}`;
    seenKeys[key] = true;
    await updateStepperVariant(child, {
      count: props.count,
      current: props.current,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const count of STEPPER_COUNTS) {
    for (const current of STEPPER_CURRENT) {
      if (stepperCurrentIndex(current) >= stepperCountNumber(count)) continue;

      const key = `${count}/${current}`;
      if (seenKeys[key]) continue;

      const component = await createStepperVariant({
        count,
        current,
        variableByName,
        fonts,
        stats,
      });
      existing.appendChild(component);
      seenKeys[key] = true;
      stats.variantsCreated += 1;
    }
  }

  layoutStepperVariants(existing);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  configureStepperProperties(existing, stats);
  return stats;
}

async function rebuildStepperComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Stepper",
    componentSetName: "Stepper",
    build: buildStepperComponent,
  });
}

async function buildProgressComponent() {
  return buildSingleAxisComponent({
    componentName: "Progress",
    componentSetName: "Progress",
    axisName: "Value",
    values: PROGRESS_VALUES,
    x: 80,
    y: 3700,
    xStep: 360,
    createVariant: createProgressVariant,
    configureProperties: configureProgressProperties,
    description: [
      "Kozmos Progress component set generated from React Progress API.",
      "Value maps to Progress.value in Code Connect.",
      "Progress is non-interactive and does not expose focus or disabled states.",
    ],
  });
}

async function updateProgressComponent() {
  return updateSingleAxisComponent({
    componentName: "Progress",
    componentSetName: "Progress",
    axisName: "Value",
    values: PROGRESS_VALUES,
    xStep: 360,
    createVariant: createProgressVariant,
    updateVariant: updateProgressVariant,
    parseVariantName: parseProgressVariantName,
    configureProperties: configureProgressProperties,
    description: [
      "Kozmos Progress component set generated from React Progress API.",
      "Value maps to Progress.value in Code Connect.",
      "Progress is non-interactive and does not expose focus or disabled states.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildSpinnerComponent() {
  return buildSingleAxisComponent({
    componentName: "Spinner",
    componentSetName: "Spinner",
    axisName: "Size",
    values: SPINNER_SIZES,
    x: 80,
    y: 3840,
    xStep: 96,
    createVariant: createSpinnerVariant,
    configureProperties: configureSpinnerProperties,
    description: [
      "Kozmos Spinner component set generated from React Spinner API.",
      "Size maps to Spinner.size in Code Connect.",
      "Spinner is status feedback and non-interactive.",
    ],
  });
}

async function updateSpinnerComponent() {
  return updateSingleAxisComponent({
    componentName: "Spinner",
    componentSetName: "Spinner",
    axisName: "Size",
    values: SPINNER_SIZES,
    xStep: 96,
    createVariant: createSpinnerVariant,
    updateVariant: updateSpinnerVariant,
    parseVariantName: parseSpinnerVariantName,
    configureProperties: configureSpinnerProperties,
    description: [
      "Kozmos Spinner component set generated from React Spinner API.",
      "Size maps to Spinner.size in Code Connect.",
      "Spinner is status feedback and non-interactive.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildAvatarComponent() {
  return buildSingleAxisComponent({
    componentName: "Avatar",
    componentSetName: "Avatar",
    axisName: "Content",
    values: AVATAR_CONTENT,
    x: 80,
    y: 3980,
    xStep: 120,
    createVariant: createAvatarVariant,
    configureProperties: configureAvatarProperties,
    description: [
      "Kozmos Avatar component set generated from React Avatar API.",
      "Content describes whether the Figma example shows image content or fallback initials.",
      "Image URL maps to AvatarImage.src.",
      "Alt Text maps to AvatarImage.alt.",
      "Fallback maps to AvatarFallback children in Code Connect.",
    ],
  });
}

async function updateAvatarComponent() {
  return updateSingleAxisComponent({
    componentName: "Avatar",
    componentSetName: "Avatar",
    axisName: "Content",
    values: AVATAR_CONTENT,
    xStep: 120,
    createVariant: createAvatarVariant,
    updateVariant: updateAvatarVariant,
    parseVariantName: parseAvatarVariantName,
    configureProperties: configureAvatarProperties,
    description: [
      "Kozmos Avatar component set generated from React Avatar API.",
      "Content describes whether the Figma example shows image content or fallback initials.",
      "Image URL maps to AvatarImage.src.",
      "Alt Text maps to AvatarImage.alt.",
      "Fallback maps to AvatarFallback children in Code Connect.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildAlertComponent() {
  return buildSingleAxisComponent({
    componentName: "Alert",
    componentSetName: "Alert",
    axisName: "Variant",
    values: ALERT_VARIANTS,
    x: 80,
    y: 4140,
    xStep: 400,
    createVariant: createAlertVariant,
    configureProperties: configureAlertProperties,
    description: [
      "Kozmos Alert component set generated from React Alert API.",
      "Variant maps to Alert.variant in Code Connect.",
      "Title maps to AlertTitle children.",
      "Description maps to AlertDescription children.",
      "Alert is non-interactive status content.",
    ],
  });
}

async function updateAlertComponent() {
  return updateSingleAxisComponent({
    componentName: "Alert",
    componentSetName: "Alert",
    axisName: "Variant",
    values: ALERT_VARIANTS,
    xStep: 400,
    createVariant: createAlertVariant,
    updateVariant: updateAlertVariant,
    parseVariantName: parseAlertVariantName,
    configureProperties: configureAlertProperties,
    description: [
      "Kozmos Alert component set generated from React Alert API.",
      "Variant maps to Alert.variant in Code Connect.",
      "Title maps to AlertTitle children.",
      "Description maps to AlertDescription children.",
      "Alert is non-interactive status content.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildEmptyStateComponent() {
  return buildSingleAxisComponent({
    componentName: "EmptyState",
    componentSetName: "EmptyState",
    axisName: "Content",
    values: EMPTY_STATE_CONTENT,
    x: 80,
    y: 6740,
    xStep: 420,
    createVariant: createEmptyStateVariant,
    configureProperties: configureEmptyStateProperties,
    description: [
      "Kozmos EmptyState component set generated from React EmptyState API.",
      "Content maps to whether icon and action content are present.",
      "Title Text maps to EmptyState.title.",
      "Description Text maps to EmptyState.description.",
      "Action examples compose a live Button instance.",
    ],
  });
}

async function updateEmptyStateComponent() {
  return updateSingleAxisComponent({
    componentName: "EmptyState",
    componentSetName: "EmptyState",
    axisName: "Content",
    values: EMPTY_STATE_CONTENT,
    xStep: 420,
    createVariant: createEmptyStateVariant,
    updateVariant: updateEmptyStateVariant,
    parseVariantName: parseEmptyStateVariantName,
    configureProperties: configureEmptyStateProperties,
    description: [
      "Kozmos EmptyState component set generated from React EmptyState API.",
      "Content maps to whether icon and action content are present.",
      "Title Text maps to EmptyState.title.",
      "Description Text maps to EmptyState.description.",
      "Action examples compose a live Button instance.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

async function buildToastComponent() {
  return buildSingleAxisComponent({
    componentName: "Toast",
    componentSetName: "Toast",
    axisName: "Content",
    values: TOAST_CONTENT,
    x: 80,
    y: 6480,
    xStep: 480,
    createVariant: createToastVariant,
    configureProperties: configureToastProperties,
    description: [
      "Kozmos Toast component set generated from React Toast API.",
      "Content maps to composed Toast children.",
      "Title Text maps to ToastTitle children.",
      "Description Text maps to ToastDescription children.",
      "Action Text maps to ToastAction children when present.",
    ],
  });
}

async function updateToastComponent() {
  return updateSingleAxisComponent({
    componentName: "Toast",
    componentSetName: "Toast",
    axisName: "Content",
    values: TOAST_CONTENT,
    xStep: 480,
    createVariant: createToastVariant,
    updateVariant: updateToastVariant,
    parseVariantName: parseToastVariantName,
    configureProperties: configureToastProperties,
    description: [
      "Kozmos Toast component set generated from React Toast API.",
      "Content maps to composed Toast children.",
      "Title Text maps to ToastTitle children.",
      "Description Text maps to ToastDescription children.",
      "Action Text maps to ToastAction children when present.",
      "Updated in place to preserve the Code Connect node ID.",
    ],
  });
}

// ---------------------------------------------------------------------------
// Product / SDK lane
//
// These component sets are map, wayfinding, and venue compositions. They are
// deliberately kept out of Core (see docs/figma-core-gap-audit.md) but still
// need canonical Figma nodes so Code Connect can link React, SwiftUI, and
// Compose. Each set uses one variant axis to avoid variant explosion, matching
// the convention used by the Core layout and data-display sets.
//
// Generated text reuses registered Core text-style keys rather than inventing
// new ones, so "Apply Text Styles" and the library audit keep working.
// ---------------------------------------------------------------------------

/**
 * Append a child, then apply its auto-layout sizing.
 *
 * Order matters and is easy to get wrong: `layoutSizingHorizontal = "FILL"`
 * throws when the node has no auto-layout parent, and `setLayoutSizing*`
 * swallows that error. Setting sizing before appending therefore does nothing
 * silently, and a text node with no explicit width collapses to one character
 * per line. Always go through this helper.
 */
function appendWithSizing(parent, child, horizontal, vertical) {
  parent.appendChild(child);
  if (horizontal) setLayoutSizingHorizontal(child, horizontal);
  if (vertical) setLayoutSizingVertical(child, vertical);
  return child;
}

function productSdkVariantValues(name, axisName, allowedValues) {
  const values = {};

  for (const part of name.split(",")) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    values[part.slice(0, index).trim()] = part.slice(index + 1).trim();
  }

  if (allowedValues.indexOf(values[axisName]) === -1) return null;
  return { value: values[axisName] };
}

function productSdkSurface(component, radiusToken, variableByName, stats) {
  component.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  component.strokes = [
    paintFromVariable(
      "Colors/background/200",
      "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.cornerRadius = radiusToken;
}

async function productSdkText({
  name,
  characters,
  styleKey,
  fonts,
  bold,
  fontSize,
  lineHeight,
  colorToken,
  colorFallback,
  variableByName,
  stats,
  width,
  wrap,
}) {
  const text = figma.createText();
  text.name = name;
  text.fontName = bold ? fonts.medium : fonts.regular;
  text.fontSize = fontSize;
  text.lineHeight = { unit: "PIXELS", value: lineHeight };
  text.textAutoResize = wrap ? "HEIGHT" : "TRUNCATE";
  await applyTextStyleToNodeAsync(text, styleKey, stats);
  text.characters = characters;
  text.fills = [
    paintFromVariable(colorToken, colorFallback, variableByName, stats),
  ];
  text.textAlignVertical = wrap ? "TOP" : "CENTER";

  // Give every text a real width up front. TRUNCATE and HEIGHT both need one,
  // and a later FILL that fails would otherwise leave the node one character
  // wide.
  if (typeof width === "number" && width > 0) {
    text.resizeWithoutConstraints(width, Math.max(lineHeight, 1));
  }

  return text;
}

// --- DirectionStep ---------------------------------------------------------

const DIRECTION_STEP_GLYPHS = {
  Straight: "↑",
  Left: "←",
  Right: "→",
  Destination: "◉",
};

async function createDirectionStepVariant(args) {
  const component = figma.createComponent();
  await updateDirectionStepVariant(component, args);
  return component;
}

function parseDirectionStepVariantName(name) {
  return productSdkVariantValues(name, "Type", DIRECTION_STEP_TYPES);
}

async function updateDirectionStepVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  component.name = `Type=${value}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 12;
  component.paddingLeft = 12;
  component.paddingRight = 12;
  component.paddingTop = 12;
  component.paddingBottom = 12;
  component.resizeWithoutConstraints(360, 72);
  component.clipsContent = false;
  productSdkSurface(component, 8, variableByName, stats);
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "DirectionStep");

  removeDirectChildren(component);

  const badge = figma.createFrame();
  badge.name = "Direction Icon";
  badge.layoutMode = "HORIZONTAL";
  badge.primaryAxisSizingMode = "FIXED";
  badge.counterAxisSizingMode = "FIXED";
  badge.primaryAxisAlignItems = "CENTER";
  badge.counterAxisAlignItems = "CENTER";
  badge.resizeWithoutConstraints(40, 40);
  badge.cornerRadius = 20;
  badge.fills = [
    paintFromVariable("Colors/theme/100", "#CAD9FC", variableByName, stats),
  ];
  badge.strokes = [];

  // The turn glyph is intentionally text, not an auto-mirroring icon: a left
  // turn stays a physical left turn in RTL locales.
  const glyph = await productSdkText({
    name: "Direction Glyph",
    characters: DIRECTION_STEP_GLYPHS[value] || DIRECTION_STEP_GLYPHS.Straight,
    styleKey: "badgeLabel",
    fonts,
    bold: true,
    fontSize: 18,
    lineHeight: 24,
    colorToken: "Colors/theme/700",
    colorFallback: "#0D44C2",
    variableByName,
    stats,
  });
  glyph.textAlignHorizontal = "CENTER";
  glyph.textAutoResize = "WIDTH_AND_HEIGHT";
  badge.appendChild(glyph);
  appendWithSizing(component, badge, "FIXED", "FIXED");

  const copy = figma.createFrame();
  copy.name = "Direction Copy";
  copy.layoutMode = "VERTICAL";
  copy.primaryAxisSizingMode = "AUTO";
  copy.counterAxisSizingMode = "FIXED";
  copy.itemSpacing = 2;
  copy.fills = [];
  copy.resizeWithoutConstraints(268, 44);

  const instruction = await productSdkText({
    name: "Instruction Text",
    characters: "Continue past the escalators",
    styleKey: "listItem",
    fonts,
    bold: true,
    fontSize: 14,
    lineHeight: 20,
    colorToken: "Colors/foreground/0",
    colorFallback: "#000000",
    variableByName,
    stats,
    width: 268,
  });
  bindFloatVariable(
    instruction,
    "fontSize",
    "DirectionStep/instruction/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    instruction,
    "lineHeight",
    "DirectionStep/instruction/line-height",
    variableByName,
    stats,
  );
  appendWithSizing(copy, instruction, "FILL", null);

  const meta = figma.createFrame();
  meta.name = "Direction Meta";
  meta.layoutMode = "HORIZONTAL";
  meta.primaryAxisSizingMode = "AUTO";
  meta.counterAxisSizingMode = "AUTO";
  meta.itemSpacing = 8;
  meta.fills = [];

  for (const [nodeName, characters] of [
    ["Distance Text", "40 m"],
    ["Duration Text", "1 min"],
  ]) {
    const metaText = await productSdkText({
      name: nodeName,
      characters,
      styleKey: "fieldMeta",
      fonts,
      bold: false,
      fontSize: 12,
      lineHeight: 16,
      colorToken: "Colors/foreground/400",
      colorFallback: "#5D626F",
      variableByName,
      stats,
    });
    metaText.textAutoResize = "WIDTH_AND_HEIGHT";
    bindFloatVariable(
      metaText,
      "fontSize",
      "DirectionStep/meta/font-size",
      variableByName,
      stats,
    );
    bindFloatVariable(
      metaText,
      "lineHeight",
      "DirectionStep/meta/line-height",
      variableByName,
      stats,
    );
    meta.appendChild(metaText);
  }

  copy.appendChild(meta);
  appendWithSizing(component, copy, "FILL", "HUG");
}

function configureDirectionStepProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Instruction Text",
    "Instruction Text",
    "Continue past the escalators",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Distance Text",
    "Distance Text",
    "40 m",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Duration Text",
    "Duration Text",
    "1 min",
    stats,
  );
}

const DIRECTION_STEP_DESCRIPTION = [
  "Kozmos DirectionStep generated from the React DirectionStep API.",
  "Type maps to DirectionStep.type (straight, left, right, destination).",
  "Instruction Text maps to DirectionStep.instruction.",
  "Distance Text maps to DirectionStep.distance.",
  "Duration Text maps to DirectionStep.duration.",
  "Turn glyphs never auto-mirror: a left turn stays physically left in RTL.",
];

async function buildDirectionStepComponent() {
  return buildSingleAxisComponent({
    componentName: "DirectionStep",
    componentSetName: "DirectionStep",
    axisName: "Type",
    values: DIRECTION_STEP_TYPES,
    x: 80,
    y: 9200,
    xStep: 420,
    createVariant: createDirectionStepVariant,
    configureProperties: configureDirectionStepProperties,
    autoReorganize: true,
    description: DIRECTION_STEP_DESCRIPTION,
  });
}

async function updateDirectionStepComponent() {
  return updateSingleAxisComponent({
    componentName: "DirectionStep",
    componentSetName: "DirectionStep",
    axisName: "Type",
    values: DIRECTION_STEP_TYPES,
    xStep: 420,
    createVariant: createDirectionStepVariant,
    updateVariant: updateDirectionStepVariant,
    parseVariantName: parseDirectionStepVariantName,
    configureProperties: configureDirectionStepProperties,
    autoReorganize: true,
    description: DIRECTION_STEP_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildDirectionStepComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "DirectionStep",
    componentSetName: "DirectionStep",
    build: buildDirectionStepComponent,
  });
}

// --- FloorSelector ---------------------------------------------------------

const FLOOR_SELECTOR_LABELS = ["L2", "L1", "G"];

async function createFloorSelectorVariant(args) {
  const component = figma.createComponent();
  await updateFloorSelectorVariant(component, args);
  return component;
}

function parseFloorSelectorVariantName(name) {
  return productSdkVariantValues(name, "Variant", FLOOR_SELECTOR_VARIANTS);
}

async function updateFloorSelectorVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const horizontal = value !== "VerticalList";
  const compact = value === "CompactStepper";
  const labels = compact ? ["L1"] : FLOOR_SELECTOR_LABELS;

  component.name = `Variant=${value}`;
  component.layoutMode = horizontal ? "HORIZONTAL" : "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "AUTO";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 4;
  component.paddingLeft = 4;
  component.paddingRight = 4;
  component.paddingTop = 4;
  component.paddingBottom = 4;
  component.clipsContent = false;
  productSdkSurface(component, 8, variableByName, stats);
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "FloorSelector");

  removeDirectChildren(component);

  if (compact) {
    const up = await floorSelectorStepperGlyph(
      "Stepper Up",
      "↑",
      fonts,
      variableByName,
      stats,
    );
    component.appendChild(up);
  }

  for (let index = 0; index < labels.length; index += 1) {
    // The first list entry represents the selected floor so designers can see
    // the selected treatment without a second variant axis. The compact
    // stepper shows only the current floor, so its single entry is selected.
    const selected = compact || index === 0;
    const item = figma.createFrame();
    item.name = selected ? "Floor Item Selected" : `Floor Item ${index + 1}`;
    item.layoutMode = "HORIZONTAL";
    item.primaryAxisSizingMode = "FIXED";
    item.counterAxisSizingMode = "FIXED";
    item.primaryAxisAlignItems = "CENTER";
    item.counterAxisAlignItems = "CENTER";
    item.resizeWithoutConstraints(44, 44);
    item.cornerRadius = 6;
    item.strokes = [];
    // theme/700 rather than theme/500: the label on this chip is
    // foreground/1000, which flips per theme, while theme/500 is #135BEC in
    // both. Pairing two flipping tokens keeps the label readable either way.
    item.fills = selected
      ? [
          paintFromVariable(
            "Colors/theme/700",
            "#0D44C2",
            variableByName,
            stats,
          ),
        ]
      : [];

    const label = await productSdkText({
      name: selected ? "Selected Floor Text" : `Floor ${index + 1} Text`,
      characters: labels[index],
      styleKey: "controlLabel",
      fonts,
      bold: true,
      fontSize: 14,
      lineHeight: 20,
      colorToken: selected ? "Colors/foreground/1000" : "Colors/foreground/0",
      colorFallback: selected ? "#FFFFFF" : "#000000",
      variableByName,
      stats,
    });
    label.textAlignHorizontal = "CENTER";
    label.textAutoResize = "WIDTH_AND_HEIGHT";
    bindFloatVariable(
      label,
      "fontSize",
      "FloorSelector/label/font-size",
      variableByName,
      stats,
    );
    bindFloatVariable(
      label,
      "lineHeight",
      "FloorSelector/label/line-height",
      variableByName,
      stats,
    );
    item.appendChild(label);
    appendWithSizing(component, item, "FIXED", "FIXED");
  }

  if (compact) {
    const down = await floorSelectorStepperGlyph(
      "Stepper Down",
      "↓",
      fonts,
      variableByName,
      stats,
    );
    component.appendChild(down);
  }
}

async function floorSelectorStepperGlyph(
  name,
  glyph,
  fonts,
  variableByName,
  stats,
) {
  const frame = figma.createFrame();
  frame.name = name;
  frame.layoutMode = "HORIZONTAL";
  frame.primaryAxisSizingMode = "FIXED";
  frame.counterAxisSizingMode = "FIXED";
  frame.primaryAxisAlignItems = "CENTER";
  frame.counterAxisAlignItems = "CENTER";
  frame.resizeWithoutConstraints(44, 44);
  frame.cornerRadius = 6;
  frame.fills = [];
  frame.strokes = [];

  const text = await productSdkText({
    name: `${name} Glyph`,
    characters: glyph,
    styleKey: "controlLabel",
    fonts,
    bold: true,
    fontSize: 14,
    lineHeight: 20,
    colorToken: "Colors/foreground/400",
    colorFallback: "#5D626F",
    variableByName,
    stats,
  });
  text.textAlignHorizontal = "CENTER";
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  frame.appendChild(text);
  setLayoutSizingHorizontal(frame, "FIXED");
  setLayoutSizingVertical(frame, "FIXED");
  return frame;
}

function configureFloorSelectorProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Selected Floor Text",
    "Selected Floor Text",
    FLOOR_SELECTOR_LABELS[0],
    stats,
  );
  for (let index = 1; index < FLOOR_SELECTOR_LABELS.length; index += 1) {
    const label = `Floor ${index + 1} Text`;
    configureNamedTextProperty(
      componentSet,
      label,
      label,
      FLOOR_SELECTOR_LABELS[index],
      stats,
    );
  }
}

const FLOOR_SELECTOR_DESCRIPTION = [
  "Kozmos FloorSelector generated from the React FloorSelector API.",
  "Variant maps to FloorSelector.variant (vertical-list, horizontal-list, compact-stepper).",
  "Selected Floor Text maps to the option matching FloorSelector.selectedFloor.",
  "Floor n Text maps to FloorSelector.floors[n].shortLabel.",
  "Floor identity and ordering stay product data, not Figma variants.",
];

async function buildFloorSelectorComponent() {
  return buildSingleAxisComponent({
    componentName: "FloorSelector",
    componentSetName: "FloorSelector",
    axisName: "Variant",
    values: FLOOR_SELECTOR_VARIANTS,
    x: 80,
    y: 9200,
    xStep: 320,
    createVariant: createFloorSelectorVariant,
    configureProperties: configureFloorSelectorProperties,
    autoReorganize: true,
    description: FLOOR_SELECTOR_DESCRIPTION,
  });
}

async function updateFloorSelectorComponent() {
  return updateSingleAxisComponent({
    componentName: "FloorSelector",
    componentSetName: "FloorSelector",
    axisName: "Variant",
    values: FLOOR_SELECTOR_VARIANTS,
    xStep: 320,
    createVariant: createFloorSelectorVariant,
    updateVariant: updateFloorSelectorVariant,
    parseVariantName: parseFloorSelectorVariantName,
    configureProperties: configureFloorSelectorProperties,
    autoReorganize: true,
    description: FLOOR_SELECTOR_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildFloorSelectorComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "FloorSelector",
    componentSetName: "FloorSelector",
    build: buildFloorSelectorComponent,
  });
}

// --- LocationPin -----------------------------------------------------------

function locationPinPalette(state) {
  if (state === "Featured") {
    return { fill: "Colors/emotional/alert/900", fallback: "#744D03" };
  }
  if (state === "Disabled" || state === "OffFloor") {
    return { fill: "Colors/foreground/400", fallback: "#5D626F" };
  }
  return { fill: "Colors/theme/700", fallback: "#0D44C2" };
}

function locationPinVariantCombinations() {
  const combinations = [];
  for (const state of LOCATION_PIN_STATES) {
    for (const size of LOCATION_PIN_SIZES) {
      combinations.push({ state, size });
    }
  }
  return combinations;
}

function locationPinVariantKey(props) {
  return `${props.state}/${props.size}`;
}

function parseLocationPinVariantName(name) {
  const values = parseVariantValueMap(name);
  if (
    LOCATION_PIN_STATES.indexOf(values.State) === -1 ||
    LOCATION_PIN_SIZES.indexOf(values.Size) === -1
  ) {
    return null;
  }
  return { state: values.State, size: values.Size };
}

function layoutLocationPinVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;
    const props = parseLocationPinVariantName(child.name);
    if (!props) continue;
    child.x = LOCATION_PIN_STATES.indexOf(props.state) * 160;
    child.y = LOCATION_PIN_SIZES.indexOf(props.size) * 110;
  }

  resizeComponentSetToContainChildren(componentSet);
}

async function createLocationPinVariant(args) {
  const component = figma.createComponent();
  await updateLocationPinVariant(component, args);
  return component;
}

async function updateLocationPinVariant(
  component,
  { props, variableByName, fonts, stats },
) {
  const selected = props.state === "Selected";
  const base = LOCATION_PIN_SIZE_DIAMETERS[props.size] || 32;
  // Selected pins grow as well as recolor, matching the native components so
  // selection is never carried by colour alone.
  const diameter = selected ? base + 8 : base;
  const palette = locationPinPalette(props.state);

  component.name = `State=${props.state}, Size=${props.size}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "AUTO";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 4;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.fills = [];
  component.strokes = [];
  component.clipsContent = false;
  component.opacity = props.state === "Disabled" ? 0.5 : 1;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "LocationPin");

  removeDirectChildren(component);

  const marker = figma.createFrame();
  marker.name = "Pin Marker";
  marker.layoutMode = "HORIZONTAL";
  marker.primaryAxisSizingMode = "FIXED";
  marker.counterAxisSizingMode = "FIXED";
  marker.primaryAxisAlignItems = "CENTER";
  marker.counterAxisAlignItems = "CENTER";
  marker.resizeWithoutConstraints(diameter, diameter);
  marker.cornerRadius = diameter / 2;
  // Off-floor pins invert to a hollow ring: the fill drops out and the brand
  // colour moves to the stroke. A dashed stroke on a circle this small renders
  // as a cogwheel rather than a dashed ring, so shape carries the state
  // instead — still not colour-only.
  const offFloor = props.state === "OffFloor";
  marker.fills = [
    offFloor
      ? paintFromVariable(
          "Colors/background/0",
          "#FFFFFF",
          variableByName,
          stats,
        )
      : paintFromVariable(
          palette.fill,
          palette.fallback,
          variableByName,
          stats,
        ),
  ];
  marker.strokes = [
    offFloor
      ? paintFromVariable(palette.fill, palette.fallback, variableByName, stats)
      : paintFromVariable(
          "Colors/foreground/1000",
          "#FFFFFF",
          variableByName,
          stats,
        ),
  ];
  marker.strokeWeight = offFloor ? 3 : 2;
  marker.dashPattern = [];

  const number = await productSdkText({
    name: "Number Text",
    characters: "1",
    styleKey: "badgeLabel",
    fonts,
    bold: true,
    fontSize: Math.round(diameter * 0.44),
    lineHeight: Math.round(diameter * 0.62),
    colorToken: offFloor ? palette.fill : "Colors/foreground/1000",
    colorFallback: offFloor ? palette.fallback : "#FFFFFF",
    variableByName,
    stats,
  });
  number.textAlignHorizontal = "CENTER";
  number.textAutoResize = "WIDTH_AND_HEIGHT";
  marker.appendChild(number);
  appendWithSizing(component, marker, "FIXED", "FIXED");

  const label = await productSdkText({
    name: "Label Text",
    characters: "Gate 42",
    styleKey: "fieldMeta",
    fonts,
    bold: true,
    fontSize: 12,
    lineHeight: 16,
    colorToken: "Colors/foreground/0",
    colorFallback: "#000000",
    variableByName,
    stats,
  });
  label.textAlignHorizontal = "CENTER";
  label.textAutoResize = "WIDTH_AND_HEIGHT";
  bindFloatVariable(
    label,
    "fontSize",
    "LocationPin/label/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    label,
    "lineHeight",
    "LocationPin/label/line-height",
    variableByName,
    stats,
  );
  component.appendChild(label);
}

function configureLocationPinProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Label Text",
    "Label Text",
    "Gate 42",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Number Text",
    "Number Text",
    "1",
    stats,
  );
}

const LOCATION_PIN_DESCRIPTION = [
  "Kozmos LocationPin generated from the React LocationPin API.",
  "State maps to the selected, featured, offFloor, and disabled props.",
  "Size maps to LocationPin.size (sm, md, lg).",
  "Label Text maps to LocationPin.label or externalLabel.",
  "Number Text maps to LocationPin.number.",
  "Off-floor pins use an outline treatment so state is not colour-only.",
  "Selected pins grow as well as recolor, matching SwiftUI and Compose.",
  "variant (colour role) is a token override; labelPlacement is renderer layout.",
  "Marker placement and collision handling stay with the map renderer.",
];

function locationPinComponentConfig() {
  return {
    componentName: "LocationPin",
    componentSetName: "LocationPin",
    x: 80,
    y: 9200,
    combinations: locationPinVariantCombinations,
    keyForProps: locationPinVariantKey,
    parseVariantName: parseLocationPinVariantName,
    createVariant: createLocationPinVariant,
    updateVariant: updateLocationPinVariant,
    layoutVariants: layoutLocationPinVariants,
    configureProperties: configureLocationPinProperties,
    description: LOCATION_PIN_DESCRIPTION,
  };
}

async function buildLocationPinComponent() {
  return buildPlannedMatrixComponent(locationPinComponentConfig());
}

async function updateLocationPinComponent() {
  return updatePlannedMatrixComponent(locationPinComponentConfig());
}

async function rebuildLocationPinComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "LocationPin",
    componentSetName: "LocationPin",
    build: buildLocationPinComponent,
  });
}

// --- MapView ---------------------------------------------------------------

async function createMapViewVariant(args) {
  const component = figma.createComponent();
  await updateMapViewVariant(component, args);
  return component;
}

function parseMapViewVariantName(name) {
  return productSdkVariantValues(name, "Content", MAP_VIEW_CONTENT);
}

async function updateMapViewVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  component.name = `Content=${value}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MAX";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 12;
  component.paddingLeft = 12;
  component.paddingRight = 12;
  component.paddingTop = 12;
  component.paddingBottom = 12;
  component.resizeWithoutConstraints(480, 320);
  component.clipsContent = true;
  component.cornerRadius = 8;
  // The map canvas itself is renderer output; Figma only models the surface.
  component.fills = [
    paintFromVariable(
      "Colors/background/100",
      "#E3E4E8",
      variableByName,
      stats,
    ),
  ];
  component.strokes = [
    paintFromVariable(
      "Colors/background/200",
      "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "MapView");

  removeDirectChildren(component);

  if (value === "Overlay") {
    const slot = figma.createFrame();
    slot.name = "Overlay Slot";
    slot.layoutMode = "VERTICAL";
    slot.primaryAxisSizingMode = "AUTO";
    slot.counterAxisSizingMode = "FIXED";
    slot.primaryAxisAlignItems = "CENTER";
    slot.counterAxisAlignItems = "CENTER";
    slot.itemSpacing = 0;
    slot.paddingLeft = 12;
    slot.paddingRight = 12;
    slot.paddingTop = 12;
    slot.paddingBottom = 12;
    slot.resizeWithoutConstraints(456, 64);
    slot.cornerRadius = 8;
    slot.fills = [
      paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
    ];
    slot.strokes = [
      paintFromVariable(
        "Colors/background/200",
        "#C7CAD1",
        variableByName,
        stats,
      ),
    ];
    slot.strokeWeight = 1;
    const slotLabel = await productSdkText({
      name: "Overlay Slot Text",
      characters: "Map overlay slot",
      styleKey: "cardDescription",
      fonts,
      bold: false,
      fontSize: 14,
      lineHeight: 20,
      colorToken: "Colors/foreground/400",
      colorFallback: "#5D626F",
      variableByName,
      stats,
      width: 432,
    });
    slotLabel.textAlignHorizontal = "CENTER";
    appendWithSizing(slot, slotLabel, "FILL", null);
    appendWithSizing(component, slot, "FILL", "HUG");
  }

  const attribution = await productSdkText({
    name: "Attribution Text",
    characters: "© Pointr · Map data",
    styleKey: "fieldMeta",
    fonts,
    bold: false,
    fontSize: 10,
    lineHeight: 14,
    colorToken: "Colors/foreground/400",
    colorFallback: "#5D626F",
    variableByName,
    stats,
  });
  attribution.textAutoResize = "WIDTH_AND_HEIGHT";
  bindFloatVariable(
    attribution,
    "fontSize",
    "MapView/attribution/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    attribution,
    "lineHeight",
    "MapView/attribution/line-height",
    variableByName,
    stats,
  );
  component.appendChild(attribution);
}

function configureMapViewProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Attribution Text",
    "Attribution Text",
    "© Pointr · Map data",
    stats,
  );
}

const MAP_VIEW_DESCRIPTION = [
  "Kozmos MapView generated from the React MapView API.",
  "Content maps to MapView children: Empty is the bare surface, Overlay shows the child slot.",
  "Attribution Text maps to renderer attribution supplied by the host app.",
  "mapLabel is an accessibility prop with no visual Figma counterpart.",
  "The map canvas is renderer output; Figma models only the surface and insets.",
];

async function buildMapViewComponent() {
  return buildSingleAxisComponent({
    componentName: "MapView",
    componentSetName: "MapView",
    axisName: "Content",
    values: MAP_VIEW_CONTENT,
    x: 80,
    y: 9200,
    xStep: 540,
    createVariant: createMapViewVariant,
    configureProperties: configureMapViewProperties,
    autoReorganize: true,
    description: MAP_VIEW_DESCRIPTION,
  });
}

async function updateMapViewComponent() {
  return updateSingleAxisComponent({
    componentName: "MapView",
    componentSetName: "MapView",
    axisName: "Content",
    values: MAP_VIEW_CONTENT,
    xStep: 540,
    createVariant: createMapViewVariant,
    updateVariant: updateMapViewVariant,
    parseVariantName: parseMapViewVariantName,
    configureProperties: configureMapViewProperties,
    autoReorganize: true,
    description: MAP_VIEW_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildMapViewComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "MapView",
    componentSetName: "MapView",
    build: buildMapViewComponent,
  });
}

// --- POICard ---------------------------------------------------------------

async function createPOICardVariant(args) {
  const component = figma.createComponent();
  await updatePOICardVariant(component, args);
  return component;
}

function parsePOICardVariantName(name) {
  return productSdkVariantValues(name, "Content", POI_CARD_CONTENT);
}

async function updatePOICardVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const showsMedia = value !== "Basic";
  const showsActions = value === "Full";

  component.name = `Content=${value}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 0;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(320, 200);
  component.clipsContent = true;
  productSdkSurface(component, 12, variableByName, stats);
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "POICard");

  removeDirectChildren(component);

  if (showsMedia) {
    const media = figma.createFrame();
    media.name = "Media Slot";
    media.layoutMode = "HORIZONTAL";
    media.primaryAxisSizingMode = "FIXED";
    media.counterAxisSizingMode = "FIXED";
    media.primaryAxisAlignItems = "CENTER";
    media.counterAxisAlignItems = "CENTER";
    media.resizeWithoutConstraints(320, 140);
    media.fills = [
      paintFromVariable(
        "Colors/background/100",
        "#E3E4E8",
        variableByName,
        stats,
      ),
    ];
    media.strokes = [];
    appendWithSizing(component, media, "FILL", "FIXED");
  }

  const body = figma.createFrame();
  body.name = "POI Body";
  body.layoutMode = "VERTICAL";
  body.primaryAxisSizingMode = "AUTO";
  body.counterAxisSizingMode = "FIXED";
  body.itemSpacing = 12;
  body.paddingLeft = 16;
  body.paddingRight = 16;
  body.paddingTop = 16;
  body.paddingBottom = 16;
  body.fills = [];
  body.resizeWithoutConstraints(320, 120);

  const heading = figma.createFrame();
  heading.name = "POI Heading";
  heading.layoutMode = "VERTICAL";
  heading.primaryAxisSizingMode = "AUTO";
  heading.counterAxisSizingMode = "FIXED";
  heading.itemSpacing = 2;
  heading.fills = [];
  heading.resizeWithoutConstraints(288, 48);

  const title = await productSdkText({
    name: "Title Text",
    characters: "Boots Pharmacy",
    styleKey: "cardTitle",
    fonts,
    bold: true,
    fontSize: 16,
    lineHeight: 24,
    colorToken: "Colors/foreground/0",
    colorFallback: "#000000",
    variableByName,
    stats,
    width: 288,
  });
  bindFloatVariable(
    title,
    "fontSize",
    "POICard/title/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    title,
    "lineHeight",
    "POICard/title/line-height",
    variableByName,
    stats,
  );
  appendWithSizing(heading, title, "FILL", null);

  const subtitle = await productSdkText({
    name: "Subtitle Text",
    characters: "Level 2 · Terminal B",
    styleKey: "cardDescription",
    fonts,
    bold: false,
    fontSize: 14,
    lineHeight: 20,
    colorToken: "Colors/foreground/400",
    colorFallback: "#5D626F",
    variableByName,
    stats,
    width: 288,
  });
  bindFloatVariable(
    subtitle,
    "fontSize",
    "POICard/subtitle/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    subtitle,
    "lineHeight",
    "POICard/subtitle/line-height",
    variableByName,
    stats,
  );
  appendWithSizing(heading, subtitle, "FILL", null);
  appendWithSizing(body, heading, "FILL", "HUG");

  const description = await productSdkText({
    name: "Description Text",
    characters: "Open until 9pm. Prescription collection available.",
    styleKey: "cardBody",
    fonts,
    bold: false,
    fontSize: 14,
    lineHeight: 20,
    colorToken: "Colors/foreground/400",
    colorFallback: "#5D626F",
    variableByName,
    stats,
    width: 288,
    wrap: true,
  });
  appendWithSizing(body, description, "FILL", "HUG");

  if (showsActions) {
    // Footer actions compose a live Button instance rather than a cloned
    // frame, matching the Core composition rule the audit enforces.
    const actions = figma.createFrame();
    actions.name = "Actions Slot";
    actions.layoutMode = "HORIZONTAL";
    actions.primaryAxisSizingMode = "AUTO";
    actions.counterAxisSizingMode = "AUTO";
    actions.primaryAxisAlignItems = "MIN";
    actions.counterAxisAlignItems = "CENTER";
    actions.itemSpacing = 8;
    actions.fills = [];

    const created = await createNestedComponentInstance({
      componentSetName: "Button",
      variantProperties: {
        Variant: "Default",
        Size: "Default",
        State: "Default",
      },
      name: "Primary Action",
      role: "poi-card-action",
      stats,
    });

    if (created) {
      setInstanceTextProperty(
        created.instance,
        created.componentSet,
        "Label Text",
        "Navigate",
        stats,
      );
      actions.appendChild(created.instance);
    } else {
      actions.appendChild(
        createMissingNestedComponentNode(
          "Primary Action",
          "Build Button first",
          stats,
        ),
      );
    }

    appendWithSizing(body, actions, "FILL", "HUG");
  }

  appendWithSizing(component, body, "FILL", "HUG");
}

function configurePOICardProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Title Text",
    "Title Text",
    "Boots Pharmacy",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Subtitle Text",
    "Subtitle Text",
    "Level 2 · Terminal B",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Description Text",
    "Description Text",
    "Open until 9pm. Prescription collection available.",
    stats,
  );
}

const POI_CARD_DESCRIPTION = [
  "Kozmos POICard generated from the React POICard API.",
  "Content maps to composed POICard anatomy: Basic, Media, and Full.",
  "Title Text maps to POICard.title.",
  "Subtitle Text maps to POICard.subtitle.",
  "Description Text maps to POICard.description.",
  "Media Slot maps to imageUrl/imageAlt supplied by the host app.",
  "Full cards compose a live Button instance for POICard.actions.",
];

async function buildPOICardComponent() {
  return buildSingleAxisComponent({
    componentName: "POICard",
    componentSetName: "POICard",
    axisName: "Content",
    values: POI_CARD_CONTENT,
    x: 80,
    y: 9200,
    xStep: 380,
    createVariant: createPOICardVariant,
    configureProperties: configurePOICardProperties,
    autoReorganize: true,
    description: POI_CARD_DESCRIPTION,
  });
}

async function updatePOICardComponent() {
  return updateSingleAxisComponent({
    componentName: "POICard",
    componentSetName: "POICard",
    axisName: "Content",
    values: POI_CARD_CONTENT,
    xStep: 380,
    createVariant: createPOICardVariant,
    updateVariant: updatePOICardVariant,
    parseVariantName: parsePOICardVariantName,
    configureProperties: configurePOICardProperties,
    autoReorganize: true,
    description: POI_CARD_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildPOICardComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "POICard",
    componentSetName: "POICard",
    build: buildPOICardComponent,
  });
}

// --- WayfindingCard --------------------------------------------------------

async function createWayfindingCardVariant(args) {
  const component = figma.createComponent();
  await updateWayfindingCardVariant(component, args);
  return component;
}

function parseWayfindingCardVariantName(name) {
  return productSdkVariantValues(name, "Content", WAYFINDING_CARD_CONTENT);
}

async function updateWayfindingCardVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const titled = value === "Titled";

  component.name = `Content=${value}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 12;
  component.paddingLeft = 16;
  component.paddingRight = 16;
  component.paddingTop = 16;
  component.paddingBottom = 16;
  component.resizeWithoutConstraints(320, 160);
  component.clipsContent = false;
  productSdkSurface(component, 12, variableByName, stats);
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "WayfindingCard");

  removeDirectChildren(component);

  if (titled) {
    const header = figma.createFrame();
    header.name = "Wayfinding Header";
    header.layoutMode = "HORIZONTAL";
    header.primaryAxisSizingMode = "FIXED";
    header.counterAxisSizingMode = "AUTO";
    header.primaryAxisAlignItems = "SPACE_BETWEEN";
    header.counterAxisAlignItems = "CENTER";
    header.itemSpacing = 12;
    header.fills = [];
    header.resizeWithoutConstraints(288, 24);

    const title = await productSdkText({
      name: "Title Text",
      characters: "Route to Gate 42",
      styleKey: "cardTitle",
      fonts,
      bold: true,
      fontSize: 16,
      lineHeight: 24,
      colorToken: "Colors/foreground/0",
      colorFallback: "#000000",
      variableByName,
      stats,
      width: 236,
    });
    bindFloatVariable(
      title,
      "fontSize",
      "WayfindingCard/title/font-size",
      variableByName,
      stats,
    );
    bindFloatVariable(
      title,
      "lineHeight",
      "WayfindingCard/title/line-height",
      variableByName,
      stats,
    );
    appendWithSizing(header, title, "FILL", null);

    const created = await createNestedComponentInstance({
      componentSetName: "IconButton",
      variantProperties: {
        Variant: "Ghost",
        Size: "Default",
        State: "Default",
      },
      name: "Close Action",
      role: "wayfinding-card-close",
      stats,
    });

    header.appendChild(
      created
        ? created.instance
        : createMissingNestedComponentNode(
            "Close Action",
            "Build IconButton first",
            stats,
          ),
    );

    appendWithSizing(component, header, "FILL", "HUG");
  }

  const slot = figma.createFrame();
  slot.name = "Content Slot";
  slot.layoutMode = "VERTICAL";
  slot.primaryAxisSizingMode = "AUTO";
  slot.counterAxisSizingMode = "FIXED";
  slot.itemSpacing = 8;
  slot.fills = [];
  slot.resizeWithoutConstraints(288, 80);

  const body = await productSdkText({
    name: "Body Text",
    characters: "Head towards the escalators and continue to Level 2.",
    styleKey: "cardBody",
    fonts,
    bold: false,
    fontSize: 14,
    lineHeight: 20,
    colorToken: "Colors/foreground/400",
    colorFallback: "#5D626F",
    variableByName,
    stats,
    width: 288,
    wrap: true,
  });
  appendWithSizing(slot, body, "FILL", "HUG");
  appendWithSizing(component, slot, "FILL", "HUG");
}

function configureWayfindingCardProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Title Text",
    "Title Text",
    "Route to Gate 42",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Body Text",
    "Body Text",
    "Head towards the escalators and continue to Level 2.",
    stats,
  );
}

const WAYFINDING_CARD_DESCRIPTION = [
  "Kozmos WayfindingCard generated from the React WayfindingCard API.",
  "Content maps to the presence of WayfindingCard.title and onClose.",
  "Title Text maps to WayfindingCard.title.",
  "Body Text maps to WayfindingCard.children.",
  "Titled cards compose a live IconButton instance for onClose.",
];

async function buildWayfindingCardComponent() {
  return buildSingleAxisComponent({
    componentName: "WayfindingCard",
    componentSetName: "WayfindingCard",
    axisName: "Content",
    values: WAYFINDING_CARD_CONTENT,
    x: 80,
    y: 9200,
    xStep: 380,
    createVariant: createWayfindingCardVariant,
    configureProperties: configureWayfindingCardProperties,
    autoReorganize: true,
    description: WAYFINDING_CARD_DESCRIPTION,
  });
}

async function updateWayfindingCardComponent() {
  return updateSingleAxisComponent({
    componentName: "WayfindingCard",
    componentSetName: "WayfindingCard",
    axisName: "Content",
    values: WAYFINDING_CARD_CONTENT,
    xStep: 380,
    createVariant: createWayfindingCardVariant,
    updateVariant: updateWayfindingCardVariant,
    parseVariantName: parseWayfindingCardVariantName,
    configureProperties: configureWayfindingCardProperties,
    autoReorganize: true,
    description: WAYFINDING_CARD_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildWayfindingCardComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "WayfindingCard",
    componentSetName: "WayfindingCard",
    build: buildWayfindingCardComponent,
  });
}

// --- Product / SDK shared anatomy ------------------------------------------
//
// The sets below are mostly nested auto-layout frames over a handful of
// repeating shapes: a padded surface, a neutral slot standing in for content a
// renderer or the product owns, and a square control button. Spelling those out
// inline buried the parts that actually differ between components.

/** Configured auto-layout frame with fills and strokes cleared. */
function productSdkFrame(name, options) {
  const settings = options || {};
  const frame = figma.createFrame();
  frame.name = name;
  frame.layoutMode =
    settings.direction === "horizontal" ? "HORIZONTAL" : "VERTICAL";
  frame.primaryAxisSizingMode = settings.primarySizing || "AUTO";
  frame.counterAxisSizingMode = settings.counterSizing || "AUTO";
  frame.primaryAxisAlignItems = settings.primaryAlign || "MIN";
  frame.counterAxisAlignItems = settings.counterAlign || "MIN";
  frame.itemSpacing = settings.spacing || 0;

  const padding = settings.padding || 0;
  frame.paddingLeft =
    typeof settings.paddingLeft === "number" ? settings.paddingLeft : padding;
  frame.paddingRight =
    typeof settings.paddingRight === "number" ? settings.paddingRight : padding;
  frame.paddingTop =
    typeof settings.paddingTop === "number" ? settings.paddingTop : padding;
  frame.paddingBottom =
    typeof settings.paddingBottom === "number"
      ? settings.paddingBottom
      : padding;
  frame.fills = [];
  frame.strokes = [];
  frame.clipsContent = false;

  if (
    typeof settings.width === "number" &&
    typeof settings.height === "number"
  ) {
    frame.resizeWithoutConstraints(settings.width, settings.height);
  }

  return frame;
}

/**
 * Configure a component frame as a Product / SDK variant root.
 *
 * Every variant repeats the same seven-line preamble; the parts worth reading
 * are the anatomy that follows it.
 */
function productSdkVariantRoot(component, componentName, variantName, options) {
  const settings = options || {};
  component.name = variantName;
  component.layoutMode =
    settings.direction === "horizontal" ? "HORIZONTAL" : "VERTICAL";
  component.primaryAxisSizingMode = settings.primarySizing || "FIXED";
  component.counterAxisSizingMode = settings.counterSizing || "FIXED";
  component.primaryAxisAlignItems = settings.primaryAlign || "MIN";
  component.counterAxisAlignItems = settings.counterAlign || "MIN";
  component.itemSpacing = settings.spacing || 0;

  const padding = settings.padding || 0;
  component.paddingLeft =
    typeof settings.paddingLeft === "number" ? settings.paddingLeft : padding;
  component.paddingRight =
    typeof settings.paddingRight === "number" ? settings.paddingRight : padding;
  component.paddingTop =
    typeof settings.paddingTop === "number" ? settings.paddingTop : padding;
  component.paddingBottom =
    typeof settings.paddingBottom === "number"
      ? settings.paddingBottom
      : padding;
  component.resizeWithoutConstraints(settings.width, settings.height);
  component.clipsContent = settings.clip === true;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", componentName);
  removeDirectChildren(component);
  return component;
}

/** Neutral placeholder for content a renderer or the product owns. */
async function productSdkSlot({
  name,
  label,
  width,
  height,
  fonts,
  variableByName,
  stats,
  muted,
}) {
  const slot = productSdkFrame(name, {
    primarySizing: "FIXED",
    counterSizing: "FIXED",
    primaryAlign: "CENTER",
    counterAlign: "CENTER",
    padding: 12,
    width,
    height,
  });
  slot.cornerRadius = 8;
  slot.fills = [
    paintFromVariable(
      muted ? "Surface/100" : "Colors/background/100",
      muted ? "#F4F5F7" : "#E3E4E8",
      variableByName,
      stats,
    ),
  ];
  slot.strokes = [
    paintFromVariable(
      "Colors/background/200",
      "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  slot.strokeWeight = 1;

  const text = await productSdkText({
    name: name + " Text",
    characters: label,
    styleKey: "cardDescription",
    fonts,
    bold: false,
    fontSize: 12,
    lineHeight: 16,
    colorToken: "Colors/foreground/400",
    colorFallback: "#5D626F",
    variableByName,
    stats,
    width: Math.max(24, width - 24),
  });
  text.textAlignHorizontal = "CENTER";
  await fitProductSdkSlotLabel(slot, text, width, height, stats);
  appendWithSizing(slot, text, "FILL", null);
  return slot;
}

/**
 * Give a slot's label the room it needs, or shrink it until it has room.
 *
 * A flat 12px padding is right on a 240px content slot and impossible on a
 * 40px icon one: 24px of it left 14px for the word "Icon", which Figma
 * truncated to an ellipsis. Seven slots across five sets shipped that way --
 * "Logo" as "L", "Controls" as "Co", "Trailing" as "Tra" -- and no gate saw
 * it, because a truncated node is a normal node at a normal width.
 *
 * Measuring beats guessing: a text node set to size itself reports the exact
 * width of its own string in its own font, so ask Figma rather than modelling
 * advance widths. Step the padding down first, then the text style, and stop
 * as soon as the label fits. Both steps land on values the system already
 * uses, so nothing here invents a new size.
 */
async function fitProductSdkSlotLabel(slot, text, width, height, stats) {
  const measure = () => {
    text.textAutoResize = "WIDTH_AND_HEIGHT";
    const measured = { width: text.width, height: text.height };
    text.textAutoResize = "TRUNCATE";
    return measured;
  };
  // The 1px stroke is drawn inside the frame, so it costs a pixel each side.
  const roomAt = (pad) => width - 2 * pad - 2;

  let padding = 12;
  let size = measure();
  if (size.width > roomAt(padding)) padding = 4;
  if (size.width > roomAt(padding)) {
    await applyTextStyleToNodeAsync(text, "fieldMeta", stats);
    size = measure();
  }
  if (size.width > roomAt(padding)) {
    pushUniqueWarning(
      stats,
      "slot-label-fit:" + slot.name,
      `"${text.characters}" needs ${Math.ceil(size.width)}px but ${slot.name} offers ${roomAt(padding)}px, so it will truncate.`,
    );
  }

  // A 24px-tall slot has no room for 12px above and below either.
  const vertical = Math.max(
    0,
    Math.min(padding, Math.floor((height - size.height) / 2)),
  );
  slot.paddingLeft = padding;
  slot.paddingRight = padding;
  slot.paddingTop = vertical;
  slot.paddingBottom = vertical;
  text.resizeWithoutConstraints(
    Math.max(1, roomAt(padding)),
    Math.max(1, size.height),
  );
}

/**
 * Square or labelled map-chrome button.
 *
 * The glyph is text rather than an icon instance on purpose, matching
 * DirectionStep: these are compass and zoom affordances that must not
 * auto-mirror in RTL.
 */
async function productSdkControlButton({
  name,
  glyph,
  label,
  pressed,
  fonts,
  variableByName,
  stats,
}) {
  const labelled = typeof label === "string" && label.length > 0;
  const button = productSdkFrame(name, {
    direction: "horizontal",
    primarySizing: labelled ? "AUTO" : "FIXED",
    counterSizing: "FIXED",
    primaryAlign: "CENTER",
    counterAlign: "CENTER",
    spacing: labelled ? 8 : 0,
    paddingLeft: labelled ? 12 : 0,
    paddingRight: labelled ? 12 : 0,
    width: 44,
    height: 44,
  });
  button.cornerRadius = 8;
  button.fills = [
    paintFromVariable(
      pressed ? "Colors/theme/100" : "Surface/0",
      pressed ? "#CAD9FC" : "#FFFFFF",
      variableByName,
      stats,
    ),
  ];
  button.strokes = [
    paintFromVariable(
      pressed ? "Colors/theme/500" : "Colors/background/200",
      pressed ? "#135BEC" : "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  button.strokeWeight = 1;

  const glyphText = await productSdkText({
    name: name + " Glyph",
    characters: glyph,
    styleKey: "badgeLabel",
    fonts,
    bold: true,
    fontSize: 16,
    lineHeight: 20,
    colorToken: pressed ? "Colors/theme/700" : "Colors/foreground/0",
    colorFallback: pressed ? "#0D44C2" : "#000000",
    variableByName,
    stats,
    width: 20,
  });
  glyphText.textAlignHorizontal = "CENTER";
  glyphText.textAutoResize = "WIDTH_AND_HEIGHT";
  button.appendChild(glyphText);

  if (labelled) {
    const labelText = await productSdkText({
      name: name + " Label",
      characters: label,
      styleKey: "controlLabel",
      fonts,
      bold: false,
      fontSize: 14,
      lineHeight: 20,
      colorToken: "Colors/foreground/0",
      colorFallback: "#000000",
      variableByName,
      stats,
      width: 96,
    });
    labelText.textAutoResize = "WIDTH_AND_HEIGHT";
    button.appendChild(labelText);
  }

  return button;
}

/** Title row with an optional trailing close affordance. */
async function productSdkPanelHeader({
  title,
  titleNodeName,
  closeGlyph,
  width,
  fonts,
  variableByName,
  stats,
}) {
  const header = productSdkFrame("Panel Header", {
    direction: "horizontal",
    primarySizing: "FIXED",
    counterSizing: "AUTO",
    primaryAlign: "SPACE_BETWEEN",
    counterAlign: "CENTER",
    spacing: 12,
    width,
    height: 32,
  });

  const titleText = await productSdkText({
    name: titleNodeName,
    characters: title,
    styleKey: "cardTitle",
    fonts,
    bold: true,
    fontSize: 16,
    lineHeight: 24,
    colorToken: "Colors/foreground/0",
    colorFallback: "#000000",
    variableByName,
    stats,
    width: Math.max(24, width - (closeGlyph ? 56 : 0)),
  });
  appendWithSizing(header, titleText, "FILL", null);

  if (closeGlyph) {
    const close = await productSdkControlButton({
      name: "Close Slot",
      glyph: closeGlyph,
      fonts,
      variableByName,
      stats,
    });
    close.resizeWithoutConstraints(32, 32);
    appendWithSizing(header, close, "FIXED", "FIXED");
  }

  return header;
}

// --- AdaptiveMapShell ------------------------------------------------------

async function createAdaptiveMapShellVariant(args) {
  const component = figma.createComponent();
  await updateAdaptiveMapShellVariant(component, args);
  return component;
}

function parseAdaptiveMapShellVariantName(name) {
  return productSdkVariantValues(
    name,
    "PanelPlacement",
    ADAPTIVE_MAP_SHELL_PANEL_PLACEMENTS,
  );
}

async function updateAdaptiveMapShellVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  productSdkVariantRoot(
    component,
    "AdaptiveMapShell",
    "PanelPlacement=" + value,
    {
      spacing: 12,
      padding: 12,
      width: 760,
      height: 440,
    },
  );
  productSdkSurface(component, 12, variableByName, stats);

  const topBar = await productSdkSlot({
    name: "Top Bar Slot",
    label: "Top bar slot",
    width: 736,
    height: 48,
    fonts,
    variableByName,
    stats,
    muted: true,
  });
  appendWithSizing(component, topBar, "FILL", "FIXED");

  const body = productSdkFrame("Shell Body", {
    direction: "horizontal",
    primarySizing: "FIXED",
    counterSizing: "FIXED",
    spacing: 12,
    width: 736,
    height: 344,
  });

  // The map surface hosts the controls slot: `controls` renders above the
  // canvas, so it is modelled as a trailing-aligned child rather than a
  // sibling column, which would read as a third pane.
  const mapSurface = productSdkFrame("Map Surface", {
    direction: "horizontal",
    primarySizing: "FIXED",
    counterSizing: "FIXED",
    primaryAlign: "MAX",
    counterAlign: "MIN",
    padding: 12,
    width: 432,
    height: 344,
  });
  mapSurface.cornerRadius = 8;
  mapSurface.clipsContent = true;
  mapSurface.fills = [
    paintFromVariable(
      "Colors/background/100",
      "#E3E4E8",
      variableByName,
      stats,
    ),
  ];
  mapSurface.strokes = [
    paintFromVariable(
      "Colors/background/200",
      "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  mapSurface.strokeWeight = 1;

  const controls = await productSdkSlot({
    name: "Controls Slot",
    label: "Controls",
    width: 60,
    height: 152,
    fonts,
    variableByName,
    stats,
  });
  mapSurface.appendChild(controls);

  const panel = await productSdkSlot({
    name: "Panel Slot",
    label: "Panel slot",
    width: 280,
    height: 344,
    fonts,
    variableByName,
    stats,
    muted: true,
  });

  // Start places the panel at the inline start, End at the inline end. Both
  // are laid out in flow, so the shell mirrors correctly in RTL.
  if (value === "Start") {
    appendWithSizing(body, panel, "FIXED", "FILL");
    appendWithSizing(body, mapSurface, "FILL", "FILL");
  } else {
    appendWithSizing(body, mapSurface, "FILL", "FILL");
    appendWithSizing(body, panel, "FIXED", "FILL");
  }

  appendWithSizing(component, body, "FILL", "FILL");
}

function configureAdaptiveMapShellProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Panel Slot Text",
    "Panel Label Text",
    "Panel slot",
    stats,
  );
}

const ADAPTIVE_MAP_SHELL_DESCRIPTION = [
  "Kozmos AdaptiveMapShell generated from the React AdaptiveMapShell API.",
  "PanelPlacement maps to AdaptiveMapShell.panelPlacement (start, end).",
  "Top Bar Slot maps to AdaptiveMapShell.topBar.",
  "Panel Slot maps to AdaptiveMapShell.panel, Controls Slot to .controls.",
  "Map Surface is renderer output; mapLabel and collisionInsets have no visual counterpart.",
];

async function buildAdaptiveMapShellComponent() {
  return buildSingleAxisComponent({
    componentName: "AdaptiveMapShell",
    componentSetName: "AdaptiveMapShell",
    axisName: "PanelPlacement",
    values: ADAPTIVE_MAP_SHELL_PANEL_PLACEMENTS,
    x: 80,
    y: 11200,
    xStep: 820,
    createVariant: createAdaptiveMapShellVariant,
    configureProperties: configureAdaptiveMapShellProperties,
    autoReorganize: true,
    description: ADAPTIVE_MAP_SHELL_DESCRIPTION,
  });
}

async function updateAdaptiveMapShellComponent() {
  return updateSingleAxisComponent({
    componentName: "AdaptiveMapShell",
    componentSetName: "AdaptiveMapShell",
    axisName: "PanelPlacement",
    values: ADAPTIVE_MAP_SHELL_PANEL_PLACEMENTS,
    xStep: 820,
    createVariant: createAdaptiveMapShellVariant,
    updateVariant: updateAdaptiveMapShellVariant,
    parseVariantName: parseAdaptiveMapShellVariantName,
    configureProperties: configureAdaptiveMapShellProperties,
    autoReorganize: true,
    description: ADAPTIVE_MAP_SHELL_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildAdaptiveMapShellComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "AdaptiveMapShell",
    componentSetName: "AdaptiveMapShell",
    build: buildAdaptiveMapShellComponent,
  });
}

// --- MapControlButton ------------------------------------------------------

async function createMapControlButtonVariant(args) {
  const component = figma.createComponent();
  await updateMapControlButtonVariant(component, args);
  return component;
}

function parseMapControlButtonVariantName(name) {
  return productSdkVariantValues(
    name,
    "Presentation",
    MAP_CONTROL_BUTTON_PRESENTATIONS,
  );
}

async function updateMapControlButtonVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const labelled = value === "Labelled";
  productSdkVariantRoot(
    component,
    "MapControlButton",
    "Presentation=" + value,
    {
      direction: "horizontal",
      primarySizing: labelled ? "AUTO" : "FIXED",
      counterSizing: "FIXED",
      primaryAlign: "CENTER",
      counterAlign: "CENTER",
      spacing: labelled ? 8 : 0,
      paddingLeft: labelled ? 12 : 0,
      paddingRight: labelled ? 12 : 0,
      // 44px on both axes even when icon-only: this is the shared touch-target
      // contract, not a visual choice.
      width: 44,
      height: 44,
    },
  );
  component.cornerRadius = 8;
  component.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  component.strokes = [
    paintFromVariable(
      "Colors/background/200",
      "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;

  const glyph = await productSdkText({
    name: "Control Glyph",
    characters: "+",
    styleKey: "badgeLabel",
    fonts,
    bold: true,
    fontSize: 18,
    lineHeight: 24,
    colorToken: "Colors/foreground/0",
    colorFallback: "#000000",
    variableByName,
    stats,
    width: 20,
  });
  glyph.textAlignHorizontal = "CENTER";
  glyph.textAutoResize = "WIDTH_AND_HEIGHT";
  component.appendChild(glyph);

  if (labelled) {
    const label = await productSdkText({
      name: "Label Text",
      characters: "Zoom in",
      styleKey: "controlLabel",
      fonts,
      bold: false,
      fontSize: 14,
      lineHeight: 20,
      colorToken: "Colors/foreground/0",
      colorFallback: "#000000",
      variableByName,
      stats,
      width: 96,
    });
    label.textAutoResize = "WIDTH_AND_HEIGHT";
    component.appendChild(label);
  }
}

function configureMapControlButtonProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Label Text",
    "Label Text",
    "Zoom in",
    stats,
  );
}

const MAP_CONTROL_BUTTON_DESCRIPTION = [
  "Kozmos MapControlButton generated from the React MapControlButton API.",
  "Presentation maps to MapControlButton.presentation (icon-only, labelled).",
  "Label Text maps to MapControlButton.label, which is the accessible name in both presentations.",
  "stateLabel is appended to the accessible name and has no visual counterpart.",
  "Both presentations keep a 44px target so the touch-target contract holds.",
];

async function buildMapControlButtonComponent() {
  return buildSingleAxisComponent({
    componentName: "MapControlButton",
    componentSetName: "MapControlButton",
    axisName: "Presentation",
    values: MAP_CONTROL_BUTTON_PRESENTATIONS,
    x: 80,
    y: 11800,
    xStep: 240,
    createVariant: createMapControlButtonVariant,
    configureProperties: configureMapControlButtonProperties,
    autoReorganize: true,
    description: MAP_CONTROL_BUTTON_DESCRIPTION,
  });
}

async function updateMapControlButtonComponent() {
  return updateSingleAxisComponent({
    componentName: "MapControlButton",
    componentSetName: "MapControlButton",
    axisName: "Presentation",
    values: MAP_CONTROL_BUTTON_PRESENTATIONS,
    xStep: 240,
    createVariant: createMapControlButtonVariant,
    updateVariant: updateMapControlButtonVariant,
    parseVariantName: parseMapControlButtonVariantName,
    configureProperties: configureMapControlButtonProperties,
    autoReorganize: true,
    description: MAP_CONTROL_BUTTON_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildMapControlButtonComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "MapControlButton",
    componentSetName: "MapControlButton",
    build: buildMapControlButtonComponent,
  });
}

// --- MapControlsGroup ------------------------------------------------------

const MAP_CONTROLS_GROUP_BUTTONS = [
  { name: "Zoom In Button", glyph: "+" },
  { name: "Zoom Out Button", glyph: "−" },
  { name: "Compass Button", glyph: "◈" },
];

async function createMapControlsGroupVariant(args) {
  const component = figma.createComponent();
  await updateMapControlsGroupVariant(component, args);
  return component;
}

function parseMapControlsGroupVariantName(name) {
  return productSdkVariantValues(
    name,
    "LocationPresentation",
    MAP_CONTROLS_GROUP_PRESENTATIONS,
  );
}

async function updateMapControlsGroupVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const labelled = value === "Labelled";
  const width = labelled ? 188 : 44;
  productSdkVariantRoot(
    component,
    "MapControlsGroup",
    "LocationPresentation=" + value,
    {
      primarySizing: "AUTO",
      counterSizing: "FIXED",
      counterAlign: "MAX",
      spacing: 8,
      width,
      height: 188,
    },
  );
  component.fills = [];
  component.strokes = [];

  for (const spec of MAP_CONTROLS_GROUP_BUTTONS) {
    const button = await productSdkControlButton({
      name: spec.name,
      glyph: spec.glyph,
      fonts,
      variableByName,
      stats,
    });
    appendWithSizing(component, button, "FIXED", "FIXED");
  }

  // Only the location control follows the axis. The zoom and compass controls
  // are always icon-only in the React component.
  const location = await productSdkControlButton({
    name: "My Location Button",
    glyph: "◎",
    label: labelled ? "My location" : null,
    fonts,
    variableByName,
    stats,
  });
  appendWithSizing(component, location, labelled ? "HUG" : "FIXED", "FIXED");
}

function configureMapControlsGroupProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "My Location Button Label",
    "Location Label Text",
    "My location",
    stats,
  );
}

const MAP_CONTROLS_GROUP_DESCRIPTION = [
  "Kozmos MapControlsGroup generated from the React MapControlsGroup API.",
  "LocationPresentation maps to MapControlsGroup.locationPresentation (icon-only, labelled).",
  "Location Label Text maps to MapControlsGroup.locationLabel.",
  "Zoom and compass controls are always icon-only, matching the React component.",
  "compassBearing rotates the renderer's compass and has no variant counterpart.",
];

async function buildMapControlsGroupComponent() {
  return buildSingleAxisComponent({
    componentName: "MapControlsGroup",
    componentSetName: "MapControlsGroup",
    axisName: "LocationPresentation",
    values: MAP_CONTROLS_GROUP_PRESENTATIONS,
    x: 80,
    y: 12200,
    xStep: 300,
    createVariant: createMapControlsGroupVariant,
    configureProperties: configureMapControlsGroupProperties,
    autoReorganize: true,
    description: MAP_CONTROLS_GROUP_DESCRIPTION,
  });
}

async function updateMapControlsGroupComponent() {
  return updateSingleAxisComponent({
    componentName: "MapControlsGroup",
    componentSetName: "MapControlsGroup",
    axisName: "LocationPresentation",
    values: MAP_CONTROLS_GROUP_PRESENTATIONS,
    xStep: 300,
    createVariant: createMapControlsGroupVariant,
    updateVariant: updateMapControlsGroupVariant,
    parseVariantName: parseMapControlsGroupVariantName,
    configureProperties: configureMapControlsGroupProperties,
    autoReorganize: true,
    description: MAP_CONTROLS_GROUP_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildMapControlsGroupComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "MapControlsGroup",
    componentSetName: "MapControlsGroup",
    build: buildMapControlsGroupComponent,
  });
}

// --- MapOverlay ------------------------------------------------------------

async function createMapOverlayVariant(args) {
  const component = figma.createComponent();
  await updateMapOverlayVariant(component, args);
  return component;
}

function parseMapOverlayVariantName(name) {
  return productSdkVariantValues(name, "Width", MAP_OVERLAY_WIDTHS);
}

async function updateMapOverlayVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const width = MAP_OVERLAY_WIDTH_SIZES[value] || MAP_OVERLAY_WIDTH_SIZES.Auto;
  productSdkVariantRoot(component, "MapOverlay", "Width=" + value, {
    primarySizing: "AUTO",
    counterSizing: "FIXED",
    spacing: 8,
    padding: 12,
    width,
    height: 96,
  });
  productSdkSurface(component, 12, variableByName, stats);

  const title = await productSdkText({
    name: "Overlay Title Text",
    characters: "Overlay",
    styleKey: "cardTitle",
    fonts,
    bold: true,
    fontSize: 14,
    lineHeight: 20,
    colorToken: "Colors/foreground/0",
    colorFallback: "#000000",
    variableByName,
    stats,
    width: width - 24,
  });
  appendWithSizing(component, title, "FILL", null);

  const slot = await productSdkSlot({
    name: "Overlay Content Slot",
    label: "Overlay content",
    width: width - 24,
    height: 44,
    fonts,
    variableByName,
    stats,
    muted: true,
  });
  appendWithSizing(component, slot, "FILL", "FIXED");
}

function configureMapOverlayProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Overlay Title Text",
    "Overlay Title Text",
    "Overlay",
    stats,
  );
}

const MAP_OVERLAY_DESCRIPTION = [
  "Kozmos MapOverlay generated from the React MapOverlay API.",
  "Width maps to MapOverlay.width (auto, sm, md, lg, full).",
  "Overlay Content Slot maps to MapOverlay children.",
  "position is renderer placement against the map canvas, so it stays out of the variant matrix; crossing 6 positions with 5 widths would produce 30 variants.",
  "collisionInsets are supplied by the map adapter and have no visual counterpart.",
];

async function buildMapOverlayComponent() {
  return buildSingleAxisComponent({
    componentName: "MapOverlay",
    componentSetName: "MapOverlay",
    axisName: "Width",
    values: MAP_OVERLAY_WIDTHS,
    x: 80,
    y: 12600,
    xStep: 720,
    createVariant: createMapOverlayVariant,
    configureProperties: configureMapOverlayProperties,
    autoReorganize: true,
    description: MAP_OVERLAY_DESCRIPTION,
  });
}

async function updateMapOverlayComponent() {
  return updateSingleAxisComponent({
    componentName: "MapOverlay",
    componentSetName: "MapOverlay",
    axisName: "Width",
    values: MAP_OVERLAY_WIDTHS,
    xStep: 720,
    createVariant: createMapOverlayVariant,
    updateVariant: updateMapOverlayVariant,
    parseVariantName: parseMapOverlayVariantName,
    configureProperties: configureMapOverlayProperties,
    autoReorganize: true,
    description: MAP_OVERLAY_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildMapOverlayComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "MapOverlay",
    componentSetName: "MapOverlay",
    build: buildMapOverlayComponent,
  });
}

// --- POIDetailPanel --------------------------------------------------------

// The glyph used to be actionLabel.charAt(0), which put a literal "N", "S"
// and "S" on the three buttons -- two of them identical, and all three reading
// as a placeholder someone forgot to fill in. Every other builder here uses a
// real mark, so these do too; the star matches SaveLocationCard's.
const POI_DETAIL_PANEL_ACTION_GLYPHS = {
  Navigate: "→",
  Save: "☆",
  Share: "↗",
};

async function createPOIDetailPanelVariant(args) {
  const component = figma.createComponent();
  await updatePOIDetailPanelVariant(component, args);
  return component;
}

function parsePOIDetailPanelVariantName(name) {
  return productSdkVariantValues(
    name,
    "Presentation",
    POI_DETAIL_PANEL_PRESENTATIONS,
  );
}

async function updatePOIDetailPanelVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const width = 380;
  const contentWidth = width - 32;
  productSdkVariantRoot(component, "POIDetailPanel", "Presentation=" + value, {
    primarySizing: "AUTO",
    counterSizing: "FIXED",
    spacing: 12,
    padding: 16,
    width,
    height: 420,
  });
  productSdkSurface(component, 12, variableByName, stats);

  // Sheet is anchored to the bottom edge, so only its top corners round and it
  // carries a grabber. Inline sits in the document flow with no elevation
  // affordance at all. Panel is the free-standing docked card.
  if (value === "Sheet") {
    component.cornerRadius = 0;
    component.topLeftRadius = 16;
    component.topRightRadius = 16;

    const grabber = productSdkFrame("Grabber", {
      primarySizing: "FIXED",
      counterSizing: "FIXED",
      width: 40,
      height: 4,
    });
    grabber.cornerRadius = 2;
    grabber.fills = [
      paintFromVariable(
        "Colors/background/200",
        "#C7CAD1",
        variableByName,
        stats,
      ),
    ];
    const grabberRow = productSdkFrame("Grabber Row", {
      direction: "horizontal",
      primarySizing: "FIXED",
      counterSizing: "FIXED",
      primaryAlign: "CENTER",
      counterAlign: "CENTER",
      width: contentWidth,
      height: 12,
    });
    grabberRow.appendChild(grabber);
    appendWithSizing(component, grabberRow, "FILL", "FIXED");
  } else if (value === "Inline") {
    component.strokes = [];
    component.cornerRadius = 0;
  }

  const header = await productSdkPanelHeader({
    title: "Kozmos Cafe",
    titleNodeName: "Title Text",
    closeGlyph: value === "Inline" ? null : "×",
    width: contentWidth,
    fonts,
    variableByName,
    stats,
  });
  appendWithSizing(component, header, "FILL", "HUG");

  const subtitle = await productSdkText({
    name: "Subtitle Text",
    characters: "Cafe · Level 2 · Open until 21:00",
    styleKey: "cardDescription",
    fonts,
    bold: false,
    fontSize: 12,
    lineHeight: 16,
    colorToken: "Colors/foreground/400",
    colorFallback: "#5D626F",
    variableByName,
    stats,
    width: contentWidth,
  });
  appendWithSizing(component, subtitle, "FILL", null);

  const media = await productSdkSlot({
    name: "Media Slot",
    label: "POIMediaGallery slot",
    width: contentWidth,
    height: 120,
    fonts,
    variableByName,
    stats,
  });
  appendWithSizing(component, media, "FILL", "FIXED");

  const description = await productSdkText({
    name: "Description Text",
    characters:
      "Speciality coffee and pastries beside the north atrium escalators.",
    styleKey: "cardBody",
    fonts,
    bold: false,
    fontSize: 14,
    lineHeight: 20,
    colorToken: "Colors/foreground/0",
    colorFallback: "#000000",
    variableByName,
    stats,
    width: contentWidth,
    wrap: true,
  });
  appendWithSizing(component, description, "FILL", "HUG");

  const services = await productSdkSlot({
    name: "Services Slot",
    label: "Services and access restrictions",
    width: contentWidth,
    height: 44,
    fonts,
    variableByName,
    stats,
    muted: true,
  });
  appendWithSizing(component, services, "FILL", "FIXED");

  const actions = productSdkFrame("Actions Slot", {
    direction: "horizontal",
    primarySizing: "FIXED",
    counterSizing: "FIXED",
    spacing: 8,
    width: contentWidth,
    height: 44,
  });
  for (const actionLabel of ["Navigate", "Save", "Share"]) {
    const action = await productSdkControlButton({
      name: actionLabel + " Action",
      glyph: POI_DETAIL_PANEL_ACTION_GLYPHS[actionLabel],
      label: actionLabel,
      fonts,
      variableByName,
      stats,
    });
    appendWithSizing(actions, action, "HUG", "FIXED");
  }
  appendWithSizing(component, actions, "FILL", "FIXED");
}

function configurePOIDetailPanelProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Title Text",
    "Title Text",
    "Kozmos Cafe",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Subtitle Text",
    "Subtitle Text",
    "Cafe · Level 2 · Open until 21:00",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Description Text",
    "Description Text",
    "Speciality coffee and pastries beside the north atrium escalators.",
    stats,
  );
}

const POI_DETAIL_PANEL_DESCRIPTION = [
  "Kozmos POIDetailPanel generated from the React POIDetailPanel API.",
  "Presentation maps to POIDetailPanel.presentation (inline, sheet, panel).",
  "Title Text maps to poi.name, Subtitle Text to the category, floor, and availability labels.",
  "Description Text maps to poi.description.",
  "Actions Slot maps to poi.actions with actionLabels; actionStates stay product state.",
  "Sheet rounds only its top corners and shows a grabber; Inline has no surface chrome.",
];

async function buildPOIDetailPanelComponent() {
  return buildSingleAxisComponent({
    componentName: "POIDetailPanel",
    componentSetName: "POIDetailPanel",
    axisName: "Presentation",
    values: POI_DETAIL_PANEL_PRESENTATIONS,
    x: 80,
    y: 13000,
    xStep: 440,
    createVariant: createPOIDetailPanelVariant,
    configureProperties: configurePOIDetailPanelProperties,
    autoReorganize: true,
    description: POI_DETAIL_PANEL_DESCRIPTION,
  });
}

async function updatePOIDetailPanelComponent() {
  return updateSingleAxisComponent({
    componentName: "POIDetailPanel",
    componentSetName: "POIDetailPanel",
    axisName: "Presentation",
    values: POI_DETAIL_PANEL_PRESENTATIONS,
    xStep: 440,
    createVariant: createPOIDetailPanelVariant,
    updateVariant: updatePOIDetailPanelVariant,
    parseVariantName: parsePOIDetailPanelVariantName,
    configureProperties: configurePOIDetailPanelProperties,
    autoReorganize: true,
    description: POI_DETAIL_PANEL_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildPOIDetailPanelComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "POIDetailPanel",
    componentSetName: "POIDetailPanel",
    build: buildPOIDetailPanelComponent,
  });
}

// --- BrowseCategoriesPanel -------------------------------------------------

async function createBrowseCategoriesPanelVariant(args) {
  const component = figma.createComponent();
  await updateBrowseCategoriesPanelVariant(component, args);
  return component;
}

function parseBrowseCategoriesPanelVariantName(name) {
  return productSdkVariantValues(
    name,
    "Content",
    BROWSE_CATEGORIES_PANEL_CONTENT,
  );
}

async function updateBrowseCategoriesPanelVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const width = 380;
  const contentWidth = width - 32;
  productSdkVariantRoot(
    component,
    "BrowseCategoriesPanel",
    "Content=" + value,
    {
      primarySizing: "AUTO",
      counterSizing: "FIXED",
      spacing: 12,
      padding: 16,
      width,
      height: 360,
    },
  );
  productSdkSurface(component, 12, variableByName, stats);

  const heading = await productSdkText({
    name: "Panel Label Text",
    characters: "Browse categories",
    styleKey: "cardTitle",
    fonts,
    bold: true,
    fontSize: 16,
    lineHeight: 24,
    colorToken: "Colors/foreground/0",
    colorFallback: "#000000",
    variableByName,
    stats,
    width: contentWidth,
  });
  appendWithSizing(component, heading, "FILL", null);

  if (value === "Search") {
    const search = await productSdkSlot({
      name: "Search Slot",
      label: "Search slot",
      width: contentWidth,
      height: 44,
      fonts,
      variableByName,
      stats,
      muted: true,
    });
    appendWithSizing(component, search, "FILL", "FIXED");
  }

  if (value === "Empty") {
    // The empty state replaces the grid rather than sitting beside it, so a
    // designer cannot accidentally show both.
    const empty = await productSdkSlot({
      name: "Empty State Slot",
      label: "EmptyState slot",
      width: contentWidth,
      height: 180,
      fonts,
      variableByName,
      stats,
      muted: true,
    });
    appendWithSizing(component, empty, "FILL", "FIXED");
    return;
  }

  const grid = productSdkFrame("Category Grid", {
    primarySizing: "AUTO",
    counterSizing: "FIXED",
    spacing: 8,
    width: contentWidth,
    height: 180,
  });

  for (let rowIndex = 0; rowIndex < 2; rowIndex += 1) {
    const row = productSdkFrame("Category Row " + (rowIndex + 1), {
      direction: "horizontal",
      primarySizing: "FIXED",
      counterSizing: "FIXED",
      spacing: 8,
      width: contentWidth,
      height: 84,
    });

    for (let columnIndex = 0; columnIndex < 3; columnIndex += 1) {
      const tile = await productSdkSlot({
        name: "Category Tile Slot",
        label: "Tile",
        width: 106,
        height: 84,
        fonts,
        variableByName,
        stats,
      });
      appendWithSizing(row, tile, "FILL", "FIXED");
    }

    appendWithSizing(grid, row, "FILL", "FIXED");
  }

  appendWithSizing(component, grid, "FILL", "HUG");
}

function configureBrowseCategoriesPanelProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Panel Label Text",
    "Panel Label Text",
    "Browse categories",
    stats,
  );
}

const BROWSE_CATEGORIES_PANEL_DESCRIPTION = [
  "Kozmos BrowseCategoriesPanel generated from the React BrowseCategoriesPanel API.",
  "Content covers the panel with and without the search slot, plus its empty state.",
  "Panel Label Text maps to BrowseCategoriesPanel.label.",
  "Category Tile Slots map to categories rendered through CategoryTile.",
  "Empty replaces the grid rather than sitting beside it, matching the React component.",
];

async function buildBrowseCategoriesPanelComponent() {
  return buildSingleAxisComponent({
    componentName: "BrowseCategoriesPanel",
    componentSetName: "BrowseCategoriesPanel",
    axisName: "Content",
    values: BROWSE_CATEGORIES_PANEL_CONTENT,
    x: 80,
    y: 13600,
    xStep: 440,
    createVariant: createBrowseCategoriesPanelVariant,
    configureProperties: configureBrowseCategoriesPanelProperties,
    autoReorganize: true,
    description: BROWSE_CATEGORIES_PANEL_DESCRIPTION,
  });
}

async function updateBrowseCategoriesPanelComponent() {
  return updateSingleAxisComponent({
    componentName: "BrowseCategoriesPanel",
    componentSetName: "BrowseCategoriesPanel",
    axisName: "Content",
    values: BROWSE_CATEGORIES_PANEL_CONTENT,
    xStep: 440,
    createVariant: createBrowseCategoriesPanelVariant,
    updateVariant: updateBrowseCategoriesPanelVariant,
    parseVariantName: parseBrowseCategoriesPanelVariantName,
    configureProperties: configureBrowseCategoriesPanelProperties,
    autoReorganize: true,
    description: BROWSE_CATEGORIES_PANEL_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildBrowseCategoriesPanelComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "BrowseCategoriesPanel",
    componentSetName: "BrowseCategoriesPanel",
    build: buildBrowseCategoriesPanelComponent,
  });
}

// --- CategoryTile ----------------------------------------------------------

async function createCategoryTileVariant(args) {
  const component = figma.createComponent();
  await updateCategoryTileVariant(component, args);
  return component;
}

function parseCategoryTileVariantName(name) {
  return productSdkVariantValues(name, "State", CATEGORY_TILE_STATES);
}

async function updateCategoryTileVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const selected = value === "Selected";
  const disabled = value === "Disabled";
  productSdkVariantRoot(component, "CategoryTile", "State=" + value, {
    primaryAlign: "CENTER",
    counterAlign: "CENTER",
    spacing: 8,
    padding: 12,
    width: 120,
    height: 104,
  });
  component.cornerRadius = 12;
  component.fills = [
    paintFromVariable(
      selected ? "Colors/theme/100" : "Surface/0",
      selected ? "#CAD9FC" : "#FFFFFF",
      variableByName,
      stats,
    ),
  ];
  component.strokes = [
    paintFromVariable(
      selected ? "Colors/theme/500" : "Colors/background/200",
      selected ? "#135BEC" : "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  // Selection thickens the border as well as recolouring it, so it survives a
  // theme where the two colours are close.
  component.strokeWeight = selected ? 2 : 1;
  component.opacity = disabled ? 0.4 : 1;

  const icon = await productSdkSlot({
    name: "Icon Slot",
    label: "Icon",
    width: 40,
    height: 40,
    fonts,
    variableByName,
    stats,
    muted: true,
  });
  icon.cornerRadius = 20;
  appendWithSizing(component, icon, "FIXED", "FIXED");

  const label = await productSdkText({
    name: "Label Text",
    characters: "Food",
    styleKey: "controlLabel",
    fonts,
    bold: selected,
    fontSize: 14,
    lineHeight: 20,
    colorToken: selected ? "Colors/theme/700" : "Colors/foreground/0",
    colorFallback: selected ? "#0B357F" : "#000000",
    variableByName,
    stats,
    width: 96,
  });
  label.textAlignHorizontal = "CENTER";
  appendWithSizing(component, label, "FILL", null);
}

function configureCategoryTileProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Label Text",
    "Label Text",
    "Food",
    stats,
  );
}

const CATEGORY_TILE_DESCRIPTION = [
  "Kozmos CategoryTile generated from the React CategoryTile API.",
  "State maps to category.selected and category.disabled.",
  "Label Text maps to category.label; Icon Slot maps to renderIcon(category).",
  "Selection thickens the border as well as recolouring it, so it is not colour-only.",
  "resultCountLabel is announced by product code and has no visual slot here.",
];

async function buildCategoryTileComponent() {
  return buildSingleAxisComponent({
    componentName: "CategoryTile",
    componentSetName: "CategoryTile",
    axisName: "State",
    values: CATEGORY_TILE_STATES,
    x: 80,
    y: 14200,
    xStep: 180,
    createVariant: createCategoryTileVariant,
    configureProperties: configureCategoryTileProperties,
    autoReorganize: true,
    description: CATEGORY_TILE_DESCRIPTION,
  });
}

async function updateCategoryTileComponent() {
  return updateSingleAxisComponent({
    componentName: "CategoryTile",
    componentSetName: "CategoryTile",
    axisName: "State",
    values: CATEGORY_TILE_STATES,
    xStep: 180,
    createVariant: createCategoryTileVariant,
    updateVariant: updateCategoryTileVariant,
    parseVariantName: parseCategoryTileVariantName,
    configureProperties: configureCategoryTileProperties,
    autoReorganize: true,
    description: CATEGORY_TILE_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildCategoryTileComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "CategoryTile",
    componentSetName: "CategoryTile",
    build: buildCategoryTileComponent,
  });
}

// --- POIMediaGallery -------------------------------------------------------

async function createPOIMediaGalleryVariant(args) {
  const component = figma.createComponent();
  await updatePOIMediaGalleryVariant(component, args);
  return component;
}

function parsePOIMediaGalleryVariantName(name) {
  return productSdkVariantValues(name, "Content", POI_MEDIA_GALLERY_CONTENT);
}

async function updatePOIMediaGalleryVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const width = 340;
  const contentWidth = width - 24;
  productSdkVariantRoot(component, "POIMediaGallery", "Content=" + value, {
    primarySizing: "AUTO",
    counterSizing: "FIXED",
    spacing: 8,
    padding: 12,
    width,
    height: 220,
  });
  productSdkSurface(component, 12, variableByName, stats);

  if (value === "Empty") {
    const empty = await productSdkSlot({
      name: "Empty Media Slot",
      label: "No licensed media",
      width: contentWidth,
      height: 140,
      fonts,
      variableByName,
      stats,
      muted: true,
    });
    appendWithSizing(component, empty, "FILL", "FIXED");
    return;
  }

  const media = await productSdkSlot({
    name: "Media Slot",
    label: "Venue image",
    width: contentWidth,
    height: 140,
    fonts,
    variableByName,
    stats,
  });
  appendWithSizing(component, media, "FILL", "FIXED");

  // Single media has nothing to page between, so it drops the whole control
  // row rather than showing disabled arrows.
  if (value === "Single") return;

  const controls = productSdkFrame("Gallery Controls", {
    direction: "horizontal",
    primarySizing: "FIXED",
    counterSizing: "FIXED",
    primaryAlign: "SPACE_BETWEEN",
    counterAlign: "CENTER",
    spacing: 8,
    width: contentWidth,
    height: 44,
  });

  const previous = await productSdkControlButton({
    name: "Previous Button",
    glyph: "‹",
    fonts,
    variableByName,
    stats,
  });
  appendWithSizing(controls, previous, "FIXED", "FIXED");

  const position = await productSdkText({
    name: "Position Text",
    characters: "1 of 4",
    styleKey: "fieldMeta",
    fonts,
    bold: false,
    fontSize: 12,
    lineHeight: 16,
    colorToken: "Colors/foreground/400",
    colorFallback: "#5D626F",
    variableByName,
    stats,
    width: 120,
  });
  position.textAlignHorizontal = "CENTER";
  appendWithSizing(controls, position, "FILL", null);

  const next = await productSdkControlButton({
    name: "Next Button",
    glyph: "›",
    fonts,
    variableByName,
    stats,
  });
  appendWithSizing(controls, next, "FIXED", "FIXED");

  appendWithSizing(component, controls, "FILL", "FIXED");
}

function configurePOIMediaGalleryProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Position Text",
    "Position Text",
    "1 of 4",
    stats,
  );
}

const POI_MEDIA_GALLERY_DESCRIPTION = [
  "Kozmos POIMediaGallery generated from the React POIMediaGallery API.",
  "Content covers one image, a pageable set, and the no-media case.",
  "Position Text maps to positionLabel(current, total).",
  "Media Slot maps to media[activeIndex]; alt text stays product data.",
  "Single drops the control row entirely rather than showing disabled arrows.",
];

async function buildPOIMediaGalleryComponent() {
  return buildSingleAxisComponent({
    componentName: "POIMediaGallery",
    componentSetName: "POIMediaGallery",
    axisName: "Content",
    values: POI_MEDIA_GALLERY_CONTENT,
    x: 80,
    y: 14600,
    xStep: 400,
    createVariant: createPOIMediaGalleryVariant,
    configureProperties: configurePOIMediaGalleryProperties,
    autoReorganize: true,
    description: POI_MEDIA_GALLERY_DESCRIPTION,
  });
}

async function updatePOIMediaGalleryComponent() {
  return updateSingleAxisComponent({
    componentName: "POIMediaGallery",
    componentSetName: "POIMediaGallery",
    axisName: "Content",
    values: POI_MEDIA_GALLERY_CONTENT,
    xStep: 400,
    createVariant: createPOIMediaGalleryVariant,
    updateVariant: updatePOIMediaGalleryVariant,
    parseVariantName: parsePOIMediaGalleryVariantName,
    configureProperties: configurePOIMediaGalleryProperties,
    autoReorganize: true,
    description: POI_MEDIA_GALLERY_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildPOIMediaGalleryComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "POIMediaGallery",
    componentSetName: "POIMediaGallery",
    build: buildPOIMediaGalleryComponent,
  });
}

// --- POIResultCard ---------------------------------------------------------

async function createPOIResultCardVariant(args) {
  const component = figma.createComponent();
  await updatePOIResultCardVariant(component, args);
  return component;
}

function parsePOIResultCardVariantName(name) {
  return productSdkVariantValues(name, "State", POI_RESULT_CARD_STATES);
}

async function updatePOIResultCardVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const selected = value === "Selected";
  const featured = value === "Featured";
  const unavailable = value === "Unavailable";
  const width = 360;
  productSdkVariantRoot(component, "POIResultCard", "State=" + value, {
    direction: "horizontal",
    primarySizing: "FIXED",
    counterSizing: "AUTO",
    counterAlign: "CENTER",
    spacing: 12,
    padding: 12,
    width,
    height: 92,
  });
  component.cornerRadius = 12;
  component.fills = [
    paintFromVariable(
      selected ? "Colors/theme/100" : "Surface/0",
      selected ? "#CAD9FC" : "#FFFFFF",
      variableByName,
      stats,
    ),
  ];
  component.strokes = [
    paintFromVariable(
      selected ? "Colors/theme/500" : "Colors/background/200",
      selected ? "#135BEC" : "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = selected ? 2 : 1;
  component.opacity = unavailable ? 0.55 : 1;

  const logo = await productSdkSlot({
    name: "Logo Slot",
    label: "Logo",
    width: 48,
    height: 48,
    fonts,
    variableByName,
    stats,
    muted: true,
  });
  appendWithSizing(component, logo, "FIXED", "FIXED");

  const copy = productSdkFrame("Result Copy", {
    primarySizing: "AUTO",
    counterSizing: "FIXED",
    spacing: 2,
    width: 240,
    height: 60,
  });

  const titleRow = productSdkFrame("Title Row", {
    direction: "horizontal",
    primarySizing: "FIXED",
    counterSizing: "AUTO",
    counterAlign: "CENTER",
    spacing: 6,
    width: 240,
    height: 20,
  });

  const title = await productSdkText({
    name: "Title Text",
    characters: "Kozmos Cafe",
    styleKey: "listItem",
    fonts,
    bold: true,
    fontSize: 14,
    lineHeight: 20,
    colorToken: "Colors/foreground/0",
    colorFallback: "#000000",
    variableByName,
    stats,
    width: featured ? 156 : 240,
  });
  appendWithSizing(titleRow, title, "FILL", null);

  // Featured is carried by an explicit badge rather than a colour swap: a
  // sponsored result must stay identifiable without relying on hue.
  if (featured) {
    const badge = productSdkFrame("Featured Badge", {
      direction: "horizontal",
      primarySizing: "AUTO",
      counterSizing: "FIXED",
      primaryAlign: "CENTER",
      counterAlign: "CENTER",
      paddingLeft: 8,
      paddingRight: 8,
      width: 72,
      height: 20,
    });
    badge.cornerRadius = 10;
    badge.fills = [
      paintFromVariable(
        "Colors/background/100",
        "#E3E4E8",
        variableByName,
        stats,
      ),
    ];
    const badgeText = await productSdkText({
      name: "Featured Text",
      characters: "Featured",
      styleKey: "badgeLabel",
      fonts,
      bold: true,
      fontSize: 11,
      lineHeight: 16,
      colorToken: "Colors/emotional/alert/900",
      colorFallback: "#744D03",
      variableByName,
      stats,
      width: 56,
    });
    badgeText.textAutoResize = "WIDTH_AND_HEIGHT";
    badge.appendChild(badgeText);
    appendWithSizing(titleRow, badge, "HUG", "FIXED");
  }

  appendWithSizing(copy, titleRow, "FILL", "HUG");

  const meta = await productSdkText({
    name: "Meta Text",
    characters: unavailable ? "Closed · Level 2" : "Level 2 · 3 min · 180 m",
    styleKey: "fieldMeta",
    fonts,
    bold: false,
    fontSize: 12,
    lineHeight: 16,
    colorToken: "Colors/foreground/300",
    colorFallback: "#464A53",
    variableByName,
    stats,
    width: 240,
  });
  appendWithSizing(copy, meta, "FILL", null);

  appendWithSizing(component, copy, "FILL", "HUG");
}

function configurePOIResultCardProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Title Text",
    "Title Text",
    "Kozmos Cafe",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Meta Text",
    "Meta Text",
    "Level 2 · 3 min · 180 m",
    stats,
  );
}

const POI_RESULT_CARD_DESCRIPTION = [
  "Kozmos POIResultCard generated from the React POIResultCard API.",
  "State maps to result.selected, result.featured, and result.available.",
  "Title Text maps to poi.name; Meta Text to floorLabel and travelEstimate.",
  "Featured uses a labelled badge, so a sponsored result is not identified by colour alone.",
  "Selection must stay in step with the matching LocationPin on the map.",
];

async function buildPOIResultCardComponent() {
  return buildSingleAxisComponent({
    componentName: "POIResultCard",
    componentSetName: "POIResultCard",
    axisName: "State",
    values: POI_RESULT_CARD_STATES,
    x: 80,
    y: 15000,
    xStep: 420,
    createVariant: createPOIResultCardVariant,
    configureProperties: configurePOIResultCardProperties,
    autoReorganize: true,
    description: POI_RESULT_CARD_DESCRIPTION,
  });
}

async function updatePOIResultCardComponent() {
  return updateSingleAxisComponent({
    componentName: "POIResultCard",
    componentSetName: "POIResultCard",
    axisName: "State",
    values: POI_RESULT_CARD_STATES,
    xStep: 420,
    createVariant: createPOIResultCardVariant,
    updateVariant: updatePOIResultCardVariant,
    parseVariantName: parsePOIResultCardVariantName,
    configureProperties: configurePOIResultCardProperties,
    autoReorganize: true,
    description: POI_RESULT_CARD_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildPOIResultCardComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "POIResultCard",
    componentSetName: "POIResultCard",
    build: buildPOIResultCardComponent,
  });
}

// --- POIResultList ---------------------------------------------------------

async function createPOIResultListVariant(args) {
  const component = figma.createComponent();
  await updatePOIResultListVariant(component, args);
  return component;
}

function parsePOIResultListVariantName(name) {
  return productSdkVariantValues(name, "Content", POI_RESULT_LIST_CONTENT);
}

async function updatePOIResultListVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const width = 380;
  const contentWidth = width - 24;
  productSdkVariantRoot(component, "POIResultList", "Content=" + value, {
    primarySizing: "AUTO",
    counterSizing: "FIXED",
    spacing: 8,
    padding: 12,
    width,
    height: 360,
  });
  productSdkSurface(component, 12, variableByName, stats);

  const count = await productSdkText({
    name: "Result Count Text",
    characters: value === "Empty" ? "No results" : "12 results",
    styleKey: "fieldMeta",
    fonts,
    bold: false,
    fontSize: 12,
    lineHeight: 16,
    colorToken: "Colors/foreground/400",
    colorFallback: "#5D626F",
    variableByName,
    stats,
    width: contentWidth,
  });
  appendWithSizing(component, count, "FILL", null);

  if (value === "Empty") {
    const empty = await productSdkSlot({
      name: "Empty State Slot",
      label: "EmptyState slot",
      width: contentWidth,
      height: 200,
      fonts,
      variableByName,
      stats,
      muted: true,
    });
    appendWithSizing(component, empty, "FILL", "FIXED");
    return;
  }

  for (let index = 0; index < 3; index += 1) {
    // Selected marks exactly one row, mirroring selectedPoiId being a single
    // canonical POI ID rather than a set.
    const isSelected = value === "Selected" && index === 1;
    const row = await productSdkSlot({
      name: isSelected ? "Selected Result Slot" : "Result Slot",
      label: isSelected ? "POIResultCard (selected)" : "POIResultCard",
      width: contentWidth,
      height: 92,
      fonts,
      variableByName,
      stats,
      muted: !isSelected,
    });
    if (isSelected) {
      row.strokes = [
        paintFromVariable("Colors/theme/500", "#135BEC", variableByName, stats),
      ];
      row.strokeWeight = 2;
    }
    appendWithSizing(component, row, "FILL", "FIXED");
  }
}

function configurePOIResultListProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Result Count Text",
    "Result Count Text",
    "12 results",
    stats,
  );
}

const POI_RESULT_LIST_DESCRIPTION = [
  "Kozmos POIResultList generated from the React POIResultList API.",
  "Content covers the plain list, a list with a selected row, and the empty state.",
  "Result Count Text maps to POIResultList.resultCountLabel.",
  "Result Slots map to items rendered through POIResultCard.",
  "Exactly one row is selected, mirroring selectedPoiId being a single canonical ID.",
];

async function buildPOIResultListComponent() {
  return buildSingleAxisComponent({
    componentName: "POIResultList",
    componentSetName: "POIResultList",
    axisName: "Content",
    values: POI_RESULT_LIST_CONTENT,
    x: 80,
    y: 15400,
    xStep: 440,
    createVariant: createPOIResultListVariant,
    configureProperties: configurePOIResultListProperties,
    autoReorganize: true,
    description: POI_RESULT_LIST_DESCRIPTION,
  });
}

async function updatePOIResultListComponent() {
  return updateSingleAxisComponent({
    componentName: "POIResultList",
    componentSetName: "POIResultList",
    axisName: "Content",
    values: POI_RESULT_LIST_CONTENT,
    xStep: 440,
    createVariant: createPOIResultListVariant,
    updateVariant: updatePOIResultListVariant,
    parseVariantName: parsePOIResultListVariantName,
    configureProperties: configurePOIResultListProperties,
    autoReorganize: true,
    description: POI_RESULT_LIST_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildPOIResultListComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "POIResultList",
    componentSetName: "POIResultList",
    build: buildPOIResultListComponent,
  });
}

// --- RouteOptionCard -------------------------------------------------------

async function createRouteOptionCardVariant(args) {
  const component = figma.createComponent();
  await updateRouteOptionCardVariant(component, args);
  return component;
}

function parseRouteOptionCardVariantName(name) {
  return productSdkVariantValues(name, "State", ROUTE_OPTION_CARD_STATES);
}

async function updateRouteOptionCardVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const selected = value === "Selected";
  const warning = value === "Warning";
  const unavailable = value === "Unavailable";
  const width = 340;
  const copyWidth = width - 24 - 48 - 12;
  productSdkVariantRoot(component, "RouteOptionCard", "State=" + value, {
    direction: "horizontal",
    primarySizing: "FIXED",
    counterSizing: "AUTO",
    counterAlign: "CENTER",
    spacing: 12,
    padding: 12,
    width,
    height: warning ? 104 : 84,
  });
  component.cornerRadius = 12;
  component.fills = [
    paintFromVariable(
      selected ? "Colors/theme/100" : "Surface/0",
      selected ? "#CAD9FC" : "#FFFFFF",
      variableByName,
      stats,
    ),
  ];
  component.strokes = [
    paintFromVariable(
      selected ? "Colors/theme/500" : "Colors/background/200",
      selected ? "#135BEC" : "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = selected ? 2 : 1;
  component.opacity = unavailable ? 0.55 : 1;

  const mode = await productSdkSlot({
    name: "Mode Icon Slot",
    label: "Mode",
    width: 48,
    height: 48,
    fonts,
    variableByName,
    stats,
    muted: true,
  });
  mode.cornerRadius = 24;
  appendWithSizing(component, mode, "FIXED", "FIXED");

  const copy = productSdkFrame("Option Copy", {
    primarySizing: "AUTO",
    counterSizing: "FIXED",
    spacing: 2,
    width: copyWidth,
    height: 60,
  });

  const label = await productSdkText({
    name: "Option Label Text",
    characters: "Step-free route",
    styleKey: "listItem",
    fonts,
    bold: true,
    fontSize: 14,
    lineHeight: 20,
    colorToken: "Colors/foreground/0",
    colorFallback: "#000000",
    variableByName,
    stats,
    width: copyWidth,
  });
  appendWithSizing(copy, label, "FILL", null);

  const meta = await productSdkText({
    name: "Option Meta Text",
    characters: "6 min · 320 m",
    styleKey: "fieldMeta",
    fonts,
    bold: false,
    fontSize: 12,
    lineHeight: 16,
    colorToken: "Colors/foreground/300",
    colorFallback: "#464A53",
    variableByName,
    stats,
    width: copyWidth,
  });
  appendWithSizing(copy, meta, "FILL", null);

  // The warning is its own text node rather than a recoloured meta line, so it
  // is readable by assistive technology as separate content.
  if (warning) {
    const warningText = await productSdkText({
      name: "Warning Text",
      characters: "Lift out of service",
      styleKey: "fieldMeta",
      fonts,
      bold: true,
      fontSize: 12,
      lineHeight: 16,
      colorToken: "Colors/emotional/alert/900",
      colorFallback: "#744D03",
      variableByName,
      stats,
      width: copyWidth,
    });
    appendWithSizing(copy, warningText, "FILL", null);
  }

  appendWithSizing(component, copy, "FILL", "HUG");
}

function configureRouteOptionCardProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Option Label Text",
    "Option Label Text",
    "Step-free route",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Option Meta Text",
    "Option Meta Text",
    "6 min · 320 m",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Warning Text",
    "Warning Text",
    "Lift out of service",
    stats,
  );
}

const ROUTE_OPTION_CARD_DESCRIPTION = [
  "Kozmos RouteOptionCard generated from the React RouteOptionCard API.",
  "State maps to option.selected, option.warning, and option.available.",
  "Option Label Text maps to option.label; Option Meta Text to durationLabel and distanceLabel.",
  "Warning Text maps to option.warning and is a separate node, not a recoloured meta line.",
  "preference is product data; it selects the icon rather than adding a variant axis.",
];

async function buildRouteOptionCardComponent() {
  return buildSingleAxisComponent({
    componentName: "RouteOptionCard",
    componentSetName: "RouteOptionCard",
    axisName: "State",
    values: ROUTE_OPTION_CARD_STATES,
    x: 80,
    y: 15800,
    xStep: 400,
    createVariant: createRouteOptionCardVariant,
    configureProperties: configureRouteOptionCardProperties,
    autoReorganize: true,
    description: ROUTE_OPTION_CARD_DESCRIPTION,
  });
}

async function updateRouteOptionCardComponent() {
  return updateSingleAxisComponent({
    componentName: "RouteOptionCard",
    componentSetName: "RouteOptionCard",
    axisName: "State",
    values: ROUTE_OPTION_CARD_STATES,
    xStep: 400,
    createVariant: createRouteOptionCardVariant,
    updateVariant: updateRouteOptionCardVariant,
    parseVariantName: parseRouteOptionCardVariantName,
    configureProperties: configureRouteOptionCardProperties,
    autoReorganize: true,
    description: ROUTE_OPTION_CARD_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildRouteOptionCardComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "RouteOptionCard",
    componentSetName: "RouteOptionCard",
    build: buildRouteOptionCardComponent,
  });
}

// --- RoutePreviewPanel -----------------------------------------------------

async function createRoutePreviewPanelVariant(args) {
  const component = figma.createComponent();
  await updateRoutePreviewPanelVariant(component, args);
  return component;
}

function parseRoutePreviewPanelVariantName(name) {
  return productSdkVariantValues(name, "Status", ROUTE_PREVIEW_PANEL_STATUSES);
}

async function updateRoutePreviewPanelVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const width = 380;
  const contentWidth = width - 32;
  const ready = value === "Ready";
  productSdkVariantRoot(component, "RoutePreviewPanel", "Status=" + value, {
    primarySizing: "AUTO",
    counterSizing: "FIXED",
    spacing: 12,
    padding: 16,
    width,
    height: 380,
  });
  productSdkSurface(component, 12, variableByName, stats);

  const header = await productSdkPanelHeader({
    title: "Kozmos Cafe",
    titleNodeName: "Destination Text",
    closeGlyph: "‹",
    width: contentWidth,
    fonts,
    variableByName,
    stats,
  });
  appendWithSizing(component, header, "FILL", "HUG");

  const statusText = await productSdkText({
    name: "Status Text",
    characters: ROUTE_PREVIEW_PANEL_STATUS_COPY[value],
    styleKey: "fieldMeta",
    fonts,
    bold: false,
    fontSize: 12,
    lineHeight: 16,
    colorToken:
      value === "Error"
        ? "Colors/emotional/danger/600"
        : "Colors/foreground/400",
    colorFallback: value === "Error" ? "#D41C42" : "#5D626F",
    variableByName,
    stats,
    width: contentWidth,
  });
  appendWithSizing(component, statusText, "FILL", null);

  // Only Ready has routes to choose between. Every other readiness shows its
  // status surface instead, so the panel never renders an empty option list.
  if (ready) {
    for (let index = 0; index < 3; index += 1) {
      const option = await productSdkSlot({
        name: index === 0 ? "Selected Option Slot" : "Option Slot",
        label: "RouteOptionCard",
        width: contentWidth,
        height: 84,
        fonts,
        variableByName,
        stats,
        muted: index !== 0,
      });
      if (index === 0) {
        option.strokes = [
          paintFromVariable(
            "Colors/theme/500",
            "#135BEC",
            variableByName,
            stats,
          ),
        ];
        option.strokeWeight = 2;
      }
      appendWithSizing(component, option, "FILL", "FIXED");
    }
  } else {
    const statusSlot = await productSdkSlot({
      name: "Status Slot",
      label: "Status content slot",
      width: contentWidth,
      height: 180,
      fonts,
      variableByName,
      stats,
      muted: true,
    });
    appendWithSizing(component, statusSlot, "FILL", "FIXED");
  }

  const actions = productSdkFrame("Panel Actions", {
    direction: "horizontal",
    primarySizing: "FIXED",
    counterSizing: "FIXED",
    spacing: 8,
    width: contentWidth,
    height: 44,
  });
  const back = await productSdkControlButton({
    name: "Back Button",
    glyph: "‹",
    label: "Back",
    fonts,
    variableByName,
    stats,
  });
  appendWithSizing(actions, back, "HUG", "FIXED");
  const proceed = await productSdkControlButton({
    name: "Continue Button",
    glyph: "→",
    label: "Continue",
    pressed: ready,
    fonts,
    variableByName,
    stats,
  });
  appendWithSizing(actions, proceed, "FILL", "FIXED");
  appendWithSizing(component, actions, "FILL", "FIXED");
}

function configureRoutePreviewPanelProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Destination Text",
    "Destination Text",
    "Kozmos Cafe",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Status Text",
    "Status Text",
    ROUTE_PREVIEW_PANEL_STATUS_COPY.Ready,
    stats,
  );
}

const ROUTE_PREVIEW_PANEL_DESCRIPTION = [
  "Kozmos RoutePreviewPanel generated from the React RoutePreviewPanel API.",
  "Status maps to RoutePreviewPanel.status (idle, calculating, ready, no-route, error).",
  "Destination Text maps to destinationName; Status Text to the readiness copy.",
  "Option Slots map to options rendered through RouteOptionCard.",
  "Only Ready lists options; every other readiness shows the status slot instead.",
];

async function buildRoutePreviewPanelComponent() {
  return buildSingleAxisComponent({
    componentName: "RoutePreviewPanel",
    componentSetName: "RoutePreviewPanel",
    axisName: "Status",
    values: ROUTE_PREVIEW_PANEL_STATUSES,
    x: 80,
    y: 16200,
    xStep: 440,
    createVariant: createRoutePreviewPanelVariant,
    configureProperties: configureRoutePreviewPanelProperties,
    autoReorganize: true,
    description: ROUTE_PREVIEW_PANEL_DESCRIPTION,
  });
}

async function updateRoutePreviewPanelComponent() {
  return updateSingleAxisComponent({
    componentName: "RoutePreviewPanel",
    componentSetName: "RoutePreviewPanel",
    axisName: "Status",
    values: ROUTE_PREVIEW_PANEL_STATUSES,
    xStep: 440,
    createVariant: createRoutePreviewPanelVariant,
    updateVariant: updateRoutePreviewPanelVariant,
    parseVariantName: parseRoutePreviewPanelVariantName,
    configureProperties: configureRoutePreviewPanelProperties,
    autoReorganize: true,
    description: ROUTE_PREVIEW_PANEL_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildRoutePreviewPanelComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "RoutePreviewPanel",
    componentSetName: "RoutePreviewPanel",
    build: buildRoutePreviewPanelComponent,
  });
}

// --- RouteSummary ----------------------------------------------------------

async function createRouteSummaryVariant(args) {
  const component = figma.createComponent();
  await updateRouteSummaryVariant(component, args);
  return component;
}

function parseRouteSummaryVariantName(name) {
  return productSdkVariantValues(name, "State", ROUTE_SUMMARY_STATES);
}

async function updateRouteSummaryVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const active = value === "Active";
  const width = 360;
  productSdkVariantRoot(component, "RouteSummary", "State=" + value, {
    direction: "horizontal",
    primarySizing: "FIXED",
    counterSizing: "AUTO",
    counterAlign: "CENTER",
    spacing: 12,
    padding: 12,
    width,
    height: 76,
  });
  productSdkSurface(component, 12, variableByName, stats);

  const mode = await productSdkSlot({
    name: "Transport Mode Slot",
    label: "Mode",
    width: 44,
    height: 44,
    fonts,
    variableByName,
    stats,
    muted: true,
  });
  mode.cornerRadius = 22;
  appendWithSizing(component, mode, "FIXED", "FIXED");

  const copy = productSdkFrame("Summary Copy", {
    primarySizing: "AUTO",
    counterSizing: "FIXED",
    spacing: 2,
    width: 152,
    height: 44,
  });

  const eta = await productSdkText({
    name: "ETA Text",
    characters: "6 min",
    styleKey: "cardTitle",
    fonts,
    bold: true,
    fontSize: 16,
    lineHeight: 24,
    colorToken: "Colors/foreground/0",
    colorFallback: "#000000",
    variableByName,
    stats,
    width: 152,
  });
  appendWithSizing(copy, eta, "FILL", null);

  const distance = await productSdkText({
    name: "Distance Text",
    characters: "320 m",
    styleKey: "fieldMeta",
    fonts,
    bold: false,
    fontSize: 12,
    lineHeight: 16,
    colorToken: "Colors/foreground/400",
    colorFallback: "#5D626F",
    variableByName,
    stats,
    width: 152,
  });
  appendWithSizing(copy, distance, "FILL", null);
  appendWithSizing(component, copy, "FILL", "HUG");

  // Preview offers Start; Active has already started, so it offers only End.
  // Showing both at once would let a designer build a state React never emits.
  const action = await productSdkControlButton({
    name: active ? "End Route Button" : "Start Navigation Button",
    glyph: active ? "×" : "→",
    label: active ? "End" : "Start",
    pressed: !active,
    fonts,
    variableByName,
    stats,
  });
  appendWithSizing(component, action, "HUG", "FIXED");
}

function configureRouteSummaryProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "ETA Text",
    "ETA Text",
    "6 min",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Distance Text",
    "Distance Text",
    "320 m",
    stats,
  );
}

const ROUTE_SUMMARY_DESCRIPTION = [
  "Kozmos RouteSummary generated from the React RouteSummary API.",
  "State maps to RouteSummary.state (preview, active).",
  "ETA Text maps to etaText; Distance Text maps to distanceText.",
  "Preview offers Start Navigation, Active offers End Route; they are never both present.",
  "Both strings arrive already localized from product code.",
];

async function buildRouteSummaryComponent() {
  return buildSingleAxisComponent({
    componentName: "RouteSummary",
    componentSetName: "RouteSummary",
    axisName: "State",
    values: ROUTE_SUMMARY_STATES,
    x: 80,
    y: 16800,
    xStep: 420,
    createVariant: createRouteSummaryVariant,
    configureProperties: configureRouteSummaryProperties,
    autoReorganize: true,
    description: ROUTE_SUMMARY_DESCRIPTION,
  });
}

async function updateRouteSummaryComponent() {
  return updateSingleAxisComponent({
    componentName: "RouteSummary",
    componentSetName: "RouteSummary",
    axisName: "State",
    values: ROUTE_SUMMARY_STATES,
    xStep: 420,
    createVariant: createRouteSummaryVariant,
    updateVariant: updateRouteSummaryVariant,
    parseVariantName: parseRouteSummaryVariantName,
    configureProperties: configureRouteSummaryProperties,
    autoReorganize: true,
    description: ROUTE_SUMMARY_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildRouteSummaryComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "RouteSummary",
    componentSetName: "RouteSummary",
    build: buildRouteSummaryComponent,
  });
}

// --- RoutingInputGroup -----------------------------------------------------

async function createRoutingInputGroupVariant(args) {
  const component = figma.createComponent();
  await updateRoutingInputGroupVariant(component, args);
  return component;
}

function parseRoutingInputGroupVariantName(name) {
  return productSdkVariantValues(name, "Content", ROUTING_INPUT_GROUP_CONTENT);
}

async function updateRoutingInputGroupVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const pointCount = value === "ThreePoints" ? 3 : 2;
  const width = 360;
  const fieldWidth = width - 24 - 44 - 8;
  productSdkVariantRoot(component, "RoutingInputGroup", "Content=" + value, {
    direction: "horizontal",
    primarySizing: "FIXED",
    counterSizing: "AUTO",
    counterAlign: "MIN",
    spacing: 8,
    padding: 12,
    width,
    height: pointCount * 52 + 24,
  });
  productSdkSurface(component, 12, variableByName, stats);

  const fields = productSdkFrame("Point Fields", {
    primarySizing: "AUTO",
    counterSizing: "FIXED",
    spacing: 8,
    width: fieldWidth,
    height: pointCount * 52,
  });

  const labels = ["Start", "Add stop", "Destination"];
  for (let index = 0; index < pointCount; index += 1) {
    // Three points inserts the intermediate stop between start and
    // destination, so the labels track the route order, not the array index.
    const label =
      pointCount === 3 ? labels[index] : labels[index === 0 ? 0 : 2];
    const field = await productSdkSlot({
      name: "Point Field Slot",
      label,
      width: fieldWidth,
      height: 44,
      fonts,
      variableByName,
      stats,
      muted: true,
    });
    appendWithSizing(fields, field, "FILL", "FIXED");
  }

  appendWithSizing(component, fields, "FILL", "HUG");

  const swap = await productSdkControlButton({
    name: "Swap Button",
    glyph: "⇅",
    fonts,
    variableByName,
    stats,
  });
  appendWithSizing(component, swap, "FIXED", "FIXED");
}

function configureRoutingInputGroupProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Point Field Slot Text",
    "Point Label Text",
    "Start",
    stats,
  );
}

const ROUTING_INPUT_GROUP_DESCRIPTION = [
  "Kozmos RoutingInputGroup generated from the React RoutingInputGroup API.",
  "Content covers the two-point origin/destination case and an added intermediate stop.",
  "Point Field Slots map to points; Point Label Text maps to point.placeholder.",
  "Swap Button maps to onSwap; onAddPoint and onRemovePoint are behaviour.",
  "Labels follow route order, so a third point reads as a stop between start and destination.",
];

async function buildRoutingInputGroupComponent() {
  return buildSingleAxisComponent({
    componentName: "RoutingInputGroup",
    componentSetName: "RoutingInputGroup",
    axisName: "Content",
    values: ROUTING_INPUT_GROUP_CONTENT,
    x: 80,
    y: 17200,
    xStep: 420,
    createVariant: createRoutingInputGroupVariant,
    configureProperties: configureRoutingInputGroupProperties,
    autoReorganize: true,
    description: ROUTING_INPUT_GROUP_DESCRIPTION,
  });
}

async function updateRoutingInputGroupComponent() {
  return updateSingleAxisComponent({
    componentName: "RoutingInputGroup",
    componentSetName: "RoutingInputGroup",
    axisName: "Content",
    values: ROUTING_INPUT_GROUP_CONTENT,
    xStep: 420,
    createVariant: createRoutingInputGroupVariant,
    updateVariant: updateRoutingInputGroupVariant,
    parseVariantName: parseRoutingInputGroupVariantName,
    configureProperties: configureRoutingInputGroupProperties,
    autoReorganize: true,
    description: ROUTING_INPUT_GROUP_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildRoutingInputGroupComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "RoutingInputGroup",
    componentSetName: "RoutingInputGroup",
    build: buildRoutingInputGroupComponent,
  });
}

// --- SaveLocationCard ------------------------------------------------------

async function createSaveLocationCardVariant(args) {
  const component = figma.createComponent();
  await updateSaveLocationCardVariant(component, args);
  return component;
}

function parseSaveLocationCardVariantName(name) {
  return productSdkVariantValues(name, "State", SAVE_LOCATION_CARD_STATES);
}

async function updateSaveLocationCardVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const saved = value === "Saved";
  const width = 340;
  const contentWidth = width - 24;
  productSdkVariantRoot(component, "SaveLocationCard", "State=" + value, {
    primarySizing: "AUTO",
    counterSizing: "FIXED",
    spacing: 8,
    padding: 12,
    width,
    height: 160,
  });
  productSdkSurface(component, 12, variableByName, stats);

  const title = await productSdkText({
    name: "Title Text",
    characters: "Save this location",
    styleKey: "cardTitle",
    fonts,
    bold: true,
    fontSize: 16,
    lineHeight: 24,
    colorToken: "Colors/foreground/0",
    colorFallback: "#000000",
    variableByName,
    stats,
    width: contentWidth,
  });
  appendWithSizing(component, title, "FILL", null);

  const description = await productSdkText({
    name: "Description Text",
    characters: "Keep it in your saved places for quick routing later.",
    styleKey: "cardDescription",
    fonts,
    bold: false,
    fontSize: 14,
    lineHeight: 20,
    colorToken: "Colors/foreground/400",
    colorFallback: "#5D626F",
    variableByName,
    stats,
    width: contentWidth,
    wrap: true,
  });
  appendWithSizing(component, description, "FILL", "HUG");

  const actions = productSdkFrame("Card Actions", {
    direction: "horizontal",
    primarySizing: "FIXED",
    counterSizing: "FIXED",
    spacing: 8,
    width: contentWidth,
    height: 44,
  });

  // Saved is a filled glyph plus a changed label, not just a tint: a saved
  // state that reads only as a colour swap fails at a glance.
  const save = await productSdkControlButton({
    name: "Save Toggle Button",
    glyph: saved ? "★" : "☆",
    label: saved ? "Saved" : "Save",
    pressed: saved,
    fonts,
    variableByName,
    stats,
  });
  appendWithSizing(actions, save, "FILL", "FIXED");

  const route = await productSdkControlButton({
    name: "Route Button",
    glyph: "→",
    fonts,
    variableByName,
    stats,
  });
  appendWithSizing(actions, route, "FIXED", "FIXED");

  const note = await productSdkControlButton({
    name: "Edit Note Button",
    glyph: "✎",
    fonts,
    variableByName,
    stats,
  });
  appendWithSizing(actions, note, "FIXED", "FIXED");

  appendWithSizing(component, actions, "FILL", "FIXED");
}

function configureSaveLocationCardProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Title Text",
    "Title Text",
    "Save this location",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Description Text",
    "Description Text",
    "Keep it in your saved places for quick routing later.",
    stats,
  );
}

const SAVE_LOCATION_CARD_DESCRIPTION = [
  "Kozmos SaveLocationCard generated from the React SaveLocationCard API.",
  "State maps to SaveLocationCard.isSaved.",
  "Title Text maps to title; Description Text maps to description.",
  "Save Toggle Button maps to onSaveToggle, Route to onRouteToLocation, Edit Note to onEditNote.",
  "Saved changes the glyph and the label, so the state is not carried by colour alone.",
];

async function buildSaveLocationCardComponent() {
  return buildSingleAxisComponent({
    componentName: "SaveLocationCard",
    componentSetName: "SaveLocationCard",
    axisName: "State",
    values: SAVE_LOCATION_CARD_STATES,
    x: 80,
    y: 17600,
    xStep: 400,
    createVariant: createSaveLocationCardVariant,
    configureProperties: configureSaveLocationCardProperties,
    autoReorganize: true,
    description: SAVE_LOCATION_CARD_DESCRIPTION,
  });
}

async function updateSaveLocationCardComponent() {
  return updateSingleAxisComponent({
    componentName: "SaveLocationCard",
    componentSetName: "SaveLocationCard",
    axisName: "State",
    values: SAVE_LOCATION_CARD_STATES,
    xStep: 400,
    createVariant: createSaveLocationCardVariant,
    updateVariant: updateSaveLocationCardVariant,
    parseVariantName: parseSaveLocationCardVariantName,
    configureProperties: configureSaveLocationCardProperties,
    autoReorganize: true,
    description: SAVE_LOCATION_CARD_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildSaveLocationCardComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "SaveLocationCard",
    componentSetName: "SaveLocationCard",
    build: buildSaveLocationCardComponent,
  });
}

// --- UserLocationMarker ----------------------------------------------------

async function createUserLocationMarkerVariant(args) {
  const component = figma.createComponent();
  await updateUserLocationMarkerVariant(component, args);
  return component;
}

function parseUserLocationMarkerVariantName(name) {
  return productSdkVariantValues(
    name,
    "Heading",
    USER_LOCATION_MARKER_HEADINGS,
  );
}

async function updateUserLocationMarkerVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const showHeading = value === "Visible";
  productSdkVariantRoot(component, "UserLocationMarker", "Heading=" + value, {
    primaryAlign: "CENTER",
    counterAlign: "CENTER",
    width: 64,
    height: 64,
  });
  component.fills = [];
  component.strokes = [];

  // The accuracy halo is the outer disc; the dot is the fix itself. Both are
  // ellipses rather than text so the marker scales cleanly on the canvas.
  const halo = figma.createEllipse();
  halo.name = "Accuracy Halo";
  halo.resizeWithoutConstraints(64, 64);
  halo.fills = [
    paintFromVariable("Colors/theme/100", "#CAD9FC", variableByName, stats),
  ];
  halo.opacity = 0.5;
  halo.strokes = [];
  component.appendChild(halo);
  halo.x = 0;
  halo.y = 0;

  if (showHeading) {
    // Heading is a distinct wedge on top of the halo, so a marker with a known
    // bearing is distinguishable from one without at any zoom level.
    const cone = figma.createPolygon();
    cone.name = "Heading Cone";
    cone.pointCount = 3;
    cone.resizeWithoutConstraints(28, 24);
    cone.fills = [
      paintFromVariable("Colors/theme/500", "#135BEC", variableByName, stats),
    ];
    cone.strokes = [];
    component.appendChild(cone);
    cone.x = 18;
    cone.y = 2;
  }

  const dot = figma.createEllipse();
  dot.name = "Location Dot";
  dot.resizeWithoutConstraints(20, 20);
  dot.fills = [
    paintFromVariable("Colors/theme/500", "#135BEC", variableByName, stats),
  ];
  dot.strokes = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  dot.strokeWeight = 3;
  component.appendChild(dot);
  dot.x = 22;
  dot.y = 22;
}

function configureUserLocationMarkerProperties(_componentSet, _stats) {
  // No editable text: the marker is pure geometry, and its accessible name is
  // owned by the map renderer that positions it.
}

const USER_LOCATION_MARKER_DESCRIPTION = [
  "Kozmos UserLocationMarker generated from the React UserLocationMarker API.",
  "Heading maps to UserLocationMarker.showHeading.",
  "heading is a bearing in degrees; it rotates the cone rather than adding a variant.",
  "The marker has no text; the map renderer owns its position and accessible name.",
  "Heading adds a distinct wedge, so a known bearing is not signalled by colour alone.",
];

async function buildUserLocationMarkerComponent() {
  return buildSingleAxisComponent({
    componentName: "UserLocationMarker",
    componentSetName: "UserLocationMarker",
    axisName: "Heading",
    values: USER_LOCATION_MARKER_HEADINGS,
    x: 80,
    y: 18000,
    xStep: 140,
    createVariant: createUserLocationMarkerVariant,
    configureProperties: configureUserLocationMarkerProperties,
    autoReorganize: true,
    description: USER_LOCATION_MARKER_DESCRIPTION,
  });
}

async function updateUserLocationMarkerComponent() {
  return updateSingleAxisComponent({
    componentName: "UserLocationMarker",
    componentSetName: "UserLocationMarker",
    axisName: "Heading",
    values: USER_LOCATION_MARKER_HEADINGS,
    xStep: 140,
    createVariant: createUserLocationMarkerVariant,
    updateVariant: updateUserLocationMarkerVariant,
    parseVariantName: parseUserLocationMarkerVariantName,
    configureProperties: configureUserLocationMarkerProperties,
    autoReorganize: true,
    description: USER_LOCATION_MARKER_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildUserLocationMarkerComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "UserLocationMarker",
    componentSetName: "UserLocationMarker",
    build: buildUserLocationMarkerComponent,
  });
}

// --- DynamicIsland ---------------------------------------------------------

async function createDynamicIslandVariant(args) {
  const component = figma.createComponent();
  await updateDynamicIslandVariant(component, args);
  return component;
}

function parseDynamicIslandVariantName(name) {
  return productSdkVariantValues(name, "State", DYNAMIC_ISLAND_STATES);
}

async function updateDynamicIslandVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const geometry = DYNAMIC_ISLAND_GEOMETRY[value];
  productSdkVariantRoot(component, "DynamicIsland", "State=" + value, {
    direction: value === "Expanded" ? "vertical" : "horizontal",
    primarySizing: "FIXED",
    counterSizing: "FIXED",
    primaryAlign: value === "Compact" ? "SPACE_BETWEEN" : "CENTER",
    counterAlign: "CENTER",
    spacing: 8,
    padding: value === "Expanded" ? 16 : 12,
    width: geometry.width,
    height: geometry.height,
    clip: true,
  });
  // The island is a black pill on the device bezel, not a themed surface, so
  // it uses the inverse foreground rather than a Surface token.
  component.cornerRadius = geometry.radius;
  component.fills = [
    paintFromVariable(
      "Colors/foreground/1000",
      "#0B0D12",
      variableByName,
      stats,
    ),
  ];
  component.strokes = [];

  if (value === "Minimal") {
    const minimal = await productSdkSlot({
      name: "Minimal Content Slot",
      label: "•",
      width: 32,
      height: 24,
      fonts,
      variableByName,
      stats,
    });
    appendWithSizing(component, minimal, "FIXED", "FIXED");
    return;
  }

  if (value === "Compact") {
    const leading = await productSdkSlot({
      name: "Compact Leading Slot",
      label: "Leading",
      width: 56,
      height: 24,
      fonts,
      variableByName,
      stats,
    });
    appendWithSizing(component, leading, "FIXED", "FIXED");

    const trailing = await productSdkSlot({
      name: "Compact Trailing Slot",
      label: "Trailing",
      width: 56,
      height: 24,
      fonts,
      variableByName,
      stats,
    });
    appendWithSizing(component, trailing, "FIXED", "FIXED");
    return;
  }

  const expanded = await productSdkSlot({
    name: "Expanded Content Slot",
    label: "Expanded content",
    width: geometry.width - 32,
    height: geometry.height - 32,
    fonts,
    variableByName,
    stats,
  });
  appendWithSizing(component, expanded, "FILL", "FILL");
}

function configureDynamicIslandProperties(_componentSet, _stats) {
  // Every region is a slot for host content; there is no island-owned text.
}

const DYNAMIC_ISLAND_DESCRIPTION = [
  "Kozmos DynamicIsland generated from the React DynamicIsland API.",
  "State maps to DynamicIsland.islandState (compact, expanded, minimal).",
  "Compact Leading and Trailing Slots map to compactLeading and compactTrailing.",
  "Expanded Content Slot maps to expandedContent; Minimal Content Slot to minimalContent.",
  "The pill sits on the device bezel, so it uses the inverse foreground rather than a Surface token.",
];

async function buildDynamicIslandComponent() {
  return buildSingleAxisComponent({
    componentName: "DynamicIsland",
    componentSetName: "DynamicIsland",
    axisName: "State",
    values: DYNAMIC_ISLAND_STATES,
    x: 80,
    y: 18400,
    xStep: 460,
    createVariant: createDynamicIslandVariant,
    configureProperties: configureDynamicIslandProperties,
    autoReorganize: true,
    description: DYNAMIC_ISLAND_DESCRIPTION,
  });
}

async function updateDynamicIslandComponent() {
  return updateSingleAxisComponent({
    componentName: "DynamicIsland",
    componentSetName: "DynamicIsland",
    axisName: "State",
    values: DYNAMIC_ISLAND_STATES,
    xStep: 460,
    createVariant: createDynamicIslandVariant,
    updateVariant: updateDynamicIslandVariant,
    parseVariantName: parseDynamicIslandVariantName,
    configureProperties: configureDynamicIslandProperties,
    autoReorganize: true,
    description: DYNAMIC_ISLAND_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildDynamicIslandComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "DynamicIsland",
    componentSetName: "DynamicIsland",
    build: buildDynamicIslandComponent,
  });
}

// --- FeedbackCard ----------------------------------------------------------

async function createFeedbackCardVariant(args) {
  const component = figma.createComponent();
  await updateFeedbackCardVariant(component, args);
  return component;
}

function parseFeedbackCardVariantName(name) {
  return productSdkVariantValues(name, "State", FEEDBACK_CARD_STATES);
}

async function updateFeedbackCardVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const submitting = value === "Submitting";
  const success = value === "Success";
  const width = 340;
  const contentWidth = width - 32;
  productSdkVariantRoot(component, "FeedbackCard", "State=" + value, {
    primarySizing: "AUTO",
    counterSizing: "FIXED",
    spacing: 12,
    padding: 16,
    width,
    height: 240,
  });
  productSdkSurface(component, 12, variableByName, stats);

  // Success replaces the form outright. Leaving a disabled form behind the
  // confirmation would suggest the rating is still editable.
  if (success) {
    const successText = await productSdkText({
      name: "Success Text",
      characters: "Thanks for your feedback.",
      styleKey: "cardTitle",
      fonts,
      bold: true,
      fontSize: 16,
      lineHeight: 24,
      colorToken: "Colors/emotional/success/900",
      colorFallback: "#14653D",
      variableByName,
      stats,
      width: contentWidth,
      wrap: true,
    });
    appendWithSizing(component, successText, "FILL", "HUG");
    return;
  }

  const title = await productSdkText({
    name: "Title Text",
    characters: "How was your visit?",
    styleKey: "cardTitle",
    fonts,
    bold: true,
    fontSize: 16,
    lineHeight: 24,
    colorToken: "Colors/foreground/0",
    colorFallback: "#000000",
    variableByName,
    stats,
    width: contentWidth,
  });
  appendWithSizing(component, title, "FILL", null);

  const description = await productSdkText({
    name: "Description Text",
    characters: "Rate your wayfinding experience and add a comment.",
    styleKey: "cardDescription",
    fonts,
    bold: false,
    fontSize: 14,
    lineHeight: 20,
    colorToken: "Colors/foreground/400",
    colorFallback: "#5D626F",
    variableByName,
    stats,
    width: contentWidth,
    wrap: true,
  });
  appendWithSizing(component, description, "FILL", "HUG");

  const rating = await productSdkSlot({
    name: "Rating Slot",
    label: "Rating slot",
    width: contentWidth,
    height: 44,
    fonts,
    variableByName,
    stats,
    muted: true,
  });
  appendWithSizing(component, rating, "FILL", "FIXED");

  const comment = await productSdkSlot({
    name: "Comment Slot",
    label: "Textarea slot",
    width: contentWidth,
    height: 56,
    fonts,
    variableByName,
    stats,
    muted: true,
  });
  appendWithSizing(component, comment, "FILL", "FIXED");

  const submit = await productSdkControlButton({
    name: "Submit Button",
    glyph: submitting ? "◌" : "→",
    label: submitting ? "Sending" : "Send feedback",
    pressed: !submitting,
    fonts,
    variableByName,
    stats,
  });
  submit.opacity = submitting ? 0.6 : 1;
  appendWithSizing(component, submit, "FILL", "FIXED");
}

function configureFeedbackCardProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Title Text",
    "Title Text",
    "How was your visit?",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Description Text",
    "Description Text",
    "Rate your wayfinding experience and add a comment.",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Success Text",
    "Success Text",
    "Thanks for your feedback.",
    stats,
  );
}

const FEEDBACK_CARD_DESCRIPTION = [
  "Kozmos FeedbackCard generated from the React FeedbackCard API.",
  "State covers the editable form, the in-flight submit, and the confirmation.",
  "Title Text maps to title; Description Text to description; Success Text to successMessage.",
  "Rating Slot and Comment Slot compose Core Rating and Textarea.",
  "Success replaces the form rather than disabling it, so nothing reads as still editable.",
];

async function buildFeedbackCardComponent() {
  return buildSingleAxisComponent({
    componentName: "FeedbackCard",
    componentSetName: "FeedbackCard",
    axisName: "State",
    values: FEEDBACK_CARD_STATES,
    x: 80,
    y: 18800,
    xStep: 400,
    createVariant: createFeedbackCardVariant,
    configureProperties: configureFeedbackCardProperties,
    autoReorganize: true,
    description: FEEDBACK_CARD_DESCRIPTION,
  });
}

async function updateFeedbackCardComponent() {
  return updateSingleAxisComponent({
    componentName: "FeedbackCard",
    componentSetName: "FeedbackCard",
    axisName: "State",
    values: FEEDBACK_CARD_STATES,
    xStep: 400,
    createVariant: createFeedbackCardVariant,
    updateVariant: updateFeedbackCardVariant,
    parseVariantName: parseFeedbackCardVariantName,
    configureProperties: configureFeedbackCardProperties,
    autoReorganize: true,
    description: FEEDBACK_CARD_DESCRIPTION.concat([
      "Updated in place to preserve the Code Connect node ID.",
    ]),
  });
}

async function rebuildFeedbackCardComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "FeedbackCard",
    componentSetName: "FeedbackCard",
    build: buildFeedbackCardComponent,
  });
}

async function buildSingleAxisComponent(config) {
  const stats = {
    created: false,
    componentSetId: null,
    urlNodeId: null,
    variants: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, config.componentName, stats);

  const existing = findComponentSetOnPage(page, config.componentSetName);
  if (existing) {
    normalizeComponentSetNodeName(existing, config.componentSetName, stats);
    const duplicateCount = removeDuplicateGeneratedComponentSetsForName(
      page,
      config.componentSetName,
      existing,
      stats,
    );
    stats.existing = true;
    stats.componentSetId = existing.id;
    stats.urlNodeId = nodeIdForUrl(existing.id);
    stats.message = `${config.componentSetName} already exists. Use Update ${config.componentName} to preserve its node ID.`;
    if (duplicateCount > 0) {
      await reorganizeAfterGeneratedComponentMutation(stats);
    }
    return stats;
  }

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const components = [];

  for (let valueIndex = 0; valueIndex < config.values.length; valueIndex += 1) {
    const value = config.values[valueIndex];
    const component = await config.createVariant({
      value,
      variableByName,
      fonts,
      stats,
    });
    component.x = valueIndex * config.xStep;
    component.y = 0;
    page.appendChild(component);
    components.push(component);
  }

  const componentSet = figma.combineAsVariants(components, page);
  componentSet.name = config.componentSetName;
  componentSet.x = config.x;
  componentSet.y = config.y;
  componentSet.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  componentSet.setSharedPluginData(
    RUN_NAMESPACE,
    "component",
    config.componentName,
  );
  applyComponentSetDescription(
    componentSet,
    config.componentSetName,
    false,
    config.description,
  );
  clearComponentSetContainerFill(componentSet);

  stats.created = true;
  stats.componentSetId = componentSet.id;
  stats.urlNodeId = nodeIdForUrl(componentSet.id);
  stats.variants = components.length;
  normalizeComponentSetVariantProperties(
    componentSet,
    expectedVariantAxesForComponentSetName(componentSet.name),
    stats,
  );
  await config.configureProperties(componentSet, stats, variableByName);
  runGeneratedComponentPostUpdateMaintenance(
    componentSet,
    config.componentName,
    stats,
  );
  await reorganizeAfterGeneratedComponentMutation(stats);
  await runGeneratedComponentPostLayoutMaintenance({
    componentName: config.componentName,
    componentSetName: config.componentSetName,
    stats,
  });
  return stats;
}

async function updateSingleAxisComponent(config) {
  const stats = {
    updated: false,
    componentSetId: null,
    urlNodeId: null,
    variantsUpdated: 0,
    variantsCreated: 0,
    variantsMissing: 0,
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  removeStaleGeneratedComponentArtifacts(page, config.componentName, stats);

  const existing = findComponentSetOnPage(page, config.componentSetName);
  if (!existing || existing.type !== "COMPONENT_SET") {
    stats.message = `${config.componentSetName} was not found. Run Build ${config.componentName} first.`;
    return stats;
  }
  normalizeComponentSetNodeName(existing, config.componentSetName, stats);
  removeDuplicateGeneratedComponentSetsForName(
    page,
    config.componentSetName,
    existing,
    stats,
  );

  const fonts = await loadButtonFonts(stats);
  const variableByName = await ensureComponentRuntimeVariables(stats);
  const seenValues = {};

  existing.setSharedPluginData(RUN_NAMESPACE, "kind", "component-set");
  existing.setSharedPluginData(
    RUN_NAMESPACE,
    "component",
    config.componentName,
  );
  applyComponentSetDescription(
    existing,
    config.componentSetName,
    true,
    config.description,
  );
  clearComponentSetContainerFill(existing);

  for (const child of existing.children) {
    if (child.type !== "COMPONENT") continue;

    const props = config.parseVariantName(child.name);
    if (!props) {
      stats.warnings.push(
        `Skipped unrecognized ${config.componentName} variant "${child.name}".`,
      );
      continue;
    }

    seenValues[props.value] = true;
    await config.updateVariant(child, {
      value: props.value,
      variableByName,
      fonts,
      stats,
    });
    stats.variantsUpdated += 1;
  }

  for (const value of config.values) {
    if (seenValues[value]) continue;

    const component = await config.createVariant({
      value,
      variableByName,
      fonts,
      stats,
    });
    existing.appendChild(component);
    seenValues[value] = true;
    stats.variantsCreated += 1;
  }

  layoutSingleAxisVariants(existing, config);

  stats.updated = true;
  stats.componentSetId = existing.id;
  stats.urlNodeId = nodeIdForUrl(existing.id);
  normalizeComponentSetVariantProperties(
    existing,
    expectedVariantAxesForComponentSetName(existing.name),
    stats,
  );
  await config.configureProperties(existing, stats, variableByName);
  runGeneratedComponentPostUpdateMaintenance(
    existing,
    config.componentName,
    stats,
  );
  await reorganizeAfterGeneratedComponentMutation(stats);
  await runGeneratedComponentPostLayoutMaintenance({
    componentName: config.componentName,
    componentSetName: config.componentSetName,
    stats,
  });
  return stats;
}

function layoutSingleAxisVariants(componentSet, config) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = config.parseVariantName(child.name);
    if (!props) continue;

    const valueIndex = config.values.indexOf(props.value);
    child.x = valueIndex * config.xStep;
    child.y = 0;
  }

  // A component set is a frame, and a frame keeps its size when its children
  // shrink. WayfindingCard was 700x1058 around 700x134 of variants -- 87% dead
  // space -- and POICard 74%, both left over from taller earlier layouts. The
  // Components page packs by bounding box, so an empty block costs a real
  // column slot, not just looks.
  resizeComponentSetToContainChildren(componentSet);
}

function layoutTextVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  const toneColumns = TEXT_TONES.length;
  const columnWidth = 240;
  const rowHeight = 86;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseTextVariantName(child.name);
    if (!props) continue;

    const sizeIndex = TEXT_SIZES.indexOf(props.size);
    const weightIndex = TEXT_WEIGHTS.indexOf(props.weight);
    const toneIndex = TEXT_TONES.indexOf(props.tone);
    child.x = (weightIndex * toneColumns + toneIndex) * columnWidth;
    child.y = sizeIndex * rowHeight;
  }
}

function layoutStackVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  const columnWidth = 380;
  const rowHeight = 160;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseStackVariantName(child.name);
    if (!props) continue;

    const directionIndex = STACK_DIRECTIONS.indexOf(props.direction);
    const gapIndex = STACK_GAPS.indexOf(props.gap);
    child.x = gapIndex * columnWidth;
    child.y = directionIndex * rowHeight;
  }
}

function layoutGridVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  const columnGap = 60;
  const rowGap = 48;
  let maxWidth = 360;
  let maxHeight = 0;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;
    if (typeof child.width === "number" && Number.isFinite(child.width)) {
      maxWidth = Math.max(maxWidth, child.width);
    }
    if (typeof child.height === "number" && Number.isFinite(child.height)) {
      maxHeight = Math.max(maxHeight, child.height);
    }
  }

  if (!maxHeight) maxHeight = 264;

  let maxColumnsIndex = 0;
  let maxGapIndex = 0;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseGridVariantName(child.name);
    if (!props) continue;

    const columnsIndex = GRID_COLUMNS.indexOf(props.columns);
    const gapIndex = GRID_GAPS.indexOf(props.gap);
    maxColumnsIndex = Math.max(maxColumnsIndex, columnsIndex);
    maxGapIndex = Math.max(maxGapIndex, gapIndex);
    child.x = columnsIndex * (maxWidth + columnGap);
    child.y = gapIndex * (maxHeight + rowGap);
  }

  resizeNodeWithoutConstraints(
    componentSet,
    (maxColumnsIndex + 1) * maxWidth + maxColumnsIndex * columnGap,
    (maxGapIndex + 1) * maxHeight + maxGapIndex * rowGap,
  );
  componentSet.clipsContent = false;
}

function layoutTabsVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseTabsVariantName(child.name);
    if (!props) continue;

    const countIndex = TABS_COUNTS.indexOf(props.count);
    const activeIndex = TABS_ACTIVE_TO_INDEX[props.active];
    const stateIndex = TABS_STATES.indexOf(props.state);
    child.x = activeIndex * 700;
    child.y = (countIndex * TABS_STATES.length + stateIndex) * 76;
  }
}

function layoutToggleButtonVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  const columnWidth = 180;
  const rowHeight = 72;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseToggleButtonVariantName(child.name);
    if (!props) continue;

    const variantIndex = TOGGLE_BUTTON_VARIANTS.indexOf(props.variant);
    const sizeIndex = TOGGLE_BUTTON_SIZES.indexOf(props.size);
    const stateIndex = TOGGLE_BUTTON_STATES.indexOf(props.state);
    child.x =
      (variantIndex * TOGGLE_BUTTON_SIZES.length + sizeIndex) * columnWidth;
    child.y = stateIndex * rowHeight;
  }

  resizeComponentSetToContainChildren(componentSet);
}

function layoutSplitButtonVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  const columnWidth = 260;
  const rowHeight = 76;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseSplitButtonVariantName(child.name);
    if (!props) continue;

    const variantIndex = SPLIT_BUTTON_VARIANTS.indexOf(props.variant);
    const sizeIndex = SPLIT_BUTTON_SIZES.indexOf(props.size);
    const stateIndex = SPLIT_BUTTON_STATES.indexOf(props.state);
    child.x =
      (variantIndex * SPLIT_BUTTON_SIZES.length + sizeIndex) * columnWidth;
    child.y = stateIndex * rowHeight;
  }

  resizeComponentSetToContainChildren(componentSet);
}

function layoutFloatingActionButtonVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  const columnWidth = 128;
  const rowHeight = 96;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseFloatingActionButtonVariantName(child.name);
    if (!props) continue;

    const variantIndex = FLOATING_ACTION_BUTTON_VARIANTS.indexOf(props.variant);
    const sizeIndex = FLOATING_ACTION_BUTTON_SIZES.indexOf(props.size);
    const stateIndex = FLOATING_ACTION_BUTTON_STATES.indexOf(props.state);
    child.x =
      (variantIndex * FLOATING_ACTION_BUTTON_SIZES.length + sizeIndex) *
      columnWidth;
    child.y = stateIndex * rowHeight;
  }

  resizeComponentSetToContainChildren(componentSet);
}

function configureProgressProperties(_componentSet, _stats) {}

function configureSpinnerProperties(_componentSet, _stats) {}

function configureAvatarProperties(componentSet, stats) {
  configureNamedTextProperty(componentSet, "Fallback", "Fallback", "AK", stats);
  configureNamedTextProperty(
    componentSet,
    "Image URL",
    "Image URL",
    "https://example.com/avatar.png",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Alt Text",
    "Alt Text",
    "Avatar",
    stats,
  );
}

function configureTextProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Text",
    "Text",
    "Kozmos text",
    stats,
  );
}

function configureHeadingProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Heading Text",
    "Heading Text",
    "Heading",
    stats,
  );
}

function configureLinkProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Link Text",
    "Link Text",
    "Open link",
    stats,
  );
  configureFocusVisibleProperty(componentSet, stats);
}

function configureLabelProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Label Text",
    "Label Text",
    "Label",
    stats,
  );
}

function configureSeparatorProperties(_componentSet, _stats) {}

function configureSkeletonProperties(_componentSet, _stats) {}

function configureBoxProperties(componentSet, stats) {
  deleteComponentPropertiesByBaseName(
    componentSet,
    ["Box Text"],
    ["TEXT"],
    stats,
  );
  configureSharedSlotProperties(componentSet, ["Content Slot"], stats);
}

function configureStackProperties(componentSet, stats) {
  configureSharedSlotProperties(componentSet, ["Content Slot"], stats);
}

function configureGridProperties(componentSet, stats) {
  deleteComponentPropertiesByBaseName(
    componentSet,
    ["Content Slot"],
    ["SLOT"],
    stats,
  );
  configureSharedSlotProperties(
    componentSet,
    gridCellSlotNames(maxGridColumnCount(), GRID_PREVIEW_ROWS),
    stats,
  );
}

function configureContainerProperties(componentSet, stats) {
  deleteComponentPropertiesByBaseName(
    componentSet,
    ["Container Text"],
    ["TEXT"],
    stats,
  );
  configureSharedSlotProperties(componentSet, ["Content Slot"], stats);
}

function configureBreadcrumbProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Item 1 Text",
    "Item 1 Text",
    "Home",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Item 2 Text",
    "Item 2 Text",
    "Components",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Current Page Text",
    "Current Page Text",
    "Breadcrumb",
    stats,
  );
}

function configurePaginationProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Previous Text",
    "Previous Text",
    "Previous",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Next Text",
    "Next Text",
    "Next",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Page 1 Text",
    "Page 1 Text",
    "1",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Page 2 Text",
    "Page 2 Text",
    "2",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Page 3 Text",
    "Page 3 Text",
    "3",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Last Page Text",
    "Last Page Text",
    "10",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Compact Text",
    "Compact Text",
    "2 / 10",
    stats,
  );
}

function configureAccordionProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Trigger Text",
    "Trigger Text",
    "Is it accessible?",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Content Text",
    "Content Text",
    "Yes. It follows the WAI-ARIA disclosure pattern.",
    stats,
  );
}

function configureChipProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Label Text",
    "Label Text",
    "Filter",
    stats,
  );
  configureFocusVisibleProperty(componentSet, stats);
}

function configureSegmentedControlProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Item 1 Text",
    "Item 1 Text",
    "Overview",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Item 2 Text",
    "Item 2 Text",
    "Details",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Item 3 Text",
    "Item 3 Text",
    "Activity",
    stats,
  );
  configureFocusVisibleProperty(componentSet, stats);
}

function configureToggleButtonProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Label Text",
    "Label Text",
    "Toggle",
    stats,
  );
  configureFocusVisibleProperty(componentSet, stats);
}

function configureSplitButtonProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Label Text",
    "Label Text",
    "Action",
    stats,
  );
  configureFocusVisibleProperty(componentSet, stats);
}

async function configureFloatingActionButtonProperties(
  componentSet,
  iconComponent,
  variableByName,
  stats,
) {
  if (!iconComponent) {
    configureFocusVisibleProperty(componentSet, stats);
    return;
  }

  const iconSourceComponents = await findKozmosIconSourceComponents();
  const preferredValues = iconPreferredValues(iconSourceComponents);
  const propertyName = ensureInstanceSwapProperty(
    componentSet,
    BUTTON_ICON_PROPERTY,
    iconComponent.id,
    stats,
    preferredValues,
  );

  if (propertyName) {
    for (const child of componentSet.children) {
      if (child.type !== "COMPONENT") continue;

      const props = parseFloatingActionButtonVariantName(child.name);
      if (!props) continue;

      const config = floatingActionButtonConfig(props.variant, props.state);
      const metrics = floatingActionButtonMetrics(props.size);
      let icon = directChildNamed(child, "Icon");
      if (
        icon &&
        icon.type === "INSTANCE" &&
        !isGeneratedIconSlotWrapper(icon)
      ) {
        syncIconSlotInstance(
          icon,
          config,
          variableByName,
          stats,
          metrics.iconSize,
          metrics.iconSizeToken,
        );
      } else {
        if (icon) icon.remove();
        icon = createIconSlotInstance(
          iconComponent,
          config.foreground,
          config.foregroundFallback,
          variableByName,
          stats,
          metrics.iconSize,
          metrics.iconSizeToken,
        );
      }
      child.appendChild(icon);
      setHugChildSizing(icon);
      bindInstanceSwapProperty(icon, propertyName, stats);
      stats.iconSlotsBound += 1;
    }
  }

  configureFocusVisibleProperty(componentSet, stats);
}

function configureAlertProperties(componentSet, stats) {
  configureNamedTextProperty(componentSet, "Title", "Title", "Heads up", stats);
  configureNamedTextProperty(
    componentSet,
    "Description",
    "Description",
    "This status message needs attention.",
    stats,
  );
}

function configureEmptyStateProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Title Text",
    "Title Text",
    "No results found",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Description Text",
    "Description Text",
    "Try adjusting your filters or search terms.",
    stats,
  );
}

function configureCardProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Title Text",
    "Title Text",
    "Create project",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Description Text",
    "Description Text",
    "Deploy your new project in one click.",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Body Text",
    "Body Text",
    "Use this contained surface for related content, supporting copy, and local actions.",
    stats,
  );
}

function configureListProperties(componentSet, stats) {
  for (let index = 1; index <= 3; index += 1) {
    const label = `Item ${index} Text`;
    configureNamedTextProperty(
      componentSet,
      label,
      label,
      `Item ${index}`,
      stats,
    );
  }
}

function configureTableProperties(componentSet, stats) {
  const headers = ["Name", "Status", "Updated"];
  for (let column = 1; column <= 3; column += 1) {
    const label = `Header ${column} Text`;
    configureNamedTextProperty(
      componentSet,
      label,
      label,
      headers[column - 1],
      stats,
    );
  }

  const rows = [
    ["Beacon sync", "Ready", "Today"],
    ["Indoor map", "Draft", "Yesterday"],
    ["Analytics", "Queued", "May 21"],
  ];
  for (let row = 1; row <= 3; row += 1) {
    for (let column = 1; column <= 3; column += 1) {
      const label = `Row ${row} Cell ${column} Text`;
      configureNamedTextProperty(
        componentSet,
        label,
        label,
        rows[row - 1][column - 1],
        stats,
      );
    }
  }
}

function configureTabsProperties(componentSet, stats) {
  const defaults = ["Overview", "Details", "Usage", "History"];
  for (let index = 0; index < defaults.length; index += 1) {
    const label = `Tab ${index + 1} Text`;
    configureNamedTextProperty(
      componentSet,
      label,
      label,
      defaults[index],
      stats,
    );
  }

  configureFocusVisibleProperty(componentSet, stats);
}

function configureTooltipProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Content Text",
    "Content Text",
    "Add to library",
    stats,
  );
}

function configureDialogProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Title Text",
    "Title Text",
    "Edit profile",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Description Text",
    "Description Text",
    "Make changes to your profile here.",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Body Text",
    "Body Text",
    "Use dialog body content for a short task, form, or confirmation.",
    stats,
  );
  deleteComponentPropertiesByBaseName(
    componentSet,
    ["Primary Action Text", "Secondary Action Text"],
    ["TEXT"],
    stats,
  );
  normalizeDialogCompositeSizing(componentSet, stats);
}

function configureDrawerProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Title Text",
    "Title Text",
    "Drawer title",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Description Text",
    "Description Text",
    "Use drawers for contextual content that stays tied to the current screen.",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Body Text",
    "Body Text",
    "Drawer body content can hold filters, navigation, or compact product workflows.",
    stats,
  );
  configureSharedSlotProperties(componentSet, ["Content Slot"], stats);
}

function configurePopoverProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Title Text",
    "Title Text",
    "Dimensions",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Description Text",
    "Description Text",
    "Set the dimensions for the layer.",
    stats,
  );
}

function configureMenuProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Label Text",
    "Label Text",
    "My Account",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Item 1 Text",
    "Item 1 Text",
    "Profile",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Item 2 Text",
    "Item 2 Text",
    "Billing",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Item 3 Text",
    "Item 3 Text",
    "Team",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Shortcut Text",
    "Shortcut Text",
    "Cmd K",
    stats,
  );
}

function configureToastProperties(componentSet, stats) {
  configureNamedTextProperty(
    componentSet,
    "Title Text",
    "Title Text",
    "Scheduled: Catch up",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Description Text",
    "Description Text",
    "Friday, February 10, 2023 at 5:57 PM",
    stats,
  );
  configureNamedTextProperty(
    componentSet,
    "Action Text",
    "Action Text",
    "Undo",
    stats,
  );
}

async function rebuildCardComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Card",
    componentSetName: "Card",
    build: buildCardComponent,
  });
}

async function rebuildListComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "List",
    componentSetName: "List",
    build: buildListComponent,
  });
}

async function rebuildTableComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Table",
    componentSetName: "Table",
    build: buildTableComponent,
  });
}

async function rebuildTabsComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Tabs",
    componentSetName: "Tabs",
    build: buildTabsComponent,
  });
}

async function rebuildTooltipComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Tooltip",
    componentSetName: "Tooltip",
    build: buildTooltipComponent,
  });
}

async function rebuildDialogComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Dialog",
    componentSetName: "Dialog",
    build: buildDialogComponent,
  });
}

async function rebuildDrawerComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Drawer",
    componentSetName: "Drawer",
    build: buildDrawerComponent,
  });
}

async function rebuildPopoverComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Popover",
    componentSetName: "Popover",
    build: buildPopoverComponent,
  });
}

async function rebuildMenuComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Menu",
    componentSetName: "Menu",
    build: buildMenuComponent,
  });
}

async function rebuildProgressComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Progress",
    componentSetName: "Progress",
    build: buildProgressComponent,
  });
}

async function rebuildSpinnerComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Spinner",
    componentSetName: "Spinner",
    build: buildSpinnerComponent,
  });
}

async function rebuildAvatarComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Avatar",
    componentSetName: "Avatar",
    build: buildAvatarComponent,
  });
}

async function rebuildAlertComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Alert",
    componentSetName: "Alert",
    build: buildAlertComponent,
  });
}

async function rebuildEmptyStateComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "EmptyState",
    componentSetName: "EmptyState",
    build: buildEmptyStateComponent,
  });
}

async function rebuildToastComponent() {
  return rebuildGeneratedComponentSet({
    componentName: "Toast",
    componentSetName: "Toast",
    build: buildToastComponent,
  });
}

async function rebuildGeneratedComponentSet(config) {
  const rebuildStats = {
    rebuilt: false,
    removed: [],
    warnings: [],
  };

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();
  await removeGeneratedNodesForRebuild(
    page,
    config.componentSetName,
    rebuildStats,
  );

  const buildResult = await config.build();
  const merged = mergeRebuildResult(rebuildStats, buildResult, config);
  await finalizeRebuiltGeneratedComponentSet(config, merged);
  return merged;
}

async function finalizeRebuiltGeneratedComponentSet(config, stats) {
  if (!config || !stats || !stats.created) return;

  const page = await ensurePage("Components");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  const componentSet = findComponentSetOnPage(page, config.componentSetName);
  if (componentSet && componentSet.type === "COMPONENT_SET") {
    runGeneratedComponentPostUpdateMaintenance(
      componentSet,
      config.componentName,
      stats,
    );
  }

  if (!stats.layout) {
    await reorganizeAfterGeneratedComponentMutation(stats);
    await runGeneratedComponentPostLayoutMaintenance({
      componentName: config.componentName,
      componentSetName: config.componentSetName,
      stats,
    });
  }
}

async function removeGeneratedNodesForRebuild(page, componentSetName, stats) {
  const nodes = collectGeneratedNodesForRebuild(page, componentSetName);

  if (nodes.length === 0) {
    stats.warnings.push(
      `${componentSetName} was not found on the Components page before rebuild; creating a fresh set.`,
    );
    return;
  }

  for (const node of nodes) {
    const oldName = node.name;
    const oldId = node.id;
    const oldType = node.type;

    stats.removed.push({
      id: oldId,
      urlNodeId: nodeIdForUrl(oldId),
      oldName,
      type: oldType,
    });

    node.remove();
  }
}

function collectGeneratedNodesForRebuild(root, componentSetName) {
  const nodes = [];

  function visit(parent) {
    if (!parent || !parent.children) return;

    for (const child of parent.children.slice()) {
      if (
        child.type === "COMPONENT_SET" &&
        componentSetNameMatches(child.name, componentSetName)
      ) {
        nodes.push(child);
        continue;
      }

      if (
        child.type === "INSTANCE" &&
        (componentSetNameMatches(child.name, componentSetName) ||
          componentSetNameMatches(
            nodeSourceComponentSet(child),
            componentSetName,
          ))
      ) {
        nodes.push(child);
        continue;
      }

      if (shouldTraverseGeneratedCleanupContainer(child)) visit(child);
    }
  }

  visit(root);
  return nodes;
}

function mergeRebuildResult(rebuildStats, buildResult, config) {
  const merged = {};
  for (const key of Object.keys(buildResult)) {
    merged[key] = buildResult[key];
  }

  merged.rebuilt = buildResult.created === true;
  merged.removed = rebuildStats.removed;

  const warnings = [];
  for (const warning of rebuildStats.warnings) warnings.push(warning);
  if (buildResult.warnings) {
    for (const warning of buildResult.warnings) warnings.push(warning);
  }
  merged.warnings = warnings;

  if (buildResult.created) {
    const removedCount = rebuildStats.removed.length;
    merged.message =
      `${config.componentSetName} was rebuilt with fresh node-id=${buildResult.urlNodeId}. ` +
      `${removedCount} old generated node(s) were removed.`;
  } else if (buildResult.existing) {
    merged.message = `Could not rebuild ${config.componentSetName}; another node with that name still exists on the Components page.`;
  }

  return merged;
}

function removeStaleGeneratedComponentArtifacts(page, componentName, stats) {
  const stale = collectStaleGeneratedComponentArtifacts(page, componentName);

  for (const child of stale) {
    child.remove();
    incrementStat(stats, "staleArtifactsRemoved");
  }
}

function collectStaleGeneratedComponentArtifacts(root, componentName) {
  const stale = [];
  const componentSetName = componentName;

  function visit(parent) {
    if (!parent || !parent.children) return;

    for (const child of parent.children.slice()) {
      if (child.type === "COMPONENT_SET") continue;

      if (
        child.type === "COMPONENT" &&
        child.getSharedPluginData &&
        child.getSharedPluginData(RUN_NAMESPACE, "component") === componentName
      ) {
        stale.push(child);
        continue;
      }

      if (
        child.type === "INSTANCE" &&
        (componentSetNameMatches(child.name, componentSetName) ||
          componentSetNameMatches(
            nodeSourceComponentSet(child),
            componentSetName,
          ))
      ) {
        stale.push(child);
        continue;
      }

      if (
        componentName === "Badge" &&
        child.type === "TEXT" &&
        child.name === "Label Text" &&
        child.characters === ""
      ) {
        stale.push(child);
        continue;
      }

      if (isTopLevelGeneratedAnatomyArtifact(child, componentName)) {
        stale.push(child);
        continue;
      }

      if (shouldTraverseGeneratedCleanupContainer(child)) visit(child);
    }
  }

  visit(root);
  return stale;
}

function isComponentDefinitionNode(node) {
  return (
    node &&
    (node.type === "COMPONENT_SET" ||
      node.type === "COMPONENT" ||
      node.type === "INSTANCE")
  );
}

function shouldTraverseGeneratedCleanupContainer(node) {
  return (
    node &&
    !isComponentDefinitionNode(node) &&
    (node.type === "PAGE" || node.type === "SECTION" || node.type === "GROUP")
  );
}

function isTopLevelGeneratedAnatomyArtifact(node, componentName) {
  if (!node || node.type === "COMPONENT_SET" || node.type === "COMPONENT") {
    return false;
  }

  const namesByComponent = {
    Accordion: [
      "Accordion Content",
      "Accordion Divider",
      "Accordion Trigger",
      "Content Text",
      "Trigger Text",
    ],
    Drawer: [
      "Body Text",
      "Content Slot",
      "Drawer Body",
      "Drawer Close",
      "Drawer Footer",
      "Drawer Header",
      "Drawer Header Content",
      "Primary Action",
      "Secondary Action",
      "Title Text",
      "Description Text",
    ],
    PasswordInput: [
      "Eye Outline",
      "Eye Pupil",
      "Eye Slash",
      "Helper Text",
      "Label Text",
      "PasswordInput Field",
      "PasswordInput Value Slot",
      "Placeholder Text",
      "Visibility Icon / hide",
      "Visibility Icon / show",
      "Visibility Toggle",
    ],
    OTPInput: [
      "Digit 1 Cell",
      "Digit 1 Text",
      "Digit 2 Cell",
      "Digit 2 Text",
      "Digit 3 Cell",
      "Digit 3 Text",
      "Digit 4 Cell",
      "Digit 4 Text",
      "Digit 5 Cell",
      "Digit 5 Text",
      "Digit 6 Cell",
      "Digit 6 Text",
      "Helper Text",
      "Label Text",
      "OTPInput Cell Group",
    ],
    Navbar: [
      "Logo Slot",
      "Site Slot",
      "Site Selector",
      "Context Slot",
      "Context Selector",
      "Navigation Slot",
      "Primary Action Slot",
      "Actions Slot",
      "Utility Slot",
      "Utility Button",
      "Account Slot",
      "Account Avatar",
    ],
  };
  const names = namesByComponent[componentName];
  return !!names && names.indexOf(node.name) !== -1;
}

async function ensurePage(name) {
  const existing = figma.root.children.find((page) => page.name === name);
  if (existing) {
    await existing.loadAsync();
    return existing;
  }

  const page = figma.createPage();
  page.name = name;
  page.setSharedPluginData(RUN_NAMESPACE, "kind", "page");
  await page.loadAsync();
  return page;
}

async function resolveConfiguredFonts(stats) {
  const config = currentFontConfig();
  const regular = await loadFirstAvailableFont(
    fontCandidatesForRole("regular"),
  );
  const medium = await loadFirstAvailableFont(
    fontCandidatesForRole("medium", regular),
  );
  const bold = await loadFirstAvailableFont(
    fontCandidatesForRole("bold", medium),
  );

  if (
    !sameFont(regular, {
      family: config.family,
      style: config.regularStyle,
    })
  ) {
    pushUniqueWarning(
      stats,
      `font-fallback:regular:${config.family}:${config.regularStyle}`,
      `Configured ${config.family} ${config.regularStyle} was unavailable for regular text; using ${fontLabel(regular)}.`,
    );
  }

  if (
    !sameFont(medium, {
      family: config.family,
      style: config.mediumStyle,
    })
  ) {
    pushUniqueWarning(
      stats,
      `font-fallback:medium:${config.family}:${config.mediumStyle}`,
      `Configured ${config.family} ${config.mediumStyle} was unavailable for medium text; using ${fontLabel(medium)}.`,
    );
  }

  if (
    !sameFont(bold, {
      family: config.family,
      style: config.boldStyle,
    })
  ) {
    pushUniqueWarning(
      stats,
      `font-fallback:bold:${config.family}:${config.boldStyle}`,
      `Configured ${config.family} ${config.boldStyle} was unavailable for bold text; using ${fontLabel(bold)}.`,
    );
  }

  if (stats) {
    stats.fontConfig = {
      requestedFamily: config.family,
      regular: fontLabel(regular),
      medium: fontLabel(medium),
      bold: fontLabel(bold),
    };
  }

  const fonts = { regular, medium, bold };
  return fonts;
}

async function loadButtonFonts(stats) {
  const fonts = await resolveConfiguredFonts(stats);
  await ensureKozmosTextStyles(fonts, stats);
  return fonts;
}

async function loadFirstAvailableFont(fonts) {
  let lastError = null;
  for (const font of fonts) {
    try {
      await figma.loadFontAsync(font);
      return font;
    } catch (error) {
      lastError = error;
    }
  }

  throw lastError || new Error("No usable font found.");
}

async function createButtonVariant({
  variant,
  size,
  state,
  variableByName,
  fonts,
  textStyle,
  stats,
}) {
  const config = buttonConfig(variant, state);
  const metrics = buttonMetrics(size);
  const component = figma.createComponent();
  component.name = `Variant=${variant}, Size=${size}, State=${state}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = metrics.gap;
  component.paddingLeft = metrics.paddingX;
  component.paddingRight = metrics.paddingX;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(metrics.width, metrics.height);
  component.cornerRadius = 8;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Button");

  component.fills = config.background
    ? [
        paintFromVariable(
          config.background,
          config.backgroundFallback,
          variableByName,
          stats,
        ),
      ]
    : [];

  if (config.stroke) {
    component.strokes = [
      paintFromVariable(
        config.stroke,
        config.strokeFallback,
        variableByName,
        stats,
      ),
    ];
    component.strokeWeight = 1;
  } else {
    component.strokes = [];
    component.strokeWeight = 0;
  }

  bindButtonGeometryVariables(
    component,
    metrics,
    config,
    variableByName,
    stats,
  );

  if (variant === "Glass") {
    component.effects = [
      {
        type: "DROP_SHADOW",
        color: { r: 0, g: 0, b: 0, a: 0.12 },
        offset: { x: 0, y: 8 },
        radius: 16,
        spread: -8,
        visible: true,
        blendMode: "NORMAL",
      },
    ];
  }

  if (state === "Loading") {
    component.appendChild(
      createSpinnerGlyph(
        config.foreground,
        config.foregroundFallback,
        variableByName,
        stats,
        metrics.spinnerSize,
        metrics.spinnerSizeToken,
      ),
    );
  }

  if (size !== "Icon") {
    const label = figma.createText();
    label.name = "Label Text";
    await applyButtonLabelTypography(
      label,
      fonts,
      textStyle,
      variableByName,
      stats,
    );
    label.characters = state === "Loading" ? "Loading" : variant;
    label.fills = [
      paintFromVariable(
        config.foreground,
        config.foregroundFallback,
        variableByName,
        stats,
      ),
    ];
    if (variant === "Link") label.textDecoration = "UNDERLINE";
    component.appendChild(label);
  }

  syncFocusRing(component, {
    enabled: state === "Default",
    width: metrics.width,
    height: metrics.height,
    radius: 8,
    variableName: "Colors/theme/500",
    fallback: "#135BEC",
    variableByName,
    stats,
  });

  return component;
}

function parseButtonVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (
    BUTTON_VARIANTS.indexOf(values.Variant) === -1 ||
    BUTTON_SIZES.indexOf(values.Size) === -1 ||
    BUTTON_STATES.indexOf(values.State) === -1
  ) {
    return null;
  }

  return {
    variant: values.Variant,
    size: values.Size,
    state: values.State,
  };
}

async function updateButtonVariant(
  component,
  { variant, size, state, variableByName, fonts, textStyle, stats },
) {
  const config = buttonConfig(variant, state);
  const metrics = buttonMetrics(size);

  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = metrics.gap;
  component.paddingLeft = metrics.paddingX;
  component.paddingRight = metrics.paddingX;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(metrics.width, metrics.height);
  component.cornerRadius = 8;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Button");

  component.fills = config.background
    ? [
        paintFromVariable(
          config.background,
          config.backgroundFallback,
          variableByName,
          stats,
        ),
      ]
    : [];

  if (config.stroke) {
    component.strokes = [
      paintFromVariable(
        config.stroke,
        config.strokeFallback,
        variableByName,
        stats,
      ),
    ];
    component.strokeWeight = 1;
  } else {
    component.strokes = [];
    component.strokeWeight = 0;
  }

  bindButtonGeometryVariables(
    component,
    metrics,
    config,
    variableByName,
    stats,
  );

  component.effects =
    variant === "Glass"
      ? [
          {
            type: "DROP_SHADOW",
            color: { r: 0, g: 0, b: 0, a: 0.12 },
            offset: { x: 0, y: 8 },
            radius: 16,
            spread: -8,
            visible: true,
            blendMode: "NORMAL",
          },
        ]
      : [];

  await syncButtonVariantChildren({
    component,
    variant,
    size,
    state,
    config,
    variableByName,
    fonts,
    textStyle,
    stats,
  });

  syncFocusRing(component, {
    enabled: state === "Default",
    width: metrics.width,
    height: metrics.height,
    radius: 8,
    variableName: "Colors/theme/500",
    fallback: "#135BEC",
    variableByName,
    stats,
  });
}

async function createIconButtonVariant({
  variant,
  size,
  state,
  variableByName,
  stats,
}) {
  const component = figma.createComponent();
  await updateIconButtonVariant(component, {
    variant,
    size,
    state,
    variableByName,
    stats,
  });
  return component;
}

function parseIconButtonVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (
    BUTTON_VARIANTS.indexOf(values.Variant) === -1 ||
    ICON_BUTTON_SIZES.indexOf(values.Size) === -1 ||
    BUTTON_STATES.indexOf(values.State) === -1
  ) {
    return null;
  }

  return {
    variant: values.Variant,
    size: values.Size,
    state: values.State,
  };
}

async function updateIconButtonVariant(
  component,
  { variant, size, state, variableByName, stats },
) {
  const config = buttonConfig(variant, state);
  const metrics = iconButtonMetrics(size);

  component.name = `Variant=${variant}, Size=${size}, State=${state}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 0;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(metrics.size, metrics.size);
  component.cornerRadius = metrics.size / 2;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "IconButton");

  component.fills = config.background
    ? [
        paintFromVariable(
          config.background,
          config.backgroundFallback,
          variableByName,
          stats,
        ),
      ]
    : [];

  if (config.stroke) {
    component.strokes = [
      paintFromVariable(
        config.stroke,
        config.strokeFallback,
        variableByName,
        stats,
      ),
    ];
    component.strokeWeight = 1;
  } else {
    component.strokes = [];
    component.strokeWeight = 0;
  }

  bindIconButtonGeometryVariables(
    component,
    metrics,
    config,
    variableByName,
    stats,
  );

  component.effects =
    variant === "Glass"
      ? [
          {
            type: "DROP_SHADOW",
            color: { r: 0, g: 0, b: 0, a: 0.12 },
            offset: { x: 0, y: 8 },
            radius: 16,
            spread: -8,
            visible: true,
            blendMode: "NORMAL",
          },
        ]
      : [];

  await syncIconButtonChildren({
    component,
    variant,
    state,
    config,
    variableByName,
    iconSize: metrics.iconSize,
    iconSizeToken: metrics.iconSizeToken,
    spinnerSize: metrics.spinnerSize,
    spinnerSizeToken: metrics.spinnerSizeToken,
    stats,
  });

  syncFocusRing(component, {
    enabled: state === "Default",
    width: metrics.size,
    height: metrics.size,
    radius: metrics.size / 2,
    variableName: "Colors/theme/500",
    fallback: "#135BEC",
    variableByName,
    stats,
  });
}

async function createToggleButtonVariant({
  variant,
  size,
  state,
  variableByName,
  fonts,
  textStyle,
  stats,
}) {
  const component = figma.createComponent();
  await updateToggleButtonVariant(component, {
    variant,
    size,
    state,
    variableByName,
    fonts,
    textStyle,
    stats,
  });
  return component;
}

function parseToggleButtonVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (
    TOGGLE_BUTTON_VARIANTS.indexOf(values.Variant) === -1 ||
    TOGGLE_BUTTON_SIZES.indexOf(values.Size) === -1 ||
    TOGGLE_BUTTON_STATES.indexOf(values.State) === -1
  ) {
    return null;
  }

  return {
    variant: values.Variant,
    size: values.Size,
    state: values.State,
  };
}

async function updateToggleButtonVariant(
  component,
  { variant, size, state, variableByName, fonts, textStyle, stats },
) {
  const config = toggleButtonConfig(variant, state);
  const metrics = toggleButtonMetrics(size);

  component.name = `Variant=${variant}, Size=${size}, State=${state}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 0;
  component.paddingLeft = metrics.paddingX;
  component.paddingRight = metrics.paddingX;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(metrics.width, metrics.height);
  component.cornerRadius = 12;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "ToggleButton");

  component.fills = config.background
    ? [
        paintFromVariable(
          config.background,
          config.backgroundFallback,
          variableByName,
          stats,
        ),
      ]
    : [];

  if (config.stroke) {
    component.strokes = [
      paintFromVariable(
        config.stroke,
        config.strokeFallback,
        variableByName,
        stats,
      ),
    ];
    component.strokeWeight = 1;
  } else {
    component.strokes = [];
    component.strokeWeight = 0;
  }

  bindToggleButtonGeometryVariables(
    component,
    metrics,
    config,
    variableByName,
    stats,
  );

  let label = directChildNamed(component, "Label Text");
  if (label && label.type !== "TEXT") {
    label.remove();
    label = null;
  }

  if (!label || label.type !== "TEXT") {
    label = figma.createText();
    label.name = "Label Text";
  }

  await applyButtonLabelTypography(
    label,
    fonts,
    textStyle,
    variableByName,
    stats,
  );
  label.characters = state === "Pressed" ? "Pressed" : "Toggle";
  label.fills = [
    paintFromVariable(
      config.foreground,
      config.foregroundFallback,
      variableByName,
      stats,
    ),
  ];
  label.textDecoration = "NONE";
  component.appendChild(label);
  setHugChildSizing(label);

  syncFocusRing(component, {
    enabled: state !== "Disabled",
    width: metrics.width,
    height: metrics.height,
    radius: 12,
    variableName: "Colors/theme/500",
    fallback: "#135BEC",
    variableByName,
    stats,
  });
}

async function createSplitButtonVariant({
  variant,
  size,
  state,
  variableByName,
  fonts,
  textStyle,
  stats,
}) {
  const component = figma.createComponent();
  await updateSplitButtonVariant(component, {
    variant,
    size,
    state,
    variableByName,
    fonts,
    textStyle,
    stats,
  });
  return component;
}

function parseSplitButtonVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (
    SPLIT_BUTTON_VARIANTS.indexOf(values.Variant) === -1 ||
    SPLIT_BUTTON_SIZES.indexOf(values.Size) === -1 ||
    SPLIT_BUTTON_STATES.indexOf(values.State) === -1
  ) {
    return null;
  }

  return {
    variant: values.Variant,
    size: values.Size,
    state: values.State,
  };
}

async function updateSplitButtonVariant(
  component,
  { variant, size, state, variableByName, fonts, textStyle, stats },
) {
  const config = splitButtonConfig(variant, state);
  const metrics = splitButtonMetrics(size);

  component.name = `Variant=${variant}, Size=${size}, State=${state}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 0;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(metrics.width, metrics.height);
  component.cornerRadius = 8;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "SplitButton");

  component.fills = config.background
    ? [
        paintFromVariable(
          config.background,
          config.backgroundFallback,
          variableByName,
          stats,
        ),
      ]
    : [];

  if (config.stroke) {
    component.strokes = [
      paintFromVariable(
        config.stroke,
        config.strokeFallback,
        variableByName,
        stats,
      ),
    ];
    component.strokeWeight = 1;
  } else {
    component.strokes = [];
    component.strokeWeight = 0;
  }

  let mainAction = directChildNamed(component, "Main Action");
  if (mainAction && mainAction.type !== "FRAME") {
    mainAction.remove();
    mainAction = null;
  }

  if (!mainAction || mainAction.type !== "FRAME") {
    mainAction = figma.createFrame();
    mainAction.name = "Main Action";
  }
  mainAction.layoutMode = "HORIZONTAL";
  mainAction.primaryAxisSizingMode = "FIXED";
  mainAction.counterAxisSizingMode = "FIXED";
  mainAction.primaryAxisAlignItems = "CENTER";
  mainAction.counterAxisAlignItems = "CENTER";
  mainAction.itemSpacing = 0;
  mainAction.paddingLeft = metrics.mainPaddingX;
  mainAction.paddingRight = metrics.mainPaddingX;
  mainAction.paddingTop = 0;
  mainAction.paddingBottom = 0;
  mainAction.resizeWithoutConstraints(
    metrics.width - metrics.triggerWidth - 1,
    metrics.height,
  );
  mainAction.fills = [];
  mainAction.strokes = [];
  mainAction.cornerRadius = 0;
  mainAction.clipsContent = false;
  setHorizontalStackFillChildSizing(mainAction);

  let label = directChildNamed(mainAction, "Label Text");
  if (label && label.type !== "TEXT") {
    label.remove();
    label = null;
  }

  if (!label || label.type !== "TEXT") {
    label = figma.createText();
    label.name = "Label Text";
  }

  await applyButtonLabelTypography(
    label,
    fonts,
    textStyle,
    variableByName,
    stats,
  );
  label.characters = variant === "Destructive" ? "Delete" : "Action";
  label.fills = [
    paintFromVariable(
      config.foreground,
      config.foregroundFallback,
      variableByName,
      stats,
    ),
  ];
  label.textDecoration = "NONE";
  mainAction.appendChild(label);
  setHugChildSizing(label);

  let divider = directChildNamed(component, "Divider");
  if (divider && divider.type !== "RECTANGLE") {
    divider.remove();
    divider = null;
  }

  if (!divider || divider.type !== "RECTANGLE") {
    divider = figma.createRectangle();
    divider.name = "Divider";
  }
  divider.resizeWithoutConstraints(1, metrics.height);
  divider.fills = [
    paintFromVariable(
      config.foreground,
      config.foregroundFallback,
      variableByName,
      stats,
    ),
  ];
  divider.opacity = state === "Disabled" ? 0.24 : 0.32;
  divider.strokes = [];
  setFixedChildSizing(divider);

  let trigger = directChildNamed(component, "Menu Trigger");
  if (trigger && trigger.type !== "FRAME") {
    trigger.remove();
    trigger = null;
  }

  if (!trigger || trigger.type !== "FRAME") {
    trigger = figma.createFrame();
    trigger.name = "Menu Trigger";
  }
  trigger.layoutMode = "HORIZONTAL";
  trigger.primaryAxisSizingMode = "FIXED";
  trigger.counterAxisSizingMode = "FIXED";
  trigger.primaryAxisAlignItems = "CENTER";
  trigger.counterAxisAlignItems = "CENTER";
  trigger.itemSpacing = 0;
  trigger.paddingLeft = 0;
  trigger.paddingRight = 0;
  trigger.paddingTop = 0;
  trigger.paddingBottom = 0;
  trigger.resizeWithoutConstraints(metrics.triggerWidth, metrics.height);
  trigger.fills = [];
  trigger.strokes = [];
  trigger.cornerRadius = 0;
  trigger.clipsContent = false;
  setFixedChildSizing(trigger);

  removeGeneratedButtonChild(trigger, "Icon", true);
  const icon = await createFixedIconInstance(
    "chevron-down",
    config.foreground,
    config.foregroundFallback,
    variableByName,
    stats,
    16,
    "SplitButton/icon/size",
  );
  trigger.appendChild(icon);

  component.appendChild(mainAction);
  component.appendChild(divider);
  component.appendChild(trigger);

  bindSplitButtonGeometryVariables(
    component,
    mainAction,
    trigger,
    divider,
    metrics,
    config,
    variableByName,
    stats,
  );

  syncFocusRing(component, {
    enabled: state !== "Disabled",
    width: metrics.width,
    height: metrics.height,
    radius: 8,
    variableName: "Colors/theme/500",
    fallback: "#135BEC",
    variableByName,
    stats,
  });
}

async function createFloatingActionButtonVariant({
  variant,
  size,
  state,
  iconComponent,
  variableByName,
  stats,
}) {
  const component = figma.createComponent();
  await updateFloatingActionButtonVariant(component, {
    variant,
    size,
    state,
    iconComponent,
    variableByName,
    stats,
  });
  return component;
}

function parseFloatingActionButtonVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (
    FLOATING_ACTION_BUTTON_VARIANTS.indexOf(values.Variant) === -1 ||
    FLOATING_ACTION_BUTTON_SIZES.indexOf(values.Size) === -1 ||
    FLOATING_ACTION_BUTTON_STATES.indexOf(values.State) === -1
  ) {
    return null;
  }

  return {
    variant: values.Variant,
    size: values.Size,
    state: values.State,
  };
}

async function updateFloatingActionButtonVariant(
  component,
  { variant, size, state, iconComponent, variableByName, stats },
) {
  const config = floatingActionButtonConfig(variant, state);
  const metrics = floatingActionButtonMetrics(size);

  component.name = `Variant=${variant}, Size=${size}, State=${state}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 0;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(metrics.size, metrics.size);
  component.cornerRadius = metrics.size / 2;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(
    RUN_NAMESPACE,
    "component",
    "FloatingActionButton",
  );

  component.fills = config.background
    ? [
        paintFromVariable(
          config.background,
          config.backgroundFallback,
          variableByName,
          stats,
        ),
      ]
    : [];

  if (config.stroke) {
    component.strokes = [
      paintFromVariable(
        config.stroke,
        config.strokeFallback,
        variableByName,
        stats,
      ),
    ];
    component.strokeWeight = 1;
  } else {
    component.strokes = [];
    component.strokeWeight = 0;
  }

  component.effects =
    state === "Disabled"
      ? []
      : [
          {
            type: "DROP_SHADOW",
            color: { r: 0, g: 0, b: 0, a: 0.18 },
            offset: { x: 0, y: 8 },
            radius: 16,
            spread: -8,
            visible: true,
            blendMode: "NORMAL",
          },
        ];

  bindFloatingActionButtonGeometryVariables(
    component,
    metrics,
    config,
    variableByName,
    stats,
  );

  removeGeneratedButtonChild(component, "Icon", true);
  let icon = null;
  const resolvedIconComponent =
    iconComponent ||
    (await resolveDefaultIconSourceComponent(variableByName, stats));
  if (resolvedIconComponent) {
    icon = createIconSlotInstance(
      resolvedIconComponent,
      config.foreground,
      config.foregroundFallback,
      variableByName,
      stats,
      metrics.iconSize,
      metrics.iconSizeToken,
    );
    component.appendChild(icon);
    setHugChildSizing(icon);
  }

  syncFocusRing(component, {
    enabled: state !== "Disabled",
    width: metrics.size,
    height: metrics.size,
    radius: metrics.size / 2,
    variableName: "Colors/theme/500",
    fallback: "#135BEC",
    variableByName,
    stats,
  });
}

async function createBadgeVariant({
  variant,
  size,
  variableByName,
  iconComponent,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateBadgeVariant(component, {
    variant,
    size,
    variableByName,
    iconComponent,
    fonts,
    stats,
  });
  return component;
}

async function createChipVariant({
  variant,
  size,
  state,
  removable,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateChipVariant(component, {
    variant,
    size,
    state,
    removable,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

async function createSegmentedControlVariant({
  size,
  active,
  state,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateSegmentedControlVariant(component, {
    size,
    active,
    state,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

async function createCounterVariant({
  tone,
  size,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateCounterVariant(component, {
    tone,
    size,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

async function createTextVariant({
  size,
  weight,
  tone,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateTextVariant(component, {
    size,
    weight,
    tone,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseTextVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (
    TEXT_SIZES.indexOf(values.Size) === -1 ||
    TEXT_WEIGHTS.indexOf(values.Weight) === -1 ||
    TEXT_TONES.indexOf(values.Tone) === -1
  ) {
    return null;
  }

  return {
    size: values.Size,
    weight: values.Weight,
    tone: values.Tone,
  };
}

async function updateTextVariant(
  component,
  { size, weight, tone, variableByName, fonts, stats },
) {
  const metrics = textMetrics(size);
  const color = textToneConfig(tone);

  component.name = `Size=${size}, Weight=${weight}, Tone=${tone}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "AUTO";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 0;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.fills = [];
  component.strokes = [];
  component.strokeWeight = 0;
  component.clipsContent = true;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Text");

  let text = directChildNamed(component, "Text");
  if (text && text.type !== "TEXT") {
    text.remove();
    text = null;
  }

  if (!text || text.type !== "TEXT") {
    text = figma.createText();
    text.name = "Text";
  }

  await applyTextTypography(text, size, weight, fonts, variableByName, stats);
  text.characters = textSampleForSize(size);
  text.fills = [
    paintFromVariable(
      color.foreground,
      color.foregroundFallback,
      variableByName,
      stats,
    ),
  ];
  setTextAutoResize(text, "WIDTH_AND_HEIGHT");
  component.appendChild(text);
}

async function createHeadingVariant({ value, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateHeadingVariant(component, {
    value,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseHeadingVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (HEADING_LEVELS.indexOf(values.Level) === -1) return null;

  return { value: values.Level, level: values.Level };
}

async function updateHeadingVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const metrics = headingMetrics(value);

  component.name = `Level=${value}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "AUTO";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 0;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.fills = [];
  component.strokes = [];
  component.strokeWeight = 0;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Heading");

  let text = directChildNamed(component, "Heading Text");
  if (text && text.type !== "TEXT") {
    text.remove();
    text = null;
  }

  if (!text || text.type !== "TEXT") {
    text = figma.createText();
    text.name = "Heading Text";
  }

  text.fontName = fonts.bold || fonts.medium;
  text.fontSize = metrics.fontSize;
  text.lineHeight = { unit: "PIXELS", value: metrics.lineHeight };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(text, textStyleKeyForHeading(value), stats);
  bindFloatVariable(
    text,
    "fontSize",
    `Text/font-size/${metrics.token}`,
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    `Text/line-height/${metrics.token}`,
    variableByName,
    stats,
  );
  text.characters = headingSampleForLevel(value);
  text.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  component.appendChild(text);
}

async function createLinkVariant({
  variant,
  state,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateLinkVariant(component, {
    variant,
    state,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseLinkVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (
    LINK_VARIANTS.indexOf(values.Variant) === -1 ||
    LINK_STATES.indexOf(values.State) === -1
  ) {
    return null;
  }

  return {
    variant: values.Variant,
    state: values.State,
  };
}

async function updateLinkVariant(
  component,
  { variant, state, variableByName, fonts, stats },
) {
  const config = linkConfig(variant);

  component.name = `Variant=${variant}, State=${state}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 0;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(128, 44);
  component.fills = [];
  component.strokes = [];
  component.strokeWeight = 0;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Link");

  bindSizeVariables(
    component,
    "Link/width/default",
    "Link/height/default",
    variableByName,
    stats,
  );

  let text = directChildNamed(component, "Link Text");
  if (text && text.type !== "TEXT") {
    text.remove();
    text = null;
  }

  if (!text || text.type !== "TEXT") {
    text = figma.createText();
    text.name = "Link Text";
  }

  text.fontName = fonts.medium;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  text.textDecoration = "UNDERLINE";
  await applyTextStyleToNodeAsync(text, "linkText", stats);
  bindFloatVariable(text, "fontSize", "Link/font-size", variableByName, stats);
  bindFloatVariable(
    text,
    "lineHeight",
    "Link/line-height",
    variableByName,
    stats,
  );
  text.characters = variant === "Subtle" ? "Learn more" : "Open link";
  text.fills = [
    paintFromVariable(
      config.foreground,
      config.foregroundFallback,
      variableByName,
      stats,
    ),
  ];
  component.appendChild(text);

  syncFocusRing(component, {
    enabled: state === "Focus",
    width: 128,
    height: 44,
    radius: 6,
    variableName: "Colors/theme/600",
    fallback: "#1051E8",
    variableByName,
    stats,
  });
  const ring = directChildNamed(component, "Focus Ring");
  if (ring) ring.visible = state === "Focus";
}

async function createLabelVariant({ value, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateLabelVariant(component, {
    value,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseLabelVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (LABEL_STATES.indexOf(values.State) === -1) return null;

  return { value: values.State, state: values.State };
}

async function updateLabelVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const disabled = value === "Disabled";

  component.name = `State=${value}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 0;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(96, 44);
  component.fills = [];
  component.strokes = [];
  component.strokeWeight = 0;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Label");
  bindFloatVariable(
    component,
    "height",
    "Label/height/default",
    variableByName,
    stats,
  );

  let text = directChildNamed(component, "Label Text");
  if (text && text.type !== "TEXT") {
    text.remove();
    text = null;
  }

  if (!text || text.type !== "TEXT") {
    text = figma.createText();
    text.name = "Label Text";
  }

  text.fontName = fonts.medium;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(text, "labelText", stats);
  bindFloatVariable(text, "fontSize", "Label/font-size", variableByName, stats);
  bindFloatVariable(
    text,
    "lineHeight",
    "Label/line-height",
    variableByName,
    stats,
  );
  text.characters = disabled ? "Disabled label" : "Label";
  text.fills = [
    paintFromVariable(
      disabled ? "Colors/foreground/500" : "Colors/foreground/0",
      disabled ? "#747B8B" : "#000000",
      variableByName,
      stats,
    ),
  ];
  component.appendChild(text);
}

async function createSeparatorVariant({ value, variableByName, stats }) {
  const component = figma.createComponent();
  await updateSeparatorVariant(component, {
    value,
    variableByName,
    stats,
  });
  return component;
}

function parseSeparatorVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (SEPARATOR_ORIENTATIONS.indexOf(values.Orientation) === -1) return null;

  return {
    value: values.Orientation,
    orientation: values.Orientation,
  };
}

async function updateSeparatorVariant(
  component,
  { value, variableByName, stats },
) {
  const horizontal = value === "Horizontal";
  component.name = `Orientation=${value}`;
  component.layoutMode = "NONE";
  component.resizeWithoutConstraints(horizontal ? 320 : 1, horizontal ? 1 : 44);
  component.fills = [
    paintFromVariable(
      "Colors/foreground/500",
      "#747B8B",
      variableByName,
      stats,
    ),
  ];
  component.strokes = [];
  component.strokeWeight = 0;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Separator");
  bindSizeVariables(
    component,
    horizontal ? "Separator/length/default" : "Separator/thickness",
    horizontal ? "Separator/thickness" : "Separator/height/vertical",
    variableByName,
    stats,
  );
}

async function createSkeletonVariant({ value, variableByName, stats }) {
  const component = figma.createComponent();
  await updateSkeletonVariant(component, {
    value,
    variableByName,
    stats,
  });
  return component;
}

function parseSkeletonVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (SKELETON_SHAPES.indexOf(values.Shape) === -1) return null;

  return {
    value: values.Shape,
    shape: values.Shape,
  };
}

async function updateSkeletonVariant(
  component,
  { value, variableByName, stats },
) {
  const metrics = skeletonMetrics(value);
  component.name = `Shape=${value}`;
  component.layoutMode = "NONE";
  component.resizeWithoutConstraints(metrics.width, metrics.height);
  component.cornerRadius = metrics.radius;
  component.fills = [
    paintFromVariable(
      "Colors/background/200",
      "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  component.strokes = [];
  component.strokeWeight = 0;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Skeleton");

  bindSizeVariables(
    component,
    metrics.widthToken,
    metrics.heightToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "cornerRadius",
    metrics.radiusToken,
    variableByName,
    stats,
  );
}

async function createBoxVariant({ value, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateBoxVariant(component, {
    value,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseBoxVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (BOX_SURFACES.indexOf(values.Surface) === -1) return null;

  return {
    value: values.Surface,
    surface: values.Surface,
  };
}

async function updateBoxVariant(component, { value, variableByName, stats }) {
  const config = boxSurfaceConfig(value);
  component.name = `Surface=${value}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 0;
  component.paddingLeft = 16;
  component.paddingRight = 16;
  component.paddingTop = 16;
  component.paddingBottom = 16;
  component.resizeWithoutConstraints(320, 120);
  component.cornerRadius = 8;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Box");

  component.fills = config.background
    ? [
        paintFromVariable(
          config.background,
          config.backgroundFallback,
          variableByName,
          stats,
        ),
      ]
    : [];

  if (config.stroke) {
    component.strokes = [
      paintFromVariable(
        config.stroke,
        config.strokeFallback,
        variableByName,
        stats,
      ),
    ];
    component.strokeWeight = 1;
  } else {
    component.strokes = [];
    component.strokeWeight = 0;
  }

  bindSizeVariables(
    component,
    "Box/width/default",
    "Box/min-height/default",
    variableByName,
    stats,
  );
  for (const field of [
    "paddingLeft",
    "paddingRight",
    "paddingTop",
    "paddingBottom",
  ]) {
    bindFloatVariable(
      component,
      field,
      "Box/padding/default",
      variableByName,
      stats,
    );
  }
  bindFloatVariable(
    component,
    "cornerRadius",
    "Box/radius",
    variableByName,
    stats,
  );
  if (config.stroke) {
    bindFloatVariable(
      component,
      "strokeWeight",
      "Box/stroke/width",
      variableByName,
      stats,
    );
  }

  removeDirectChildrenNamed(component, ["Box Text", "Content Slot"]);
  const contentSlot = appendComponentContentSlot({
    component,
    height: 88,
    ownerName: "Box",
    parent: component,
    slotName: "Content Slot",
    stats,
    width: 288,
  });
  setVerticalStackFillChildSizing(contentSlot);
}

async function createStackVariant({ direction, gap, variableByName, stats }) {
  const component = figma.createComponent();
  await updateStackVariant(component, {
    direction,
    gap,
    variableByName,
    stats,
  });
  return component;
}

function parseStackVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (
    STACK_DIRECTIONS.indexOf(values.Direction) === -1 ||
    STACK_GAPS.indexOf(values.Gap) === -1
  ) {
    return null;
  }

  return {
    direction: values.Direction,
    gap: values.Gap,
  };
}

async function updateStackVariant(
  component,
  { direction, gap, variableByName, stats },
) {
  const horizontal = direction === "Row";
  component.name = `Direction=${direction}, Gap=${gap}`;
  component.layoutMode = horizontal ? "HORIZONTAL" : "VERTICAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = Number(gap) * 4;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(320, horizontal ? 72 : 120);
  component.fills = [];
  component.strokes = [];
  component.strokeWeight = 0;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Stack");

  bindSizeVariables(
    component,
    "Stack/width/default",
    horizontal ? "Stack/height/row" : "Stack/height/column",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    `Stack/gap/${gap}`,
    variableByName,
    stats,
  );

  const existingItems = (component.children || []).filter(
    (child) => /^Stack Item /.test(child.name) || child.name === "Content Slot",
  );
  for (const child of existingItems) child.remove();

  const contentSlot = appendComponentContentSlot({
    component,
    height: horizontal ? 72 : 120,
    itemSpacing: Number(gap) * 4,
    layoutMode: horizontal ? "HORIZONTAL" : "VERTICAL",
    ownerName: "Stack",
    parent: component,
    slotName: "Content Slot",
    stats,
    width: 320,
  });
  setVerticalStackFillChildSizing(contentSlot);
}

async function createGridVariant({ columns, gap, variableByName, stats }) {
  const component = figma.createComponent();
  await updateGridVariant(component, {
    columns,
    gap,
    variableByName,
    stats,
  });
  return component;
}

function parseGridVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (
    GRID_COLUMNS.indexOf(values.Columns) === -1 ||
    GRID_GAPS.indexOf(values.Gap) === -1
  ) {
    return null;
  }

  return {
    columns: values.Columns,
    gap: values.Gap,
  };
}

async function updateGridVariant(
  component,
  { columns, gap, variableByName, stats },
) {
  const gapPx = Number(gap) * 4;
  const columnCount = Number(columns);
  const width = 360;
  const cellHeight = 40;
  const height =
    GRID_PREVIEW_ROWS * cellHeight + (GRID_PREVIEW_ROWS - 1) * gapPx;
  const cellWidth = (width - (columnCount - 1) * gapPx) / columnCount;

  component.name = `Columns=${columns}, Gap=${gap}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = gapPx;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(width, height);
  component.fills = [];
  component.strokes = [];
  component.strokeWeight = 0;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Grid");

  bindSizeVariables(
    component,
    "Grid/width/default",
    `Grid/height/${gap}`,
    variableByName,
    stats,
  );

  removeDirectChildrenMatching(
    component,
    (child) =>
      isGridCellSlotName(child.name) ||
      isGridRowFrameName(child.name) ||
      isLegacyGridCellFrameName(child.name) ||
      child.name === "Content Slot",
  );

  for (let rowIndex = 0; rowIndex < GRID_PREVIEW_ROWS; rowIndex += 1) {
    const row = createGridRowFrame({
      gapPx,
      height: cellHeight,
      rowIndex,
      width,
    });
    component.appendChild(row);
    setVerticalStackFillChildSizing(row);

    for (let columnIndex = 0; columnIndex < columnCount; columnIndex += 1) {
      const slotIndex = rowIndex * columnCount + columnIndex;
      const cellSlot = appendComponentContentSlot({
        component,
        height: cellHeight,
        itemSpacing: 0,
        layoutMode: "VERTICAL",
        ownerName: "Grid",
        parent: row,
        slotName: `Cell ${slotIndex + 1} Slot`,
        stats,
        width: cellWidth,
      });
      cellSlot.setSharedPluginData(RUN_NAMESPACE, "component", "Grid");
      cellSlot.setSharedPluginData(
        RUN_NAMESPACE,
        "grid-cell-index",
        String(slotIndex + 1),
      );
      cellSlot.setSharedPluginData(
        RUN_NAMESPACE,
        "grid-row",
        String(rowIndex + 1),
      );
      cellSlot.setSharedPluginData(
        RUN_NAMESPACE,
        "grid-column",
        String(columnIndex + 1),
      );
      bindGridCellSlotVariables(cellSlot, variableByName, stats);
      setHorizontalStackFillChildSizing(cellSlot);
    }
  }
}

function createGridRowFrame({ gapPx, height, rowIndex, width }) {
  const row = figma.createFrame();
  row.name = `Grid Row ${rowIndex + 1}`;
  row.layoutMode = "HORIZONTAL";
  row.primaryAxisSizingMode = "FIXED";
  row.counterAxisSizingMode = "FIXED";
  row.primaryAxisAlignItems = "MIN";
  row.counterAxisAlignItems = "MIN";
  row.itemSpacing = gapPx;
  row.paddingLeft = 0;
  row.paddingRight = 0;
  row.paddingTop = 0;
  row.paddingBottom = 0;
  row.resizeWithoutConstraints(width, height);
  row.fills = [];
  row.strokes = [];
  row.clipsContent = false;
  row.setSharedPluginData(RUN_NAMESPACE, "kind", "grid-row");
  row.setSharedPluginData(RUN_NAMESPACE, "component", "Grid");
  return row;
}

function gridCellSlotNames(columns, rows) {
  const names = [];
  const total = columns * rows;
  for (let index = 0; index < total; index += 1) {
    names.push(`Cell ${index + 1} Slot`);
  }
  return names;
}

function maxGridColumnCount() {
  let max = 0;
  for (const columns of GRID_COLUMNS) {
    max = Math.max(max, Number(columns));
  }
  return max;
}

function isGridCellSlotName(name) {
  return /^Cell \d+ Slot$/.test(name || "");
}

function isGridRowFrameName(name) {
  return /^Grid Row(?: \d+)?$/.test(name || "");
}

function isLegacyGridCellFrameName(name) {
  return /^Grid Cell(?: \d+)?$/.test(name || "");
}

function bindGridCellSlotVariables(slot, variableByName, stats) {
  bindFloatVariable(
    slot,
    "height",
    "Grid/cell/height/default",
    variableByName,
    stats,
  );
}

async function createContainerVariant({ value, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateContainerVariant(component, {
    value,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseContainerVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (CONTAINER_CENTERED.indexOf(values.Centered) === -1) return null;

  return {
    value: values.Centered,
    centered: values.Centered,
  };
}

async function updateContainerVariant(
  component,
  { value, variableByName, stats },
) {
  const centered = value === "True";
  component.name = `Centered=${value}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = centered ? "CENTER" : "MIN";
  component.itemSpacing = 0;
  component.paddingLeft = 24;
  component.paddingRight = 24;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(480, 120);
  component.fills = [];
  component.strokes = [];
  component.strokeWeight = 0;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Container");

  bindSizeVariables(
    component,
    "Container/width/default",
    "Container/min-height/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingLeft",
    "Container/padding/x",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "paddingRight",
    "Container/padding/x",
    variableByName,
    stats,
  );

  let content = directChildNamed(component, "Container Content");
  if (content && content.type !== "FRAME") {
    content.remove();
    content = null;
  }

  if (!content || content.type !== "FRAME") {
    content = figma.createFrame();
    content.name = "Container Content";
  }

  const contentWidth = centered ? 320 : 432;
  content.layoutMode = "HORIZONTAL";
  content.primaryAxisSizingMode = "FIXED";
  content.counterAxisSizingMode = "FIXED";
  content.primaryAxisAlignItems = "CENTER";
  content.counterAxisAlignItems = "CENTER";
  content.itemSpacing = 0;
  content.paddingLeft = 16;
  content.paddingRight = 16;
  content.paddingTop = 16;
  content.paddingBottom = 16;
  content.resizeWithoutConstraints(contentWidth, 64);
  content.cornerRadius = 8;
  content.fills = [
    paintFromVariable(
      "Colors/background/100",
      "#F4F5F7",
      variableByName,
      stats,
    ),
  ];
  content.strokes = [
    paintFromVariable(
      "Colors/background/200",
      "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  content.strokeWeight = 1;
  content.clipsContent = false;

  bindSizeVariables(
    content,
    centered
      ? "Container/content-width/centered"
      : "Container/content-width/fluid",
    "Container/content-height",
    variableByName,
    stats,
  );
  for (const field of [
    "paddingLeft",
    "paddingRight",
    "paddingTop",
    "paddingBottom",
  ]) {
    bindFloatVariable(
      content,
      field,
      "Container/content-padding",
      variableByName,
      stats,
    );
  }
  bindFloatVariable(
    content,
    "cornerRadius",
    "Container/content-radius",
    variableByName,
    stats,
  );
  bindFloatVariable(
    content,
    "strokeWeight",
    "Container/content-stroke/width",
    variableByName,
    stats,
  );

  removeDirectChildrenNamed(content, ["Container Text", "Content Slot"]);
  const contentSlot = appendComponentContentSlot({
    component,
    height: 32,
    layoutMode: "HORIZONTAL",
    ownerName: "Container",
    parent: content,
    slotName: "Content Slot",
    stats,
    width: contentWidth - 32,
  });
  setLayoutSizingHorizontal(contentSlot, "FILL");
  setLayoutSizingVertical(contentSlot, "FILL");
  component.appendChild(content);
}

async function createBreadcrumbVariant({
  value,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateBreadcrumbVariant(component, {
    value,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseBreadcrumbVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (BREADCRUMB_CONTENT.indexOf(values.Content) === -1) return null;

  return {
    value: values.Content,
    content: values.Content,
  };
}

async function updateBreadcrumbVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const isEllipsis = value === "Ellipsis";
  component.name = `Content=${value}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 8;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(isEllipsis ? 284 : 360, 44);
  component.fills = [];
  component.strokes = [];
  component.strokeWeight = 0;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Breadcrumb");

  bindSizeVariables(
    component,
    isEllipsis ? "Breadcrumb/width/ellipsis" : "Breadcrumb/width/basic",
    "Breadcrumb/height",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "Breadcrumb/gap",
    variableByName,
    stats,
  );

  const oldChildren = [];
  for (const child of component.children || []) {
    oldChildren.push(child);
  }
  for (const child of oldChildren) {
    child.remove();
  }

  await appendBreadcrumbText({
    parent: component,
    name: "Item 1 Text",
    characters: "Home",
    colorToken: "Colors/foreground/400",
    colorFallback: "#5D626F",
    fonts,
    variableByName,
    stats,
  });
  await appendBreadcrumbSeparator(component, variableByName, stats);

  if (isEllipsis) {
    appendBreadcrumbEllipsis(component, variableByName, stats);
  } else {
    await appendBreadcrumbText({
      parent: component,
      name: "Item 2 Text",
      characters: "Components",
      colorToken: "Colors/foreground/400",
      colorFallback: "#5D626F",
      fonts,
      variableByName,
      stats,
    });
  }

  await appendBreadcrumbSeparator(component, variableByName, stats);
  await appendBreadcrumbText({
    parent: component,
    name: "Current Page Text",
    characters: "Breadcrumb",
    colorToken: "Colors/foreground/0",
    colorFallback: "#000000",
    fonts,
    variableByName,
    stats,
  });
}

async function appendBreadcrumbText({
  parent,
  name,
  characters,
  colorToken,
  colorFallback,
  fonts,
  variableByName,
  stats,
}) {
  const text = figma.createText();
  text.name = name;
  text.fontName = fonts.regular;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  await applyTextStyleToNodeAsync(text, "breadcrumbItem", stats);
  text.characters = characters;
  text.fills = [
    paintFromVariable(colorToken, colorFallback, variableByName, stats),
  ];
  setTextAutoResize(text, "WIDTH_AND_HEIGHT");
  bindFloatVariable(
    text,
    "fontSize",
    "Breadcrumb/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Breadcrumb/line-height",
    variableByName,
    stats,
  );
  parent.appendChild(text);
  setHugChildSizing(text);
  return text;
}

async function appendBreadcrumbSeparator(parent, variableByName, stats) {
  const icon = await createFixedIconInstance(
    "chevron-right",
    "Colors/foreground/400",
    "#5D626F",
    variableByName,
    stats,
    14,
  );
  parent.appendChild(icon);
  setHugChildSizing(icon);
  return icon;
}

function appendBreadcrumbEllipsis(parent, variableByName, stats) {
  const frame = figma.createFrame();
  frame.name = "Breadcrumb Ellipsis";
  frame.layoutMode = "HORIZONTAL";
  frame.primaryAxisSizingMode = "FIXED";
  frame.counterAxisSizingMode = "FIXED";
  frame.primaryAxisAlignItems = "CENTER";
  frame.counterAxisAlignItems = "CENTER";
  frame.itemSpacing = 3;
  frame.paddingLeft = 0;
  frame.paddingRight = 0;
  frame.paddingTop = 0;
  frame.paddingBottom = 0;
  frame.resizeWithoutConstraints(36, 36);
  frame.fills = [];
  frame.strokes = [];
  frame.strokeWeight = 0;
  frame.clipsContent = false;
  bindSizeVariables(
    frame,
    "Breadcrumb/ellipsis/size",
    "Breadcrumb/ellipsis/size",
    variableByName,
    stats,
  );

  for (let index = 0; index < 3; index += 1) {
    const dot = figma.createEllipse();
    dot.name = "Ellipsis Dot";
    dot.resizeWithoutConstraints(4, 4);
    dot.fills = [
      paintFromVariable(
        "Colors/foreground/400",
        "#5D626F",
        variableByName,
        stats,
      ),
    ];
    dot.strokes = [];
    frame.appendChild(dot);
    setHugChildSizing(dot);
  }

  parent.appendChild(frame);
}

async function createPaginationVariant({
  content,
  size,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updatePaginationVariant(component, {
    content,
    size,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parsePaginationVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (
    PAGINATION_CONTENT.indexOf(values.Content) === -1 ||
    PAGINATION_SIZES.indexOf(values.Size) === -1
  ) {
    return null;
  }

  return {
    content: values.Content,
    size: values.Size,
  };
}

function paginationVariantKey(props) {
  return `${props.content}/${props.size}`;
}

function layoutPaginationVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parsePaginationVariantName(child.name);
    if (!props) continue;

    const contentIndex = PAGINATION_CONTENT.indexOf(props.content);
    const sizeIndex = PAGINATION_SIZES.indexOf(props.size);
    child.x = contentIndex * 540;
    child.y = sizeIndex * 100;
  }
}

async function updatePaginationVariant(
  component,
  { content, size, variableByName, fonts, stats },
) {
  const metrics = paginationMetrics(size, content);

  component.name = `Content=${content}, Size=${size}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = metrics.gap;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(metrics.width, metrics.height);
  component.fills = [];
  component.strokes = [];
  component.strokeWeight = 0;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Pagination");

  const oldChildren = Array.from(component.children || []);
  for (const child of oldChildren) child.remove();

  const controls = [];
  controls.push(
    await appendPaginationControl({
      parent: component,
      name: "Pagination Previous",
      textName: "Previous Text",
      text: "Previous",
      role: "previous",
      widthToken: metrics.actionWidthToken,
      width: metrics.actionWidth,
      icon: "chevron-left",
      iconPosition: "before",
      active: false,
      metrics,
      variableByName,
      fonts,
      stats,
    }),
  );

  if (content === "Compact") {
    controls.push(
      await appendPaginationControl({
        parent: component,
        name: "Pagination Compact",
        textName: "Compact Text",
        text: "2 / 10",
        role: "compact",
        widthToken: metrics.compactLabelWidthToken,
        width: metrics.compactLabelWidth,
        active: false,
        quiet: true,
        metrics,
        variableByName,
        fonts,
        stats,
      }),
    );
  } else {
    controls.push(
      await appendPaginationControl({
        parent: component,
        name: "Pagination Page 1",
        textName: "Page 1 Text",
        text: "1",
        role: "page",
        width: metrics.itemSize,
        active: false,
        metrics,
        variableByName,
        fonts,
        stats,
      }),
    );
    controls.push(
      await appendPaginationControl({
        parent: component,
        name: "Pagination Page 2",
        textName: "Page 2 Text",
        text: "2",
        role: "page",
        width: metrics.itemSize,
        active: true,
        metrics,
        variableByName,
        fonts,
        stats,
      }),
    );
    controls.push(
      await appendPaginationControl({
        parent: component,
        name: "Pagination Page 3",
        textName: "Page 3 Text",
        text: "3",
        role: "page",
        width: metrics.itemSize,
        active: false,
        metrics,
        variableByName,
        fonts,
        stats,
      }),
    );

    if (content === "Ellipsis") {
      controls.push(
        appendPaginationEllipsis(component, metrics, variableByName, stats),
      );
      controls.push(
        await appendPaginationControl({
          parent: component,
          name: "Pagination Last Page",
          textName: "Last Page Text",
          text: "10",
          role: "page",
          width: metrics.itemSize,
          active: false,
          metrics,
          variableByName,
          fonts,
          stats,
        }),
      );
    }
  }

  controls.push(
    await appendPaginationControl({
      parent: component,
      name: "Pagination Next",
      textName: "Next Text",
      text: "Next",
      role: "next",
      widthToken: metrics.actionWidthToken,
      width: metrics.actionWidth,
      icon: "chevron-right",
      iconPosition: "after",
      active: false,
      metrics,
      variableByName,
      fonts,
      stats,
    }),
  );

  bindPaginationGeometryVariables(
    component,
    metrics,
    controls,
    variableByName,
    stats,
  );
}

async function appendPaginationControl({
  parent,
  name,
  textName,
  text,
  role,
  widthToken,
  width,
  icon,
  iconPosition,
  active,
  quiet,
  metrics,
  variableByName,
  fonts,
  stats,
}) {
  const frame = figma.createFrame();
  frame.name = name;
  frame.layoutMode = "HORIZONTAL";
  frame.primaryAxisSizingMode = "FIXED";
  frame.counterAxisSizingMode = "FIXED";
  frame.primaryAxisAlignItems = "CENTER";
  frame.counterAxisAlignItems = "CENTER";
  frame.itemSpacing = icon ? 6 : 0;
  frame.paddingLeft = 0;
  frame.paddingRight = 0;
  frame.paddingTop = 0;
  frame.paddingBottom = 0;
  frame.resizeWithoutConstraints(width, metrics.height);
  frame.cornerRadius = metrics.radius;
  frame.clipsContent = false;
  frame.setSharedPluginData(RUN_NAMESPACE, "kind", "pagination-control");
  frame.setSharedPluginData(RUN_NAMESPACE, "role", role);

  if (active) {
    frame.fills = [
      paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
    ];
    frame.strokes = [
      paintFromVariable("Colors/theme/600", "#1051E8", variableByName, stats),
    ];
    frame.strokeWeight = 1;
  } else {
    frame.fills = [];
    frame.strokes = [];
    frame.strokeWeight = 0;
  }

  const foreground = active
    ? { token: "Colors/theme/600", fallback: "#1051E8" }
    : quiet
      ? { token: "Colors/foreground/400", fallback: "#5D626F" }
      : { token: "Colors/foreground/0", fallback: "#000000" };

  async function appendIcon(iconName) {
    const iconNode = await createFixedIconInstance(
      iconName,
      foreground.token,
      foreground.fallback,
      variableByName,
      stats,
      metrics.iconSize,
      "Pagination/icon/size",
    );
    frame.appendChild(iconNode);
    setHugChildSizing(iconNode);
  }

  if (icon && iconPosition === "before") await appendIcon(icon);

  const label = figma.createText();
  label.name = textName;
  await applyPaginationLabelTypography(
    label,
    metrics,
    fonts,
    variableByName,
    stats,
  );
  label.characters = text;
  label.fills = [
    paintFromVariable(
      foreground.token,
      foreground.fallback,
      variableByName,
      stats,
    ),
  ];
  label.textAlignHorizontal = "CENTER";
  label.textAlignVertical = "CENTER";
  frame.appendChild(label);
  setHugChildSizing(label);

  if (icon && iconPosition === "after") await appendIcon(icon);

  parent.appendChild(frame);

  return {
    node: frame,
    widthToken: widthToken || "Pagination/item/size",
    hasStroke: active,
  };
}

function appendPaginationEllipsis(parent, metrics, variableByName, stats) {
  const frame = figma.createFrame();
  frame.name = "Pagination Ellipsis";
  frame.layoutMode = "HORIZONTAL";
  frame.primaryAxisSizingMode = "FIXED";
  frame.counterAxisSizingMode = "FIXED";
  frame.primaryAxisAlignItems = "CENTER";
  frame.counterAxisAlignItems = "CENTER";
  frame.itemSpacing = 3;
  frame.paddingLeft = 0;
  frame.paddingRight = 0;
  frame.paddingTop = 0;
  frame.paddingBottom = 0;
  frame.resizeWithoutConstraints(metrics.itemSize, metrics.height);
  frame.cornerRadius = metrics.radius;
  frame.fills = [];
  frame.strokes = [];
  frame.strokeWeight = 0;
  frame.clipsContent = false;
  frame.setSharedPluginData(RUN_NAMESPACE, "kind", "pagination-control");
  frame.setSharedPluginData(RUN_NAMESPACE, "role", "ellipsis");

  for (let index = 0; index < 3; index += 1) {
    const dot = figma.createEllipse();
    dot.name = "Ellipsis Dot";
    dot.resizeWithoutConstraints(4, 4);
    dot.fills = [
      paintFromVariable(
        "Colors/foreground/400",
        "#5D626F",
        variableByName,
        stats,
      ),
    ];
    dot.strokes = [];
    frame.appendChild(dot);
    setHugChildSizing(dot);
  }

  parent.appendChild(frame);

  return {
    node: frame,
    widthToken: "Pagination/item/size",
    hasStroke: false,
  };
}

async function createAccordionVariant({ value, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateAccordionVariant(component, {
    value,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseAccordionVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (ACCORDION_STATES.indexOf(values.State) === -1) return null;

  return {
    value: values.State,
    state: values.State,
  };
}

async function updateAccordionVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const isOpen = value === "Open";
  component.name = `State=${value}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 0;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(360, isOpen ? 128 : 53);
  component.fills = [];
  component.strokes = [
    paintFromVariable(
      "Colors/background/200",
      "#C7CAD1",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.strokeTopWeight = 0;
  component.strokeRightWeight = 0;
  component.strokeBottomWeight = 1;
  component.strokeLeftWeight = 0;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Accordion");

  bindFloatVariable(
    component,
    "width",
    "Accordion/width/default",
    variableByName,
    stats,
  );
  clearBoundVariable(component, "height", stats);
  setLayoutSizingHorizontal(component, "FIXED");
  setLayoutSizingVertical(component, "HUG");
  bindFloatVariable(
    component,
    "strokeBottomWeight",
    "Accordion/divider/height",
    variableByName,
    stats,
  );

  const oldChildren = [];
  for (const child of component.children || []) {
    oldChildren.push(child);
  }
  for (const child of oldChildren) {
    child.remove();
  }

  const trigger = figma.createFrame();
  trigger.name = "Accordion Trigger";
  trigger.layoutMode = "HORIZONTAL";
  trigger.primaryAxisSizingMode = "FIXED";
  trigger.counterAxisSizingMode = "FIXED";
  trigger.primaryAxisAlignItems = "MIN";
  trigger.counterAxisAlignItems = "CENTER";
  trigger.itemSpacing = 16;
  trigger.paddingLeft = 0;
  trigger.paddingRight = 0;
  trigger.paddingTop = 0;
  trigger.paddingBottom = 0;
  trigger.resizeWithoutConstraints(360, 52);
  trigger.fills = [];
  trigger.strokes = [];
  trigger.strokeWeight = 0;
  trigger.clipsContent = false;
  bindSizeVariables(
    trigger,
    "Accordion/width/default",
    "Accordion/trigger/height",
    variableByName,
    stats,
  );
  setVerticalFixedFillChildSizing(trigger);

  await appendAccordionText({
    parent: trigger,
    name: "Trigger Text",
    characters: "Is it accessible?",
    fontToken: "Accordion/trigger/font-size",
    lineHeightToken: "Accordion/trigger/line-height",
    colorToken: "Colors/foreground/0",
    colorFallback: "#000000",
    fillHorizontal: true,
    fonts,
    variableByName,
    stats,
  });
  await appendAccordionChevron({
    parent: trigger,
    isOpen,
    variableByName,
    stats,
  });
  component.appendChild(trigger);

  if (isOpen) {
    const content = figma.createFrame();
    content.name = "Accordion Content";
    content.layoutMode = "VERTICAL";
    content.primaryAxisSizingMode = "AUTO";
    content.counterAxisSizingMode = "FIXED";
    content.primaryAxisAlignItems = "MIN";
    content.counterAxisAlignItems = "MIN";
    content.itemSpacing = 0;
    content.paddingLeft = 0;
    content.paddingRight = 0;
    content.paddingTop = 0;
    content.paddingBottom = 16;
    content.resizeWithoutConstraints(360, 36);
    content.fills = [];
    content.strokes = [];
    content.strokeWeight = 0;
    content.clipsContent = false;
    bindFloatVariable(
      content,
      "width",
      "Accordion/width/default",
      variableByName,
      stats,
    );
    clearBoundVariable(content, "height", stats);
    bindFloatVariable(
      content,
      "paddingBottom",
      "Accordion/content/padding-bottom",
      variableByName,
      stats,
    );
    setVerticalStackChildSizing(content);
    await appendAccordionText({
      parent: content,
      name: "Content Text",
      characters: "Yes. It follows the WAI-ARIA disclosure pattern.",
      fontToken: "Accordion/content/font-size",
      lineHeightToken: "Accordion/content/line-height",
      colorToken: "Colors/foreground/400",
      colorFallback: "#5D626F",
      fillHorizontal: true,
      fonts,
      variableByName,
      stats,
    });
    component.appendChild(content);
  }
}

async function appendAccordionText({
  parent,
  name,
  characters,
  fontToken,
  lineHeightToken,
  colorToken,
  colorFallback,
  fillHorizontal = false,
  fonts,
  variableByName,
  stats,
}) {
  const text = figma.createText();
  text.name = name;
  text.fontName = fonts.regular;
  text.fontSize = 14;
  text.lineHeight = { unit: "PIXELS", value: 20 };
  await applyTextStyleToNodeAsync(
    text,
    name === "Trigger Text" ? "accordionTrigger" : "accordionContent",
    stats,
  );
  text.characters = characters;
  text.fills = [
    paintFromVariable(colorToken, colorFallback, variableByName, stats),
  ];
  setNodePropertyIfSupported(text, "textAlignHorizontal", "LEFT");
  setNodePropertyIfSupported(
    text,
    "textAlignVertical",
    name === "Trigger Text" ? "CENTER" : "TOP",
  );
  setTextAutoResize(text, fillHorizontal ? "HEIGHT" : "WIDTH_AND_HEIGHT");
  bindFloatVariable(text, "fontSize", fontToken, variableByName, stats);
  bindFloatVariable(text, "lineHeight", lineHeightToken, variableByName, stats);
  parent.appendChild(text);
  if (fillHorizontal) {
    setLayoutSizingHorizontal(text, "FILL");
    setLayoutSizingVertical(text, "HUG");
    try {
      text.layoutAlign = "STRETCH";
    } catch (_error) {
      // layoutAlign is unavailable on older plugin runtimes.
    }
    try {
      text.layoutGrow = 1;
    } catch (_error) {
      // layoutGrow is unavailable on older plugin runtimes.
    }
    try {
      text.constraints = { horizontal: "STRETCH", vertical: "MIN" };
    } catch (_error) {
      // Constraints are not available on every node type.
    }
  } else {
    setHugChildSizing(text);
  }
  return text;
}

async function appendAccordionChevron({
  parent,
  isOpen,
  variableByName,
  stats,
}) {
  const icon = await createFixedIconInstance(
    isOpen ? "chevron-up" : "chevron-down",
    "Colors/foreground/400",
    "#5D626F",
    variableByName,
    stats,
    16,
  );
  parent.appendChild(icon);
  setHugChildSizing(icon);
  return icon;
}

function parseCounterVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (
    COUNTER_TONES.indexOf(values.Tone) === -1 ||
    COUNTER_SIZES.indexOf(values.Size) === -1
  ) {
    return null;
  }

  return {
    tone: values.Tone,
    size: values.Size,
  };
}

function parseChipVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (
    CHIP_VARIANTS.indexOf(values.Variant) === -1 ||
    CHIP_SIZES.indexOf(values.Size) === -1 ||
    CHIP_STATES.indexOf(values.State) === -1 ||
    CHIP_REMOVABLE.indexOf(values.Removable) === -1
  ) {
    return null;
  }

  return {
    variant: values.Variant,
    size: values.Size,
    state: values.State,
    removable: values.Removable,
  };
}

function chipVariantKey(props) {
  return `${props.variant}/${props.size}/${props.state}/${props.removable}`;
}

function layoutChipVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseChipVariantName(child.name);
    if (!props) continue;

    const variantIndex = CHIP_VARIANTS.indexOf(props.variant);
    const sizeIndex = CHIP_SIZES.indexOf(props.size);
    const stateIndex = CHIP_STATES.indexOf(props.state);
    const removableIndex = CHIP_REMOVABLE.indexOf(props.removable);
    const flatIndex =
      ((variantIndex * CHIP_SIZES.length + sizeIndex) * CHIP_STATES.length +
        stateIndex) *
        CHIP_REMOVABLE.length +
      removableIndex;
    child.x = (flatIndex % 6) * 180;
    child.y = Math.floor(flatIndex / 6) * 76;
  }
}

async function updateChipVariant(
  component,
  { variant, size, state, removable, variableByName, fonts, stats },
) {
  const metrics = chipMetrics(size);
  const config = chipConfig(variant, state);

  component.name = `Variant=${variant}, Size=${size}, State=${state}, Removable=${removable}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = metrics.gap;
  component.paddingLeft = metrics.paddingX;
  component.paddingRight = metrics.paddingX;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(metrics.minWidth, metrics.height);
  component.cornerRadius = 9999;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Chip");

  component.fills = config.background
    ? [
        paintFromVariable(
          config.background,
          config.backgroundFallback,
          variableByName,
          stats,
        ),
      ]
    : [];
  component.strokes = [
    paintFromVariable(
      config.stroke,
      config.strokeFallback,
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;

  bindChipGeometryVariables(component, metrics, variableByName, stats);

  await syncChipVariantChildren({
    component,
    variant,
    size,
    removable,
    config,
    variableByName,
    fonts,
    stats,
  });

  syncFocusRing(component, {
    enabled: state !== "Disabled",
    width: Math.max(metrics.minWidth, component.width),
    height: metrics.height,
    radius: 9999,
    variableName: "Colors/theme/500",
    fallback: "#135BEC",
    variableByName,
    stats,
  });
}

function parseSegmentedControlVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (
    SEGMENTED_CONTROL_SIZES.indexOf(values.Size) === -1 ||
    SEGMENTED_CONTROL_ACTIVE.indexOf(values.Active) === -1 ||
    SEGMENTED_CONTROL_STATES.indexOf(values.State) === -1
  ) {
    return null;
  }

  return {
    size: values.Size,
    active: values.Active,
    state: values.State,
  };
}

function segmentedControlVariantKey(props) {
  return `${props.size}/${props.active}/${props.state}`;
}

function layoutSegmentedControlVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  const columns = 4;
  const columnGap = 48;
  const rowGap = 32;
  let maxWidth = 0;
  let maxHeight = 0;

  for (const size of SEGMENTED_CONTROL_SIZES) {
    const metrics = segmentedControlMetrics(size);
    maxWidth = Math.max(maxWidth, metrics.width);
    maxHeight = Math.max(maxHeight, metrics.height);
  }

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;
    if (typeof child.width === "number" && Number.isFinite(child.width)) {
      maxWidth = Math.max(maxWidth, child.width);
    }
  }

  const xStep = maxWidth + columnGap;
  const yStep = maxHeight + rowGap;
  let maxFlatIndex = 0;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseSegmentedControlVariantName(child.name);
    if (!props) continue;

    const sizeIndex = SEGMENTED_CONTROL_SIZES.indexOf(props.size);
    const activeIndex = SEGMENTED_CONTROL_ACTIVE.indexOf(props.active);
    const stateIndex = SEGMENTED_CONTROL_STATES.indexOf(props.state);
    const flatIndex =
      (sizeIndex * SEGMENTED_CONTROL_ACTIVE.length + activeIndex) *
        SEGMENTED_CONTROL_STATES.length +
      stateIndex;
    maxFlatIndex = Math.max(maxFlatIndex, flatIndex);
    child.x = (flatIndex % columns) * xStep;
    child.y = Math.floor(flatIndex / columns) * yStep;
  }

  const rows = Math.floor(maxFlatIndex / columns) + 1;
  resizeNodeWithoutConstraints(
    componentSet,
    columns * maxWidth + (columns - 1) * columnGap,
    rows * maxHeight + (rows - 1) * rowGap,
  );
  componentSet.clipsContent = false;
}

async function updateSegmentedControlVariant(
  component,
  { size, active, state, variableByName, fonts, stats },
) {
  const metrics = segmentedControlMetrics(size);
  const activeIndex = segmentedControlActiveIndex(active);
  const config = segmentedControlConfig(state);

  component.name = `Size=${size}, Active=${active}, State=${state}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 0;
  component.paddingLeft = metrics.padding;
  component.paddingRight = metrics.padding;
  component.paddingTop = metrics.padding;
  component.paddingBottom = metrics.padding;
  component.resizeWithoutConstraints(metrics.width, metrics.height);
  setNodePropertyIfSupported(component, "minWidth", metrics.width);
  setNodePropertyIfSupported(component, "minHeight", metrics.height);
  component.cornerRadius = metrics.radius;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "SegmentedControl");

  component.fills = [
    paintFromVariable(
      config.background,
      config.backgroundFallback,
      variableByName,
      stats,
    ),
  ];
  component.strokes = [
    paintFromVariable(
      config.stroke,
      config.strokeFallback,
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;

  bindSegmentedControlGeometryVariables(
    component,
    metrics,
    variableByName,
    stats,
  );

  const sizing = await syncSegmentedControlVariantChildren({
    component,
    activeIndex,
    state,
    metrics,
    variableByName,
    fonts,
    stats,
  });

  resizeNodeWithoutConstraints(
    component,
    sizing && sizing.controlWidth ? sizing.controlWidth : metrics.width,
    metrics.height,
  );
  component.primaryAxisSizingMode = "AUTO";
}

async function updateCounterVariant(
  component,
  { tone, size, variableByName, fonts, stats },
) {
  const config = counterConfig(tone);
  const metrics = counterMetrics(size);

  component.name = `Tone=${tone}, Size=${size}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 0;
  component.paddingLeft = metrics.paddingX;
  component.paddingRight = metrics.paddingX;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(metrics.minWidth, metrics.height);
  component.cornerRadius = 9999;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Counter");

  component.fills = [
    paintFromVariable(
      config.background,
      config.backgroundFallback,
      variableByName,
      stats,
    ),
  ];
  component.strokes = [];
  component.strokeWeight = 0;

  bindCounterGeometryVariables(component, metrics, variableByName, stats);

  await syncCounterVariantChildren({
    component,
    tone,
    size,
    config,
    variableByName,
    fonts,
    stats,
  });
}

function parseBadgeVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (
    BADGE_VARIANTS.indexOf(values.Variant) === -1 ||
    BADGE_SIZES.indexOf(values.Size) === -1
  ) {
    return null;
  }

  return {
    variant: values.Variant,
    size: values.Size,
  };
}

async function updateBadgeVariant(
  component,
  { variant, size, variableByName, iconComponent, fonts, stats },
) {
  const config = badgeConfig(variant);
  const metrics = badgeMetrics(size);

  component.name = `Variant=${variant}, Size=${size}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = size === "Icon" ? "FIXED" : "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = size === "Icon" ? 0 : 4;
  component.paddingLeft = metrics.paddingX;
  component.paddingRight = metrics.paddingX;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(metrics.width, metrics.height);
  component.cornerRadius = 8;
  component.clipsContent = false;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Badge");

  component.fills = config.background
    ? [
        paintFromVariable(
          config.background,
          config.backgroundFallback,
          variableByName,
          stats,
        ),
      ]
    : [];

  if (config.stroke) {
    component.strokes = [
      paintFromVariable(
        config.stroke,
        config.strokeFallback,
        variableByName,
        stats,
      ),
    ];
    component.strokeWeight = 1;
  } else {
    component.strokes = [];
    component.strokeWeight = 0;
  }

  bindBadgeGeometryVariables(component, metrics, config, variableByName, stats);

  await syncBadgeVariantChildren({
    component,
    variant,
    size,
    config,
    variableByName,
    iconComponent,
    fonts,
    stats,
  });
}

async function createCardVariant({ value, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateCardVariant(component, {
    value,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseCardVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (CARD_CONTENT.indexOf(values.Content) === -1) {
    return null;
  }

  return {
    value: values.Content,
  };
}

async function updateCardVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  component.name = `Content=${value}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 0;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(360, 100);
  component.cornerRadius = 16;
  component.clipsContent = false;
  component.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  component.strokes = [
    paintFromVariable(
      "Colors/foreground/500",
      "#747B8B",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.effects = [
    {
      type: "DROP_SHADOW",
      color: { r: 0, g: 0, b: 0, a: 0.05 },
      offset: { x: 0, y: 1 },
      radius: 2,
      spread: 0,
      visible: true,
      blendMode: "NORMAL",
    },
  ];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Card");

  await syncCardVariantChildren({
    component,
    value,
    variableByName,
    fonts,
    stats,
  });
}

async function createListVariant({ value, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateListVariant(component, {
    value,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseListVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (LIST_DENSITIES.indexOf(values.Density) === -1) {
    return null;
  }

  return {
    value: values.Density,
  };
}

function listMetrics(density) {
  const compact = density === "Compact";
  return {
    width: 360,
    rowHeight: compact ? 40 : 48,
    paddingY: compact ? 8 : 12,
    textHeight: 20,
  };
}

async function updateListVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const metrics = listMetrics(value);
  const itemCount = 3;

  component.name = `Density=${value}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 0;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(
    metrics.width,
    metrics.rowHeight * itemCount,
  );
  component.cornerRadius = 8;
  component.clipsContent = true;
  component.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  component.strokes = [
    paintFromVariable(
      "Colors/background/200",
      "#E3E4E8",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "List");

  await syncListVariantChildren({
    component,
    density: value,
    metrics,
    variableByName,
    fonts,
    stats,
  });
}

async function createTableVariant({ value, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateTableVariant(component, {
    value,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseTableVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (TABLE_DENSITIES.indexOf(values.Density) === -1) {
    return null;
  }

  return {
    value: values.Density,
  };
}

function tableMetrics(density) {
  const compact = density === "Compact";
  return {
    width: 720,
    rowHeight: compact ? 40 : 48,
    paddingY: compact ? 8 : 12,
    columnCount: 3,
    rowCount: 4,
    textHeight: 20,
  };
}

async function updateTableVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const metrics = tableMetrics(value);

  component.name = `Density=${value}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 0;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(
    metrics.width,
    metrics.rowHeight * metrics.rowCount,
  );
  component.cornerRadius = 8;
  component.clipsContent = true;
  component.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  component.strokes = [
    paintFromVariable(
      "Colors/background/200",
      "#E3E4E8",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Table");

  await syncTableVariantChildren({
    component,
    density: value,
    metrics,
    variableByName,
    fonts,
    stats,
  });
}

async function createTabsVariant({
  count,
  active,
  state,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateTabsVariant(component, {
    count,
    active,
    state,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseTabsVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (
    TABS_COUNTS.indexOf(values.Count) === -1 ||
    TABS_ACTIVE.indexOf(values.Active) === -1 ||
    TABS_STATES.indexOf(values.State) === -1 ||
    !tabsActiveIsValidForCount(values.Active, values.Count)
  ) {
    return null;
  }

  return {
    count: values.Count,
    active: values.Active,
    state: values.State,
  };
}

async function updateTabsVariant(
  component,
  { count, active, state, variableByName, fonts, stats },
) {
  const tabCount = TABS_COUNT_TO_NUMBER[count];
  const width = tabsWidthForCount(count);

  component.name = `Count=${count}, Active=${active}, State=${state}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 0;
  component.paddingLeft = 4;
  component.paddingRight = 4;
  component.paddingTop = 4;
  component.paddingBottom = 4;
  component.resizeWithoutConstraints(width, 44);
  component.cornerRadius = 16;
  component.clipsContent = false;
  component.fills = [
    paintFromVariable(
      "Colors/background/100",
      "#E3E4E8",
      variableByName,
      stats,
    ),
  ];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Tabs");

  await syncTabsVariantChildren({
    component,
    count,
    active,
    state,
    tabCount,
    width,
    variableByName,
    fonts,
    stats,
  });
}

async function createTooltipVariant({ value, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateTooltipVariant(component, {
    value,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseTooltipVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (TOOLTIP_SIDES.indexOf(values.Side) === -1) {
    return null;
  }

  return {
    value: values.Side,
  };
}

async function updateTooltipVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const side = value;

  component.name = `Side=${side}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "AUTO";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 0;
  component.paddingLeft = 12;
  component.paddingRight = 12;
  component.paddingTop = 6;
  component.paddingBottom = 6;
  component.cornerRadius = 8;
  component.clipsContent = false;
  component.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  component.strokes = [
    paintFromVariable(
      "Colors/background/200",
      "#E3E4E8",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.effects = tooltipShadowEffects();
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Tooltip");
  component.setSharedPluginData(RUN_NAMESPACE, "side", side);

  await syncTooltipVariantChildren({
    component,
    side,
    variableByName,
    fonts,
    stats,
  });
}

async function createDialogVariant({ value, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateDialogVariant(component, {
    value,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseDialogVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (DIALOG_CONTENT.indexOf(values.Content) === -1) {
    return null;
  }

  return {
    value: values.Content,
  };
}

async function updateDialogVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  component.name = `Content=${value}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 16;
  component.paddingLeft = 24;
  component.paddingRight = 24;
  component.paddingTop = 24;
  component.paddingBottom = 24;
  component.resizeWithoutConstraints(512, 100);
  component.cornerRadius = 16;
  component.clipsContent = false;
  component.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  component.strokes = [
    paintFromVariable(
      "Colors/foreground/500",
      "#747B8B",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.effects = tooltipShadowEffects();
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Dialog");

  await syncDialogVariantChildren({
    component,
    value,
    variableByName,
    fonts,
    stats,
  });
  normalizeDialogVariantCompositeSizing(component);
}

async function createDrawerVariant({ value, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateDrawerVariant(component, {
    value,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseDrawerVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (DRAWER_SIDES.indexOf(values.Side) === -1) {
    return null;
  }

  return {
    value: values.Side,
  };
}

function drawerVariantSize(side) {
  const horizontal = side === "Left" || side === "Right";
  return {
    width: horizontal ? 408 : 520,
    height: horizontal ? 360 : 320,
  };
}

async function updateDrawerVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const side = value;
  const size = drawerVariantSize(side);

  component.name = `Side=${side}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 16;
  component.paddingLeft = 24;
  component.paddingRight = 24;
  component.paddingTop = 24;
  component.paddingBottom = 24;
  component.resizeWithoutConstraints(size.width, size.height);
  component.cornerRadius = 8;
  component.clipsContent = false;
  component.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  component.strokes = [
    paintFromVariable(
      "Colors/foreground/500",
      "#747B8B",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.effects = tooltipShadowEffects();
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Drawer");
  component.setSharedPluginData(RUN_NAMESPACE, "side", side);

  await syncDrawerVariantChildren({
    component,
    side,
    variableByName,
    fonts,
    stats,
  });
}

async function createPopoverVariant({ value, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updatePopoverVariant(component, {
    value,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parsePopoverVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (POPOVER_SIDES.indexOf(values.Side) === -1) {
    return null;
  }

  return {
    value: values.Side,
  };
}

async function updatePopoverVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const side = value;

  component.name = `Side=${side}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 16;
  component.paddingLeft = 16;
  component.paddingRight = 16;
  component.paddingTop = 16;
  component.paddingBottom = 16;
  component.resizeWithoutConstraints(288, 100);
  component.cornerRadius = 8;
  component.clipsContent = false;
  component.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  component.strokes = [
    paintFromVariable(
      "Colors/foreground/500",
      "#747B8B",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.effects = tooltipShadowEffects();
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Popover");
  component.setSharedPluginData(RUN_NAMESPACE, "side", side);

  await syncPopoverVariantChildren({
    component,
    side,
    variableByName,
    fonts,
    stats,
  });
}

async function createMenuVariant({ value, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateMenuVariant(component, {
    value,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseMenuVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (MENU_CONTENT.indexOf(values.Content) === -1) {
    return null;
  }

  return {
    value: values.Content,
  };
}

async function updateMenuVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  component.name = `Content=${value}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 0;
  component.paddingLeft = 4;
  component.paddingRight = 4;
  component.paddingTop = 4;
  component.paddingBottom = 4;
  component.resizeWithoutConstraints(192, 100);
  component.cornerRadius = 8;
  component.clipsContent = false;
  component.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  component.strokes = [
    paintFromVariable(
      "Colors/foreground/500",
      "#747B8B",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.effects = tooltipShadowEffects();
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Menu");

  await syncMenuVariantChildren({
    component,
    value,
    variableByName,
    fonts,
    stats,
  });
}

async function createCheckboxVariant({
  checked,
  state,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateCheckboxVariant(component, {
    checked,
    state,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseCheckboxVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (
    CHECKBOX_CHECKED.indexOf(values.Checked) === -1 ||
    CHECKBOX_STATES.indexOf(values.State) === -1
  ) {
    return null;
  }

  return {
    checked: values.Checked,
    state: values.State,
  };
}

async function updateCheckboxVariant(
  component,
  { checked, state, variableByName, fonts, stats },
) {
  const config = checkboxConfig(checked, state);

  component.name = `Checked=${checked}, State=${state}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 8;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(176, 44);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Checkbox");

  await syncCheckboxVariantChildren({
    component,
    checked,
    state,
    config,
    variableByName,
    fonts,
    stats,
  });
}

async function createRadioVariant({
  checked,
  state,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateRadioVariant(component, {
    checked,
    state,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseRadioVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (
    RADIO_CHECKED.indexOf(values.Checked) === -1 ||
    RADIO_STATES.indexOf(values.State) === -1
  ) {
    return null;
  }

  return {
    checked: values.Checked,
    state: values.State,
  };
}

async function updateRadioVariant(
  component,
  { checked, state, variableByName, fonts, stats },
) {
  const config = radioConfig(checked, state);

  component.name = `Checked=${checked}, State=${state}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 8;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(176, 44);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Radio");

  await syncRadioVariantChildren({
    component,
    checked,
    state,
    config,
    variableByName,
    fonts,
    stats,
  });
}

async function createSwitchVariant({
  checked,
  state,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateSwitchVariant(component, {
    checked,
    state,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseSwitchVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  if (
    SWITCH_CHECKED.indexOf(values.Checked) === -1 ||
    SWITCH_STATES.indexOf(values.State) === -1
  ) {
    return null;
  }

  return {
    checked: values.Checked,
    state: values.State,
  };
}

async function updateSwitchVariant(
  component,
  { checked, state, variableByName, fonts, stats },
) {
  const config = switchConfig(checked, state);

  component.name = `Checked=${checked}, State=${state}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 8;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(176, 44);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Switch");

  await syncSwitchVariantChildren({
    component,
    checked,
    state,
    config,
    variableByName,
    fonts,
    stats,
  });
}

async function createInputVariant({
  state,
  status,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateInputVariant(component, {
    state,
    status,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseInputVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  let state = values.State;
  let status = values.Status || "Default";

  if (state === "Error" && !values.Status) {
    state = "Default";
    status = "Error";
  }

  if (INPUT_STATES.indexOf(state) === -1) {
    return null;
  }

  if (INPUT_STATUSES.indexOf(status) === -1) {
    return null;
  }

  return {
    state,
    status,
  };
}

async function updateInputVariant(
  component,
  { state, status, variableByName, fonts, stats },
) {
  const config = inputConfig(state, status);

  component.name = `State=${state}, Status=${status}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 6;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(320, 96);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Input");

  await syncInputVariantChildren({
    component,
    state,
    status,
    config,
    variableByName,
    fonts,
    stats,
  });
}

function layoutInputVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseInputVariantName(child.name);
    if (!props) continue;

    const stateIndex = INPUT_STATES.indexOf(props.state);
    const statusIndex = INPUT_STATUSES.indexOf(props.status);
    child.x = statusIndex * 360;
    child.y = stateIndex * 126;
  }
}

async function createPasswordInputVariant({
  state,
  status,
  visibility,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updatePasswordInputVariant(component, {
    state,
    status,
    visibility,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parsePasswordInputVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  const state = values.State;
  const status = values.Status || "Default";
  const visibility = values.Visibility || "Hidden";

  if (
    INPUT_STATES.indexOf(state) === -1 ||
    INPUT_STATUSES.indexOf(status) === -1 ||
    PASSWORD_INPUT_VISIBILITY.indexOf(visibility) === -1
  ) {
    return null;
  }

  return { state, status, visibility };
}

async function updatePasswordInputVariant(
  component,
  { state, status, visibility, variableByName, fonts, stats },
) {
  const config = inputConfig(state, status);

  component.name = `State=${state}, Status=${status}, Visibility=${visibility}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 6;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(320, 96);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "PasswordInput");

  await syncPasswordInputVariantChildren({
    component,
    state,
    status,
    visibility,
    config,
    variableByName,
    fonts,
    stats,
  });
}

function layoutPasswordInputVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parsePasswordInputVariantName(child.name);
    if (!props) continue;

    const stateIndex = INPUT_STATES.indexOf(props.state);
    const statusIndex = INPUT_STATUSES.indexOf(props.status);
    const visibilityIndex = PASSWORD_INPUT_VISIBILITY.indexOf(props.visibility);
    child.x = statusIndex * 720 + visibilityIndex * 360;
    child.y = stateIndex * 126;
  }
}

async function createFormFieldVariant({
  content,
  status,
  required,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateFormFieldVariant(component, {
    content,
    status,
    required,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseFormFieldVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  const content = values.Content || "Basic";
  const status = values.Status || "Default";
  const required = values.Required || "False";

  if (
    FORM_FIELD_CONTENT.indexOf(content) === -1 ||
    FORM_FIELD_STATUSES.indexOf(status) === -1 ||
    FORM_FIELD_REQUIRED.indexOf(required) === -1
  ) {
    return null;
  }

  return { content, status, required };
}

async function updateFormFieldVariant(
  component,
  { content, status, required, variableByName, fonts, stats },
) {
  const config = inputConfig("Default", status);
  const height =
    20 +
    6 +
    (content === "Description" || content === "Full" ? 26 : 0) +
    44 +
    (content === "Helper" || content === "Full" || status !== "Default"
      ? 26
      : 0);

  component.name = `Content=${content}, Status=${status}, Required=${required}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 6;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(320, height);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "FormField");

  await syncFormFieldVariantChildren({
    component,
    content,
    status,
    required,
    config,
    variableByName,
    fonts,
    stats,
  });
}

function layoutFormFieldVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseFormFieldVariantName(child.name);
    if (!props) continue;

    const statusIndex = FORM_FIELD_STATUSES.indexOf(props.status);
    const requiredIndex = FORM_FIELD_REQUIRED.indexOf(props.required);
    const contentIndex = FORM_FIELD_CONTENT.indexOf(props.content);
    child.x = statusIndex * 760 + requiredIndex * 380;
    child.y = contentIndex * 150;
  }
}

async function createNumberInputVariant({
  state,
  status,
  steppers,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateNumberInputVariant(component, {
    state,
    status,
    steppers,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseNumberInputVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  const state = values.State;
  const status = values.Status || "Default";
  const steppers = values.Steppers || "True";

  if (
    INPUT_STATES.indexOf(state) === -1 ||
    INPUT_STATUSES.indexOf(status) === -1 ||
    NUMBER_INPUT_STEPPERS.indexOf(steppers) === -1
  ) {
    return null;
  }

  return { state, status, steppers };
}

async function updateNumberInputVariant(
  component,
  { state, status, steppers, variableByName, fonts, stats },
) {
  const config = inputConfig(state, status);

  component.name = `State=${state}, Status=${status}, Steppers=${steppers}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 6;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(320, 96);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "NumberInput");

  await syncNumberInputVariantChildren({
    component,
    state,
    status,
    steppers,
    config,
    variableByName,
    fonts,
    stats,
  });
}

function layoutNumberInputVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseNumberInputVariantName(child.name);
    if (!props) continue;

    const stateIndex = INPUT_STATES.indexOf(props.state);
    const statusIndex = INPUT_STATUSES.indexOf(props.status);
    const stepperIndex = NUMBER_INPUT_STEPPERS.indexOf(props.steppers);
    child.x = statusIndex * 720 + stepperIndex * 360;
    child.y = stateIndex * 126;
  }
}

async function createOTPInputVariant({
  length,
  state,
  status,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateOTPInputVariant(component, {
    length,
    state,
    status,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseOTPInputVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  const length = values.Length || "6";
  const state = values.State;
  const status = values.Status || "Default";

  if (
    OTP_INPUT_LENGTHS.indexOf(length) === -1 ||
    INPUT_STATES.indexOf(state) === -1 ||
    INPUT_STATUSES.indexOf(status) === -1
  ) {
    return null;
  }

  return { length, state, status };
}

async function updateOTPInputVariant(
  component,
  { length, state, status, variableByName, fonts, stats },
) {
  const config = inputConfig(state, status);

  component.name = `Length=${length}, State=${state}, Status=${status}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 6;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(320, 96);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "OTPInput");

  await syncOTPInputVariantChildren({
    component,
    length,
    state,
    status,
    config,
    variableByName,
    fonts,
    stats,
  });
}

function layoutOTPInputVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseOTPInputVariantName(child.name);
    if (!props) continue;

    const stateIndex = INPUT_STATES.indexOf(props.state);
    const statusIndex = INPUT_STATUSES.indexOf(props.status);
    const lengthIndex = OTP_INPUT_LENGTHS.indexOf(props.length);
    child.x = statusIndex * 760 + lengthIndex * 380;
    child.y = stateIndex * 126;
  }
}

async function createComboboxVariant({
  state,
  status,
  content,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateComboboxVariant(component, {
    state,
    status,
    content,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseComboboxVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  const state = values.State;
  const status = values.Status || "Default";
  const content = values.Content || "Closed";

  if (
    COMBOBOX_STATES.indexOf(state) === -1 ||
    COMBOBOX_STATUSES.indexOf(status) === -1 ||
    COMBOBOX_CONTENT.indexOf(content) === -1
  ) {
    return null;
  }

  return { state, status, content };
}

async function updateComboboxVariant(
  component,
  { state, status, content, variableByName, fonts, stats },
) {
  const config = inputConfig(state, status);
  const isOpen = content === "Open";

  component.name = `State=${state}, Status=${status}, Content=${content}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 6;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(320, isOpen ? 236 : 96);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Combobox");

  await syncComboboxVariantChildren({
    component,
    state,
    status,
    content,
    config,
    variableByName,
    fonts,
    stats,
  });
}

function layoutComboboxVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseComboboxVariantName(child.name);
    if (!props) continue;

    const stateIndex = COMBOBOX_STATES.indexOf(props.state);
    const statusIndex = COMBOBOX_STATUSES.indexOf(props.status);
    const contentIndex = COMBOBOX_CONTENT.indexOf(props.content);
    child.x = statusIndex * 760 + contentIndex * 380;
    child.y = stateIndex * 260;
  }
}

async function createMultiSelectVariant({
  state,
  status,
  content,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateMultiSelectVariant(component, {
    state,
    status,
    content,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseMultiSelectVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  const state = values.State;
  const status = values.Status || "Default";
  const content = values.Content || "Empty";

  if (
    MULTI_SELECT_STATES.indexOf(state) === -1 ||
    MULTI_SELECT_STATUSES.indexOf(status) === -1 ||
    MULTI_SELECT_CONTENT.indexOf(content) === -1
  ) {
    return null;
  }

  return { state, status, content };
}

async function updateMultiSelectVariant(
  component,
  { state, status, content, variableByName, fonts, stats },
) {
  const config = inputConfig(state, status);
  const isOpen = content === "Open";

  component.name = `State=${state}, Status=${status}, Content=${content}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 6;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(320, isOpen ? 272 : 96);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "MultiSelect");

  await syncMultiSelectVariantChildren({
    component,
    state,
    status,
    content,
    config,
    variableByName,
    fonts,
    stats,
  });
}

function layoutMultiSelectVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseMultiSelectVariantName(child.name);
    if (!props) continue;

    const stateIndex = MULTI_SELECT_STATES.indexOf(props.state);
    const statusIndex = MULTI_SELECT_STATUSES.indexOf(props.status);
    const contentIndex = MULTI_SELECT_CONTENT.indexOf(props.content);
    child.x = statusIndex * 1140 + contentIndex * 380;
    child.y = stateIndex * 300;
  }
}

async function createListboxVariant({
  selection,
  state,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateListboxVariant(component, {
    selection,
    state,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseListboxVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  const selection = values.Selection || "Single";
  const state = values.State;

  if (
    LISTBOX_SELECTION.indexOf(selection) === -1 ||
    LISTBOX_STATES.indexOf(state) === -1
  ) {
    return null;
  }

  return { selection, state };
}

async function updateListboxVariant(
  component,
  { selection, state, variableByName, fonts, stats },
) {
  component.name = `Selection=${selection}, State=${state}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 2;
  component.paddingLeft = 4;
  component.paddingRight = 4;
  component.paddingTop = 4;
  component.paddingBottom = 4;
  component.resizeWithoutConstraints(320, 176);
  component.cornerRadius = 8;
  component.clipsContent = false;
  component.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  component.strokes = [
    paintFromVariable(
      "Colors/foreground/500",
      "#747B8B",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Listbox");

  await syncListboxVariantChildren({
    component,
    selection,
    state,
    variableByName,
    fonts,
    stats,
  });
}

function layoutListboxVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseListboxVariantName(child.name);
    if (!props) continue;

    const selectionIndex = LISTBOX_SELECTION.indexOf(props.selection);
    const stateIndex = LISTBOX_STATES.indexOf(props.state);
    child.x = selectionIndex * 360;
    child.y = stateIndex * 220;
  }
}

async function createDatePickerVariant({
  state,
  status,
  content,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateDatePickerVariant(component, {
    state,
    status,
    content,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseDatePickerVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  const state = values.State;
  const status = values.Status || "Default";
  const content = values.Content || "Closed";

  if (
    DATE_PICKER_STATES.indexOf(state) === -1 ||
    DATE_PICKER_STATUSES.indexOf(status) === -1 ||
    DATE_PICKER_CONTENT.indexOf(content) === -1
  ) {
    return null;
  }

  return { state, status, content };
}

async function updateDatePickerVariant(
  component,
  { state, status, content, variableByName, fonts, stats },
) {
  const config = inputConfig(state, status);
  const isOpen = content === "Open";

  component.name = `State=${state}, Status=${status}, Content=${content}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 6;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(
    320,
    isOpen ? DATE_PICKER_OPEN_VARIANT_HEIGHT : 96,
  );
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "DatePicker");

  await syncDatePickerVariantChildren({
    component,
    state,
    status,
    content,
    config,
    variableByName,
    fonts,
    stats,
  });
}

function layoutDatePickerVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseDatePickerVariantName(child.name);
    if (!props) continue;

    const stateIndex = DATE_PICKER_STATES.indexOf(props.state);
    const statusIndex = DATE_PICKER_STATUSES.indexOf(props.status);
    const contentIndex = DATE_PICKER_CONTENT.indexOf(props.content);
    child.x = statusIndex * 760 + contentIndex * 380;
    child.y = stateIndex * DATE_PICKER_STATE_ROW_STEP;
  }
}

async function createDateRangePickerVariant({
  state,
  status,
  content,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateDateRangePickerVariant(component, {
    state,
    status,
    content,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseDateRangePickerVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  const state = values.State;
  const status = values.Status || "Default";
  const content = values.Content || "Closed";

  if (
    DATE_RANGE_PICKER_STATES.indexOf(state) === -1 ||
    DATE_RANGE_PICKER_STATUSES.indexOf(status) === -1 ||
    DATE_RANGE_PICKER_CONTENT.indexOf(content) === -1
  ) {
    return null;
  }

  return { state, status, content };
}

async function updateDateRangePickerVariant(
  component,
  { state, status, content, variableByName, fonts, stats },
) {
  const config = inputConfig(state, status);
  const isOpen = content === "Open";

  component.name = `State=${state}, Status=${status}, Content=${content}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 6;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(
    672,
    isOpen ? DATE_RANGE_PICKER_OPEN_VARIANT_HEIGHT : 116,
  );
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "DateRangePicker");

  await syncDateRangePickerVariantChildren({
    component,
    state,
    status,
    content,
    config,
    variableByName,
    fonts,
    stats,
  });
}

function layoutDateRangePickerVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseDateRangePickerVariantName(child.name);
    if (!props) continue;

    const stateIndex = DATE_RANGE_PICKER_STATES.indexOf(props.state);
    const statusIndex = DATE_RANGE_PICKER_STATUSES.indexOf(props.status);
    const contentIndex = DATE_RANGE_PICKER_CONTENT.indexOf(props.content);
    child.x = statusIndex * 1440 + contentIndex * 720;
    child.y = stateIndex * DATE_RANGE_PICKER_STATE_ROW_STEP;
  }
}

async function createTimePickerVariant({
  state,
  status,
  content,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateTimePickerVariant(component, {
    state,
    status,
    content,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseTimePickerVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  const state = values.State;
  const status = values.Status || "Default";
  const content = values.Content || "Closed";

  if (
    TIME_PICKER_STATES.indexOf(state) === -1 ||
    TIME_PICKER_STATUSES.indexOf(status) === -1 ||
    TIME_PICKER_CONTENT.indexOf(content) === -1
  ) {
    return null;
  }

  return { state, status, content };
}

async function updateTimePickerVariant(
  component,
  { state, status, content, variableByName, fonts, stats },
) {
  const config = inputConfig(state, status);
  const isOpen = content === "Open";

  component.name = `State=${state}, Status=${status}, Content=${content}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 6;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(320, isOpen ? 292 : 96);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "TimePicker");

  await syncTimePickerVariantChildren({
    component,
    state,
    status,
    content,
    config,
    variableByName,
    fonts,
    stats,
  });
}

function layoutTimePickerVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseTimePickerVariantName(child.name);
    if (!props) continue;

    const stateIndex = TIME_PICKER_STATES.indexOf(props.state);
    const statusIndex = TIME_PICKER_STATUSES.indexOf(props.status);
    const contentIndex = TIME_PICKER_CONTENT.indexOf(props.content);
    child.x = statusIndex * 760 + contentIndex * 380;
    child.y = stateIndex * 320;
  }
}

async function createFileUploadVariant({
  state,
  status,
  content,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateFileUploadVariant(component, {
    state,
    status,
    content,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseFileUploadVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  const state = values.State;
  const status = values.Status || "Default";
  const content = values.Content || "Dropzone";

  if (
    FILE_UPLOAD_STATES.indexOf(state) === -1 ||
    FILE_UPLOAD_STATUSES.indexOf(status) === -1 ||
    FILE_UPLOAD_CONTENT.indexOf(content) === -1
  ) {
    return null;
  }

  return { state, status, content };
}

async function updateFileUploadVariant(
  component,
  { state, status, content, variableByName, fonts, stats },
) {
  const config = inputConfig(state, status);
  const hasFiles = content === "Files";

  component.name = `State=${state}, Status=${status}, Content=${content}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 6;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(384, hasFiles ? 302 : 218);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "FileUpload");

  await syncFileUploadVariantChildren({
    component,
    state,
    status,
    content,
    config,
    variableByName,
    fonts,
    stats,
  });
}

function layoutFileUploadVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseFileUploadVariantName(child.name);
    if (!props) continue;

    const stateIndex = FILE_UPLOAD_STATES.indexOf(props.state);
    const statusIndex = FILE_UPLOAD_STATUSES.indexOf(props.status);
    const contentIndex = FILE_UPLOAD_CONTENT.indexOf(props.content);
    child.x = statusIndex * 880 + contentIndex * 440;
    child.y = stateIndex * 390;
  }
}

async function createColorPickerVariant({
  state,
  status,
  content,
  format,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateColorPickerVariant(component, {
    state,
    status,
    content,
    format,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseColorPickerVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  const state = values.State;
  const status = values.Status || "Default";
  const content = values.Content || "Closed";
  const format = values.Format || "HSL";

  if (
    COLOR_PICKER_STATES.indexOf(state) === -1 ||
    COLOR_PICKER_STATUSES.indexOf(status) === -1 ||
    COLOR_PICKER_CONTENT.indexOf(content) === -1 ||
    COLOR_PICKER_FORMATS.indexOf(format) === -1
  ) {
    return null;
  }

  return { state, status, content, format };
}

async function updateColorPickerVariant(
  component,
  { state, status, content, format, variableByName, fonts, stats },
) {
  const config = inputConfig(state, status);
  const isOpen = content === "Open";
  const resolvedFormat =
    COLOR_PICKER_FORMATS.indexOf(format) === -1 ? "HSL" : format;

  component.name = `State=${state}, Status=${status}, Content=${content}, Format=${resolvedFormat}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 6;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(320, isOpen ? 520 : 96);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "ColorPicker");

  await syncColorPickerVariantChildren({
    component,
    state,
    status,
    content,
    format: resolvedFormat,
    config,
    variableByName,
    fonts,
    stats,
  });
}

function layoutColorPickerVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  const statusStep = 1140;
  const contentStep = 380;
  const formatStep = 720;
  const stateStep = 2300;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseColorPickerVariantName(child.name);
    if (!props) continue;

    const stateIndex = COLOR_PICKER_STATES.indexOf(props.state);
    const statusIndex = COLOR_PICKER_STATUSES.indexOf(props.status);
    const contentIndex = COLOR_PICKER_CONTENT.indexOf(props.content);
    const formatIndex = COLOR_PICKER_FORMATS.indexOf(props.format);
    child.x = statusIndex * statusStep + contentIndex * contentStep;
    child.y = stateIndex * stateStep + formatIndex * formatStep;
  }

  resizeComponentSetToContainChildren(componentSet);
}

async function createTextareaVariant({
  state,
  status,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateTextareaVariant(component, {
    state,
    status,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseTextareaVariantName(name) {
  return parseStateStatusVariantName(name, TEXTAREA_STATES, TEXTAREA_STATUSES);
}

async function updateTextareaVariant(
  component,
  { state, status, variableByName, fonts, stats },
) {
  const config = inputConfig(state, status);

  component.name = `State=${state}, Status=${status}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 6;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(320, 132);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Textarea");

  await syncTextareaVariantChildren({
    component,
    state,
    status,
    config,
    variableByName,
    fonts,
    stats,
  });
}

async function createSearchVariant({
  state,
  status,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateSearchVariant(component, {
    state,
    status,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseSearchVariantName(name) {
  return parseStateStatusVariantName(name, SEARCH_STATES, SEARCH_STATUSES);
}

async function updateSearchVariant(
  component,
  { state, status, variableByName, fonts, stats },
) {
  const config = inputConfig(state, status);

  component.name = `State=${state}, Status=${status}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 6;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(320, 96);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Search");

  await syncSearchVariantChildren({
    component,
    state,
    status,
    config,
    variableByName,
    fonts,
    stats,
  });
}

async function createSelectVariant({
  state,
  status,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateSelectVariant(component, {
    state,
    status,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseSelectVariantName(name) {
  return parseStateStatusVariantName(name, SELECT_STATES, SELECT_STATUSES);
}

async function updateSelectVariant(
  component,
  { state, status, variableByName, fonts, stats },
) {
  const config = inputConfig(state, status);

  component.name = `State=${state}, Status=${status}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 0;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(320, 44);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Select");

  await syncSelectVariantChildren({
    component,
    state,
    status,
    config,
    variableByName,
    fonts,
    stats,
  });
}

async function createSliderVariant({
  state,
  status,
  type,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateSliderVariant(component, {
    state,
    status,
    type,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseSliderVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  const state = values.State;
  const status = values.Status || "Default";
  const type = values.Type || "Single";

  if (
    SLIDER_STATES.indexOf(state) === -1 ||
    SLIDER_STATUSES.indexOf(status) === -1 ||
    SLIDER_TYPES.indexOf(type) === -1
  ) {
    return null;
  }

  return { state, status, type };
}

async function updateSliderVariant(
  component,
  { state, status, type, variableByName, fonts, stats },
) {
  const config = sliderConfig(state, status);
  const sliderType = type || "Single";

  component.name = `State=${state}, Status=${status}, Type=${sliderType}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 6;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(320, 70);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Slider");

  await syncSliderVariantChildren({
    component,
    state,
    status,
    type: sliderType,
    config,
    variableByName,
    fonts,
    stats,
  });
}

async function createRatingVariant({
  value,
  state,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateRatingVariant(component, {
    value,
    state,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseRatingVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  const value = values.Value;
  const state = values.State || "Default";

  if (
    RATING_VALUES.indexOf(value) === -1 ||
    RATING_STATES.indexOf(state) === -1
  ) {
    return null;
  }

  return { value, state };
}

async function updateRatingVariant(
  component,
  { value, state, variableByName, fonts, stats },
) {
  component.name = `Value=${value}, State=${state}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 0;
  component.paddingLeft = 0;
  component.paddingRight = 0;
  component.paddingTop = 0;
  component.paddingBottom = 0;
  component.resizeWithoutConstraints(220, 44);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Rating");

  await syncRatingVariantChildren({
    component,
    value,
    state,
    variableByName,
    fonts,
    stats,
  });
}

function layoutRatingVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseRatingVariantName(child.name);
    if (!props) continue;

    const valueIndex = RATING_VALUES.indexOf(props.value);
    const stateIndex = RATING_STATES.indexOf(props.state);
    child.x = valueIndex * 260;
    child.y = stateIndex * 80;
  }

  resizeComponentSetToContainChildren(componentSet);
}

async function createStepperVariant({
  count,
  current,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateStepperVariant(component, {
    count,
    current,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseStepperVariantName(name) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  const count = values.Count;
  const current = values.Current || "1";

  if (
    STEPPER_COUNTS.indexOf(count) === -1 ||
    STEPPER_CURRENT.indexOf(current) === -1 ||
    stepperCurrentIndex(current) >= stepperCountNumber(count)
  ) {
    return null;
  }

  return { count, current };
}

async function updateStepperVariant(
  component,
  { count, current, variableByName, fonts, stats },
) {
  const countNumber = stepperCountNumber(count);
  const width = stepperWidth(countNumber);

  component.name = `Count=${count}, Current=${current}`;
  component.layoutMode = "NONE";
  component.resizeWithoutConstraints(width, 72);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Stepper");

  await syncStepperVariantChildren({
    component,
    count,
    current,
    variableByName,
    fonts,
    stats,
  });
}

function layoutStepperVariants(componentSet) {
  if (!componentSet || !componentSet.children) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseStepperVariantName(child.name);
    if (!props) continue;

    const countIndex = STEPPER_COUNTS.indexOf(props.count);
    const currentIndex = STEPPER_CURRENT.indexOf(props.current);
    child.x = countIndex * 760;
    child.y = currentIndex * 116;
  }

  resizeComponentSetToContainChildren(componentSet);
}

async function syncRatingVariantChildren({
  component,
  value,
  state,
  variableByName,
  fonts,
  stats,
}) {
  for (const child of Array.from(component.children || [])) {
    child.remove();
  }

  const valueNumber = Number(value);
  const readonly = state === "Readonly";

  for (let index = 1; index <= 5; index += 1) {
    const star = await createRatingStar({
      index,
      filled: index <= valueNumber,
      readonly,
      variableByName,
      fonts,
      stats,
    });
    component.appendChild(star);
  }
}

async function createRatingStar({
  index,
  filled,
  readonly,
  variableByName,
  fonts,
  stats,
}) {
  const cell = figma.createFrame();
  cell.name = `Rating Star ${index}`;
  cell.layoutMode = "HORIZONTAL";
  cell.primaryAxisSizingMode = "FIXED";
  cell.counterAxisSizingMode = "FIXED";
  cell.primaryAxisAlignItems = "CENTER";
  cell.counterAxisAlignItems = "CENTER";
  cell.resizeWithoutConstraints(44, 44);
  cell.cornerRadius = 8;
  cell.clipsContent = false;
  cell.fills = [];
  cell.strokes = [];
  cell.opacity = readonly ? 0.72 : 1;
  cell.setSharedPluginData(RUN_NAMESPACE, "kind", "rating-star-hit-area");
  setFixedChildSizing(cell);

  if (typeof figma.createStar === "function") {
    const glyph = figma.createStar();
    glyph.name = filled ? "Star / filled" : "Star / empty";
    glyph.pointCount = 5;
    glyph.innerRadius = 0.45;
    glyph.resizeWithoutConstraints(22, 22);
    glyph.fills = filled
      ? [paintFromVariable("Data/Yellow", "#A66A00", variableByName, stats)]
      : [];
    glyph.strokes = [
      paintFromVariable(
        filled ? "Data/Yellow" : "Colors/foreground/500",
        filled ? "#A66A00" : "#747B8B",
        variableByName,
        stats,
      ),
    ];
    glyph.strokeWeight = 1.5;
    glyph.setSharedPluginData(RUN_NAMESPACE, "kind", "rating-star-glyph");
    cell.appendChild(glyph);
    return cell;
  }

  const fallback = figma.createText();
  fallback.name = filled ? "Star / filled" : "Star / empty";
  fallback.fontName = fonts.medium;
  fallback.fontSize = 20;
  fallback.lineHeight = { unit: "PIXELS", value: 20 };
  fallback.characters = "*";
  await applyTextStyleToNodeAsync(fallback, "buttonLabel", stats);
  fallback.textAlignHorizontal = "CENTER";
  fallback.textAlignVertical = "CENTER";
  fallback.fills = [
    paintFromVariable(
      filled ? "Data/Yellow" : "Colors/foreground/500",
      filled ? "#A66A00" : "#747B8B",
      variableByName,
      stats,
    ),
  ];
  setTextAutoResize(fallback, "WIDTH_AND_HEIGHT");
  cell.appendChild(fallback);
  return cell;
}

function stepperCountNumber(count) {
  const parsed = Number(count);
  return Number.isFinite(parsed) ? parsed : 2;
}

function stepperCurrentIndex(current) {
  const parsed = Number(current);
  return Number.isFinite(parsed) ? Math.max(0, parsed - 1) : 0;
}

function stepperWidth(countNumber) {
  const stepWidth = 88;
  const connectorWidth = 92;
  return (
    countNumber * stepWidth + Math.max(0, countNumber - 1) * connectorWidth
  );
}

async function syncStepperVariantChildren({
  component,
  count,
  current,
  variableByName,
  fonts,
  stats,
}) {
  for (const child of Array.from(component.children || [])) {
    child.remove();
  }

  const countNumber = stepperCountNumber(count);
  const currentIndex = stepperCurrentIndex(current);
  const stepWidth = 88;
  const connectorWidth = 92;
  const stepLabels = ["Details", "Shipping", "Payment", "Confirm"];

  for (let index = 0; index < countNumber; index += 1) {
    const step = await createStepperStepItem({
      index,
      label: stepLabels[index] || `Step ${index + 1}`,
      currentIndex,
      variableByName,
      fonts,
      stats,
    });
    step.x = index * (stepWidth + connectorWidth);
    step.y = 0;
    component.appendChild(step);

    if (index < countNumber - 1) {
      const connector = createStepperConnector({
        index,
        active: index < currentIndex,
        variableByName,
        stats,
      });
      connector.x = step.x + stepWidth;
      connector.y = 15;
      component.appendChild(connector);
    }
  }
}

async function createStepperStepItem({
  index,
  label,
  currentIndex,
  variableByName,
  fonts,
  stats,
}) {
  const completed = index < currentIndex;
  const current = index === currentIndex;

  const item = figma.createFrame();
  item.name = `Step ${index + 1}`;
  item.layoutMode = "VERTICAL";
  item.primaryAxisSizingMode = "FIXED";
  item.counterAxisSizingMode = "FIXED";
  item.primaryAxisAlignItems = "MIN";
  item.counterAxisAlignItems = "CENTER";
  item.itemSpacing = 8;
  item.paddingLeft = 0;
  item.paddingRight = 0;
  item.paddingTop = 0;
  item.paddingBottom = 0;
  item.resizeWithoutConstraints(88, 72);
  item.fills = [];
  item.strokes = [];
  item.clipsContent = false;
  item.setSharedPluginData(RUN_NAMESPACE, "kind", "stepper-step");

  const indicator = figma.createFrame();
  indicator.name = `Step ${index + 1} Indicator`;
  indicator.layoutMode = "HORIZONTAL";
  indicator.primaryAxisSizingMode = "FIXED";
  indicator.counterAxisSizingMode = "FIXED";
  indicator.primaryAxisAlignItems = "CENTER";
  indicator.counterAxisAlignItems = "CENTER";
  indicator.resizeWithoutConstraints(32, 32);
  indicator.cornerRadius = 9999;
  indicator.clipsContent = false;
  indicator.fills = [
    paintFromVariable(
      completed ? "Colors/theme/500" : "Surface/0",
      completed ? "#135BEC" : "#FFFFFF",
      variableByName,
      stats,
    ),
  ];
  indicator.strokes = [
    paintFromVariable(
      current ? "Colors/theme/500" : "Colors/foreground/500",
      current ? "#135BEC" : "#747B8B",
      variableByName,
      stats,
    ),
  ];
  indicator.strokeWeight = current || completed ? 2 : 1;
  indicator.setSharedPluginData(RUN_NAMESPACE, "kind", "stepper-indicator");
  setFixedChildSizing(indicator);

  if (completed) {
    const check = await createFixedIconInstance(
      "check",
      "Surface/0",
      "#FFFFFF",
      variableByName,
      stats,
      16,
      null,
    );
    check.name = "Completed Icon";
    indicator.appendChild(check);
  } else {
    const number = figma.createText();
    number.name = "Step Number";
    number.fontName = fonts.medium;
    number.fontSize = 14;
    number.lineHeight = { unit: "PIXELS", value: 20 };
    number.characters = String(index + 1);
    await applyTextStyleToNodeAsync(number, "fieldText", stats);
    number.textAlignHorizontal = "CENTER";
    number.textAlignVertical = "CENTER";
    number.fills = [
      paintFromVariable(
        "Colors/foreground/0",
        "#0B0D12",
        variableByName,
        stats,
      ),
    ];
    setTextAutoResize(number, "WIDTH_AND_HEIGHT");
    indicator.appendChild(number);
  }

  const text = figma.createText();
  text.name = `Step ${index + 1} Text`;
  text.fontName = current ? fonts.medium : fonts.regular;
  text.fontSize = 12;
  text.lineHeight = { unit: "PIXELS", value: 16 };
  text.characters = label;
  await applyTextStyleToNodeAsync(text, "fieldMeta", stats);
  text.textAlignHorizontal = "CENTER";
  text.textAlignVertical = "CENTER";
  text.resizeWithoutConstraints(88, 32);
  setTextAutoResize(text, "HEIGHT");
  text.fills = [
    paintFromVariable("Colors/foreground/0", "#0B0D12", variableByName, stats),
  ];
  text.setSharedPluginData(RUN_NAMESPACE, "kind", "stepper-label");
  setFixedChildSizing(text);

  item.appendChild(indicator);
  item.appendChild(text);
  return item;
}

function createStepperConnector({ index, active, variableByName, stats }) {
  const connector = figma.createRectangle();
  connector.name = `Step ${index + 1} Connector`;
  connector.resizeWithoutConstraints(92, 2);
  connector.cornerRadius = 9999;
  connector.fills = [
    paintFromVariable(
      active ? "Colors/theme/500" : "Colors/foreground/500",
      active ? "#135BEC" : "#747B8B",
      variableByName,
      stats,
    ),
  ];
  connector.strokes = [];
  connector.opacity = active ? 1 : 0.32;
  connector.setSharedPluginData(RUN_NAMESPACE, "kind", "stepper-connector");
  return connector;
}

async function createProgressVariant({ value, variableByName, stats }) {
  const component = figma.createComponent();
  await updateProgressVariant(component, {
    value,
    variableByName,
    stats,
  });
  return component;
}

function parseProgressVariantName(name) {
  return parseSingleAxisVariantName(name, "Value", PROGRESS_VALUES);
}

async function updateProgressVariant(
  component,
  { value, variableByName, stats },
) {
  const config = progressConfig(value);

  component.name = `Value=${value}`;
  component.layoutMode = "NONE";
  component.resizeWithoutConstraints(320, 8);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Progress");

  await syncProgressVariantChildren({
    component,
    value,
    config,
    variableByName,
    stats,
  });
}

async function createSpinnerVariant({ value, variableByName, stats }) {
  const component = figma.createComponent();
  await updateSpinnerVariant(component, {
    value,
    variableByName,
    stats,
  });
  return component;
}

function parseSpinnerVariantName(name) {
  return parseSingleAxisVariantName(name, "Size", SPINNER_SIZES);
}

async function updateSpinnerVariant(
  component,
  { value, variableByName, stats },
) {
  const metrics = spinnerMetrics(value);

  component.name = `Size=${value}`;
  component.layoutMode = "NONE";
  component.resizeWithoutConstraints(metrics.size, metrics.size);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Spinner");
  bindSizeVariables(
    component,
    metrics.sizeToken,
    metrics.sizeToken,
    variableByName,
    stats,
  );

  await syncSpinnerVariantChildren({
    component,
    metrics,
    variableByName,
    stats,
  });
}

async function createAvatarVariant({ value, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateAvatarVariant(component, {
    value,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseAvatarVariantName(name) {
  return parseSingleAxisVariantName(name, "Content", AVATAR_CONTENT);
}

async function updateAvatarVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const config = avatarConfig(value);

  component.name = `Content=${value}`;
  component.layoutMode = "NONE";
  component.resizeWithoutConstraints(40, 40);
  component.cornerRadius = 9999;
  component.clipsContent = true;
  component.fills = [
    paintFromVariable(
      config.background,
      config.backgroundFallback,
      variableByName,
      stats,
    ),
  ];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Avatar");
  bindAvatarGeometryVariables(component, variableByName, stats);

  await syncAvatarVariantChildren({
    component,
    value,
    config,
    variableByName,
    fonts,
    stats,
  });
}

async function createAlertVariant({ value, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateAlertVariant(component, {
    value,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseAlertVariantName(name) {
  return parseSingleAxisVariantName(name, "Variant", ALERT_VARIANTS);
}

async function updateAlertVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  const config = alertConfig(value);

  component.name = `Variant=${value}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "AUTO";
  component.primaryAxisAlignItems = "MIN";
  component.counterAxisAlignItems = "MIN";
  component.itemSpacing = 8;
  component.paddingLeft = 16;
  component.paddingRight = 16;
  component.paddingTop = 16;
  component.paddingBottom = 16;
  component.resizeWithoutConstraints(360, 92);
  component.cornerRadius = 12;
  component.clipsContent = false;
  component.fills = [
    paintFromVariable(
      config.background,
      config.backgroundFallback,
      variableByName,
      stats,
    ),
  ];
  component.strokes = [
    paintFromVariable(
      config.stroke,
      config.strokeFallback,
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Alert");

  await syncAlertVariantChildren({
    component,
    value,
    config,
    variableByName,
    fonts,
    stats,
  });
}

async function createEmptyStateVariant({
  value,
  variableByName,
  fonts,
  stats,
}) {
  const component = figma.createComponent();
  await updateEmptyStateVariant(component, {
    value,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseEmptyStateVariantName(name) {
  return parseSingleAxisVariantName(name, "Content", EMPTY_STATE_CONTENT);
}

async function updateEmptyStateVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  component.name = `Content=${value}`;
  component.layoutMode = "VERTICAL";
  component.primaryAxisSizingMode = "AUTO";
  component.counterAxisSizingMode = "FIXED";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 16;
  component.paddingLeft = 32;
  component.paddingRight = 32;
  component.paddingTop = 32;
  component.paddingBottom = 32;
  component.resizeWithoutConstraints(360, 196);
  component.cornerRadius = 0;
  component.clipsContent = false;
  component.fills = [];
  component.strokes = [];
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "EmptyState");

  await syncEmptyStateVariantChildren({
    component,
    value,
    variableByName,
    fonts,
    stats,
  });
}

async function createToastVariant({ value, variableByName, fonts, stats }) {
  const component = figma.createComponent();
  await updateToastVariant(component, {
    value,
    variableByName,
    fonts,
    stats,
  });
  return component;
}

function parseToastVariantName(name) {
  return parseSingleAxisVariantName(name, "Content", TOAST_CONTENT);
}

async function updateToastVariant(
  component,
  { value, variableByName, fonts, stats },
) {
  component.name = `Content=${value}`;
  component.layoutMode = "HORIZONTAL";
  component.primaryAxisSizingMode = "FIXED";
  component.counterAxisSizingMode = "AUTO";
  component.primaryAxisAlignItems = "CENTER";
  component.counterAxisAlignItems = "CENTER";
  component.itemSpacing = 16;
  component.paddingLeft = 24;
  component.paddingRight = 32;
  component.paddingTop = 24;
  component.paddingBottom = 24;
  component.resizeWithoutConstraints(420, 100);
  component.cornerRadius = 8;
  component.clipsContent = false;
  component.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  component.strokes = [
    paintFromVariable(
      "Colors/foreground/500",
      "#747B8B",
      variableByName,
      stats,
    ),
  ];
  component.strokeWeight = 1;
  component.effects = tooltipShadowEffects();
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "component-variant");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Toast");

  await syncToastVariantChildren({
    component,
    value,
    variableByName,
    fonts,
    stats,
  });
}

function parseSingleAxisVariantName(name, axisName, values) {
  const parts = name.split(",");
  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    if (key !== axisName || values.indexOf(value) === -1) continue;
    return { value };
  }

  return null;
}

function parseStateStatusVariantName(name, states, statuses) {
  const values = {};
  const parts = name.split(",");

  for (const part of parts) {
    const index = part.indexOf("=");
    if (index === -1) continue;
    const key = part.slice(0, index).trim();
    const value = part.slice(index + 1).trim();
    values[key] = value;
  }

  const state = values.State;
  const status = values.Status || "Default";

  if (states.indexOf(state) === -1 || statuses.indexOf(status) === -1) {
    return null;
  }

  return { state, status };
}

async function syncIconButtonChildren({
  component,
  variant,
  state,
  config,
  variableByName,
  iconSize,
  iconSizeToken,
  spinnerSize,
  spinnerSizeToken,
  stats,
}) {
  removeGeneratedButtonChild(
    component,
    "Loading Indicator",
    state !== "Loading",
  );
  removeGeneratedButtonChild(component, "Icon", state === "Loading");
  removeGeneratedButtonChild(component, "Label Text", true);

  if (state === "Loading") {
    let spinner = directChildNamed(component, "Loading Indicator");
    if (spinner && spinner.type !== "ELLIPSE") {
      spinner.remove();
      spinner = null;
    }

    if (!spinner) {
      spinner = createSpinnerGlyph(
        config.foreground,
        config.foregroundFallback,
        variableByName,
        stats,
        spinnerSize,
        spinnerSizeToken,
      );
    } else {
      spinner.resize(spinnerSize, spinnerSize);
      bindSizeVariables(
        spinner,
        spinnerSizeToken,
        spinnerSizeToken,
        variableByName,
        stats,
      );
      spinner.fills = [];
      spinner.strokes = [
        paintFromVariable(
          config.foreground,
          config.foregroundFallback,
          variableByName,
          stats,
        ),
      ];
      spinner.strokeWeight = 2;
      spinner.dashPattern = [8, 4];
    }
    component.appendChild(spinner);
    return;
  }
}

async function syncButtonVariantChildren({
  component,
  variant,
  size,
  state,
  config,
  variableByName,
  fonts,
  textStyle,
  stats,
}) {
  const metrics = buttonMetrics(size);
  removeGeneratedButtonChild(
    component,
    "Loading Indicator",
    state !== "Loading",
  );
  removeGeneratedButtonChild(
    component,
    "Icon",
    size !== "Icon" || state === "Loading",
  );
  removeGeneratedButtonChild(component, "Label Text", size === "Icon");

  if (state === "Loading") {
    let spinner = directChildNamed(component, "Loading Indicator");
    if (spinner && spinner.type !== "ELLIPSE") {
      spinner.remove();
      spinner = null;
    }

    if (!spinner) {
      spinner = createSpinnerGlyph(
        config.foreground,
        config.foregroundFallback,
        variableByName,
        stats,
        metrics.spinnerSize,
        metrics.spinnerSizeToken,
      );
    } else {
      spinner.resize(metrics.spinnerSize, metrics.spinnerSize);
      bindSizeVariables(
        spinner,
        metrics.spinnerSizeToken,
        metrics.spinnerSizeToken,
        variableByName,
        stats,
      );
      spinner.fills = [];
      spinner.strokes = [
        paintFromVariable(
          config.foreground,
          config.foregroundFallback,
          variableByName,
          stats,
        ),
      ];
      spinner.strokeWeight = 2;
      spinner.dashPattern = [8, 4];
    }
    component.appendChild(spinner);
  }

  if (size !== "Icon") {
    let label = directChildNamed(component, "Label Text");
    if (label && label.type !== "TEXT") {
      label.remove();
      label = null;
    }

    if (!label || label.type !== "TEXT") {
      label = figma.createText();
      label.name = "Label Text";
    }

    await applyButtonLabelTypography(
      label,
      fonts,
      textStyle,
      variableByName,
      stats,
    );
    label.characters = state === "Loading" ? "Loading" : variant;
    label.fills = [
      paintFromVariable(
        config.foreground,
        config.foregroundFallback,
        variableByName,
        stats,
      ),
    ];
    label.textDecoration = variant === "Link" ? "UNDERLINE" : "NONE";
    component.appendChild(label);
  }
}

async function syncCounterVariantChildren({
  component,
  tone,
  size,
  config,
  variableByName,
  fonts,
  stats,
}) {
  let text = directChildNamed(component, "Counter Text");
  if (text && text.type !== "TEXT") {
    text.remove();
    text = null;
  }

  if (!text || text.type !== "TEXT") {
    text = figma.createText();
    text.name = "Counter Text";
  }

  await applyCounterTypography(text, size, fonts, variableByName, stats);
  text.characters = tone === "Brand" ? "12" : "2";
  text.fills = [
    paintFromVariable(
      config.foreground,
      config.foregroundFallback,
      variableByName,
      stats,
    ),
  ];
  text.textDecoration = "NONE";
  text.textAlignHorizontal = "CENTER";
  text.textAlignVertical = "CENTER";
  component.appendChild(text);
  setHugChildSizing(text);
}

async function syncBadgeVariantChildren({
  component,
  variant,
  size,
  config,
  variableByName,
  iconComponent,
  fonts,
  stats,
}) {
  removeGeneratedButtonChild(component, "Loading Indicator", true);
  removeGeneratedButtonChild(component, "Icon", size !== "Icon");
  removeGeneratedButtonChild(component, "Label Text", size === "Icon");
  removeGeneratedButtonChild(component, "Counter", size === "Icon");
  removeGeneratedButtonChild(component, "Counter Text", true);

  if (size === "Icon") {
    let icon = directChildNamed(component, "Icon");
    if (icon && icon.type === "INSTANCE" && !isGeneratedIconSlotWrapper(icon)) {
      syncIconSlotInstance(
        icon,
        config,
        variableByName,
        stats,
        16,
        "Badge/icon/size",
      );
    } else if (iconComponent) {
      if (icon) icon.remove();
      icon = createIconSlotInstance(
        iconComponent,
        config.foreground,
        config.foregroundFallback,
        variableByName,
        stats,
        16,
        "Badge/icon/size",
      );
    }

    if (icon) {
      component.appendChild(icon);
      setHugChildSizing(icon);
    }
    return;
  }

  let label = directChildNamed(component, "Label Text");
  if (label && label.type !== "TEXT") {
    label.remove();
    label = null;
  }

  if (!label || label.type !== "TEXT") {
    label = figma.createText();
    label.name = "Label Text";
  }

  await applyBadgeLabelTypography(label, fonts, variableByName, stats);
  label.characters = badgeLabelText(variant, size);
  label.fills = [
    paintFromVariable(
      config.foreground,
      config.foregroundFallback,
      variableByName,
      stats,
    ),
  ];
  label.textDecoration = variant === "Link" ? "UNDERLINE" : "NONE";
  label.textAlignHorizontal = "CENTER";
  label.textAlignVertical = "CENTER";
  component.appendChild(label);
  setHugChildSizing(label);

  let counter = directChildNamed(component, "Counter");
  if (counter && !isNestedComponentInstance(counter, "Counter")) {
    counter.remove();
    counter = null;
  }

  let counterSet = null;
  const counterTone = counterToneForBadgeVariant(variant);
  if (!counter) {
    const created = await createNestedComponentInstance({
      componentSetName: "Counter",
      variantProperties: {
        Tone: counterTone,
        Size: "Default",
      },
      name: "Counter",
      stats,
    });
    if (!created) {
      counter = createMissingNestedComponentNode(
        "Counter",
        "Build Counter before updating Badge.",
        stats,
      );
    } else {
      counter = created.instance;
      counterSet = created.componentSet;
    }
  } else {
    counterSet = await findLocalComponentSetByName("Counter");
  }

  if (counter.type === "INSTANCE" && counterSet) {
    try {
      counter.setProperties({
        Tone: counterTone,
        Size: "Default",
      });
    } catch (error) {
      stats.warnings.push(
        `Badge Counter: could not set Counter variant properties (${messageFor(error)}).`,
      );
    }
    setInstanceTextProperty(counter, counterSet, "Counter Text", "2", stats);
    markNestedComponentInstance(counter, "Counter", "badge-counter");
  }

  if (counter.setSharedPluginData) {
    counter.setSharedPluginData(RUN_NAMESPACE, "role", "badge-counter");
  }
  counter.visible = false;
  component.appendChild(counter);
  setHugChildSizing(counter);

  if (counter.type === "INSTANCE" && counterSet) {
    exposeNestedCounterInstance(counter, stats);
  }
}

async function syncChipVariantChildren({
  component,
  variant,
  size,
  removable,
  config,
  variableByName,
  fonts,
  stats,
}) {
  const existingFocusRing = directChildNamed(component, "Focus Ring");
  if (existingFocusRing) existingFocusRing.remove();

  removeGeneratedButtonChild(component, "Label Text", false);
  removeGeneratedButtonChild(component, "Remove Button", removable !== "True");

  let label = directChildNamed(component, "Label Text");
  if (label && label.type !== "TEXT") {
    label.remove();
    label = null;
  }

  if (!label || label.type !== "TEXT") {
    label = figma.createText();
    label.name = "Label Text";
  }

  await applyChipLabelTypography(label, size, fonts, variableByName, stats);
  label.characters = chipLabelText(variant);
  label.fills = [
    paintFromVariable(
      config.foreground,
      config.foregroundFallback,
      variableByName,
      stats,
    ),
  ];
  label.textDecoration = "NONE";
  label.textAlignHorizontal = "CENTER";
  label.textAlignVertical = "CENTER";
  component.appendChild(label);
  setHugChildSizing(label);

  if (removable === "True") {
    const removeButton = await syncInlineCloseButton({
      parent: component,
      name: "Remove Button",
      kind: "chip-remove",
      size: 20,
      iconSize: 12,
      iconSizeToken: "Chip/remove/icon/size",
      colorToken: config.foreground,
      colorFallback: config.foregroundFallback,
      variableByName,
      stats,
    });
    bindSizeVariables(
      removeButton,
      "Chip/remove/size",
      "Chip/remove/size",
      variableByName,
      stats,
    );
    setHugChildSizing(removeButton);
  }
}

async function syncSegmentedControlVariantChildren({
  component,
  activeIndex,
  state,
  metrics,
  variableByName,
  fonts,
  stats,
}) {
  const labelNames = ["Item 1 Text", "Item 2 Text", "Item 3 Text"];
  const preservedText = collectDescendantTextCharactersByName(
    component,
    labelNames,
  );
  const existingChildren = Array.from(component.children || []);
  for (const child of existingChildren) {
    child.remove();
  }

  const disabled = state === "Disabled";
  const error = state === "Error";
  const labels = [
    { name: "Item 1 Text", text: preservedText["Item 1 Text"] || "Overview" },
    { name: "Item 2 Text", text: preservedText["Item 2 Text"] || "Details" },
    { name: "Item 3 Text", text: preservedText["Item 3 Text"] || "Activity" },
  ];
  const labelNodes = [];
  const itemWidths = [];

  for (const labelData of labels) {
    const label = figma.createText();
    label.name = labelData.name;
    await applySegmentedControlLabelTypography(
      label,
      metrics,
      fonts,
      variableByName,
      stats,
    );
    label.characters = labelData.text;
    label.textAlignHorizontal = "CENTER";
    label.textAlignVertical = "CENTER";
    setTextAutoResize(label, "WIDTH_AND_HEIGHT");
    itemWidths.push(
      Math.max(
        metrics.itemWidth,
        Math.ceil((label.width || 0) + metrics.itemPaddingX * 2),
      ),
    );
    labelNodes.push(label);
  }

  const controlWidth =
    itemWidths.reduce((total, width) => total + width, 0) + metrics.padding * 2;

  for (let index = 0; index < labels.length; index += 1) {
    const selected = index === activeIndex;
    const itemWidth = itemWidths[index] || metrics.itemWidth;
    const item = figma.createFrame();
    item.name = `Segment ${index + 1}`;
    item.layoutMode = "HORIZONTAL";
    item.primaryAxisSizingMode = "AUTO";
    item.counterAxisSizingMode = "FIXED";
    item.primaryAxisAlignItems = "CENTER";
    item.counterAxisAlignItems = "CENTER";
    item.itemSpacing = 0;
    item.paddingLeft = metrics.itemPaddingX;
    item.paddingRight = metrics.itemPaddingX;
    item.paddingTop = 0;
    item.paddingBottom = 0;
    item.resizeWithoutConstraints(itemWidth, metrics.itemHeight);
    setNodePropertyIfSupported(item, "minWidth", metrics.itemWidth);
    setNodePropertyIfSupported(item, "minHeight", metrics.itemHeight);
    item.cornerRadius = metrics.itemRadius;
    item.clipsContent = false;
    item.setSharedPluginData(
      RUN_NAMESPACE,
      "kind",
      "segmented-control-segment",
    );
    item.strokes = [];
    item.fills = selected
      ? [
          paintFromVariable(
            disabled ? "Colors/background/200" : "Surface/0",
            disabled ? "#C7CAD1" : "#FFFFFF",
            variableByName,
            stats,
          ),
        ]
      : [];

    clearBoundVariable(item, "width", stats);
    bindFloatVariable(
      item,
      "height",
      metrics.itemHeightToken,
      variableByName,
      stats,
    );
    bindFloatVariable(
      item,
      "cornerRadius",
      "SegmentedControl/item-radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      item,
      "paddingLeft",
      metrics.itemPaddingXToken,
      variableByName,
      stats,
    );
    bindFloatVariable(
      item,
      "paddingRight",
      metrics.itemPaddingXToken,
      variableByName,
      stats,
    );

    const label = labelNodes[index];
    label.fills = [
      paintFromVariable(
        segmentedControlLabelColor({
          disabled,
          error,
          selected,
        }).token,
        segmentedControlLabelColor({
          disabled,
          error,
          selected,
        }).fallback,
        variableByName,
        stats,
      ),
    ];
    item.appendChild(label);
    setHugChildSizing(label);
    syncFocusRing(item, {
      enabled: state !== "Disabled" && selected,
      width: itemWidth,
      height: metrics.itemHeight,
      radius: metrics.itemRadius,
      variableName: focusRingVariableForState(state),
      fallback: focusRingFallbackForState(state),
      variableByName,
      stats,
    });
    component.appendChild(item);
    setHugChildSizing(item);
  }

  return { controlWidth, itemWidths };
}

async function syncCardVariantChildren({
  component,
  value,
  variableByName,
  fonts,
  stats,
}) {
  const hasHeader = value === "Header" || value === "Full";
  const hasFooter = value === "Full";

  let header = directChildNamed(component, "Card Header");
  if (header && header.type !== "FRAME") {
    header.remove();
    header = null;
  }

  if (hasHeader && (!header || header.type !== "FRAME")) {
    header = figma.createFrame();
    header.name = "Card Header";
  }

  if (header && hasHeader) {
    header.layoutMode = "VERTICAL";
    header.primaryAxisSizingMode = "AUTO";
    header.counterAxisSizingMode = "FIXED";
    header.primaryAxisAlignItems = "MIN";
    header.counterAxisAlignItems = "MIN";
    header.itemSpacing = 6;
    header.paddingLeft = 24;
    header.paddingRight = 24;
    header.paddingTop = 24;
    header.paddingBottom = 24;
    header.resizeWithoutConstraints(360, 80);
    header.fills = [];
    header.strokes = [];
    header.clipsContent = false;
    setLayoutSizingHorizontal(header, "FILL");

    let title = directChildNamed(header, "Title Text");
    if (title && title.type !== "TEXT") {
      title.remove();
      title = null;
    }
    if (!title || title.type !== "TEXT") {
      title = figma.createText();
      title.name = "Title Text";
    }
    await applyCardTitleTypography(title, fonts, variableByName, stats);
    title.characters = cardTitleText(value);
    title.fills = [
      paintFromVariable(
        "Colors/foreground/0",
        "#000000",
        variableByName,
        stats,
      ),
    ];
    title.resizeWithoutConstraints(312, 24);
    setLayoutSizingHorizontal(title, "FILL");
    setTextAutoResize(title, "HEIGHT");

    let description = directChildNamed(header, "Description Text");
    if (description && description.type !== "TEXT") {
      description.remove();
      description = null;
    }
    if (!description || description.type !== "TEXT") {
      description = figma.createText();
      description.name = "Description Text";
    }
    await applyCardDescriptionTypography(
      description,
      fonts,
      variableByName,
      stats,
    );
    description.characters = cardDescriptionText(value);
    description.fills = [
      paintFromVariable(
        "Colors/foreground/400",
        "#5D626F",
        variableByName,
        stats,
      ),
    ];
    description.resizeWithoutConstraints(312, 40);
    setLayoutSizingHorizontal(description, "FILL");
    setTextAutoResize(description, "HEIGHT");

    header.appendChild(title);
    header.appendChild(description);
    appendWithSizing(component, header, "FILL", "HUG");
  } else if (header) {
    header.remove();
    header = null;
  }

  let body = directChildNamed(component, "Card Content");
  if (body && body.type !== "FRAME") {
    body.remove();
    body = null;
  }
  if (!body || body.type !== "FRAME") {
    body = figma.createFrame();
    body.name = "Card Content";
  }

  body.layoutMode = "VERTICAL";
  body.primaryAxisSizingMode = "AUTO";
  body.counterAxisSizingMode = "FIXED";
  body.primaryAxisAlignItems = "MIN";
  body.counterAxisAlignItems = "MIN";
  body.itemSpacing = 12;
  body.paddingLeft = 24;
  body.paddingRight = 24;
  body.paddingTop = hasHeader ? 0 : 24;
  body.paddingBottom = 24;
  body.resizeWithoutConstraints(360, 92);
  body.fills = [];
  body.strokes = [];
  body.clipsContent = false;
  setLayoutSizingHorizontal(body, "FILL");

  let bodyText = directChildNamed(body, "Body Text");
  if (bodyText && bodyText.type !== "TEXT") {
    bodyText.remove();
    bodyText = null;
  }
  if (!bodyText || bodyText.type !== "TEXT") {
    bodyText = figma.createText();
    bodyText.name = "Body Text";
  }
  await applyCardBodyTypography(bodyText, fonts, variableByName, stats);
  bodyText.characters = cardBodyText(value);
  bodyText.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  bodyText.resizeWithoutConstraints(312, 66);
  setLayoutSizingHorizontal(bodyText, "FILL");
  setTextAutoResize(bodyText, "HEIGHT");
  body.appendChild(bodyText);
  component.appendChild(body);

  let footer = directChildNamed(component, "Card Footer");
  if (footer && footer.type !== "FRAME") {
    footer.remove();
    footer = null;
  }

  if (hasFooter && (!footer || footer.type !== "FRAME")) {
    footer = figma.createFrame();
    footer.name = "Card Footer";
  }

  if (footer && hasFooter) {
    footer.layoutMode = "HORIZONTAL";
    footer.primaryAxisSizingMode = "AUTO";
    footer.counterAxisSizingMode = "FIXED";
    footer.primaryAxisAlignItems = "MAX";
    footer.counterAxisAlignItems = "CENTER";
    footer.itemSpacing = 12;
    footer.paddingLeft = 24;
    footer.paddingRight = 24;
    footer.paddingTop = 0;
    footer.paddingBottom = 24;
    footer.resizeWithoutConstraints(360, 68);
    footer.fills = [];
    footer.strokes = [];
    footer.clipsContent = false;
    setLayoutSizingHorizontal(footer, "FILL");

    await syncCardFooterAction({
      footer,
      name: "Secondary Action",
      label: "Cancel",
      primary: false,
      stats,
    });
    await syncCardFooterAction({
      footer,
      name: "Primary Action",
      label: "Save",
      primary: true,
      stats,
    });
    component.appendChild(footer);
  } else if (footer) {
    footer.remove();
    footer = null;
  }

  bindCardGeometryVariables(
    component,
    header,
    body,
    footer,
    variableByName,
    stats,
  );
  if (body && value === "Basic") {
    bindFloatVariable(
      body,
      "paddingTop",
      "Card/padding",
      variableByName,
      stats,
    );
  }
}

async function syncCardFooterAction({ footer, name, label, primary, stats }) {
  let action = directChildNamed(footer, name);
  if (action && !isNestedComponentInstance(action, "Button")) {
    action.remove();
    action = null;
  }

  let buttonSet = null;
  if (!action) {
    const created = await createNestedComponentInstance({
      componentSetName: "Button",
      variantProperties: {
        Variant: primary ? "Default" : "Outline",
        Size: "Default",
        State: "Default",
      },
      name,
      stats,
    });
    if (!created) {
      action = createMissingNestedComponentNode(
        name,
        "Build Button before updating Card.",
        stats,
      );
    } else {
      action = created.instance;
      buttonSet = created.componentSet;
    }
  } else {
    buttonSet = await findLocalComponentSetByName("Button");
  }

  if (action.type === "INSTANCE" && buttonSet) {
    try {
      action.setProperties({
        Variant: primary ? "Default" : "Outline",
        Size: "Default",
        State: "Default",
      });
    } catch (error) {
      stats.warnings.push(
        `Card ${name}: could not set Button variant properties (${messageFor(error)}).`,
      );
    }
    setInstanceTextProperty(action, buttonSet, "Label Text", label, stats);
  }

  action.setSharedPluginData(
    RUN_NAMESPACE,
    "role",
    primary ? "primary" : "secondary",
  );
  setLayoutSizingHorizontal(action, "HUG");
  footer.appendChild(action);
}

async function syncListVariantChildren({
  component,
  density,
  metrics,
  variableByName,
  fonts,
  stats,
}) {
  removeDirectChildren(component);
  const items = [];
  const defaults = ["First item", "Second item", "Third item"];

  for (let index = 0; index < defaults.length; index += 1) {
    const item = figma.createFrame();
    item.name = `List Item ${index + 1}`;
    item.layoutMode = "HORIZONTAL";
    item.primaryAxisSizingMode = "FIXED";
    item.counterAxisSizingMode = "FIXED";
    item.primaryAxisAlignItems = "CENTER";
    item.counterAxisAlignItems = "CENTER";
    item.itemSpacing = 0;
    item.paddingLeft = 16;
    item.paddingRight = 16;
    item.paddingTop = metrics.paddingY;
    item.paddingBottom = metrics.paddingY;
    item.resizeWithoutConstraints(metrics.width, metrics.rowHeight);
    item.fills = [];
    item.strokes =
      index < defaults.length - 1
        ? [
            paintFromVariable(
              "Colors/background/200",
              "#E3E4E8",
              variableByName,
              stats,
            ),
          ]
        : [];
    item.strokeWeight = index < defaults.length - 1 ? 1 : 0;
    item.strokeTopWeight = 0;
    item.strokeRightWeight = 0;
    item.strokeBottomWeight = index < defaults.length - 1 ? 1 : 0;
    item.strokeLeftWeight = 0;
    item.clipsContent = false;
    item.setSharedPluginData(RUN_NAMESPACE, "kind", "list-item");
    setLayoutSizingHorizontal(item, "FILL");
    setLayoutSizingVertical(item, "FIXED");

    const text = figma.createText();
    text.name = `Item ${index + 1} Text`;
    await applyListItemTypography(text, fonts, variableByName, stats);
    text.characters = defaults[index];
    text.textAlignHorizontal = "LEFT";
    text.textAlignVertical = "CENTER";
    text.fills = [
      paintFromVariable(
        "Colors/foreground/0",
        "#000000",
        variableByName,
        stats,
      ),
    ];
    text.resizeWithoutConstraints(metrics.width - 32, metrics.textHeight);
    setLayoutSizingHorizontal(text, "FILL");
    setLayoutSizingVertical(text, "HUG");
    item.appendChild(text);

    component.appendChild(item);
    items.push(item);
  }

  bindListGeometryVariables(component, density, items, variableByName, stats);
}

async function syncTableVariantChildren({
  component,
  density,
  metrics,
  variableByName,
  fonts,
  stats,
}) {
  removeDirectChildren(component);
  const rows = [];
  const cells = [];
  const headers = ["Name", "Status", "Updated"];
  const rowData = [
    ["Beacon sync", "Ready", "Today"],
    ["Indoor map", "Draft", "Yesterday"],
    ["Analytics", "Queued", "May 21"],
  ];

  const header = await syncTableRow({
    component,
    name: "Table Header",
    values: headers,
    rowIndex: 0,
    metrics,
    header: true,
    variableByName,
    fonts,
    stats,
  });
  rows.push(header.row);
  for (const cell of header.cells) {
    cells.push(cell);
  }

  for (let rowIndex = 0; rowIndex < rowData.length; rowIndex += 1) {
    const row = await syncTableRow({
      component,
      name: `Table Row ${rowIndex + 1}`,
      values: rowData[rowIndex],
      rowIndex: rowIndex + 1,
      metrics,
      header: false,
      variableByName,
      fonts,
      stats,
    });
    rows.push(row.row);
    for (const cell of row.cells) {
      cells.push(cell);
    }
  }

  bindTableGeometryVariables(
    component,
    density,
    rows,
    cells,
    variableByName,
    stats,
  );
}

async function syncTableRow({
  component,
  name,
  values,
  rowIndex,
  metrics,
  header,
  variableByName,
  fonts,
  stats,
}) {
  const row = figma.createFrame();
  row.name = name;
  row.layoutMode = "HORIZONTAL";
  row.primaryAxisSizingMode = "FIXED";
  row.counterAxisSizingMode = "FIXED";
  row.primaryAxisAlignItems = "CENTER";
  row.counterAxisAlignItems = "CENTER";
  row.itemSpacing = 0;
  row.paddingLeft = 0;
  row.paddingRight = 0;
  row.paddingTop = 0;
  row.paddingBottom = 0;
  row.resizeWithoutConstraints(metrics.width, metrics.rowHeight);
  row.fills = header
    ? [paintFromVariable("Surface/100", "#F7F7F8", variableByName, stats)]
    : [];
  row.strokes =
    rowIndex < metrics.rowCount - 1
      ? [
          paintFromVariable(
            "Colors/background/200",
            "#E3E4E8",
            variableByName,
            stats,
          ),
        ]
      : [];
  row.strokeWeight = rowIndex < metrics.rowCount - 1 ? 1 : 0;
  row.strokeTopWeight = 0;
  row.strokeRightWeight = 0;
  row.strokeBottomWeight = rowIndex < metrics.rowCount - 1 ? 1 : 0;
  row.strokeLeftWeight = 0;
  row.clipsContent = false;
  row.setSharedPluginData(
    RUN_NAMESPACE,
    "kind",
    header ? "table-header" : "table-row",
  );
  setLayoutSizingHorizontal(row, "FILL");
  setLayoutSizingVertical(row, "FIXED");

  const rowCells = [];
  for (
    let columnIndex = 0;
    columnIndex < metrics.columnCount;
    columnIndex += 1
  ) {
    const cell = figma.createFrame();
    cell.name = header
      ? `Header ${columnIndex + 1} Cell`
      : `Row ${rowIndex} Cell ${columnIndex + 1}`;
    cell.layoutMode = "HORIZONTAL";
    cell.primaryAxisSizingMode = "FIXED";
    cell.counterAxisSizingMode = "FIXED";
    cell.primaryAxisAlignItems = "MIN";
    cell.counterAxisAlignItems = "CENTER";
    cell.itemSpacing = 0;
    cell.paddingLeft = 16;
    cell.paddingRight = 16;
    cell.paddingTop = metrics.paddingY;
    cell.paddingBottom = metrics.paddingY;
    cell.resizeWithoutConstraints(
      metrics.width / metrics.columnCount,
      metrics.rowHeight,
    );
    cell.fills = [];
    cell.strokes = [];
    cell.clipsContent = false;
    cell.setSharedPluginData(
      RUN_NAMESPACE,
      "kind",
      header ? "table-header-cell" : "table-cell",
    );
    setLayoutSizingHorizontal(cell, "FILL");
    setLayoutSizingVertical(cell, "FIXED");
    try {
      cell.layoutGrow = 1;
    } catch (_error) {
      // layoutGrow is unavailable on older plugin runtimes.
    }

    const text = figma.createText();
    text.name = header
      ? `Header ${columnIndex + 1} Text`
      : `Row ${rowIndex} Cell ${columnIndex + 1} Text`;
    if (header) {
      await applyTableHeaderTypography(text, fonts, variableByName, stats);
    } else {
      await applyTableCellTypography(text, fonts, variableByName, stats);
    }
    text.characters = values[columnIndex];
    text.textAlignHorizontal = "LEFT";
    text.textAlignVertical = "CENTER";
    text.fills = [
      paintFromVariable(
        header ? "Colors/foreground/400" : "Colors/foreground/0",
        header ? "#5D626F" : "#000000",
        variableByName,
        stats,
      ),
    ];
    text.resizeWithoutConstraints(
      metrics.width / metrics.columnCount - 32,
      metrics.textHeight,
    );
    setLayoutSizingHorizontal(text, "FILL");
    setLayoutSizingVertical(text, "HUG");
    cell.appendChild(text);
    row.appendChild(cell);
    rowCells.push(cell);
  }

  component.appendChild(row);
  return { row, cells: rowCells };
}

function syncRowSeparator({ parent, name, width, y, variableByName, stats }) {
  const separator = figma.createRectangle();
  separator.name = name;
  separator.resizeWithoutConstraints(width, 1);
  separator.x = 0;
  separator.y = y;
  separator.fills = [
    paintFromVariable(
      "Colors/background/200",
      "#E3E4E8",
      variableByName,
      stats,
    ),
  ];
  separator.strokes = [];
  separator.strokeWeight = 0;
  separator.setSharedPluginData(RUN_NAMESPACE, "kind", "row-separator");

  try {
    separator.layoutPositioning = "ABSOLUTE";
  } catch (_error) {
    // Older Figma runtimes may not expose absolute positioning on rectangles.
  }

  try {
    separator.constraints = { horizontal: "STRETCH", vertical: "MAX" };
  } catch (_error) {
    // Constraints can be rejected on some shape contexts in older runtimes.
  }

  parent.appendChild(separator);
}

async function syncTabsVariantChildren({
  component,
  count,
  active,
  state,
  tabCount,
  width,
  variableByName,
  fonts,
  stats,
}) {
  const triggerWidth = (width - 8) / tabCount;
  const triggers = [];

  const existingChildren = [];
  for (const child of component.children) existingChildren.push(child);

  for (const child of existingChildren) {
    const match = /^Tab (\d+) Trigger$/.exec(child.name || "");
    if (match && Number(match[1]) > tabCount) {
      child.remove();
    }
  }

  for (let index = 0; index < tabCount; index += 1) {
    const triggerName = `Tab ${index + 1} Trigger`;
    const textName = `Tab ${index + 1} Text`;
    const isActive = TABS_ACTIVE[index] === active;
    const config = tabsTriggerConfig(state, isActive);

    let trigger = directChildNamed(component, triggerName);
    if (trigger && trigger.type !== "FRAME") {
      trigger.remove();
      trigger = null;
    }
    if (!trigger || trigger.type !== "FRAME") {
      trigger = figma.createFrame();
      trigger.name = triggerName;
    }

    trigger.layoutMode = "HORIZONTAL";
    trigger.primaryAxisSizingMode = "FIXED";
    trigger.counterAxisSizingMode = "FIXED";
    trigger.primaryAxisAlignItems = "CENTER";
    trigger.counterAxisAlignItems = "CENTER";
    trigger.itemSpacing = 0;
    trigger.paddingLeft = 12;
    trigger.paddingRight = 12;
    trigger.paddingTop = 0;
    trigger.paddingBottom = 0;
    trigger.resizeWithoutConstraints(triggerWidth, 36);
    trigger.cornerRadius = 12;
    trigger.clipsContent = false;
    trigger.fills = config.background
      ? [
          paintFromVariable(
            config.background,
            config.backgroundFallback,
            variableByName,
            stats,
          ),
        ]
      : [];
    trigger.strokes = [];
    trigger.setSharedPluginData(RUN_NAMESPACE, "kind", "tabs-trigger");
    trigger.setSharedPluginData(
      RUN_NAMESPACE,
      "active",
      isActive ? "true" : "false",
    );
    setLayoutSizingHorizontal(trigger, "FILL");
    try {
      trigger.layoutGrow = 1;
    } catch (_error) {
      // layoutGrow is unavailable on older plugin runtimes.
    }

    let text = directChildNamed(trigger, textName);
    if (text && text.type !== "TEXT") {
      text.remove();
      text = null;
    }
    if (!text || text.type !== "TEXT") {
      text = figma.createText();
      text.name = textName;
    }

    await applyTabsTriggerTypography(text, fonts, variableByName, stats);
    text.characters = tabsDefaultLabel(index);
    text.fills = [
      paintFromVariable(
        config.foreground,
        config.foregroundFallback,
        variableByName,
        stats,
      ),
    ];
    text.textAlignHorizontal = "CENTER";
    text.textAlignVertical = "CENTER";
    trigger.appendChild(text);

    syncFocusRing(trigger, {
      enabled: state === "Focus" && isActive,
      width: triggerWidth,
      height: 36,
      radius: 12,
      variableName: "Colors/theme/500",
      fallback: "#135BEC",
      variableByName,
      stats,
    });

    component.appendChild(trigger);
    triggers.push(trigger);
  }

  bindTabsGeometryVariables(component, count, triggers, variableByName, stats);
}

async function syncTooltipVariantChildren({
  component,
  side,
  variableByName,
  fonts,
  stats,
}) {
  let text = directChildNamed(component, "Content Text");
  if (text && text.type !== "TEXT") {
    text.remove();
    text = null;
  }

  if (!text || text.type !== "TEXT") {
    text = figma.createText();
    text.name = "Content Text";
  }

  await applyTooltipContentTypography(text, fonts, variableByName, stats);
  text.characters = tooltipContentText(side);
  text.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  text.textAlignHorizontal = "CENTER";
  text.textAlignVertical = "CENTER";

  component.appendChild(text);
  bindTooltipGeometryVariables(component, variableByName, stats);
  await syncTooltipTip({
    component,
    side,
    variableByName,
    stats,
  });
}

async function syncTooltipTip({ component, side, variableByName, stats }) {
  let tip = directChildNamed(component, "Tip");
  if (tip && tip.type !== "FRAME") {
    tip.remove();
    tip = null;
  }

  if (!tip || tip.type !== "FRAME") {
    tip = figma.createFrame();
    tip.name = "Tip";
  }

  component.appendChild(tip);

  try {
    tip.layoutPositioning = "ABSOLUTE";
  } catch (_error) {
    // Older Figma runtimes may not expose absolute positioning on every node.
  }

  try {
    tip.constraints = tooltipTipConstraints(side);
  } catch (_error) {
    // Constraints can be rejected on some shape contexts in older runtimes.
  }

  const longEdge = 8;
  const shortEdge = 4;
  const width = side === "Left" || side === "Right" ? shortEdge : longEdge;
  const height = side === "Left" || side === "Right" ? longEdge : shortEdge;
  const overlap = 1;
  const halfWidth = width / 2;
  const halfHeight = height / 2;
  tip.resizeWithoutConstraints(width, height);
  tip.rotation = 0;
  tip.clipsContent = false;
  tip.fills = [];
  tip.strokes = [];
  tip.strokeWeight = 0;
  tip.setSharedPluginData(RUN_NAMESPACE, "kind", "tooltip-tip");
  tip.setSharedPluginData(RUN_NAMESPACE, "side", side);
  tip.setSharedPluginData(RUN_NAMESPACE, "shape", "triangle");

  await syncTooltipTipFill({
    tip,
    side,
    width,
    height,
    variableByName,
    stats,
  });

  if (side === "Top") {
    tip.x = component.width / 2 - halfWidth;
    tip.y = component.height - overlap;
  } else if (side === "Bottom") {
    tip.x = component.width / 2 - halfWidth;
    tip.y = -height + overlap;
  } else if (side === "Right") {
    tip.x = -width + overlap;
    tip.y = component.height / 2 - halfHeight;
  } else {
    tip.x = component.width - overlap;
    tip.y = component.height / 2 - halfHeight;
  }

  bindTooltipTipVariables(tip, side, variableByName, stats);
}

async function syncTooltipTipFill({
  tip,
  side,
  width,
  height,
  variableByName,
  stats,
}) {
  let fill = directChildNamed(tip, "Tip Fill");
  if (fill && fill.type !== "VECTOR") {
    fill.remove();
    fill = null;
  }

  if (!fill || fill.type !== "VECTOR") {
    fill = figma.createVector();
    fill.name = "Tip Fill";
  }

  tip.appendChild(fill);
  fill.x = 0;
  fill.y = 0;
  fill.resizeWithoutConstraints(width, height);
  await applyVectorNetwork(fill, tooltipTipVectorNetwork(side, width, height));
  fill.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  fill.strokes = [];
  fill.strokeWeight = 0;

  try {
    fill.constraints = { horizontal: "STRETCH", vertical: "STRETCH" };
  } catch (_error) {
    // Older plugin runtimes can reject constraints on some vector nodes.
  }
}

async function applyVectorNetwork(node, vectorNetwork) {
  if (node && typeof node.setVectorNetworkAsync === "function") {
    await node.setVectorNetworkAsync(vectorNetwork);
    return;
  }
  node.vectorNetwork = vectorNetwork;
}

function tooltipTipVectorNetwork(side, width, height) {
  const points = tooltipTipPoints(side, width, height);
  return {
    vertices: [
      {
        x: points[0].x,
        y: points[0].y,
        strokeCap: "NONE",
        strokeJoin: "MITER",
      },
      {
        x: points[1].x,
        y: points[1].y,
        strokeCap: "NONE",
        strokeJoin: "MITER",
      },
      {
        x: points[2].x,
        y: points[2].y,
        strokeCap: "NONE",
        strokeJoin: "MITER",
      },
    ],
    segments: [
      {
        start: 0,
        end: 1,
        tangentStart: { x: 0, y: 0 },
        tangentEnd: { x: 0, y: 0 },
      },
      {
        start: 1,
        end: 2,
        tangentStart: { x: 0, y: 0 },
        tangentEnd: { x: 0, y: 0 },
      },
      {
        start: 2,
        end: 0,
        tangentStart: { x: 0, y: 0 },
        tangentEnd: { x: 0, y: 0 },
      },
    ],
    regions: [{ windingRule: "NONZERO", loops: [[0, 1, 2]] }],
  };
}

function tooltipTipPoints(side, width, height) {
  if (side === "Top") {
    return [
      { x: 0, y: 0 },
      { x: width, y: 0 },
      { x: width / 2, y: height },
    ];
  }
  if (side === "Bottom") {
    return [
      { x: width / 2, y: 0 },
      { x: width, y: height },
      { x: 0, y: height },
    ];
  }
  if (side === "Right") {
    return [
      { x: 0, y: height / 2 },
      { x: width, y: 0 },
      { x: width, y: height },
    ];
  }
  return [
    { x: width, y: height / 2 },
    { x: 0, y: height },
    { x: 0, y: 0 },
  ];
}

function tooltipTipConstraints(side) {
  if (side === "Top") {
    return { horizontal: "CENTER", vertical: "MAX" };
  }
  if (side === "Bottom") {
    return { horizontal: "CENTER", vertical: "MIN" };
  }
  if (side === "Right") {
    return { horizontal: "MIN", vertical: "CENTER" };
  }
  return { horizontal: "MAX", vertical: "CENTER" };
}

function tooltipContentText(side) {
  if (side === "Top") return "Add to library";
  if (side === "Right") return "View details";
  if (side === "Bottom") return "Copied";
  return "Keyboard shortcut";
}

function tooltipShadowEffects() {
  return [
    {
      type: "DROP_SHADOW",
      color: { r: 0, g: 0, b: 0, a: 0.1 },
      offset: { x: 0, y: 4 },
      radius: 6,
      spread: -1,
      visible: true,
      blendMode: "NORMAL",
    },
    {
      type: "DROP_SHADOW",
      color: { r: 0, g: 0, b: 0, a: 0.06 },
      offset: { x: 0, y: 2 },
      radius: 4,
      spread: -1,
      visible: true,
      blendMode: "NORMAL",
    },
  ];
}

async function syncDialogVariantChildren({
  component,
  value,
  variableByName,
  fonts,
  stats,
}) {
  const hasFooter = value === "Form" || value === "Footer";
  removeGeneratedButtonChild(component, "Dialog Close", true);

  let header = directChildNamed(component, "Dialog Header");
  if (header && header.type !== "FRAME") {
    header.remove();
    header = null;
  }
  if (!header || header.type !== "FRAME") {
    header = figma.createFrame();
    header.name = "Dialog Header";
  }
  header.layoutMode = "HORIZONTAL";
  header.primaryAxisSizingMode = "AUTO";
  header.counterAxisSizingMode = "FIXED";
  header.primaryAxisAlignItems = "MIN";
  header.counterAxisAlignItems = "MIN";
  header.itemSpacing = 12;
  header.paddingLeft = 0;
  header.paddingRight = 0;
  header.paddingTop = 0;
  header.paddingBottom = 0;
  header.resizeWithoutConstraints(464, 56);
  header.fills = [];
  header.strokes = [];
  header.clipsContent = false;
  setLayoutSizingHorizontal(header, "FILL");

  let headerContent = directChildNamed(header, "Dialog Header Content");
  if (headerContent && headerContent.type !== "FRAME") {
    headerContent.remove();
    headerContent = null;
  }
  if (!headerContent || headerContent.type !== "FRAME") {
    headerContent = figma.createFrame();
    headerContent.name = "Dialog Header Content";
  }
  headerContent.layoutMode = "VERTICAL";
  headerContent.primaryAxisSizingMode = "AUTO";
  headerContent.counterAxisSizingMode = "FIXED";
  headerContent.primaryAxisAlignItems = "MIN";
  headerContent.counterAxisAlignItems = "MIN";
  headerContent.itemSpacing = 6;
  headerContent.paddingLeft = 0;
  headerContent.paddingRight = 0;
  headerContent.paddingTop = 0;
  headerContent.paddingBottom = 0;
  headerContent.resizeWithoutConstraints(420, 56);
  headerContent.fills = [];
  headerContent.strokes = [];
  headerContent.clipsContent = false;
  setLayoutSizingHorizontal(headerContent, "FILL");
  try {
    headerContent.layoutGrow = 1;
  } catch (_error) {
    // layoutGrow is unavailable on older plugin runtimes.
  }

  let title =
    directChildNamed(headerContent, "Title Text") ||
    directChildNamed(header, "Title Text");
  if (title && title.type !== "TEXT") {
    title.remove();
    title = null;
  }
  if (!title || title.type !== "TEXT") {
    title = figma.createText();
    title.name = "Title Text";
  }
  await applyDialogTitleTypography(title, fonts, variableByName, stats);
  title.characters = dialogTitleText(value);
  title.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  title.resizeWithoutConstraints(420, 24);
  setLayoutSizingHorizontal(title, "FILL");
  setTextAutoResize(title, "HEIGHT");

  let description =
    directChildNamed(headerContent, "Description Text") ||
    directChildNamed(header, "Description Text");
  if (description && description.type !== "TEXT") {
    description.remove();
    description = null;
  }
  if (!description || description.type !== "TEXT") {
    description = figma.createText();
    description.name = "Description Text";
  }
  await applyDialogDescriptionTypography(
    description,
    fonts,
    variableByName,
    stats,
  );
  description.characters = dialogDescriptionText(value);
  description.fills = [
    paintFromVariable(
      "Colors/foreground/400",
      "#5D626F",
      variableByName,
      stats,
    ),
  ];
  description.resizeWithoutConstraints(420, 40);
  setLayoutSizingHorizontal(description, "FILL");
  setTextAutoResize(description, "HEIGHT");

  headerContent.appendChild(title);
  setLayoutSizingHorizontal(title, "FILL");
  setLayoutSizingVertical(title, "HUG");
  headerContent.appendChild(description);
  setLayoutSizingHorizontal(description, "FILL");
  setLayoutSizingVertical(description, "HUG");
  header.appendChild(headerContent);
  setLayoutSizingHorizontal(headerContent, "FILL");
  setLayoutSizingVertical(headerContent, "HUG");

  const close = await syncInlineCloseButton({
    parent: header,
    name: "Dialog Close",
    kind: "dialog-close",
    size: 32,
    iconSize: 16,
    iconSizeToken: "Dialog/close/icon/size",
    variableByName,
    stats,
  });

  component.appendChild(header);
  setVerticalStackChildSizing(header);

  let body = directChildNamed(component, "Dialog Body");
  if (body && body.type !== "FRAME") {
    body.remove();
    body = null;
  }
  if (!body || body.type !== "FRAME") {
    body = figma.createFrame();
    body.name = "Dialog Body";
  }
  body.layoutMode = "VERTICAL";
  body.primaryAxisSizingMode = "AUTO";
  body.counterAxisSizingMode = "FIXED";
  body.primaryAxisAlignItems = "MIN";
  body.counterAxisAlignItems = "MIN";
  body.itemSpacing = 12;
  body.paddingLeft = 0;
  body.paddingRight = 0;
  body.paddingTop = 0;
  body.paddingBottom = 0;
  body.resizeWithoutConstraints(464, value === "Form" ? 132 : 60);
  body.fills = [];
  body.strokes = [];
  body.clipsContent = false;
  setLayoutSizingHorizontal(body, "FILL");
  component.appendChild(body);
  setVerticalStackChildSizing(body);

  let bodyText = directChildNamed(body, "Body Text");
  if (bodyText && bodyText.type !== "TEXT") {
    bodyText.remove();
    bodyText = null;
  }
  if (!bodyText || bodyText.type !== "TEXT") {
    bodyText = figma.createText();
    bodyText.name = "Body Text";
  }
  await applyDialogBodyTypography(bodyText, fonts, variableByName, stats);
  bodyText.characters = dialogBodyText(value);
  bodyText.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  bodyText.resizeWithoutConstraints(464, 44);
  setLayoutSizingHorizontal(bodyText, "FILL");
  setTextAutoResize(bodyText, "HEIGHT");
  body.appendChild(bodyText);
  setLayoutSizingHorizontal(bodyText, "FILL");
  setLayoutSizingVertical(bodyText, "HUG");

  if (value === "Form") {
    removeGeneratedButtonChild(body, "Name Field", true);
    removeGeneratedButtonChild(body, "Username Field", true);
    await syncDialogInput({
      body,
      name: "Name Input",
      label: "Name",
      placeholder: "Pedro Duarte",
      stats,
    });
    await syncDialogInput({
      body,
      name: "Username Input",
      label: "Username",
      placeholder: "@peduarte",
      stats,
    });
  } else {
    removeGeneratedButtonChild(body, "Name Field", true);
    removeGeneratedButtonChild(body, "Username Field", true);
    removeGeneratedButtonChild(body, "Name Input", true);
    removeGeneratedButtonChild(body, "Username Input", true);
  }

  let footer = directChildNamed(component, "Dialog Footer");
  if (footer && footer.type !== "FRAME") {
    footer.remove();
    footer = null;
  }

  if (hasFooter && (!footer || footer.type !== "FRAME")) {
    footer = figma.createFrame();
    footer.name = "Dialog Footer";
  }

  if (footer && hasFooter) {
    footer.layoutMode = "HORIZONTAL";
    footer.primaryAxisSizingMode = "AUTO";
    footer.counterAxisSizingMode = "FIXED";
    footer.primaryAxisAlignItems = "MAX";
    footer.counterAxisAlignItems = "CENTER";
    footer.itemSpacing = 8;
    footer.paddingLeft = 0;
    footer.paddingRight = 0;
    footer.paddingTop = 0;
    footer.paddingBottom = 0;
    footer.resizeWithoutConstraints(464, 44);
    footer.fills = [];
    footer.strokes = [];
    footer.clipsContent = false;

    await syncDialogFooterAction({
      footer,
      name: "Secondary Action",
      label: "Cancel",
      primary: false,
      stats,
    });
    await syncDialogFooterAction({
      footer,
      name: "Primary Action",
      label: "Save changes",
      primary: true,
      stats,
    });
    component.appendChild(footer);
    setDialogFooterContainerSizing(footer, variableByName, stats);
  } else if (footer) {
    footer.remove();
    footer = null;
  }

  bindDialogGeometryVariables(
    component,
    header,
    body,
    footer,
    close,
    variableByName,
    stats,
  );
}

async function syncBottomSheetVariantChildren({
  component,
  content,
  variableByName,
  fonts,
  stats,
}) {
  const reusableContentSlot = extractDescendantNamed(
    component,
    "Content Slot",
    stats,
    "BottomSheet content slot",
  );
  removeDirectChildrenExcept(component, reusableContentSlot);
  const contentWidth = Math.max(
    1,
    (component.width || 360) -
      (component.paddingLeft || 0) -
      (component.paddingRight || 0),
  );

  const handle = figma.createRectangle();
  handle.name = "Drag Handle";
  handle.resizeWithoutConstraints(48, 5);
  handle.cornerRadius = 9999;
  handle.fills = [
    paintFromVariableWithOpacity(
      "Colors/foreground/500",
      "#747B8B",
      0.36,
      variableByName,
      stats,
    ),
  ];
  handle.strokes = [];
  handle.setSharedPluginData(RUN_NAMESPACE, "kind", "bottom-sheet-handle");
  component.appendChild(handle);
  setHugChildSizing(handle);

  const header = figma.createFrame();
  header.name = "BottomSheet Header";
  header.layoutMode = "HORIZONTAL";
  header.primaryAxisSizingMode = "AUTO";
  header.counterAxisSizingMode = "FIXED";
  header.primaryAxisAlignItems = "MIN";
  header.counterAxisAlignItems = "MIN";
  header.itemSpacing = 12;
  header.paddingLeft = 0;
  header.paddingRight = 0;
  header.paddingTop = 0;
  header.paddingBottom = 0;
  header.resizeWithoutConstraints(contentWidth, 64);
  header.fills = [];
  header.strokes = [];
  header.clipsContent = false;
  setLayoutSizingHorizontal(header, "FILL");

  const headerContent = figma.createFrame();
  headerContent.name = "BottomSheet Header Content";
  headerContent.layoutMode = "VERTICAL";
  headerContent.primaryAxisSizingMode = "AUTO";
  headerContent.counterAxisSizingMode = "FIXED";
  headerContent.primaryAxisAlignItems = "MIN";
  headerContent.counterAxisAlignItems = "MIN";
  headerContent.itemSpacing = 6;
  headerContent.paddingLeft = 0;
  headerContent.paddingRight = 0;
  headerContent.paddingTop = 0;
  headerContent.paddingBottom = 0;
  headerContent.resizeWithoutConstraints(contentWidth - 56, 64);
  headerContent.fills = [];
  headerContent.strokes = [];
  headerContent.clipsContent = false;
  setLayoutSizingHorizontal(headerContent, "FIXED");
  try {
    headerContent.layoutGrow = 0;
  } catch (_error) {
    // layoutGrow is unavailable on older plugin runtimes.
  }

  const title = figma.createText();
  title.name = "Title Text";
  await applyDrawerTitleTypography(title, fonts, variableByName, stats);
  title.characters = bottomSheetTitleText(content);
  title.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  title.resizeWithoutConstraints(contentWidth - 56, 24);
  setLayoutSizingHorizontal(title, "FILL");
  setTextAutoResize(title, "HEIGHT");

  const description = figma.createText();
  description.name = "Description Text";
  await applyDrawerDescriptionTypography(
    description,
    fonts,
    variableByName,
    stats,
  );
  description.characters = bottomSheetDescriptionText(content);
  description.fills = [
    paintFromVariable(
      "Colors/foreground/400",
      "#5D626F",
      variableByName,
      stats,
    ),
  ];
  description.resizeWithoutConstraints(contentWidth - 56, 40);
  setLayoutSizingHorizontal(description, "FILL");
  setTextAutoResize(description, "HEIGHT");

  headerContent.appendChild(title);
  setLayoutSizingHorizontal(title, "FILL");
  setLayoutSizingVertical(title, "HUG");
  headerContent.appendChild(description);
  setLayoutSizingHorizontal(description, "FILL");
  setLayoutSizingVertical(description, "HUG");
  header.appendChild(headerContent);
  setLayoutSizingHorizontal(headerContent, "FIXED");
  setLayoutSizingVertical(headerContent, "HUG");

  const close = await syncInlineCloseButton({
    parent: header,
    name: "BottomSheet Close Button",
    kind: "bottom-sheet-close",
    size: 44,
    iconSize: 16,
    iconSizeToken: "Drawer/close/icon/size",
    variableByName,
    stats,
  });
  setLayoutSizingHorizontal(close, "FIXED");
  setLayoutSizingVertical(close, "FIXED");
  const closeIcon = directChildNamed(close, "Close Icon");
  if (closeIcon) {
    closeIcon.x = 20;
    closeIcon.y = 8;
  }

  component.appendChild(header);
  setVerticalStackChildSizing(header);

  const body = figma.createFrame();
  body.name = "BottomSheet Body";
  body.layoutMode = "VERTICAL";
  body.primaryAxisSizingMode = "AUTO";
  body.counterAxisSizingMode = "FIXED";
  body.primaryAxisAlignItems = "MIN";
  body.counterAxisAlignItems = "MIN";
  body.itemSpacing = 12;
  body.paddingLeft = 0;
  body.paddingRight = 0;
  body.paddingTop = 0;
  body.paddingBottom = 0;
  body.resizeWithoutConstraints(contentWidth, content === "Form" ? 224 : 120);
  body.fills = [];
  body.strokes = [];
  body.clipsContent = false;
  setLayoutSizingHorizontal(body, "FILL");
  component.appendChild(body);
  setVerticalStackChildSizing(body);

  if (content === "Form") {
    for (const formInput of BOTTOM_SHEET_FORM_INPUTS) {
      await syncBottomSheetInput({
        body,
        name: formInput.name,
        label: formInput.label,
        placeholder: formInput.placeholder,
        stats,
      });
    }
  }

  const contentSlot = appendBottomSheetContentSlot({
    body,
    component,
    contentWidth,
    reusableSlot: reusableContentSlot,
    stats,
  });
  contentSlot.visible = content !== "Form";
  setVerticalStackChildSizing(contentSlot);
  if (
    Array.from(body.children || []).some((child) => child.visible !== false)
  ) {
    resizeVerticalAutoLayoutFrameToVisibleChildren(body, contentWidth);
  } else {
    resizeNodeWithoutConstraints(body, contentWidth, 1);
  }
  if (
    content !== "Form" &&
    Number(body.height || 0) < BOTTOM_SHEET_CONTENT_SLOT_HEIGHT
  ) {
    resizeNodeWithoutConstraints(
      body,
      contentWidth,
      BOTTOM_SHEET_CONTENT_SLOT_HEIGHT,
    );
  }

  let footer = null;
  if (content === "Footer") {
    footer = figma.createFrame();
    footer.name = "BottomSheet Footer";
    footer.layoutMode = "HORIZONTAL";
    footer.primaryAxisSizingMode = "AUTO";
    footer.counterAxisSizingMode = "FIXED";
    footer.primaryAxisAlignItems = "MAX";
    footer.counterAxisAlignItems = "CENTER";
    footer.itemSpacing = 8;
    footer.paddingLeft = 0;
    footer.paddingRight = 0;
    footer.paddingTop = 0;
    footer.paddingBottom = 0;
    footer.resizeWithoutConstraints(contentWidth, 44);
    footer.fills = [];
    footer.strokes = [];
    footer.clipsContent = false;

    await syncBottomSheetFooterAction({
      footer,
      name: "Secondary Action",
      label: "Cancel",
      primary: false,
      stats,
    });
    await syncBottomSheetFooterAction({
      footer,
      name: "Primary Action",
      label: "Apply",
      primary: true,
      stats,
    });
    component.appendChild(footer);
    setBottomSheetFooterContainerSizing(footer, variableByName, stats);
  }

  bindBottomSheetGeometryVariables(
    component,
    header,
    body,
    footer,
    close,
    handle,
    variableByName,
    stats,
  );
}

function appendBottomSheetContentSlot({
  body,
  component,
  contentWidth,
  reusableSlot,
  stats,
}) {
  if (reusableSlot) {
    configureContentSlotNode(reusableSlot, {
      height: BOTTOM_SHEET_CONTENT_SLOT_HEIGHT,
      itemSpacing: 12,
      layoutMode: "VERTICAL",
      slotKind:
        reusableSlot.getSharedPluginData &&
        reusableSlot.getSharedPluginData(RUN_NAMESPACE, "slot-kind")
          ? reusableSlot.getSharedPluginData(RUN_NAMESPACE, "slot-kind")
          : "native",
      slotName: "Content Slot",
      width: contentWidth,
    });
    try {
      body.appendChild(reusableSlot);
      return reusableSlot;
    } catch (error) {
      if (stats && stats.warnings) {
        stats.warnings.push(
          `BottomSheet ${component.name}: could not reuse Content Slot (${messageFor(error)}); creating a fresh slot.`,
        );
      }
      try {
        reusableSlot.remove();
      } catch (_removeError) {
        // The reusable slot may already have been detached by Figma.
      }
    }
  }

  return appendComponentContentSlot({
    component,
    height: BOTTOM_SHEET_CONTENT_SLOT_HEIGHT,
    ownerName: "BottomSheet",
    parent: body,
    slotName: "Content Slot",
    stats,
    width: contentWidth,
  });
}

async function syncBottomSheetInput({ body, name, label, placeholder, stats }) {
  let input = directChildNamed(body, name);
  if (input && !isNestedComponentInstance(input, "Input")) {
    input.remove();
    input = null;
  }

  let inputSet = null;
  if (!input) {
    const created = await createNestedComponentInstance({
      componentSetName: "Input",
      variantProperties: {
        State: "Default",
        Status: "Default",
      },
      name,
      stats,
    });
    if (!created) {
      input = createMissingNestedComponentNode(
        name,
        "Build Input before updating BottomSheet.",
        stats,
      );
    } else {
      input = created.instance;
      inputSet = created.componentSet;
    }
  } else {
    inputSet = await findLocalComponentSetByName("Input");
  }

  if (input.type === "INSTANCE") {
    markNestedComponentInstance(input, "Input");
  }

  applyCompositeInputInstanceProperties({
    input,
    inputSet,
    label,
    name,
    ownerName: "BottomSheet",
    placeholder,
    stats,
  });

  input = await ensureCompositeFormInputChildSizing({
    input,
    label,
    missingMessage: "Build Input before updating BottomSheet.",
    name,
    ownerName: "BottomSheet",
    parent: body,
    placeholder,
    stats,
  });
}

function applyCompositeInputInstanceProperties({
  input,
  inputSet,
  label,
  name,
  ownerName,
  placeholder,
  stats,
}) {
  if (!input || input.type !== "INSTANCE" || !inputSet) return;

  markNestedComponentInstance(input, "Input");

  try {
    input.setProperties({
      State: "Default",
      Status: "Default",
    });
  } catch (error) {
    stats.warnings.push(
      `${ownerName} ${name}: could not set Input variant properties (${messageFor(error)}).`,
    );
  }
  setInstanceTextProperty(input, inputSet, "Label Text", label, stats);
  setInstanceTextProperty(
    input,
    inputSet,
    "Placeholder Text",
    placeholder,
    stats,
  );
  setInstanceBooleanProperty(input, inputSet, "Show Helper Text", false, stats);
}

async function ensureCompositeFormInputChildSizing({
  input,
  label,
  missingMessage,
  name,
  ownerName,
  parent,
  placeholder,
  stats,
}) {
  if (!input) return input;

  parent.appendChild(input);
  if (setCompositeFormInputChildSizing(input, stats)) return input;

  if (input.type !== "INSTANCE") return input;

  const created = await createNestedComponentInstance({
    componentSetName: "Input",
    variantProperties: {
      State: "Default",
      Status: "Default",
    },
    name,
    stats,
  });

  if (!created) {
    stats.warnings.push(`${name}: ${missingMessage}`);
    return input;
  }

  try {
    input.remove();
  } catch (_error) {
    // Figma may already have detached the stale nested instance during update.
  }

  const replacement = created.instance;
  applyCompositeInputInstanceProperties({
    input: replacement,
    inputSet: created.componentSet,
    label,
    name,
    ownerName,
    placeholder,
    stats,
  });
  parent.appendChild(replacement);
  if (!setCompositeFormInputChildSizing(replacement, stats)) {
    stats.warnings.push(
      `${ownerName} ${name}: nested Input instance could not be resized to fill its auto-layout parent (${compositeFormInputSizingDiagnostic(replacement)}).`,
    );
  } else {
    stats.compositeInputInstancesRecreated =
      (stats.compositeInputInstancesRecreated || 0) + 1;
  }

  return replacement;
}

function compositeFormInputSizingPasses(input) {
  if (!input) return false;

  return (
    (input.layoutSizingHorizontal === "FILL" ||
      input.layoutAlign === "STRETCH") &&
    input.layoutSizingVertical === "HUG"
  );
}

function setCompositeFormInputParentSizingContext(parent) {
  if (!parent || parent.layoutMode !== "VERTICAL") return;

  setNodePropertyIfSupported(parent, "counterAxisAlignItems", "MIN");
}

function clearCompositeFormInputRootSizeBindings(input, stats) {
  if (!input) return;

  clearBoundVariable(input, "width", stats);
  clearBoundVariable(input, "height", stats);
}

function compositeFormInputSizingDiagnostic(input) {
  if (!input) return "missing node";

  const parent = input.parent;
  const parentSummary = parent
    ? `${parent.name || "unnamed"}/${parent.layoutMode || "unset"}/${
        parent.counterAxisAlignItems || "unset"
      }`
    : "missing";

  return [
    `layoutSizingHorizontal=${input.layoutSizingHorizontal || "unset"}`,
    `layoutAlign=${input.layoutAlign || "unset"}`,
    `layoutSizingVertical=${input.layoutSizingVertical || "unset"}`,
    `boundWidth=${nodeHasBoundWidthVariable(input) ? "yes" : "no"}`,
    `parent=${parentSummary}`,
  ].join(", ");
}

function compositeSizingActualSummary(node) {
  return [
    `${node.layoutSizingHorizontal || "unset"} / ${node.layoutAlign || "unset"}`,
    `boundWidth=${nodeHasBoundWidthVariable(node) ? "yes" : "no"}`,
  ].join("; ");
}

function nodeHasBoundWidthVariable(node) {
  const boundVariables = node && node.boundVariables;
  if (!boundVariables) return false;

  return Boolean(
    boundVariables.width ||
    (boundVariables.size && boundVariables.size.x) ||
    (Array.isArray(boundVariables.size) && boundVariables.size.length > 0),
  );
}

function compositeFormInputParentInnerWidth(input) {
  const parent = input && input.parent;
  if (!parent || typeof parent.width !== "number") return null;

  const paddingLeft = Number(parent.paddingLeft || 0);
  const paddingRight = Number(parent.paddingRight || 0);
  const innerWidth = parent.width - paddingLeft - paddingRight;

  return Number.isFinite(innerWidth) && innerWidth > 0 ? innerWidth : null;
}

function resizeCompositeFormInputToParent(input) {
  const width = compositeFormInputParentInnerWidth(input);
  if (!width) return false;

  const height =
    typeof input.height === "number" && Number.isFinite(input.height)
      ? Math.max(1, input.height)
      : 70;

  return resizeNodeWithoutConstraints(input, width, height);
}

function setCompositeFormInputChildSizing(input, stats) {
  if (!input) return false;

  clearCompositeFormInputRootSizeBindings(input, stats);
  setCompositeFormInputParentSizingContext(input.parent);
  setNodePropertyIfSupported(input, "layoutPositioning", "AUTO");
  setNodePropertyIfSupported(input, "layoutAlign", "STRETCH");
  setLayoutSizingVertical(input, "HUG");
  setNodePropertyIfSupported(input, "layoutGrow", 0);
  resizeCompositeFormInputToParent(input);

  try {
    input.constraints = { horizontal: "STRETCH", vertical: "MIN" };
  } catch (_error) {
    // Constraints are not available on every node type.
  }

  // Re-apply after constraints because Figma can reset instance child sizing
  // when moving an instance between auto-layout parents.
  clearCompositeFormInputRootSizeBindings(input, stats);
  setNodePropertyIfSupported(input, "layoutAlign", "STRETCH");
  setLayoutSizingHorizontal(input, "FILL");
  setLayoutSizingVertical(input, "HUG");

  return compositeFormInputSizingPasses(input);
}

async function syncBottomSheetFooterAction({
  footer,
  name,
  label,
  primary,
  stats,
}) {
  const size = "Default";
  let action = directChildNamed(footer, name);
  if (action && !isNestedComponentInstance(action, "Button")) {
    action.remove();
    action = null;
  }

  let buttonSet = null;
  if (!action) {
    const created = await createNestedComponentInstance({
      componentSetName: "Button",
      variantProperties: {
        Variant: primary ? "Default" : "Outline",
        Size: size,
        State: "Default",
      },
      name,
      stats,
    });
    if (!created) {
      action = createMissingNestedComponentNode(
        name,
        "Build Button before updating BottomSheet.",
        stats,
      );
    } else {
      action = created.instance;
      buttonSet = created.componentSet;
    }
  } else {
    buttonSet = await findLocalComponentSetByName("Button");
  }

  if (action.type === "INSTANCE") {
    markNestedComponentInstance(
      action,
      "Button",
      primary ? "primary" : "secondary",
    );
  }

  if (action.type === "INSTANCE" && buttonSet) {
    try {
      action.setProperties({
        Variant: primary ? "Default" : "Outline",
        Size: size,
        State: "Default",
      });
    } catch (error) {
      stats.warnings.push(
        `BottomSheet ${name}: could not set Button variant properties (${messageFor(error)}).`,
      );
    }
    setInstanceTextProperty(action, buttonSet, "Label Text", label, stats);
  }

  action.setSharedPluginData(
    RUN_NAMESPACE,
    "role",
    primary ? "primary" : "secondary",
  );
  footer.appendChild(action);
  setDialogFooterActionSizing(action, label, size);
}

async function syncDrawerVariantChildren({
  component,
  side,
  variableByName,
  fonts,
  stats,
}) {
  const reusableContentSlot = extractDescendantNamed(
    component,
    "Content Slot",
    stats,
    "Drawer content slot",
  );
  removeDirectChildrenExcept(component, reusableContentSlot);
  const size = drawerVariantSize(side);
  const contentWidth = size.width - 48;

  const header = figma.createFrame();
  header.name = "Drawer Header";
  header.layoutMode = "HORIZONTAL";
  header.primaryAxisSizingMode = "AUTO";
  header.counterAxisSizingMode = "FIXED";
  header.primaryAxisAlignItems = "MIN";
  header.counterAxisAlignItems = "MIN";
  header.itemSpacing = 12;
  header.paddingLeft = 0;
  header.paddingRight = 0;
  header.paddingTop = 0;
  header.paddingBottom = 0;
  header.resizeWithoutConstraints(contentWidth, 64);
  header.fills = [];
  header.strokes = [];
  header.clipsContent = false;
  setLayoutSizingHorizontal(header, "FILL");

  const headerContent = figma.createFrame();
  headerContent.name = "Drawer Header Content";
  headerContent.layoutMode = "VERTICAL";
  headerContent.primaryAxisSizingMode = "AUTO";
  headerContent.counterAxisSizingMode = "FIXED";
  headerContent.primaryAxisAlignItems = "MIN";
  headerContent.counterAxisAlignItems = "MIN";
  headerContent.itemSpacing = 6;
  headerContent.paddingLeft = 0;
  headerContent.paddingRight = 0;
  headerContent.paddingTop = 0;
  headerContent.paddingBottom = 0;
  headerContent.resizeWithoutConstraints(contentWidth - 56, 64);
  headerContent.fills = [];
  headerContent.strokes = [];
  headerContent.clipsContent = false;
  setLayoutSizingHorizontal(headerContent, "FILL");
  try {
    headerContent.layoutGrow = 1;
  } catch (_error) {
    // layoutGrow is unavailable on older plugin runtimes.
  }

  const title = figma.createText();
  title.name = "Title Text";
  await applyDrawerTitleTypography(title, fonts, variableByName, stats);
  title.characters = drawerTitleText(side);
  title.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  title.resizeWithoutConstraints(contentWidth - 56, 24);
  setLayoutSizingHorizontal(title, "FILL");
  setTextAutoResize(title, "HEIGHT");

  const description = figma.createText();
  description.name = "Description Text";
  await applyDrawerDescriptionTypography(
    description,
    fonts,
    variableByName,
    stats,
  );
  description.characters = drawerDescriptionText(side);
  description.fills = [
    paintFromVariable(
      "Colors/foreground/400",
      "#5D626F",
      variableByName,
      stats,
    ),
  ];
  description.resizeWithoutConstraints(contentWidth - 56, 40);
  setLayoutSizingHorizontal(description, "FILL");
  setTextAutoResize(description, "HEIGHT");

  headerContent.appendChild(title);
  setLayoutSizingHorizontal(title, "FILL");
  setLayoutSizingVertical(title, "HUG");
  headerContent.appendChild(description);
  setLayoutSizingHorizontal(description, "FILL");
  setLayoutSizingVertical(description, "HUG");
  header.appendChild(headerContent);
  setLayoutSizingHorizontal(headerContent, "FILL");
  setLayoutSizingVertical(headerContent, "HUG");

  const close = await syncInlineCloseButton({
    parent: header,
    name: "Drawer Close",
    kind: "drawer-close",
    size: 44,
    iconSize: 16,
    iconSizeToken: "Drawer/close/icon/size",
    variableByName,
    stats,
  });
  setLayoutSizingHorizontal(close, "FIXED");
  setLayoutSizingVertical(close, "FIXED");

  component.appendChild(header);
  setVerticalStackChildSizing(header);

  const body = figma.createFrame();
  body.name = "Drawer Body";
  body.layoutMode = "VERTICAL";
  body.primaryAxisSizingMode = "FIXED";
  body.counterAxisSizingMode = "FIXED";
  body.primaryAxisAlignItems = "MIN";
  body.counterAxisAlignItems = "MIN";
  body.itemSpacing = 12;
  body.paddingLeft = 0;
  body.paddingRight = 0;
  body.paddingTop = 0;
  body.paddingBottom = 0;
  body.resizeWithoutConstraints(contentWidth, Math.max(120, size.height - 196));
  body.fills = [];
  body.strokes = [];
  body.clipsContent = false;
  setLayoutSizingHorizontal(body, "FILL");

  const bodyText = figma.createText();
  bodyText.name = "Body Text";
  await applyDrawerBodyTypography(bodyText, fonts, variableByName, stats);
  bodyText.characters = drawerBodyText(side);
  bodyText.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  bodyText.resizeWithoutConstraints(contentWidth, 64);
  setLayoutSizingHorizontal(bodyText, "FILL");
  setTextAutoResize(bodyText, "HEIGHT");
  body.appendChild(bodyText);
  setLayoutSizingHorizontal(bodyText, "FILL");
  setLayoutSizingVertical(bodyText, "HUG");

  const contentSlot = appendDrawerContentSlot({
    body,
    component,
    contentWidth,
    reusableSlot: reusableContentSlot,
    stats,
  });
  setVerticalStackFillChildSizing(contentSlot);

  component.appendChild(body);
  setVerticalStackFillChildSizing(body);

  const footer = figma.createFrame();
  footer.name = "Drawer Footer";
  footer.layoutMode = "HORIZONTAL";
  footer.primaryAxisSizingMode = "AUTO";
  footer.counterAxisSizingMode = "FIXED";
  footer.primaryAxisAlignItems = "MAX";
  footer.counterAxisAlignItems = "CENTER";
  footer.itemSpacing = 8;
  footer.paddingLeft = 0;
  footer.paddingRight = 0;
  footer.paddingTop = 0;
  footer.paddingBottom = 0;
  footer.resizeWithoutConstraints(contentWidth, 44);
  footer.fills = [];
  footer.strokes = [];
  footer.clipsContent = false;

  await syncDrawerFooterAction({
    footer,
    name: "Secondary Action",
    label: "Cancel",
    primary: false,
    stats,
  });
  await syncDrawerFooterAction({
    footer,
    name: "Primary Action",
    label: "Save changes",
    primary: true,
    stats,
  });
  component.appendChild(footer);
  setDrawerFooterContainerSizing(footer, variableByName, stats);

  bindDrawerGeometryVariables(
    component,
    side,
    header,
    body,
    footer,
    close,
    variableByName,
    stats,
  );
}

function appendDrawerContentSlot({
  body,
  component,
  contentWidth,
  reusableSlot,
  stats,
}) {
  if (reusableSlot) {
    configureContentSlotNode(reusableSlot, {
      height: 44,
      itemSpacing: 12,
      layoutMode: "VERTICAL",
      slotKind:
        reusableSlot.getSharedPluginData &&
        reusableSlot.getSharedPluginData(RUN_NAMESPACE, "slot-kind")
          ? reusableSlot.getSharedPluginData(RUN_NAMESPACE, "slot-kind")
          : "native",
      slotName: "Content Slot",
      width: contentWidth,
    });
    try {
      body.appendChild(reusableSlot);
      return reusableSlot;
    } catch (error) {
      if (stats && stats.warnings) {
        stats.warnings.push(
          `Drawer ${component.name}: could not reuse Content Slot (${messageFor(error)}); creating a fresh slot.`,
        );
      }
      try {
        reusableSlot.remove();
      } catch (_removeError) {
        // The reusable slot may already have been detached by Figma.
      }
    }
  }

  return appendComponentContentSlot({
    component,
    height: 44,
    ownerName: "Drawer",
    parent: body,
    slotName: "Content Slot",
    stats,
    width: contentWidth,
  });
}

function appendComponentContentSlot({
  component,
  height,
  itemSpacing = 12,
  layoutMode = "VERTICAL",
  ownerName,
  parent,
  slotName,
  stats,
  width,
}) {
  const created = createComponentContentSlot({
    component,
    height,
    itemSpacing,
    layoutMode,
    ownerName,
    slotName,
    stats,
    width,
  });

  try {
    parent.appendChild(created.node);
    return created.node;
  } catch (error) {
    if (created.nativeSlot && stats && stats.warnings) {
      stats.warnings.push(
        `${ownerName} ${component.name}: could not place native ${slotName} (${messageFor(error)}); using frame fallback.`,
      );
    }

    try {
      created.node.remove();
    } catch (_removeError) {
      // The node may already have been detached by the failed append.
    }

    const fallback = createContentSlotFrame({
      height,
      itemSpacing,
      layoutMode,
      slotName,
      width,
    });
    parent.appendChild(fallback);
    return fallback;
  }
}

function createComponentContentSlot({
  component,
  height,
  itemSpacing = 12,
  layoutMode = "VERTICAL",
  ownerName,
  slotName,
  stats,
  width,
}) {
  if (component && typeof component.createSlot === "function") {
    try {
      if (!isVariantInsideComponentSet(component)) {
        deleteComponentPropertiesByBaseName(
          component,
          [slotName],
          ["SLOT"],
          stats,
        );
      }
      const previousSlotPropertyNames = componentSlotPropertyNames(
        component,
        stats,
        ownerName,
      );
      const slot = component.createSlot();
      configureContentSlotNode(slot, {
        height,
        itemSpacing,
        layoutMode,
        slotKind: "native",
        slotName,
        width,
      });
      renameContentSlotProperty(
        component,
        previousSlotPropertyNames,
        slotName,
        stats,
        ownerName,
      );
      return { node: slot, nativeSlot: true };
    } catch (error) {
      if (stats && stats.warnings) {
        stats.warnings.push(
          `${ownerName} ${component.name}: could not create native ${slotName} (${messageFor(error)}); using frame fallback.`,
        );
      }
    }
  }

  return {
    node: createContentSlotFrame({
      height,
      itemSpacing,
      layoutMode,
      slotName,
      width,
    }),
    nativeSlot: false,
  };
}

function isVariantInsideComponentSet(component) {
  return (
    component &&
    component.type === "COMPONENT" &&
    component.parent &&
    component.parent.type === "COMPONENT_SET"
  );
}

function componentSlotPropertyNames(component, stats, ownerName) {
  const read = safeComponentPropertyDefinitions(
    component,
    stats,
    `read ${ownerName} slot properties`,
  );
  if (read.error) return [];

  const names = [];
  for (const propertyName of Object.keys(read.definitions)) {
    if (read.definitions[propertyName].type === "SLOT") {
      names.push(propertyName);
    }
  }
  return names;
}

function renameContentSlotProperty(
  component,
  previousPropertyNames,
  slotName,
  stats,
  ownerName,
) {
  const read = safeComponentPropertyDefinitions(
    component,
    stats,
    `rename ${ownerName} content slot property`,
  );
  if (read.error) return;

  const nextSlotPropertyName = Object.keys(read.definitions).find(
    (propertyName) =>
      read.definitions[propertyName].type === "SLOT" &&
      previousPropertyNames.indexOf(propertyName) === -1,
  );
  if (!nextSlotPropertyName) return;
  if (nextSlotPropertyName.split("#")[0] === slotName) return;
  if (!component.editComponentProperty) return;

  try {
    component.editComponentProperty(nextSlotPropertyName, {
      name: slotName,
    });
  } catch (error) {
    if (stats && stats.warnings) {
      stats.warnings.push(
        `Could not rename ${ownerName} content slot property (${messageFor(error)}).`,
      );
    }
  }
}

function configureSharedSlotProperties(componentSet, slotNames, stats) {
  if (!componentSet || !Array.isArray(slotNames) || slotNames.length === 0) {
    return;
  }

  for (const slotName of slotNames) {
    const propertyName = ensureSlotProperty(componentSet, slotName, stats);
    if (!propertyName) continue;

    const bindings = bindSlotPropertyToNodesNamed(
      componentSet,
      slotName,
      propertyName,
      stats,
    );
    if (bindings.failed === 0) {
      deleteDuplicateComponentPropertiesByBaseName(
        componentSet,
        slotName,
        ["SLOT"],
        propertyName,
        stats,
      );
    }
  }
}

function bindSlotPropertyToNodesNamed(
  componentSet,
  slotName,
  propertyName,
  stats,
) {
  const read = safeComponentPropertyDefinitions(
    componentSet,
    stats,
    `bind ${slotName} slot property`,
  );
  if (read.error) return { attempted: 0, bound: 0, failed: 0 };
  const bindings = { attempted: 0, bound: 0, failed: 0 };

  function walk(node) {
    if (!node) return;
    if (node.name === slotName && isSlotReferenceCandidate(node)) {
      bindings.attempted += 1;
      if (
        bindSlotProperty(
          node,
          componentSet,
          propertyName,
          read.definitions,
          stats,
        )
      ) {
        bindings.bound += 1;
      } else {
        bindings.failed += 1;
      }
    }

    if (!node.children) return;
    for (const child of node.children) walk(child);
  }

  walk(componentSet);
  return bindings;
}

function isSlotReferenceCandidate(node) {
  return node && (node.type === "FRAME" || node.type === "SLOT");
}

function bindSlotProperty(
  node,
  componentSet,
  propertyName,
  propertyDefinitions,
  stats,
) {
  if (!node || !propertyName) return;

  const referenceKey = slotPropertyReferenceKeyForNode(
    node,
    propertyDefinitions,
  );
  const references = {};
  const existing = node.componentPropertyReferences || {};

  for (const key of Object.keys(existing)) {
    references[key] = existing[key];
  }
  references[referenceKey] = propertyName;

  try {
    node.componentPropertyReferences = references;
    if (node.setSharedPluginData) {
      node.setSharedPluginData(RUN_NAMESPACE, "slot-property", propertyName);
      node.setSharedPluginData(
        RUN_NAMESPACE,
        "slot-property-owner",
        componentSet.name,
      );
    }
    return true;
  } catch (error) {
    if (stats && stats.warnings) {
      stats.warnings.push(
        `${componentSet.name} ${node.name}: could not bind shared slot property (${messageFor(error)}).`,
      );
    }
    return false;
  }
}

function slotPropertyReferenceKeyForNode(node, propertyDefinitions) {
  const references = node.componentPropertyReferences || {};

  for (const key of Object.keys(references)) {
    const definition = propertyDefinitions[references[key]];
    if (definition && definition.type === "SLOT") return key;
  }

  return "slot";
}

function createContentSlotFrame({
  height,
  itemSpacing = 12,
  layoutMode = "VERTICAL",
  slotName,
  width,
}) {
  const frame = figma.createFrame();
  configureContentSlotNode(frame, {
    height,
    itemSpacing,
    layoutMode,
    slotKind: "frame-fallback",
    slotName,
    width,
  });
  return frame;
}

function configureContentSlotNode(
  node,
  {
    height,
    itemSpacing = 12,
    layoutMode = "VERTICAL",
    slotKind,
    slotName,
    width,
  },
) {
  node.name = slotName;
  setNodePropertyIfSupported(node, "layoutMode", layoutMode);
  setNodePropertyIfSupported(node, "primaryAxisSizingMode", "FIXED");
  setNodePropertyIfSupported(node, "counterAxisSizingMode", "FIXED");
  setNodePropertyIfSupported(node, "primaryAxisAlignItems", "MIN");
  setNodePropertyIfSupported(node, "counterAxisAlignItems", "MIN");
  setNodePropertyIfSupported(node, "itemSpacing", itemSpacing);
  setNodePropertyIfSupported(node, "paddingLeft", 0);
  setNodePropertyIfSupported(node, "paddingRight", 0);
  setNodePropertyIfSupported(node, "paddingTop", 0);
  setNodePropertyIfSupported(node, "paddingBottom", 0);
  resizeNodeWithoutConstraints(node, width, height);
  setNodePropertyIfSupported(node, "fills", []);
  setNodePropertyIfSupported(node, "strokes", []);
  setNodePropertyIfSupported(node, "clipsContent", false);

  if (node.setSharedPluginData) {
    node.setSharedPluginData(RUN_NAMESPACE, "kind", "content-slot");
    node.setSharedPluginData(RUN_NAMESPACE, "slot-kind", slotKind);
  }
}

async function syncDialogInput({ body, name, label, placeholder, stats }) {
  let input = directChildNamed(body, name);
  if (input && !isNestedComponentInstance(input, "Input")) {
    input.remove();
    input = null;
  }

  let inputSet = null;
  if (!input) {
    const created = await createNestedComponentInstance({
      componentSetName: "Input",
      variantProperties: {
        State: "Default",
        Status: "Default",
      },
      name,
      stats,
    });
    if (!created) {
      input = createMissingNestedComponentNode(
        name,
        "Build Input before updating Dialog.",
        stats,
      );
    } else {
      input = created.instance;
      inputSet = created.componentSet;
    }
  } else {
    inputSet = await findLocalComponentSetByName("Input");
  }

  if (input.type === "INSTANCE") {
    markNestedComponentInstance(input, "Input");
  }

  applyCompositeInputInstanceProperties({
    input,
    inputSet,
    label,
    name,
    ownerName: "Dialog",
    placeholder,
    stats,
  });

  input = await ensureCompositeFormInputChildSizing({
    input,
    label,
    missingMessage: "Build Input before updating Dialog.",
    name,
    ownerName: "Dialog",
    parent: body,
    placeholder,
    stats,
  });
}

async function syncDialogFooterAction({ footer, name, label, primary, stats }) {
  const size = primary ? "Large" : "Default";
  let action = directChildNamed(footer, name);
  if (action && !isNestedComponentInstance(action, "Button")) {
    action.remove();
    action = null;
  }

  let buttonSet = null;
  if (!action) {
    const created = await createNestedComponentInstance({
      componentSetName: "Button",
      variantProperties: {
        Variant: primary ? "Default" : "Outline",
        Size: size,
        State: "Default",
      },
      name,
      stats,
    });
    if (!created) {
      action = createMissingNestedComponentNode(
        name,
        "Build Button before updating Dialog.",
        stats,
      );
    } else {
      action = created.instance;
      buttonSet = created.componentSet;
    }
  } else {
    buttonSet = await findLocalComponentSetByName("Button");
  }

  if (action.type === "INSTANCE") {
    markNestedComponentInstance(
      action,
      "Button",
      primary ? "primary" : "secondary",
    );
  }

  if (action.type === "INSTANCE" && buttonSet) {
    try {
      action.setProperties({
        Variant: primary ? "Default" : "Outline",
        Size: size,
        State: "Default",
      });
    } catch (error) {
      stats.warnings.push(
        `Dialog ${name}: could not set Button variant properties (${messageFor(error)}).`,
      );
    }
    setInstanceTextProperty(action, buttonSet, "Label Text", label, stats);
  }

  action.setSharedPluginData(
    RUN_NAMESPACE,
    "role",
    primary ? "primary" : "secondary",
  );
  footer.appendChild(action);
  setDialogFooterActionSizing(action, label, size);
}

async function syncDrawerFooterAction({ footer, name, label, primary, stats }) {
  const size = "Default";
  let action = directChildNamed(footer, name);
  if (action && !isNestedComponentInstance(action, "Button")) {
    action.remove();
    action = null;
  }

  let buttonSet = null;
  if (!action) {
    const created = await createNestedComponentInstance({
      componentSetName: "Button",
      variantProperties: {
        Variant: primary ? "Default" : "Outline",
        Size: size,
        State: "Default",
      },
      name,
      stats,
    });
    if (!created) {
      action = createMissingNestedComponentNode(
        name,
        "Build Button before updating Drawer.",
        stats,
      );
    } else {
      action = created.instance;
      buttonSet = created.componentSet;
    }
  } else {
    buttonSet = await findLocalComponentSetByName("Button");
  }

  if (action.type === "INSTANCE") {
    markNestedComponentInstance(
      action,
      "Button",
      primary ? "primary" : "secondary",
    );
  }

  if (action.type === "INSTANCE" && buttonSet) {
    try {
      action.setProperties({
        Variant: primary ? "Default" : "Outline",
        Size: size,
        State: "Default",
      });
    } catch (error) {
      stats.warnings.push(
        `Drawer ${name}: could not set Button variant properties (${messageFor(error)}).`,
      );
    }
    setInstanceTextProperty(action, buttonSet, "Label Text", label, stats);
  }

  action.setSharedPluginData(
    RUN_NAMESPACE,
    "role",
    primary ? "primary" : "secondary",
  );
  footer.appendChild(action);
  setDialogFooterActionSizing(action, label, size);
}

function setDrawerFooterContainerSizing(footer, variableByName, stats) {
  resizeNodeWithoutConstraints(footer, footer.width || 336, 44);
  setLayoutSizingHorizontal(footer, "FILL");
  setLayoutSizingVertical(footer, "FIXED");
  bindFloatVariable(
    footer,
    "height",
    "Drawer/footer/height",
    variableByName,
    stats,
  );

  try {
    footer.layoutAlign = "STRETCH";
  } catch (_error) {
    // layoutAlign is unavailable on older plugin runtimes.
  }

  try {
    footer.layoutGrow = 0;
  } catch (_error) {
    // layoutGrow is unavailable on older plugin runtimes.
  }
}

function setBottomSheetFooterContainerSizing(footer, variableByName, stats) {
  resizeNodeWithoutConstraints(footer, footer.width || 320, 44);
  setLayoutSizingHorizontal(footer, "FILL");
  setLayoutSizingVertical(footer, "FIXED");
  bindFloatVariable(
    footer,
    "height",
    "BottomSheet/footer/height",
    variableByName,
    stats,
  );
  bindFloatVariable(
    footer,
    "itemSpacing",
    "BottomSheet/footer/gap",
    variableByName,
    stats,
  );

  try {
    footer.layoutAlign = "STRETCH";
  } catch (_error) {
    // layoutAlign is unavailable on older plugin runtimes.
  }

  try {
    footer.layoutGrow = 0;
  } catch (_error) {
    // layoutGrow is unavailable on older plugin runtimes.
  }
}

function setDialogFooterContainerSizing(footer, variableByName, stats) {
  resizeNodeWithoutConstraints(footer, footer.width || 464, 44);
  setLayoutSizingHorizontal(footer, "FILL");
  setLayoutSizingVertical(footer, "FIXED");
  bindFloatVariable(
    footer,
    "height",
    "Dialog/footer/height",
    variableByName,
    stats,
  );

  try {
    footer.layoutAlign = "STRETCH";
  } catch (_error) {
    // layoutAlign is unavailable on older plugin runtimes.
  }

  try {
    footer.layoutGrow = 0;
  } catch (_error) {
    // layoutGrow is unavailable on older plugin runtimes.
  }
}

function setDialogFooterActionSizing(action, label, size) {
  if (!action) return;

  const metrics = buttonMetrics(size);
  const width = expectedDialogFooterActionWidth(label, size);
  const height = metrics.height;

  setLayoutSizingHorizontal(action, "FIXED");
  setLayoutSizingVertical(action, "FIXED");
  resizeNodeWithoutConstraints(action, width, height);

  try {
    action.layoutAlign = "CENTER";
  } catch (_error) {
    // layoutAlign is unavailable on older plugin runtimes.
  }

  try {
    action.layoutGrow = 0;
  } catch (_error) {
    // layoutGrow is unavailable on older plugin runtimes.
  }
}

function expectedDialogFooterActionWidth(label, size) {
  const metrics = buttonMetrics(size);
  const text = String(label || "");
  const estimatedLabelWidth = Math.ceil(text.length * 7.5);
  return Math.max(metrics.width, estimatedLabelWidth + metrics.paddingX * 2);
}

async function syncPopoverVariantChildren({
  component,
  side,
  variableByName,
  fonts,
  stats,
}) {
  let content = directChildNamed(component, "Popover Content");
  if (content && content.type !== "FRAME") {
    content.remove();
    content = null;
  }
  if (!content || content.type !== "FRAME") {
    content = figma.createFrame();
    content.name = "Popover Content";
  }
  content.layoutMode = "VERTICAL";
  content.primaryAxisSizingMode = "AUTO";
  content.counterAxisSizingMode = "FIXED";
  content.primaryAxisAlignItems = "MIN";
  content.counterAxisAlignItems = "MIN";
  content.itemSpacing = 8;
  content.paddingLeft = 0;
  content.paddingRight = 0;
  content.paddingTop = 0;
  content.paddingBottom = 0;
  content.resizeWithoutConstraints(256, 68);
  content.fills = [];
  content.strokes = [];
  content.clipsContent = false;
  setLayoutSizingHorizontal(content, "FILL");

  let title = directChildNamed(content, "Title Text");
  if (title && title.type !== "TEXT") {
    title.remove();
    title = null;
  }
  if (!title || title.type !== "TEXT") {
    title = figma.createText();
    title.name = "Title Text";
  }
  await applyPopoverTitleTypography(title, fonts, variableByName, stats);
  title.characters = popoverTitleText(side);
  title.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  title.resizeWithoutConstraints(256, 20);
  setLayoutSizingHorizontal(title, "FILL");
  setTextAutoResize(title, "HEIGHT");

  let description = directChildNamed(content, "Description Text");
  if (description && description.type !== "TEXT") {
    description.remove();
    description = null;
  }
  if (!description || description.type !== "TEXT") {
    description = figma.createText();
    description.name = "Description Text";
  }
  await applyPopoverDescriptionTypography(
    description,
    fonts,
    variableByName,
    stats,
  );
  description.characters = popoverDescriptionText(side);
  description.fills = [
    paintFromVariable(
      "Colors/foreground/400",
      "#5D626F",
      variableByName,
      stats,
    ),
  ];
  description.resizeWithoutConstraints(256, 40);
  setLayoutSizingHorizontal(description, "FILL");
  setTextAutoResize(description, "HEIGHT");

  content.appendChild(title);
  content.appendChild(description);
  component.appendChild(content);
  bindPopoverGeometryVariables(component, content, variableByName, stats);
}

async function syncMenuVariantChildren({
  component,
  value,
  variableByName,
  fonts,
  stats,
}) {
  removeDirectChildren(component);
  const rows = [];

  rows.push(
    await syncMenuItemRow({
      component,
      name: "Menu Label",
      textName: "Label Text",
      text: menuLabelText(value),
      strong: true,
      variableByName,
      fonts,
      stats,
    }),
  );
  rows.push(syncMenuSeparator({ component, variableByName, stats }));

  if (value === "Checkbox") {
    rows.push(
      await syncMenuItemRow({
        component,
        name: "Menu Item 1",
        textName: "Item 1 Text",
        text: "Show traffic",
        icon: "check",
        variableByName,
        fonts,
        stats,
      }),
    );
    rows.push(
      await syncMenuItemRow({
        component,
        name: "Menu Item 2",
        textName: "Item 2 Text",
        text: "Accessible route",
        variableByName,
        fonts,
        stats,
      }),
    );
    rows.push(
      await syncMenuItemRow({
        component,
        name: "Menu Item 3",
        textName: "Item 3 Text",
        text: "Indoor mode",
        icon: "check",
        variableByName,
        fonts,
        stats,
      }),
    );
  } else if (value === "Radio") {
    rows.push(
      await syncMenuItemRow({
        component,
        name: "Menu Item 1",
        textName: "Item 1 Text",
        text: "Fastest",
        radio: true,
        variableByName,
        fonts,
        stats,
      }),
    );
    rows.push(
      await syncMenuItemRow({
        component,
        name: "Menu Item 2",
        textName: "Item 2 Text",
        text: "Accessible",
        variableByName,
        fonts,
        stats,
      }),
    );
    rows.push(
      await syncMenuItemRow({
        component,
        name: "Menu Item 3",
        textName: "Item 3 Text",
        text: "Fewest transfers",
        variableByName,
        fonts,
        stats,
      }),
    );
  } else if (value === "Submenu") {
    rows.push(
      await syncMenuItemRow({
        component,
        name: "Menu Item 1",
        textName: "Item 1 Text",
        text: "Profile",
        variableByName,
        fonts,
        stats,
      }),
    );
    rows.push(
      await syncMenuItemRow({
        component,
        name: "Menu Item 2",
        textName: "Item 2 Text",
        text: "Billing",
        variableByName,
        fonts,
        stats,
      }),
    );
    rows.push(
      await syncMenuItemRow({
        component,
        name: "Menu Item 3",
        textName: "Item 3 Text",
        text: "More",
        submenu: true,
        variableByName,
        fonts,
        stats,
      }),
    );
  } else {
    rows.push(
      await syncMenuItemRow({
        component,
        name: "Menu Item 1",
        textName: "Item 1 Text",
        text: "Profile",
        shortcut: "Cmd K",
        variableByName,
        fonts,
        stats,
      }),
    );
    rows.push(
      await syncMenuItemRow({
        component,
        name: "Menu Item 2",
        textName: "Item 2 Text",
        text: "Billing",
        variableByName,
        fonts,
        stats,
      }),
    );
    rows.push(
      await syncMenuItemRow({
        component,
        name: "Menu Item 3",
        textName: "Item 3 Text",
        text: "Team",
        variableByName,
        fonts,
        stats,
      }),
    );
  }

  bindMenuGeometryVariables(
    component,
    rows.filter(Boolean),
    variableByName,
    stats,
  );
}

async function syncMenuItemRow({
  component,
  name,
  textName,
  text,
  strong,
  icon,
  radio,
  submenu,
  shortcut,
  variableByName,
  fonts,
  stats,
}) {
  const row = figma.createFrame();
  row.name = name;
  row.layoutMode = "HORIZONTAL";
  row.primaryAxisSizingMode = "FIXED";
  row.counterAxisSizingMode = "FIXED";
  row.primaryAxisAlignItems = "MIN";
  row.counterAxisAlignItems = "CENTER";
  row.itemSpacing = 8;
  row.paddingLeft = 8;
  row.paddingRight = 8;
  row.paddingTop = 0;
  row.paddingBottom = 0;
  row.resizeWithoutConstraints(184, 32);
  row.cornerRadius = 4;
  row.fills = [];
  row.strokes = [];
  row.clipsContent = false;
  row.setSharedPluginData(RUN_NAMESPACE, "kind", "menu-item");
  setLayoutSizingHorizontal(row, "FILL");

  if (icon) {
    const iconNode = await createFixedIconInstance(
      icon,
      "Colors/foreground/0",
      "#000000",
      variableByName,
      stats,
      16,
      "Menu/icon/size",
    );
    iconNode.name = "Item Icon";
    row.appendChild(iconNode);
  } else if (radio) {
    const dot = figma.createEllipse();
    dot.name = "Radio Dot";
    dot.resizeWithoutConstraints(8, 8);
    dot.fills = [
      paintFromVariable("Colors/theme/500", "#135BEC", variableByName, stats),
    ];
    dot.strokes = [];
    row.appendChild(dot);
  }

  const label = figma.createText();
  label.name = textName;
  await applyMenuItemTypography(label, fonts, variableByName, stats, strong);
  label.characters = text;
  label.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  try {
    label.layoutGrow = 1;
  } catch (_error) {
    // layoutGrow is unavailable on older plugin runtimes.
  }
  row.appendChild(label);

  if (shortcut) {
    const shortcutText = figma.createText();
    shortcutText.name = "Shortcut Text";
    await applyMenuShortcutTypography(
      shortcutText,
      fonts,
      variableByName,
      stats,
    );
    shortcutText.characters = shortcut;
    shortcutText.fills = [
      paintFromVariable(
        "Colors/foreground/400",
        "#5D626F",
        variableByName,
        stats,
      ),
    ];
    row.appendChild(shortcutText);
  }

  if (submenu) {
    const iconNode = await createFixedIconInstance(
      "chevron-right",
      "Colors/foreground/400",
      "#5D626F",
      variableByName,
      stats,
      16,
      "Menu/icon/size",
    );
    iconNode.name = "Submenu Icon";
    row.appendChild(iconNode);
  }

  component.appendChild(row);
  return row;
}

function syncMenuSeparator({ component, variableByName, stats }) {
  const separator = figma.createRectangle();
  separator.name = "Menu Separator";
  separator.resizeWithoutConstraints(184, 1);
  separator.fills = [
    paintFromVariable(
      "Colors/foreground/500",
      "#747B8B",
      variableByName,
      stats,
    ),
  ];
  separator.strokes = [];
  separator.setSharedPluginData(RUN_NAMESPACE, "kind", "menu-separator");
  setLayoutSizingHorizontal(separator, "FILL");
  bindFloatVariable(
    separator,
    "height",
    "Menu/separator/height",
    variableByName,
    stats,
  );
  component.appendChild(separator);
  return separator;
}

async function syncToastVariantChildren({
  component,
  value,
  variableByName,
  fonts,
  stats,
}) {
  let textGroup = directChildNamed(component, "Toast Content");
  if (textGroup && textGroup.type !== "FRAME") {
    textGroup.remove();
    textGroup = null;
  }
  if (!textGroup || textGroup.type !== "FRAME") {
    textGroup = figma.createFrame();
    textGroup.name = "Toast Content";
  }
  textGroup.layoutMode = "VERTICAL";
  textGroup.primaryAxisSizingMode = "AUTO";
  textGroup.counterAxisSizingMode = "FIXED";
  textGroup.primaryAxisAlignItems = "MIN";
  textGroup.counterAxisAlignItems = "MIN";
  textGroup.itemSpacing = 4;
  textGroup.paddingLeft = 0;
  textGroup.paddingRight = 0;
  textGroup.paddingTop = 0;
  textGroup.paddingBottom = 0;
  textGroup.resizeWithoutConstraints(value === "Action" ? 240 : 300, 44);
  textGroup.fills = [];
  textGroup.strokes = [];
  textGroup.clipsContent = false;
  setLayoutSizingHorizontal(textGroup, "FILL");
  try {
    textGroup.layoutGrow = 1;
  } catch (_error) {
    // layoutGrow is unavailable on older plugin runtimes.
  }

  let title = directChildNamed(textGroup, "Title Text");
  if (title && title.type !== "TEXT") {
    title.remove();
    title = null;
  }
  if (!title || title.type !== "TEXT") {
    title = figma.createText();
    title.name = "Title Text";
  }
  await applyToastTitleTypography(title, fonts, variableByName, stats);
  title.characters = "Scheduled: Catch up";
  title.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  title.resizeWithoutConstraints(260, 20);
  setLayoutSizingHorizontal(title, "FILL");
  setTextAutoResize(title, "HEIGHT");

  let description = directChildNamed(textGroup, "Description Text");
  if (description && description.type !== "TEXT") {
    description.remove();
    description = null;
  }
  if (!description || description.type !== "TEXT") {
    description = figma.createText();
    description.name = "Description Text";
  }
  await applyToastDescriptionTypography(
    description,
    fonts,
    variableByName,
    stats,
  );
  description.characters = "Friday, February 10, 2023 at 5:57 PM";
  description.fills = [
    paintFromVariable(
      "Colors/foreground/400",
      "#5D626F",
      variableByName,
      stats,
    ),
  ];
  description.resizeWithoutConstraints(260, 20);
  setLayoutSizingHorizontal(description, "FILL");
  setTextAutoResize(description, "HEIGHT");

  textGroup.appendChild(title);
  textGroup.appendChild(description);
  component.appendChild(textGroup);

  let action = directChildNamed(component, "Toast Action");
  if (action && action.type !== "FRAME") {
    action.remove();
    action = null;
  }

  if (value === "Action") {
    if (!action || action.type !== "FRAME") {
      action = figma.createFrame();
      action.name = "Toast Action";
    }
    action.layoutMode = "HORIZONTAL";
    action.primaryAxisSizingMode = "AUTO";
    action.counterAxisSizingMode = "FIXED";
    action.primaryAxisAlignItems = "CENTER";
    action.counterAxisAlignItems = "CENTER";
    action.itemSpacing = 0;
    action.paddingLeft = 12;
    action.paddingRight = 12;
    action.paddingTop = 0;
    action.paddingBottom = 0;
    action.resizeWithoutConstraints(66, 32);
    action.cornerRadius = 8;
    action.fills = [];
    action.strokes = [
      paintFromVariable(
        "Colors/foreground/500",
        "#747B8B",
        variableByName,
        stats,
      ),
    ];
    action.strokeWeight = 1;
    action.clipsContent = false;
    action.setSharedPluginData(RUN_NAMESPACE, "kind", "toast-action");

    let actionText = directChildNamed(action, "Action Text");
    if (actionText && actionText.type !== "TEXT") {
      actionText.remove();
      actionText = null;
    }
    if (!actionText || actionText.type !== "TEXT") {
      actionText = figma.createText();
      actionText.name = "Action Text";
    }
    actionText.fontName = fonts.medium;
    actionText.fontSize = 14;
    actionText.lineHeight = { unit: "PIXELS", value: 20 };
    actionText.textAutoResize = "WIDTH_AND_HEIGHT";
    await applyTextStyleToNodeAsync(actionText, "toastTitle", stats);
    bindFloatVariable(
      actionText,
      "fontSize",
      "Toast/title/font-size",
      variableByName,
      stats,
    );
    bindFloatVariable(
      actionText,
      "lineHeight",
      "Toast/title/line-height",
      variableByName,
      stats,
    );
    actionText.characters = "Undo";
    actionText.fills = [
      paintFromVariable(
        "Colors/foreground/0",
        "#000000",
        variableByName,
        stats,
      ),
    ];
    action.appendChild(actionText);
    component.appendChild(action);
  } else if (action) {
    action.remove();
    action = null;
  }

  const close = await syncInlineCloseButton({
    parent: component,
    name: "Toast Close",
    kind: "toast-close",
    size: 24,
    iconSize: 16,
    iconSizeToken: "Toast/close/icon/size",
    variableByName,
    stats,
  });

  bindToastGeometryVariables(
    component,
    textGroup,
    action,
    close,
    variableByName,
    stats,
  );
}

async function syncOverlayCloseButton({
  parent,
  name,
  kind,
  size,
  iconSize,
  iconSizeToken,
  x,
  y,
  variableByName,
  stats,
}) {
  let close = directChildNamed(parent, name);
  if (close && close.type !== "FRAME") {
    close.remove();
    close = null;
  }
  if (!close || close.type !== "FRAME") {
    close = figma.createFrame();
    close.name = name;
  }
  close.layoutMode = "NONE";
  close.resizeWithoutConstraints(size, size);
  close.x = x;
  close.y = y;
  close.cornerRadius = Math.min(8, size / 2);
  close.fills = [];
  close.strokes = [];
  close.clipsContent = false;
  close.setSharedPluginData(RUN_NAMESPACE, "kind", kind);
  parent.appendChild(close);

  try {
    close.layoutPositioning = "ABSOLUTE";
  } catch (_error) {
    // Older Figma runtimes may not expose absolute positioning on every node.
  }

  removeDirectChildren(close);
  const icon = await createFixedIconInstance(
    "x-close",
    "Colors/foreground/400",
    "#5D626F",
    variableByName,
    stats,
    iconSize,
    iconSizeToken,
  );
  icon.name = "Close Icon";
  icon.x = (size - iconSize) / 2;
  icon.y = (size - iconSize) / 2;
  close.appendChild(icon);
  return close;
}

async function syncInlineCloseButton({
  parent,
  name,
  kind,
  size,
  iconSize,
  iconSizeToken,
  colorToken,
  colorFallback,
  variableByName,
  stats,
}) {
  let close = directChildNamed(parent, name);
  if (close && close.type !== "FRAME") {
    close.remove();
    close = null;
  }
  if (!close || close.type !== "FRAME") {
    close = figma.createFrame();
    close.name = name;
  }
  close.layoutMode = "NONE";
  close.resizeWithoutConstraints(size, size);
  close.cornerRadius = Math.min(8, size / 2);
  close.fills = [];
  close.strokes = [];
  close.clipsContent = false;
  close.setSharedPluginData(RUN_NAMESPACE, "kind", kind);

  try {
    close.layoutPositioning = "AUTO";
  } catch (_error) {
    // Older Figma runtimes may not expose absolute positioning on every node.
  }

  parent.appendChild(close);

  removeDirectChildren(close);
  const icon = await createFixedIconInstance(
    "x-close",
    colorToken || "Colors/foreground/400",
    colorFallback || "#5D626F",
    variableByName,
    stats,
    iconSize,
    iconSizeToken,
  );
  icon.name = "Close Icon";
  icon.x = (size - iconSize) / 2;
  icon.y = (size - iconSize) / 2;
  close.appendChild(icon);
  return close;
}

function removeDirectChildren(parent) {
  if (!parent || !parent.children) return;
  const children = [];
  for (const child of parent.children) children.push(child);
  for (const child of children) child.remove();
}

function removeDirectChildrenExcept(parent, keptChild) {
  if (!parent || !parent.children) return;
  const children = [];
  for (const child of parent.children) {
    if (keptChild && child.id === keptChild.id) continue;
    children.push(child);
  }
  for (const child of children) child.remove();
}

function removeDirectChildrenExceptMany(parent, keptChildren) {
  if (!parent || !parent.children) return;
  const keptIds = new Set();
  for (const child of keptChildren || []) {
    if (child && child.id) keptIds.add(child.id);
  }

  const children = [];
  for (const child of parent.children) {
    if (keptIds.has(child.id)) continue;
    children.push(child);
  }
  for (const child of children) child.remove();
}

function removeDirectChildrenNamed(parent, names) {
  if (!parent || !parent.children) return;
  const nameSet = new Set(names);
  const children = [];
  for (const child of parent.children) {
    if (nameSet.has(child.name)) children.push(child);
  }
  for (const child of children) child.remove();
}

function removeDirectChildrenMatching(parent, predicate) {
  if (!parent || !parent.children) return;
  const children = [];
  for (const child of parent.children) {
    if (predicate(child)) children.push(child);
  }
  for (const child of children) child.remove();
}

function findDescendantNamed(parent, name) {
  if (!parent || !parent.children) return null;

  for (const child of parent.children) {
    if (child.name === name) return child;
    const nested = findDescendantNamed(child, name);
    if (nested) return nested;
  }

  return null;
}

function extractDescendantNamed(parent, name, stats, context) {
  const node = findDescendantNamed(parent, name);
  if (!node) return null;

  try {
    parent.appendChild(node);
    return node;
  } catch (error) {
    if (stats && stats.warnings) {
      stats.warnings.push(
        `Could not preserve ${context || name} (${messageFor(error)}).`,
      );
    }
    return null;
  }
}

function extractReusableSlotsByName(parent, slotNames, stats, ownerName) {
  const slots = {};

  for (const slotName of slotNames || []) {
    const node = extractDescendantNamed(
      parent,
      slotName,
      stats,
      `${ownerName} ${slotName}`,
    );
    if (!node) continue;

    if (!isSlotReferenceCandidate(node)) {
      if (stats && stats.warnings) {
        stats.warnings.push(
          `${ownerName} ${slotName}: preserved node is not a slot-compatible frame.`,
        );
      }
      continue;
    }

    slots[slotName] = node;
  }

  return slots;
}

function reusableSlotNamed(reusableSlots, slotName) {
  if (!reusableSlots) return null;
  return reusableSlots[slotName] || null;
}

function shouldRefreshGeneratedShellSlotDefaults(ownerName, slotName, slot) {
  if (!slot || !slot.children || slot.children.length === 0) return false;

  if (ownerName === "Sidebar" && slotName === "Navigation Slot") {
    return slot.children.every(isGeneratedSidebarNavigationSlotChild);
  }

  if (ownerName === "Navbar" && slotName === "Navigation Slot") {
    return slot.children.every(isGeneratedNavbarNavigationSlotChild);
  }

  return false;
}

function isGeneratedSidebarNavigationSlotChild(node) {
  if (!node) return false;
  if (isGeneratedNavigationItemShellInstance(node)) return true;
  if (node.type === "TEXT" && node.name === "Section Text") return true;
  return (
    generatedKind(node) === "missing-nested-component" &&
    /^(Item \d+ Text(?: Rail)? Row|Tool Text Row|Settings Text Row)$/.test(
      node.name || "",
    )
  );
}

function isGeneratedNavbarNavigationSlotChild(node) {
  if (!node) return false;
  if (isGeneratedNavigationItemShellInstance(node)) return true;
  return (
    generatedKind(node) === "missing-nested-component" &&
    /^Item \d+ Text Frame$/.test(node.name || "")
  );
}

function isGeneratedNavigationItemShellInstance(node) {
  return (
    isGeneratedNestedComponentInstance(node) &&
    nodeSourceComponentSet(node) === "NavigationItem"
  );
}

function generatedKind(node) {
  if (!node || !node.getSharedPluginData) return "";
  try {
    return node.getSharedPluginData(RUN_NAMESPACE, "kind") || "";
  } catch (_error) {
    return "";
  }
}

function createOrReuseShellSlot({
  component,
  height,
  itemSpacing = 12,
  layoutMode = "VERTICAL",
  ownerName,
  reusableSlot,
  slotName,
  stats,
  width,
}) {
  const hasReusableContent =
    reusableSlot && reusableSlot.children && reusableSlot.children.length > 0;
  const refreshGeneratedDefaults = shouldRefreshGeneratedShellSlotDefaults(
    ownerName,
    slotName,
    reusableSlot,
  );
  if (refreshGeneratedDefaults) {
    removeDirectChildren(reusableSlot);
    incrementStat(stats, "generatedShellSlotDefaultsRefreshed");
  }
  const reusedContent = hasReusableContent && !refreshGeneratedDefaults;

  if (reusableSlot && reusableSlot.type === "SLOT") {
    configureContentSlotNode(reusableSlot, {
      height,
      itemSpacing,
      layoutMode,
      slotKind:
        reusableSlot.getSharedPluginData &&
        reusableSlot.getSharedPluginData(RUN_NAMESPACE, "slot-kind")
          ? reusableSlot.getSharedPluginData(RUN_NAMESPACE, "slot-kind")
          : "native",
      slotName,
      width,
    });
    return { node: reusableSlot, reusedContent };
  }

  const created = createComponentContentSlot({
    component,
    height,
    itemSpacing,
    layoutMode,
    ownerName,
    slotName,
    stats,
    width,
  });

  if (reusableSlot) {
    const moved = moveDirectChildrenIntoSlot(
      reusableSlot,
      created.node,
      stats,
      `${ownerName} ${slotName}`,
    );
    if (!moved) {
      try {
        created.node.remove();
      } catch (_removeError) {
        // The freshly created slot may already have been detached by Figma.
      }
      configureContentSlotNode(reusableSlot, {
        height,
        itemSpacing,
        layoutMode,
        slotKind:
          reusableSlot.getSharedPluginData &&
          reusableSlot.getSharedPluginData(RUN_NAMESPACE, "slot-kind")
            ? reusableSlot.getSharedPluginData(RUN_NAMESPACE, "slot-kind")
            : "frame-fallback",
        slotName,
        width,
      });
      return { node: reusableSlot, reusedContent };
    }

    try {
      reusableSlot.remove();
    } catch (_removeError) {
      // The reusable frame may already be detached after moving its children.
    }
  }

  return { node: created.node, reusedContent };
}

function moveDirectChildrenIntoSlot(source, target, stats, context) {
  if (!source || !source.children || !target || !target.appendChild) {
    return true;
  }

  const children = [];
  for (const child of source.children) children.push(child);

  for (const child of children) {
    try {
      target.appendChild(child);
    } catch (error) {
      if (stats && stats.warnings) {
        stats.warnings.push(
          `Could not migrate ${context} content into native slot (${messageFor(error)}).`,
        );
      }
      return false;
    }
  }

  return true;
}

function dialogTitleText(value) {
  if (value === "Form") return "Edit profile";
  if (value === "Footer") return "Confirm changes";
  return "Dialog title";
}

function dialogDescriptionText(value) {
  if (value === "Form") return "Make changes to your profile here.";
  if (value === "Footer") return "Review this decision before continuing.";
  return "Use dialogs for focused tasks that need a clear return path.";
}

function dialogBodyText(value) {
  if (value === "Form")
    return "Update the fields below and save when you are done.";
  if (value === "Footer")
    return "This content explains the action, risk, or expected result.";
  return "Dialog body content can hold a short message, form summary, or composed product content.";
}

function bottomSheetTitleText(content) {
  if (content === "Form") return "Edit details";
  if (content === "Footer") return "Review changes";
  return "Sheet title";
}

function bottomSheetDescriptionText(content) {
  if (content === "Form")
    return "Collect a small amount of information without leaving the current screen.";
  if (content === "Footer")
    return "Confirm the action or return to the previous step.";
  return "Use bottom sheets for compact mobile tasks without leaving the current screen.";
}

function drawerTitleText(side) {
  if (side === "Left") return "Navigation drawer";
  if (side === "Top") return "Panel controls";
  if (side === "Bottom") return "Bottom drawer";
  return "Drawer title";
}

function drawerDescriptionText(side) {
  if (side === "Left")
    return "Use left drawers for navigation or broad context.";
  if (side === "Top") return "Use top drawers for temporary global controls.";
  if (side === "Bottom")
    return "Use bottom drawers for compact mobile details.";
  return "Use drawers for contextual content tied to the current screen.";
}

function drawerBodyText(side) {
  if (side === "Left")
    return "Compose navigation links, saved filters, or workspace controls inside the drawer body.";
  if (side === "Top")
    return "Compose short controls, status filters, or contextual options here.";
  if (side === "Bottom")
    return "Compose compact details or next-step actions without leaving the current screen.";
  return "Drawer body content can hold filters, navigation, or compact product workflows.";
}

function popoverTitleText(side) {
  if (side === "Right") return "Filters";
  if (side === "Bottom") return "Details";
  if (side === "Left") return "Display options";
  return "Dimensions";
}

function popoverDescriptionText(side) {
  if (side === "Right")
    return "Adjust visible results without leaving this view.";
  if (side === "Bottom")
    return "Review compact supporting details before continuing.";
  if (side === "Left") return "Choose which supporting options stay visible.";
  return "Set the dimensions for the layer.";
}

function menuLabelText(content) {
  if (content === "Checkbox") return "Display options";
  if (content === "Radio") return "Preference";
  if (content === "Submenu") return "Workspace";
  return "My Account";
}

async function syncCheckboxVariantChildren({
  component,
  checked,
  state,
  config,
  variableByName,
  fonts,
  stats,
}) {
  let control = directChildNamed(component, "Checkbox Control");
  if (control && control.type !== "FRAME") {
    control.remove();
    control = null;
  }

  if (!control || control.type !== "FRAME") {
    control = figma.createFrame();
    control.name = "Checkbox Control";
  }

  control.layoutMode = "NONE";
  control.resizeWithoutConstraints(20, 20);
  control.cornerRadius = 4;
  control.clipsContent = false;
  control.setSharedPluginData(RUN_NAMESPACE, "kind", "checkbox-control");
  control.fills = config.controlFill
    ? [
        paintFromVariable(
          config.controlFill,
          config.controlFillFallback,
          variableByName,
          stats,
        ),
      ]
    : [];
  control.strokes = [
    paintFromVariable(
      config.controlStroke,
      config.controlStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  control.strokeWeight = 1;
  bindCheckboxGeometryVariables(component, control, variableByName, stats);

  removeGeneratedButtonChild(control, "Checkbox Mark Short", true);
  removeGeneratedButtonChild(control, "Checkbox Mark Long", true);
  removeGeneratedButtonChild(control, "Checkbox Mark", checked !== "Checked");

  if (checked === "Checked") {
    await syncCheckboxMark(control, config, variableByName, stats);
  }

  syncFocusRing(control, {
    enabled: state !== "Disabled",
    width: 20,
    height: 20,
    radius: 4,
    variableName: focusRingVariableForState(state),
    fallback: focusRingFallbackForState(state),
    variableByName,
    stats,
  });

  let label = directChildNamed(component, "Label Text");
  if (label && label.type !== "TEXT") {
    label.remove();
    label = null;
  }

  if (!label || label.type !== "TEXT") {
    label = figma.createText();
    label.name = "Label Text";
  }

  await applyCheckboxLabelTypography(label, fonts, variableByName, stats);
  label.characters = "Checkbox";
  label.fills = [
    paintFromVariable(
      config.label,
      config.labelFallback,
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";

  component.appendChild(control);
  component.appendChild(label);
}

async function syncCheckboxMark(control, config, variableByName, stats) {
  let mark = directChildNamed(control, "Checkbox Mark");
  if (mark && mark.type !== "INSTANCE") {
    mark.remove();
    mark = null;
  }

  if (!mark) {
    const iconComponent = await findKozmosIconSourceComponent("check");
    if (iconComponent) {
      mark = iconComponent.createInstance();
      mark.name = "Checkbox Mark";
    } else {
      mark = createFallbackCheckboxMark(config, variableByName, stats);
    }
  }

  mark.resize(16, 16);
  mark.x = 2;
  mark.y = 2;
  try {
    mark.constraints = {
      horizontal: "CENTER",
      vertical: "CENTER",
    };
  } catch (_error) {
    // Older plugin runtimes can reject constraints on some instance types.
  }

  storeCheckboxMarkAuditMetadata(mark, config.mark, config.markFallback, stats);

  if (mark.type === "INSTANCE") {
    applyIconColorOverrides(
      mark,
      config.mark,
      config.markFallback,
      variableByName,
      stats,
    );
  }

  control.appendChild(mark);
}

function createFallbackCheckboxMark(config, variableByName, stats) {
  const mark = figma.createVector();
  mark.name = "Checkbox Mark";
  mark.resize(16, 16);
  mark.vectorNetwork = {
    vertices: [
      { x: 3.5, y: 8.25, strokeCap: "ROUND", strokeJoin: "ROUND" },
      { x: 6.75, y: 11.5, strokeCap: "ROUND", strokeJoin: "ROUND" },
      { x: 12.75, y: 4.5, strokeCap: "ROUND", strokeJoin: "ROUND" },
    ],
    segments: [
      {
        start: 0,
        end: 1,
        tangentStart: { x: 0, y: 0 },
        tangentEnd: { x: 0, y: 0 },
      },
      {
        start: 1,
        end: 2,
        tangentStart: { x: 0, y: 0 },
        tangentEnd: { x: 0, y: 0 },
      },
    ],
  };
  mark.fills = [];
  mark.strokes = [
    paintFromVariable(config.mark, config.markFallback, variableByName, stats),
  ];
  mark.strokeWeight = 2;
  mark.strokeCap = "ROUND";
  mark.strokeJoin = "ROUND";
  return mark;
}

function storeCheckboxMarkAuditMetadata(mark, variableName, fallback, stats) {
  if (!mark || !mark.setSharedPluginData) return;

  try {
    mark.setSharedPluginData(RUN_NAMESPACE, "kind", "checkbox-mark");
    mark.setSharedPluginData(RUN_NAMESPACE, "foreground-token", variableName);
    mark.setSharedPluginData(RUN_NAMESPACE, "foreground-fallback", fallback);
  } catch (error) {
    stats.warnings.push(
      `Could not store Checkbox mark audit metadata (${messageFor(error)}).`,
    );
  }
}

async function syncRadioVariantChildren({
  component,
  checked,
  state,
  config,
  variableByName,
  fonts,
  stats,
}) {
  let control = directChildNamed(component, "Radio Control");
  if (control && control.type !== "FRAME") {
    control.remove();
    control = null;
  }

  if (!control || control.type !== "FRAME") {
    control = figma.createFrame();
    control.name = "Radio Control";
  }

  control.layoutMode = "NONE";
  control.resizeWithoutConstraints(20, 20);
  control.cornerRadius = 9999;
  control.clipsContent = false;
  control.setSharedPluginData(RUN_NAMESPACE, "kind", "radio-control");
  control.fills = [];
  control.strokes = [
    paintFromVariable(
      config.controlStroke,
      config.controlStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  control.strokeWeight = 1;

  removeGeneratedButtonChild(control, "Radio Dot", checked !== "Checked");
  let dot = directChildNamed(control, "Radio Dot");
  if (checked === "Checked") {
    if (dot && dot.type !== "ELLIPSE") {
      dot.remove();
      dot = null;
    }

    if (!dot || dot.type !== "ELLIPSE") {
      dot = figma.createEllipse();
      dot.name = "Radio Dot";
    }

    dot.resize(10, 10);
    dot.x = 5;
    dot.y = 5;
    dot.fills = [
      paintFromVariable(config.dot, config.dotFallback, variableByName, stats),
    ];
    dot.strokes = [];
    dot.setSharedPluginData(RUN_NAMESPACE, "kind", "radio-dot");
    control.appendChild(dot);
  }

  syncFocusRing(control, {
    enabled: state !== "Disabled",
    width: 20,
    height: 20,
    radius: 9999,
    variableName: focusRingVariableForState(state),
    fallback: focusRingFallbackForState(state),
    variableByName,
    stats,
  });

  bindRadioGeometryVariables(component, control, dot, variableByName, stats);

  let label = directChildNamed(component, "Label Text");
  if (label && label.type !== "TEXT") {
    label.remove();
    label = null;
  }

  if (!label || label.type !== "TEXT") {
    label = figma.createText();
    label.name = "Label Text";
  }

  await applyRadioLabelTypography(label, fonts, variableByName, stats);
  label.characters = "Radio";
  label.fills = [
    paintFromVariable(
      config.label,
      config.labelFallback,
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";

  component.appendChild(control);
  component.appendChild(label);
}

async function syncSwitchVariantChildren({
  component,
  checked,
  state,
  config,
  variableByName,
  fonts,
  stats,
}) {
  let track = directChildNamed(component, "Switch Track");
  if (track && track.type !== "FRAME") {
    track.remove();
    track = null;
  }

  if (!track || track.type !== "FRAME") {
    track = figma.createFrame();
    track.name = "Switch Track";
  }

  track.layoutMode = "NONE";
  track.resizeWithoutConstraints(44, 24);
  track.cornerRadius = 9999;
  track.clipsContent = false;
  track.setSharedPluginData(RUN_NAMESPACE, "kind", "switch-track");
  track.fills = [
    paintFromVariable(
      config.trackFill,
      config.trackFillFallback,
      variableByName,
      stats,
    ),
  ];
  track.strokes = [
    paintFromVariable(
      config.trackStroke,
      config.trackStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  track.strokeWeight = 2;

  let thumb = directChildNamed(track, "Switch Thumb");
  if (thumb && thumb.type !== "ELLIPSE") {
    thumb.remove();
    thumb = null;
  }

  if (!thumb || thumb.type !== "ELLIPSE") {
    thumb = figma.createEllipse();
    thumb.name = "Switch Thumb";
  }

  thumb.resize(20, 20);
  thumb.x = checked === "Checked" ? 22 : 2;
  thumb.y = 2;
  thumb.fills = [
    paintFromVariable(
      config.thumbFill,
      config.thumbFillFallback,
      variableByName,
      stats,
    ),
  ];
  thumb.strokes = [];
  thumb.setSharedPluginData(RUN_NAMESPACE, "kind", "switch-thumb");
  track.appendChild(thumb);

  syncFocusRing(track, {
    enabled: state !== "Disabled",
    width: 44,
    height: 24,
    radius: 9999,
    variableName: focusRingVariableForState(state),
    fallback: focusRingFallbackForState(state),
    variableByName,
    stats,
  });

  bindSwitchGeometryVariables(component, track, thumb, variableByName, stats);

  let label = directChildNamed(component, "Label Text");
  if (label && label.type !== "TEXT") {
    label.remove();
    label = null;
  }

  if (!label || label.type !== "TEXT") {
    label = figma.createText();
    label.name = "Label Text";
  }

  await applySwitchLabelTypography(label, fonts, variableByName, stats);
  label.characters = "Switch";
  label.fills = [
    paintFromVariable(
      config.label,
      config.labelFallback,
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";

  component.appendChild(track);
  component.appendChild(label);
}

async function syncInputVariantChildren({
  component,
  state,
  status,
  config,
  variableByName,
  fonts,
  stats,
}) {
  let label = directChildNamed(component, "Label Text");
  if (label && label.type !== "TEXT") {
    label.remove();
    label = null;
  }

  if (!label || label.type !== "TEXT") {
    label = figma.createText();
    label.name = "Label Text";
  }

  await applyInputLabelTypography(label, fonts, variableByName, stats);
  label.characters = "Input";
  label.fills = [
    paintFromVariable(
      config.label,
      config.labelFallback,
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";

  let field = directChildNamed(component, "Input Field");
  if (field && field.type !== "FRAME") {
    field.remove();
    field = null;
  }

  if (!field || field.type !== "FRAME") {
    field = figma.createFrame();
    field.name = "Input Field";
  }

  field.layoutMode = "HORIZONTAL";
  field.primaryAxisSizingMode = "FIXED";
  field.counterAxisSizingMode = "FIXED";
  field.primaryAxisAlignItems = "MIN";
  field.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(field, "FILL");
  field.itemSpacing = 0;
  field.paddingLeft = 12;
  field.paddingRight = 12;
  field.paddingTop = 0;
  field.paddingBottom = 0;
  field.resizeWithoutConstraints(320, 44);
  field.cornerRadius = 8;
  field.setSharedPluginData(RUN_NAMESPACE, "kind", "input-field");
  field.fills = [
    paintFromVariable(
      config.fieldFill,
      config.fieldFillFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokes = [
    paintFromVariable(
      config.fieldStroke,
      config.fieldStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokeWeight = 1;
  field.clipsContent = false;

  let placeholder = directChildNamed(field, "Placeholder Text");
  if (placeholder && placeholder.type !== "TEXT") {
    placeholder.remove();
    placeholder = null;
  }

  if (!placeholder || placeholder.type !== "TEXT") {
    placeholder = figma.createText();
    placeholder.name = "Placeholder Text";
  }

  await applyInputTextTypography(placeholder, fonts, variableByName, stats);
  placeholder.characters = "Placeholder";
  setHorizontalFillTextSizing(placeholder);
  setTextAutoResize(placeholder, "TRUNCATE");
  placeholder.fills = [
    paintFromVariable(
      config.placeholder,
      config.placeholderFallback,
      variableByName,
      stats,
    ),
  ];
  placeholder.textAlignVertical = "CENTER";
  field.appendChild(placeholder);

  syncFocusRing(field, {
    enabled: state !== "Disabled",
    width: 320,
    height: 44,
    radius: 8,
    variableName: focusRingVariableForInputStatus(status),
    fallback: focusRingFallbackForInputStatus(status),
    variableByName,
    stats,
  });

  let helper = directChildNamed(component, "Helper Text");
  if (helper && helper.type !== "TEXT") {
    helper.remove();
    helper = null;
  }

  if (!helper || helper.type !== "TEXT") {
    helper = figma.createText();
    helper.name = "Helper Text";
  }

  await applyInputTextTypography(helper, fonts, variableByName, stats);
  helper.characters = helperTextForInputStatus(status);
  helper.visible = status !== "Default";
  setVerticalStackChildSizing(helper);
  setTextAutoResize(helper, "HEIGHT");
  helper.resizeWithoutConstraints(320, 20);
  helper.fills = [
    paintFromVariable(
      config.helper,
      config.helperFallback,
      variableByName,
      stats,
    ),
  ];
  helper.textAlignVertical = "CENTER";

  bindInputGeometryVariables(component, field, variableByName, stats);

  component.appendChild(label);
  component.appendChild(field);
  component.appendChild(helper);
  normalizeInputFamilyChildSizing(component);
}

async function syncPasswordInputVariantChildren({
  component,
  state,
  status,
  visibility,
  config,
  variableByName,
  fonts,
  stats,
}) {
  const preservedText = collectDescendantTextCharactersByName(component, [
    "Label Text",
    "Placeholder Text",
    "Helper Text",
  ]);
  const existingChildren = Array.from(component.children || []);
  for (const child of existingChildren) {
    child.remove();
  }

  const isVisible = visibility === "Visible";
  const iconToken = config.placeholder;
  const iconFallback = config.placeholderFallback;

  const label = figma.createText();
  label.name = "Label Text";
  component.appendChild(label);
  await applyFieldLabelTypography(
    label,
    fonts,
    "PasswordInput",
    variableByName,
    stats,
  );
  label.characters = preservedText["Label Text"] || "Password";
  label.fills = [
    paintFromVariable(
      config.label,
      config.labelFallback,
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";

  const field = figma.createFrame();
  field.name = "PasswordInput Field";
  component.appendChild(field);
  field.layoutMode = "HORIZONTAL";
  field.primaryAxisSizingMode = "FIXED";
  field.counterAxisSizingMode = "FIXED";
  field.primaryAxisAlignItems = "MIN";
  field.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(field, "FILL");
  field.itemSpacing = 0;
  field.paddingLeft = 0;
  field.paddingRight = 0;
  field.paddingTop = 0;
  field.paddingBottom = 0;
  field.resizeWithoutConstraints(320, 44);
  field.cornerRadius = 8;
  field.setSharedPluginData(RUN_NAMESPACE, "kind", "input-field");
  field.fills = [
    paintFromVariable(
      config.fieldFill,
      config.fieldFillFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokes = [
    paintFromVariable(
      config.fieldStroke,
      config.fieldStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokeWeight = 1;
  field.clipsContent = false;

  const valueSlot = figma.createFrame();
  valueSlot.name = "PasswordInput Value Slot";
  field.appendChild(valueSlot);
  valueSlot.layoutMode = "HORIZONTAL";
  valueSlot.primaryAxisSizingMode = "FIXED";
  valueSlot.counterAxisSizingMode = "FIXED";
  valueSlot.primaryAxisAlignItems = "MIN";
  valueSlot.counterAxisAlignItems = "CENTER";
  valueSlot.itemSpacing = 0;
  valueSlot.paddingLeft = 12;
  valueSlot.paddingRight = 12;
  valueSlot.paddingTop = 0;
  valueSlot.paddingBottom = 0;
  valueSlot.resizeWithoutConstraints(276, 44);
  valueSlot.fills = [];
  valueSlot.strokes = [];
  valueSlot.clipsContent = false;
  setHorizontalStackFillChildSizing(valueSlot);

  const placeholder = figma.createText();
  placeholder.name = "Placeholder Text";
  valueSlot.appendChild(placeholder);
  await applyFieldTextTypography(
    placeholder,
    fonts,
    "PasswordInput",
    variableByName,
    stats,
  );
  placeholder.characters = preservedText["Placeholder Text"] || "Password";
  setHorizontalFillTextSizing(placeholder);
  setTextAutoResize(placeholder, "TRUNCATE");
  placeholder.fills = [
    paintFromVariable(
      config.placeholder,
      config.placeholderFallback,
      variableByName,
      stats,
    ),
  ];
  placeholder.textAlignVertical = "CENTER";

  const toggle = figma.createFrame();
  toggle.name = "Visibility Toggle";
  field.appendChild(toggle);
  toggle.layoutMode = "HORIZONTAL";
  toggle.primaryAxisSizingMode = "FIXED";
  toggle.counterAxisSizingMode = "FIXED";
  toggle.primaryAxisAlignItems = "CENTER";
  toggle.counterAxisAlignItems = "CENTER";
  toggle.itemSpacing = 0;
  toggle.paddingLeft = 0;
  toggle.paddingRight = 0;
  toggle.paddingTop = 0;
  toggle.paddingBottom = 0;
  toggle.resizeWithoutConstraints(44, 44);
  toggle.cornerRadius = 8;
  toggle.fills = [];
  toggle.strokes = [];
  toggle.clipsContent = false;
  toggle.setSharedPluginData(
    RUN_NAMESPACE,
    "aria-label",
    isVisible ? "Hide password" : "Show password",
  );
  toggle.setSharedPluginData(
    RUN_NAMESPACE,
    "aria-pressed",
    isVisible ? "true" : "false",
  );

  const icon = await createPasswordVisibilityIcon({
    isVisible,
    variableName: iconToken,
    fallback: iconFallback,
    variableByName,
    stats,
  });
  toggle.appendChild(icon);

  syncFocusRing(field, {
    enabled: state !== "Disabled",
    width: 320,
    height: 44,
    radius: 8,
    variableName: focusRingVariableForInputStatus(status),
    fallback: focusRingFallbackForInputStatus(status),
    variableByName,
    stats,
  });

  const helper = figma.createText();
  helper.name = "Helper Text";
  component.appendChild(helper);
  await applyFieldTextTypography(
    helper,
    fonts,
    "PasswordInput",
    variableByName,
    stats,
  );
  helper.characters =
    preservedText["Helper Text"] || helperTextForInputStatus(status);
  helper.visible = status !== "Default";
  setVerticalStackChildSizing(helper);
  setTextAutoResize(helper, "HEIGHT");
  helper.resizeWithoutConstraints(320, 20);
  helper.fills = [
    paintFromVariable(
      config.helper,
      config.helperFallback,
      variableByName,
      stats,
    ),
  ];
  helper.textAlignVertical = "CENTER";

  bindPasswordInputGeometryVariables(
    component,
    field,
    valueSlot,
    toggle,
    icon,
    variableByName,
    stats,
  );
  normalizeInputFamilyChildSizing(component);
}

async function syncFormFieldVariantChildren({
  component,
  content,
  status,
  required,
  config,
  variableByName,
  fonts,
  stats,
}) {
  const reusableContentSlot = extractDescendantNamed(
    component,
    "Content Slot",
    stats,
    "FormField content slot",
  );
  const preservedText = collectDescendantTextCharactersByName(component, [
    "Label Text",
    "Description Text",
    "Optional Text",
    "Helper Text",
  ]);
  removeDirectChildrenExcept(component, reusableContentSlot);

  const showDescription = content === "Description" || content === "Full";
  const showHelper =
    content === "Helper" || content === "Full" || status !== "Default";
  const isRequired = required === "True";

  const header = figma.createFrame();
  header.name = "Field Header";
  header.layoutMode = "HORIZONTAL";
  header.primaryAxisSizingMode = "FIXED";
  header.counterAxisSizingMode = "AUTO";
  header.primaryAxisAlignItems = "MIN";
  header.counterAxisAlignItems = "MIN";
  header.itemSpacing = 12;
  header.paddingLeft = 0;
  header.paddingRight = 0;
  header.paddingTop = 0;
  header.paddingBottom = 0;
  header.resizeWithoutConstraints(320, 20);
  header.fills = [];
  header.strokes = [];
  header.clipsContent = false;
  setLayoutSizingHorizontal(header, "FILL");

  const labelWrap = figma.createFrame();
  labelWrap.name = "Field Label Group";
  labelWrap.layoutMode = "HORIZONTAL";
  labelWrap.primaryAxisSizingMode = "AUTO";
  labelWrap.counterAxisSizingMode = "AUTO";
  labelWrap.primaryAxisAlignItems = "MIN";
  labelWrap.counterAxisAlignItems = "CENTER";
  labelWrap.itemSpacing = 2;
  labelWrap.paddingLeft = 0;
  labelWrap.paddingRight = 0;
  labelWrap.paddingTop = 0;
  labelWrap.paddingBottom = 0;
  labelWrap.fills = [];
  labelWrap.strokes = [];
  labelWrap.clipsContent = false;
  setHorizontalStackFillHugChildSizing(labelWrap);

  const label = figma.createText();
  label.name = "Label Text";
  await applyFieldLabelTypography(
    label,
    fonts,
    "FormField",
    variableByName,
    stats,
  );
  label.characters = preservedText["Label Text"] || "Label";
  label.fills = [
    paintFromVariable(
      config.label,
      config.labelFallback,
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";
  labelWrap.appendChild(label);

  if (isRequired) {
    const mark = figma.createText();
    mark.name = "Required Mark";
    await applyFieldLabelTypography(
      mark,
      fonts,
      "FormField",
      variableByName,
      stats,
    );
    mark.characters = "*";
    mark.fills = [
      paintFromVariable(
        "Colors/emotional/danger/600",
        "#D41C42",
        variableByName,
        stats,
      ),
    ];
    mark.textAlignVertical = "CENTER";
    labelWrap.appendChild(mark);
  }

  header.appendChild(labelWrap);

  if (!isRequired && content === "Full") {
    const optional = figma.createText();
    optional.name = "Optional Text";
    await applyFormFieldMetaTypography(optional, fonts, variableByName, stats);
    optional.characters = preservedText["Optional Text"] || "Optional";
    optional.fills = [
      paintFromVariable(
        "Colors/foreground/400",
        "#5D626F",
        variableByName,
        stats,
      ),
    ];
    optional.textAlignVertical = "CENTER";
    header.appendChild(optional);
  }

  component.appendChild(header);

  let description = null;
  if (showDescription) {
    description = figma.createText();
    description.name = "Description Text";
    await applyFieldTextTypography(
      description,
      fonts,
      "FormField",
      variableByName,
      stats,
    );
    description.characters =
      preservedText["Description Text"] ||
      "Use clear supporting guidance when the field needs it.";
    description.fills = [
      paintFromVariable(
        "Colors/foreground/400",
        "#5D626F",
        variableByName,
        stats,
      ),
    ];
    description.resizeWithoutConstraints(320, 20);
    setVerticalStackChildSizing(description);
    setTextAutoResize(description, "HEIGHT");
    component.appendChild(description);
  }

  let contentSlot = reusableContentSlot;
  if (contentSlot) {
    configureContentSlotNode(contentSlot, {
      height: 44,
      itemSpacing: 0,
      layoutMode: "VERTICAL",
      slotKind:
        contentSlot.getSharedPluginData &&
        contentSlot.getSharedPluginData(RUN_NAMESPACE, "slot-kind")
          ? contentSlot.getSharedPluginData(RUN_NAMESPACE, "slot-kind")
          : "native",
      slotName: "Content Slot",
      width: 320,
    });
    component.appendChild(contentSlot);
  } else {
    contentSlot = appendComponentContentSlot({
      component,
      height: 44,
      itemSpacing: 0,
      layoutMode: "VERTICAL",
      ownerName: "FormField",
      parent: component,
      slotName: "Content Slot",
      stats,
      width: 320,
    });
  }
  setFormFieldContentSlotSizing(contentSlot);

  let helper = null;
  if (showHelper) {
    helper = figma.createText();
    helper.name = "Helper Text";
    await applyFieldTextTypography(
      helper,
      fonts,
      "FormField",
      variableByName,
      stats,
    );
    helper.characters =
      preservedText["Helper Text"] || helperTextForInputStatus(status);
    helper.fills = [
      paintFromVariable(
        config.helper,
        config.helperFallback,
        variableByName,
        stats,
      ),
    ];
    helper.resizeWithoutConstraints(320, 20);
    setVerticalStackChildSizing(helper);
    setTextAutoResize(helper, "HEIGHT");
    component.appendChild(helper);
  }

  bindFormFieldGeometryVariables(component, header, variableByName, stats);
  normalizeInputFamilyChildSizing(component);
  normalizeFormFieldSizing(component, {
    header,
    labelWrap,
    description,
    contentSlot,
    helper,
  });
}

function normalizeFormFieldSizing(
  component,
  { header, labelWrap, description, contentSlot, helper },
) {
  if (component) {
    component.primaryAxisSizingMode = "AUTO";
    component.counterAxisSizingMode = "FIXED";
    setLayoutSizingHorizontal(component, "FILL");
    setLayoutSizingVertical(component, "HUG");
    resizeNodeWithoutConstraints(
      component,
      320,
      typeof component.height === "number" ? component.height : 142,
    );
  }

  if (header) {
    header.primaryAxisAlignItems = "MIN";
    setVerticalStackChildSizing(header);
  }

  if (labelWrap) {
    setHorizontalStackFillHugChildSizing(labelWrap);
  }

  if (description) {
    setVerticalStackChildSizing(description);
  }

  if (contentSlot) {
    setFormFieldContentSlotSizing(contentSlot);
  }

  if (helper) {
    setVerticalStackChildSizing(helper);
  }
}

function setFormFieldContentSlotSizing(contentSlot) {
  if (!contentSlot) return;

  setNodePropertyIfSupported(contentSlot, "primaryAxisSizingMode", "AUTO");
  setNodePropertyIfSupported(contentSlot, "counterAxisSizingMode", "FIXED");
  setVerticalStackChildSizing(contentSlot);
}

async function syncNumberInputVariantChildren({
  component,
  state,
  status,
  steppers,
  config,
  variableByName,
  fonts,
  stats,
}) {
  const preservedText = collectDescendantTextCharactersByName(component, [
    "Label Text",
    "Value Text",
    "Placeholder Text",
    "Helper Text",
  ]);
  const existingChildren = Array.from(component.children || []);
  for (const child of existingChildren) {
    child.remove();
  }

  const showSteppers = steppers !== "False";
  const valueColor = numberInputValueColorConfig(state, config);

  const label = figma.createText();
  label.name = "Label Text";
  await applyFieldLabelTypography(
    label,
    fonts,
    "NumberInput",
    variableByName,
    stats,
  );
  label.characters = preservedText["Label Text"] || "Number";
  label.fills = [
    paintFromVariable(
      config.label,
      config.labelFallback,
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";

  const field = figma.createFrame();
  field.name = "NumberInput Field";
  field.layoutMode = "HORIZONTAL";
  field.primaryAxisSizingMode = "FIXED";
  field.counterAxisSizingMode = "FIXED";
  field.primaryAxisAlignItems = "MIN";
  field.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(field, "FILL");
  field.itemSpacing = 0;
  field.paddingLeft = 0;
  field.paddingRight = 0;
  field.paddingTop = 0;
  field.paddingBottom = 0;
  field.resizeWithoutConstraints(320, 44);
  field.cornerRadius = 8;
  field.setSharedPluginData(RUN_NAMESPACE, "kind", "input-field");
  field.fills = [
    paintFromVariable(
      config.fieldFill,
      config.fieldFillFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokes = [
    paintFromVariable(
      config.fieldStroke,
      config.fieldStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokeWeight = 1;
  field.clipsContent = false;

  const stepperNodes = [];

  if (showSteppers) {
    const decrement = await createNumberInputStepper({
      name: "Decrement Stepper",
      iconName: "minus",
      colorToken: valueColor.token,
      colorFallback: valueColor.fallback,
      variableByName,
      stats,
    });
    field.appendChild(decrement);
    stepperNodes.push(decrement);
    field.appendChild(createNumberInputDivider(config, variableByName, stats));
  }

  const valueSlot = figma.createFrame();
  valueSlot.name = "NumberInput Value Slot";
  valueSlot.layoutMode = "HORIZONTAL";
  valueSlot.primaryAxisSizingMode = "FIXED";
  valueSlot.counterAxisSizingMode = "FIXED";
  valueSlot.primaryAxisAlignItems = showSteppers ? "CENTER" : "MIN";
  valueSlot.counterAxisAlignItems = "CENTER";
  valueSlot.itemSpacing = 0;
  valueSlot.paddingLeft = 12;
  valueSlot.paddingRight = 12;
  valueSlot.paddingTop = 0;
  valueSlot.paddingBottom = 0;
  valueSlot.resizeWithoutConstraints(showSteppers ? 230 : 320, 44);
  valueSlot.fills = [];
  valueSlot.strokes = [];
  valueSlot.clipsContent = false;
  setHorizontalStackFillChildSizing(valueSlot);

  const valueText = figma.createText();
  valueText.name = "Value Text";
  await applyFieldTextTypography(
    valueText,
    fonts,
    "NumberInput",
    variableByName,
    stats,
  );
  valueText.characters = preservedText["Value Text"] || "12";
  valueText.fills = [
    paintFromVariable(
      valueColor.token,
      valueColor.fallback,
      variableByName,
      stats,
    ),
  ];
  valueText.textAlignHorizontal = showSteppers ? "CENTER" : "LEFT";
  valueText.textAlignVertical = "CENTER";
  setHorizontalFillTextSizing(valueText);
  setTextAutoResize(valueText, "TRUNCATE");

  const placeholder = figma.createText();
  placeholder.name = "Placeholder Text";
  await applyFieldTextTypography(
    placeholder,
    fonts,
    "NumberInput",
    variableByName,
    stats,
  );
  placeholder.characters = preservedText["Placeholder Text"] || "0";
  placeholder.visible = false;
  placeholder.fills = [
    paintFromVariable(
      config.placeholder,
      config.placeholderFallback,
      variableByName,
      stats,
    ),
  ];
  placeholder.textAlignVertical = "CENTER";
  setHorizontalFillTextSizing(placeholder);
  setTextAutoResize(placeholder, "TRUNCATE");
  valueSlot.appendChild(valueText);
  valueSlot.appendChild(placeholder);
  field.appendChild(valueSlot);
  setHorizontalStackFillChildSizing(valueSlot);

  if (showSteppers) {
    field.appendChild(createNumberInputDivider(config, variableByName, stats));
    const increment = await createNumberInputStepper({
      name: "Increment Stepper",
      iconName: "plus",
      colorToken: valueColor.token,
      colorFallback: valueColor.fallback,
      variableByName,
      stats,
    });
    field.appendChild(increment);
    stepperNodes.push(increment);
  }

  syncFocusRing(field, {
    enabled: state !== "Disabled",
    width: 320,
    height: 44,
    radius: 8,
    variableName: focusRingVariableForInputStatus(status),
    fallback: focusRingFallbackForInputStatus(status),
    variableByName,
    stats,
  });

  const helper = figma.createText();
  helper.name = "Helper Text";
  await applyFieldTextTypography(
    helper,
    fonts,
    "NumberInput",
    variableByName,
    stats,
  );
  helper.characters =
    preservedText["Helper Text"] || helperTextForInputStatus(status);
  helper.visible = status !== "Default";
  setVerticalStackChildSizing(helper);
  setTextAutoResize(helper, "HEIGHT");
  helper.resizeWithoutConstraints(320, 20);
  helper.fills = [
    paintFromVariable(
      config.helper,
      config.helperFallback,
      variableByName,
      stats,
    ),
  ];
  helper.textAlignVertical = "CENTER";

  bindNumberInputGeometryVariables(
    component,
    field,
    valueSlot,
    stepperNodes,
    variableByName,
    stats,
  );

  component.appendChild(label);
  component.appendChild(field);
  component.appendChild(helper);
  normalizeInputFamilyChildSizing(component);
  normalizeNumberInputSizing(component, field, valueSlot, stepperNodes);
}

function normalizeNumberInputSizing(component, field, valueSlot, stepperNodes) {
  if (component) {
    component.primaryAxisSizingMode = "AUTO";
    component.counterAxisSizingMode = "FIXED";
    setLayoutSizingHorizontal(component, "FILL");
    setLayoutSizingVertical(component, "HUG");
    resizeNodeWithoutConstraints(
      component,
      320,
      typeof component.height === "number" ? component.height : 70,
    );
  }

  if (field) {
    setVerticalFixedFillChildSizing(field);
  }

  if (valueSlot) {
    setHorizontalStackFillChildSizing(valueSlot);
  }

  for (const stepper of stepperNodes || []) {
    setFixedChildSizing(stepper);
  }

  for (const child of field && field.children ? field.children : []) {
    if (child.name === "NumberInput Divider") {
      setFixedChildSizing(child);
    }
  }
}

async function syncOTPInputVariantChildren({
  component,
  length,
  state,
  status,
  config,
  variableByName,
  fonts,
  stats,
}) {
  const digitNames = [
    "Digit 1 Text",
    "Digit 2 Text",
    "Digit 3 Text",
    "Digit 4 Text",
    "Digit 5 Text",
    "Digit 6 Text",
  ];
  const preservedText = collectDescendantTextCharactersByName(
    component,
    ["Label Text", "Helper Text"].concat(digitNames),
  );
  const existingChildren = Array.from(component.children || []);
  for (const child of existingChildren) {
    child.remove();
  }

  const digitCount = Number(length);
  const defaultDigits = ["1", "3", "5", "8", "2", "6"];
  const activeIndex = 0;

  const label = figma.createText();
  label.name = "Label Text";
  await applyFieldLabelTypography(
    label,
    fonts,
    "OTPInput",
    variableByName,
    stats,
  );
  label.characters = preservedText["Label Text"] || "Verification code";
  label.fills = [
    paintFromVariable(
      config.label,
      config.labelFallback,
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";

  const group = figma.createFrame();
  group.name = "OTPInput Cell Group";
  group.layoutMode = "HORIZONTAL";
  group.primaryAxisSizingMode = "AUTO";
  group.counterAxisSizingMode = "FIXED";
  group.primaryAxisAlignItems = "MIN";
  group.counterAxisAlignItems = "CENTER";
  group.itemSpacing = 8;
  group.paddingLeft = 0;
  group.paddingRight = 0;
  group.paddingTop = 0;
  group.paddingBottom = 0;
  group.resizeWithoutConstraints(320, 44);
  group.fills = [];
  group.strokes = [];
  group.clipsContent = false;
  group.setSharedPluginData(RUN_NAMESPACE, "kind", "otpinput-cell-group");
  setVerticalStackChildSizing(group);

  const cells = [];
  for (let index = 0; index < digitCount; index += 1) {
    const cellName = digitNames[index];
    const digitConfig =
      state === "Focus" && index === activeIndex
        ? inputConfig("Focus", status)
        : inputConfig(state === "Focus" ? "Default" : state, status);
    const cell = await createOTPInputCell({
      name: `Digit ${index + 1} Cell`,
      textName: cellName,
      text: preservedText[cellName] || defaultDigits[index],
      config: digitConfig,
      isActive: index === activeIndex,
      state,
      status,
      variableByName,
      fonts,
      stats,
    });
    group.appendChild(cell);
    cells.push(cell);
  }

  const helper = figma.createText();
  helper.name = "Helper Text";
  await applyFieldTextTypography(
    helper,
    fonts,
    "OTPInput",
    variableByName,
    stats,
  );
  helper.characters =
    preservedText["Helper Text"] || helperTextForInputStatus(status);
  helper.visible = status !== "Default";
  setVerticalStackChildSizing(helper);
  setTextAutoResize(helper, "HEIGHT");
  helper.resizeWithoutConstraints(320, 20);
  helper.fills = [
    paintFromVariable(
      config.helper,
      config.helperFallback,
      variableByName,
      stats,
    ),
  ];
  helper.textAlignVertical = "CENTER";

  bindOTPInputGeometryVariables(component, group, cells, variableByName, stats);

  component.appendChild(label);
  component.appendChild(group);
  component.appendChild(helper);
  normalizeInputFamilyChildSizing(component);
  normalizeOTPInputSizing(component, group, cells);
}

async function createOTPInputCell({
  name,
  textName,
  text,
  config,
  isActive,
  state,
  status,
  variableByName,
  fonts,
  stats,
}) {
  const cell = figma.createFrame();
  cell.name = name;
  cell.layoutMode = "HORIZONTAL";
  cell.primaryAxisSizingMode = "FIXED";
  cell.counterAxisSizingMode = "FIXED";
  cell.primaryAxisAlignItems = "CENTER";
  cell.counterAxisAlignItems = "CENTER";
  cell.itemSpacing = 0;
  cell.paddingLeft = 0;
  cell.paddingRight = 0;
  cell.paddingTop = 0;
  cell.paddingBottom = 0;
  cell.resizeWithoutConstraints(44, 44);
  cell.cornerRadius = 8;
  cell.fills = [
    paintFromVariable(
      config.fieldFill,
      config.fieldFillFallback,
      variableByName,
      stats,
    ),
  ];
  cell.strokes = [
    paintFromVariable(
      config.fieldStroke,
      config.fieldStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  cell.strokeWeight = 1;
  cell.clipsContent = false;
  cell.setSharedPluginData(RUN_NAMESPACE, "kind", "otpinput-cell");

  const digit = figma.createText();
  digit.name = textName;
  await applyFieldTextTypography(
    digit,
    fonts,
    "OTPInput",
    variableByName,
    stats,
  );
  digit.characters = text;
  digit.fills = [
    paintFromVariable(
      state === "Disabled" ? config.placeholder : "Colors/foreground/0",
      state === "Disabled" ? config.placeholderFallback : "#000000",
      variableByName,
      stats,
    ),
  ];
  digit.textAlignHorizontal = "CENTER";
  digit.textAlignVertical = "CENTER";
  setTextAutoResize(digit, "WIDTH_AND_HEIGHT");
  cell.appendChild(digit);

  syncFocusRing(cell, {
    enabled: state !== "Disabled" && isActive,
    width: 44,
    height: 44,
    radius: 8,
    variableName: focusRingVariableForInputStatus(status),
    fallback: focusRingFallbackForInputStatus(status),
    variableByName,
    stats,
  });

  return cell;
}

function bindOTPInputGeometryVariables(
  component,
  group,
  cells,
  variableByName,
  stats,
) {
  bindFloatVariable(
    component,
    "width",
    "OTPInput/width/default",
    variableByName,
    stats,
  );
  bindFloatVariable(
    component,
    "itemSpacing",
    "OTPInput/gap",
    variableByName,
    stats,
  );

  if (group) {
    bindFloatVariable(
      group,
      "itemSpacing",
      "OTPInput/cell/gap",
      variableByName,
      stats,
    );
  }

  for (const cell of cells || []) {
    bindSizeVariables(
      cell,
      "OTPInput/cell/size",
      "OTPInput/cell/size",
      variableByName,
      stats,
    );
    bindFloatVariable(
      cell,
      "cornerRadius",
      "OTPInput/cell/radius",
      variableByName,
      stats,
    );
    bindFloatVariable(
      cell,
      "strokeWeight",
      "OTPInput/cell/stroke/width",
      variableByName,
      stats,
    );
  }
}

function normalizeOTPInputSizing(component, group, cells) {
  if (component) {
    component.primaryAxisSizingMode = "AUTO";
    component.counterAxisSizingMode = "FIXED";
    setLayoutSizingHorizontal(component, "FILL");
    setLayoutSizingVertical(component, "HUG");
    resizeNodeWithoutConstraints(
      component,
      320,
      typeof component.height === "number" ? component.height : 96,
    );
  }

  if (group) {
    setVerticalStackChildSizing(group);
  }

  for (const cell of cells || []) {
    setFixedChildSizing(cell);
  }
}

async function syncComboboxVariantChildren({
  component,
  state,
  status,
  content,
  format,
  config,
  variableByName,
  fonts,
  stats,
}) {
  const preservedText = collectDescendantTextCharactersByName(component, [
    "Label Text",
    "Placeholder Text",
    "Option 1 Text",
    "Option 2 Text",
    "Option 3 Text",
    "Helper Text",
  ]);
  const existingChildren = Array.from(component.children || []);
  for (const child of existingChildren) {
    child.remove();
  }

  const disabled = state === "Disabled";
  const isOpen = content === "Open";

  const label = figma.createText();
  label.name = "Label Text";
  await applyFieldLabelTypography(
    label,
    fonts,
    "Combobox",
    variableByName,
    stats,
  );
  label.characters = preservedText["Label Text"] || "Combobox";
  label.fills = [
    paintFromVariable(
      config.label,
      config.labelFallback,
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";

  const field = figma.createFrame();
  field.name = "Combobox Field";
  field.layoutMode = "HORIZONTAL";
  field.primaryAxisSizingMode = "FIXED";
  field.counterAxisSizingMode = "FIXED";
  field.primaryAxisAlignItems = "MIN";
  field.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(field, "FILL");
  field.itemSpacing = 8;
  field.paddingLeft = 12;
  field.paddingRight = 12;
  field.paddingTop = 0;
  field.paddingBottom = 0;
  field.resizeWithoutConstraints(320, 44);
  field.cornerRadius = 8;
  field.setSharedPluginData(RUN_NAMESPACE, "kind", "input-field");
  field.fills = [
    paintFromVariable(
      config.fieldFill,
      config.fieldFillFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokes = [
    paintFromVariable(
      config.fieldStroke,
      config.fieldStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokeWeight = 1;
  field.clipsContent = false;

  const leadingIcon = await createFixedIconInstance(
    "search-md",
    config.placeholder,
    config.placeholderFallback,
    variableByName,
    stats,
    16,
    "Combobox/icon/size",
  );
  field.appendChild(leadingIcon);

  const placeholder = figma.createText();
  placeholder.name = "Placeholder Text";
  await applyFieldTextTypography(
    placeholder,
    fonts,
    "Combobox",
    variableByName,
    stats,
  );
  placeholder.characters =
    preservedText["Placeholder Text"] || "Search or select";
  setHorizontalFillTextSizing(placeholder);
  setTextAutoResize(placeholder, "TRUNCATE");
  placeholder.fills = [
    paintFromVariable(
      config.placeholder,
      config.placeholderFallback,
      variableByName,
      stats,
    ),
  ];
  placeholder.textAlignVertical = "CENTER";
  field.appendChild(placeholder);

  const trailingIcon = await createFixedIconInstance(
    isOpen ? "chevron-up" : "chevron-down",
    config.placeholder,
    config.placeholderFallback,
    variableByName,
    stats,
    16,
    "Combobox/icon/size",
  );
  field.appendChild(trailingIcon);

  syncFocusRing(field, {
    enabled: state !== "Disabled",
    width: 320,
    height: 44,
    radius: 8,
    variableName: focusRingVariableForInputStatus(status),
    fallback: focusRingFallbackForInputStatus(status),
    variableByName,
    stats,
  });

  let listbox = null;
  const optionNodes = [];
  const optionIcons = [];
  if (isOpen) {
    listbox = figma.createFrame();
    listbox.name = "Combobox Listbox";
    listbox.layoutMode = "VERTICAL";
    listbox.primaryAxisSizingMode = "AUTO";
    listbox.counterAxisSizingMode = "FIXED";
    listbox.primaryAxisAlignItems = "MIN";
    listbox.counterAxisAlignItems = "MIN";
    setLayoutSizingHorizontal(listbox, "FILL");
    listbox.itemSpacing = 2;
    listbox.paddingLeft = 4;
    listbox.paddingRight = 4;
    listbox.paddingTop = 4;
    listbox.paddingBottom = 4;
    listbox.resizeWithoutConstraints(320, 144);
    listbox.cornerRadius = 8;
    listbox.clipsContent = false;
    listbox.setSharedPluginData(RUN_NAMESPACE, "kind", "combobox-listbox");
    listbox.fills = [
      paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
    ];
    listbox.strokes = [
      paintFromVariable(
        "Colors/foreground/500",
        "#747B8B",
        variableByName,
        stats,
      ),
    ];
    listbox.strokeWeight = 1;
    listbox.effects = tooltipShadowEffects();

    const optionLabels = [
      preservedText["Option 1 Text"] || "Metro Station",
      preservedText["Option 2 Text"] || "Bus Stop",
      preservedText["Option 3 Text"] || "Bike Parking",
    ];
    for (let index = 0; index < optionLabels.length; index += 1) {
      const option = await createComboboxOption({
        index,
        text: optionLabels[index],
        selected: index === 0,
        disabled,
        variableByName,
        fonts,
        stats,
      });
      optionNodes.push(option);
      const icon = directChildNamed(option, "Icon");
      if (icon) optionIcons.push(icon);
      listbox.appendChild(option);
    }
  }

  const helper = figma.createText();
  helper.name = "Helper Text";
  await applyFieldTextTypography(
    helper,
    fonts,
    "Combobox",
    variableByName,
    stats,
  );
  helper.characters =
    preservedText["Helper Text"] || helperTextForInputStatus(status);
  helper.visible = status !== "Default";
  setVerticalStackChildSizing(helper);
  setTextAutoResize(helper, "HEIGHT");
  helper.resizeWithoutConstraints(320, 20);
  helper.fills = [
    paintFromVariable(
      config.helper,
      config.helperFallback,
      variableByName,
      stats,
    ),
  ];
  helper.textAlignVertical = "CENTER";

  bindComboboxGeometryVariables(
    component,
    field,
    listbox,
    optionNodes,
    [leadingIcon, trailingIcon].concat(optionIcons),
    variableByName,
    stats,
  );

  component.appendChild(label);
  component.appendChild(field);
  if (listbox) component.appendChild(listbox);
  component.appendChild(helper);
  normalizeInputFamilyChildSizing(component);
}

async function createComboboxOption({
  index,
  text,
  selected,
  disabled,
  variableByName,
  fonts,
  stats,
}) {
  const option = figma.createFrame();
  option.name = `Combobox Option ${index + 1}`;
  option.layoutMode = "HORIZONTAL";
  option.primaryAxisSizingMode = "FIXED";
  option.counterAxisSizingMode = "FIXED";
  option.primaryAxisAlignItems = "SPACE_BETWEEN";
  option.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(option, "FILL");
  option.itemSpacing = 8;
  option.paddingLeft = 12;
  option.paddingRight = 12;
  option.paddingTop = 0;
  option.paddingBottom = 0;
  option.resizeWithoutConstraints(312, 44);
  option.cornerRadius = 4;
  option.clipsContent = false;
  option.setSharedPluginData(RUN_NAMESPACE, "kind", "combobox-option");
  option.fills = selected
    ? [
        paintFromVariable(
          disabled ? "Colors/background/200" : "Colors/background/100",
          disabled ? "#C7CAD1" : "#E3E4E8",
          variableByName,
          stats,
        ),
      ]
    : [];
  option.strokes = [];

  const label = figma.createText();
  label.name = `Option ${index + 1} Text`;
  await applyFieldTextTypography(
    label,
    fonts,
    "Combobox",
    variableByName,
    stats,
  );
  label.characters = text;
  setHorizontalFillTextSizing(label);
  setTextAutoResize(label, "TRUNCATE");
  label.fills = [
    paintFromVariable(
      disabled ? "Colors/foreground/500" : "Colors/foreground/0",
      disabled ? "#747B8B" : "#000000",
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";
  option.appendChild(label);

  if (selected) {
    const icon = await createFixedIconInstance(
      "check",
      disabled ? "Colors/foreground/500" : "Colors/theme/500",
      disabled ? "#747B8B" : "#135BEC",
      variableByName,
      stats,
      16,
      "Combobox/icon/size",
    );
    option.appendChild(icon);
  }

  return option;
}

async function syncMultiSelectVariantChildren({
  component,
  state,
  status,
  content,
  format,
  config,
  variableByName,
  fonts,
  stats,
}) {
  const preservedText = collectDescendantTextCharactersByName(component, [
    "Label Text",
    "Placeholder Text",
    "Chip 1 Text",
    "Chip 2 Text",
    "Option 1 Text",
    "Option 2 Text",
    "Option 3 Text",
    "Helper Text",
  ]);
  const existingChildren = Array.from(component.children || []);
  for (const child of existingChildren) {
    child.remove();
  }

  const disabled = state === "Disabled";
  const readonly = state === "Readonly";
  const hasSelections = content !== "Empty";
  const isOpen = content === "Open";

  const label = figma.createText();
  label.name = "Label Text";
  await applyFieldLabelTypography(
    label,
    fonts,
    "MultiSelect",
    variableByName,
    stats,
  );
  label.characters = preservedText["Label Text"] || "Facilities";
  label.fills = [
    paintFromVariable(
      config.label,
      config.labelFallback,
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";

  const field = figma.createFrame();
  field.name = "MultiSelect Field";
  field.layoutMode = "HORIZONTAL";
  field.primaryAxisSizingMode = "FIXED";
  field.counterAxisSizingMode = "FIXED";
  field.primaryAxisAlignItems = "MIN";
  field.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(field, "FILL");
  field.itemSpacing = 6;
  field.paddingLeft = 8;
  field.paddingRight = 8;
  field.paddingTop = 4;
  field.paddingBottom = 4;
  field.resizeWithoutConstraints(320, 44);
  field.cornerRadius = 8;
  field.setSharedPluginData(RUN_NAMESPACE, "kind", "input-field");
  field.fills = [
    paintFromVariable(
      config.fieldFill,
      config.fieldFillFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokes = [
    paintFromVariable(
      config.fieldStroke,
      config.fieldStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokeWeight = 1;
  field.clipsContent = false;

  const chipNodes = [];
  const icons = [];
  const chipLabels = [
    preservedText["Chip 1 Text"] ||
      multiSelectChipLabelText(component, 1) ||
      "Metro",
    preservedText["Chip 2 Text"] ||
      multiSelectChipLabelText(component, 2) ||
      "Bus",
  ];
  if (hasSelections) {
    for (let index = 0; index < chipLabels.length; index += 1) {
      const chip = await createMultiSelectChip({
        index,
        text: chipLabels[index],
        disabled,
        readonly,
        variableByName,
        fonts,
        stats,
      });
      chipNodes.push(chip);
      const icon = directChildNamed(chip, "Icon");
      if (icon) icons.push(icon);
      field.appendChild(chip);
    }
  }

  const placeholder = figma.createText();
  placeholder.name = "Placeholder Text";
  await applyFieldTextTypography(
    placeholder,
    fonts,
    "MultiSelect",
    variableByName,
    stats,
  );
  placeholder.characters =
    preservedText["Placeholder Text"] ||
    (hasSelections ? "Type to filter" : "Select options");
  setHorizontalFillTextSizing(placeholder);
  setTextAutoResize(placeholder, "TRUNCATE");
  placeholder.fills = [
    paintFromVariable(
      config.placeholder,
      config.placeholderFallback,
      variableByName,
      stats,
    ),
  ];
  placeholder.textAlignVertical = "CENTER";
  field.appendChild(placeholder);

  if (hasSelections && !disabled && !readonly) {
    const clearIcon = await createFixedIconInstance(
      "x-close",
      config.placeholder,
      config.placeholderFallback,
      variableByName,
      stats,
      16,
      "MultiSelect/icon/size",
    );
    icons.push(clearIcon);
    field.appendChild(clearIcon);
  }

  const trailingIcon = await createFixedIconInstance(
    isOpen ? "chevron-up" : "chevron-down",
    config.placeholder,
    config.placeholderFallback,
    variableByName,
    stats,
    16,
    "MultiSelect/icon/size",
  );
  icons.push(trailingIcon);
  field.appendChild(trailingIcon);

  syncFocusRing(field, {
    enabled: state !== "Disabled",
    width: 320,
    height: 44,
    radius: 8,
    variableName: focusRingVariableForInputStatus(status),
    fallback: focusRingFallbackForInputStatus(status),
    variableByName,
    stats,
  });

  let listbox = null;
  const optionNodes = [];
  if (isOpen) {
    listbox = figma.createFrame();
    listbox.name = "MultiSelect Listbox";
    listbox.layoutMode = "VERTICAL";
    listbox.primaryAxisSizingMode = "AUTO";
    listbox.counterAxisSizingMode = "FIXED";
    listbox.primaryAxisAlignItems = "MIN";
    listbox.counterAxisAlignItems = "MIN";
    setLayoutSizingHorizontal(listbox, "FILL");
    listbox.itemSpacing = 2;
    listbox.paddingLeft = 4;
    listbox.paddingRight = 4;
    listbox.paddingTop = 4;
    listbox.paddingBottom = 4;
    listbox.resizeWithoutConstraints(320, 144);
    listbox.cornerRadius = 8;
    listbox.clipsContent = false;
    listbox.setSharedPluginData(RUN_NAMESPACE, "kind", "multiselect-listbox");
    listbox.fills = [
      paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
    ];
    listbox.strokes = [
      paintFromVariable(
        "Colors/foreground/500",
        "#747B8B",
        variableByName,
        stats,
      ),
    ];
    listbox.strokeWeight = 1;
    listbox.effects = tooltipShadowEffects();

    const optionLabels = [
      preservedText["Option 1 Text"] || "Metro Station",
      preservedText["Option 2 Text"] || "Bus Stop",
      preservedText["Option 3 Text"] || "Bike Parking",
    ];
    for (let index = 0; index < optionLabels.length; index += 1) {
      const option = await createMultiSelectOption({
        index,
        text: optionLabels[index],
        selected: index < 2,
        active: index === 2,
        disabled,
        variableByName,
        fonts,
        stats,
      });
      optionNodes.push(option);
      const icon = directChildNamed(option, "Icon");
      if (icon) icons.push(icon);
      listbox.appendChild(option);
    }
  }

  const helper = figma.createText();
  helper.name = "Helper Text";
  await applyFieldTextTypography(
    helper,
    fonts,
    "MultiSelect",
    variableByName,
    stats,
  );
  helper.characters =
    preservedText["Helper Text"] || helperTextForInputStatus(status);
  helper.visible = status !== "Default";
  setVerticalStackChildSizing(helper);
  setTextAutoResize(helper, "HEIGHT");
  helper.resizeWithoutConstraints(320, 20);
  helper.fills = [
    paintFromVariable(
      config.helper,
      config.helperFallback,
      variableByName,
      stats,
    ),
  ];
  helper.textAlignVertical = "CENTER";

  bindMultiSelectGeometryVariables(
    component,
    field,
    chipNodes,
    listbox,
    optionNodes,
    icons,
    variableByName,
    stats,
  );

  component.appendChild(label);
  component.appendChild(field);
  if (listbox) component.appendChild(listbox);
  component.appendChild(helper);
  normalizeInputFamilyChildSizing(component);
}

async function createMultiSelectChip({
  index,
  text,
  disabled,
  readonly,
  variableByName,
  fonts,
  stats,
}) {
  const nested = await createNestedComponentInstance({
    componentSetName: "Chip",
    variantProperties: {
      Variant: "Neutral",
      Size: "Small",
      State: disabled ? "Disabled" : "Selected",
      Removable: readonly ? "False" : "True",
    },
    name: `MultiSelect Chip ${index + 1}`,
    role: "multiselect-chip",
    stats,
  });
  if (nested && nested.instance) {
    const chip = nested.instance;
    setInstanceTextProperty(
      chip,
      nested.componentSet,
      "Label Text",
      text,
      stats,
    );
    try {
      chip.isExposedInstance = true;
    } catch (_error) {
      // Older Figma runtimes may not support exposing nested instances.
    }
    try {
      chip.clipsContent = false;
    } catch (_error) {
      // Keep the generated fallback resilient in older runtimes.
    }
    setHugChildSizing(chip);
    return chip;
  }

  const chip = figma.createFrame();
  chip.name = `MultiSelect Chip ${index + 1}`;
  chip.layoutMode = "HORIZONTAL";
  chip.primaryAxisSizingMode = "AUTO";
  chip.counterAxisSizingMode = "FIXED";
  chip.primaryAxisAlignItems = "MIN";
  chip.counterAxisAlignItems = "CENTER";
  chip.itemSpacing = 4;
  chip.paddingLeft = 8;
  chip.paddingRight = readonly ? 8 : 6;
  chip.paddingTop = 0;
  chip.paddingBottom = 0;
  chip.resizeWithoutConstraints(72, 24);
  chip.cornerRadius = 9999;
  chip.clipsContent = false;
  chip.setSharedPluginData(RUN_NAMESPACE, "kind", "multiselect-chip");
  chip.fills = [
    paintFromVariable(
      disabled ? "Colors/background/200" : "Colors/background/100",
      disabled ? "#C7CAD1" : "#E3E4E8",
      variableByName,
      stats,
    ),
  ];
  chip.strokes = [];

  const label = figma.createText();
  label.name = `Chip ${index + 1} Text`;
  await applyFieldTextTypography(
    label,
    fonts,
    "MultiSelect",
    variableByName,
    stats,
  );
  label.characters = text;
  setHugChildSizing(label);
  setTextAutoResize(label, "WIDTH_AND_HEIGHT");
  label.fills = [
    paintFromVariable(
      disabled ? "Colors/foreground/500" : "Colors/foreground/0",
      disabled ? "#747B8B" : "#000000",
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";
  chip.appendChild(label);

  if (!readonly) {
    const icon = await createFixedIconInstance(
      "x-close",
      disabled ? "Colors/foreground/500" : "Colors/foreground/400",
      disabled ? "#747B8B" : "#5D626F",
      variableByName,
      stats,
      16,
      "MultiSelect/icon/size",
    );
    chip.appendChild(icon);
  }

  return chip;
}

async function createMultiSelectOption({
  index,
  text,
  selected,
  active,
  disabled,
  variableByName,
  fonts,
  stats,
}) {
  const option = figma.createFrame();
  option.name = `MultiSelect Option ${index + 1}`;
  option.layoutMode = "HORIZONTAL";
  option.primaryAxisSizingMode = "FIXED";
  option.counterAxisSizingMode = "FIXED";
  option.primaryAxisAlignItems = "SPACE_BETWEEN";
  option.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(option, "FILL");
  option.itemSpacing = 8;
  option.paddingLeft = 12;
  option.paddingRight = 12;
  option.paddingTop = 0;
  option.paddingBottom = 0;
  option.resizeWithoutConstraints(312, 44);
  option.cornerRadius = 4;
  option.clipsContent = false;
  option.setSharedPluginData(RUN_NAMESPACE, "kind", "multiselect-option");
  option.fills =
    selected || active
      ? [
          paintFromVariable(
            disabled ? "Colors/background/200" : "Colors/background/100",
            disabled ? "#C7CAD1" : "#E3E4E8",
            variableByName,
            stats,
          ),
        ]
      : [];
  option.strokes = [];

  const label = figma.createText();
  label.name = `Option ${index + 1} Text`;
  await applyFieldTextTypography(
    label,
    fonts,
    "MultiSelect",
    variableByName,
    stats,
  );
  label.characters = text;
  setHorizontalFillTextSizing(label);
  setTextAutoResize(label, "TRUNCATE");
  label.fills = [
    paintFromVariable(
      disabled ? "Colors/foreground/500" : "Colors/foreground/0",
      disabled ? "#747B8B" : "#000000",
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";
  option.appendChild(label);

  if (selected) {
    const icon = await createFixedIconInstance(
      "check",
      disabled ? "Colors/foreground/500" : "Colors/theme/500",
      disabled ? "#747B8B" : "#135BEC",
      variableByName,
      stats,
      16,
      "MultiSelect/icon/size",
    );
    option.appendChild(icon);
  }

  return option;
}

async function syncListboxVariantChildren({
  component,
  selection,
  state,
  variableByName,
  fonts,
  stats,
}) {
  const preservedText = collectDescendantTextCharactersByName(component, [
    "Option 1 Text",
    "Option 1 Description",
    "Option 2 Text",
    "Option 2 Description",
    "Option 3 Text",
    "Option 3 Description",
  ]);
  const existingChildren = Array.from(component.children || []);
  for (const child of existingChildren) {
    child.remove();
  }

  const disabled = state === "Disabled";
  const multiple = selection === "Multiple";
  const labels = [
    preservedText["Option 1 Text"] || "Metro Station",
    preservedText["Option 2 Text"] || "Bus Stop",
    preservedText["Option 3 Text"] || "Bike Parking",
  ];
  const descriptions = [
    preservedText["Option 1 Description"] || "Fast transit connections",
    preservedText["Option 2 Description"] || "Frequent local service",
    preservedText["Option 3 Description"] || "Secure cycle storage",
  ];
  const optionNodes = [];
  const icons = [];

  for (let index = 0; index < labels.length; index += 1) {
    const selected = multiple ? index !== 1 : index === 0;
    const option = await createListboxOption({
      index,
      label: labels[index],
      description: descriptions[index],
      selected,
      active: state === "Focus" && index === 1,
      multiple,
      disabled,
      variableByName,
      fonts,
      stats,
    });
    optionNodes.push(option);
    const icon = directChildNamed(option, "Icon");
    if (icon) icons.push(icon);
    component.appendChild(option);
  }

  syncFocusRing(component, {
    enabled: state !== "Disabled",
    width: 320,
    height: 176,
    radius: 8,
    variableName: "Colors/theme/500",
    fallback: "#135BEC",
    variableByName,
    stats,
  });

  bindListboxGeometryVariables(
    component,
    optionNodes,
    icons,
    variableByName,
    stats,
  );
  normalizeInputFamilyChildSizing(component);
}

async function createListboxOption({
  index,
  label,
  description,
  selected,
  active,
  multiple,
  disabled,
  variableByName,
  fonts,
  stats,
}) {
  const option = figma.createFrame();
  option.name = `Listbox Option ${index + 1}`;
  option.layoutMode = "HORIZONTAL";
  option.primaryAxisSizingMode = "FIXED";
  option.counterAxisSizingMode = "FIXED";
  option.primaryAxisAlignItems = "SPACE_BETWEEN";
  option.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(option, "FILL");
  option.itemSpacing = 8;
  option.paddingLeft = 12;
  option.paddingRight = 12;
  option.paddingTop = 6;
  option.paddingBottom = 6;
  option.resizeWithoutConstraints(312, 52);
  option.cornerRadius = 4;
  option.clipsContent = false;
  option.setSharedPluginData(RUN_NAMESPACE, "kind", "listbox-option");
  option.fills =
    selected || active
      ? [
          paintFromVariable(
            disabled ? "Colors/background/200" : "Colors/background/100",
            disabled ? "#C7CAD1" : "#E3E4E8",
            variableByName,
            stats,
          ),
        ]
      : [];
  option.strokes = [];

  const textStack = figma.createFrame();
  textStack.name = "Listbox Option Text";
  textStack.layoutMode = "VERTICAL";
  textStack.primaryAxisSizingMode = "AUTO";
  textStack.counterAxisSizingMode = "FIXED";
  textStack.primaryAxisAlignItems = "MIN";
  textStack.counterAxisAlignItems = "MIN";
  setHorizontalFillTextSizing(textStack);
  textStack.itemSpacing = 0;
  textStack.paddingLeft = 0;
  textStack.paddingRight = 0;
  textStack.paddingTop = 0;
  textStack.paddingBottom = 0;
  textStack.fills = [];
  textStack.strokes = [];

  const title = figma.createText();
  title.name = `Option ${index + 1} Text`;
  await applyFieldTextTypography(
    title,
    fonts,
    "Listbox",
    variableByName,
    stats,
  );
  title.characters = label;
  setHorizontalFillTextSizing(title);
  setTextAutoResize(title, "TRUNCATE");
  title.fills = [
    paintFromVariable(
      disabled ? "Colors/foreground/500" : "Colors/foreground/0",
      disabled ? "#747B8B" : "#000000",
      variableByName,
      stats,
    ),
  ];
  title.textAlignVertical = "CENTER";
  textStack.appendChild(title);

  const detail = figma.createText();
  detail.name = `Option ${index + 1} Description`;
  await applyListboxDescriptionTypography(detail, fonts, variableByName, stats);
  detail.characters = description;
  setHorizontalFillTextSizing(detail);
  setTextAutoResize(detail, "TRUNCATE");
  detail.fills = [
    paintFromVariable(
      disabled ? "Colors/foreground/500" : "Colors/foreground/400",
      disabled ? "#747B8B" : "#5D626F",
      variableByName,
      stats,
    ),
  ];
  detail.textAlignVertical = "CENTER";
  textStack.appendChild(detail);
  option.appendChild(textStack);

  if (selected) {
    const icon = await createFixedIconInstance(
      "check",
      disabled ? "Colors/foreground/500" : "Colors/theme/500",
      disabled ? "#747B8B" : "#135BEC",
      variableByName,
      stats,
      16,
      "Listbox/icon/size",
    );
    option.appendChild(icon);
  }

  return option;
}

async function applyListboxDescriptionTypography(
  text,
  fonts,
  variableByName,
  stats,
) {
  text.fontName = fonts.regular;
  text.fontSize = 12;
  text.lineHeight = { unit: "PIXELS", value: 16 };
  text.textAutoResize = "WIDTH_AND_HEIGHT";
  await applyTextStyleToNodeAsync(text, "fieldText", stats);

  bindFloatVariable(
    text,
    "fontSize",
    "Listbox/description/font-size",
    variableByName,
    stats,
  );
  bindFloatVariable(
    text,
    "lineHeight",
    "Listbox/description/line-height",
    variableByName,
    stats,
  );
}

async function syncDatePickerVariantChildren({
  component,
  state,
  status,
  content,
  config,
  variableByName,
  fonts,
  stats,
}) {
  const preservedText = collectDescendantTextCharactersByName(component, [
    "Label Text",
    "Value Text",
    "Month Text",
    "Helper Text",
  ]);
  const existingChildren = Array.from(component.children || []);
  for (const child of existingChildren) {
    child.remove();
  }

  const disabled = state === "Disabled";
  const isOpen = content === "Open";

  const label = figma.createText();
  label.name = "Label Text";
  await applyFieldLabelTypography(
    label,
    fonts,
    "DatePicker",
    variableByName,
    stats,
  );
  label.characters = preservedText["Label Text"] || "Date";
  label.fills = [
    paintFromVariable(
      config.label,
      config.labelFallback,
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";

  const field = figma.createFrame();
  field.name = "DatePicker Field";
  field.layoutMode = "HORIZONTAL";
  field.primaryAxisSizingMode = "FIXED";
  field.counterAxisSizingMode = "FIXED";
  field.primaryAxisAlignItems = "MIN";
  field.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(field, "FILL");
  field.itemSpacing = 8;
  field.paddingLeft = 12;
  field.paddingRight = 12;
  field.paddingTop = 0;
  field.paddingBottom = 0;
  field.resizeWithoutConstraints(320, 44);
  field.cornerRadius = 8;
  field.setSharedPluginData(RUN_NAMESPACE, "kind", "input-field");
  field.fills = [
    paintFromVariable(
      config.fieldFill,
      config.fieldFillFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokes = [
    paintFromVariable(
      config.fieldStroke,
      config.fieldStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokeWeight = 1;
  field.clipsContent = false;

  const leadingIcon = await createFixedIconInstance(
    "calendar",
    config.placeholder,
    config.placeholderFallback,
    variableByName,
    stats,
    16,
    "DatePicker/icon/size",
  );
  field.appendChild(leadingIcon);

  const value = figma.createText();
  value.name = "Value Text";
  await applyFieldTextTypography(
    value,
    fonts,
    "DatePicker",
    variableByName,
    stats,
  );
  value.characters = preservedText["Value Text"] || "May 25, 2026";
  setHorizontalFillTextSizing(value);
  setTextAutoResize(value, "TRUNCATE");
  value.fills = [
    paintFromVariable(
      disabled ? config.placeholder : "Colors/foreground/0",
      disabled ? config.placeholderFallback : "#000000",
      variableByName,
      stats,
    ),
  ];
  value.textAlignVertical = "CENTER";
  field.appendChild(value);

  const trailingIcon = await createFixedIconInstance(
    isOpen ? "chevron-up" : "chevron-down",
    config.placeholder,
    config.placeholderFallback,
    variableByName,
    stats,
    16,
    "DatePicker/icon/size",
  );
  field.appendChild(trailingIcon);

  syncFocusRing(field, {
    enabled: state !== "Disabled",
    width: 320,
    height: 44,
    radius: 8,
    variableName: focusRingVariableForInputStatus(status),
    fallback: focusRingFallbackForInputStatus(status),
    variableByName,
    stats,
  });

  let calendar = null;
  let calendarHeader = null;
  const dayNodes = [];
  const icons = [leadingIcon, trailingIcon];
  if (isOpen) {
    const builtCalendar = await createDatePickerCalendar({
      monthText: preservedText["Month Text"] || "May 2026",
      disabled,
      variableByName,
      fonts,
      stats,
    });
    calendar = builtCalendar.calendar;
    calendarHeader = builtCalendar.header;
    for (const dayNode of builtCalendar.dayNodes) {
      dayNodes.push(dayNode);
    }
    for (const icon of builtCalendar.icons) {
      icons.push(icon);
    }
  }

  const helper = figma.createText();
  helper.name = "Helper Text";
  await applyFieldTextTypography(
    helper,
    fonts,
    "DatePicker",
    variableByName,
    stats,
  );
  helper.characters =
    preservedText["Helper Text"] || helperTextForInputStatus(status);
  helper.visible = status !== "Default";
  setVerticalStackChildSizing(helper);
  setTextAutoResize(helper, "HEIGHT");
  helper.resizeWithoutConstraints(320, 20);
  helper.fills = [
    paintFromVariable(
      config.helper,
      config.helperFallback,
      variableByName,
      stats,
    ),
  ];
  helper.textAlignVertical = "CENTER";

  bindDatePickerGeometryVariables(
    component,
    field,
    calendar,
    calendarHeader,
    dayNodes,
    icons,
    variableByName,
    stats,
  );

  component.appendChild(label);
  component.appendChild(field);
  if (calendar) component.appendChild(calendar);
  component.appendChild(helper);
  normalizeInputFamilyChildSizing(component);
}

function createCalendarDaySpecs({
  monthIndex,
  rangeEndDate,
  rangeStartDate,
  selectedDate,
  todayDate,
  year,
}) {
  const firstDayOffset = new Date(Date.UTC(year, monthIndex, 1)).getUTCDay();
  const selectedKey = calendarDateKey(selectedDate);
  const todayKey = calendarDateKey(todayDate);
  const rangeStartKey = calendarDateKey(rangeStartDate);
  const rangeEndKey = calendarDateKey(rangeEndDate);
  const specs = [];

  for (let index = 0; index < 42; index += 1) {
    const date = new Date(
      Date.UTC(year, monthIndex, 1 - firstDayOffset + index),
    );
    const key = Date.UTC(
      date.getUTCFullYear(),
      date.getUTCMonth(),
      date.getUTCDate(),
    );
    const inCurrentMonth =
      date.getUTCFullYear() === year && date.getUTCMonth() === monthIndex;

    specs.push({
      inRange:
        rangeStartKey !== null &&
        rangeEndKey !== null &&
        key >= rangeStartKey &&
        key <= rangeEndKey,
      outsideMonth: !inCurrentMonth,
      rangeEnd: rangeEndKey !== null && key === rangeEndKey,
      rangeStart: rangeStartKey !== null && key === rangeStartKey,
      selected: selectedKey !== null && key === selectedKey,
      today: todayKey !== null && key === todayKey,
      value: String(date.getUTCDate()),
    });
  }

  return specs;
}

function calendarDateKey(date) {
  if (!date) return null;

  return Date.UTC(date.year, date.monthIndex, date.day);
}

async function createDatePickerCalendar({
  monthText,
  disabled,
  variableByName,
  fonts,
  stats,
}) {
  const calendar = figma.createFrame();
  calendar.name = "DatePicker Calendar";
  calendar.layoutMode = "VERTICAL";
  calendar.primaryAxisSizingMode = "AUTO";
  calendar.counterAxisSizingMode = "FIXED";
  calendar.primaryAxisAlignItems = "MIN";
  calendar.counterAxisAlignItems = "MIN";
  setLayoutSizingHorizontal(calendar, "FILL");
  calendar.itemSpacing = 8;
  calendar.paddingLeft = 12;
  calendar.paddingRight = 12;
  calendar.paddingTop = 12;
  calendar.paddingBottom = 12;
  calendar.resizeWithoutConstraints(320, DATE_PICKER_CALENDAR_HEIGHT);
  calendar.cornerRadius = 8;
  calendar.clipsContent = false;
  calendar.setSharedPluginData(RUN_NAMESPACE, "kind", "datepicker-calendar");
  calendar.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  calendar.strokes = [
    paintFromVariable(
      "Colors/foreground/500",
      "#747B8B",
      variableByName,
      stats,
    ),
  ];
  calendar.strokeWeight = 1;
  calendar.effects = tooltipShadowEffects();

  const header = figma.createFrame();
  header.name = "DatePicker Calendar Header";
  header.layoutMode = "HORIZONTAL";
  header.primaryAxisSizingMode = "FIXED";
  header.counterAxisSizingMode = "FIXED";
  header.primaryAxisAlignItems = "SPACE_BETWEEN";
  header.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(header, "FILL");
  header.itemSpacing = 8;
  header.paddingLeft = 0;
  header.paddingRight = 0;
  header.paddingTop = 0;
  header.paddingBottom = 0;
  header.resizeWithoutConstraints(296, 32);
  header.fills = [];
  header.strokes = [];
  header.clipsContent = false;

  const month = figma.createText();
  month.name = "Month Text";
  await applyFieldLabelTypography(
    month,
    fonts,
    "DatePicker",
    variableByName,
    stats,
  );
  month.characters = monthText;
  setHorizontalFillTextSizing(month);
  month.fills = [
    paintFromVariable(
      disabled ? "Colors/foreground/500" : "Colors/foreground/0",
      disabled ? "#747B8B" : "#000000",
      variableByName,
      stats,
    ),
  ];
  month.textAlignVertical = "CENTER";
  header.appendChild(month);

  const nav = figma.createFrame();
  nav.name = "DatePicker Calendar Navigation";
  nav.layoutMode = "HORIZONTAL";
  nav.primaryAxisSizingMode = "AUTO";
  nav.counterAxisSizingMode = "AUTO";
  nav.primaryAxisAlignItems = "MIN";
  nav.counterAxisAlignItems = "CENTER";
  setLayoutSizingVertical(nav, "HUG");
  nav.itemSpacing = 4;
  nav.paddingLeft = 0;
  nav.paddingRight = 0;
  nav.paddingTop = 0;
  nav.paddingBottom = 0;
  nav.resizeWithoutConstraints(68, 32);
  nav.fills = [];
  nav.strokes = [];
  nav.clipsContent = false;

  const icons = [];
  const prev = await createDatePickerCalendarNavButton({
    iconName: "chevron-left",
    disabled,
    variableByName,
    stats,
  });
  icons.push(directChildNamed(prev, "Icon"));
  nav.appendChild(prev);

  const next = await createDatePickerCalendarNavButton({
    iconName: "chevron-right",
    disabled,
    variableByName,
    stats,
  });
  icons.push(directChildNamed(next, "Icon"));
  nav.appendChild(next);
  header.appendChild(nav);
  calendar.appendChild(header);

  const weekdayRow = figma.createFrame();
  weekdayRow.name = "DatePicker Weekdays";
  weekdayRow.layoutMode = "HORIZONTAL";
  weekdayRow.primaryAxisSizingMode = "FIXED";
  weekdayRow.counterAxisSizingMode = "AUTO";
  weekdayRow.primaryAxisAlignItems = "MIN";
  weekdayRow.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(weekdayRow, "FILL");
  weekdayRow.itemSpacing = 4;
  weekdayRow.paddingLeft = 0;
  weekdayRow.paddingRight = 0;
  weekdayRow.paddingTop = 0;
  weekdayRow.paddingBottom = 0;
  weekdayRow.resizeWithoutConstraints(296, 20);
  weekdayRow.fills = [];
  weekdayRow.strokes = [];
  weekdayRow.clipsContent = false;

  for (const weekday of ["Su", "Mo", "Tu", "We", "Th", "Fr", "Sa"]) {
    const label = figma.createText();
    label.name = "Weekday Text";
    await applyDatePickerMetaTypography(label, fonts, variableByName, stats);
    label.characters = weekday;
    label.resizeWithoutConstraints(36, 20);
    label.textAlignHorizontal = "CENTER";
    label.textAlignVertical = "CENTER";
    label.fills = [
      paintFromVariable(
        "Colors/foreground/400",
        "#5D626F",
        variableByName,
        stats,
      ),
    ];
    weekdayRow.appendChild(label);
  }
  calendar.appendChild(weekdayRow);

  const grid = figma.createFrame();
  grid.name = "DatePicker Calendar Grid";
  grid.layoutMode = "VERTICAL";
  grid.primaryAxisSizingMode = "AUTO";
  grid.counterAxisSizingMode = "FIXED";
  grid.primaryAxisAlignItems = "MIN";
  grid.counterAxisAlignItems = "MIN";
  setLayoutSizingHorizontal(grid, "FILL");
  grid.itemSpacing = 4;
  grid.paddingLeft = 0;
  grid.paddingRight = 0;
  grid.paddingTop = 0;
  grid.paddingBottom = 0;
  grid.resizeWithoutConstraints(296, DATE_PICKER_GRID_HEIGHT);
  grid.fills = [];
  grid.strokes = [];
  grid.clipsContent = false;

  const daySpecs = createCalendarDaySpecs({
    monthIndex: DATE_PICKER_EXAMPLE.monthIndex,
    selectedDate: {
      day: DATE_PICKER_EXAMPLE.selectedDay,
      monthIndex: DATE_PICKER_EXAMPLE.monthIndex,
      year: DATE_PICKER_EXAMPLE.year,
    },
    todayDate: {
      day: DATE_PICKER_EXAMPLE.todayDay,
      monthIndex: DATE_PICKER_EXAMPLE.monthIndex,
      year: DATE_PICKER_EXAMPLE.year,
    },
    year: DATE_PICKER_EXAMPLE.year,
  });
  const dayNodes = [];
  for (let rowIndex = 0; rowIndex < DATE_PICKER_GRID_ROWS; rowIndex += 1) {
    const row = figma.createFrame();
    row.name = `DatePicker Week ${rowIndex + 1}`;
    row.layoutMode = "HORIZONTAL";
    row.primaryAxisSizingMode = "FIXED";
    row.counterAxisSizingMode = "FIXED";
    row.primaryAxisAlignItems = "MIN";
    row.counterAxisAlignItems = "CENTER";
    setLayoutSizingHorizontal(row, "FILL");
    row.itemSpacing = 4;
    row.paddingLeft = 0;
    row.paddingRight = 0;
    row.paddingTop = 0;
    row.paddingBottom = 0;
    row.resizeWithoutConstraints(296, 36);
    row.fills = [];
    row.strokes = [];
    row.clipsContent = false;

    for (let columnIndex = 0; columnIndex < 7; columnIndex += 1) {
      const index = rowIndex * 7 + columnIndex;
      const daySpec = daySpecs[index];
      const day = await createDatePickerCalendarDay({
        value: daySpec.value,
        outsideMonth: daySpec.outsideMonth,
        selected: daySpec.selected,
        today: daySpec.today,
        disabled,
        variableByName,
        fonts,
        stats,
      });
      dayNodes.push(day);
      row.appendChild(day);
    }
    grid.appendChild(row);
  }
  calendar.appendChild(grid);

  return {
    calendar,
    dayNodes,
    header,
    icons: icons.filter(Boolean),
  };
}

async function createDatePickerCalendarNavButton({
  iconName,
  disabled,
  variableByName,
  stats,
}) {
  const button = figma.createFrame();
  button.name =
    iconName === "chevron-left" ? "Previous Month Button" : "Next Month Button";
  button.layoutMode = "HORIZONTAL";
  button.primaryAxisSizingMode = "FIXED";
  button.counterAxisSizingMode = "FIXED";
  button.primaryAxisAlignItems = "CENTER";
  button.counterAxisAlignItems = "CENTER";
  button.itemSpacing = 0;
  button.paddingLeft = 0;
  button.paddingRight = 0;
  button.paddingTop = 0;
  button.paddingBottom = 0;
  button.resizeWithoutConstraints(32, 32);
  button.cornerRadius = 8;
  button.clipsContent = false;
  button.fills = [];
  button.strokes = [];

  const icon = await createFixedIconInstance(
    iconName,
    disabled ? "Colors/foreground/500" : "Colors/foreground/400",
    disabled ? "#747B8B" : "#5D626F",
    variableByName,
    stats,
    16,
    "DatePicker/icon/size",
  );
  button.appendChild(icon);
  return button;
}

async function createDatePickerCalendarDay({
  value,
  outsideMonth,
  selected,
  today,
  disabled,
  variableByName,
  fonts,
  stats,
}) {
  const day = figma.createFrame();
  day.name = selected ? "Selected Day" : today ? "Today Day" : "Calendar Day";
  day.layoutMode = "HORIZONTAL";
  day.primaryAxisSizingMode = "FIXED";
  day.counterAxisSizingMode = "FIXED";
  day.primaryAxisAlignItems = "CENTER";
  day.counterAxisAlignItems = "CENTER";
  day.itemSpacing = 0;
  day.paddingLeft = 0;
  day.paddingRight = 0;
  day.paddingTop = 0;
  day.paddingBottom = 0;
  day.resizeWithoutConstraints(36, 36);
  day.cornerRadius = 8;
  day.clipsContent = false;
  day.setSharedPluginData(RUN_NAMESPACE, "kind", "datepicker-day");
  day.setSharedPluginData(
    RUN_NAMESPACE,
    "availability",
    outsideMonth ? "unavailable" : "available",
  );
  day.setSharedPluginData(
    RUN_NAMESPACE,
    "calendar-state",
    selected ? "selected" : today ? "today" : "default",
  );
  day.fills = selected
    ? [paintFromVariable("Colors/theme/500", "#135BEC", variableByName, stats)]
    : today
      ? [
          paintFromVariable(
            "Colors/background/100",
            "#E3E4E8",
            variableByName,
            stats,
          ),
        ]
      : [];
  day.strokes = [];

  const label = figma.createText();
  label.name = "Day Text";
  await applyFieldTextTypography(
    label,
    fonts,
    "DatePicker",
    variableByName,
    stats,
  );
  label.characters = value;
  label.textAlignHorizontal = "CENTER";
  label.textAlignVertical = "CENTER";
  label.fills = [
    paintFromVariable(
      selected
        ? "Colors/foreground/inverse"
        : disabled || outsideMonth
          ? disabled
            ? "Colors/foreground/500"
            : "Colors/foreground/400"
          : "Colors/foreground/0",
      selected
        ? "#FFFFFF"
        : disabled
          ? "#747B8B"
          : outsideMonth
            ? "#5D626F"
            : "#000000",
      variableByName,
      stats,
    ),
  ];
  day.appendChild(label);
  return day;
}

async function syncDateRangePickerVariantChildren({
  component,
  state,
  status,
  content,
  config,
  variableByName,
  fonts,
  stats,
}) {
  const preservedText = collectDescendantTextCharactersByName(component, [
    "Label Text",
    "Start Label Text",
    "End Label Text",
    "Start Value Text",
    "End Value Text",
    "Start Month Text",
    "End Month Text",
    "Helper Text",
  ]);
  const existingChildren = Array.from(component.children || []);
  for (const child of existingChildren) {
    child.remove();
  }

  const disabled = state === "Disabled";
  const isOpen = content === "Open";

  const label = figma.createText();
  label.name = "Label Text";
  await applyFieldLabelTypography(
    label,
    fonts,
    "DateRangePicker",
    variableByName,
    stats,
  );
  label.characters = preservedText["Label Text"] || "Date range";
  label.fills = [
    paintFromVariable(
      config.label,
      config.labelFallback,
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";

  const fieldsRow = figma.createFrame();
  fieldsRow.name = "DateRangePicker Fields";
  fieldsRow.layoutMode = "HORIZONTAL";
  fieldsRow.primaryAxisSizingMode = "FIXED";
  fieldsRow.counterAxisSizingMode = "AUTO";
  fieldsRow.primaryAxisAlignItems = "MIN";
  fieldsRow.counterAxisAlignItems = "MIN";
  setLayoutSizingHorizontal(fieldsRow, "FILL");
  fieldsRow.itemSpacing = 12;
  fieldsRow.paddingLeft = 0;
  fieldsRow.paddingRight = 0;
  fieldsRow.paddingTop = 0;
  fieldsRow.paddingBottom = 0;
  fieldsRow.resizeWithoutConstraints(672, 70);
  fieldsRow.fills = [];
  fieldsRow.strokes = [];
  fieldsRow.clipsContent = false;

  const startField = await createDateRangePickerField({
    groupName: "Start Date Field Group",
    labelName: "Start Label Text",
    labelText: preservedText["Start Label Text"] || "Start date",
    valueName: "Start Value Text",
    valueText: preservedText["Start Value Text"] || "May 21, 2026",
    disabled,
    config,
    variableByName,
    fonts,
    stats,
  });
  fieldsRow.appendChild(startField.group);

  const endField = await createDateRangePickerField({
    groupName: "End Date Field Group",
    labelName: "End Label Text",
    labelText: preservedText["End Label Text"] || "End date",
    valueName: "End Value Text",
    valueText: preservedText["End Value Text"] || "May 25, 2026",
    disabled,
    config,
    variableByName,
    fonts,
    stats,
  });
  fieldsRow.appendChild(endField.group);

  syncFocusRing(startField.field, {
    enabled: state !== "Disabled",
    width: 330,
    height: 44,
    radius: 8,
    variableName: focusRingVariableForInputStatus(status),
    fallback: focusRingFallbackForInputStatus(status),
    variableByName,
    stats,
  });
  syncFocusRing(endField.field, {
    enabled: state !== "Disabled",
    width: 330,
    height: 44,
    radius: 8,
    variableName: focusRingVariableForInputStatus(status),
    fallback: focusRingFallbackForInputStatus(status),
    variableByName,
    stats,
  });

  let calendar = null;
  const monthNodes = [];
  const dayNodes = [];
  const icons = [];
  icons.push(startField.icon);
  icons.push(endField.icon);
  if (isOpen) {
    const builtCalendar = await createDateRangePickerCalendar({
      startMonthText: preservedText["Start Month Text"] || "May 2026",
      endMonthText: preservedText["End Month Text"] || "June 2026",
      disabled,
      variableByName,
      fonts,
      stats,
    });
    calendar = builtCalendar.calendar;
    for (const monthNode of builtCalendar.monthNodes) {
      monthNodes.push(monthNode);
    }
    for (const dayNode of builtCalendar.dayNodes) {
      dayNodes.push(dayNode);
    }
    for (const icon of builtCalendar.icons) {
      icons.push(icon);
    }
  }

  const helper = figma.createText();
  helper.name = "Helper Text";
  await applyFieldTextTypography(
    helper,
    fonts,
    "DateRangePicker",
    variableByName,
    stats,
  );
  helper.characters =
    preservedText["Helper Text"] || helperTextForInputStatus(status);
  helper.visible = status !== "Default";
  setVerticalStackChildSizing(helper);
  setTextAutoResize(helper, "HEIGHT");
  helper.resizeWithoutConstraints(672, 20);
  helper.fills = [
    paintFromVariable(
      config.helper,
      config.helperFallback,
      variableByName,
      stats,
    ),
  ];
  helper.textAlignVertical = "CENTER";

  bindDateRangePickerGeometryVariables(
    component,
    fieldsRow,
    [startField.group, endField.group],
    [startField.field, endField.field],
    calendar,
    monthNodes,
    dayNodes,
    icons.filter(Boolean),
    variableByName,
    stats,
  );

  component.appendChild(label);
  component.appendChild(fieldsRow);
  if (calendar) component.appendChild(calendar);
  component.appendChild(helper);
  normalizeInputFamilyChildSizing(component);
}

async function createDateRangePickerField({
  groupName,
  labelName,
  labelText,
  valueName,
  valueText,
  disabled,
  config,
  variableByName,
  fonts,
  stats,
}) {
  const group = figma.createFrame();
  group.name = groupName;
  group.layoutMode = "VERTICAL";
  group.primaryAxisSizingMode = "AUTO";
  group.counterAxisSizingMode = "FIXED";
  group.primaryAxisAlignItems = "MIN";
  group.counterAxisAlignItems = "MIN";
  group.itemSpacing = 6;
  group.paddingLeft = 0;
  group.paddingRight = 0;
  group.paddingTop = 0;
  group.paddingBottom = 0;
  group.resizeWithoutConstraints(330, 70);
  group.fills = [];
  group.strokes = [];
  group.clipsContent = false;

  const label = figma.createText();
  label.name = labelName;
  await applyFieldLabelTypography(
    label,
    fonts,
    "DateRangePicker",
    variableByName,
    stats,
  );
  label.characters = labelText;
  label.fills = [
    paintFromVariable(
      config.label,
      config.labelFallback,
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";
  group.appendChild(label);

  const field = figma.createFrame();
  field.name =
    valueName === "Start Value Text" ? "Start Date Field" : "End Date Field";
  field.layoutMode = "HORIZONTAL";
  field.primaryAxisSizingMode = "FIXED";
  field.counterAxisSizingMode = "FIXED";
  field.primaryAxisAlignItems = "MIN";
  field.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(field, "FILL");
  field.itemSpacing = 8;
  field.paddingLeft = 12;
  field.paddingRight = 12;
  field.paddingTop = 0;
  field.paddingBottom = 0;
  field.resizeWithoutConstraints(330, 44);
  field.cornerRadius = 8;
  field.clipsContent = false;
  field.setSharedPluginData(RUN_NAMESPACE, "kind", "input-field");
  field.fills = [
    paintFromVariable(
      config.fieldFill,
      config.fieldFillFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokes = [
    paintFromVariable(
      config.fieldStroke,
      config.fieldStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokeWeight = 1;

  const icon = await createFixedIconInstance(
    "calendar",
    config.placeholder,
    config.placeholderFallback,
    variableByName,
    stats,
    16,
    "DateRangePicker/icon/size",
  );
  field.appendChild(icon);

  const value = figma.createText();
  value.name = valueName;
  await applyFieldTextTypography(
    value,
    fonts,
    "DateRangePicker",
    variableByName,
    stats,
  );
  value.characters = valueText;
  setHorizontalFillTextSizing(value);
  setTextAutoResize(value, "TRUNCATE");
  value.fills = [
    paintFromVariable(
      disabled ? config.placeholder : "Colors/foreground/0",
      disabled ? config.placeholderFallback : "#000000",
      variableByName,
      stats,
    ),
  ];
  value.textAlignVertical = "CENTER";
  field.appendChild(value);
  group.appendChild(field);

  return { field, group, icon };
}

async function createDateRangePickerCalendar({
  startMonthText,
  endMonthText,
  disabled,
  variableByName,
  fonts,
  stats,
}) {
  const calendar = figma.createFrame();
  calendar.name = "DateRangePicker Calendar";
  calendar.layoutMode = "HORIZONTAL";
  calendar.primaryAxisSizingMode = "FIXED";
  calendar.counterAxisSizingMode = "AUTO";
  calendar.primaryAxisAlignItems = "MIN";
  calendar.counterAxisAlignItems = "MIN";
  setLayoutSizingHorizontal(calendar, "FILL");
  calendar.itemSpacing = 12;
  calendar.paddingLeft = 12;
  calendar.paddingRight = 12;
  calendar.paddingTop = 12;
  calendar.paddingBottom = 12;
  calendar.resizeWithoutConstraints(672, DATE_RANGE_PICKER_CALENDAR_HEIGHT);
  calendar.cornerRadius = 8;
  calendar.clipsContent = false;
  calendar.setSharedPluginData(RUN_NAMESPACE, "kind", "daterange-calendar");
  calendar.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  calendar.strokes = [
    paintFromVariable(
      "Colors/foreground/500",
      "#747B8B",
      variableByName,
      stats,
    ),
  ];
  calendar.strokeWeight = 1;
  calendar.effects = tooltipShadowEffects();

  const startMonth = await createDateRangePickerMonth({
    daySpecs: createCalendarDaySpecs({
      monthIndex: DATE_RANGE_PICKER_EXAMPLE.startMonthIndex,
      rangeEndDate: {
        day: DATE_RANGE_PICKER_EXAMPLE.endDay,
        monthIndex: DATE_RANGE_PICKER_EXAMPLE.endMonthIndex,
        year: DATE_RANGE_PICKER_EXAMPLE.endYear,
      },
      rangeStartDate: {
        day: DATE_RANGE_PICKER_EXAMPLE.startDay,
        monthIndex: DATE_RANGE_PICKER_EXAMPLE.startMonthIndex,
        year: DATE_RANGE_PICKER_EXAMPLE.startYear,
      },
      year: DATE_RANGE_PICKER_EXAMPLE.startYear,
    }),
    monthTextName: "Start Month Text",
    monthText: startMonthText,
    disabled,
    variableByName,
    fonts,
    stats,
  });
  calendar.appendChild(startMonth.month);

  const endMonth = await createDateRangePickerMonth({
    daySpecs: createCalendarDaySpecs({
      monthIndex: DATE_RANGE_PICKER_EXAMPLE.nextMonthIndex,
      rangeEndDate: {
        day: DATE_RANGE_PICKER_EXAMPLE.endDay,
        monthIndex: DATE_RANGE_PICKER_EXAMPLE.endMonthIndex,
        year: DATE_RANGE_PICKER_EXAMPLE.endYear,
      },
      rangeStartDate: {
        day: DATE_RANGE_PICKER_EXAMPLE.startDay,
        monthIndex: DATE_RANGE_PICKER_EXAMPLE.startMonthIndex,
        year: DATE_RANGE_PICKER_EXAMPLE.startYear,
      },
      year: DATE_RANGE_PICKER_EXAMPLE.endYear,
    }),
    monthTextName: "End Month Text",
    monthText: endMonthText,
    disabled,
    variableByName,
    fonts,
    stats,
  });
  calendar.appendChild(endMonth.month);

  const monthNodes = [startMonth.month, endMonth.month];
  const dayNodes = [];
  for (const dayNode of startMonth.dayNodes) dayNodes.push(dayNode);
  for (const dayNode of endMonth.dayNodes) dayNodes.push(dayNode);

  return {
    calendar,
    dayNodes,
    icons: [],
    monthNodes,
  };
}

async function createDateRangePickerMonth({
  daySpecs,
  monthTextName,
  monthText,
  disabled,
  variableByName,
  fonts,
  stats,
}) {
  const month = figma.createFrame();
  month.name =
    monthTextName === "Start Month Text" ? "Start Month" : "End Month";
  month.layoutMode = "VERTICAL";
  month.primaryAxisSizingMode = "AUTO";
  month.counterAxisSizingMode = "FIXED";
  month.primaryAxisAlignItems = "MIN";
  month.counterAxisAlignItems = "MIN";
  month.itemSpacing = 8;
  month.paddingLeft = 0;
  month.paddingRight = 0;
  month.paddingTop = 0;
  month.paddingBottom = 0;
  month.resizeWithoutConstraints(318, DATE_RANGE_PICKER_MONTH_HEIGHT);
  month.fills = [];
  month.strokes = [];
  month.clipsContent = false;

  const heading = figma.createText();
  heading.name = monthTextName;
  await applyFieldLabelTypography(
    heading,
    fonts,
    "DateRangePicker",
    variableByName,
    stats,
  );
  heading.characters = monthText;
  heading.resizeWithoutConstraints(318, 32);
  heading.fills = [
    paintFromVariable(
      disabled ? "Colors/foreground/500" : "Colors/foreground/0",
      disabled ? "#747B8B" : "#000000",
      variableByName,
      stats,
    ),
  ];
  heading.textAlignVertical = "CENTER";
  month.appendChild(heading);

  const weekdayRow = figma.createFrame();
  weekdayRow.name = `${month.name} Weekdays`;
  weekdayRow.layoutMode = "HORIZONTAL";
  weekdayRow.primaryAxisSizingMode = "FIXED";
  weekdayRow.counterAxisSizingMode = "AUTO";
  weekdayRow.primaryAxisAlignItems = "MIN";
  weekdayRow.counterAxisAlignItems = "CENTER";
  weekdayRow.itemSpacing = 4;
  weekdayRow.paddingLeft = 0;
  weekdayRow.paddingRight = 0;
  weekdayRow.paddingTop = 0;
  weekdayRow.paddingBottom = 0;
  weekdayRow.resizeWithoutConstraints(304, 20);
  weekdayRow.fills = [];
  weekdayRow.strokes = [];
  weekdayRow.clipsContent = false;

  for (const weekday of ["Su", "Mo", "Tu", "We", "Th", "Fr", "Sa"]) {
    const label = figma.createText();
    label.name = "Weekday Text";
    await applyDateRangePickerMetaTypography(
      label,
      fonts,
      variableByName,
      stats,
    );
    label.characters = weekday;
    label.resizeWithoutConstraints(40, 20);
    label.textAlignHorizontal = "CENTER";
    label.textAlignVertical = "CENTER";
    label.fills = [
      paintFromVariable(
        "Colors/foreground/400",
        "#5D626F",
        variableByName,
        stats,
      ),
    ];
    weekdayRow.appendChild(label);
  }
  month.appendChild(weekdayRow);

  const grid = figma.createFrame();
  grid.name = `${month.name} Grid`;
  grid.layoutMode = "VERTICAL";
  grid.primaryAxisSizingMode = "AUTO";
  grid.counterAxisSizingMode = "FIXED";
  grid.primaryAxisAlignItems = "MIN";
  grid.counterAxisAlignItems = "MIN";
  grid.itemSpacing = 4;
  grid.paddingLeft = 0;
  grid.paddingRight = 0;
  grid.paddingTop = 0;
  grid.paddingBottom = 0;
  grid.resizeWithoutConstraints(304, DATE_RANGE_PICKER_GRID_HEIGHT);
  grid.fills = [];
  grid.strokes = [];
  grid.clipsContent = false;

  const dayNodes = [];
  for (
    let rowIndex = 0;
    rowIndex < DATE_RANGE_PICKER_GRID_ROWS;
    rowIndex += 1
  ) {
    const row = figma.createFrame();
    row.name = `${month.name} Week ${rowIndex + 1}`;
    row.layoutMode = "HORIZONTAL";
    row.primaryAxisSizingMode = "FIXED";
    row.counterAxisSizingMode = "FIXED";
    row.primaryAxisAlignItems = "MIN";
    row.counterAxisAlignItems = "CENTER";
    row.itemSpacing = 4;
    row.paddingLeft = 0;
    row.paddingRight = 0;
    row.paddingTop = 0;
    row.paddingBottom = 0;
    row.resizeWithoutConstraints(304, 40);
    row.fills = [];
    row.strokes = [];
    row.clipsContent = false;

    for (let columnIndex = 0; columnIndex < 7; columnIndex += 1) {
      const index = rowIndex * 7 + columnIndex;
      const daySpec = daySpecs[index];
      const day = await createDateRangePickerCalendarDay({
        value: daySpec.value,
        outsideMonth: daySpec.outsideMonth,
        rangeStart: daySpec.rangeStart,
        rangeEnd: daySpec.rangeEnd,
        inRange: daySpec.inRange,
        disabled,
        variableByName,
        fonts,
        stats,
      });
      dayNodes.push(day);
      row.appendChild(day);
    }
    grid.appendChild(row);
  }
  month.appendChild(grid);

  return { dayNodes, month };
}

async function createDateRangePickerCalendarDay({
  value,
  outsideMonth,
  rangeStart,
  rangeEnd,
  inRange,
  disabled,
  variableByName,
  fonts,
  stats,
}) {
  const selected = rangeStart || rangeEnd;
  const day = figma.createFrame();
  day.name = selected
    ? rangeStart
      ? "Range Start Day"
      : "Range End Day"
    : inRange
      ? "Range Day"
      : "Calendar Day";
  day.layoutMode = "HORIZONTAL";
  day.primaryAxisSizingMode = "FIXED";
  day.counterAxisSizingMode = "FIXED";
  day.primaryAxisAlignItems = "CENTER";
  day.counterAxisAlignItems = "CENTER";
  day.itemSpacing = 0;
  day.paddingLeft = 0;
  day.paddingRight = 0;
  day.paddingTop = 0;
  day.paddingBottom = 0;
  day.resizeWithoutConstraints(40, 40);
  day.cornerRadius = 8;
  day.clipsContent = false;
  day.setSharedPluginData(RUN_NAMESPACE, "kind", "daterange-day");
  day.setSharedPluginData(
    RUN_NAMESPACE,
    "availability",
    outsideMonth ? "unavailable" : "available",
  );
  day.setSharedPluginData(
    RUN_NAMESPACE,
    "calendar-state",
    selected ? "selected" : inRange ? "in-range" : "default",
  );
  day.fills = selected
    ? [paintFromVariable("Colors/theme/500", "#135BEC", variableByName, stats)]
    : inRange
      ? [
          paintFromVariable(
            "Colors/theme/100",
            "#E7EFFD",
            variableByName,
            stats,
          ),
        ]
      : [];
  day.strokes = [];

  const label = figma.createText();
  label.name = "Day Text";
  await applyFieldTextTypography(
    label,
    fonts,
    "DateRangePicker",
    variableByName,
    stats,
  );
  label.characters = value;
  label.textAlignHorizontal = "CENTER";
  label.textAlignVertical = "CENTER";
  label.fills = [
    paintFromVariable(
      selected
        ? "Colors/foreground/inverse"
        : disabled || outsideMonth
          ? disabled
            ? "Colors/foreground/500"
            : "Colors/foreground/400"
          : "Colors/foreground/0",
      selected
        ? "#FFFFFF"
        : disabled
          ? "#747B8B"
          : outsideMonth
            ? "#5D626F"
            : "#000000",
      variableByName,
      stats,
    ),
  ];
  day.appendChild(label);
  return day;
}

async function syncTimePickerVariantChildren({
  component,
  state,
  status,
  content,
  config,
  variableByName,
  fonts,
  stats,
}) {
  const preservedText = collectDescendantTextCharactersByName(component, [
    "Label Text",
    "Value Text",
    "Option 1 Text",
    "Option 2 Text",
    "Option 3 Text",
    "Option 4 Text",
    "Helper Text",
  ]);
  const existingChildren = Array.from(component.children || []);
  for (const child of existingChildren) {
    child.remove();
  }

  const disabled = state === "Disabled";
  const isOpen = content === "Open";

  const label = figma.createText();
  label.name = "Label Text";
  await applyFieldLabelTypography(
    label,
    fonts,
    "TimePicker",
    variableByName,
    stats,
  );
  label.characters = preservedText["Label Text"] || "Time";
  label.fills = [
    paintFromVariable(
      config.label,
      config.labelFallback,
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";

  const field = figma.createFrame();
  field.name = "TimePicker Field";
  field.layoutMode = "HORIZONTAL";
  field.primaryAxisSizingMode = "FIXED";
  field.counterAxisSizingMode = "FIXED";
  field.primaryAxisAlignItems = "MIN";
  field.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(field, "FILL");
  field.itemSpacing = 8;
  field.paddingLeft = 12;
  field.paddingRight = 12;
  field.paddingTop = 0;
  field.paddingBottom = 0;
  field.resizeWithoutConstraints(320, 44);
  field.cornerRadius = 8;
  field.setSharedPluginData(RUN_NAMESPACE, "kind", "input-field");
  field.fills = [
    paintFromVariable(
      config.fieldFill,
      config.fieldFillFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokes = [
    paintFromVariable(
      config.fieldStroke,
      config.fieldStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokeWeight = 1;
  field.clipsContent = false;

  const leadingIcon = await createFixedIconInstance(
    "clock",
    config.placeholder,
    config.placeholderFallback,
    variableByName,
    stats,
    16,
    "TimePicker/icon/size",
  );
  field.appendChild(leadingIcon);

  const value = figma.createText();
  value.name = "Value Text";
  await applyFieldTextTypography(
    value,
    fonts,
    "TimePicker",
    variableByName,
    stats,
  );
  value.characters = preservedText["Value Text"] || "09:30";
  setHorizontalFillTextSizing(value);
  setTextAutoResize(value, "TRUNCATE");
  value.fills = [
    paintFromVariable(
      disabled ? config.placeholder : "Colors/foreground/0",
      disabled ? config.placeholderFallback : "#000000",
      variableByName,
      stats,
    ),
  ];
  value.textAlignVertical = "CENTER";
  field.appendChild(value);

  const trailingIcon = await createFixedIconInstance(
    isOpen ? "chevron-up" : "chevron-down",
    config.placeholder,
    config.placeholderFallback,
    variableByName,
    stats,
    16,
    "TimePicker/icon/size",
  );
  field.appendChild(trailingIcon);

  syncFocusRing(field, {
    enabled: state !== "Disabled",
    width: 320,
    height: 44,
    radius: 8,
    variableName: focusRingVariableForInputStatus(status),
    fallback: focusRingFallbackForInputStatus(status),
    variableByName,
    stats,
  });

  let listbox = null;
  const optionNodes = [];
  const icons = [leadingIcon, trailingIcon];
  if (isOpen) {
    listbox = figma.createFrame();
    listbox.name = "TimePicker Listbox";
    listbox.layoutMode = "VERTICAL";
    listbox.primaryAxisSizingMode = "AUTO";
    listbox.counterAxisSizingMode = "FIXED";
    listbox.primaryAxisAlignItems = "MIN";
    listbox.counterAxisAlignItems = "MIN";
    setLayoutSizingHorizontal(listbox, "FILL");
    listbox.itemSpacing = 2;
    listbox.paddingLeft = 4;
    listbox.paddingRight = 4;
    listbox.paddingTop = 4;
    listbox.paddingBottom = 4;
    listbox.resizeWithoutConstraints(320, 188);
    listbox.cornerRadius = 8;
    listbox.clipsContent = false;
    listbox.setSharedPluginData(RUN_NAMESPACE, "kind", "timepicker-listbox");
    listbox.fills = [
      paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
    ];
    listbox.strokes = [
      paintFromVariable(
        "Colors/foreground/500",
        "#747B8B",
        variableByName,
        stats,
      ),
    ];
    listbox.strokeWeight = 1;
    listbox.effects = tooltipShadowEffects();

    const optionLabels = [
      preservedText["Option 1 Text"] || "08:00",
      preservedText["Option 2 Text"] || "08:30",
      preservedText["Option 3 Text"] || "09:00",
      preservedText["Option 4 Text"] || "09:30",
    ];
    for (let index = 0; index < optionLabels.length; index += 1) {
      const option = await createTimePickerOption({
        index,
        text: optionLabels[index],
        selected: index === 3,
        active: index === 2,
        disabled,
        variableByName,
        fonts,
        stats,
      });
      optionNodes.push(option);
      const icon = directChildNamed(option, "Icon");
      if (icon) icons.push(icon);
      listbox.appendChild(option);
    }
  }

  const helper = figma.createText();
  helper.name = "Helper Text";
  await applyFieldTextTypography(
    helper,
    fonts,
    "TimePicker",
    variableByName,
    stats,
  );
  helper.characters =
    preservedText["Helper Text"] || helperTextForInputStatus(status);
  helper.visible = status !== "Default";
  setVerticalStackChildSizing(helper);
  setTextAutoResize(helper, "HEIGHT");
  helper.resizeWithoutConstraints(320, 20);
  helper.fills = [
    paintFromVariable(
      config.helper,
      config.helperFallback,
      variableByName,
      stats,
    ),
  ];
  helper.textAlignVertical = "CENTER";

  bindTimePickerGeometryVariables(
    component,
    field,
    listbox,
    optionNodes,
    icons,
    variableByName,
    stats,
  );

  component.appendChild(label);
  component.appendChild(field);
  if (listbox) component.appendChild(listbox);
  component.appendChild(helper);
  normalizeInputFamilyChildSizing(component);
}

async function createTimePickerOption({
  index,
  text,
  selected,
  active,
  disabled,
  variableByName,
  fonts,
  stats,
}) {
  const option = figma.createFrame();
  option.name = `TimePicker Option ${index + 1}`;
  option.layoutMode = "HORIZONTAL";
  option.primaryAxisSizingMode = "FIXED";
  option.counterAxisSizingMode = "FIXED";
  option.primaryAxisAlignItems = "SPACE_BETWEEN";
  option.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(option, "FILL");
  option.itemSpacing = 8;
  option.paddingLeft = 12;
  option.paddingRight = 12;
  option.paddingTop = 0;
  option.paddingBottom = 0;
  option.resizeWithoutConstraints(312, 44);
  option.cornerRadius = 4;
  option.clipsContent = false;
  option.setSharedPluginData(RUN_NAMESPACE, "kind", "timepicker-option");
  option.fills =
    selected || active
      ? [
          paintFromVariable(
            disabled ? "Colors/background/200" : "Colors/background/100",
            disabled ? "#C7CAD1" : "#E3E4E8",
            variableByName,
            stats,
          ),
        ]
      : [];
  option.strokes = [];

  const label = figma.createText();
  label.name = `Option ${index + 1} Text`;
  await applyFieldTextTypography(
    label,
    fonts,
    "TimePicker",
    variableByName,
    stats,
  );
  label.characters = text;
  setHorizontalFillTextSizing(label);
  setTextAutoResize(label, "TRUNCATE");
  label.fills = [
    paintFromVariable(
      disabled ? "Colors/foreground/500" : "Colors/foreground/0",
      disabled ? "#747B8B" : "#000000",
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";
  option.appendChild(label);

  if (selected) {
    const icon = await createFixedIconInstance(
      "check",
      disabled ? "Colors/foreground/500" : "Colors/theme/500",
      disabled ? "#747B8B" : "#135BEC",
      variableByName,
      stats,
      16,
      "TimePicker/icon/size",
    );
    option.appendChild(icon);
  }

  return option;
}

async function syncFileUploadVariantChildren({
  component,
  state,
  status,
  content,
  config,
  variableByName,
  fonts,
  stats,
}) {
  const preservedText = collectDescendantTextCharactersByName(component, [
    "Label Text",
    "Browse Text",
    "Drop Text",
    "Description Text",
    "File Name Text",
    "File Meta Text",
    "Helper Text",
  ]);
  const existingChildren = Array.from(component.children || []);
  for (const child of existingChildren) {
    child.remove();
  }

  const disabled = state === "Disabled";
  const isDragging = state === "Dragging";
  const hasFiles = content === "Files";
  const dropzoneStroke = isDragging ? "Colors/theme/500" : config.fieldStroke;
  const dropzoneStrokeFallback = isDragging
    ? "#135BEC"
    : config.fieldStrokeFallback;
  const dropzoneFill = isDragging ? "Colors/background/100" : config.fieldFill;
  const dropzoneFillFallback = isDragging
    ? "#E3E4E8"
    : config.fieldFillFallback;

  const label = figma.createText();
  label.name = "Label Text";
  await applyFieldLabelTypography(
    label,
    fonts,
    "FileUpload",
    variableByName,
    stats,
  );
  label.characters = preservedText["Label Text"] || "Attachment";
  label.fills = [
    paintFromVariable(
      config.label,
      config.labelFallback,
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";

  const dropzone = figma.createFrame();
  dropzone.name = "FileUpload Dropzone";
  dropzone.layoutMode = "VERTICAL";
  dropzone.primaryAxisSizingMode = "FIXED";
  dropzone.counterAxisSizingMode = "FIXED";
  dropzone.primaryAxisAlignItems = "CENTER";
  dropzone.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(dropzone, "FILL");
  dropzone.itemSpacing = 8;
  dropzone.paddingLeft = 16;
  dropzone.paddingRight = 16;
  dropzone.paddingTop = 24;
  dropzone.paddingBottom = 24;
  dropzone.resizeWithoutConstraints(384, 144);
  dropzone.cornerRadius = 8;
  dropzone.clipsContent = false;
  dropzone.setSharedPluginData(RUN_NAMESPACE, "kind", "file-upload-dropzone");
  dropzone.fills = [
    paintFromVariable(
      dropzoneFill,
      dropzoneFillFallback,
      variableByName,
      stats,
    ),
  ];
  dropzone.strokes = [
    paintFromVariable(
      dropzoneStroke,
      dropzoneStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  dropzone.strokeWeight = 1;
  try {
    dropzone.dashPattern = [6, 4];
  } catch (_error) {
    // Older Figma plugin runtimes do not expose dashed stroke editing.
  }

  const uploadIcon = await createFixedIconInstance(
    "upload-01",
    disabled ? "Colors/foreground/500" : "Colors/foreground/400",
    disabled ? "#747B8B" : "#5D626F",
    variableByName,
    stats,
    28,
    "FileUpload/icon/size",
  );
  dropzone.appendChild(uploadIcon);

  const instruction = figma.createFrame();
  instruction.name = "FileUpload Instruction";
  instruction.layoutMode = "HORIZONTAL";
  instruction.primaryAxisSizingMode = "AUTO";
  instruction.counterAxisSizingMode = "AUTO";
  instruction.primaryAxisAlignItems = "CENTER";
  instruction.counterAxisAlignItems = "CENTER";
  instruction.itemSpacing = 4;
  instruction.paddingLeft = 0;
  instruction.paddingRight = 0;
  instruction.paddingTop = 0;
  instruction.paddingBottom = 0;
  instruction.fills = [];
  instruction.strokes = [];
  instruction.clipsContent = false;

  const browse = figma.createText();
  browse.name = "Browse Text";
  await applyFieldTextTypography(
    browse,
    fonts,
    "FileUpload",
    variableByName,
    stats,
  );
  browse.fontName = fonts.medium;
  browse.characters = preservedText["Browse Text"] || "Click to upload";
  browse.fills = [
    paintFromVariable(
      disabled ? "Colors/foreground/500" : "Colors/foreground/0",
      disabled ? "#747B8B" : "#000000",
      variableByName,
      stats,
    ),
  ];
  browse.textAlignVertical = "CENTER";
  instruction.appendChild(browse);

  const drop = figma.createText();
  drop.name = "Drop Text";
  await applyFieldTextTypography(
    drop,
    fonts,
    "FileUpload",
    variableByName,
    stats,
  );
  drop.characters = preservedText["Drop Text"] || "or drag and drop";
  drop.fills = [
    paintFromVariable(
      disabled ? "Colors/foreground/500" : "Colors/foreground/400",
      disabled ? "#747B8B" : "#5D626F",
      variableByName,
      stats,
    ),
  ];
  drop.textAlignVertical = "CENTER";
  instruction.appendChild(drop);
  dropzone.appendChild(instruction);

  const description = figma.createText();
  description.name = "Description Text";
  await applyFileUploadMetaTypography(
    description,
    fonts,
    variableByName,
    stats,
  );
  description.characters =
    preservedText["Description Text"] || "Images and PDFs up to 8 MB.";
  description.textAlignHorizontal = "CENTER";
  description.textAlignVertical = "CENTER";
  description.fills = [
    paintFromVariable(
      disabled ? "Colors/foreground/500" : "Colors/foreground/400",
      disabled ? "#747B8B" : "#5D626F",
      variableByName,
      stats,
    ),
  ];
  setTextAutoResize(description, "HEIGHT");
  description.resizeWithoutConstraints(320, 16);
  setLayoutSizingHorizontal(description, "FILL");
  dropzone.appendChild(description);

  syncFocusRing(dropzone, {
    enabled: !disabled,
    width: 384,
    height: 144,
    radius: 8,
    variableName: focusRingVariableForInputStatus(status),
    fallback: focusRingFallbackForInputStatus(status),
    variableByName,
    stats,
  });

  const fileRows = [];
  const icons = [uploadIcon];
  let fileList = null;
  if (hasFiles) {
    fileList = figma.createFrame();
    fileList.name = "Selected Files";
    fileList.layoutMode = "VERTICAL";
    fileList.primaryAxisSizingMode = "AUTO";
    fileList.counterAxisSizingMode = "FIXED";
    fileList.primaryAxisAlignItems = "MIN";
    fileList.counterAxisAlignItems = "MIN";
    setLayoutSizingHorizontal(fileList, "FILL");
    fileList.itemSpacing = 8;
    fileList.paddingLeft = 0;
    fileList.paddingRight = 0;
    fileList.paddingTop = 0;
    fileList.paddingBottom = 0;
    fileList.fills = [];
    fileList.strokes = [];
    fileList.clipsContent = false;

    const firstRow = await createFileUploadFileRow({
      index: 0,
      fileName: preservedText["File Name Text"] || "floor-plan.pdf",
      fileMeta: preservedText["File Meta Text"] || "2.4 MB",
      disabled,
      variableByName,
      fonts,
      stats,
    });
    fileRows.push(firstRow.row);
    icons.push(firstRow.fileIcon);
    icons.push(firstRow.removeIcon);
    fileList.appendChild(firstRow.row);

    const secondRow = await createFileUploadFileRow({
      index: 1,
      fileName: "site-photo.jpg",
      fileMeta: "842 KB",
      disabled,
      variableByName,
      fonts,
      stats,
    });
    fileRows.push(secondRow.row);
    icons.push(secondRow.fileIcon);
    icons.push(secondRow.removeIcon);
    fileList.appendChild(secondRow.row);
  }

  const helper = figma.createText();
  helper.name = "Helper Text";
  await applyFieldTextTypography(
    helper,
    fonts,
    "FileUpload",
    variableByName,
    stats,
  );
  helper.characters =
    preservedText["Helper Text"] || helperTextForInputStatus(status);
  helper.visible = status !== "Default";
  setVerticalStackChildSizing(helper);
  setTextAutoResize(helper, "HEIGHT");
  helper.resizeWithoutConstraints(384, 20);
  helper.fills = [
    paintFromVariable(
      config.helper,
      config.helperFallback,
      variableByName,
      stats,
    ),
  ];
  helper.textAlignVertical = "CENTER";

  bindFileUploadGeometryVariables(
    component,
    dropzone,
    fileRows,
    icons,
    variableByName,
    stats,
  );

  component.appendChild(label);
  component.appendChild(dropzone);
  if (fileList) component.appendChild(fileList);
  component.appendChild(helper);
  normalizeInputFamilyChildSizing(component);
}

async function createFileUploadFileRow({
  index,
  fileName,
  fileMeta,
  disabled,
  variableByName,
  fonts,
  stats,
}) {
  const row = figma.createFrame();
  row.name = `FileUpload File Row ${index + 1}`;
  row.layoutMode = "HORIZONTAL";
  row.primaryAxisSizingMode = "FIXED";
  row.counterAxisSizingMode = "FIXED";
  row.primaryAxisAlignItems = "MIN";
  row.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(row, "FILL");
  row.itemSpacing = 12;
  row.paddingLeft = 12;
  row.paddingRight = 12;
  row.paddingTop = 8;
  row.paddingBottom = 8;
  row.resizeWithoutConstraints(384, 44);
  row.cornerRadius = 8;
  row.clipsContent = false;
  row.setSharedPluginData(RUN_NAMESPACE, "kind", "file-upload-file-row");
  row.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  row.strokes = [
    paintFromVariable(
      "Colors/foreground/500",
      "#747B8B",
      variableByName,
      stats,
    ),
  ];
  row.strokeWeight = 1;

  const fileIcon = await createFixedIconInstance(
    "upload-01",
    disabled ? "Colors/foreground/500" : "Colors/theme/500",
    disabled ? "#747B8B" : "#135BEC",
    variableByName,
    stats,
    20,
    "FileUpload/file-icon/size",
  );
  fileIcon.setSharedPluginData(RUN_NAMESPACE, "role", "file-row-icon");
  row.appendChild(fileIcon);

  const copy = figma.createFrame();
  copy.name = "FileUpload File Copy";
  copy.layoutMode = "VERTICAL";
  copy.primaryAxisSizingMode = "AUTO";
  copy.counterAxisSizingMode = "FIXED";
  copy.primaryAxisAlignItems = "MIN";
  copy.counterAxisAlignItems = "MIN";
  setHorizontalStackFillChildSizing(copy);
  copy.itemSpacing = 0;
  copy.paddingLeft = 0;
  copy.paddingRight = 0;
  copy.paddingTop = 0;
  copy.paddingBottom = 0;
  copy.fills = [];
  copy.strokes = [];
  copy.clipsContent = false;

  const nameText = figma.createText();
  nameText.name = index === 0 ? "File Name Text" : "File Name Text 2";
  await applyFieldTextTypography(
    nameText,
    fonts,
    "FileUpload",
    variableByName,
    stats,
  );
  nameText.characters = fileName;
  // The copy block is a VERTICAL stack, so horizontal is its counter axis.
  // setHorizontalFillTextSizing ends on layoutAlign = "CENTER", which is the
  // right cross-axis answer inside a horizontal row and the wrong one here: it
  // overwrites the FILL set a line earlier and leaves the node frozen at its
  // own natural width. "2.4 MB" then rendered as "2.4" plus an ellipsis.
  setVerticalStackChildSizing(nameText);
  setTextAutoResize(nameText, "TRUNCATE");
  nameText.fills = [
    paintFromVariable(
      disabled ? "Colors/foreground/500" : "Colors/foreground/0",
      disabled ? "#747B8B" : "#000000",
      variableByName,
      stats,
    ),
  ];
  nameText.textAlignVertical = "CENTER";
  copy.appendChild(nameText);

  const metaText = figma.createText();
  metaText.name = index === 0 ? "File Meta Text" : "File Meta Text 2";
  await applyFileUploadMetaTypography(metaText, fonts, variableByName, stats);
  metaText.characters = fileMeta;
  setVerticalStackChildSizing(metaText);
  setTextAutoResize(metaText, "TRUNCATE");
  metaText.fills = [
    paintFromVariable(
      "Colors/foreground/400",
      "#5D626F",
      variableByName,
      stats,
    ),
  ];
  metaText.textAlignVertical = "CENTER";
  copy.appendChild(metaText);
  row.appendChild(copy);

  const removeIcon = await createFixedIconInstance(
    "x-close",
    disabled ? "Colors/foreground/500" : "Colors/foreground/400",
    disabled ? "#747B8B" : "#5D626F",
    variableByName,
    stats,
    20,
    "FileUpload/file-icon/size",
  );
  removeIcon.name = "Remove Icon";
  removeIcon.setSharedPluginData(RUN_NAMESPACE, "role", "file-row-icon");
  row.appendChild(removeIcon);

  return { row, fileIcon, removeIcon };
}

async function syncColorPickerVariantChildren({
  component,
  state,
  status,
  content,
  format,
  config,
  variableByName,
  fonts,
  stats,
}) {
  const preservedText = collectDescendantTextCharactersByName(component, [
    "Label Text",
    "Value Text",
    "Hex Label Text",
    "Hue Label Text",
    "Alpha Label Text",
    "Hue Value Text",
    "Saturation Value Text",
    "Lightness Value Text",
    "Red Value Text",
    "Green Value Text",
    "Blue Value Text",
    "Alpha Value Text",
    "Alpha Unit Text",
    "Palette Text",
    "Helper Text",
  ]);
  const existingChildren = Array.from(component.children || []);
  for (const child of existingChildren) {
    child.remove();
  }

  const disabled = state === "Disabled";
  const isOpen = content === "Open";
  const valueText = preservedText["Value Text"] || "#135BEC";

  const label = figma.createText();
  label.name = "Label Text";
  await applyFieldLabelTypography(
    label,
    fonts,
    "ColorPicker",
    variableByName,
    stats,
  );
  label.characters = preservedText["Label Text"] || "Brand color";
  label.fills = [
    paintFromVariable(
      config.label,
      config.labelFallback,
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";

  const field = figma.createFrame();
  field.name = "ColorPicker Field";
  field.layoutMode = "HORIZONTAL";
  field.primaryAxisSizingMode = "FIXED";
  field.counterAxisSizingMode = "FIXED";
  field.primaryAxisAlignItems = "MIN";
  field.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(field, "FILL");
  field.itemSpacing = 8;
  field.paddingLeft = 12;
  field.paddingRight = 12;
  field.paddingTop = 0;
  field.paddingBottom = 0;
  field.resizeWithoutConstraints(320, 44);
  field.cornerRadius = 8;
  field.setSharedPluginData(RUN_NAMESPACE, "kind", "input-field");
  field.fills = [
    paintFromVariable(
      config.fieldFill,
      config.fieldFillFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokes = [
    paintFromVariable(
      config.fieldStroke,
      config.fieldStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokeWeight = 1;
  field.clipsContent = false;

  const fieldSwatch = createColorPickerSwatch({
    name: "Selected Color Swatch",
    value: valueText,
    disabled,
    selected: false,
    size: 24,
    variableByName,
    stats,
  });
  field.appendChild(fieldSwatch);

  const value = figma.createText();
  value.name = "Value Text";
  await applyFieldTextTypography(
    value,
    fonts,
    "ColorPicker",
    variableByName,
    stats,
  );
  value.characters = valueText;
  setHorizontalFillTextSizing(value);
  setTextAutoResize(value, "TRUNCATE");
  value.fills = [
    paintFromVariable(
      disabled ? config.placeholder : "Colors/foreground/0",
      disabled ? config.placeholderFallback : "#000000",
      variableByName,
      stats,
    ),
  ];
  value.textAlignVertical = "CENTER";
  field.appendChild(value);

  const trailingIcon = await createFixedIconInstance(
    isOpen ? "chevron-up" : "chevron-down",
    config.placeholder,
    config.placeholderFallback,
    variableByName,
    stats,
    16,
    "ColorPicker/icon/size",
  );
  field.appendChild(trailingIcon);

  syncFocusRing(field, {
    enabled: state !== "Disabled",
    width: 320,
    height: 44,
    radius: 8,
    variableName: focusRingVariableForInputStatus(status),
    fallback: focusRingFallbackForInputStatus(status),
    variableByName,
    stats,
  });

  let popover = null;
  let colorArea = null;
  const sliderRails = [];
  const sliderTracks = [];
  const sliderThumbs = [];
  const swatches = [fieldSwatch];
  const presets = [];
  const icons = [trailingIcon];

  if (isOpen) {
    const builtPopover = await createColorPickerPopover({
      valueText,
      format,
      hexLabelText: preservedText["Hex Label Text"] || "HEX",
      hueLabelText: preservedText["Hue Label Text"] || "Hue",
      alphaLabelText: preservedText["Alpha Label Text"] || "Opacity",
      hueValueText: preservedText["Hue Value Text"] || "0",
      saturationValueText: preservedText["Saturation Value Text"] || "0",
      lightnessValueText: preservedText["Lightness Value Text"] || "85",
      redValueText: preservedText["Red Value Text"] || "19",
      greenValueText: preservedText["Green Value Text"] || "91",
      blueValueText: preservedText["Blue Value Text"] || "236",
      alphaValueText: preservedText["Alpha Value Text"] || "100",
      paletteText: preservedText["Palette Text"] || "Kozmos Design System 2.0",
      disabled,
      variableByName,
      fonts,
      stats,
    });
    popover = builtPopover.popover;
    colorArea = builtPopover.colorArea;
    for (const swatch of builtPopover.swatches) swatches.push(swatch);
    for (const preset of builtPopover.presets) presets.push(preset);
    for (const rail of builtPopover.sliderRails) sliderRails.push(rail);
    for (const track of builtPopover.sliderTracks) sliderTracks.push(track);
    for (const thumb of builtPopover.sliderThumbs) sliderThumbs.push(thumb);
    for (const icon of builtPopover.icons) icons.push(icon);
  }

  const helper = figma.createText();
  helper.name = "Helper Text";
  await applyFieldTextTypography(
    helper,
    fonts,
    "ColorPicker",
    variableByName,
    stats,
  );
  helper.characters =
    preservedText["Helper Text"] || helperTextForInputStatus(status);
  helper.visible = status !== "Default";
  setVerticalStackChildSizing(helper);
  setTextAutoResize(helper, "HEIGHT");
  helper.resizeWithoutConstraints(320, 20);
  helper.fills = [
    paintFromVariable(
      config.helper,
      config.helperFallback,
      variableByName,
      stats,
    ),
  ];
  helper.textAlignVertical = "CENTER";

  bindColorPickerGeometryVariables(
    component,
    field,
    swatches,
    popover,
    colorArea,
    sliderRails,
    sliderTracks,
    sliderThumbs,
    presets,
    icons,
    variableByName,
    stats,
  );

  component.appendChild(label);
  component.appendChild(field);
  if (popover) component.appendChild(popover);
  component.appendChild(helper);
  normalizeInputFamilyChildSizing(component);
  resizeVerticalAutoLayoutFrameToVisibleChildren(component, 320);
}

function createColorPickerSwatch({
  name,
  value,
  disabled,
  selected,
  size,
  variableByName,
  stats,
}) {
  const swatch = figma.createFrame();
  swatch.name = name;
  swatch.layoutMode = "HORIZONTAL";
  swatch.primaryAxisSizingMode = "FIXED";
  swatch.counterAxisSizingMode = "FIXED";
  swatch.primaryAxisAlignItems = "CENTER";
  swatch.counterAxisAlignItems = "CENTER";
  swatch.itemSpacing = 0;
  swatch.paddingLeft = 0;
  swatch.paddingRight = 0;
  swatch.paddingTop = 0;
  swatch.paddingBottom = 0;
  swatch.resizeWithoutConstraints(size, size);
  swatch.cornerRadius = 6;
  swatch.clipsContent = false;
  swatch.opacity = disabled ? 0.56 : 1;
  swatch.setSharedPluginData(RUN_NAMESPACE, "kind", "colorpicker-swatch");
  swatch.fills = [paintFromHex(colorPickerSafeHex(value))];
  swatch.strokes = [
    paintFromVariable(
      selected ? "Colors/theme/500" : "Colors/foreground/500",
      selected ? "#135BEC" : "#747B8B",
      variableByName,
      stats,
    ),
  ];
  swatch.strokeWeight = selected ? 2 : 1;
  return swatch;
}

function colorPickerSafeHex(value) {
  const raw = typeof value === "string" ? value.trim() : "";
  const hex = raw.replace(/^#/, "");

  if (/^[0-9a-fA-F]{3}$/.test(hex)) {
    return `#${hex
      .split("")
      .map((part) => part + part)
      .join("")
      .toUpperCase()}`;
  }

  if (/^[0-9a-fA-F]{6}$/.test(hex)) {
    return `#${hex.toUpperCase()}`;
  }

  return "#135BEC";
}

function colorPickerGradientPaint(direction, gradientStops) {
  return {
    type: "GRADIENT_LINEAR",
    gradientTransform:
      direction === "vertical"
        ? [
            [0, 1, 0],
            [-1, 0, 1],
          ]
        : [
            [1, 0, 0],
            [0, 1, 0],
          ],
    gradientStops,
  };
}

async function createColorPickerPopover({
  valueText,
  format,
  hexLabelText,
  hueLabelText,
  alphaLabelText,
  hueValueText,
  saturationValueText,
  lightnessValueText,
  redValueText,
  greenValueText,
  blueValueText,
  alphaValueText,
  paletteText,
  disabled,
  variableByName,
  fonts,
  stats,
}) {
  const popover = figma.createFrame();
  popover.name = "ColorPicker Popover";
  popover.layoutMode = "VERTICAL";
  popover.primaryAxisSizingMode = "AUTO";
  popover.counterAxisSizingMode = "FIXED";
  popover.primaryAxisAlignItems = "MIN";
  popover.counterAxisAlignItems = "MIN";
  setLayoutSizingHorizontal(popover, "FILL");
  popover.itemSpacing = 12;
  popover.paddingLeft = 12;
  popover.paddingRight = 12;
  popover.paddingTop = 12;
  popover.paddingBottom = 12;
  popover.resizeWithoutConstraints(320, 350);
  popover.cornerRadius = 8;
  popover.clipsContent = false;
  popover.setSharedPluginData(RUN_NAMESPACE, "kind", "colorpicker-popover");
  popover.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  popover.strokes = [
    paintFromVariable(
      "Colors/foreground/500",
      "#747B8B",
      variableByName,
      stats,
    ),
  ];
  popover.strokeWeight = 1;
  popover.effects = tooltipShadowEffects();

  const builtColorArea = createColorPickerColorArea({
    valueText,
    disabled,
    variableByName,
    stats,
  });
  const colorArea = builtColorArea.area;
  const areaHandle = builtColorArea.handle;
  popover.appendChild(colorArea);

  const hue = await createColorPickerSlider({
    labelName: "Hue Label Text",
    labelText: hueLabelText,
    trackName: "ColorPicker Hue Track",
    fill: "#135BEC",
    thumbX: 220,
    disabled,
    variableByName,
    fonts,
    stats,
  });
  popover.appendChild(hue.row);

  const alpha = await createColorPickerSlider({
    labelName: "Alpha Label Text",
    labelText: alphaLabelText,
    trackName: "ColorPicker Opacity Track",
    fill: "#8FB4FF",
    thumbX: 262,
    disabled,
    variableByName,
    fonts,
    stats,
  });
  popover.appendChild(alpha.row);

  const valueRow = await createColorPickerValueRow({
    format,
    hexLabelText,
    valueText,
    hueValueText,
    saturationValueText,
    lightnessValueText,
    redValueText,
    greenValueText,
    blueValueText,
    alphaValueText,
    disabled,
    variableByName,
    fonts,
    stats,
  });
  popover.appendChild(valueRow);

  const paletteSelect = await createColorPickerPaletteSelect({
    paletteText,
    disabled,
    variableByName,
    fonts,
    stats,
  });
  popover.appendChild(paletteSelect);

  const paletteGrid = createColorPickerPresetGrid({
    disabled,
    variableByName,
    stats,
  });
  popover.appendChild(paletteGrid.grid);
  resizeVerticalAutoLayoutFrameToVisibleChildren(popover, 320);

  return {
    popover,
    colorArea,
    swatches: [],
    presets: paletteGrid.presets,
    sliderRails: [hue.rail, alpha.rail].filter(Boolean),
    sliderTracks: [hue.track, alpha.track].filter(Boolean),
    sliderThumbs: [areaHandle, hue.thumb, alpha.thumb].filter(Boolean),
    icons: [],
  };
}

async function createColorPickerValueRow({
  format,
  hexLabelText,
  valueText,
  hueValueText,
  saturationValueText,
  lightnessValueText,
  redValueText,
  greenValueText,
  blueValueText,
  alphaValueText,
  disabled,
  variableByName,
  fonts,
  stats,
}) {
  const resolvedFormat =
    COLOR_PICKER_FORMATS.indexOf(format) === -1 ? "HSL" : format;
  const stack = figma.createFrame();
  stack.name = "ColorPicker Value Stack";
  stack.layoutMode = "VERTICAL";
  stack.primaryAxisSizingMode = "AUTO";
  stack.counterAxisSizingMode = "FIXED";
  stack.primaryAxisAlignItems = "MIN";
  stack.counterAxisAlignItems = "MIN";
  setLayoutSizingHorizontal(stack, "FILL");
  setLayoutSizingVertical(stack, "HUG");
  stack.itemSpacing = 8;
  stack.paddingLeft = 0;
  stack.paddingRight = 0;
  stack.paddingTop = 0;
  stack.paddingBottom = 0;
  stack.resizeWithoutConstraints(296, 80);
  stack.fills = [];
  stack.strokes = [];
  stack.clipsContent = false;
  stack.setSharedPluginData(RUN_NAMESPACE, "kind", "colorpicker-value-stack");

  const channelRow = figma.createFrame();
  channelRow.name = "ColorPicker Channel Row";
  channelRow.layoutMode = "HORIZONTAL";
  channelRow.primaryAxisSizingMode = "FIXED";
  channelRow.counterAxisSizingMode = "FIXED";
  channelRow.primaryAxisAlignItems = "MIN";
  channelRow.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(channelRow, "FILL");
  setLayoutSizingVertical(channelRow, "HUG");
  channelRow.itemSpacing = 6;
  channelRow.paddingLeft = 0;
  channelRow.paddingRight = 0;
  channelRow.paddingTop = 0;
  channelRow.paddingBottom = 0;
  channelRow.resizeWithoutConstraints(296, 36);
  channelRow.fills = [];
  channelRow.strokes = [];
  channelRow.clipsContent = false;

  const modeField = await createColorPickerMiniField({
    name: "ColorPicker Mode Field",
    textName: "Mode Text",
    text: resolvedFormat,
    width: 56,
    sizing: "fixed",
    disabled,
    variableByName,
    fonts,
    stats,
  });
  channelRow.appendChild(modeField);

  if (resolvedFormat === "HEX") {
    const hexField = await createColorPickerMiniField({
      name: "ColorPicker Hex Field",
      textName: "Value Text",
      text: valueText || hexLabelText,
      width: 112,
      sizing: "horizontal-fill",
      disabled,
      variableByName,
      fonts,
      stats,
    });
    channelRow.appendChild(hexField);
  } else {
    const channelValues =
      resolvedFormat === "RGB"
        ? [
            { name: "Red Value Text", text: redValueText || "19", width: 42 },
            {
              name: "Green Value Text",
              text: greenValueText || "91",
              width: 42,
            },
            {
              name: "Blue Value Text",
              text: blueValueText || "236",
              width: 42,
            },
          ]
        : [
            { name: "Hue Value Text", text: hueValueText || "0", width: 42 },
            {
              name: "Saturation Value Text",
              text: saturationValueText || "0",
              width: 42,
            },
            {
              name: "Lightness Value Text",
              text: lightnessValueText || "85",
              width: 42,
            },
          ];
    for (const value of channelValues) {
      const field = await createColorPickerMiniField({
        name: `ColorPicker ${value.name.replace(" Text", "")} Field`,
        textName: value.name,
        text: value.text,
        width: value.width,
        sizing: "fixed",
        disabled,
        variableByName,
        fonts,
        stats,
      });
      channelRow.appendChild(field);
    }
  }

  const alphaField = await createColorPickerMiniField({
    name: "ColorPicker Alpha Value Field",
    textName: "Alpha Value Text",
    text: alphaValueText || "100",
    width: 44,
    sizing: "fixed",
    disabled,
    variableByName,
    fonts,
    stats,
  });
  channelRow.appendChild(alphaField);

  const alphaUnit = figma.createText();
  alphaUnit.name = "Alpha Unit Text";
  await applyColorPickerMetaTypography(alphaUnit, fonts, variableByName, stats);
  alphaUnit.characters = "%";
  alphaUnit.fills = [
    paintFromVariable(
      disabled ? "Colors/foreground/500" : "Colors/foreground/400",
      disabled ? "#747B8B" : "#5D626F",
      variableByName,
      stats,
    ),
  ];
  alphaUnit.textAlignVertical = "CENTER";
  channelRow.appendChild(alphaUnit);
  stack.appendChild(channelRow);
  resizeVerticalAutoLayoutFrameToVisibleChildren(stack, 296);

  return stack;
}

async function createColorPickerMiniField({
  name,
  textName,
  text,
  width,
  sizing,
  disabled,
  variableByName,
  fonts,
  stats,
}) {
  const field = figma.createFrame();
  field.name = name;
  field.layoutMode = "HORIZONTAL";
  field.primaryAxisSizingMode = "FIXED";
  field.counterAxisSizingMode = "FIXED";
  field.primaryAxisAlignItems = "CENTER";
  field.counterAxisAlignItems = "CENTER";
  if (sizing === "vertical-fill") {
    setVerticalStackChildSizing(field);
  } else if (sizing === "horizontal-fill") {
    setHorizontalStackFillHugChildSizing(field);
  } else {
    setFixedChildSizing(field);
  }
  field.itemSpacing = 0;
  field.paddingLeft = 8;
  field.paddingRight = 8;
  field.paddingTop = 0;
  field.paddingBottom = 0;
  field.resizeWithoutConstraints(width, 36);
  field.cornerRadius = 8;
  field.clipsContent = false;
  field.setSharedPluginData(RUN_NAMESPACE, "kind", "colorpicker-compact-field");
  field.fills = [
    paintFromVariable(
      disabled ? "Colors/background/100" : "Colors/background/0",
      disabled ? "#E3E4E8" : "#FFFFFF",
      variableByName,
      stats,
    ),
  ];
  field.strokes = [
    paintFromVariable(
      "Colors/foreground/500",
      "#747B8B",
      variableByName,
      stats,
    ),
  ];
  field.strokeWeight = 1;

  const label = figma.createText();
  label.name = textName;
  await applyColorPickerMetaTypography(label, fonts, variableByName, stats);
  label.characters = text;
  setHorizontalFillTextSizing(label);
  setTextAutoResize(label, "TRUNCATE");
  label.fills = [
    paintFromVariable(
      disabled ? "Colors/foreground/500" : "Colors/foreground/0",
      disabled ? "#747B8B" : "#000000",
      variableByName,
      stats,
    ),
  ];
  label.textAlignHorizontal = "CENTER";
  label.textAlignVertical = "CENTER";
  field.appendChild(label);
  return field;
}

async function createColorPickerPaletteSelect({
  paletteText,
  disabled,
  variableByName,
  fonts,
  stats,
}) {
  const created = await createNestedComponentInstance({
    componentSetName: "Select",
    variantProperties: {
      State: disabled ? "Disabled" : "Default",
      Status: "Default",
    },
    name: "ColorPicker Palette Select",
    role: "palette-select",
    stats,
  });

  if (created) {
    const selectInstance = created.instance;
    try {
      selectInstance.setProperties({
        State: disabled ? "Disabled" : "Default",
        Status: "Default",
      });
    } catch (error) {
      stats.warnings.push(
        `ColorPicker Palette Select: could not set Select variant properties (${messageFor(error)}).`,
      );
    }
    setInstanceTextProperty(
      selectInstance,
      created.componentSet,
      "Placeholder Text",
      paletteText,
      stats,
    );
    setVerticalFixedFillChildSizing(selectInstance);
    resizeNodeWithoutConstraints(selectInstance, 296, 44);
    return selectInstance;
  }

  const select = figma.createFrame();
  select.name = "ColorPicker Palette Select";
  select.layoutMode = "HORIZONTAL";
  select.primaryAxisSizingMode = "FIXED";
  select.counterAxisSizingMode = "FIXED";
  select.primaryAxisAlignItems = "MIN";
  select.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(select, "FILL");
  setLayoutSizingVertical(select, "FIXED");
  select.itemSpacing = 8;
  select.paddingLeft = 12;
  select.paddingRight = 12;
  select.paddingTop = 0;
  select.paddingBottom = 0;
  select.resizeWithoutConstraints(296, 36);
  select.cornerRadius = 8;
  select.clipsContent = false;
  select.setSharedPluginData(RUN_NAMESPACE, "kind", "colorpicker-select");
  select.fills = [
    paintFromVariable(
      disabled ? "Colors/background/100" : "Colors/background/0",
      disabled ? "#E3E4E8" : "#FFFFFF",
      variableByName,
      stats,
    ),
  ];
  select.strokes = [
    paintFromVariable(
      "Colors/foreground/500",
      "#747B8B",
      variableByName,
      stats,
    ),
  ];
  select.strokeWeight = 1;

  const label = figma.createText();
  label.name = "Palette Text";
  await applyFieldTextTypography(
    label,
    fonts,
    "ColorPicker",
    variableByName,
    stats,
  );
  label.characters = paletteText;
  setHorizontalFillTextSizing(label);
  setTextAutoResize(label, "TRUNCATE");
  label.fills = [
    paintFromVariable(
      disabled ? "Colors/foreground/500" : "Colors/foreground/0",
      disabled ? "#747B8B" : "#000000",
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";
  select.appendChild(label);

  const icon = await createFixedIconInstance(
    "chevron-down",
    disabled ? "Colors/foreground/500" : "Colors/foreground/400",
    disabled ? "#747B8B" : "#5D626F",
    variableByName,
    stats,
    16,
    "ColorPicker/icon/size",
  );
  select.appendChild(icon);
  return select;
}

function createColorPickerPresetGrid({ disabled, variableByName, stats }) {
  const grid = figma.createFrame();
  grid.name = "ColorPicker Presets";
  grid.layoutMode = "VERTICAL";
  grid.primaryAxisSizingMode = "AUTO";
  grid.counterAxisSizingMode = "FIXED";
  grid.primaryAxisAlignItems = "MIN";
  grid.counterAxisAlignItems = "MIN";
  setLayoutSizingHorizontal(grid, "FILL");
  setLayoutSizingVertical(grid, "HUG");
  grid.itemSpacing = 8;
  grid.paddingLeft = 0;
  grid.paddingRight = 0;
  grid.paddingTop = 0;
  grid.paddingBottom = 0;
  grid.fills = [];
  grid.strokes = [];
  grid.clipsContent = false;

  const presetValues = [
    "#135BEC",
    "#8FB4FF",
    "#7C3AED",
    "#0F766E",
    "#1492A6",
    "#2E97CC",
    "#0EA5E9",
    "#38BDF8",
    "#C2410C",
    "#B91C1C",
    "#A16207",
    "#CA8A04",
    "#15803D",
    "#22C55E",
    "#BE185D",
    "#EC4899",
    "#4F46E5",
    "#6366F1",
    "#7E22CE",
    "#A855F7",
    "#111827",
    "#374151",
    "#747B8B",
    "#C7CAD1",
  ];
  const columns = 8;
  const swatchSize = 30;
  const gap = 8;
  const rowCount = Math.ceil(presetValues.length / columns);
  grid.resizeWithoutConstraints(
    296,
    rowCount * swatchSize + (rowCount - 1) * gap,
  );
  const presets = [];
  for (let rowIndex = 0; rowIndex < rowCount; rowIndex += 1) {
    const row = figma.createFrame();
    row.name = `ColorPicker Preset Row ${rowIndex + 1}`;
    row.layoutMode = "HORIZONTAL";
    row.primaryAxisSizingMode = "FIXED";
    row.counterAxisSizingMode = "FIXED";
    row.primaryAxisAlignItems = "MIN";
    row.counterAxisAlignItems = "CENTER";
    setLayoutSizingHorizontal(row, "FILL");
    row.itemSpacing = gap;
    row.paddingLeft = 0;
    row.paddingRight = 0;
    row.paddingTop = 0;
    row.paddingBottom = 0;
    row.resizeWithoutConstraints(296, swatchSize);
    row.fills = [];
    row.strokes = [];
    row.clipsContent = false;

    for (let columnIndex = 0; columnIndex < columns; columnIndex += 1) {
      const index = rowIndex * columns + columnIndex;
      if (!presetValues[index]) continue;
      const preset = createColorPickerSwatch({
        name: `Preset Swatch ${index + 1}`,
        value: presetValues[index],
        disabled,
        selected: index === 0,
        size: swatchSize,
        variableByName,
        stats,
      });
      presets.push(preset);
      row.appendChild(preset);
    }
    grid.appendChild(row);
  }

  return { grid, presets };
}

function createColorPickerColorArea({
  valueText,
  disabled,
  variableByName,
  stats,
}) {
  const area = figma.createFrame();
  area.name = "ColorPicker Color Area";
  area.layoutMode = "NONE";
  setLayoutSizingHorizontal(area, "FILL");
  area.resizeWithoutConstraints(296, 160);
  area.cornerRadius = 8;
  area.clipsContent = false;
  area.setSharedPluginData(RUN_NAMESPACE, "kind", "colorpicker-color-area");
  area.fills = disabled
    ? [
        paintFromVariable(
          "Colors/background/200",
          "#C7CAD1",
          variableByName,
          stats,
        ),
      ]
    : [
        paintFromHex(colorPickerSafeHex(valueText)),
        colorPickerGradientPaint("horizontal", [
          { position: 0, color: { r: 1, g: 1, b: 1, a: 1 } },
          { position: 1, color: { r: 1, g: 1, b: 1, a: 0 } },
        ]),
        colorPickerGradientPaint("vertical", [
          { position: 0, color: { r: 0, g: 0, b: 0, a: 0 } },
          { position: 1, color: { r: 0, g: 0, b: 0, a: 1 } },
        ]),
      ];
  area.strokes = [
    paintFromVariable(
      "Colors/foreground/500",
      "#747B8B",
      variableByName,
      stats,
    ),
  ];
  area.strokeWeight = 1;

  const handle = figma.createEllipse();
  handle.name = "Color Area Handle";
  handle.resizeWithoutConstraints(20, 20);
  handle.x = 210;
  handle.y = 66;
  try {
    handle.constraints = { horizontal: "SCALE", vertical: "SCALE" };
  } catch (_error) {
    // Constraints are unavailable on some older plugin runtimes.
  }
  handle.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  handle.strokes = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  handle.strokeWeight = 1;
  handle.setSharedPluginData(RUN_NAMESPACE, "kind", "colorpicker-slider-thumb");
  area.appendChild(handle);
  return { area, handle };
}

async function createColorPickerSlider({
  labelName,
  labelText,
  trackName,
  fill,
  thumbX,
  disabled,
  variableByName,
  fonts,
  stats,
}) {
  const created = await createNestedComponentInstance({
    componentSetName: "Slider",
    variantProperties: {
      State: disabled ? "Disabled" : "Default",
      Status: "Default",
    },
    name: `${labelText} Slider`,
    role: "slider",
    stats,
  });

  if (created) {
    const sliderInstance = created.instance;
    try {
      sliderInstance.setProperties({
        State: disabled ? "Disabled" : "Default",
        Status: "Default",
      });
    } catch (error) {
      stats.warnings.push(
        `ColorPicker ${labelText} Slider: could not set Slider variant properties (${messageFor(error)}).`,
      );
    }
    setInstanceTextProperty(
      sliderInstance,
      created.componentSet,
      "Label Text",
      labelText,
      stats,
    );
    setInstanceBooleanProperty(
      sliderInstance,
      created.componentSet,
      "Focus Visible",
      false,
      stats,
    );
    setVerticalStackChildSizing(sliderInstance);
    resizeNodeWithoutConstraints(
      sliderInstance,
      296,
      sliderInstance.height || 70,
    );
    return { row: sliderInstance, rail: null, track: null, thumb: null };
  }

  const row = figma.createFrame();
  row.name = `${labelText} Slider`;
  row.layoutMode = "VERTICAL";
  row.primaryAxisSizingMode = "AUTO";
  row.counterAxisSizingMode = "FIXED";
  row.primaryAxisAlignItems = "MIN";
  row.counterAxisAlignItems = "MIN";
  setLayoutSizingHorizontal(row, "FILL");
  row.itemSpacing = 4;
  row.paddingLeft = 0;
  row.paddingRight = 0;
  row.paddingTop = 0;
  row.paddingBottom = 0;
  row.resizeWithoutConstraints(296, 48);
  row.fills = [];
  row.strokes = [];
  row.clipsContent = false;

  const label = figma.createText();
  label.name = labelName;
  await applyColorPickerMetaTypography(label, fonts, variableByName, stats);
  label.characters = labelText;
  label.fills = [
    paintFromVariable(
      disabled ? "Colors/foreground/500" : "Colors/foreground/400",
      disabled ? "#747B8B" : "#5D626F",
      variableByName,
      stats,
    ),
  ];
  label.textAlignVertical = "CENTER";
  row.appendChild(label);

  const rail = figma.createFrame();
  rail.name = `${labelText} Rail`;
  rail.layoutMode = "NONE";
  setLayoutSizingHorizontal(rail, "FILL");
  rail.resizeWithoutConstraints(296, 24);
  rail.fills = [];
  rail.strokes = [];
  rail.clipsContent = false;

  const track = figma.createFrame();
  track.name = trackName;
  track.resizeWithoutConstraints(296, 8);
  track.x = 0;
  track.y = 8;
  try {
    track.constraints = { horizontal: "STRETCH", vertical: "CENTER" };
  } catch (_error) {
    // Constraints are unavailable on some older plugin runtimes.
  }
  track.cornerRadius = 999;
  track.fills = disabled
    ? [
        paintFromVariable(
          "Colors/background/200",
          "#C7CAD1",
          variableByName,
          stats,
        ),
      ]
    : [paintFromHex(fill)];
  track.strokes = [
    paintFromVariable(
      "Colors/foreground/500",
      "#747B8B",
      variableByName,
      stats,
    ),
  ];
  track.strokeWeight = 1;
  track.setSharedPluginData(RUN_NAMESPACE, "kind", "colorpicker-slider-track");
  rail.appendChild(track);

  const thumb = figma.createEllipse();
  thumb.name = `${labelText} Thumb`;
  thumb.resizeWithoutConstraints(20, 20);
  thumb.x = thumbX;
  thumb.y = 2;
  try {
    thumb.constraints = { horizontal: "SCALE", vertical: "CENTER" };
  } catch (_error) {
    // Constraints are unavailable on some older plugin runtimes.
  }
  thumb.fills = [
    paintFromVariable("Surface/0", "#FFFFFF", variableByName, stats),
  ];
  thumb.strokes = [
    paintFromVariable(
      disabled ? "Colors/foreground/500" : "Colors/theme/500",
      disabled ? "#747B8B" : "#135BEC",
      variableByName,
      stats,
    ),
  ];
  thumb.strokeWeight = 2;
  thumb.setSharedPluginData(RUN_NAMESPACE, "kind", "colorpicker-slider-thumb");
  rail.appendChild(thumb);
  row.appendChild(rail);

  return { row, rail, track, thumb };
}

function numberInputValueColorConfig(state, config) {
  if (state === "Disabled") {
    return { token: config.placeholder, fallback: config.placeholderFallback };
  }

  return { token: "Colors/foreground/0", fallback: "#000000" };
}

async function createNumberInputStepper({
  name,
  iconName,
  colorToken,
  colorFallback,
  variableByName,
  stats,
}) {
  const stepper = figma.createFrame();
  stepper.name = name;
  stepper.layoutMode = "HORIZONTAL";
  stepper.primaryAxisSizingMode = "FIXED";
  stepper.counterAxisSizingMode = "FIXED";
  stepper.primaryAxisAlignItems = "CENTER";
  stepper.counterAxisAlignItems = "CENTER";
  stepper.itemSpacing = 0;
  stepper.paddingLeft = 0;
  stepper.paddingRight = 0;
  stepper.paddingTop = 0;
  stepper.paddingBottom = 0;
  stepper.resizeWithoutConstraints(44, 44);
  stepper.fills = [];
  stepper.strokes = [];
  stepper.clipsContent = false;
  setFixedChildSizing(stepper);

  const icon = await createFixedIconInstance(
    iconName,
    colorToken,
    colorFallback,
    variableByName,
    stats,
    16,
    "NumberInput/stepper/icon/size",
  );
  stepper.appendChild(icon);
  setFixedChildSizing(icon);
  return stepper;
}

function createNumberInputDivider(config, variableByName, stats) {
  const divider = figma.createRectangle();
  divider.name = "NumberInput Divider";
  divider.resizeWithoutConstraints(1, 44);
  divider.fills = [
    paintFromVariable(
      config.fieldStroke,
      config.fieldStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  divider.strokes = [];
  bindFloatVariable(
    divider,
    "width",
    "NumberInput/divider/width",
    variableByName,
    stats,
  );
  bindFloatVariable(
    divider,
    "height",
    "NumberInput/field/height",
    variableByName,
    stats,
  );
  setFixedChildSizing(divider);
  return divider;
}

async function syncTextareaVariantChildren({
  component,
  state,
  status,
  config,
  variableByName,
  fonts,
  stats,
}) {
  const label = await syncFieldLabel({
    component,
    text: "Textarea",
    tokenPrefix: "Textarea",
    colorToken: config.label,
    colorFallback: config.labelFallback,
    variableByName,
    fonts,
    stats,
  });

  let field = directChildNamed(component, "Textarea Field");
  if (field && field.type !== "FRAME") {
    field.remove();
    field = null;
  }

  if (!field || field.type !== "FRAME") {
    field = figma.createFrame();
    field.name = "Textarea Field";
  }

  field.layoutMode = "VERTICAL";
  field.primaryAxisSizingMode = "FIXED";
  field.counterAxisSizingMode = "FIXED";
  field.primaryAxisAlignItems = "MIN";
  field.counterAxisAlignItems = "MIN";
  setLayoutSizingHorizontal(field, "FILL");
  field.itemSpacing = 0;
  field.paddingLeft = 12;
  field.paddingRight = 12;
  field.paddingTop = 8;
  field.paddingBottom = 8;
  field.resizeWithoutConstraints(320, 80);
  field.cornerRadius = 8;
  field.setSharedPluginData(RUN_NAMESPACE, "kind", "input-field");
  field.fills = [
    paintFromVariable(
      config.fieldFill,
      config.fieldFillFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokes = [
    paintFromVariable(
      config.fieldStroke,
      config.fieldStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokeWeight = 1;
  field.clipsContent = false;

  let placeholder = directChildNamed(field, "Placeholder Text");
  if (placeholder && placeholder.type !== "TEXT") {
    placeholder.remove();
    placeholder = null;
  }

  if (!placeholder || placeholder.type !== "TEXT") {
    placeholder = figma.createText();
    placeholder.name = "Placeholder Text";
  }

  await applyFieldTextTypography(
    placeholder,
    fonts,
    "Textarea",
    variableByName,
    stats,
  );
  placeholder.characters =
    status === "Error" ? "Describe the issue" : "Placeholder";
  setVerticalStackChildSizing(placeholder);
  setTextAutoResize(placeholder, "HEIGHT");
  placeholder.resizeWithoutConstraints(296, 40);
  placeholder.fills = [
    paintFromVariable(
      config.placeholder,
      config.placeholderFallback,
      variableByName,
      stats,
    ),
  ];
  placeholder.textAlignVertical = "TOP";
  field.appendChild(placeholder);

  syncFocusRing(field, {
    enabled: state !== "Disabled",
    width: 320,
    height: 80,
    radius: 8,
    variableName: focusRingVariableForInputStatus(status),
    fallback: focusRingFallbackForInputStatus(status),
    variableByName,
    stats,
  });

  bindTextareaGeometryVariables(component, field, variableByName, stats);

  component.appendChild(label);
  component.appendChild(field);
  normalizeInputFamilyChildSizing(component);
}

async function syncSearchVariantChildren({
  component,
  state,
  status,
  config,
  variableByName,
  fonts,
  stats,
}) {
  const label = await syncFieldLabel({
    component,
    text: "Search",
    tokenPrefix: "Search",
    colorToken: config.label,
    colorFallback: config.labelFallback,
    variableByName,
    fonts,
    stats,
  });

  let field = directChildNamed(component, "Search Field");
  if (field && field.type !== "FRAME") {
    field.remove();
    field = null;
  }

  if (!field || field.type !== "FRAME") {
    field = figma.createFrame();
    field.name = "Search Field";
  }

  field.layoutMode = "HORIZONTAL";
  field.primaryAxisSizingMode = "FIXED";
  field.counterAxisSizingMode = "FIXED";
  field.primaryAxisAlignItems = "MIN";
  field.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(field, "FILL");
  field.itemSpacing = 8;
  field.paddingLeft = 12;
  field.paddingRight = 12;
  field.paddingTop = 0;
  field.paddingBottom = 0;
  field.resizeWithoutConstraints(320, 44);
  field.cornerRadius = 8;
  field.setSharedPluginData(RUN_NAMESPACE, "kind", "input-field");
  field.fills = [
    paintFromVariable(
      config.fieldFill,
      config.fieldFillFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokes = [
    paintFromVariable(
      config.fieldStroke,
      config.fieldStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  field.strokeWeight = 1;
  field.clipsContent = false;

  removeGeneratedButtonChild(field, "Icon", true);
  const icon = await createFixedIconInstance(
    "search-md",
    config.placeholder,
    config.placeholderFallback,
    variableByName,
    stats,
    16,
    "Search/icon/size",
  );
  field.appendChild(icon);

  let placeholder = directChildNamed(field, "Placeholder Text");
  if (placeholder && placeholder.type !== "TEXT") {
    placeholder.remove();
    placeholder = null;
  }

  if (!placeholder || placeholder.type !== "TEXT") {
    placeholder = figma.createText();
    placeholder.name = "Placeholder Text";
  }

  await applyFieldTextTypography(
    placeholder,
    fonts,
    "Search",
    variableByName,
    stats,
  );
  placeholder.characters = "Search";
  setHorizontalFillTextSizing(placeholder);
  setTextAutoResize(placeholder, "TRUNCATE");
  placeholder.fills = [
    paintFromVariable(
      config.placeholder,
      config.placeholderFallback,
      variableByName,
      stats,
    ),
  ];
  placeholder.textAlignVertical = "CENTER";
  field.appendChild(placeholder);

  syncFocusRing(field, {
    enabled: state !== "Disabled",
    width: 320,
    height: 44,
    radius: 8,
    variableName: focusRingVariableForInputStatus(status),
    fallback: focusRingFallbackForInputStatus(status),
    variableByName,
    stats,
  });

  bindSearchGeometryVariables(component, field, icon, variableByName, stats);

  component.appendChild(label);
  component.appendChild(field);
  normalizeInputFamilyChildSizing(component);
}

async function syncSelectVariantChildren({
  component,
  state,
  status,
  config,
  variableByName,
  fonts,
  stats,
}) {
  let trigger = directChildNamed(component, "Select Trigger");
  if (trigger && trigger.type !== "FRAME") {
    trigger.remove();
    trigger = null;
  }

  if (!trigger || trigger.type !== "FRAME") {
    trigger = figma.createFrame();
    trigger.name = "Select Trigger";
  }

  trigger.layoutMode = "HORIZONTAL";
  trigger.primaryAxisSizingMode = "FIXED";
  trigger.counterAxisSizingMode = "FIXED";
  trigger.primaryAxisAlignItems = "SPACE_BETWEEN";
  trigger.counterAxisAlignItems = "CENTER";
  setLayoutSizingHorizontal(trigger, "FILL");
  trigger.itemSpacing = 8;
  trigger.paddingLeft = 12;
  trigger.paddingRight = 12;
  trigger.paddingTop = 0;
  trigger.paddingBottom = 0;
  trigger.resizeWithoutConstraints(320, 44);
  trigger.cornerRadius = 8;
  trigger.setSharedPluginData(RUN_NAMESPACE, "kind", "input-field");
  trigger.fills = [
    paintFromVariable(
      config.fieldFill,
      config.fieldFillFallback,
      variableByName,
      stats,
    ),
  ];
  trigger.strokes = [
    paintFromVariable(
      config.fieldStroke,
      config.fieldStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  trigger.strokeWeight = 1;
  trigger.clipsContent = false;

  let placeholder = directChildNamed(trigger, "Placeholder Text");
  if (placeholder && placeholder.type !== "TEXT") {
    placeholder.remove();
    placeholder = null;
  }

  if (!placeholder || placeholder.type !== "TEXT") {
    placeholder = figma.createText();
    placeholder.name = "Placeholder Text";
  }

  await applyFieldTextTypography(
    placeholder,
    fonts,
    "Select",
    variableByName,
    stats,
  );
  placeholder.characters = "Select an option";
  setHorizontalFillTextSizing(placeholder);
  setTextAutoResize(placeholder, "TRUNCATE");
  placeholder.fills = [
    paintFromVariable(
      config.placeholder,
      config.placeholderFallback,
      variableByName,
      stats,
    ),
  ];
  placeholder.textAlignVertical = "CENTER";
  trigger.appendChild(placeholder);

  removeGeneratedButtonChild(trigger, "Icon", true);
  const icon = await createFixedIconInstance(
    "chevron-down",
    config.placeholder,
    config.placeholderFallback,
    variableByName,
    stats,
    16,
    "Select/icon/size",
  );
  trigger.appendChild(icon);

  syncFocusRing(trigger, {
    enabled: state !== "Disabled",
    width: 320,
    height: 44,
    radius: 8,
    variableName: focusRingVariableForInputStatus(status),
    fallback: focusRingFallbackForInputStatus(status),
    variableByName,
    stats,
  });

  bindSelectGeometryVariables(component, trigger, icon, variableByName, stats);

  component.appendChild(trigger);
  normalizeInputFamilyChildSizing(component);
}

async function syncSliderVariantChildren({
  component,
  state,
  status,
  type,
  config,
  variableByName,
  fonts,
  stats,
}) {
  const label = await syncFieldLabel({
    component,
    text: "Slider",
    tokenPrefix: "Slider",
    colorToken: config.label,
    colorFallback: config.labelFallback,
    variableByName,
    fonts,
    stats,
  });

  let root = directChildNamed(component, "Slider Control");
  if (root && root.type !== "FRAME") {
    root.remove();
    root = null;
  }

  if (!root || root.type !== "FRAME") {
    root = figma.createFrame();
    root.name = "Slider Control";
  }

  root.layoutMode = "NONE";
  setVerticalFixedFillChildSizing(root);
  root.resizeWithoutConstraints(320, 44);
  root.fills = [];
  root.strokes = [];
  root.clipsContent = false;
  root.setSharedPluginData(RUN_NAMESPACE, "kind", "slider-control");

  let track = directChildNamed(root, "Slider Track");
  if (track && track.type !== "FRAME") {
    track.remove();
    track = null;
  }

  if (!track || track.type !== "FRAME") {
    track = figma.createFrame();
    track.name = "Slider Track";
  }

  track.layoutMode = "NONE";
  setLayoutSizingHorizontal(track, "FILL");
  track.resizeWithoutConstraints(320, 8);
  track.x = 0;
  track.y = 18;
  try {
    track.constraints = { horizontal: "STRETCH", vertical: "CENTER" };
  } catch (_error) {
    // Constraints are unavailable on some older plugin runtimes.
  }
  track.cornerRadius = 9999;
  track.fills = [
    paintFromVariable(
      config.trackFill,
      config.trackFillFallback,
      variableByName,
      stats,
    ),
  ];
  track.strokes = [
    paintFromVariable(
      config.trackStroke,
      config.trackStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  track.strokeWeight = 1;
  track.clipsContent = true;
  track.setSharedPluginData(RUN_NAMESPACE, "kind", "slider-track");

  let range = directChildNamed(track, "Slider Range");
  if (range && range.type !== "RECTANGLE") {
    range.remove();
    range = null;
  }

  if (!range || range.type !== "RECTANGLE") {
    range = figma.createRectangle();
    range.name = "Slider Range";
  }

  const isRange = type === "Range";
  range.resizeWithoutConstraints(160, 8);
  range.x = isRange ? 80 : 0;
  range.y = 0;
  try {
    range.constraints = { horizontal: "SCALE", vertical: "CENTER" };
  } catch (_error) {
    // Constraints are unavailable on some older plugin runtimes.
  }
  range.cornerRadius = 9999;
  range.fills = [
    paintFromVariable(
      config.rangeFill,
      config.rangeFillFallback,
      variableByName,
      stats,
    ),
  ];
  range.strokes = [];
  range.setSharedPluginData(RUN_NAMESPACE, "kind", "slider-range");
  track.appendChild(range);
  root.appendChild(track);

  const thumbSpecs = isRange
    ? [
        {
          anchorName: "Slider Thumb Anchor Start",
          name: "Slider Thumb Start",
          centerX: 80,
          focus: false,
        },
        {
          anchorName: "Slider Thumb Anchor End",
          name: "Slider Thumb End",
          centerX: 240,
          focus: true,
        },
      ]
    : [
        {
          anchorName: "Slider Thumb Anchor",
          name: "Slider Thumb",
          centerX: 160,
          focus: true,
        },
      ];
  const desiredAnchorNames = thumbSpecs.map((spec) => spec.anchorName);

  for (const child of Array.from(root.children || [])) {
    if (isGeneratedSliderThumbAnchor(child)) {
      if (desiredAnchorNames.indexOf(child.name) === -1) {
        child.remove();
      }
      continue;
    }

    if (isGeneratedSliderThumb(child)) {
      // Legacy direct thumbs stretch under Figma SCALE constraints. Range-aware
      // thumbs now live inside zero-width responsive anchors so the handle size
      // stays fixed when the parent instance is resized.
      child.remove();
    }
  }

  const thumbs = [];
  for (const spec of thumbSpecs) {
    let anchor = directChildNamed(root, spec.anchorName);
    if (anchor && anchor.type !== "FRAME") {
      anchor.remove();
      anchor = null;
    }

    if (!anchor || anchor.type !== "FRAME") {
      anchor = figma.createFrame();
    }

    anchor.name = spec.anchorName;
    anchor.layoutMode = "NONE";
    anchor.resizeWithoutConstraints(SLIDER_THUMB_SIZE, 44);
    anchor.x = spec.centerX - SLIDER_THUMB_SIZE / 2;
    anchor.y = 0;
    try {
      anchor.constraints = { horizontal: "SCALE", vertical: "STRETCH" };
    } catch (_error) {
      // Constraints are unavailable on some older plugin runtimes.
    }
    anchor.fills = [];
    anchor.strokes = [];
    anchor.clipsContent = false;
    anchor.setSharedPluginData(RUN_NAMESPACE, "kind", "slider-thumb-anchor");

    for (const child of Array.from(anchor.children || [])) {
      if (isGeneratedSliderThumb(child) && child.name !== spec.name) {
        child.remove();
      }
    }

    let thumb = directChildNamed(anchor, spec.name);
    if (thumb && thumb.type !== "FRAME") {
      thumb.remove();
      thumb = null;
    }

    if (!thumb || thumb.type !== "FRAME") {
      thumb = figma.createFrame();
    }

    thumb.name = spec.name;
    thumb.layoutMode = "NONE";
    thumb.resize(SLIDER_THUMB_SIZE, SLIDER_THUMB_SIZE);
    thumb.x = 0;
    thumb.y = 12;
    try {
      thumb.constraints = { horizontal: "CENTER", vertical: "CENTER" };
    } catch (_error) {
      // Constraints are unavailable on some older plugin runtimes.
    }
    thumb.cornerRadius = 9999;
    thumb.clipsContent = false;
    thumb.fills = [
      paintFromVariable(
        config.thumbFill,
        config.thumbFillFallback,
        variableByName,
        stats,
      ),
    ];
    thumb.strokes = [
      paintFromVariable(
        config.thumbStroke,
        config.thumbStrokeFallback,
        variableByName,
        stats,
      ),
    ];
    thumb.strokeWeight = 2;
    thumb.setSharedPluginData(RUN_NAMESPACE, "kind", "slider-thumb");

    syncFocusRing(thumb, {
      enabled: state !== "Disabled" && spec.focus,
      width: SLIDER_THUMB_SIZE,
      height: SLIDER_THUMB_SIZE,
      radius: 9999,
      variableName: focusRingVariableForInputStatus(status),
      fallback: focusRingFallbackForInputStatus(status),
      variableByName,
      stats,
    });

    anchor.appendChild(thumb);
    root.appendChild(anchor);
    thumbs.push(thumb);
  }

  bindSliderGeometryVariables(
    component,
    root,
    track,
    thumbs,
    variableByName,
    stats,
  );

  component.appendChild(label);
  component.appendChild(root);
  normalizeInputFamilyChildSizing(component);
}

async function syncProgressVariantChildren({
  component,
  value,
  config,
  variableByName,
  stats,
}) {
  let track = directChildNamed(component, "Progress Track");
  if (track && track.type !== "FRAME") {
    track.remove();
    track = null;
  }

  if (!track || track.type !== "FRAME") {
    track = figma.createFrame();
    track.name = "Progress Track";
  }

  track.layoutMode = "NONE";
  track.resizeWithoutConstraints(320, 8);
  track.x = 0;
  track.y = 0;
  track.cornerRadius = 9999;
  track.fills = [
    paintFromVariable(
      config.trackFill,
      config.trackFillFallback,
      variableByName,
      stats,
    ),
  ];
  track.strokes = [
    paintFromVariable(
      config.trackStroke,
      config.trackStrokeFallback,
      variableByName,
      stats,
    ),
  ];
  track.strokeWeight = 1;
  track.clipsContent = true;
  track.setSharedPluginData(RUN_NAMESPACE, "kind", "progress-track");

  let range = directChildNamed(track, "Progress Range");
  if (range && range.type !== "RECTANGLE") {
    range.remove();
    range = null;
  }

  if (!range || range.type !== "RECTANGLE") {
    range = figma.createRectangle();
    range.name = "Progress Range";
  }

  const rangeWidth = Math.max(1, Math.round((Number(value) / 100) * 320));
  range.resizeWithoutConstraints(rangeWidth, 8);
  range.x = 0;
  range.y = 0;
  range.cornerRadius = 9999;
  range.visible = value !== "0";
  range.fills = [
    paintFromVariable(
      config.rangeFill,
      config.rangeFillFallback,
      variableByName,
      stats,
    ),
  ];
  range.strokes = [];
  range.setSharedPluginData(RUN_NAMESPACE, "kind", "progress-range");

  track.appendChild(range);
  component.appendChild(track);
  bindProgressGeometryVariables(component, track, range, variableByName, stats);
}

async function syncSpinnerVariantChildren({
  component,
  metrics,
  variableByName,
  stats,
}) {
  let glyph = directChildNamed(component, "Spinner Glyph");
  if (glyph && glyph.type !== "ELLIPSE") {
    glyph.remove();
    glyph = null;
  }

  if (!glyph || glyph.type !== "ELLIPSE") {
    glyph = figma.createEllipse();
    glyph.name = "Spinner Glyph";
  }

  glyph.resize(metrics.size, metrics.size);
  glyph.x = 0;
  glyph.y = 0;
  glyph.fills = [];
  glyph.strokes = [
    paintFromVariable("Colors/theme/500", "#135BEC", variableByName, stats),
  ];
  glyph.strokeWeight = 2;
  glyph.dashPattern = [8, 4];
  glyph.setSharedPluginData(RUN_NAMESPACE, "kind", "spinner-glyph");
  bindSizeVariables(
    glyph,
    metrics.sizeToken,
    metrics.sizeToken,
    variableByName,
    stats,
  );
  bindFloatVariable(
    glyph,
    "strokeWeight",
    "Spinner/stroke/width",
    variableByName,
    stats,
  );

  component.appendChild(glyph);
}

async function syncAvatarVariantChildren({
  component,
  value,
  config,
  variableByName,
  fonts,
  stats,
}) {
  removeGeneratedButtonChild(component, "Icon", true);
  removeGeneratedButtonChild(component, "Fallback", value !== "Fallback");

  if (value === "Image") {
    const icon = await createFixedIconInstance(
      "user-01",
      "Colors/foreground/1000",
      "#FFFFFF",
      variableByName,
      stats,
      20,
      null,
    );
    icon.x = 10;
    icon.y = 10;
    component.appendChild(icon);
  }

  if (value === "Fallback") {
    let fallback = directChildNamed(component, "Fallback");
    if (fallback && fallback.type !== "TEXT") {
      fallback.remove();
      fallback = null;
    }

    if (!fallback || fallback.type !== "TEXT") {
      fallback = figma.createText();
      fallback.name = "Fallback";
    }

    await applyAvatarFallbackTypography(fallback, fonts, variableByName, stats);
    fallback.characters = "AK";
    fallback.fills = [
      paintFromVariable(
        config.foreground,
        config.foregroundFallback,
        variableByName,
        stats,
      ),
    ];
    fallback.textAlignHorizontal = "CENTER";
    fallback.textAlignVertical = "CENTER";
    fallback.x = 10;
    fallback.y = 10;
    component.appendChild(fallback);
  }

  await syncHiddenAvatarTextPropertyNode({
    component,
    name: "Image URL",
    value: "https://example.com/avatar.png",
    fonts,
    variableByName,
    stats,
  });
  await syncHiddenAvatarTextPropertyNode({
    component,
    name: "Alt Text",
    value: "Avatar",
    fonts,
    variableByName,
    stats,
  });
}

async function syncHiddenAvatarTextPropertyNode({
  component,
  name,
  value,
  fonts,
  variableByName,
  stats,
}) {
  let text = directChildNamed(component, name);
  if (text && text.type !== "TEXT") {
    text.remove();
    text = null;
  }

  if (!text || text.type !== "TEXT") {
    text = figma.createText();
    text.name = name;
  }

  await applyAvatarFallbackTypography(text, fonts, variableByName, stats);
  text.characters = value;
  text.visible = false;
  text.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  component.appendChild(text);
}

async function syncAlertVariantChildren({
  component,
  value,
  config,
  variableByName,
  fonts,
  stats,
}) {
  removeGeneratedButtonChild(component, "Icon", true);
  const icon = await createFixedIconInstance(
    config.icon,
    config.foreground,
    config.foregroundFallback,
    variableByName,
    stats,
    16,
    "Alert/icon/size",
  );
  component.appendChild(icon);

  let content = directChildNamed(component, "Content");
  if (content && content.type !== "FRAME") {
    content.remove();
    content = null;
  }

  if (!content || content.type !== "FRAME") {
    content = figma.createFrame();
    content.name = "Content";
  }

  content.layoutMode = "VERTICAL";
  content.primaryAxisSizingMode = "AUTO";
  content.counterAxisSizingMode = "FIXED";
  content.primaryAxisAlignItems = "MIN";
  content.counterAxisAlignItems = "MIN";
  content.itemSpacing = 4;
  content.paddingLeft = 0;
  content.paddingRight = 0;
  content.paddingTop = 0;
  content.paddingBottom = 0;
  content.resizeWithoutConstraints(296, 56);
  content.fills = [];
  content.strokes = [];
  content.clipsContent = false;
  setLayoutSizingHorizontal(content, "FILL");

  let title = directChildNamed(content, "Title");
  if (title && title.type !== "TEXT") {
    title.remove();
    title = null;
  }

  if (!title || title.type !== "TEXT") {
    title = figma.createText();
    title.name = "Title";
  }

  await applyAlertTitleTypography(title, fonts, variableByName, stats);
  title.characters = alertTitleForVariant(value);
  title.fills = [
    paintFromVariable(
      config.foreground,
      config.foregroundFallback,
      variableByName,
      stats,
    ),
  ];
  setLayoutSizingHorizontal(title, "FILL");
  setTextAutoResize(title, "HEIGHT");
  title.resizeWithoutConstraints(296, 20);

  let description = directChildNamed(content, "Description");
  if (description && description.type !== "TEXT") {
    description.remove();
    description = null;
  }

  if (!description || description.type !== "TEXT") {
    description = figma.createText();
    description.name = "Description";
  }

  await applyAlertDescriptionTypography(
    description,
    fonts,
    variableByName,
    stats,
  );
  description.characters = alertDescriptionForVariant(value);
  description.fills = [
    paintFromVariable(
      config.foreground,
      config.foregroundFallback,
      variableByName,
      stats,
    ),
  ];
  setLayoutSizingHorizontal(description, "FILL");
  setTextAutoResize(description, "HEIGHT");
  description.resizeWithoutConstraints(296, 36);

  content.appendChild(title);
  content.appendChild(description);
  component.appendChild(content);
  bindAlertGeometryVariables(component, icon, variableByName, stats);
}

async function syncEmptyStateVariantChildren({
  component,
  value,
  variableByName,
  fonts,
  stats,
}) {
  removeDirectChildren(component);

  const hasIcon = value !== "Basic";
  const hasAction = value === "Action";
  let iconContainer = null;

  if (hasIcon) {
    iconContainer = figma.createFrame();
    iconContainer.name = "Icon Container";
    iconContainer.layoutMode = "HORIZONTAL";
    iconContainer.primaryAxisSizingMode = "FIXED";
    iconContainer.counterAxisSizingMode = "FIXED";
    iconContainer.primaryAxisAlignItems = "CENTER";
    iconContainer.counterAxisAlignItems = "CENTER";
    iconContainer.itemSpacing = 0;
    iconContainer.paddingLeft = 0;
    iconContainer.paddingRight = 0;
    iconContainer.paddingTop = 0;
    iconContainer.paddingBottom = 0;
    iconContainer.resizeWithoutConstraints(64, 64);
    iconContainer.cornerRadius = 9999;
    iconContainer.clipsContent = false;
    iconContainer.fills = [
      paintFromVariable(
        "Colors/background/100",
        "#F1F2F4",
        variableByName,
        stats,
      ),
    ];
    iconContainer.strokes = [];

    const icon = await createFixedIconInstance(
      "search-md",
      "Colors/foreground/400",
      "#5E6575",
      variableByName,
      stats,
      32,
      "EmptyState/icon/size",
    );
    iconContainer.appendChild(icon);
    setHugChildSizing(icon);
    component.appendChild(iconContainer);
  }

  const content = figma.createFrame();
  content.name = "Content";
  content.layoutMode = "VERTICAL";
  content.primaryAxisSizingMode = "AUTO";
  content.counterAxisSizingMode = "FIXED";
  content.primaryAxisAlignItems = "CENTER";
  content.counterAxisAlignItems = "CENTER";
  content.itemSpacing = 4;
  content.paddingLeft = 0;
  content.paddingRight = 0;
  content.paddingTop = 0;
  content.paddingBottom = 0;
  content.resizeWithoutConstraints(296, 64);
  content.fills = [];
  content.strokes = [];
  content.clipsContent = false;
  setLayoutSizingHorizontal(content, "FILL");

  const title = figma.createText();
  title.name = "Title Text";
  await applyEmptyStateTitleTypography(title, fonts, variableByName, stats);
  title.characters = "No results found";
  title.textAlignHorizontal = "CENTER";
  title.textAlignVertical = "CENTER";
  title.fills = [
    paintFromVariable("Colors/foreground/0", "#000000", variableByName, stats),
  ];
  setTextAutoResize(title, "HEIGHT");
  title.resizeWithoutConstraints(296, 20);
  setLayoutSizingHorizontal(title, "FILL");

  const description = figma.createText();
  description.name = "Description Text";
  await applyEmptyStateDescriptionTypography(
    description,
    fonts,
    variableByName,
    stats,
  );
  description.characters = "Try adjusting your filters or search terms.";
  description.textAlignHorizontal = "CENTER";
  description.textAlignVertical = "CENTER";
  description.fills = [
    paintFromVariable(
      "Colors/foreground/400",
      "#5E6575",
      variableByName,
      stats,
    ),
  ];
  setTextAutoResize(description, "HEIGHT");
  description.resizeWithoutConstraints(280, 40);
  setLayoutSizingHorizontal(description, "FILL");

  content.appendChild(title);
  content.appendChild(description);
  component.appendChild(content);

  if (hasAction) {
    const action = await createEmptyStateActionButton(stats);
    component.appendChild(action);
    setLayoutSizingHorizontal(action, "HUG");
  }

  bindEmptyStateGeometryVariables(
    component,
    iconContainer,
    content,
    variableByName,
    stats,
  );
}

async function createEmptyStateActionButton(stats) {
  let action = null;
  let buttonSet = null;
  const created = await createNestedComponentInstance({
    componentSetName: "Button",
    variantProperties: {
      Variant: "Outline",
      Size: "Default",
      State: "Default",
    },
    name: "Action",
    stats,
  });

  if (!created) {
    action = createMissingNestedComponentNode(
      "Action",
      "Build Button before updating EmptyState.",
      stats,
    );
  } else {
    action = created.instance;
    buttonSet = created.componentSet;
  }

  if (action.type === "INSTANCE" && buttonSet) {
    try {
      action.setProperties({
        Variant: "Outline",
        Size: "Default",
        State: "Default",
      });
    } catch (error) {
      stats.warnings.push(
        `EmptyState Action: could not set Button variant properties (${messageFor(error)}).`,
      );
    }
    setInstanceTextProperty(
      action,
      buttonSet,
      "Label Text",
      "Clear filters",
      stats,
    );
    markNestedComponentInstance(action, "Button", "empty-state-action");
  }

  if (action.setSharedPluginData) {
    action.setSharedPluginData(RUN_NAMESPACE, "role", "empty-state-action");
  }

  return action;
}

async function syncFieldLabel({
  component,
  text,
  tokenPrefix,
  colorToken,
  colorFallback,
  variableByName,
  fonts,
  stats,
}) {
  let label = directChildNamed(component, "Label Text");
  if (label && label.type !== "TEXT") {
    label.remove();
    label = null;
  }

  if (!label || label.type !== "TEXT") {
    label = figma.createText();
    label.name = "Label Text";
  }

  await applyFieldLabelTypography(
    label,
    fonts,
    tokenPrefix,
    variableByName,
    stats,
  );
  label.characters = text;
  label.fills = [
    paintFromVariable(colorToken, colorFallback, variableByName, stats),
  ];
  label.textAlignVertical = "CENTER";
  return label;
}

async function createFixedIconInstance(
  iconName,
  variableName,
  fallback,
  variableByName,
  stats,
  size,
  sizeToken,
) {
  const iconComponent =
    (await findKozmosIconSourceComponent(iconName)) ||
    (await resolveDefaultIconSourceComponent(variableByName, stats));
  return createIconSlotInstance(
    iconComponent,
    variableName,
    fallback,
    variableByName,
    stats,
    size,
    sizeToken,
  );
}

async function createPasswordVisibilityIcon({
  isVisible,
  variableName,
  fallback,
  variableByName,
  stats,
}) {
  const iconComponent =
    (await findKozmosIconSourceComponent(isVisible ? "x-close" : "lock-01")) ||
    (await findKozmosIconSourceComponent("lock-01")) ||
    (await findKozmosIconSourceComponent(DEFAULT_CURATED_ICON_NAME));

  if (iconComponent) {
    const icon = createIconSlotInstance(
      iconComponent,
      variableName,
      fallback,
      variableByName,
      stats,
      16,
      "PasswordInput/toggle/icon/size",
    );
    try {
      icon.name = isVisible
        ? "Visibility Icon / hide"
        : "Visibility Icon / show";
      icon.setSharedPluginData(
        RUN_NAMESPACE,
        "kind",
        "password-visibility-icon",
      );
      icon.setSharedPluginData(
        RUN_NAMESPACE,
        "visibility",
        isVisible ? "hide" : "show",
      );
      return icon;
    } catch (error) {
      try {
        icon.remove();
      } catch (_removeError) {
        // The instance may already have been detached by Figma.
      }
      throw error;
    }
  }

  const icon = figma.createFrame();
  icon.name = isVisible ? "Visibility Icon / hide" : "Visibility Icon / show";
  icon.layoutMode = "HORIZONTAL";
  icon.primaryAxisSizingMode = "FIXED";
  icon.counterAxisSizingMode = "FIXED";
  icon.primaryAxisAlignItems = "CENTER";
  icon.counterAxisAlignItems = "CENTER";
  icon.resizeWithoutConstraints(16, 16);
  icon.fills = [];
  icon.strokes = [];
  icon.clipsContent = false;
  icon.setSharedPluginData(RUN_NAMESPACE, "kind", "password-visibility-icon");
  icon.setSharedPluginData(RUN_NAMESPACE, "foreground-token", variableName);
  icon.setSharedPluginData(RUN_NAMESPACE, "foreground-fallback", fallback);
  icon.setSharedPluginData(
    RUN_NAMESPACE,
    "visibility",
    isVisible ? "hide" : "show",
  );

  try {
    resetIconGlyph(icon, variableName, fallback, variableByName, stats, 16);
  } catch (error) {
    try {
      icon.remove();
    } catch (_removeError) {
      // The frame may already have been detached by Figma.
    }
    throw error;
  }

  return icon;
}

function helperTextForInputStatus(status) {
  if (status === "Error") return "Invalid value";
  if (status === "Warning") return "Check this value";
  if (status === "Success") return "Looks good";
  return "Helper text";
}

function focusRingVariableForState(state) {
  return state === "Error" ? "Colors/emotional/danger/600" : "Colors/theme/500";
}

function focusRingFallbackForState(state) {
  return state === "Error" ? "#D41C42" : "#135BEC";
}

function focusRingVariableForInputStatus(status) {
  if (status === "Error") return "Colors/emotional/danger/600";
  if (status === "Warning") return "Colors/emotional/alert/900";
  if (status === "Success") return "Colors/emotional/success/900";
  return "Colors/theme/500";
}

function focusRingFallbackForInputStatus(status) {
  if (status === "Error") return "#D41C42";
  if (status === "Warning") return "#744D03";
  if (status === "Success") return "#14653D";
  return "#135BEC";
}

function syncFocusRing(
  target,
  {
    enabled,
    width,
    height,
    radius,
    variableName,
    fallback,
    variableByName,
    stats,
  },
) {
  if (!target) return;

  let ring = directChildNamed(target, "Focus Ring");
  if (!enabled) {
    if (ring) ring.remove();
    return;
  }

  if (ring && ring.type !== "RECTANGLE") {
    ring.remove();
    ring = null;
  }

  if (!ring || ring.type !== "RECTANGLE") {
    ring = figma.createRectangle();
    ring.name = "Focus Ring";
  }

  target.appendChild(ring);

  try {
    target.clipsContent = false;
  } catch (_error) {
    // Focus rings intentionally sit outside the target bounds.
  }

  try {
    ring.layoutPositioning = "ABSOLUTE";
  } catch (_error) {
    // Older Figma runtimes may not expose absolute positioning on every node.
  }

  try {
    ring.constraints = {
      horizontal: "STRETCH",
      vertical: "STRETCH",
    };
  } catch (_error) {
    // Constraints can be rejected on some shape contexts in older runtimes.
  }

  const offset = 4;
  const measuredWidth =
    typeof target.width === "number" && Number.isFinite(target.width)
      ? target.width
      : width;
  const measuredHeight =
    typeof target.height === "number" && Number.isFinite(target.height)
      ? target.height
      : height;
  ring.resizeWithoutConstraints(
    measuredWidth + offset * 2,
    measuredHeight + offset * 2,
  );
  ring.x = -offset;
  ring.y = -offset;
  ring.cornerRadius = radius >= 9999 ? 9999 : radius + offset;
  ring.fills = [];
  ring.strokes = [
    paintFromVariable(variableName, fallback, variableByName, stats),
  ];
  ring.strokeWeight = 2;
  ring.visible = false;
  ring.setSharedPluginData(RUN_NAMESPACE, "kind", "focus-ring");
}

function setLayoutSizingHorizontal(node, value) {
  if (!node) return false;

  try {
    node.layoutSizingHorizontal = value;
  } catch (error) {
    // Older Figma runtimes may not expose layout sizing on every node type,
    // and FILL throws outside an auto-layout parent. Both are recorded.
    recordLayoutSizingFailure("layoutSizingHorizontal", node, value, error);
    return false;
  }

  if (node.layoutSizingHorizontal === value) return true;
  recordLayoutSizingFailure("layoutSizingHorizontal", node, value, null);
  return false;
}

function setLayoutSizingVertical(node, value) {
  if (!node) return false;

  try {
    node.layoutSizingVertical = value;
  } catch (error) {
    // Older Figma runtimes may not expose layout sizing on every node type,
    // and FILL throws outside an auto-layout parent. Both are recorded.
    recordLayoutSizingFailure("layoutSizingVertical", node, value, error);
    return false;
  }

  if (node.layoutSizingVertical === value) return true;
  recordLayoutSizingFailure("layoutSizingVertical", node, value, null);
  return false;
}

function setNodePropertyIfSupported(node, propertyName, value) {
  if (!node) return false;

  try {
    node[propertyName] = value;
    return node[propertyName] === value;
  } catch (_error) {
    // Native slots and older plugin runtimes do not expose every frame field.
    return false;
  }
}

function resizeNodeWithoutConstraints(node, width, height) {
  if (!node) return false;

  try {
    if (node.resizeWithoutConstraints) {
      node.resizeWithoutConstraints(width, height);
      return true;
    }

    if (node.resize) {
      node.resize(width, height);
      return true;
    }
  } catch (_error) {
    // Some instance types cannot be resized in older Figma runtimes.
  }

  return false;
}

function resizeVerticalAutoLayoutFrameToVisibleChildren(node, widthFallback) {
  if (!node || !node.children) return;

  const visibleChildren = Array.from(node.children).filter(
    (child) => child.visible !== false,
  );
  if (visibleChildren.length === 0) return;

  const childrenHeight = visibleChildren.reduce(
    (total, child) => total + Math.max(0, child.height || 0),
    0,
  );
  const spacing =
    Math.max(0, visibleChildren.length - 1) * (node.itemSpacing || 0);
  const padding = (node.paddingTop || 0) + (node.paddingBottom || 0);
  const width = widthFallback || node.width || 1;
  const height = Math.max(1, childrenHeight + spacing + padding);

  resizeNodeWithoutConstraints(node, width, height);
}

function resizeComponentSetToContainChildren(componentSet, padding = 0) {
  if (!componentSet || !componentSet.children) return;

  let maxRight = 0;
  let maxBottom = 0;
  let foundChild = false;

  for (const child of componentSet.children) {
    if (child.visible === false) continue;
    const right = (child.x || 0) + Math.max(0, child.width || 0);
    const bottom = (child.y || 0) + Math.max(0, child.height || 0);
    maxRight = Math.max(maxRight, right);
    maxBottom = Math.max(maxBottom, bottom);
    foundChild = true;
  }

  if (!foundChild) return;

  resizeNodeWithoutConstraints(
    componentSet,
    Math.max(1, maxRight + padding),
    Math.max(1, maxBottom + padding),
  );
  componentSet.clipsContent = false;
}

function setVerticalStackChildSizing(node) {
  try {
    node.layoutPositioning = "AUTO";
  } catch (_error) {
    // layoutPositioning is unavailable on older plugin runtimes and some nodes.
  }

  setLayoutSizingHorizontal(node, "FILL");
  setLayoutSizingVertical(node, "HUG");

  try {
    node.layoutAlign = "STRETCH";
  } catch (_error) {
    // layoutAlign is the older Plugin API equivalent for cross-axis fill.
  }

  try {
    node.layoutGrow = 0;
  } catch (_error) {
    // layoutGrow is unavailable on older plugin runtimes.
  }
}

function setVerticalFixedFillChildSizing(node) {
  setLayoutSizingHorizontal(node, "FILL");
  setLayoutSizingVertical(node, "FIXED");

  try {
    node.layoutAlign = "STRETCH";
  } catch (_error) {
    // layoutAlign is the older Plugin API equivalent for cross-axis fill.
  }

  try {
    node.layoutGrow = 0;
  } catch (_error) {
    // layoutGrow is unavailable on older plugin runtimes.
  }

  try {
    node.constraints = { horizontal: "STRETCH", vertical: "MIN" };
  } catch (_error) {
    // Constraints are not available on every node type.
  }
}

function setVerticalStackFillChildSizing(node) {
  setLayoutSizingHorizontal(node, "FILL");
  setLayoutSizingVertical(node, "FILL");

  try {
    node.layoutAlign = "STRETCH";
  } catch (_error) {
    // layoutAlign is the older Plugin API equivalent for cross-axis fill.
  }

  try {
    node.layoutGrow = 1;
  } catch (_error) {
    // layoutGrow is unavailable on older plugin runtimes.
  }
}

function setHorizontalStackFillHugChildSizing(node) {
  setLayoutSizingHorizontal(node, "FILL");
  setLayoutSizingVertical(node, "HUG");

  try {
    node.layoutGrow = 1;
  } catch (_error) {
    // layoutGrow is unavailable on older plugin runtimes.
  }
}

function setHorizontalStackFillFixedChildSizing(node) {
  setLayoutSizingHorizontal(node, "FILL");
  setLayoutSizingVertical(node, "FIXED");

  try {
    node.layoutAlign = "CENTER";
  } catch (_error) {
    // layoutAlign is unavailable on older plugin runtimes.
  }

  try {
    node.layoutGrow = 1;
  } catch (_error) {
    // layoutGrow is unavailable on older plugin runtimes.
  }
}

function setHorizontalStackFillChildSizing(node) {
  setLayoutSizingHorizontal(node, "FILL");
  setLayoutSizingVertical(node, "FILL");

  try {
    node.layoutAlign = "STRETCH";
  } catch (_error) {
    // layoutAlign is the older Plugin API equivalent for cross-axis fill.
  }

  try {
    node.layoutGrow = 1;
  } catch (_error) {
    // layoutGrow is unavailable on older plugin runtimes.
  }
}

const INPUT_FAMILY_VERTICAL_FIXED_FILL_NODE_NAMES = new Set([
  "Input Field",
  "PasswordInput Field",
  "NumberInput Field",
  "Combobox Field",
  "MultiSelect Field",
  "DatePicker Field",
  "TimePicker Field",
  "FileUpload Dropzone",
  "ColorPicker Field",
  "ColorPicker Hex Field",
  "ColorPicker Color Area",
  "Select Trigger",
  "Search Field",
  "Textarea Field",
  "Slider Control",
  "Start Date Field",
  "End Date Field",
]);

const INPUT_FAMILY_VERTICAL_HUG_FILL_NODE_NAMES = new Set([
  "Combobox Listbox",
  "MultiSelect Listbox",
  "TimePicker Listbox",
  "DatePicker Calendar",
  "DateRangePicker Calendar",
  "ColorPicker Popover",
  "DateRangePicker Fields",
  "OTPInput Cell Group",
  "Selected Files",
]);

const INPUT_FAMILY_HORIZONTAL_FILL_NODE_NAMES = new Set([
  "PasswordInput Value Slot",
  "NumberInput Value Slot",
  "Start Date Field Group",
  "End Date Field Group",
  "FileUpload File Copy",
  "Listbox Option Text",
]);

const INPUT_FAMILY_VERTICAL_FIXED_FILL_KINDS = new Set([
  "input-field",
  "combobox-option",
  "multiselect-option",
  "listbox-option",
  "timepicker-option",
  "file-upload-dropzone",
  "file-upload-file-row",
  "colorpicker-color-area",
  "slider-control",
]);

const INPUT_FAMILY_VERTICAL_HUG_FILL_KINDS = new Set([
  "combobox-listbox",
  "multiselect-listbox",
  "timepicker-listbox",
  "datepicker-calendar",
  "daterange-calendar",
  "colorpicker-popover",
  "otpinput-cell-group",
]);

function normalizeInputFamilyChildSizing(component) {
  if (!component || !component.children) return;

  for (const child of component.children) {
    normalizeInputFamilyNodeSizing(child, { directComponentChild: true });
  }
}

function sharedPluginDataValue(node, namespace, key) {
  if (!node || typeof node.getSharedPluginData !== "function") return "";

  try {
    return node.getSharedPluginData(namespace, key) || "";
  } catch (_error) {
    return "";
  }
}

function normalizeInputFamilyNodeSizing(node, context = {}) {
  if (!node) return;

  if (context.directComponentChild && node.type === "TEXT") {
    setVerticalStackChildSizing(node);
  }

  const kind = sharedPluginDataValue(node, RUN_NAMESPACE, "kind");
  if (
    INPUT_FAMILY_VERTICAL_FIXED_FILL_NODE_NAMES.has(node.name) ||
    INPUT_FAMILY_VERTICAL_FIXED_FILL_KINDS.has(kind)
  ) {
    setVerticalFixedFillChildSizing(node);
  } else if (
    INPUT_FAMILY_VERTICAL_HUG_FILL_NODE_NAMES.has(node.name) ||
    INPUT_FAMILY_VERTICAL_HUG_FILL_KINDS.has(kind)
  ) {
    setVerticalStackChildSizing(node);
  } else if (INPUT_FAMILY_HORIZONTAL_FILL_NODE_NAMES.has(node.name)) {
    setHorizontalStackFillChildSizing(node);
  }

  if (!node.children) return;

  for (const child of node.children) {
    normalizeInputFamilyNodeSizing(child, { directComponentChild: false });
  }
}

function setFixedChildSizing(node) {
  setLayoutSizingHorizontal(node, "FIXED");
  setLayoutSizingVertical(node, "FIXED");

  try {
    node.layoutAlign = "CENTER";
  } catch (_error) {
    // layoutAlign is unavailable on older plugin runtimes.
  }

  try {
    node.layoutGrow = 0;
  } catch (_error) {
    // layoutGrow is unavailable on older plugin runtimes.
  }
}

function setHugChildSizing(node) {
  setLayoutSizingHorizontal(node, "HUG");
  setLayoutSizingVertical(node, "HUG");

  try {
    node.layoutAlign = "CENTER";
  } catch (_error) {
    // layoutAlign is unavailable on older plugin runtimes.
  }

  try {
    node.layoutGrow = 0;
  } catch (_error) {
    // layoutGrow is unavailable on older plugin runtimes.
  }
}

function setTextAutoResize(textNode, value) {
  if (!textNode || textNode.type !== "TEXT") return;

  try {
    textNode.textAutoResize = value;
  } catch (_error) {
    textNode.textAutoResize = "NONE";
  }
}

function directChildNamed(parent, name) {
  if (!parent.children) return null;
  for (const child of parent.children) {
    if (child.name === name) return child;
  }

  return null;
}

function collectDescendantTextCharactersByName(parent, names) {
  const nameSet = new Set(names);
  const values = {};

  function visit(node) {
    if (!node) return;

    if (
      node.type === "TEXT" &&
      nameSet.has(node.name) &&
      typeof node.characters === "string"
    ) {
      values[node.name] = node.characters;
    }

    if (!node.children) return;
    for (const child of node.children) visit(child);
  }

  visit(parent);
  return values;
}

function firstDescendantTextCharactersByName(parent, name) {
  let value = null;

  function visit(node) {
    if (!node || value !== null) return;

    if (
      node.type === "TEXT" &&
      node.name === name &&
      typeof node.characters === "string"
    ) {
      value = node.characters;
      return;
    }

    if (!node.children) return;
    for (const child of node.children) visit(child);
  }

  visit(parent);
  return value;
}

function instanceTextPropertyValue(instance, baseName) {
  if (!instance || !instance.componentProperties) return null;

  for (const propertyName of Object.keys(instance.componentProperties)) {
    if (propertyName.split("#")[0] !== baseName) continue;
    const property = instance.componentProperties[propertyName];
    if (
      property &&
      property.type === "TEXT" &&
      typeof property.value === "string"
    ) {
      return property.value;
    }
  }

  return null;
}

function multiSelectChipLabelText(component, index) {
  const chipName = `MultiSelect Chip ${index}`;
  const field = directChildNamed(component, "MultiSelect Field");
  const chip =
    directChildNamed(field || { children: [] }, chipName) ||
    directChildNamed(component, chipName);
  if (!chip) return null;

  return (
    instanceTextPropertyValue(chip, "Label Text") ||
    firstDescendantTextCharactersByName(chip, "Label Text") ||
    firstDescendantTextCharactersByName(chip, `Chip ${index} Text`)
  );
}

function removeGeneratedButtonChild(component, name, shouldRemove) {
  if (!shouldRemove || !component.children) return;
  const removable = [];
  for (const child of component.children) {
    if (child.name === name) removable.push(child);
  }

  for (const child of removable) {
    child.remove();
  }
}

function syncIconSlotInstance(
  icon,
  config,
  variableByName,
  stats,
  iconSize,
  iconSizeToken,
) {
  const size = iconSize || 16;
  const existingToken = icon.getSharedPluginData
    ? icon.getSharedPluginData(RUN_NAMESPACE, "foreground-token")
    : "";
  const existingFallback = icon.getSharedPluginData
    ? icon.getSharedPluginData(RUN_NAMESPACE, "foreground-fallback")
    : "";
  const shouldRetint =
    existingToken !== config.foreground ||
    existingFallback !== config.foregroundFallback;

  icon.resize(size, size);
  if (iconSizeToken) {
    bindSizeVariables(
      icon,
      iconSizeToken,
      iconSizeToken,
      variableByName,
      stats,
    );
  }
  storeIconSlotAuditMetadata(
    icon,
    config.foreground,
    config.foregroundFallback,
    stats,
  );

  if (isGeneratedIconSlotWrapper(icon)) {
    stats.warnings.push(
      "Encountered a legacy masked Icon wrapper. Run Update Button/IconButton to rebuild direct icon instances.",
    );
    return;
  }

  if (icon.type === "INSTANCE") {
    icon.name = "Icon";
    if (shouldRetint) {
      tintIconSlotMark(icon, config, variableByName, stats);
    } else {
      incrementStat(stats, "iconSlotRetintsSkipped");
    }
    return;
  }

  if (icon.type !== "FRAME") return;

  icon.name = "Icon";
  resetIconGlyph(
    icon,
    config.foreground,
    config.foregroundFallback,
    variableByName,
    stats,
    size,
  );
}

function createIconSlotInstance(
  iconComponent,
  variableName,
  fallback,
  variableByName,
  stats,
  iconSize,
  iconSizeToken,
) {
  const size = iconSize || 16;
  const icon = iconComponent.createInstance();
  icon.name = "Icon";
  icon.resize(size, size);
  if (iconSizeToken) {
    bindSizeVariables(
      icon,
      iconSizeToken,
      iconSizeToken,
      variableByName,
      stats,
    );
  }
  icon.x = 0;
  icon.y = 0;
  storeIconSlotAuditMetadata(icon, variableName, fallback, stats);
  applyIconColorOverrides(icon, variableName, fallback, variableByName, stats);
  return icon;
}

function storeIconSlotAuditMetadata(icon, variableName, fallback, stats) {
  if (!icon || !icon.setSharedPluginData) return;

  try {
    icon.setSharedPluginData(RUN_NAMESPACE, "kind", "icon-slot-instance");
    icon.setSharedPluginData(RUN_NAMESPACE, "foreground-token", variableName);
    icon.setSharedPluginData(RUN_NAMESPACE, "foreground-fallback", fallback);
  } catch (error) {
    stats.warnings.push(
      `Could not store Icon audit metadata (${messageFor(error)}).`,
    );
  }
}

async function ensureDefaultIconComponent(variableByName, stats) {
  const existing = await findComponentByName(DEFAULT_ICON_COMPONENT_NAME);
  if (existing) {
    existing.description =
      "Default generated icon slot fallback for neutral button surfaces. Replace with Pointr Icon Library instances in consuming components.";
    resetIconGlyph(
      existing,
      "Colors/foreground/0",
      "#000000",
      variableByName,
      stats,
      16,
    );
    return existing;
  }

  const page = await ensurePage("Utilities");
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  const component = figma.createComponent();
  component.name = DEFAULT_ICON_COMPONENT_NAME;
  component.resize(16, 16);
  component.x = 80;
  component.y = 80;
  component.description =
    "Default icon artwork for instance-swap slots. Replace with Pointr Icon Library instances in consuming components.";
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "slot-default");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Icon");
  resetIconGlyph(
    component,
    "Colors/foreground/0",
    "#000000",
    variableByName,
    stats,
    16,
  );
  return component;
}

async function resolveDefaultIconSourceComponent(variableByName, stats) {
  const curated = await findKozmosIconSourceComponent(
    DEFAULT_CURATED_ICON_NAME,
  );
  if (curated) return curated;

  return ensureDefaultIconComponent(variableByName, stats);
}

async function findComponentByName(name) {
  for (const page of figma.root.children) {
    await page.loadAsync();
    const component = page.findOne(
      (node) => node.type === "COMPONENT" && node.name === name,
    );
    if (component) return component;
  }

  return null;
}

function kozmosIconComponentName(name) {
  return `Icon / ${name}`;
}

async function findKozmosIconSourceComponent(name) {
  return findComponentByName(kozmosIconComponentName(name));
}

async function findKozmosIconSourceComponents() {
  const page = figma.root.children.find(
    (child) => child.name === ICON_PAGE_NAME,
  );
  if (!page) return [];

  await page.loadAsync();
  const components = [];
  for (const definition of KOSMOS_ICON_DEFINITIONS) {
    const component = page.findOne(
      (node) =>
        node.type === "COMPONENT" &&
        node.name === kozmosIconComponentName(definition.name),
    );
    if (component) components.push(component);
  }

  return components;
}

function iconPreferredValues(components) {
  const values = [];
  for (const component of components) {
    if (!component.key) continue;
    values.push({
      type: "COMPONENT",
      key: component.key,
    });
  }

  return values;
}

async function syncIconSourceLibrary() {
  const stats = {
    updated: true,
    page: ICON_PAGE_NAME,
    planned: KOSMOS_ICON_DEFINITIONS.length,
    imported: 0,
    created: 0,
    refreshed: 0,
    failed: 0,
    preferredValuesApplied: 0,
    warnings: [],
  };

  const page = await ensurePage(ICON_PAGE_NAME);
  await figma.setCurrentPageAsync(page);
  await page.loadAsync();

  for (let index = 0; index < KOSMOS_ICON_DEFINITIONS.length; index += 1) {
    const definition = KOSMOS_ICON_DEFINITIONS[index];

    try {
      const sourceComponent = await figma.importComponentByKeyAsync(
        definition.componentKey,
      );
      stats.imported += 1;

      let component = page.findOne(
        (node) =>
          node.type === "COMPONENT" &&
          node.name === kozmosIconComponentName(definition.name),
      );

      if (!component) {
        component = figma.createComponent();
        component.name = kozmosIconComponentName(definition.name);
        page.appendChild(component);
        stats.created += 1;
      } else {
        stats.refreshed += 1;
      }

      syncKozmosIconSourceComponent(
        component,
        sourceComponent,
        definition,
        index,
        stats,
      );
    } catch (error) {
      stats.failed += 1;
      stats.warnings.push(
        `Could not import icon "${definition.name}" (${messageFor(error)}).`,
      );
    }
  }

  await refreshComponentIconSlotsAfterIconSync(stats);
  await applyCuratedIconSourcesToSlots(stats);
  stats.iconSourceCount = (await findKozmosIconSourceComponents()).length;
  return stats;
}

function syncKozmosIconSourceComponent(
  component,
  sourceComponent,
  definition,
  index,
  stats,
) {
  component.name = kozmosIconComponentName(definition.name);
  component.description = [
    `Kozmos curated icon source: ${definition.name}.`,
    `Source: Pointr Icon Library / ${definition.figmaName}.`,
    `Category: ${definition.category}.`,
    definition.description,
    "Use as an instance-swap source. Consuming components own size and foreground color.",
  ].join("\n");
  component.setSharedPluginData(RUN_NAMESPACE, "kind", "icon-source");
  component.setSharedPluginData(RUN_NAMESPACE, "component", "Icon");
  component.setSharedPluginData(RUN_NAMESPACE, "icon-name", definition.name);
  component.setSharedPluginData(
    RUN_NAMESPACE,
    "figma-name",
    definition.figmaName,
  );
  component.setSharedPluginData(
    RUN_NAMESPACE,
    "source-component-key",
    definition.componentKey,
  );

  if (component.layoutMode !== undefined) {
    component.layoutMode = "NONE";
  }
  component.resize(24, 24);
  component.clipsContent = true;
  component.fills = [];
  component.strokes = [];
  component.x = 80 + (index % 8) * 120;
  component.y = 80 + Math.floor(index / 8) * 96;

  if (component.children) {
    const existing = [];
    for (const child of component.children) existing.push(child);
    for (const child of existing) child.remove();
  }

  const source = sourceComponent.createInstance();
  source.name = "Pointr Source";
  source.x = 0;
  source.y = 0;
  try {
    source.resize(24, 24);
  } catch (error) {
    stats.warnings.push(
      `Could not resize icon "${definition.name}" source (${messageFor(error)}).`,
    );
  }
  component.appendChild(source);
  try {
    source.constraints = {
      horizontal: "STRETCH",
      vertical: "STRETCH",
    };
  } catch (error) {
    stats.warnings.push(
      `Could not set icon "${definition.name}" source constraints (${messageFor(error)}).`,
    );
  }
}

async function applyCuratedIconSourcesToSlots(stats) {
  const iconComponents = await findKozmosIconSourceComponents();
  const defaultComponent = await findKozmosIconSourceComponent(
    DEFAULT_CURATED_ICON_NAME,
  );

  if (!defaultComponent) {
    stats.warnings.push(
      `Could not apply Icon preferred values because ${kozmosIconComponentName(DEFAULT_CURATED_ICON_NAME)} was not found.`,
    );
    return;
  }

  const preferredValues = iconPreferredValues(iconComponents);
  const componentsPage = figma.root.children.find(
    (page) => page.name === "Components",
  );
  if (!componentsPage) return;

  await componentsPage.loadAsync();
  const targetNames = ["Button", "IconButton", "Badge"];

  for (const targetName of targetNames) {
    const componentSet = findComponentSetOnPage(componentsPage, targetName);
    if (!componentSet) continue;

    const propertyName = ensureInstanceSwapProperty(
      componentSet,
      BUTTON_ICON_PROPERTY,
      defaultComponent.id,
      stats,
      preferredValues,
    );
    if (propertyName) stats.preferredValuesApplied += 1;
  }
}

async function refreshComponentIconSlotsAfterIconSync(stats) {
  const componentsPage = figma.root.children.find(
    (page) => page.name === "Components",
  );
  if (!componentsPage) return;

  await componentsPage.loadAsync();
  const variableByName = await ensureComponentRuntimeVariables(stats);

  await refreshButtonSlotsIfPresent(componentsPage, variableByName, stats);
  await refreshIconButtonSlotsIfPresent(componentsPage, variableByName, stats);
  await refreshFloatingActionButtonSlotsIfPresent(
    componentsPage,
    variableByName,
    stats,
  );
  await refreshBadgeSlotsIfPresent(componentsPage, variableByName, stats);
}

async function configureButtonIconSlot(componentSet, variableByName, stats) {
  const iconComponent = await resolveDefaultIconSourceComponent(
    variableByName,
    stats,
  );
  const iconSourceComponents = await findKozmosIconSourceComponents();
  const preferredValues = iconPreferredValues(iconSourceComponents);
  const propertyName = ensureInstanceSwapProperty(
    componentSet,
    BUTTON_ICON_PROPERTY,
    iconComponent.id,
    stats,
    preferredValues,
  );

  deleteInstanceSwapPropertiesByBaseName(
    componentSet,
    [
      BUTTON_ICON_ON_FILL_PROPERTY,
      BUTTON_ICON_ON_ACCENT_PROPERTY,
      BUTTON_ICON_ON_NEUTRAL_PROPERTY,
    ],
    stats,
  );

  if (!propertyName) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseButtonVariantName(child.name);
    if (!props || props.size !== "Icon" || props.state === "Loading") {
      continue;
    }

    const config = buttonConfig(props.variant, props.state);
    const metrics = buttonMetrics(props.size);
    let icon = directChildNamed(child, "Icon");
    if (icon && icon.type === "INSTANCE" && !isGeneratedIconSlotWrapper(icon)) {
      syncIconSlotInstance(
        icon,
        config,
        variableByName,
        stats,
        metrics.iconSize,
        metrics.iconSizeToken,
      );
    } else {
      if (icon) icon.remove();
      icon = createIconSlotInstance(
        iconComponent,
        config.foreground,
        config.foregroundFallback,
        variableByName,
        stats,
        metrics.iconSize,
        metrics.iconSizeToken,
      );
    }
    child.appendChild(icon);
    bindInstanceSwapProperty(icon, propertyName, stats);
    stats.iconSlotsBound += 1;
  }
}

async function configureBadgeIconSlot(
  componentSet,
  iconComponent,
  variableByName,
  stats,
) {
  if (!iconComponent) return;

  const iconSourceComponents = await findKozmosIconSourceComponents();
  const preferredValues = iconPreferredValues(iconSourceComponents);
  const propertyName = ensureInstanceSwapProperty(
    componentSet,
    BUTTON_ICON_PROPERTY,
    iconComponent.id,
    stats,
    preferredValues,
  );

  if (!propertyName) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseBadgeVariantName(child.name);
    if (!props || props.size !== "Icon") continue;

    const config = badgeConfig(props.variant);
    let icon = directChildNamed(child, "Icon");
    if (icon && icon.type === "INSTANCE" && !isGeneratedIconSlotWrapper(icon)) {
      syncIconSlotInstance(
        icon,
        config,
        variableByName,
        stats,
        16,
        "Badge/icon/size",
      );
    } else {
      if (icon) icon.remove();
      icon = createIconSlotInstance(
        iconComponent,
        config.foreground,
        config.foregroundFallback,
        variableByName,
        stats,
        16,
        "Badge/icon/size",
      );
    }
    child.appendChild(icon);
    setHugChildSizing(icon);
    bindInstanceSwapProperty(icon, propertyName, stats);
    stats.iconSlotsBound = (stats.iconSlotsBound || 0) + 1;
  }
}

async function configureIconButtonSlot(
  componentSet,
  iconComponent,
  variableByName,
  stats,
) {
  const iconSourceComponents = await findKozmosIconSourceComponents();
  const preferredValues = iconPreferredValues(iconSourceComponents);
  const propertyName = ensureInstanceSwapProperty(
    componentSet,
    BUTTON_ICON_PROPERTY,
    iconComponent.id,
    stats,
    preferredValues,
  );

  if (!propertyName) return;

  for (const child of componentSet.children) {
    if (child.type !== "COMPONENT") continue;

    const props = parseIconButtonVariantName(child.name);
    if (!props || props.state === "Loading") {
      continue;
    }

    const config = buttonConfig(props.variant, props.state);
    const metrics = iconButtonMetrics(props.size);
    let icon = directChildNamed(child, "Icon");
    if (icon && icon.type === "INSTANCE" && !isGeneratedIconSlotWrapper(icon)) {
      syncIconSlotInstance(
        icon,
        config,
        variableByName,
        stats,
        metrics.iconSize,
        metrics.iconSizeToken,
      );
    } else {
      if (icon) icon.remove();
      icon = createIconSlotInstance(
        iconComponent,
        config.foreground,
        config.foregroundFallback,
        variableByName,
        stats,
        metrics.iconSize,
        metrics.iconSizeToken,
      );
    }
    child.appendChild(icon);
    bindInstanceSwapProperty(icon, propertyName, stats);
    stats.iconSlotsBound += 1;
  }
}

async function refreshIconButtonSlotsIfPresent(page, variableByName, stats) {
  const existing = findComponentSetOnPage(page, "IconButton");
  if (!existing) return;

  const iconComponent = await resolveDefaultIconSourceComponent(
    variableByName,
    stats,
  );
  await configureIconButtonSlot(existing, iconComponent, variableByName, stats);
  stats.relatedIconSlotsRefreshed = (stats.relatedIconSlotsRefreshed || 0) + 1;
}

async function refreshFloatingActionButtonSlotsIfPresent(
  page,
  variableByName,
  stats,
) {
  const existing = findComponentSetOnPage(page, "FloatingActionButton");
  if (!existing) return;

  const iconComponent = await resolveDefaultIconSourceComponent(
    variableByName,
    stats,
  );
  await configureFloatingActionButtonProperties(
    existing,
    iconComponent,
    variableByName,
    stats,
  );
  stats.relatedIconSlotsRefreshed = (stats.relatedIconSlotsRefreshed || 0) + 1;
}

async function refreshBadgeSlotsIfPresent(page, variableByName, stats) {
  const existing = findComponentSetOnPage(page, "Badge");
  if (!existing) return;

  const iconComponent = await resolveDefaultIconSourceComponent(
    variableByName,
    stats,
  );
  await configureBadgeIconSlot(existing, iconComponent, variableByName, stats);
  stats.relatedIconSlotsRefreshed = (stats.relatedIconSlotsRefreshed || 0) + 1;
}

async function refreshButtonSlotsIfPresent(page, variableByName, stats) {
  const existing = findComponentSetOnPage(page, "Button");
  if (!existing) return;

  await configureButtonIconSlot(existing, variableByName, stats);
  stats.relatedIconSlotsRefreshed = (stats.relatedIconSlotsRefreshed || 0) + 1;
}

async function pruneLegacyGeneratedIconSlotComponents(stats) {
  const utilities = figma.root.children.find(
    (page) => page.name === "Utilities",
  );
  if (!utilities) return;

  await utilities.loadAsync();
  const legacy = [];
  for (const child of utilities.children) {
    if (isLegacyGeneratedIconSlotComponent(child)) {
      legacy.push(child);
    }
  }

  if (legacy.length === 0) return;

  for (const component of legacy) {
    component.remove();
    stats.legacyIconSlotsRemoved = (stats.legacyIconSlotsRemoved || 0) + 1;
  }
}

function isLegacyGeneratedIconSlotComponent(node) {
  return node.type === "COMPONENT" && /^Icon \/ Slot \//.test(node.name);
}

function safeComponentPropertyDefinitions(componentSet, stats, context) {
  try {
    return {
      definitions: componentSet.componentPropertyDefinitions || {},
      error: null,
    };
  } catch (error) {
    const message = messageFor(error);
    if (stats) {
      stats.warnings.push(
        `Skipped ${context} because Figma could not read component property definitions (${message}). The variant nodes were still updated; run the updater again after Figma clears the existing property errors.`,
      );
    }
    return {
      definitions: {},
      error: message,
    };
  }
}

function normalizeComponentSetVariantProperties(
  componentSet,
  expectedAxes,
  stats,
) {
  if (!expectedAxes) return;

  const read = safeComponentPropertyDefinitions(
    componentSet,
    stats,
    "normalize variant properties",
  );
  const definitions = read.definitions;
  if (read.error) return;

  for (const propertyName of Object.keys(definitions)) {
    const definition = definitions[propertyName];
    if (definition.type !== "VARIANT") continue;

    const baseName = propertyName.split("#")[0];
    if (expectedAxes[baseName]) continue;

    if (!componentSet.deleteComponentProperty) {
      stats.warnings.push(
        `Could not remove unexpected variant property "${propertyName}"; this Figma runtime does not expose deleteComponentProperty.`,
      );
      continue;
    }

    try {
      componentSet.deleteComponentProperty(propertyName);
      stats.variantPropertiesRemoved =
        (stats.variantPropertiesRemoved || 0) + 1;
    } catch (error) {
      stats.warnings.push(
        `Could not remove unexpected variant property "${propertyName}" (${messageFor(error)}).`,
      );
    }
  }
}

function ensureInstanceSwapProperty(
  componentSet,
  name,
  defaultValue,
  stats,
  preferredValues,
) {
  const read = safeComponentPropertyDefinitions(
    componentSet,
    stats,
    `ensure ${name} instance-swap property`,
  );
  const definitions = read.definitions;
  if (read.error) return null;

  for (const propertyName of Object.keys(definitions)) {
    const definition = definitions[propertyName];
    const baseName = propertyName.split("#")[0];
    if (baseName === name && definition.type === "INSTANCE_SWAP") {
      updateInstanceSwapPropertyDefault(
        componentSet,
        propertyName,
        defaultValue,
        preferredValues,
      );
      return propertyName;
    }
  }

  if (!componentSet.addComponentProperty) {
    stats.warnings.push(
      "This Figma runtime does not expose addComponentProperty for Icon slots.",
    );
    return null;
  }

  try {
    const options = {};
    if (preferredValues && preferredValues.length > 0) {
      options.preferredValues = preferredValues;
    }

    return componentSet.addComponentProperty(
      name,
      "INSTANCE_SWAP",
      defaultValue,
      options,
    );
  } catch (error) {
    stats.warnings.push(
      `Could not create Icon instance-swap property (${messageFor(error)}).`,
    );
    return null;
  }
}

function ensureSlotProperty(componentSet, name, stats) {
  const read = safeComponentPropertyDefinitions(
    componentSet,
    stats,
    `ensure ${name} slot property`,
  );
  const definitions = read.definitions;
  if (read.error) return null;

  for (const propertyName of Object.keys(definitions)) {
    const definition = definitions[propertyName];
    const baseName = propertyName.split("#")[0];
    if (baseName === name && definition.type === "SLOT") {
      return propertyName;
    }
  }

  if (!componentSet.addComponentProperty) {
    stats.warnings.push(
      `This Figma runtime does not expose addComponentProperty for ${name}.`,
    );
    return null;
  }

  try {
    return componentSet.addComponentProperty(name, "SLOT", "");
  } catch (error) {
    stats.warnings.push(
      `Could not create ${name} slot property (${messageFor(error)}).`,
    );
    return null;
  }
}

function updateInstanceSwapPropertyDefault(
  componentSet,
  propertyName,
  defaultValue,
  preferredValues,
) {
  if (!componentSet.editComponentProperty) return;

  try {
    const nextValue = { defaultValue: defaultValue };
    if (preferredValues && preferredValues.length > 0) {
      nextValue.preferredValues = preferredValues;
    }

    componentSet.editComponentProperty(propertyName, nextValue);
  } catch (_error) {
    // Older plugin runtimes may not support editing the default value in place.
  }
}

function configureLabelTextProperty(componentSet, defaultValue, stats) {
  const propertyName = ensureTextProperty(
    componentSet,
    "Label Text",
    defaultValue,
    stats,
  );
  if (!propertyName) return;

  let boundCount = 0;

  function walk(node) {
    if (node.type === "TEXT" && node.name === "Label Text") {
      bindTextProperty(node, propertyName, stats);
      boundCount += 1;
    }

    if (node.children) {
      for (const child of node.children) walk(child);
    }
  }

  walk(componentSet);
  stats.labelTextBindings = boundCount;
}

function configurePlaceholderTextProperty(componentSet, defaultValue, stats) {
  const propertyName = ensureTextProperty(
    componentSet,
    "Placeholder Text",
    defaultValue,
    stats,
  );
  if (!propertyName) return;

  let boundCount = 0;

  function walk(node) {
    if (node.type === "TEXT" && node.name === "Placeholder Text") {
      bindTextProperty(node, propertyName, stats);
      boundCount += 1;
    }

    if (node.children) {
      for (const child of node.children) walk(child);
    }
  }

  walk(componentSet);
  stats.placeholderTextBindings = boundCount;
}

function configureHelperTextProperty(componentSet, defaultValue, stats) {
  const propertyName = ensureTextProperty(
    componentSet,
    "Helper Text",
    defaultValue,
    stats,
  );
  if (!propertyName) return;

  let boundCount = 0;

  function walk(node) {
    if (node.type === "TEXT" && node.name === "Helper Text") {
      bindTextProperty(node, propertyName, stats);
      boundCount += 1;
    }

    if (node.children) {
      for (const child of node.children) walk(child);
    }
  }

  walk(componentSet);
  stats.helperTextBindings = boundCount;
}

function configureNamedTextProperty(
  componentSet,
  nodeName,
  propertyName,
  defaultValue,
  stats,
) {
  const resolvedPropertyName = ensureTextProperty(
    componentSet,
    propertyName,
    defaultValue,
    stats,
  );
  if (!resolvedPropertyName) return;

  let boundCount = 0;

  function walk(node) {
    if (node.type === "TEXT" && node.name === nodeName) {
      bindTextProperty(node, resolvedPropertyName, stats);
      boundCount += 1;
    }

    if (node.children) {
      for (const child of node.children) walk(child);
    }
  }

  walk(componentSet);
  const key = `${propertyName
    .replace(/[^A-Za-z0-9]+/g, "")
    .replace(/^./, (letter) => letter.toLowerCase())}Bindings`;
  stats[key] = boundCount;
}

function configureHelperVisibilityProperty(componentSet, stats) {
  const propertyName = ensureBooleanProperty(
    componentSet,
    "Show Helper Text",
    false,
    stats,
  );
  if (!propertyName) return;

  let boundCount = 0;

  function walk(node) {
    if (node.type === "TEXT" && node.name === "Helper Text") {
      bindVisibilityProperty(node, propertyName, stats);
      boundCount += 1;
    }

    if (node.children) {
      for (const child of node.children) walk(child);
    }
  }

  walk(componentSet);
  stats.helperVisibilityBindings = boundCount;
}

function configureBadgeCounterVisibilityProperty(componentSet, stats) {
  const propertyName = ensureBooleanProperty(
    componentSet,
    "Show Counter",
    false,
    stats,
  );
  if (!propertyName) return;

  let boundCount = 0;

  function walk(node) {
    if (isBadgeCounterNode(node)) {
      bindVisibilityProperty(node, propertyName, stats);
      boundCount += 1;
    }

    if (node.children) {
      for (const child of node.children) walk(child);
    }
  }

  walk(componentSet);
  stats.badgeCounterVisibilityBindings = boundCount;
}

function exposeNestedCounterInstance(instance, stats) {
  if (!instance || instance.type !== "INSTANCE") return;
  if (!nodeHasComponentAncestor(instance)) {
    if (stats && stats.warnings) {
      stats.warnings.push(
        "Could not expose nested Counter instance because it is not inside a component yet.",
      );
    }
    return;
  }

  try {
    instance.isExposedInstance = true;
  } catch (error) {
    if (stats && stats.warnings) {
      stats.warnings.push(
        `Could not expose nested Counter instance (${messageFor(error)}).`,
      );
    }
  }
}

function nodeHasComponentAncestor(node) {
  let current = node ? node.parent : null;
  while (current) {
    if (current.type === "COMPONENT" || current.type === "COMPONENT_SET") {
      return true;
    }
    current = current.parent;
  }

  return false;
}

function configureFocusVisibleProperty(componentSet, stats) {
  const propertyName = ensureBooleanProperty(
    componentSet,
    FOCUS_VISIBLE_PROPERTY_NAME,
    false,
    stats,
  );
  if (!propertyName) return;

  let boundCount = 0;

  function walk(node) {
    if (
      node.name === "Focus Ring" &&
      node.getSharedPluginData &&
      node.getSharedPluginData(RUN_NAMESPACE, "kind") === "focus-ring"
    ) {
      bindVisibilityProperty(node, propertyName, stats);
      boundCount += 1;
    }

    if (node.children) {
      for (const child of node.children) walk(child);
    }
  }

  walk(componentSet);
  stats.focusVisibleBindings = boundCount;
}

function ensureTextProperty(componentSet, name, defaultValue, stats) {
  const read = safeComponentPropertyDefinitions(
    componentSet,
    stats,
    `ensure ${name} text property`,
  );
  const definitions = read.definitions;
  if (read.error) return null;

  for (const propertyName of Object.keys(definitions)) {
    const definition = definitions[propertyName];
    const baseName = propertyName.split("#")[0];
    if (baseName === name && definition.type === "TEXT") {
      updateTextPropertyDefault(componentSet, propertyName, defaultValue);
      return propertyName;
    }
  }

  if (!componentSet.addComponentProperty) {
    stats.warnings.push(
      `This Figma runtime does not expose addComponentProperty for ${name}.`,
    );
    return null;
  }

  try {
    return componentSet.addComponentProperty(name, "TEXT", defaultValue);
  } catch (error) {
    stats.warnings.push(
      `Could not create ${name} text property (${messageFor(error)}).`,
    );
    return null;
  }
}

function ensureBooleanProperty(componentSet, name, defaultValue, stats) {
  const read = safeComponentPropertyDefinitions(
    componentSet,
    stats,
    `ensure ${name} boolean property`,
  );
  const definitions = read.definitions;
  if (read.error) return null;

  for (const propertyName of Object.keys(definitions)) {
    const definition = definitions[propertyName];
    const baseName = propertyName.split("#")[0];
    if (baseName === name && definition.type === "BOOLEAN") {
      updateBooleanPropertyDefault(componentSet, propertyName, defaultValue);
      return propertyName;
    }
  }

  if (!componentSet.addComponentProperty) {
    stats.warnings.push(
      `This Figma runtime does not expose addComponentProperty for ${name}.`,
    );
    return null;
  }

  try {
    return componentSet.addComponentProperty(name, "BOOLEAN", defaultValue);
  } catch (error) {
    stats.warnings.push(
      `Could not create ${name} boolean property (${messageFor(error)}).`,
    );
    return null;
  }
}

function updateTextPropertyDefault(componentSet, propertyName, defaultValue) {
  if (!componentSet.editComponentProperty) return;

  try {
    componentSet.editComponentProperty(propertyName, { defaultValue });
  } catch (_error) {
    // Older plugin runtimes may not support editing the default value in place.
  }
}

function updateBooleanPropertyDefault(
  componentSet,
  propertyName,
  defaultValue,
) {
  if (!componentSet.editComponentProperty) return;

  try {
    componentSet.editComponentProperty(propertyName, { defaultValue });
  } catch (_error) {
    // Older plugin runtimes may not support editing the default value in place.
  }
}

function bindTextProperty(textNode, propertyName, stats) {
  if (!textNode || textNode.type !== "TEXT") {
    stats.warnings.push(
      "Could not bind Label Text property; the generated label is not a text node.",
    );
    return;
  }

  try {
    const references = {};
    const existing = textNode.componentPropertyReferences || {};
    for (const key of Object.keys(existing)) {
      references[key] = existing[key];
    }
    references.characters = propertyName;
    textNode.componentPropertyReferences = references;
  } catch (error) {
    stats.warnings.push(
      `Could not bind Label Text property (${messageFor(error)}).`,
    );
  }
}

function bindVisibilityProperty(node, propertyName, stats) {
  if (!node) {
    stats.warnings.push(
      "Could not bind visibility property; the generated node is missing.",
    );
    return;
  }

  try {
    const references = {};
    const existing = node.componentPropertyReferences || {};
    for (const key of Object.keys(existing)) {
      references[key] = existing[key];
    }
    references.visible = propertyName;
    node.componentPropertyReferences = references;
  } catch (error) {
    stats.warnings.push(
      `Could not bind visibility property (${messageFor(error)}).`,
    );
  }
}

function deleteComponentPropertiesByBaseName(
  componentSet,
  baseNames,
  types,
  stats,
) {
  const read = safeComponentPropertyDefinitions(
    componentSet,
    stats,
    "delete stale component properties",
  );
  const definitions = read.definitions;
  if (read.error) return;

  for (const propertyName of Object.keys(definitions)) {
    const definition = definitions[propertyName];
    const baseName = propertyName.split("#")[0];
    if (baseNames.indexOf(baseName) === -1) continue;
    if (types && types.indexOf(definition.type) === -1) continue;

    if (!componentSet.deleteComponentProperty) {
      continue;
    }

    try {
      componentSet.deleteComponentProperty(propertyName);
      stats.componentPropertiesRemoved =
        (stats.componentPropertiesRemoved || 0) + 1;
    } catch (error) {
      stats.warnings.push(
        `Could not remove stale component property "${propertyName}" (${messageFor(error)}).`,
      );
    }
  }
}

function deleteDuplicateComponentPropertiesByBaseName(
  componentSet,
  baseName,
  types,
  keepPropertyName,
  stats,
) {
  const read = safeComponentPropertyDefinitions(
    componentSet,
    stats,
    "delete duplicate component properties",
  );
  const definitions = read.definitions;
  if (read.error) return;

  for (const propertyName of Object.keys(definitions)) {
    const definition = definitions[propertyName];
    if (propertyName === keepPropertyName) continue;
    if (propertyName.split("#")[0] !== baseName) continue;
    if (types && types.indexOf(definition.type) === -1) continue;
    if (!componentSet.deleteComponentProperty) continue;

    try {
      componentSet.deleteComponentProperty(propertyName);
      stats.componentPropertiesRemoved =
        (stats.componentPropertiesRemoved || 0) + 1;
    } catch (error) {
      stats.warnings.push(
        `Could not remove duplicate component property "${propertyName}" (${messageFor(error)}).`,
      );
    }
  }
}

function deleteInstanceSwapPropertiesByBaseName(
  componentSet,
  baseNames,
  stats,
) {
  const read = safeComponentPropertyDefinitions(
    componentSet,
    stats,
    "delete stale instance-swap properties",
  );
  const definitions = read.definitions;
  if (read.error) return;

  for (const propertyName of Object.keys(definitions)) {
    const definition = definitions[propertyName];
    const baseName = propertyName.split("#")[0];
    if (
      baseNames.indexOf(baseName) === -1 ||
      definition.type !== "INSTANCE_SWAP"
    ) {
      continue;
    }

    if (!componentSet.deleteComponentProperty) {
      continue;
    }

    try {
      componentSet.deleteComponentProperty(propertyName);
      stats.iconSlotPropertiesRemoved =
        (stats.iconSlotPropertiesRemoved || 0) + 1;
    } catch (error) {
      stats.warnings.push(
        `Could not remove stale Icon property "${propertyName}" (${messageFor(error)}).`,
      );
    }
  }
}

function bindInstanceSwapProperty(instance, propertyName, stats) {
  const target = instance;
  if (!target || target.type !== "INSTANCE") {
    stats.warnings.push(
      "Could not bind Icon instance-swap property; the generated slot is not an instance.",
    );
    return;
  }

  try {
    target.componentPropertyReferences = {
      mainComponent: propertyName,
    };
  } catch (error) {
    stats.warnings.push(
      `Could not bind Icon instance-swap property (${messageFor(error)}).`,
    );
  }
}

function tintIconSlotMark(icon, config, variableByName, stats) {
  if (isGeneratedIconSlotWrapper(icon)) {
    stats.warnings.push(
      "Encountered a legacy masked Icon wrapper while tinting. Run Update Button/IconButton to rebuild direct icon instances.",
    );
    return;
  }

  if (icon.type === "INSTANCE") {
    applyIconColorOverrides(
      icon,
      config.foreground,
      config.foregroundFallback,
      variableByName,
      stats,
    );
    return;
  }

  const paint = paintFromVariable(
    config.foreground,
    config.foregroundFallback,
    variableByName,
    stats,
  );
  let applied = false;

  function walk(node) {
    try {
      if (node.name === "Icon Circle" && node.type === "ELLIPSE") {
        node.fills = [];
        node.strokes = [clonePaint(paint)];
        node.strokeWeight = 2;
        applied = true;
      } else if (node.name === "Icon Handle" && node.type === "RECTANGLE") {
        node.fills = [clonePaint(paint)];
        node.strokes = [];
        applied = true;
      } else if (node.name === "Icon Mark" && node.type === "RECTANGLE") {
        node.fills = [clonePaint(paint)];
        node.strokes = [];
        applied = true;
      }
    } catch (error) {
      stats.warnings.push(
        `Could not tint icon slot "${node.name}" (${messageFor(error)}).`,
      );
    }

    if (node.children) {
      for (const child of node.children) walk(child);
    }
  }

  walk(icon);

  if (!applied && icon.type === "FRAME") {
    resetIconGlyph(
      icon,
      config.foreground,
      config.foregroundFallback,
      variableByName,
      stats,
      Math.round(icon.width || 16),
    );
  }
}

function isGeneratedIconSlotWrapper(node) {
  return (
    node &&
    node.name === "Icon" &&
    node.type === "FRAME" &&
    node.getSharedPluginData &&
    node.getSharedPluginData(RUN_NAMESPACE, "kind") === "icon-slot-instance"
  );
}

function applyIconColorOverrides(
  icon,
  variableName,
  fallback,
  variableByName,
  stats,
) {
  const paint = paintFromVariable(
    variableName,
    fallback,
    variableByName,
    stats,
  );
  let applied = false;

  function walk(node) {
    try {
      if (Array.isArray(node.fills) && firstVisibleSolidPaint(node.fills)) {
        node.fills = [clonePaint(paint)];
        applied = true;
      }

      if (Array.isArray(node.strokes) && firstVisibleSolidPaint(node.strokes)) {
        node.strokes = [clonePaint(paint)];
        applied = true;
      }
    } catch (error) {
      stats.warnings.push(
        `Could not override icon color on "${node.name || node.type}" (${messageFor(error)}).`,
      );
    }

    if (node.children) {
      for (const child of node.children) walk(child);
    }
  }

  walk(icon);

  if (!applied) {
    stats.warnings.push(
      `Could not find tintable fill or stroke layers in Icon slot for "${variableName}".`,
    );
  }
}

function resetIconGlyph(
  parent,
  variableName,
  fallback,
  variableByName,
  stats,
  size,
) {
  const iconSize = size || 16;
  const paint = paintFromVariable(
    variableName,
    fallback,
    variableByName,
    stats,
  );

  if (parent.layoutMode !== undefined) {
    parent.layoutMode = "NONE";
  }
  parent.resize(iconSize, iconSize);
  parent.fills = [];
  parent.strokes = [];

  if (parent.children) {
    const existing = [];
    for (const child of parent.children) existing.push(child);
    for (const child of existing) child.remove();
  }

  const circle = figma.createEllipse();
  circle.name = "Icon Circle";
  circle.resize(iconSize * 0.5, iconSize * 0.5);
  circle.x = iconSize * 0.18;
  circle.y = iconSize * 0.18;
  circle.fills = [];
  circle.strokes = [clonePaint(paint)];
  circle.strokeWeight = Math.max(1.5, iconSize * 0.12);
  circle.constraints = {
    horizontal: "SCALE",
    vertical: "SCALE",
  };

  const handle = figma.createRectangle();
  handle.name = "Icon Handle";
  handle.resize(iconSize * 0.34, Math.max(1.5, iconSize * 0.12));
  handle.x = iconSize * 0.58;
  handle.y = iconSize * 0.7;
  handle.rotation = 45;
  handle.cornerRadius = Math.max(1, iconSize * 0.06);
  handle.fills = [clonePaint(paint)];
  handle.strokes = [];
  handle.constraints = {
    horizontal: "SCALE",
    vertical: "SCALE",
  };

  parent.appendChild(circle);
  parent.appendChild(handle);
}

function clonePaint(paint) {
  const copy = {
    type: paint.type,
    color: {
      r: paint.color.r,
      g: paint.color.g,
      b: paint.color.b,
    },
    opacity: paint.opacity,
  };

  if (paint.boundVariables) copy.boundVariables = paint.boundVariables;
  if (paint.visible !== undefined) copy.visible = paint.visible;
  if (paint.blendMode) copy.blendMode = paint.blendMode;
  return copy;
}

function buttonMetrics(size) {
  if (size === "Small") {
    return {
      width: 96,
      height: 44,
      paddingX: 12,
      gap: 8,
      iconSize: 16,
      iconSizeToken: "Button/icon/size",
      spinnerSize: 14,
      spinnerSizeToken: "Button/spinner/size",
      widthToken: "Button/width/small",
      heightToken: "Button/height/small",
      paddingXToken: "Button/padding/x/small",
      gapToken: "Button/gap/default",
    };
  }

  if (size === "Large") {
    return {
      width: 136,
      height: 44,
      paddingX: 32,
      gap: 8,
      iconSize: 16,
      iconSizeToken: "Button/icon/size",
      spinnerSize: 14,
      spinnerSizeToken: "Button/spinner/size",
      widthToken: "Button/width/large",
      heightToken: "Button/height/large",
      paddingXToken: "Button/padding/x/large",
      gapToken: "Button/gap/default",
    };
  }

  if (size === "Icon") {
    return {
      width: 44,
      height: 44,
      paddingX: 0,
      gap: 0,
      iconSize: 16,
      iconSizeToken: "Button/icon/size",
      spinnerSize: 14,
      spinnerSizeToken: "Button/spinner/size",
      widthToken: "Button/width/icon",
      heightToken: "Button/height/icon",
      paddingXToken: "Button/padding/x/icon",
      gapToken: "Button/gap/icon",
    };
  }

  return {
    width: 112,
    height: 44,
    paddingX: 16,
    gap: 8,
    iconSize: 16,
    iconSizeToken: "Button/icon/size",
    spinnerSize: 14,
    spinnerSizeToken: "Button/spinner/size",
    widthToken: "Button/width/default",
    heightToken: "Button/height/default",
    paddingXToken: "Button/padding/x/default",
    gapToken: "Button/gap/default",
  };
}

function iconButtonMetrics(size) {
  if (size === "Small") {
    return {
      size: 44,
      iconSize: 14,
      spinnerSize: 14,
      sizeToken: "IconButton/size/small",
      iconSizeToken: "IconButton/icon/size/small",
      spinnerSizeToken: "IconButton/spinner/size/small",
    };
  }

  if (size === "Large") {
    return {
      size: 44,
      iconSize: 20,
      spinnerSize: 16,
      sizeToken: "IconButton/size/large",
      iconSizeToken: "IconButton/icon/size/large",
      spinnerSizeToken: "IconButton/spinner/size/large",
    };
  }

  return {
    size: 44,
    iconSize: 16,
    spinnerSize: 14,
    sizeToken: "IconButton/size/default",
    iconSizeToken: "IconButton/icon/size/default",
    spinnerSizeToken: "IconButton/spinner/size/default",
  };
}

function toggleButtonMetrics(size) {
  if (size === "Small") {
    return {
      width: 96,
      height: 44,
      paddingX: 12,
      widthToken: "ToggleButton/width/small",
      heightToken: "ToggleButton/height/small",
      paddingXToken: "ToggleButton/padding/x/small",
    };
  }

  if (size === "Large") {
    return {
      width: 136,
      height: 44,
      paddingX: 24,
      widthToken: "ToggleButton/width/large",
      heightToken: "ToggleButton/height/large",
      paddingXToken: "ToggleButton/padding/x/large",
    };
  }

  return {
    width: 112,
    height: 44,
    paddingX: 16,
    widthToken: "ToggleButton/width/default",
    heightToken: "ToggleButton/height/default",
    paddingXToken: "ToggleButton/padding/x/default",
  };
}

function splitButtonMetrics(size) {
  if (size === "Small") {
    return {
      width: 160,
      height: 44,
      triggerWidth: 44,
      mainPaddingX: 12,
      widthToken: "SplitButton/width/small",
      heightToken: "SplitButton/height/small",
      triggerWidthToken: "SplitButton/trigger/width/small",
      mainPaddingXToken: "SplitButton/main/padding/x/small",
    };
  }

  if (size === "Large") {
    return {
      width: 216,
      height: 44,
      triggerWidth: 44,
      mainPaddingX: 24,
      widthToken: "SplitButton/width/large",
      heightToken: "SplitButton/height/large",
      triggerWidthToken: "SplitButton/trigger/width/large",
      mainPaddingXToken: "SplitButton/main/padding/x/large",
    };
  }

  return {
    width: 184,
    height: 44,
    triggerWidth: 44,
    mainPaddingX: 16,
    widthToken: "SplitButton/width/default",
    heightToken: "SplitButton/height/default",
    triggerWidthToken: "SplitButton/trigger/width/default",
    mainPaddingXToken: "SplitButton/main/padding/x/default",
  };
}

function floatingActionButtonMetrics(size) {
  if (size === "Small") {
    return {
      size: 48,
      iconSize: 20,
      sizeToken: "FloatingActionButton/size/small",
      iconSizeToken: "FloatingActionButton/icon/size/small",
    };
  }

  if (size === "Large") {
    return {
      size: 64,
      iconSize: 28,
      sizeToken: "FloatingActionButton/size/large",
      iconSizeToken: "FloatingActionButton/icon/size/large",
    };
  }

  return {
    size: 56,
    iconSize: 24,
    sizeToken: "FloatingActionButton/size/default",
    iconSizeToken: "FloatingActionButton/icon/size/default",
  };
}

function badgeMetrics(size) {
  if (size === "Small") {
    return {
      width: 58,
      height: 44,
      paddingX: 12,
      heightToken: "Badge/height/small",
      paddingXToken: "Badge/padding/x/small",
    };
  }

  if (size === "Large") {
    return {
      width: 86,
      height: 44,
      paddingX: 32,
      heightToken: "Badge/height/large",
      paddingXToken: "Badge/padding/x/large",
    };
  }

  if (size === "Icon") {
    return {
      width: 44,
      height: 44,
      paddingX: 0,
      iconSize: 16,
      widthToken: "Badge/width/icon",
      heightToken: "Badge/height/icon",
      paddingXToken: "Badge/padding/x/icon",
      iconSizeToken: "Badge/icon/size",
    };
  }

  return {
    width: 66,
    height: 44,
    paddingX: 16,
    heightToken: "Badge/height/default",
    paddingXToken: "Badge/padding/x/default",
  };
}

function chipMetrics(size) {
  if (size === "Small") {
    return {
      minWidth: 48,
      height: 28,
      paddingX: 12,
      gap: 6,
      fontSize: 12,
      lineHeight: 16,
      heightToken: "Chip/height/small",
      paddingXToken: "Chip/padding/x/small",
      fontSizeToken: "Chip/label/font-size/small",
      lineHeightToken: "Chip/label/line-height/small",
    };
  }

  if (size === "Large") {
    return {
      minWidth: 72,
      height: 36,
      paddingX: 20,
      gap: 6,
      fontSize: 14,
      lineHeight: 20,
      heightToken: "Chip/height/large",
      paddingXToken: "Chip/padding/x/large",
      fontSizeToken: "Chip/label/font-size/large",
      lineHeightToken: "Chip/label/line-height/large",
    };
  }

  return {
    minWidth: 64,
    height: 32,
    paddingX: 16,
    gap: 6,
    fontSize: 14,
    lineHeight: 20,
    heightToken: "Chip/height/default",
    paddingXToken: "Chip/padding/x/default",
    fontSizeToken: "Chip/label/font-size/default",
    lineHeightToken: "Chip/label/line-height/default",
  };
}

function counterMetrics(size) {
  if (size === "Small") {
    return {
      minWidth: 18,
      height: 18,
      paddingX: 5,
      fontSize: 11,
      lineHeight: 14,
      minWidthToken: "Counter/min-width/small",
      heightToken: "Counter/height/small",
      paddingXToken: "Counter/padding/x/small",
      fontSizeToken: "Counter/font-size/small",
      lineHeightToken: "Counter/line-height/small",
    };
  }

  return {
    minWidth: 20,
    height: 20,
    paddingX: 6,
    fontSize: 12,
    lineHeight: 16,
    minWidthToken: "Counter/min-width/default",
    heightToken: "Counter/height/default",
    paddingXToken: "Counter/padding/x/default",
    fontSizeToken: "Counter/font-size/default",
    lineHeightToken: "Counter/line-height/default",
  };
}

function paginationMetrics(size, content) {
  const compact = size === "Small";
  const key = compact ? "small" : "default";
  const contentKey = String(content || "Basic").toLowerCase();
  const widthByContent = {
    Basic: compact ? 332 : 372,
    Ellipsis: compact ? 428 : 468,
    Compact: compact ? 252 : 304,
  };

  return {
    size,
    content,
    key,
    width: widthByContent[content] || widthByContent.Basic,
    height: 44,
    itemSize: 44,
    actionWidth: compact ? 92 : 112,
    compactLabelWidth: compact ? 60 : 72,
    gap: 4,
    radius: 8,
    iconSize: 16,
    fontSize: compact ? 12 : 14,
    lineHeight: compact ? 16 : 20,
    widthToken: `Pagination/width/${contentKey}/${key}`,
    actionWidthToken: `Pagination/action/width/${key}`,
    compactLabelWidthToken: `Pagination/compact-label/width/${key}`,
    fontSizeToken: `Pagination/label/font-size/${key}`,
    lineHeightToken: `Pagination/label/line-height/${key}`,
  };
}

function badgeLabelText(variant, size) {
  if (size === "Icon") return "1";
  if (variant === "Default") return "Badge";
  return variant;
}

function chipLabelText(variant) {
  if (variant === "Brand") return "Featured";
  if (variant === "Destructive") return "Remove";
  return "Filter";
}

function counterToneForBadgeVariant(variant) {
  if (variant === "Default" || variant === "Destructive") return "Inverse";
  return "Neutral";
}

function cardTitleText(_content) {
  return "Create project";
}

function cardDescriptionText(_content) {
  return "Deploy your new project in one click.";
}

function cardBodyText(content) {
  if (content === "Basic") {
    return "Use a basic Card when the surrounding heading already provides enough context.";
  }

  if (content === "Full") {
    return "Full cards can carry a short decision, summary, or setting with local supporting actions.";
  }

  return "Header cards pair a clear title with supporting content so related information stays scannable.";
}

function tabsDefaultLabel(index) {
  return (
    ["Overview", "Details", "Usage", "History"][index] || `Tab ${index + 1}`
  );
}

function textVariantCombinations() {
  const combinations = [];

  for (const size of TEXT_SIZES) {
    for (const weight of TEXT_WEIGHTS) {
      for (const tone of TEXT_TONES) {
        combinations.push({ size, weight, tone });
      }
    }
  }

  return combinations;
}

function textVariantKey(props) {
  return `${props.size}/${props.weight}/${props.tone}`;
}

function stackVariantCombinations() {
  const combinations = [];

  for (const direction of STACK_DIRECTIONS) {
    for (const gap of STACK_GAPS) {
      combinations.push({ direction, gap });
    }
  }

  return combinations;
}

function stackVariantKey(props) {
  return `${props.direction}/${props.gap}`;
}

function gridVariantCombinations() {
  const combinations = [];

  for (const columns of GRID_COLUMNS) {
    for (const gap of GRID_GAPS) {
      combinations.push({ columns, gap });
    }
  }

  return combinations;
}

function gridVariantKey(props) {
  return `${props.columns}/${props.gap}`;
}

function textMetrics(size) {
  const metrics = {
    XS: { fontSize: 12, lineHeight: 16, token: "xs" },
    Small: { fontSize: 14, lineHeight: 20, token: "sm" },
    Base: { fontSize: 16, lineHeight: 24, token: "base" },
    Large: { fontSize: 18, lineHeight: 28, token: "lg" },
    XLarge: { fontSize: 20, lineHeight: 28, token: "xl" },
    "2XLarge": { fontSize: 24, lineHeight: 32, token: "2xl" },
    "3XLarge": { fontSize: 30, lineHeight: 36, token: "3xl" },
    "4XLarge": { fontSize: 36, lineHeight: 40, token: "4xl" },
  };

  return metrics[size] || metrics.Base;
}

function textFontForWeight(weight, fonts) {
  if (weight === "Bold") return fonts.bold || fonts.medium || fonts.regular;
  if (weight === "Semibold" || weight === "Medium") {
    return fonts.medium || fonts.regular;
  }
  return fonts.regular;
}

function textToneConfig(tone) {
  const configs = {
    Default: {
      foreground: "Colors/foreground/0",
      foregroundFallback: "#000000",
    },
    Muted: {
      foreground: "Colors/foreground/400",
      foregroundFallback: "#5D626F",
    },
    Primary: {
      foreground: "Colors/theme/600",
      foregroundFallback: "#1051E8",
    },
    Destructive: {
      foreground: "Colors/emotional/danger/600",
      foregroundFallback: "#D41C42",
    },
  };

  return configs[tone] || configs.Default;
}

function linkConfig(variant) {
  if (variant === "Subtle") {
    return {
      foreground: "Colors/foreground/400",
      foregroundFallback: "#5D626F",
    };
  }

  return {
    foreground: "Colors/theme/600",
    foregroundFallback: "#1051E8",
  };
}

function textSampleForSize(size) {
  if (size === "XS" || size === "Small") return "Kozmos label";
  if (size === "3XLarge" || size === "4XLarge") return "Kozmos";
  return "Kozmos text";
}

function headingMetrics(level) {
  const metrics = {
    H1: { fontSize: 36, lineHeight: 40, token: "4xl" },
    H2: { fontSize: 30, lineHeight: 36, token: "3xl" },
    H3: { fontSize: 24, lineHeight: 32, token: "2xl" },
    H4: { fontSize: 20, lineHeight: 28, token: "xl" },
    H5: { fontSize: 18, lineHeight: 28, token: "lg" },
    H6: { fontSize: 16, lineHeight: 24, token: "base" },
  };

  return metrics[level] || metrics.H2;
}

function headingSampleForLevel(level) {
  if (level === "H1" || level === "H2") return "Kozmos";
  return `${level} Heading`;
}

function skeletonMetrics(shape) {
  if (shape === "Circle") {
    return {
      width: 40,
      height: 40,
      radius: 9999,
      widthToken: "Skeleton/size/circle",
      heightToken: "Skeleton/size/circle",
      radiusToken: "Skeleton/radius/circle",
    };
  }

  if (shape === "Block") {
    return {
      width: 320,
      height: 80,
      radius: 8,
      widthToken: "Skeleton/width/block",
      heightToken: "Skeleton/height/block",
      radiusToken: "Skeleton/radius/default",
    };
  }

  return {
    width: 240,
    height: 16,
    radius: 8,
    widthToken: "Skeleton/width/line",
    heightToken: "Skeleton/height/line",
    radiusToken: "Skeleton/radius/default",
  };
}

function boxSurfaceConfig(surface) {
  if (surface === "Surface") {
    return {
      background: "Colors/background/100",
      backgroundFallback: "#E4E6EA",
    };
  }

  if (surface === "Outlined") {
    return {
      background: "Surface/0",
      stroke: "Colors/background/200",
      backgroundFallback: "#FFFFFF",
      strokeFallback: "#C7CAD1",
    };
  }

  return {};
}

function tabsWidthForCount(count) {
  if (count === "Four") return 640;
  if (count === "Three") return 480;
  return 320;
}

function tabsActiveIsValidForCount(active, count) {
  const tabCount = TABS_COUNT_TO_NUMBER[count];
  const activeIndex = TABS_ACTIVE_TO_INDEX[active];
  return (
    Number.isFinite(tabCount) && activeIndex >= 0 && activeIndex < tabCount
  );
}

function tabsVariantCombinations() {
  const combinations = [];

  for (const count of TABS_COUNTS) {
    for (const active of TABS_ACTIVE) {
      if (!tabsActiveIsValidForCount(active, count)) continue;
      for (const state of TABS_STATES) {
        combinations.push({ count, active, state });
      }
    }
  }

  return combinations;
}

function tabsTriggerConfig(state, isActive) {
  if (state === "Disabled") {
    return {
      background: isActive ? "Colors/background/200" : null,
      foreground: "Colors/foreground/500",
      backgroundFallback: "#C7CAD1",
      foregroundFallback: "#747B8B",
    };
  }

  return {
    background: isActive ? "Colors/background/0" : null,
    foreground: isActive ? "Colors/foreground/0" : "Colors/foreground/400",
    backgroundFallback: "#FFFFFF",
    foregroundFallback: isActive ? "#000000" : "#5D626F",
  };
}

function buttonConfig(variant, state) {
  const tokenState = state === "Disabled" ? "disabled" : "idle";
  const configs = {
    Default: {
      background: `Primary Buttons/themed/button/background/${tokenState}`,
      foreground: `Primary Buttons/themed/button/foreground/content/${tokenState}`,
      backgroundFallback: state === "Disabled" ? "#2E3138" : "#0D44C2",
      foregroundFallback: state === "Disabled" ? "#464A53" : "#FFFFFF",
    },
    Destructive: {
      background: `Primary Buttons/danger/button/background/${tokenState}`,
      foreground: `Primary Buttons/danger/button/foreground/content/${tokenState}`,
      backgroundFallback: state === "Disabled" ? "#2E3138" : "#B01736",
      foregroundFallback: state === "Disabled" ? "#464A53" : "#FFFFFF",
    },
    Secondary: {
      background: "Colors/background/200",
      foreground: "Colors/foreground/0",
      backgroundFallback: "#C7CAD1",
      foregroundFallback: "#000000",
    },
    Outline: {
      background: "Surface/0",
      foreground: `Secondary Buttons/themed/button/foreground/content/${tokenState}`,
      stroke: `Secondary Buttons/themed/button/foreground/content/${tokenState}`,
      backgroundFallback: "#FFFFFF",
      foregroundFallback: state === "Disabled" ? "#464A53" : "#0D44C2",
      strokeFallback: state === "Disabled" ? "#464A53" : "#0D44C2",
    },
    Ghost: {
      foreground: `Secondary Buttons/themed/button/foreground/content/${tokenState}`,
      foregroundFallback: state === "Disabled" ? "#464A53" : "#0D44C2",
    },
    Link: {
      foreground: `Secondary Buttons/themed/button/foreground/content/${tokenState}`,
      foregroundFallback: state === "Disabled" ? "#464A53" : "#0D44C2",
    },
    Glass: {
      background: "Colors/transparent/inverted/10",
      foreground: "Colors/foreground/0",
      stroke: "Border/bevel/top",
      backgroundFallback: "#FCFCFD1A",
      foregroundFallback: "#000000",
      strokeFallback: "#FFFFFF80",
    },
  };

  return configs[variant] || configs.Default;
}

function toggleButtonConfig(variant, state) {
  if (state === "Disabled") {
    return {
      background: "Colors/background/100",
      foreground: "Colors/foreground/500",
      stroke: variant === "Outline" ? "Colors/background/200" : null,
      backgroundFallback: "#E4E6EA",
      foregroundFallback: "#747B8B",
      strokeFallback: "#C7CAD1",
    };
  }

  if (state === "Pressed") {
    return {
      background: "Colors/theme/600",
      foreground: "Colors/foreground/1000",
      stroke: variant === "Outline" ? "Colors/theme/600" : null,
      backgroundFallback: "#1051E8",
      foregroundFallback: "#FFFFFF",
      strokeFallback: "#1051E8",
    };
  }

  if (variant === "Outline") {
    return {
      background: "Surface/0",
      foreground: "Colors/foreground/0",
      stroke: "Colors/background/200",
      backgroundFallback: "#FFFFFF",
      foregroundFallback: "#000000",
      strokeFallback: "#C7CAD1",
    };
  }

  return {
    background: "Colors/background/100",
    foreground: "Colors/foreground/0",
    backgroundFallback: "#E4E6EA",
    foregroundFallback: "#000000",
  };
}

function splitButtonConfig(variant, state) {
  return buttonConfig(variant, state === "Focus" ? "Default" : state);
}

function floatingActionButtonConfig(variant, state) {
  return buttonConfig(variant, state === "Focus" ? "Default" : state);
}

function badgeConfig(variant) {
  const configs = {
    Default: {
      background: "Colors/theme/600",
      foreground: "Colors/foreground/1000",
      backgroundFallback: "#1051E8",
      foregroundFallback: "#FFFFFF",
    },
    Destructive: {
      background: "Colors/emotional/danger/600",
      foreground: "Colors/foreground/1000",
      backgroundFallback: "#D41C42",
      foregroundFallback: "#FFFFFF",
    },
    Secondary: {
      background: "Colors/background/200",
      foreground: "Colors/foreground/0",
      backgroundFallback: "#C7CAD1",
      foregroundFallback: "#000000",
    },
    Outline: {
      background: "Surface/0",
      foreground: "Colors/foreground/0",
      stroke: "Colors/background/200",
      backgroundFallback: "#FFFFFF",
      foregroundFallback: "#000000",
      strokeFallback: "#C7CAD1",
    },
    Ghost: {
      foreground: "Colors/foreground/0",
      foregroundFallback: "#000000",
    },
    Link: {
      foreground: "Colors/theme/600",
      foregroundFallback: "#1051E8",
    },
  };

  return configs[variant] || configs.Default;
}

function chipConfig(variant, state) {
  if (state === "Disabled") {
    return {
      background: "Colors/background/100",
      foreground: "Colors/foreground/500",
      stroke: "Colors/background/200",
      backgroundFallback: "#E4E6EA",
      foregroundFallback: "#747B8B",
      strokeFallback: "#C7CAD1",
    };
  }

  if (state === "Selected") {
    if (variant === "Destructive") {
      return {
        background: "Colors/emotional/danger/600",
        foreground: "Colors/foreground/1000",
        stroke: "Colors/emotional/danger/600",
        backgroundFallback: "#D41C42",
        foregroundFallback: "#FFFFFF",
        strokeFallback: "#D41C42",
      };
    }

    return {
      background: "Colors/theme/600",
      foreground: "Colors/foreground/1000",
      stroke: "Colors/theme/600",
      backgroundFallback: "#1051E8",
      foregroundFallback: "#FFFFFF",
      strokeFallback: "#1051E8",
    };
  }

  if (variant === "Brand") {
    return {
      background: "Surface/0",
      foreground: "Colors/theme/600",
      stroke: "Colors/theme/600",
      backgroundFallback: "#FFFFFF",
      foregroundFallback: "#1051E8",
      strokeFallback: "#1051E8",
    };
  }

  if (variant === "Destructive") {
    return {
      background: "Surface/0",
      foreground: "Colors/emotional/danger/600",
      stroke: "Colors/emotional/danger/600",
      backgroundFallback: "#FFFFFF",
      foregroundFallback: "#D41C42",
      strokeFallback: "#D41C42",
    };
  }

  return {
    background: "Surface/0",
    foreground: "Colors/foreground/0",
    stroke: "Colors/background/200",
    backgroundFallback: "#FFFFFF",
    foregroundFallback: "#000000",
    strokeFallback: "#C7CAD1",
  };
}

function segmentedControlMetrics(size) {
  if (size === "Small") {
    return {
      size,
      width: 300,
      height: 52,
      itemWidth: 97.333,
      itemHeight: 44,
      padding: 4,
      itemPaddingX: 12,
      radius: 16,
      itemRadius: 12,
      fontSize: 12,
      lineHeight: 16,
      heightToken: "SegmentedControl/height/small",
      itemHeightToken: "SegmentedControl/item-height/small",
      itemPaddingXToken: "SegmentedControl/item-padding/x/small",
      fontSizeToken: "SegmentedControl/label/font-size/small",
      lineHeightToken: "SegmentedControl/label/line-height/small",
    };
  }

  if (size === "Large") {
    return {
      size,
      width: 420,
      height: 56,
      itemWidth: 137.333,
      itemHeight: 48,
      padding: 4,
      itemPaddingX: 20,
      radius: 16,
      itemRadius: 12,
      fontSize: 14,
      lineHeight: 20,
      heightToken: "SegmentedControl/height/large",
      itemHeightToken: "SegmentedControl/item-height/large",
      itemPaddingXToken: "SegmentedControl/item-padding/x/large",
      fontSizeToken: "SegmentedControl/label/font-size/large",
      lineHeightToken: "SegmentedControl/label/line-height/large",
    };
  }

  return {
    size,
    width: 360,
    height: 52,
    itemWidth: 117.333,
    itemHeight: 44,
    padding: 4,
    itemPaddingX: 16,
    radius: 16,
    itemRadius: 12,
    fontSize: 14,
    lineHeight: 20,
    heightToken: "SegmentedControl/height/default",
    itemHeightToken: "SegmentedControl/item-height/default",
    itemPaddingXToken: "SegmentedControl/item-padding/x/default",
    fontSizeToken: "SegmentedControl/label/font-size/default",
    lineHeightToken: "SegmentedControl/label/line-height/default",
  };
}

function segmentedControlActiveIndex(active) {
  const index = SEGMENTED_CONTROL_ACTIVE.indexOf(active);
  return index === -1 ? 0 : index;
}

function segmentedControlConfig(state) {
  if (state === "Disabled") {
    return {
      background: "Colors/background/100",
      stroke: "Colors/background/200",
      backgroundFallback: "#E4E6EA",
      strokeFallback: "#C7CAD1",
    };
  }

  if (state === "Error") {
    return {
      background: "Colors/background/100",
      stroke: "Colors/emotional/danger/600",
      backgroundFallback: "#E4E6EA",
      strokeFallback: "#D41C42",
    };
  }

  return {
    background: "Colors/background/100",
    stroke: "Colors/background/200",
    backgroundFallback: "#E4E6EA",
    strokeFallback: "#C7CAD1",
  };
}

function segmentedControlLabelColor({ disabled, error, selected }) {
  if (disabled) {
    return {
      token: "Colors/foreground/500",
      fallback: "#747B8B",
    };
  }

  if (error && selected) {
    return {
      token: "Colors/emotional/danger/600",
      fallback: "#D41C42",
    };
  }

  if (selected) {
    return {
      token: "Colors/foreground/0",
      fallback: "#000000",
    };
  }

  return {
    token: "Colors/foreground/400",
    fallback: "#5D626F",
  };
}

function counterConfig(tone) {
  const configs = {
    Neutral: {
      background: "Colors/background/200",
      foreground: "Colors/foreground/0",
      backgroundFallback: "#C7CAD1",
      foregroundFallback: "#000000",
    },
    Brand: {
      background: "Colors/theme/600",
      foreground: "Colors/foreground/1000",
      backgroundFallback: "#1051E8",
      foregroundFallback: "#FFFFFF",
    },
    Destructive: {
      background: "Colors/emotional/danger/600",
      foreground: "Colors/foreground/1000",
      backgroundFallback: "#D41C42",
      foregroundFallback: "#FFFFFF",
    },
    Inverse: {
      background: "Surface/0",
      foreground: "Colors/foreground/0",
      backgroundFallback: "#FFFFFF",
      foregroundFallback: "#000000",
    },
  };

  return configs[tone] || configs.Neutral;
}

function checkboxConfig(checked, state) {
  const isChecked = checked === "Checked";
  const isDisabled = state === "Disabled";
  const isError = state === "Error";

  if (isDisabled) {
    return {
      controlFill: isChecked ? "Colors/background/200" : null,
      controlStroke: "Colors/foreground/500",
      mark: "Colors/foreground/500",
      label: "Colors/foreground/500",
      controlFillFallback: "#C7CAD1",
      controlStrokeFallback: "#747B8B",
      markFallback: "#747B8B",
      labelFallback: "#747B8B",
    };
  }

  return {
    controlFill: isChecked ? "Colors/theme/500" : null,
    controlStroke: isError
      ? "Colors/emotional/danger/600"
      : isChecked
        ? "Colors/theme/500"
        : "Colors/foreground/500",
    mark: "Colors/foreground/1000",
    label: isError ? "Colors/emotional/danger/600" : "Colors/foreground/0",
    controlFillFallback: "#135BEC",
    controlStrokeFallback: isError
      ? "#D41C42"
      : isChecked
        ? "#135BEC"
        : "#747B8B",
    markFallback: "#FFFFFF",
    labelFallback: isError ? "#D41C42" : "#000000",
  };
}

function radioConfig(checked, state) {
  const isChecked = checked === "Checked";
  const isDisabled = state === "Disabled";
  const isError = state === "Error";

  if (isDisabled) {
    return {
      controlStroke: "Colors/foreground/500",
      dot: "Colors/foreground/500",
      label: "Colors/foreground/500",
      controlStrokeFallback: "#747B8B",
      dotFallback: "#747B8B",
      labelFallback: "#747B8B",
    };
  }

  return {
    controlStroke: isError
      ? "Colors/emotional/danger/600"
      : isChecked
        ? "Colors/theme/500"
        : "Colors/foreground/500",
    dot: isError ? "Colors/emotional/danger/600" : "Colors/theme/500",
    label: isError ? "Colors/emotional/danger/600" : "Colors/foreground/0",
    controlStrokeFallback: isError
      ? "#D41C42"
      : isChecked
        ? "#135BEC"
        : "#747B8B",
    dotFallback: isError ? "#D41C42" : "#135BEC",
    labelFallback: isError ? "#D41C42" : "#000000",
  };
}

function switchConfig(checked, state) {
  const isChecked = checked === "Checked";
  const isDisabled = state === "Disabled";
  const isError = state === "Error";

  if (isDisabled) {
    return {
      trackFill: "Colors/foreground/500",
      trackStroke: "Colors/foreground/500",
      thumbFill: "Colors/background/0",
      label: "Colors/foreground/500",
      trackFillFallback: "#747B8B",
      trackStrokeFallback: "#747B8B",
      thumbFillFallback: "#FFFFFF",
      labelFallback: "#747B8B",
    };
  }

  if (isError) {
    return {
      trackFill: isChecked
        ? "Colors/emotional/danger/600"
        : "Colors/foreground/500",
      trackStroke: "Colors/emotional/danger/600",
      thumbFill: "Colors/background/0",
      label: "Colors/emotional/danger/600",
      trackFillFallback: isChecked ? "#D41C42" : "#747B8B",
      trackStrokeFallback: "#D41C42",
      thumbFillFallback: "#FFFFFF",
      labelFallback: "#D41C42",
    };
  }

  return {
    trackFill: isChecked ? "Colors/theme/500" : "Colors/foreground/500",
    trackStroke: isChecked ? "Colors/theme/500" : "Colors/foreground/500",
    thumbFill: "Colors/background/0",
    label: "Colors/foreground/0",
    trackFillFallback: isChecked ? "#135BEC" : "#747B8B",
    trackStrokeFallback: isChecked ? "#135BEC" : "#747B8B",
    thumbFillFallback: "#FFFFFF",
    labelFallback: "#000000",
  };
}

function inputConfig(state, status) {
  const isDisabled = state === "Disabled";
  const isReadonly = state === "Readonly";
  const isFocus = state === "Focus";
  const isError = status === "Error";
  const isWarning = status === "Warning";
  const isSuccess = status === "Success";

  if (isDisabled) {
    return {
      fieldFill: "Colors/background/100",
      fieldStroke: "Colors/foreground/500",
      label: "Colors/foreground/500",
      placeholder: "Colors/foreground/500",
      helper: "Colors/foreground/500",
      fieldFillFallback: "#E3E4E8",
      fieldStrokeFallback: "#747B8B",
      labelFallback: "#747B8B",
      placeholderFallback: "#747B8B",
      helperFallback: "#747B8B",
    };
  }

  if (isError || isWarning || isSuccess) {
    const tone = isError
      ? {
          token: "Colors/emotional/danger/600",
          fallback: "#D41C42",
        }
      : isWarning
        ? {
            token: "Colors/emotional/alert/900",
            fallback: "#744D03",
          }
        : {
            token: "Colors/emotional/success/900",
            fallback: "#14653D",
          };

    return {
      fieldFill: isReadonly ? "Colors/background/100" : "Colors/background/0",
      fieldStroke: tone.token,
      label: tone.token,
      placeholder: "Colors/foreground/400",
      helper: tone.token,
      fieldFillFallback: isReadonly ? "#E3E4E8" : "#FFFFFF",
      fieldStrokeFallback: tone.fallback,
      labelFallback: tone.fallback,
      placeholderFallback: "#5D626F",
      helperFallback: tone.fallback,
    };
  }

  return {
    fieldFill: isReadonly ? "Colors/background/100" : "Colors/background/0",
    fieldStroke: isFocus ? "Colors/theme/500" : "Colors/foreground/500",
    label: "Colors/foreground/0",
    placeholder: "Colors/foreground/400",
    helper: "Colors/foreground/400",
    fieldFillFallback: isReadonly ? "#E3E4E8" : "#FFFFFF",
    fieldStrokeFallback: isFocus ? "#135BEC" : "#747B8B",
    labelFallback: "#000000",
    placeholderFallback: "#5D626F",
    helperFallback: "#5D626F",
  };
}

function sliderConfig(state, status) {
  const isDisabled = state === "Disabled";
  const isError = status === "Error";

  if (isDisabled) {
    return {
      trackFill: "Colors/background/200",
      trackStroke: "Colors/foreground/500",
      rangeFill: "Colors/foreground/500",
      thumbFill: "Colors/background/0",
      thumbStroke: "Colors/foreground/500",
      label: "Colors/foreground/500",
      trackFillFallback: "#C7CAD1",
      trackStrokeFallback: "#747B8B",
      rangeFillFallback: "#747B8B",
      thumbFillFallback: "#FFFFFF",
      thumbStrokeFallback: "#747B8B",
      labelFallback: "#747B8B",
    };
  }

  if (isError) {
    return {
      trackFill: "Colors/background/200",
      trackStroke: "Colors/foreground/500",
      rangeFill: "Colors/emotional/danger/600",
      thumbFill: "Colors/background/0",
      thumbStroke: "Colors/emotional/danger/600",
      label: "Colors/emotional/danger/600",
      trackFillFallback: "#C7CAD1",
      trackStrokeFallback: "#747B8B",
      rangeFillFallback: "#D41C42",
      thumbFillFallback: "#FFFFFF",
      thumbStrokeFallback: "#D41C42",
      labelFallback: "#D41C42",
    };
  }

  return {
    trackFill: "Colors/background/200",
    trackStroke: "Colors/foreground/500",
    rangeFill: "Colors/theme/600",
    thumbFill: "Colors/background/0",
    thumbStroke: "Colors/theme/600",
    label: "Colors/foreground/0",
    trackFillFallback: "#C7CAD1",
    trackStrokeFallback: "#747B8B",
    rangeFillFallback: "#1051E8",
    thumbFillFallback: "#FFFFFF",
    thumbStrokeFallback: "#1051E8",
    labelFallback: "#000000",
  };
}

function progressConfig(_value) {
  return {
    trackFill: "Colors/background/200",
    trackStroke: "Colors/foreground/500",
    rangeFill: "Colors/theme/600",
    trackFillFallback: "#C7CAD1",
    trackStrokeFallback: "#747B8B",
    rangeFillFallback: "#1051E8",
  };
}

function spinnerMetrics(size) {
  if (size === "Small") {
    return {
      size: 16,
      sizeToken: "Spinner/size/small",
    };
  }

  if (size === "Large") {
    return {
      size: 32,
      sizeToken: "Spinner/size/large",
    };
  }

  if (size === "XLarge") {
    return {
      size: 48,
      sizeToken: "Spinner/size/xlarge",
    };
  }

  return {
    size: 24,
    sizeToken: "Spinner/size/medium",
  };
}

function avatarConfig(content) {
  if (content === "Image") {
    return {
      background: "Colors/theme/600",
      foreground: "Colors/foreground/1000",
      backgroundFallback: "#1051E8",
      foregroundFallback: "#FFFFFF",
    };
  }

  return {
    background: "Colors/background/200",
    foreground: "Colors/foreground/0",
    backgroundFallback: "#C7CAD1",
    foregroundFallback: "#000000",
  };
}

function alertConfig(variant) {
  const toneConfigs = {
    Default: {
      icon: "info-circle",
      background: "Surface/0",
      stroke: "Colors/foreground/500",
      foreground: "Colors/foreground/0",
      backgroundFallback: "#FFFFFF",
      strokeFallback: "#747B8B",
      foregroundFallback: "#000000",
    },
    Destructive: {
      icon: "alert-circle",
      background: "Surface/0",
      stroke: "Colors/emotional/danger/600",
      foreground: "Colors/emotional/danger/600",
      backgroundFallback: "#FFFFFF",
      strokeFallback: "#D41C42",
      foregroundFallback: "#D41C42",
    },
    Success: {
      icon: "check",
      background: "Surface/0",
      stroke: "Colors/emotional/success/900",
      foreground: "Colors/emotional/success/900",
      backgroundFallback: "#FFFFFF",
      strokeFallback: "#14653D",
      foregroundFallback: "#14653D",
    },
    Warning: {
      icon: "alert-triangle",
      background: "Surface/0",
      stroke: "Colors/emotional/alert/900",
      foreground: "Colors/emotional/alert/900",
      backgroundFallback: "#FFFFFF",
      strokeFallback: "#744D03",
      foregroundFallback: "#744D03",
    },
    Info: {
      icon: "info-circle",
      background: "Surface/0",
      stroke: "Colors/theme/600",
      foreground: "Colors/theme/600",
      backgroundFallback: "#FFFFFF",
      strokeFallback: "#1051E8",
      foregroundFallback: "#1051E8",
    },
  };

  return toneConfigs[variant] || toneConfigs.Default;
}

function alertTitleForVariant(variant) {
  if (variant === "Destructive") return "Error";
  if (variant === "Success") return "Success";
  if (variant === "Warning") return "Warning";
  if (variant === "Info") return "Info";
  return "Heads up";
}

function alertDescriptionForVariant(variant) {
  if (variant === "Destructive") return "Something needs your attention.";
  if (variant === "Success") return "The operation completed successfully.";
  if (variant === "Warning") return "Review this before continuing.";
  if (variant === "Info") return "Helpful context is available here.";
  return "This status message needs attention.";
}

function clearComponentSetContainerFill(componentSet) {
  componentSet.fills = [];
  componentSet.clipsContent = false;
}

function createSpinnerGlyph(
  variableName,
  fallback,
  variableByName,
  stats,
  size,
  sizeToken,
) {
  const glyph = figma.createEllipse();
  glyph.name = "Loading Indicator";
  const spinnerSize = size || 14;
  glyph.resize(spinnerSize, spinnerSize);
  if (sizeToken) {
    bindSizeVariables(glyph, sizeToken, sizeToken, variableByName, stats);
  }
  glyph.fills = [];
  glyph.strokes = [
    paintFromVariable(variableName, fallback, variableByName, stats),
  ];
  glyph.strokeWeight = 2;
  glyph.dashPattern = [8, 4];
  return glyph;
}

function paintFromVariable(name, fallback, variableByName, stats) {
  const color = parseColor(fallback);
  const paint = {
    type: "SOLID",
    color: { r: color.r, g: color.g, b: color.b },
    opacity: color.a,
  };
  const variable = variableByName.get(name);

  if (!variable) {
    stats.warnings.push(`Missing variable "${name}", used ${fallback}.`);
    return paint;
  }

  if (figma.variables.setBoundVariableForPaint) {
    try {
      return figma.variables.setBoundVariableForPaint(paint, "color", variable);
    } catch (error) {
      stats.warnings.push(`Could not bind "${name}" (${messageFor(error)}).`);
    }
  }

  return paint;
}

function paintFromVariableWithOpacity(
  name,
  fallback,
  opacity,
  variableByName,
  stats,
) {
  const paint = paintFromVariable(name, fallback, variableByName, stats);
  const tinted = clonePaint(paint);
  tinted.opacity = opacity;
  return tinted;
}

function nodeIdForUrl(id) {
  return id.replace(/:/g, "-");
}
